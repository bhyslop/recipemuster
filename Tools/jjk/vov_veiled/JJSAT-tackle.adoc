//axvd_sheaf axd_aspirant
This is an aspirant portion of the {jjs0_imprimatur}: not yet binding — do not rely on it, decide from it, or cite the names it mints until it is infused into the codex.

= Tackle — Filesets and Their Interrelating Rulesets (Aspirant Nucleation)

[IMPORTANT]
====
*Aspirant nucleation* (AXLA `axd_aspirant`): not yet binding.
A feature-definition space under active mulling — to be sophisticated and revised until ready for
development, then infused into this codex or retired.
A reader must not rely on its content; released code must not cite identifiers it mints.
The word *tackle* carried a maximal meaning in the retired V4 execution workup, now recast in
the breeze aspirant sheaf (JJSAB); the one-word-one-concept resolution is stated below.
The exploratory record — the inclusion-polarity argument and the SlickEdit specimen autopsy —
is `Memos/memo-20260707-tackle-polarity-and-graph.md` (provenance, never authority).
====

== What tackle is

Tackle is a strong way to identify and curate sets of files, and to declare how rulesets
interrelating those filesets apply — the two-part charter this sheaf hangs from:

* *Filesets* — the identified, curated sets:
  "these globs are BCG bash, those are RCG rust, that subtree is theurge, that one is ifrit."
  Identification is the claim rules; curation is the coverage check that keeps every claim
  deliberate and every gap loud (Scope semantics below).
* *Interrelating rulesets* — the declared relations between filesets, and the disciplines that
  govern the relations themselves:
  a spec family balances an implementation, a test domain exercises a subject domain, a guide
  governs a fileset (The association graph below).

One codebase holds several discipline domains at once (ACG's own observation; the worked
census below), so the binding is file-grain, not repo-grain — and it is the binding that
spares the operator from writing "write BCG-compliant code for all the bash you touch" into
every chat.

*Tackle already exists in the home repo, denormalized.*
The CLAUDE.md `@`-include tree, the test-suite table ("regime changes → reveille +
regime-poison"), the hand-maintained acronym maps, the matricula's file-selection allowlist
(VOSMM Tier 0), and the retired SlickEdit workspace files (`Tools/vslf-rbw/`) are each a
hand-maintained *projection* of a scope→discipline→association structure that has no single
home.
Tackle is the normalization of that standing structure, not new machinery;
every consumer becomes a projection that re-derives instead of drifting.

== The worked census (RB home repo)

The example the concept must serve, sketched at domain grain (rows illustrative, not a
binding inventory):

[cols="1,2,2"]
|===
| Domain | Scope | Governed by

| JJ specs | `Tools/jjk/vov_veiled/**/*.adoc` | MCM/AXLA authoring + JJS0 doctrine
| RB specs | `Tools/rbk/vov_veiled/**/*.adoc` | MCM/AXLA authoring + RBS0
| JJ rust | JJ crate `**/*.rs` | RCG
| JJ tests | JJ crate test modules | RCG + test conduct
| Theurge | `Tools/rbk/rbtd/**/*.rs` | RCG + RBSTC + theurge-ifrit context
| Ifrit | `rbev-vessels/common-ifrit-context/` | RBSIP posture
| BUK bash | `Tools/buk/**/*.sh` | BCG
| RB bash | `Tools/rbk/*.sh` | BCG + RBK conduct
| Cloud steps | `Tools/rbk/rbgj*/` | CBG
| Jailer sh | sentry/pentacle vessel scripts | JDG
| Tabtargets | `tt/*.sh` | BUS0 dispatch discipline
| Launchers | `rbmm_moorings/rbml_launchers/*.sh` | BUK launcher discipline
| Diagrams | `diagrams/*.puml` | PCG
| Memos | `Memos/**/*.md` | provenance-never-authority
| Generated | `rbtdgc_consts.rs`, tabtarget-context.md | regenerate-never-hand-edit
|===

Two structural discoveries the census forces:

* *The operator thinks domains-first.*
  The census speaks in named domains ("theurge tests", "ifrit tests"), never in patterns;
  globs are merely how a domain grabs its files.
  So the binding factors: globs → domain → everything else — never direct glob→discipline.
* *Disciplines attach to edges, not only nodes.*
  ACG is intelligible only as an edge discipline: it governs the *relation* between RB bash
  and RB specification (reference-the-home, rivet citations), not either fileset alone.
  "Theurge tests the crucible" and "JJS specs specify JJ rust" are further typed relations.
  A flat glob→discipline table cannot express any of these.

Humble disciplines are first-class rows: "generated — regenerate, never hand-edit" and
"provenance, never authority" are genuine governing statements, which is what lets coverage
close without a catch-all bucket.

The table above is a frozen illustration and deliberately does not grow;
the *living* census — more domains, the fuzzy edges, and the full arrows inventory — is
`Memos/memo-20260707-rbm-domain-census.md` (provenance), the concept and its worked example
flying in formation.

== A tackle is countable

*A tackle is one domain binding* — a fileset plus the governance it carries;
the *tackle set* is an upstream's table of them.
The breeze sheaf's usage ("blaze — owned by *a* tackle") agrees, and V4's countable
resource-bundle sense maps onto the same node.
The aggregate needs no minted name of its own:
plain prose — "the tackle set", "the upstream's tackles" — does the work, the tackle table
beside the pedigree is the aggregate's home, and a mint waits until the aggregate itself
acquires behavior worth naming.

== Candidate vocabulary (breeze idiom: mulling words, gates at graduation)

Slots identified for the subparts; words elected only where the concept has stopped moving.
Every word below is candidate only, re-ratifying under MCM Lapidary at graduation;
disqualifications are recorded so they are not re-tried.
Governing test: MCM Lapidary's mint-follows-API rule (cited, never restated).

[cols="1,3,2"]
|===
| Slot | Meaning | Word status

| a tackle
| The countable domain binding: one fileset + its governance members.
| Settled; the word already exists — no new mint.

| tackle name
| The row key and spoken handle: a plain word used in audit and error speech
  ("this file is claimed by both rb-bash and cloud-steps").
| Settled as a name, not an identity: resolution is files-first and no durable store
  references a tackle, so there is no minted insignia, no seed, no glyph —
  rename is a one-file act.
  Identity re-enters only with the first durable referencer
  (hames at their normalization act; per-heat overlays), and only for that channel.

| claim rule
| The intensional membership rule of one tackle: an *anchored* subtree × shape conjunction
  (e.g. `Tools/rbk/rbtd/**` × `*.rs`).
  Distinct from the derived list by design.
| The member is `jjottn_claims` — the doctrine's own word.
  remuda released: it mutated in operator recall ("Remula"), failing the spoken test.
  The rule/extension distinction stays prose-grade ("the tackle's claims" vs "the files
  they yield"); no second word is spent.

| derived roster
| The extension: the concrete file list a claim rule yields against a tree state.
  Always derived, never authored (Scope semantics: intensional table).
| An anonymous return value unless the API proves otherwise (mint follows API).

| interrelation
| One declared, typed edge between two tackles (specifies, tests, crosscuts), able to carry
  its own governing discipline.
| ELECTED: *hame* (the collar-piece traces attach to) — deferred whole with the association
  graph: the MVP carries spec-binding as node members (flattened edges), and hames arrive
  with the first consumer of the relation itself.
  Alternate held: swingletree.
  Disqualified: trace (trodden: stack/tracing), yoke (RBK-minted, RBSDY), harness (trodden).

| governance members
| What a tackle binds: typed members — guides, conduct, specs, balances, and the check
  commands — each with its own consumer and its own value type (documents vs commands).
| trappings released, never infused: the umbrella flattened four natures into one list a
  consumer could only discriminate by reading the documents — the linguistic inference the
  design forbids.
  Growth is member-wise, never kinds threaded through a generic list (schema section).
  A genuine contradiction between co-governing documents is surfaced to the operator, never
  auto-resolved.

| blaze
| A role word defined once per table in the registries (`₿bash-guide`), bound there to a
  concrete document or set, referenced from tackle members, dockets, and gaits;
  ₿-sigiled in wire keys and at reference sites.
| The breeze workup's blaze, given its definition home.
  A declared variable, never an identity: a role outlives any particular binding, and
  resolution runs against whichever table is in play — which is what keeps gaits portable
  across governed bodies and keeps a crossbred row correct on arrival.
  Keys are role words, never document acronyms (`₿bash-guide`, not the guide's name).

| verdicts
| The three mechanical answers for any path: claimed / unclaimed-inside-ambit /
  beyond-ambit.
| Deliberately unnamed — the middle verdict rhymes exactly with VOSMM's `vosme_estray`
  (unclaimed-but-ours, surfaced as product); whether that concept generalizes across corpora
  or needs a sibling word is a Lapidary question held open.

| governed boundary
| The per-upstream declared boundary within which coverage is owed (Scope semantics).
| ELECTED: *ambit* — `jjotrn_ambit`, the extent of jurisdiction.
  Rejected: *palisade* (the ROE Palisade names jurisdiction-impossible — cannot edit —
  while the ambit is jurisdiction-not-yet-extended: can edit, haven't legislated) and
  *pale* (already minted RB-side as the upstream trust boundary, the Pale).
|===

== Scope semantics

The allowlist/denylist question dissolves once *how a set is defined* (polarity) is
separated from *what happens to a file nothing claims* (the default).
The position, argued in full in the memo:

* *Claims are positive, at domain grain.*
  A claim rule is an anchored subtree × shape conjunction — wildcards retained as the
  intension mechanism (`**` subtree recursion, `*.rs` shape globs): what is banned is the
  *unanchored* floating pattern, and what is audited is maximal width (below).
  New files and new subdirectories inside a claimed domain auto-fall-in — negative
  inclusion's auto-coverage exactly where it is correct, because the domain owner chose the
  criterion once.
  A *new domain* is not automatic, and that is right: it deserves a founding act, the
  pedigree's own deliberate-ceremony posture one level down.
  With domain-grain claims, *placing a file is affiliating it* — the location decision that
  already happens is the discipline decision.
* *There is no default.*
  No catch-all row; totality is reached because humble disciplines (generated, inert asset,
  provenance-only) are first-class rows, never because a bucket swallowed the remainder.
* *Overlap means co-membership.*
  A file may sit in two domains at once — the standing example: workstation bash that with
  slight modification also runs in the cloud, BCG and CBG both applying.
  Consumers take the union of governance members; no precedence machinery exists; coverage
  reads *at-least-one* claim.
  Tackle wins by being simple rather than over-fitted.
  Accidental overlap is guarded by claims written at honest grain (the census's own rows are
  naturally disjoint) and by the audit report showing every overlap — never hand-mirrored
  exclusions (the SlickEdit disease, memo §autopsy).
  A supplanting relation ("this specific claim excludes that general one"), if ever truly
  needed, is a future deliberate addition.
* *The failure asymmetry is the decisive argument.*
  Under-coverage (positive's failure) is mechanically detectable: universe minus claims is a
  set subtraction any validator runs, no judgment needed.
  Mis-affiliation (negative's failure) is undetectable by construction: the table *is* the
  definition of "right", so the wrong discipline surfaces only when it misbehaves — for
  security-posture bindings, discovery by incident.
  Given teeth, choose the failure mode that can be automated away.
  (Prior art agrees: firewall default-deny, gitattributes, CODEOWNERS — see memo.)
* *Recursion is preserved in the claim; the hazard is anchor width.*
  Root-anchored recursive claims have maximal swallow radius (the SlickEdit
  launcher-swallow, memo §autopsy); deep-anchored ones are tame.
  Claims are therefore always path-anchored, and a root-anchored recursive claim or a
  bare-`*` shape is an *audit flag*, not a prohibition.
* *The table is intensional — rules, never lists; every fileset is derived.*
  The two query directions scale differently and the design leans on that:
  path→discipline is O(rules) per path with no tree walk (the saddle-time direction — how
  gitattributes serves million-file repos);
  the census direction (fileset materialization, coverage audit) walks the ambit only, is
  batch by nature, and is where caching lands, keyed (table version, tree state).
  A materialized roster is derived, per-clone, disposable state: it lives in station-local
  scratch, *never the studbook* — the same law as worktree paths (regional record,
  station-local fact).
  Caching, if the trees go huge, is the deliberate supersession VOSMM's transience invariant
  (VOr_k3p) already anticipates — never drift.
* *Coverage is owed within the ambit; three verdicts.*
  Per governed body of work, its tackle table declares the ambit (for the home repo: the
  whole git-tracked tree; for a large vanilla upstream: the subtrees JJ works).
  Any path then has one of three mechanical verdicts:
  *claimed* (some tackle governs it);
  *unclaimed inside the ambit* (the defect — loud, surfaced by the coverage subtraction);
  *beyond the ambit* (a known-unknown, not a defect, but a session touching one is told so).
  The same boundary bounds census cost: the coverage walk prunes beyond-ambit subtrees
  entirely — governance honesty and traversal cost turn out to be one boundary.
* *Dead rows are flagged.*
  A claim rule matching zero files is rot (the specimen's `RBM-recipes` row); the census
  reports it (lean: notice, not error).

== The association graph

The scope containment of claim rules is a tree;
the declared interrelations make it a graph.
A hame is a typed edge between two tackles — *specifies*, *tests*, *crosscuts* are the
census-observed types; the taxonomy stays open — and an edge can carry its own governing
discipline:
ACG is the type specimen, governing the code↔spec citation relation (reference-the-home,
rivet residue) rather than either side alone.

*Declared, never deduced.*
Every edge is authored — the pedigree-ceremony posture again — and traversal is mechanical:
no similarity inference, no embedding neighborhood, no linguistic guess (the VOSMM
mechanical-never-linguistic ethos, VOr_m7w, applied to context provisioning).
This is what makes the graph trustworthy enough to *govern* rather than merely suggest.

The consumer horizon (vision, not this sheaf's design):
a frontier-tier planning session judges "this implementation needs balancing against this
spec" *once*, captures the judgment as a declared edge, and every later provisioning replays
it mechanically — school walks the graph to queue exactly the context a weaker session needs
to execute cheaply.
A weak model cannot discover missing context; the declared graph is the precondition for
handing work down-tier at all.
The same freeze pattern as the grimoire: authored once with full wisdom, read forever by
exact match.
Execution-side design (gait affinities, blaze resolution, exclusivity) remains joint work
with the breeze sheaf (JJSAB, Reconciliation section) and is deferred below.

*Boxes before arrows* (sequencing comfort): the concept model holds nodes and edges, but the
build sequence need not — an MVP of nodes alone (claims → governance members) is complete and
useful on its own, with hames arriving when their first consumer (school, or an ACG-citation
lint) actually lands.
The MVP's document members are that ruling enacted: each specs entry — and each
edge-discipline guide, ACG the type specimen — is a *flattened edge*, the relation collapsed
onto its node because the only MVP consumer is provisioning, and the session that needs the
document is the one writing the code.
What flattening drops is exactly the pairing the relation-consumers need (that ACG governs
the citations *into that spec set*), so those members normalize into hames at the same act
that lands the first consumer; ACG is the first migrant.
The grammar stays tiny either way: a node is a row, an edge is a tagged pair of rows;
sophistication lives in what consumers do with the tags, app-side, per the substrate split.

== Consumers (all projections)

* *Session provisioning* (farrier sheaf, launch-time): the dispatcher reads the pedigree's
  tackle set and provisions context at file grain, replacing repo-grain `@`-includes.
  Honing is prediction and starvation fails silent, so the farrier sheaf's standing repairs
  ride along: an invariant conduct core ships unhoned, and a pull door turns starvation into
  one extra round-trip.
* *The matricula* (VOSMM Tier 0): its file-selection allowlist is tackle's first working
  consumer — shaped schema-compatible, homed VOK-side until representation delivery,
  migrating to the pedigree home at studbook founding.
  Its file-role layer (declaration-bearing / reference-only / index-of-record / generated) is
  a governance-member facet at scan grain.
  The matricula also owns the deep lint surface: coverage subtraction, anchor-width and
  dead-row findings, lintel audits, balance resolution — reading the same table through the
  same crate.
  The table reader itself validates only what it needs to function.
* *Dynamic context*: the hand-maintained CLAUDE.md / `claude-*.md` acronym tables become
  generated projections, presented only when tackle says they are relevant — the matricula
  MVP's acronym-index regeneration is the first step already in motion.
  Doctrinal ancestor noted: MCM's task-lens machinery (focused activation, focal depths —
  Core / Expanded / Suppressed) is this same selective-context idea at concept grain;
  tackle-driven provisioning is its file-grain sibling, compiling a work-scoped view instead
  of shipping the whole constellation.
* *School / breeze* (horizon): gait selection and beat-map context assembly read the graph;
  design deferred to the joint seam.
  Type-specimen use case: the recurring failure where a model writes bash from ambient
  training habits unless reminded to read BCG.
  The tackle binding is what lets school *mechanically* place a
  read-the-governing-guide/review-against-guide beat in the beat map for any beat whose file
  scope lands in a BCG domain — retiring the operator's memory as the enforcement mechanism.
  Tackle's part is only the binding and the integrity declarations; the beat-shape is gait
  design, breeze-side.

Each consumer reads facets of the same rows; none owns the table.

== The substrate split (direction — not yet an API commitment)

Where tackle services divide between the shared substrate crate (the vvx-land library both
JJ and the matricula read) and the applications above it.
The dividing line is the matricula's own mechanical/judgment boundary (VOr_m7w), applied to
tackle:

* *The crate owns the mechanical half* — everything that is set operations over declared
  rules, no judgment: schema and load, claim-rule evaluation (path → verdict), roster
  derivation (rule × tree state → files), coverage subtraction (ambit minus claims),
  declared-edge traversal.
  Four query surfaces, matching the query directions in Scope semantics: resolve one path,
  derive one roster, audit coverage, walk edges.
* *The applications own the judgment half* — what to *do* with verdicts: JJ dispatch decides
  provisioning policy (honing, conduct core, pull door, warn-vs-refuse at the loudness
  seats); the matricula maps governance members to vestures and scan roles; school composes
  beats.
  Policy never sinks into the crate; rules never float into the apps.
* *One writer, many readers*: JJ is the sole author — every table edit goes through a JJ
  verb, engine-written under the journal ceremony and the studbook lock, validating
  semantics at the door exactly as gallops does (a dangling blaze, a duplicate role word,
  an unanchored glob refuses at write).
  The matricula reads and lints, never writes (VOr_q4f); its census products are the
  coverage remainder, audit flags, dead rows, and lintel findings.
* *Not yet claimable*: signatures and cache mechanics; the serialized form is settled (the
  schema section); the sequencing ruling holds.
  The shape above is a lean the matricula's interim allowlist reader should stay compatible
  with, not an interface to build against.
* *Distribution*: the crate ships (it is the reader the direction already places in the
  shipped substrate library); the matricula consumes it and never flows into it (VOr_q4f);
  the tackle *data* is regional, pedigree-side, while any materialized cache stays
  station-local (Scope semantics).
* *Names in data, shape in code*: the crate and apps know the *schema* — what a domain row
  is, what a tagged edge is — and never any particular name: no `BCG`, no `theurge`, no
  edge-type enum baked into vvx.
  Documents are bound to role-word blazes in the table's registries; edge labels are opaque
  tags the apps match by string; the BURE tweak-name gate is the house precedent (enforce
  the shape, never the census).
  A tag graduates into code behavior only by a later deliberate act, never by drift.
  This is also what keeps domain fluidity cheap: a development-time domain change is a table
  edit (journaled, studbook-side), never a rebuild.

== The table — settling schema

One file per governed body of work's tackle set, studbook-side, beside the pedigree.
JJ is the sole author: every write goes through a JJ verb under the journal ceremony and
the studbook lock, validating semantics at the door — the gallops posture, never a
hand-edited document (The substrate split).
Serialization is gallops-grade: deterministic key order; optional members skip-when-absent
so an untouched store stays byte-canonical; read-side tolerance stands as defense in
depth — a bad value surfaces loud at use, and a store never becomes illegitimate.
A tackle serializes as one contiguous, self-contained, diff-clean block, and crossbreeding —
copying a tackle between projects' tables — is a block copy that *stays correct*: its blaze
references are late-bound roles, so the copied row binds to the receiving table's own
definitions.

Wire keys carry the `jjot` sprue.
Standing reservation: `jjo` is the JJ JSON-sprue container — a non-terminal naming nothing
itself; each JJ-authored JSON wire format takes one fourth letter beneath it (`jjot` =
tackle); gallops `jjg` predates the rule and stands unchanged.
(The `jj` third-letter space is nearly full, and reservations live partly in the JJK README
prefix registry, which code grep cannot see — the registry is part of any future gate.)
Tail grammar per gallops: one structure letter (`r` root, `t` tackle row, `g` guide entry,
`c` codex entry; `e` reserved for hames), then kind `n` (field key) or `e` (enum token).
The sprue carries uniqueness, so member tails stay plain English; the operator-spoken words
are *tackle*, *claims*, *blaze*, and *ambit*.
Blaze keys ride project-authored wires ₿-sigiled per the widened AXLA glyph-carriage
amendment (Reconciliation obligations): a sigiled key is self-typing, and a wrong-kind
reference fails at the parse boundary.

[source,json]
----
{
  "jjotrn_ambit": ["Tools/**", "Memos/**", "tt/*.sh"],
  "jjotrn_guides": {
    "₿bash-guide": { "jjotgn_path": "Tools/buk/vov_veiled/BCG-BashConsoleGuide.md" },
    "₿rust-guide": { "jjotgn_path": "Tools/vok/vov_veiled/RCG-RustCodingGuide.md" },
    "₿rb-conduct": { "jjotgn_path": "Tools/rbk/claude-rbk-conduct.md" }
  },
  "jjotrn_codices": {
    "₿rb-codex": { "jjotcn_dir": "Tools/rbk/vov_veiled", "jjotcn_codex": "RBS0-SpecTop.adoc" },
    "₿jj-codex": { "jjotcn_dir": "Tools/jjk/vov_veiled", "jjotcn_codex": "JJS0_JobJockeySpec.adoc" }
  },
  "jjotrn_tackles": {
    "theurge": {
      "jjottn_claims":      ["Tools/rbk/rbtd/**/*.rs"],
      "jjottn_guides":      ["₿rust-guide"],
      "jjottn_conduct":     ["₿rb-conduct"],
      "jjottn_specs":       ["₿rb-codex"],
      "jjottn_balances":    ["RBSTC-theurge_cosmology.adoc"],
      "jjottn_check_build": ["tt/rbw-tb.Build.sh"],
      "jjottn_check_test":  ["tt/rbw-tt.Test.sh"]
    },
    "rb-bash": {
      "jjottn_claims":     ["Tools/rbk/*.sh"],
      "jjottn_guides":     ["₿bash-guide"],
      "jjottn_conduct":    ["₿rb-conduct"],
      "jjottn_specs":      ["₿rb-codex"],
      "jjottn_check_lint": ["tt/rbw-tl.Shellcheck.sh"]
    },
    "generated": {
      "jjottn_claims":  ["Tools/rbk/rbtd/src/rbtdgc_consts.rs",
                         "Tools/rbk/claude-rbk-tabtarget-context.md"],
      "jjottn_conduct": ["₿rb-conduct"]
    }
  }
}
----

Members, root grain:

* `jjotrn_ambit` — the governed boundary: positive globs within which coverage is owed;
  a beyond-ambit path is a known-unknown, never a defect (Scope semantics).
  The ambit seats here, in the tackle table — coverage is tackle machinery — and the
  pedigree carries no duplicate.
* `jjotrn_guides` — blaze definitions for single governing documents.
  Keys are ₿-sigiled *role words* (`₿bash-guide`), never document acronyms: the role
  outlives any particular binding, and a consumer asking for the role receives whatever
  document the table binds today.
  Entry members: `jjotgn_path` (home-relative path), and skip-when-absent
  `jjotgn_corpus` — absent means the document lives in the governed body's own tree;
  present, it names the holding corpus (tokens elect with the central-corpus fork,
  Open forks).
* `jjotrn_codices` — blaze definitions for spec sets: `jjotcn_dir` (the set's one
  directory) plus `jjotcn_codex` (the SpecTop basename — the entry door), and the same
  skip-when-absent `jjotcn_corpus`.
  A set entry deliberately does not enumerate sheaves: membership is the claim rows' job,
  and on-demand loading rides the codex's own index — restating it here would recreate the
  home (ACG).
  One blaze namespace spans every registry: a role word defines once per table.

Members, tackle grain (each optional, skip-when-absent):

* The key — the tackle's *name*: a plain spoken word, the map key, used in audit and error
  speech; a name, never an identity, since nothing durable references it
  (Candidate vocabulary).
* `jjottn_claims` — the fileset definition: anchored subtree × shape globs.
  Wildcards are the mechanism; unanchored patterns refuse at write; root-anchored recursion
  and bare-`*` shapes are matricula audit findings, not prohibitions.
* `jjottn_guides` — authoring-discipline blazes, provisioned before writing begins; the
  member any future honing intelligence operates on.
* `jjottn_conduct` — behavioral-membrane blazes; never honed away (the farrier sheaf's
  unhoned conduct core).
* `jjottn_specs` — codex blazes the domain balances against; provisioned as the entry
  door, never inlined wholesale.
* `jjottn_balances` — express sheaf basenames within a referenced set, for a domain whose
  balance target is finer than the codex; resolution against the registered sets is a
  matricula lint, not a reader gate.
* `jjottn_check_build` / `jjottn_check_test` / `jjottn_check_lint` — verification
  commands, ordered, run from the tree root; kind-agnostic strings (a vanilla body's
  `npm test` sits as comfortably as a tabtarget).
  MVP consumers surface them to the session; gait auto-tool steps and chukker-boundary
  decisions are the anticipated readers (breeze sheaf).
* Reserved, unbuilt: `jjotrn_hames` — declared interrelations, arriving with the first
  relation consumer (The association graph); how an edge references a tackle (name-key or
  minted identity) is decided at that normalization act — the recorded identity re-entry
  condition.
  Enum tokens (`jjot«s»e_`) — none yet; the corpus tokens are the first anticipated.

Growth rule: sophistication lands as new members — never as new kinds threaded through a
generic list — and skip-when-absent keeps standing stores byte-canonical as members
arrive.

== Where it lives

Studbook-side, beside the pedigree (one tackle set per upstream) — never in the hippodrome,
which carries no JJ inserts (canon: the revision-control cosmology sheaf,
vanilla-hippodrome premise).
Tackle and farrier config are sibling entries on the pedigree answering different questions
with different change cadences:
tackle binds *files to rules* (changes when code domains shift);
farrier binds *the repo to its mechanics* (changes when repo topology shifts).
The ambit seats in the tackle table itself — coverage is tackle machinery — and the
pedigree carries no duplicate (schema section).
Letting tackle swallow VCS config would regrow the maximal tackle the MVP deliberately
defers.
The vanilla-hippodrome premise also forecloses the gitignore/gitattributes scaling trick of
in-tree distributed rule files: tackle must be centralized-external, which is what makes the
intensional-table + live-evaluator design mandatory rather than optional.

The registered documents divide by home: a guide is either *locale* — living in the body of
work it serves, a legitimate and expected pattern as a project accretes its own excellent
guidance — or *central*: the operator's proprietary corpus, as closed as the record itself,
never planted in a governed repo, its existence undisclosed to a foreign project or
disclosed only as deliberately whittled variants.
The home repo's in-tree guides are simply the first locale instance.
The central corpus is the guide family plus the cross-project law documents (MCM, AXLA,
GMG) — author-facing discipline portable to any sire bearing the artifact class — while a
system's specs are locale documents of their own sire.
A registry entry names the holding corpus only when it is not the governed tree (the
skip-when-absent corpus member); which private home holds the central corpus, and with it
the corpus tokens, is an open fork (Open forks).

== How it reaches a session

At saddle time (farrier sheaf, launch-time provisioning): the dispatcher reads the pedigree's
tackle set and provisions the session's context accordingly.
This replaces repo-resident `@`-include context at repo grain with runtime binding at file
grain, and is what lets vanilla external projects receive discipline context without carrying
any of it.

== One word, one concept (the V4 resolution)

The retired V4 execution workup defined a maximal tackle: a silks-identified resource bundle
carrying ₿-blaze file scopes, actions with exclusivity, gait affinities, read-also entries —
school-time execution machinery for the breeze pipeline.
Resolution: *this sheaf's definition is the tackle; the maximal version extends it later.*
One definition re-homed and grown — never two tackles side by side.
The countable ruling completes the resolution: V4's countable usage and the table-of-bindings
sense reconcile as node and set.
The maximal layer is banked below (Deferred); its execution context (school, gaits, chukker
boundaries) lives in the breeze aspirant sheaf (JJSAB), and the future tackle design pace
inherits this sheaf as its floor.

== Sequencing (operator ruling)

Tackle architecture waits until the operator has worked inside the new system long enough to
find footing — sequenced *after* the studbook/farrier MVP ships, never alongside it.
The matricula soft landing (Consumers above) is the footing experience that ruling wants.

== Deferred (explicitly not MVP) — the banked maximal layer

Banked from the retired V4 workup so the ideas survive their source heat; each is a future
design decision, not a commitment:

* *₿ blazes, the deferred remainder* — the definition mechanism itself lands with the MVP
  registries (role-word keys, table-global namespace — settling the workup's unresolved
  uniqueness scope; Candidate vocabulary).
  Deferred: arbitrary per-domain *resource* blazes beyond documents (fixture data, scratch
  roots) that dockets and gaits reference and school resolves when writing warrants —
  breeze never sees ₿ symbols.
  Validation sketch stands: no duplicate definitions, no orphan references.
* *Actions* — named commands with exclusivity scopes (candidate enum: worktree / workstation /
  project), the orchestrator holding mutexes during concurrent dispatch when an action's
  scope demands it.
* *Gait affinities and read-also entries* — which gaits a tackle suits, and standing context
  attachments.
* *Integrity declarations, the deferred remainder* — the check members land MVP (schema
  section; the CLAUDE.md after-code-changes suite table is this content, hand-maintained
  today).
  Deferred: quality requirements around them (retry-up-to-N / fail-fast) and the
  chukker-boundary policy that consumes them, breeze-side.
* *Tackle–gait interface* — gaits declare required blazes; compatibility validates at school
  time.
* *Registry shape* — tackles as silks-identified, mutable resource bundles in a top-level
  registry beside the gait registry.
* *Per-heat tackle mutability* — MVP tackle is per-upstream standing config.
  A provisional reconciliation (unreviewed supposition — design review required):
  the per-upstream tackle set is the standing base; a heat may later overlay heat-scoped
  additions (blazes, actions) that reference and extend the base; "tackle" names the layered
  whole.
  This bullet is that fork's one home.

== Reconciliation obligations toward sibling sheaves (banked nudges — not yet applied)

Each is an edit owed to a sibling *after* the relevant ruling ratifies, deliberately not
applied yet:

* *AXLA `axd_insignia`, glyph-carriage amendment* — owed from the sigil-in-wire ruling
  (schema section); canon edit, so it lands as a deliberate act under the CMK covenant
  (in-conversation, operator-ratified), never a drive-by.
  Draft replacement for the glyph sentence, wording to settle at the edit:
  "The identity is the bare encoded body; the associated unicode glyph is its *type
  sentinel* — an out-of-alphabet character at position zero naming the identity's type,
  never contributing to its value.
  Carriage is surface-keyed, three ways: mandatory in operator-facing output; mandatory in
  project-authored structured wires (JSON stores and envelopes), where the sentinel makes
  every persisted identity self-typing and a wrong-type identity fails at the parse
  boundary; forbidden on foreign-traversed surfaces (git refs, filesystem paths, env vars,
  shell arguments), where non-ASCII fragility is real.
  Input is tolerant: readers accept and strip the sentinel, validating it against the
  expected type where declared."
  Harmonizes with `axd_algebraic`'s subordinate sentinel clause rather than adding a second
  concept; `jjrf_favor.rs` comments stay true (identity = body); the gallops store is
  already compliant, so no key sweep and no reprieve episode.
  Widened by the blaze ruling: the same surface-keyed carriage extends beyond insignia to
  blaze keys — ₿-sigiled role words in project-authored wires — worded at the edit as a
  companion clause, never a second doctrine.
* *JJK README prefix registry* — owed at delivery: the `jjo` container row and its
  child-letter convention (`jjot` = tackle wire).
* *JJS0 codex, at infusion* — two AXLA obligations:
  the lintel here carries the legacy one-token form (`axd_aspirant` alone), and current AXLA
  law wants two dimensions in fixed order (normativeness, then genre — for this sheaf
  plausibly `axd_entity`, the VOSMM exemplar); upgrading the lintel and JJS0's matching
  cartouche is one act at infusion, never done one-sided (attestation-agreement drift).
  And the laws this sheaf leans on (no-catch-all, cache-never-studbook,
  positive-at-domain-grain) are rivet/premise-shaped: at infusion they voice formally
  (`axvc_dictum` / `axk_premise`) with opaque `JJr_` tails minted then, not now.
* *JJSAB breeze* — three edits owed: its vocabulary table lists tackles among
  silks-identified gallops entities, superseded by the names-not-identities ruling
  (tackles carry plain names; nothing durable references them);
  its blaze row reconciles — the definition home is the tackle table's registries,
  the namespace table-global (the workup's per-heat and per-tackle scopes yield),
  gaits reference role words and school resolves against the table in play;
  and its identity ladder re-anchors with no tackle rung at all.
  Its "Tackle's execution layer" reconciliation bullet should still gain the
  association-graph pointer (school walks declared edges, not just blazes).
* *JJSVS studbook* — the pedigree bullet "tackle bindings (file scope → governing
  discipline) as sibling entries" narrows to the charter's first half; it should widen to
  filesets *and* interrelations, and it must not grow an ambit entry — the ambit seats in
  the tackle table, never on the pedigree (Where it lives).
* *The registered-body wording sweep* — this sheaf joins the recorded sweep scope
  (paddock: upstream/origin-URL phrasing re-worded across the canon sheaves once the held
  word election lands); it is the heaviest user of the old wording, and new prose here
  says "governed body of work" meanwhile.
  The sweep still follows the election as one deliberate act, never piecemeal.
* *Farrier aspirant sheaf* — launch-time provisioning already carries the conduct-core +
  pull-door repairs; confirm at infusion that they are stated as tackle-consumer
  obligations, not farrier-local conveniences.
* *VOSMM* — the allowlist-as-projection direction is already recorded there; owed later:
  the estray naming question (token-level `vosme_estray` vs file-level
  unclaimed-inside-ambit — one generalizing concept or two words) resolves at tackle
  graduation, and the file-role layer should be named as a governance-member facet.
* *AXLA `axl_timeless`, aspirant-exemption question* — raised, not committed: the exemption
  permits journal content in aspirant sheaves, but this sheaf now practices timeless prose
  by operator preference (decision records live in git and the memos), while sibling
  aspirants still carry dated ledgers.
  If the tighter practice is a standing norm, it wants its doctrine home at AXLA rather
  than living as per-sheaf habit.

== Settling register (drain before implementation)

Per the mulling-heat doctrine (JJS0, The Mulling Heat), implementation paces wait for
*settling*: this register, drained entry by entry into named settling paces or retired.
Each entry cites its substance's home; nothing is restated here.
Entries carry a *drain mode*, discriminated by one test — does the choice's outcome reshape
the pace decomposition?

* SHAPES THE CUT — settle before slating implementation paces at all.
* SETTLING PACE — judgment work cut as its own early pace; pace order handles precedence.
* RIDES A PACE — mechanical, folded into an implementation pace.

The counterweight stands: what a mount agent decides better against live code is
deliberately *not* settled early — the docket anti-patterns govern that direction.

. OUTER GATE — the sequencing ruling: implementation follows the studbook/farrier MVP (see
  Sequencing); this register drains after that ships.
. SHAPES THE CUT — the API sketch: the four crate surfaces (substrate split) written
  against the matricula's and JJ dispatch's *actual* queries; its outcome determines what
  the implementation paces are, and it settles the remaining word spends (mint follows
  API).
. SETTLING PACE — the AXLA glyph-carriage amendment, widened with the blaze-sigil clause
  (draft: Reconciliation obligations): the schema's sigiled wire keys are illegal under
  unamended doctrine, so this pace precedes the schema-implementation pace in order.
. SETTLING PACE, soft — the semantic-simplification pass: prose that defends words the
  schema now carries is deletable; the sheaf should read as specification, not
  deliberation, before it hardens.
. RIDES A PACE — the JJK README prefix registry gains the `jjo` container row
  (Reconciliation obligations).
. RIDES A PACE — the matricula's interim allowlist is confirmed schema-compatible against
  the settled schema (Consumers; the soft landing), with migration owed at studbook
  founding.

== Open forks (the mulling surface)

* *Representation* — settled: the schema section is the one home (one home or none, never
  two half-homes inviting confabulation).
  Still standing from the direction: one table read by a shared substrate crate for BOTH the
  JJ dispatch layer and the Vox matricula; the matricula MVP allowlist is shaped
  schema-compatible, homed VOK-side, migrating to the pedigree home at studbook founding;
  the reader lives in the shipped substrate library, and the matricula consumes the library
  and never flows into it (VOr_q4f).
* *The central-corpus home* — the reference form is SETTLED: first-class registry entries
  keyed by ₿-sigiled role words (schema section).
  The recorded rationale: a role outlives any particular binding (the day the bash guide
  changes, the key does not lie); one home per document, moved once; whittled per-project
  variants and version-pinned experiments hang naturally on an entry (intended
  partial-release whittling of BCG; one guide at two versions, outcomes compared);
  and late-bound roles make a crossbred row *more* self-contained semantically, not less —
  it binds to the receiving table's own definitions — dissolving the raw-path pole's one
  advantage.
  Open: which private, never-distributed home holds the central corpus —
  the studbook as its first documents tenant (operator inclination: one regional data
  home; friction: the blotter is engine-written and linear, so hand-authored documents
  need a declared carve-out area or an edit ceremony),
  the kit repo (chartered "engine + provisioned context", adds no repo),
  or a standalone guides repo (cleanest lifecycle isolation, but a new repo).
  The corpus tokens (`jjotgn_corpus` / `jjotcn_corpus` values) elect with that home.
* *Claim-rule kinds* — the aspirant-sheaf domain exposes a new need: some real domains are
  defined by a *declared in-file marker* (the `axvd_sheaf axd_aspirant` lintel), not by
  path shape — aspirant sheaves are only partly path-regular: the JJ family rides `JJSA*`,
  but VOSMM, RBSCV, and RBSPC carry the aspirant lintel inside their codex directories with
  no path marker.
  The lintel is designed for mechanical reading (AXLA), so a lintel-predicate claim would
  still be declared-never-deduced — the declaration simply lives in the file instead of
  the table.
  Lean, per simplicity: the claims stay glob-only and the census *audits* the lintel (a
  file whose lintel carries `axd_aspirant` outside the aspirant tackle's claims is a
  finding); a lintel-predicate claim kind is the anticipated growth if that audit's
  maintenance burden proves real.
* *Loudness seats* — where the coverage remainder fires: the matricula census (repo
  hygiene, batch), saddle time (the session is about to touch an unaffiliated file — the
  pull-door shape), or both; and warn vs refuse at each seat.
* *Check-command semantics* — deferred until the base farrier is built and the commands
  have a live consumer; needs real design, not a drive-by cinch: what order means on
  failure (run-all vs stop-on-first), whether the members interdepend (a test presumes its
  build), tier ladders (a fast check vs a release suite), and where declared semantics stop
  before they recreate gait-side policy.
  Until settled, consumers treat the check lists as surfaced advice, never an executed
  contract.
* *Edge-tag taxonomy* — specifies / tests / crosscuts are census-observed, not yet an enum;
  more census before it deserves one.
* *Verdict naming* — held with the estray question (Candidate vocabulary).
* *Remaining word elections* — verdict names; the corpus tokens (with the central-corpus
  home); role-word conventions for the registries, if practice wants any.
  Elections run gates at a census-style pace, not mid-mulling.
  Word-health evidence banked for that gate: ambit reported not-working by the operator;
  tackle itself sits two letters from the minted gallops tack — an audible-register hazard;
  the family re-ratifies with this evidence weighed.
* *Delivery* — direction resolved: app-generated, work-honed context at saddle time, with a
  retain-everything default until honing intelligence exists (farrier sheaf, launch-time
  provisioning).

== Sources (provenance, not authority)

* Retired heat jjk-v4-4-school-breeze-founding (₣Ah) — its paddock's tackle/blaze vocabulary
  and its tackle design docket, the maximal layer banked above; the execution context is
  recast in the breeze aspirant sheaf (JJSAB).
* ACG (Allodial Coding Guide) — the several-domains observation, and the edge-discipline
  type specimen.
* Chat gamma-fable-regional-gallops (260704) — the MVP re-scope and pedigree-sibling
  placement.
* Design chat pym-beta-tackle (260707, Fable) — the charter wording, inclusion-polarity
  resolution, worked census, association graph, countable ruling, schema settling and word
  elections; exploratory record in `Memos/memo-20260707-tackle-polarity-and-graph.md`.
* SlickEdit specimen `Tools/vslf-rbw/` + `Tools/vslk/` — the failed worked example mined for
  fileset-definition lessons (memo §autopsy).
* Gallops serialization (`Tools/jjk/vov_veiled/src/jjrt_types.rs`) — the sprue grammar,
  identity-keying law, and store-manners precedents the schema copies.
* The tackle-model urge session (260711, Fable) — typed governance members replacing
  trappings; blaze registries with role-word keys; the names-not-identities ruling and
  files-first resolution; JJ-sole-author write posture with matricula lint ownership;
  ambit seating; the flattened-edge declaration with ACG as first migrant.
