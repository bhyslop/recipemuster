//axvd_sheaf axd_aspirant axd_roadmap
This is an aspirant portion of the {jjs0_imprimatur}: not yet binding — do not rely on it, decide from it, or cite the names it mints until it is infused into the codex.

= Farrier — Revision-Control Driver and Dispatch (Aspirant Nucleation)

[IMPORTANT]
====
*Aspirant nucleation* (AXLA `axd_aspirant`): not yet binding.
A feature-definition space under active mulling — to be sophisticated and revised until ready for
development, then infused into this codex or retired.
A reader must not rely on its content; released code must not cite identifiers it mints.
The driver's contract — entity, facets, op census, vocabulary Palisade, rejection kinds — is
canon: drained 260706 to the farrier entity sheaf (`JJSVF-farrier.adoc`, the `jjdf_` quoins).
The dispatch layer is canon: drained to the dispatch sheaf (`JJSVD-dispatch.adoc`,
the `jjdd_` quoins).
What remains below is mint provenance and plan content;
the census pace ran the formal mint ceremony 260705 (gates fresh, elections ledgered).
====

== What the farrier is

Canon: drained 260706 to the farrier entity sheaf (`JJSVF-farrier.adoc`, `jjdf_farrier`) —
the entity, the facet-split capability model (`jjdf_core`/`jjdf_lock`/`jjdf_billet`, per the
paddock's farrier-facets cinch), and the no-transaction-ownership line.

== Two axes, two generalization points

Canon: drained 260706 to the farrier entity sheaf (`JJSVF-farrier.adoc`), with the billet
generalization point re-anchored on the partition axis per the banked delta — a root with a
lifecycle; worktree the plain-git implementation, full clone the fallback for kinds without
worktrees.
The constellation-axis counterfoil point was already canon at the studbook sheaf
(`JJSVS-studbook.adoc`, `jjdb_counterfoil`).

== Hearting ops (MVP census)

Canon: drained 260706 to the farrier entity sheaf (`JJSVF-farrier.adoc`) — the vocabulary
Palisade (`jjdf_palisade`), the op census as method contracts (with `identify` carrying the
260706 cinched contract — `jjdf_identify`: explicit probe path, claim-or-decline, the four
resolutions — superseding the old census row's plain-git shape), the cross-cutting no-cwd and
named-rejection laws, the rejection-kind taxonomy, the RAII lock-guard entity design, and the
never-force rivet homed at the consign contract.
Mint provenance kept here: the census re-mints of 260705 and their rationales live in the
Naming ledger below;
the RCG check of the taxonomy (error idioms deferred by design; Zeroes, Identity Rule, String
Boundary, Output all pass; typed taxonomy a deliberate step up from the crate's habitual
`Result<T, String>`) is provenance of the settling, recorded 260704.

== Toothing compositions

The primitives are canon (`JJSVF-farrier.adoc`); the journal ceremony is its own routine
sheaf (`JJSVJ-journal.adoc`);
the dispatch rows drained to the dispatch sheaf (`JJSVD-dispatch.adoc`).
The remaining compositions stay aspirant:

[cols="1,3"]
|===
|Toothing |Composition

|*(officium open — not a spoken verb)* |Canon: drained — `identify` composes the
station/hippodrome-or-billet/session billet, and (260718, ₢B3·CAAAO) the staleness lead
composes in too, via the dispatch board's own post-glean ancestry probe
(`jjrds_staleness_notice`/`jjrfr_outstripped`), never the `sync_state` line named below — see
`JJSVF-farrier.adoc`, "Toothing: officium open", for the full composition and why

|mount / orient |studbook read (no lock) + `sync_state` staleness line

|muster / parade / groom |studbook reads only

|notch |hippodrome: `comb` → `lodge` (explicit files) → `counterfoil` — then the journal
ceremony carrying the counterfoil

|wrap |as notch, plus pace-state mutation in the journal

|slate / reslate / paddock |journal ceremony only — no hippodrome ops

|saddle / lunge |Canon: drained — the entrance spine (`JJSVD-dispatch.adoc`)

|break |Canon: drained — the cashier door (`JJSVD-dispatch.adoc`, `jjdd_cashier`), citing
the journal sheaf's break sequence per lock
|===

A ring-survey toothing was considered and *demoted* (260704): the studbook dissolved
cross-clone state sync, billets partition parallel work, saddle housekeeping reaps stale
billets, and `sync_state` reports at open — if a whole-infield status panel ever earns its
keep, it is a report mode, not a verb.

== Saddle, lunge, billet — the dispatch layer

Canon: drained to the dispatch sheaf (`JJSVD-dispatch.adoc`, the `jjdd_` quoins) —
the two doors, the billet entity (both kinds, dirnames per the yard split), the entrance
spine with muck as its leading step (no unsaddle door), the muck and refit contracts, the
cashier door, the lifecycle doctrine (chat-ephemeral billets over durable branches,
structural session isolation, sole entrance), and the session-launch tier machinery.

== The BUK meld (studied 260704)

Canon: drained to the dispatch sheaf's BUK meld section — the per-billet BURV export
contract (`BURV_OUTPUT_ROOT_DIR` / `BURV_TEMP_ROOT_DIR` / `BURV_LOG_DIR`), billet-scoped
chain facts, theurge-unchanged.
Plan content kept here: the elected `BURV_LOG_DIR` launcher build slots as an early
standalone implementation pace at cut time (self-testable via `buw-st`), not a
groom-session side-edit — meticulous execution demanded by the operator.

== Launch-time provisioning (the vanilla-hippodrome consequence)

Canon: drained to the dispatch sheaf's Launch-time provisioning section — provision
at session launch, generated work-scoped context with the retain-everything default, the
conduct core and pull door repairs, and the officium-exchange forward reference.
The tackle-binding forward reference has landed: the bindings are canon at the tackle sheaf,
which claims the two repairs as its own consumer obligations.
The haiku orchestration line stays breeze-era (breeze sheaf).

== Prerequisite remint

DONE: the interior orient-context routine formerly named saddle (`jjrsd_saddle.rs`,
`JJSCSD-saddle.adoc`) is re-minted to the boring hearting name `mount` (`jjrmt_mount.rs`,
`JJSCMT-mount.adoc`), every reference swept, repo-wide grep for saddle clean of interior
uses.

== Naming ledger (candidate mints of 260704, alternates preserved)

* *farrier* — the driver API; fits the horse to the ground. F freed by formbook's rejection.
  INFUSED 260706 (farrier sheaf, `jjdf_farrier`).
* *saddle* — the work-dispatch verb; the real-world sequence saddle-then-mount lands in
  the right order across the two vocabulary layers. Alternate held: leg-up.
  Election RATIFIED at the census (260705); the grep gate was OPEN by contingency until
  the interior-rename pace freed the word — `jjrsd_saddle.rs` / `JJSCSD-saddle.adoc` are
  now `jjrmt_mount.rs` / `JJSCMT-mount.adoc` (Prerequisite remint above).
  INFUSED (dispatch sheaf, `jjdd_saddle`); the gate is CLOSED.
* *unsaddle* — RETIRED before infusion: the reap folded into the entrance spine as
  muck's own behavior, leaving no door to name, and its refuse-if-dirty was redundant with
  muck's dirty guard.
  Never infused, so the word simply releases; the saddle election above is untouched.
* *lunge* — the groom dispatch verb (minted 260704): working the horse from the ground on the
  long line, deliberately unmounted — assessment without riding.
  Grep-clean both spellings; US *lunge* elected over UK *longe*.
  Restores saddle's firemark spelling to its natural meaning (the heat's next actionable pace),
  retiring the length-picks-protocol collapse.
  INFUSED (dispatch sheaf, `jjdd_lunge`).
* *billet* — the standing lodging entity; the soldier's assigned temporary quarters, with the
  equestrian foothold of saddle billet straps. RATIFIED at the census (260705) by eviction:
  the word collided with the catalogued window-overlay quoin `jjdxw_billet`, and the window
  sense yielded — interior plumbing, spec-prose-only footprint, while this billet is the
  dispatch layer's core noun — re-worded to `jjdxw_hatchment` (JJS0 swept, gate clean).
  Both held alternates died at the census gates: livery is foreign-minted in RBK
  (`chaining-fact-livery`, `RBTDRM_FIXTURE_CHAINING_LIVERY`); stall is trodden
  (stalling/stalls saturate ambient prose, JJK core doctrine included).
  Disqualified earlier: seat (per-seat licensing troddenness; RBK's minted *unseat* as
  near-neighbor).
  INFUSED (dispatch sheaf, `jjdd_billet`).
* *comb* / *lodge* / *glean* / *consign* — the census re-mints (260705) for the four
  git-worded ops; gates clean.
  lodge's hits are incidental MCM prose ("images lodge across sessions") plus the lodging
  gloss on billet above — judged a composing echo, not a collision: a session lodges its work
  at its billet, the deposit sense and the quarters sense reinforcing.
  Independent convergence noted: current-JJ's `jjrvl_validate.rs` doc prose already says
  "consigns the gallops" for the commit act — comment-only, no mint.
  INFUSED 260706 (farrier sheaf, op census — core facet).
* *stake* / *pluck* / *sight* — the lock trio (260705), the guidon supplying its verbs; the
  CRUD-reflex rationale is canon at the farrier sheaf's lock facet (`JJSVF-farrier.adoc`).
  Seed disposition: plant taken (`jjx_plant`, live foray command), strike taken (RBSME
  escheat's orphan sweep), read barred (the CRUD/git reflex word).
  stake's racing sense (stakes race) noted and accepted: the trio is sequence-internal, never
  operator-typed, and the claim sense dominates in imperative position.
  INFUSED 260706 (farrier sheaf, op census — lock facet).
* *freshen* — COLLIDED with VOK (`tt/vow-F.Freshen.sh`; VOSO "Freshen CLAUDE.md"); banked and
  RESOLVED at the census (260705), the word swept from the sheaves.
  The untangle, per the trunk-resync cinch: the session-facing resync operation is *refit* —
  fair-faced for the staleness warning it names the remedy in, and composing with this sheaf's
  own opening metaphor (the farrier fits the horse to the ground; when the ground moves,
  refit); gate clean.
  The bare merge-trunk-into-billet primitive is *enfold* (hearting; gate clean, 0 hits).
  Alternates held for refit: revictual (the billet-logistics composition — resupply the
  quarters), limber (the artillery-horse attach-to-move sense).
  Disqualified: dress (MCM's exposure-ladder *dressing* is load-bearing masonry vocabulary),
  absorb (a named ROE Palisade conduct step), incorporate (AXLA's include-incorporation).
  enfold INFUSED 260706 (farrier sheaf, op census — billet facet); refit INFUSED
  (dispatch sheaf, `jjdd_refit`).
* *muck* — the sweep, the spine's leading step (no longer a door, never operator-typed):
  mucking out the stable — the plan-then-confirm nuke-on-start reap of closed-pace billets
  past retention, silent when the reap set is empty.
  Gate clean (0 hits); *sluice* held as alternate (washing — less exact than the spent-bedding
  removal muck names).
  INFUSED (dispatch sheaf, `jjdd_muck`).
* *stirrup* — the launch primitive (elected 260707, session-launch design pace): the
  mounting-assist apparatus — the step by which the rider gets aboard the already-prepared
  horse — naming the interior act at the end of the entrance spine that starts a configured
  session in a billet (parameterized billet, tier, opening prompt; pace-blind; the tier-roster
  table's one launch consumer).
  HEARTING per the enfold precedent (uncatalogued, grep hygiene only); gate clean (0 hits).
  Disqualified: launch (trodden, docket-pre-ruled), launcher (BUK-bound), spawn/dispatch
  (trodden chat brushes); compound forms barred by operator ruling — leg-up and put-up died
  there despite clean gates.
  Alternates: spur held with a false-friend note (confidently misreads as urging an
  already-running session); gee deliberately released to fallow — the operator's trot-advance
  cry, kept unenclosed like lean.
* *cashier* — the break-all door (elected 260714, break-door pace): to dismiss an officer from
  service, with cause — the deliberateness sits in the word, which is the whole property the
  door adds to the break sequence it composes.
  The derelict holder is dismissed; its {jjdb_guidon} — a cavalry standard — comes down.
  ASHLAR (operator-typed, fair-faced on first contact); gate clean in code and spec (the sole
  repo-wide hit is a chat-archive transcript of an earlier candidate battery).
  Alternates: *unhorse* (clean, squarely equestrian, but reads as acting on a rider — a live
  session — where the door acts on a standard, and the holder is usually already dead);
  *reave* (clean but opaque at first contact, and a near-homophone of reeve — weak as an
  ashlar).
  Disqualified: *strike* and *spike* (trodden, ~160 and ~1000 hits); *furl* (gate-clean at
  zero, but one prefix from JJK's standing *unfurl* image verb — two near-identical words for
  "view a diagram" and "break a lock" is a collision minted on purpose).
  INFUSED (dispatch sheaf, `jjdd_cashier`).
* Exposure tiers: *saddle*, *lunge*, *cashier*, and *refit* are ASHLAR — operator-typed, so
  fair-faced and coffer-drawn, with broadside registration deferred to cutover alongside the
  cosmology nouns — the surface is inert until then (JJ's broadside: the JJK README glossary).
  *muck* is ashlar by output alone — never typed, but its confirm plan names the act to the
  operator; *unsaddle* left the roster with its retirement.
  On the centrality axis they are toothings — the acts performed on the model-constituting
  nouns — with saddle/lunge the heaviest-worn pair; the bondstone nouns they act on (billet,
  studbook, hippodrome, trunk, pedigree, guidon) hold first-letter separation as a set.
  (The paddock's "likely bondstone" phrasing resolves per MCM's axis split: verbs are
  toothings; bondstone is the noun tier.)
* Ring-survey toothing — demoted 260704, no verb needed; the *stables* candidate is released.

== Open forks (the mulling surface)

* Constellation backends (submodules, Android `repo`): future farrier kinds; the counterfoil
  and billet generalization points are canon (`JJSVS-studbook.adoc`, `JJSVF-farrier.adoc`),
  implementations deferred.

== Sources (provenance, not authority)

* `Memos/memo-20260630-jj-revision-control-architecture.md` — the VCS-driver and lock sections
  this sheaf supersedes as mulling home.
* BUK dispatch spine: `tt/z-launcher.sh`, `Tools/buk/bul_launcher.sh`, `bud_dispatch.sh`,
  `burc_regime.sh`, `burs_regime.sh`; theurge BURV sites: `rbtdri_invocation.rs`,
  `rbtdrh_chain.rs`.
* Chat gamma-fable-regional-gallops (260704) — op census, billet lifecycle, BUK meld study,
  dispatch cinch.
