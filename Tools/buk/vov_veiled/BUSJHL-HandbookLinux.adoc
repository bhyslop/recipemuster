//axhob_operation
{bust_handbook_linux}

NOTE: Per-platform handbook surface for Linux — operator-manual scope
that {busn_caparison} cannot or should not automate. Renders to the
operator's terminal as the entry point into Linux-specific bootstrap
procedures: Tailscale install + login, sshd install/enable on minimal
distros, and any other Linux posture an operator must establish before
{bust_caparison_linux} can run. No remote contact and no profile
mutation.

TODO: subsequent pace fleshes the step list and prose content.
