//axhob_operation
{bust_invigilate_windows}

NOTE: {busn_invigilate} ceremony on Windows — read-only host posture
verification. Asserts each posture fact (admin SSH key trust,
`sshd_config` hardening, `rbtww-main` WSL distribution presence under
admin, powercfg sleep/hibernate state, Tailscale service StartupType,
operator-managed registry keys) matches its expected value. No
mutations, no remediation. Failure pointers discriminate
operator-managed posture (→ {bust_handbook_windows}) from
caparison-managed posture (→ {bust_caparison_windows}).

Invocation contexts: {bust_caparison_windows} post-completion check;
garrison precondition check on Windows nodes
({bust_garrison_cygwin}, {bust_garrison_wsl}).

TODO: subsequent pace fleshes preconditions, fact-by-fact step list,
guarantee, and completion clauses.
