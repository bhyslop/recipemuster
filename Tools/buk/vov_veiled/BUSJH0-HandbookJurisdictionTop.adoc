//axhob_operation
{bust_handbook_jurisdiction_top}

NOTE: Top-level dispatcher for the {bus_jurisdiction} handbook.
Renders the per-platform handbook entry points
({bust_handbook_windows}, {bust_handbook_macos},
{bust_handbook_linux}) plus cross-platform pointers to
{busn_caparison}, {busn_invigilate}, and {busn_garrison}.
No remote contact and no profile mutation.

//axhos_step
. *Render dispatcher*: emit the jurisdiction handbook top content —
pointers to the per-platform handbook tabtargets
({bust_handbook_windows}, {bust_handbook_macos},
{bust_handbook_linux}), the verb dispatch chain
({busn_caparison} → {busn_invigilate} → {busn_garrison}), and
the workload dispatch tabtargets ({bust_knock},
{bust_command_file}, {bust_interactive_session}).

//axhog_guarantee
No remote contact occurred and no local file was written.

//axhoc_completion
Exit 0 after the landing content is printed.
Non-zero only when the handbook content cannot be located or rendered.
