//axvd_sheaf axd_normative
This is a normative portion of the {rbs0_imprimatur}: binding — code, tests, and decisions may rely on it.
//axhrb_regime
{rbrr_regime}

This regime specifies variables that define repository-wide configuration and installation-wide settings within the Recipe Bottle system.
Each
{rbrr_regime}
instance represents foundational infrastructure configuration that rarely changes and is shared across all Recipe Bottle operations.
RBRR assignment files are committed to the repository and configure the runtime container prefix, vessel directory, container DNS, Cloud Build timeouts, secrets directory, and public docs URL.
Depot-time-immutable identity and pool configuration live in the sibling
{rbrd_regime}.

//axhrgb_group
{rbrr_group_resource_prefixing}

Operator-chosen identifier that scopes machine-local resource names — container names, Docker network names, and other runtime identifiers on the operator's workstation.
Disambiguates the runtime resources Recipe Bottle lifecycle-manages — containers and networks — from other container systems sharing the same runtime, so Recipe Bottle operations do not disturb unrelated arrangements.
Must be populated — the marshal-zero path blanks it so a fresh regime requires an explicit operator decision before `rbrr_validate` passes.
Must start with a lowercase letter and end with a hyphen (regex `^[a-z][a-z0-9-]*-$`).

//axhrgv_variable
{rbrr_runtime_prefix}

Prefix prepended to machine-local resource names: container names, Docker network names, and other runtime identifiers on the operator's workstation.

//axhrgb_group
{rbrr_group_service_accounts}

The payor credential path derived from a single enrolled directory.
The payor RBRO file contains OAuth refresh credentials for
{rbtr_payor}
operations — the system's sole durable secret.
The operational roles (governor, director, retriever) hold no on-disk credential: the operator dons their mantles through federated avowal, minting short-lived tokens.

//axhrgv_variable
{rbrr_secrets_dir}

Directory on the operator's local filesystem outside the source repository that contains the payor credential file.
Enrolled and validated to exist; the payor file path is derived from this directory at lock time.

//axhrgv_variable
{rbrr_payor_rbro_file}

Path derived from
{rbrr_secrets_dir}
as `payor/rbro.env`.
Enables the operator to perform
{rbtr_payor}
OAuth operations using locally stored refresh credentials.

//axhrgb_group
{rbrr_group_container_runtime}

Variables that configure container runtime and deployment environment settings.
These settings control DNS resolution and vessel directory layout.

//axhrgv_variable
{rbrr_vessel_dir}

Directory path containing vessel configuration subdirectories.
Each subdirectory defines a vessel image source (bind or conjure mode).

//axhrgv_variable
{rbrr_dns_server}

DNS server IP address used by sentry containers for name resolution.
Injected into container environment to control DNS behavior.

//axhrgb_group
{rbrr_group_federation_selection}

The active-foedus selector: which standing
{rbtf_foedus}
this installation authenticates against.
A cross-domain reference into the
{rbrf_regime}
library — {rbrr_regime} names which foedus is active; the foedus's configuration lives in that foedus's own subdirectory's rbrf.env, never duplicated here.
Production names its single foedus and rarely changes it; the test-bed re-points it among standing foedera.

//axhrgv_variable
{rbrr_active_foedus}

The subdirectory name of the active
{rbtf_foedus}
— one of the foedus subdirectories in the moorings foedera library, each bearing the `rbef_` foedus-instance sprue (the standing members are `rbef_entrada`, the interactive Entra trust, and `rbef_keycloak`, the programmatic test trust).
It is the operator-facing folio passed to
{rbtgo_foedus_instate}
and
{rbtgo_foedus_descry},
and the durable identity of the foedus itself, distinct from the manor pool it draws on.
The authenticate-against-active consumers (avow, the accessor, the federated-access and mantle-access probes) require this selector committed before they run;
{rbtgo_foedus_instate}
rewrites it and leaves it uncommitted for the operator.

//axhrgb_group
{rbrr_group_build_configuration}

Operator-chosen
{gcb_service}
build parameters that may change across builds within a depot's lifetime.
Depot-time-immutable machine type lives in the
{rbrd_regime}.

//axhrgv_variable
{rbrr_gcb_min_concurrent_builds}

Minimum number of concurrent Cloud Build executions required.
With private pools, the relevant quota metric is `concurrent_private_pool_build_cpus` on the pool's host project.
Machine type is determined by the pool configuration at creation time, not by individual build requests.

//axhrgv_variable
{rbrr_gcb_timeout}

Maximum duration for Cloud Build operations before timeout.
Specified in seconds or duration format (e.g., `3600s`).

//axhrgb_group
{rbrr_group_public_docs}

Public documentation URL for user-facing references.

//axhrgv_variable
{rbrr_public_docs_url}

Complete URL for the public documentation entry point.
Updated per-release (swap branch name) or per-incorporation (point at a fork).
Used by buh_tlt glossary links and user-facing output.

//axhro_kindle
{rbkro_kindle}

Kindles the {rbrr_regime} fields by enrolling each field with its type-specific constraints under named groups.
Detects unexpected RBRR_ variables.
Composes the docker environment injection set and locks the regime against further modification.

//axhro_validate
{rbkro_validate}

Validates enrolled fields against type-specific constraints from enrollment.
Validates directory existence for {rbrr_vessel_dir} and {rbrr_secrets_dir}.
Enforces format constraints on {rbrr_runtime_prefix}.

//axhro_render
{rbkro_render}

Renders the {rbrr_regime} grouped by section with presentation gates per group.
Sections: Runtime Prefix, Vessel and Local Configuration, Federation Selection, Google Cloud Build Configuration, Secrets Directory, Public Docs.
