//axhob_operation
{mkr_bottle_launch}

NOTE: This sequence within
{opbs_bottle_start}
instantiates and configures the service container for the
{at_bottle_service}.

//axhos_step
. *Launch
{at_bottle_container}*:
.. {rbbc_call}
container run (via {rbrn_runtime}) with:
... image:
{rbrn_bottle_vessel}:{rbrn_bottle_consecration}
... name:
{at_bottle_container_name}
... network: exclusive attachment to
{at_enclave_network}
... volumes: each semicolon-delimited entry from the active volume mount field ({rbrn_docker_volume_mounts} or {rbrn_podman_volume_mounts} per {rbrn_runtime}), passed verbatim as a `--volume` argument
... restart policy: unless-stopped
.. {rbbc_require}
container launched successfully

//axhos_step
. *Verify Network Configuration*:
.. Allow container initialization with podman's default network configuration
.. {rbbc_call}
{mkc_interface_check}
to verify container network interface is active
.. {rbbc_require}
network interface ready for operation
