//axhob_operation
{opbr_bottle_run}

NOTE: This operation creates and executes a transient
{at_bottle_container}
via `docker compose run --rm`.
Compose manages container creation, dependency startup (sentry + censer), and automatic cleanup.

//axhos_step
. *Ensure Infrastructure*:
.. {rbbc_call}
`docker compose run` starts sentry and censer as dependencies if not already running (same health check chain as sessile: sentry healthy → censer starts → censer healthy → bottle starts)
.. {rbbc_require}
sentry and censer healthy before transient bottle container starts

//axhos_step
. *Execute via Compose Run*:
.. {rbbc_call}
{mkr_bottle_create}
— compose creates a transient container from the bottle service definition
.. {rbbc_call}
{mkr_command_exec}
— the specified command runs in the transient container
.. The `--rm` flag ensures automatic removal on exit

//axhoc_completion
Successful execution runs the requested operation in a transient container with the full security stack (sentry + censer) active.
The container is removed automatically on exit.
