//axvd_sheaf rbs0_imprimatur axd_normative
//axhob_operation
{rbtgo_foedus_establish}

NOTE: *Contract, not yet built.* This subdoc is the contract the programmatic-foedus
establishment build pace fills; it states what done looks like, not the implementation.
{rbtgo_foedus_establish}
is a *descriptive* operation quoin — the civic asterism verb (the
{rbtf_affiance}-register
word for this act) is deferred to that build pace, per the federation vocabulary census.

NOTE: This operation stands up a
{rbrf_mechanism_programmatic}
{rbtf_foedus}
end-to-end: a workforce pool whose provider validates self-supplied JWTs against an
uploaded *public* JWKS, with no human and no device flow. It is the durable,
REST-only successor to the throwaway gcloud proof that first ran the chain. The
{rbtf_affiance}-sibling
half — creating the pool and the programmatic provider from the uploaded JWKS — is
{rbtgo_manor_affiance}'s
programmatic branch, called here, never duplicated; this operation owns the keypair,
the public JWKS, and the regime facts that branch consumes.

NOTE: *Scope — marshal / test-rig.* A programmatic foedus is the unattended test-bed's
trust, never a production foedus (the production manor is interactive-only). The role
that performs this establishment (a marshal / test-rig lifecycle) touches the
premise-bearing payor-health question and is *not* asserted as a settled allocation
here — that graduation is operator-owned.

NOTE: *Durable-secret quarantine — the keystone.* The signing key is *stable* (no
per-charge rotation). Its *private* half is fenced in the marshal-only estate home (the
never-committed station file, working name `RBRS_ESTATE_KEYCLOAK_SECRET`) — it never
enters the committed regime and never reaches a production path. Its *public* half is the
committed JWKS, the canonical reference. The discriminator splits public config from the
one durable secret along exactly this seam.

NOTE: *Membrane.* All test-IdP knowledge (that the trust is Keycloak, the realm shape, the
grant) is quarantined in this establishment and the baked vessel DATA.
{rbtgo_manor_affiance}
never learns it — it consumes an opaque JWKS file and an issuer string. The membrane is
what keeps the de-lamination from silently re-laminating.

NOTE: *Harness-proven (260622).* The ride-or-die proof ran this chain GREEN end-to-end from
a clean headless shell: a test-IdP-minted OIDC id_token, via uploaded JWKS, federated into
GCP. The path is proven, not merely doc-confirmed; what remains is the durable REST-only
transform of the expedient gcloud POC (operator cinch: REST-only, no direct gcloud).

The ordered shape (impl deferred — do not read REST calls, function names, paths, or a key
algorithm into it):

//axhos_step
. *Obtain the stable keypair*: the test IdP's realm signing key (a real conformant OIDC
server — preferred, so routine runs exercise a genuine token-endpoint and signature path)
or a marshal-minted self-signed key (fallback). Either way the private half is fenced (see
the quarantine NOTE) and the minter choice is *below the regime* — GCP sees the identical
uploaded-JWKS provider.

//axhos_step
. *Derive the public JWKS*, stripped to standard RSA public-key members (`kty/kid/use/alg/n/e`);
an exporter's `x5c`/`x5t` certificate fields are rejected by GCP's strict parser and must be
removed.

//axhos_step
. *Write the vendor-agnostic core regime facts* with
{rbrf_mechanism}
=
{rbrf_mechanism_programmatic},
naming the uploaded public JWKS
({rbrf_idp_jwks_json}).
The issuer is the test IdP's self-declared `https://`-scheme identifier (string-matched
against the JWT `iss`, never resolved — GCP requires the scheme but a local IdP's
`http://localhost` is rejected); `client-id` equals the JWT `aud`.

//axhos_step
. *Hand off to
{rbtgo_manor_affiance}'s
programmatic branch* to create the pool and provider from the uploaded JWKS. Reference that
operation; do not duplicate its provider-create body.

//axhoc_completion
Successful execution leaves a programmatic
{rbtf_foedus}
standing: a workforce pool and an uploaded-JWKS provider exist under the manor, the regime
names the active foedus with
{rbrf_mechanism_programmatic},
and the matching private signing key is reachable only from the marshal-fenced estate home.
A caller may now acquire a federated token over it via
{rbtgo_foedus_acquire}.
First-contact trips (`client-id` = `aud`, the `https://` issuer scheme, JWT `exp` skew, the
JWKS RSA-member strip) are catalogued in the degenerate-federation test-personas memo and the
config-model proof findings. This operation must not enumerate a vendor, require a setup
guide or console, or pre-bake function names, paths, or a key algorithm.
