//axvd_sheaf axd_normative
This is a normative portion of the {rbs0_imprimatur}: binding — code, tests, and decisions may rely on it.
//axhob_operation
{rbth_ostend}

NOTE: This operation is the reveal ceremony's irreversible act — the
once-per-cycle showing of the standing dry
{rbth_candidate},
performing every assert itself while every push is typed by the operator —
the machine-asserts-around-human-pushes law of the cosmology's command seam.
It never re-cuts: the dry candidate is bit-for-bit what ships, and a terminal
re-cut would break exactly that identity.

NOTE: *The proving is behind it.* The
{rbth_docimasy}
already gated the
{rbth_quarantine},
conducted the preview, and ran the gauntlet; its
{rbth_cachet}
is what admits this operation.
Ostend therefore holds no cloud credential and spends nothing: its acts are
remote reads and the operator's own pushes.

NOTE: *The acts are decoupled in time.* Days may pass between the
{rbth_docimasy}
and the showing, so the ground is re-asserted here, never presumed —
delivered-bytes-bearing drift since the dry cut sends the cycle back to laps,
never forward.

//axhos_step
. *Require the
{rbth_cachet}*:
.. {rbbc_require}
a cachet standing beside the
{rbth_candidate},
its recorded tree hash equal to the standing candidate's — the proof that
the gauntlet ran green against exactly these bytes
.. {rbbc_fatal}
on absence or mismatch, naming the remedy: conduct the
{rbth_docimasy}

//axhos_step
. *Re-assert the ground*:
.. {rbbc_require}
an anonymous read of the
{rbth_quarantine}
repository to 404 — anything else means the quarantine is public or misnamed
.. {rbbc_require}
the quarantine's `POSTULANT_LOCAL` tip equal to the candidate tip — the
preview stands
.. {rbbc_require}
candidate freshness through the one matcher: a scratch re-cut of the
maintainer tree's shipped bytes, tree-equal to the standing candidate, then
disposed
.. {rbbc_fatal}
on any deficit — the cycle returns to
{rbth_essai},
never forward

//axhos_step
. *File-list review — the operator's own eyes*:
.. {rbbc_show}
the candidate's full file list
.. {rbbc_prompt}
and wait: no machine judgment substitutes for the maintainer reading what
they are about to publish

//axhos_step
. *The
{rbth_disclosure}*
(irreversible):
.. {rbbc_store}
the public repository's `main` tip via ls-remote, before
.. {rbbc_show}
the staging push line — explicit public URL,
`POSTULANT_LOCAL:POSTULANT_LOCAL` — for the operator to type
.. {rbbc_require}
after: the public `main` tip unchanged (the before/after assert lives here,
at the push that must not move `main`), and the new `POSTULANT_LOCAL` ref at
the candidate tip

//axhos_step
. *Promotion* (discoverability):
.. {rbbc_show}
the promotion line — fast-forward-only, from a fresh clone or fetch of the
public repository, never the candidate directory; the one
`POSTULANT_LOCAL:main` refspec in the corpus
.. {rbbc_require}
after: `main`'s tree hash equal to the walked candidate tip — the byte claim
is checked, not assumed
.. {rbbc_fatal}
guidance on a refused fast-forward: `main` moved since the cut — stop, never
force, re-cut atop the moved base

//axhoot_typed_output
{rbth_candidate}
. *Close the reveal*:
.. {rbbc_show}
the hand-off to the
{rbth_harbinger}
command for the confirmation coldwalk
{rbth_rig}
against promoted `main`
.. {rbbc_show}
the ceremony-hygiene reminders the ceremony document owns: delete the public
staging branch, the private
{rbth_quarantine},
and the candidate directory once dispositioned

//axhoc_completion
Successful execution conducts the showing of a standing dry
{rbth_candidate}
under a granted
{rbth_cachet}:
every assert machine-performed, every push human-typed, the
{rbth_disclosure}
and the promotion each spelled exactly once, and the promoted `main`
tree-hash-equal to the candidate the laps proved.

// eof
