#!/bin/bash
# Launcher stub - delegates to the veiled hierophant workbench.
# Withheld from delivery by the rbml_launchers/ perambulation row (only the buw
# and rbw launchers ship); it references a veiled workbench path.
source "Tools/buk/bul_launcher.sh"
bul_launch "${BURC_TOOLS_DIR}/rbk/vov_veiled/rbthd/rbthw_workbench.sh" "$@"
