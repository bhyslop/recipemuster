# Use Alpine Linux as the base image
FROM alpine:latest

# Set a label for better container management
LABEL maintainer="Your Name <your.email@example.com>"
LABEL description="A simple Hello World container"

# Set the working directory in the container
WORKDIR /app

# Use ENTRYPOINT for the main command
# CMD can be used to provide default arguments to ENTRYPOINT
ENTRYPOINT ["echo"]
CMD ["Hello, World!"]

