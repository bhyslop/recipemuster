The single
{rbtgr_rubric}
lifecycle command.
Batch-processes all conjure vessels: refreshes pins, generates all
{rbtgr_build_json}
files, copies all build contexts, syncs everything to the
{rbtgr_rubric_repo},
and ensures all triggers exist.

// ⟦axs_inputs⟧

. *None* — inscribe always processes all conjure vessels in `rbev-vessels/`.
Non-conjure vessels (no `RBRV_CONJURE_DOCKERFILE` in {rbrv_regime}) are skipped.

// ⟦axs_preconditions⟧

* {rbrr_gdc_connection_name} and {rbrr_gdc_region} configured in regime
* {rbra_rubric_repo_url} configured and reachable
* Vessel configurations loadable ({rbrv_regime})

// ⟦axs_behavior⟧

. *Pin Staleness Gate*:
.. Verify `RBRR_GCB_PINS_REFRESHED_AT` is less than 1 day old
.. If stale: die with guidance to run pin refresh manually, commit, and re-run inscribe
.. `rbrr_refresh_gcb_pins` remains independently callable for manual inspection

. *Generate All Rubric JSON*:
.. For each conjure vessel in `rbev-vessels/`:
... Load vessel configuration via existing `zrbf_load_vessel()`
... Generate
{rbtgr_build_json}
from step scripts (`rbgjb/*.sh`) and regime pins
... All values baked: GAR coordinates, builder image pins (digest-pinned), vessel identity, platforms, inscribe timestamp
... Substitutions block includes four placeholders in the committed main-repo copy: `_RBGY_RUBRIC_REPO`, `_RBGY_RUBRIC_COMMIT`, `_RBGY_GIT_COMMIT`, and `_RBGY_INSCRIBE_TIMESTAMP` — filled at inscribe time in the {rbtgr_rubric_repo} clone (step 4)
... All other values deterministic: same inputs produce byte-identical output (placeholders ensure the committed copy is stable across commits)

. *Verify All Committed*:
.. Compare each generated JSON against its committed copy in main repo
.. If any differ: STOP and list which vessels have stale committed JSON
.. Operator reviews diffs, commits, and pushes; re-run proceeds
.. All-or-nothing: all vessels must match before sync proceeds

. *Fresh Clone and Sync to Rubric Repo*:
.. Delete local clone at `.rbk/rubric/` if it exists
.. `git clone --depth 1` the
{rbtgr_rubric_repo}
using
{rbra_rubric_repo_url}
.. For each conjure vessel:
... Copy vessel directory to per-vessel directory in clone, excluding `rbrv.env` (regime config is not build material)
.. Fill all four placeholders in each clone copy of
{rbtgr_build_json}:
`_RBGY_RUBRIC_REPO` (actual URL), `_RBGY_GIT_COMMIT` (main repo HEAD), `_RBGY_INSCRIBE_TIMESTAMP` (current invocation) before commit; `_RBGY_RUBRIC_COMMIT` (rubric repo HEAD) after commit
.. One commit and push for all vessels (fully orchestrated — main repo is the reviewed source of truth)

NOTE: Always-fresh-clone eliminates stale state.
The
{rbtgr_rubric_repo}
is small (per-vessel directories with Dockerfile + build context + JSON) — shallow clone is sub-second.

. *Ensure Infrastructure (for each vessel)*:
.. Create
{rbtgi_source_link}
for the {rbtgr_rubric_repo} if it does not exist (via Developer Connect REST API using
{rbtgi_source_connection})
.. Create
{rbtgr_vessel_trigger}
if it does not exist (via Cloud Build REST API)
.. Trigger name derived from constant prefix + vessel sigil

// ⟦axs_outputs⟧

* All conjure vessel directories synced in {rbtgr_rubric_repo} with current build material (including filled rubric repo metadata)
* {rbtgi_source_link} binding the connection to the {rbtgr_rubric_repo}
* {rbtgr_vessel_trigger} for each conjure vessel pointing to its
{rbtgr_build_json}
in the
{rbtgr_rubric_repo}

// ⟦axs_completion⟧

NOTE: Inscribe is idempotent.
A fresh vessel (no directory in rubric repo, no trigger) gets all created.
An existing vessel gets its JSON and build context synced — infrastructure resources are left in place.
One rubric repo commit covers all vessels at their current pin state.
Proceed to
{rbtgo_trigger_build}
to dispatch a build for any individual vessel.
