.claude/jjm/jjg_gallops.json:7218:        "₢BBAAB",
.claude/jjm/jjg_gallops.json:7399:              "text": "## Character\n\nDocumentation pace. Write the operator-facing release procedure as the README \"Release\" section. Concise — 5 human steps. References `rbw-tP` and the `pristine-lifecycle` fixture (built in subsequent paces) by name; neither needs to exist yet for this docket to land. Establishes the contract that the rest of the heat fulfills.\n\n## Work\n\nAdd a \"Release Procedure\" section to README.md with the sequence:\n\n1. **Confirm Payor health** — `tt/rbw-gPR.PayorRefresh.sh` (or `tt/rbw-gPI.PayorInstall.sh` if not yet installed). Payor OAuth is the only prerequisite credential; everything else (governor, retriever, director SAs and their RBRA files) is minted by the qualification.\n2. **Marshal zero, commit** — `tt/rbw-MZ.MarshalZeroes.sh`; commit the result with the marshal-zero signature in the message (signature shape designed in BBAAB)\n3. **Run pristine qualification** — `tt/rbw-tP.QualifyPristine.sh`; ~1 hour wall-clock. Runs the `pristine-lifecycle` fixture (degenerate-zero gate + idempotent throwaway lifecycle of depot + 3 SAs), then mantles canonical governor + deploys RBRA, charters canonical retriever + deploys RBRA, knights canonical director + deploys RBRA, levies canonical depot, then proceeds through reliquary inscribe, hallmark builds, crucible suite, cleanup.\n4. **If green, prep release** — `/rbk-prep-release` (upstream contribution ceremony)\n5. **Push** — to upstream remote\n\nEach step gets one paragraph: what it does, what it costs, what success looks like, what failure means. Failure mode contract called out explicitly: mid-qualification failure means start-over-from-zero, not patch-and-continue.\n\nQuota note for step 3: depot creation is quota-bearing (GCP daily caps), and the `depot-lifecycle` fixture case adds one throwaway depot per run on top of the canonical depot. Two GCP projects per pristine run. Project deletion is asynchronous (30-day soft delete), so each pristine run leaves pending-delete projects in the project list. Accept the cost.\n\n## Verification\n\n- README.md has a \"Release Procedure\" section\n- Section is readable cold by the operator without conversation context\n- Section explicitly names the marshal-zero-just-committed entry contract for `rbw-tP`\n- Section explicitly names the start-over-from-zero failure contract\n- Section explicitly names Payor as the only prerequisite credential\n- Section explicitly names the two-depot quota cost per run\n\n## Out of scope\n\n- Implementing `rbw-tP` itself (subsequent paces)\n- Authoring the `pristine-lifecycle` fixture (BBAAB)\n- Canonical infrastructure setup phase (BBAAC)\n- Revising `rbw-tr` or `rbw-tf` (preserved as-is)\n- Multi-operator workflow (single-operator scope by design)",
.claude/jjm/jjg_gallops.json:7406:              "text": "## Character\n\nDocumentation pace. Write the operator-facing release procedure as the README \"Release\" section. Concise — 5 human steps. References `rbw-tP` and the `pristine-lifecycle` fixture (built in subsequent paces) by name; neither needs to exist yet for this docket to land. Establishes the contract that the rest of the heat fulfills.\n\n## Work\n\nAdd a \"Release Procedure\" section to README.md with the sequence:\n\n1. **Confirm Payor health** — `tt/rbw-gPR.PayorRefresh.sh` (or `tt/rbw-gPI.PayorInstall.sh` if not yet installed). Payor OAuth is the only prerequisite credential; everything else (governor, retriever, director SAs and their RBRA files) is minted by the qualification.\n2. **Marshal zero, commit** — `tt/rbw-MZ.MarshalZeroes.sh`; commit the result with the marshal-zero signature in the message (signature shape designed in BBAAB)\n3. **Run pristine qualification** — `tt/rbw-tP.QualifyPristine.sh`; ~1 hour wall-clock. Runs the `pristine-lifecycle` fixture (degenerate-zero gate + idempotent throwaway lifecycle of depot + 3 SAs), then mantles canonical governor + deploys RBRA, charters canonical retriever + deploys RBRA, knights canonical director + deploys RBRA, levies canonical depot, then proceeds through reliquary inscribe, hallmark builds, crucible suite, cleanup.\n4. **If green, prep release** — `/rbk-prep-release` (upstream contribution ceremony)\n5. **Push** — to upstream remote\n\nEach step gets one paragraph: what it does, what it costs, what success looks like, what failure means. Failure mode contract called out explicitly: mid-qualification failure means start-over-from-zero, not patch-and-continue.\n\nQuota note for step 3: depot creation is quota-bearing (GCP daily caps), and the `depot-lifecycle` fixture case adds one throwaway depot per run on top of the canonical depot. Two GCP projects per pristine run. Project deletion is asynchronous (30-day soft delete), so each pristine run leaves pending-delete projects in the project list. Accept the cost.\n\n## Verification\n\n- README.md has a \"Release Procedure\" section\n- Section is readable cold by the operator without conversation context\n- Section explicitly names the marshal-zero-just-committed entry contract for `rbw-tP`\n- Section explicitly names the start-over-from-zero failure contract\n- Section explicitly names Payor as the only prerequisite credential\n- Section explicitly names the two-depot quota cost per run\n\n## Out of scope\n\n- Implementing `rbw-tP` itself (subsequent paces)\n- Authoring the `pristine-lifecycle` fixture (BBAAB)\n- Canonical infrastructure setup phase (BBAAC)\n- Revising `rbw-tr` or `rbw-tf` (preserved as-is)\n- Multi-operator workflow (single-operator scope by design)",
.claude/jjm/jjg_gallops.json:7419:        "₢BBAAB": {
.claude/jjm/jjg_gallops.json:7425:              "silks": "phase-0-pristine-preflight-detector",
.claude/jjm/jjg_gallops.json:7432:              "silks": "phase-0-pristine-preflight-detector",
.claude/jjm/jjg_gallops.json:7439:              "silks": "phase-0-pristine-preflight-detector",
.claude/jjm/jjg_gallops.json:7446:              "silks": "phase-0-pristine-preflight-detector",
.claude/jjm/jjg_gallops.json:7456:              "text": "## Character\n\nCreates the persistent cloud infrastructure used by all downstream phases of `rbw-tP`. Runs after the pristine-lifecycle fixture (BBAAB) gates and the lifecycle exercises pass. This is the \"real\" mantling/investing plus the \"real\" canonical depot levy — the state that persists for reliquary inscribe, hallmark builds, and crucible runs.\n\nQuota-aware: canonical depot creation is one of the daily quota draws (the other being the throwaway depot in BBAAB's depot-lifecycle case). Two GCP projects per pristine run total.\n\nDepends on BBAAN landing (cult-verb foundation) so the new colophons are available.\n\n## Work\n\nAdd a phase to `rbw-tP` that runs after the pristine-lifecycle fixture passes:\n\n1. **Levy canonical depot** — `tt/rbw-dL.PayorLeviesDepot.sh`; verify GAR repository exists, worker pools provisioned. Use the canonical name (RBRR-derived), distinct from throwaway names used in BBAAB.\n2. **Mantle canonical governor** — `tt/rbw-aM.PayorMantlesGovernor.sh`; deploy governor RBRA file at the configured RBRR path.\n3. **Invest canonical retriever** — `rbw-arI` colophon (BBAAN); RBRA written to assay slot, copied to retriever's production path.\n4. **Invest canonical director** — `rbw-adI` colophon (BBAAN); RBRA written to assay slot, copied to director's production path.\n5. **Roster + verify** — `rbw-arr` (retrievers) and `rbw-adr` (directors) emit per-role observation facts via type-as-extension naming. Governor presence verified implicitly via depot health (Mason SA HEAD probe surfaces in `rbw-dl` depot state per BBAAM Step 4 multi-fact reshape). Verification via fact-file presence and depot-state, not stdout-substring assertion.\n6. **Active depot assertion** — `RBDC_*` derived constants resolve to the canonical depot.\n7. **IAM propagation wait** — phase cannot exit until IAM propagation completes for all three production SAs; downstream phases depend on auth working.\n8. **Discrete checkpoint UX** — phase exits with a clear \"Canonical infrastructure ready. Re-run rbw-tP to proceed; this phase will detect existing canonical state and skip if no marshal-zero has occurred since.\" message.\n\nRe-run discipline: when operator re-invokes `rbw-tP` after BBAAC has already created canonical infra, BBAAC detects existing canonical state (depot exists matching RBRR-derived name, RBRAs present) and skips the create steps. Note tension: the lifecycle fixture's case 1 gate (marshal-zero attestation) will still fail if RBRAs are present, so re-runs after canonical setup require running rbw-tP with case 1 skipped (e.g., resume from BBAAC onward via a phase-skip flag, or by single-case running individual lifecycle cases). Design call to be made during this pace.\n\nPer BBAAN's invest-fatal-on-exists posture: skip-when-present logic must rely on cloud probe before any invest call, since blind invest atop existing cloud SA fatals. Per BBAAN's divest-deletes-production-RBRA: any \"tear down before re-create\" path must invest after divest, never invest atop existing cloud SA.\n\n## Verification\n\n- After phase runs: canonical depot exists, all three RBRAs present at configured paths\n- `rbw-arr` and `rbw-adr` roster outputs surface all expected SAs via observation facts\n- `tt/rbw-dl.PayorListsDepots.sh` shows canonical depot active\n- IAM propagation has completed (subsequent reliquary inscribe + hallmark ops succeed without auth-related retries)\n- Re-running rbw-tP after this phase detects existing canonical infra and skips the levy/mantle/invest/invest steps\n- Quota cost recorded (one canonical depot + one throwaway depot from BBAAB = two daily quota units)\n- Failed levy or mantle surfaces with operator-actionable diagnostic (quota? auth? config?)\n\n## Out of scope\n\n- Throwaway-named SA/depot lifecycle (BBAAB)\n- Reliquary inscribe (BBAAE)\n- Crucible runs (BBAAF)\n- Cleanup at end-of-tier (BBAAG)\n- Resolving the case-1-gate-vs-canonical-state re-run tension beyond a design call (full design lives in this pace)",
.claude/jjm/jjg_gallops.json:7463:              "text": "## Character\n\nCreates the persistent cloud infrastructure used by all downstream phases of `rbw-tP`. Runs after the pristine-lifecycle fixture (BBAAB) gates and the lifecycle exercises pass. This is the \"real\" mantling/investing plus the \"real\" canonical depot levy — the state that persists for reliquary inscribe, hallmark builds, and crucible runs.\n\nQuota-aware: canonical depot creation is one of the daily quota draws (the other being the throwaway depot in BBAAB's depot-lifecycle case). Two GCP projects per pristine run total.\n\nDepends on BBAAN landing (cult-verb foundation) so the new colophons are available.\n\n## Work\n\nAdd a phase to `rbw-tP` that runs after the pristine-lifecycle fixture passes:\n\n1. **Levy canonical depot** — `tt/rbw-dL.PayorLeviesDepot.sh`; verify GAR repository exists, worker pools provisioned. Use the canonical name (RBRR-derived), distinct from throwaway names used in BBAAB.\n2. **Mantle canonical governor** — `tt/rbw-aM.PayorMantlesGovernor.sh`; deploy governor RBRA file at the configured RBRR path.\n3. **Invest canonical retriever** — `rbw-arI` colophon (BBAAN); RBRA written to assay slot, copied to retriever's production path.\n4. **Invest canonical director** — `rbw-adI` colophon (BBAAN); RBRA written to assay slot, copied to director's production path.\n5. **Roster + verify** — `rbw-arr` (retrievers) and `rbw-adr` (directors) emit per-role observation facts via type-as-extension naming. Governor presence verified implicitly via depot health (Mason SA HEAD probe surfaces in `rbw-dl` depot state per BBAAM Step 4 multi-fact reshape). Verification via fact-file presence and depot-state, not stdout-substring assertion.\n6. **Active depot assertion** — `RBDC_*` derived constants resolve to the canonical depot.\n7. **IAM propagation wait** — phase cannot exit until IAM propagation completes for all three production SAs; downstream phases depend on auth working.\n8. **Discrete checkpoint UX** — phase exits with a clear \"Canonical infrastructure ready. Re-run rbw-tP to proceed; this phase will detect existing canonical state and skip if no marshal-zero has occurred since.\" message.\n\nRe-run discipline: when operator re-invokes `rbw-tP` after BBAAC has already created canonical infra, BBAAC detects existing canonical state (depot exists matching RBRR-derived name, RBRAs present) and skips the create steps. Note tension: the lifecycle fixture's case 1 gate (marshal-zero attestation) will still fail if RBRAs are present, so re-runs after canonical setup require running rbw-tP with case 1 skipped (e.g., resume from BBAAC onward via a phase-skip flag, or by single-case running individual lifecycle cases). Design call to be made during this pace.\n\nPer BBAAN's invest-fatal-on-exists posture: skip-when-present logic must rely on cloud probe before any invest call, since blind invest atop existing cloud SA fatals. Per BBAAN's divest-deletes-production-RBRA: any \"tear down before re-create\" path must invest after divest, never invest atop existing cloud SA.\n\n## Verification\n\n- After phase runs: canonical depot exists, all three RBRAs present at configured paths\n- `rbw-arr` and `rbw-adr` roster outputs surface all expected SAs via observation facts\n- `tt/rbw-dl.PayorListsDepots.sh` shows canonical depot active\n- IAM propagation has completed (subsequent reliquary inscribe + hallmark ops succeed without auth-related retries)\n- Re-running rbw-tP after this phase detects existing canonical infra and skips the levy/mantle/invest/invest steps\n- Quota cost recorded (one canonical depot + one throwaway depot from BBAAB = two daily quota units)\n- Failed levy or mantle surfaces with operator-actionable diagnostic (quota? auth? config?)\n\n## Out of scope\n\n- Throwaway-named SA/depot lifecycle (BBAAB)\n- Reliquary inscribe (BBAAE)\n- Crucible runs (BBAAF)\n- Cleanup at end-of-tier (BBAAG)\n- Resolving the case-1-gate-vs-canonical-state re-run tension beyond a design call (full design lives in this pace)",
.claude/jjm/jjg_gallops.json:7470:              "text": "## Character\n\nCreates the persistent cloud infrastructure used by all downstream phases of `rbw-tP`. Runs after the pristine-lifecycle fixture (BBAAB) gates and the lifecycle exercises pass. This is the \"real\" mantling/investing plus the \"real\" canonical depot levy — the state that persists for reliquary inscribe, hallmark builds, and crucible runs.\n\nQuota-aware: canonical depot creation is one of the daily quota draws (the other being the throwaway depot in BBAAB's depot-lifecycle case). Two GCP projects per pristine run total.\n\nDepends on BBAAN landing (cult-verb foundation) so the new colophons are available.\n\n## Work\n\nAdd a phase to `rbw-tP` that runs after the pristine-lifecycle fixture passes:\n\n1. **Levy canonical depot** — `tt/rbw-dL.PayorLeviesDepot.sh`; verify GAR repository exists, worker pools provisioned. Use the canonical name (RBRR-derived), distinct from throwaway names used in BBAAB.\n2. **Mantle canonical governor** — `tt/rbw-aM.PayorMantlesGovernor.sh`; deploy governor RBRA file at the configured RBRR path.\n3. **Invest canonical retriever** — `rbw-arI` colophon (BBAAN); RBRA written to assay slot, copied to retriever's production path.\n4. **Invest canonical director** — `rbw-adI` colophon (BBAAN); RBRA written to assay slot, copied to director's production path.\n5. **Muster + verify** — `rbw-arm` (retrievers) and `rbw-adm` (directors) emit per-role observation facts via type-as-extension naming. Governor presence verified implicitly via depot health (Mason SA HEAD probe surfaces in `rbw-dl` depot state per BBAAM Step 4 multi-fact reshape). Verification via fact-file presence and depot-state, not stdout-substring assertion.\n6. **Active depot assertion** — `RBDC_*` derived constants resolve to the canonical depot.\n7. **IAM propagation wait** — phase cannot exit until IAM propagation completes for all three production SAs; downstream phases depend on auth working.\n8. **Discrete checkpoint UX** — phase exits with a clear \"Canonical infrastructure ready. Re-run rbw-tP to proceed; this phase will detect existing canonical state and skip if no marshal-zero has occurred since.\" message.\n\nRe-run discipline: when operator re-invokes `rbw-tP` after BBAAC has already created canonical infra, BBAAC detects existing canonical state (depot exists matching RBRR-derived name, RBRAs present) and skips the create steps. Note tension: the lifecycle fixture's case 1 gate (marshal-zero attestation) will still fail if RBRAs are present, so re-runs after canonical setup require running rbw-tP with case 1 skipped (e.g., resume from BBAAC onward via a phase-skip flag, or by single-case running individual lifecycle cases). Design call to be made during this pace.\n\nPer BBAAN's invest-fatal-on-exists posture: skip-when-present logic must rely on cloud probe before any invest call, since blind invest atop existing cloud SA fatals. Per BBAAN's divest-deletes-production-RBRA: any \"tear down before re-create\" path must invest after divest, never invest atop existing cloud SA.\n\n## Verification\n\n- After phase runs: canonical depot exists, all three RBRAs present at configured paths\n- `rbw-arm` and `rbw-adm` muster outputs surface all expected SAs via observation facts\n- `tt/rbw-dl.PayorListsDepots.sh` shows canonical depot active\n- IAM propagation has completed (subsequent reliquary inscribe + hallmark ops succeed without auth-related retries)\n- Re-running rbw-tP after this phase detects existing canonical infra and skips the levy/mantle/invest/invest steps\n- Quota cost recorded (one canonical depot + one throwaway depot from BBAAB = two daily quota units)\n- Failed levy or mantle surfaces with operator-actionable diagnostic (quota? auth? config?)\n\n## Out of scope\n\n- Throwaway-named SA/depot lifecycle (BBAAB)\n- Reliquary inscribe (BBAAE)\n- Crucible runs (BBAAF)\n- Cleanup at end-of-tier (BBAAG)\n- Resolving the case-1-gate-vs-canonical-state re-run tension beyond a design call (full design lives in this pace)",
.claude/jjm/jjg_gallops.json:7477:              "text": "## Character\n\nCreates the persistent cloud infrastructure used by all downstream phases of `rbw-tP`. Runs after the pristine-lifecycle fixture (BBAAB) gates and the lifecycle exercises pass. This is the \"real\" mantling/chartering/knighting plus the \"real\" canonical depot levy — the state that persists for reliquary inscribe, hallmark builds, and crucible runs.\n\nQuota-aware: canonical depot creation is one of the daily quota draws (the other being the throwaway depot in BBAAB's depot-lifecycle case). Two GCP projects per pristine run total.\n\n## Work\n\nAdd a phase to `rbw-tP` that runs after the pristine-lifecycle fixture passes:\n\n1. **Levy canonical depot** — `tt/rbw-dL.PayorLeviesDepot.sh`; verify GAR repository exists, worker pools provisioned. Use the canonical name (RBRR-derived), distinct from throwaway names used in BBAAB.\n2. **Mantle canonical governor** — `tt/rbw-aM.PayorMantlesGovernor.sh`; deploy governor RBRA file at the configured RBRR path\n3. **Charter canonical retriever** — `tt/rbw-aC.GovernorChartersRetriever.sh`; deploy retriever RBRA file\n4. **Knight canonical director** — `tt/rbw-aK.GovernorKnightsDirector.sh`; deploy director RBRA file\n5. **List + verify** — `tt/rbw-aL.GovernorListsServiceAccounts.sh` enumerates all three with correct role classifications\n6. **Active depot assertion** — `RBDC_*` derived constants resolve to the canonical depot\n7. **IAM propagation wait** — phase cannot exit until IAM propagation completes for all three production SAs; downstream phases depend on auth working\n8. **Discrete checkpoint UX** — phase exits with a clear \"Canonical infrastructure ready. Re-run rbw-tP to proceed; this phase will detect existing canonical state and skip if no marshal-zero has occurred since.\" message\n\nRe-run discipline: when operator re-invokes `rbw-tP` after BBAAC has already created canonical infra, BBAAC detects existing canonical state (depot exists matching RBRR-derived name, RBRAs present) and skips the create steps. Note tension: the lifecycle fixture's case 1 gate (marshal-zero attestation) will still fail if RBRAs are present, so re-runs after canonical setup require running rbw-tP with case 1 skipped (e.g., resume from BBAAC onward via a phase-skip flag, or by single-case running individual lifecycle cases). Design call to be made during this pace.\n\n## Verification\n\n- After phase runs: canonical depot exists, all three RBRAs present at configured paths\n- `tt/rbw-aL.*` lists all three SAs with correct roles\n- `tt/rbw-dl.PayorListsDepots.sh` shows canonical depot active\n- IAM propagation has completed (subsequent reliquary inscribe + hallmark ops succeed without auth-related retries)\n- Re-running rbw-tP after this phase detects existing canonical infra and skips the levy/mantle/charter/knight steps\n- Quota cost recorded (one canonical depot + one throwaway depot from BBAAB = two daily quota units)\n- Failed levy or mantle surfaces with operator-actionable diagnostic (quota? auth? config?)\n\n## Out of scope\n\n- Throwaway-named SA/depot lifecycle (BBAAB)\n- Reliquary inscribe (BBAAE)\n- Crucible runs (BBAAF)\n- Cleanup at end-of-tier (BBAAG)\n- Resolving the case-1-gate-vs-canonical-state re-run tension beyond a design call (full design lives in this pace)",
.claude/jjm/jjg_gallops.json:7634:              "text": "## Character\n\nMaintenance pace — re-read RELEASE.md with fresh eyes after the rest of the heat lands and apply accumulated fixes the operator identified during heat work but did not address inline.\n\n## Work\n\n- Re-read RELEASE.md cold\n- Apply corrections, clarifications, and restructurings captured during BBAAB–BBAAH execution but deferred from those paces\n- Verify references to `rbw-tP`, the `pristine-lifecycle` fixture, and the marshal-zero signature shape match what BBAAB–BBAAH actually delivered\n- Confirm cross-doc links from RELEASE.md to README.md anchors still resolve\n- Confirm the README's pointer paragraph and reference-tree entry still match RELEASE.md content\n\n## Verification\n\n- Updates committed\n- Operator confirms the doc reads correctly cold from a release-day perspective\n\n## Out of scope\n\n- README.md restructuring beyond cross-doc consistency\n- Re-architecting the release procedure itself (architectural changes belong in a separate heat)",
.claude/jjm/jjg_gallops.json:7645:              "text": "## Character\n\nSibling pace to BBAAB. Implements the simpler half of pristine-lifecycle: depot lifecycle (case 2) and governor lifecycle (case 3). No probe, no assay/canonical shuffle — these cases exercise create/list/destroy patterns without credential probing.\n\nDepends on BBAAB landing first (fixture skeleton + case 1 must compile and register the fixture).\n\n**Lands the shared `rbtdrp_install_throwaway_prefixes` helper that BBAAK reuses** for its case 4/5 prefix-installation step.\n\n## Locked patterns\n\nImplementation patterns are locked in the paddock under \"Implementation patterns (locked during BBAAJ)\". Read those before implementing. Summary:\n\n- **Throwaway prefixes:** edit `.rbk/rbrr.env` + `git add` + `git commit` (mimics human workflow). Env-override doesn't work — `rbrr_cli.sh` sources rbrr.env after kindle, overwriting any pre-set env.\n- **Idempotent install helper:** `rbtdrp_install_throwaway_prefixes` — no-op if prefixes already throwaway. One commit per fixture run.\n- **Tabtarget invocation:** Tabtargets that emit identifying values (project_ids, SA emails) write `rbgp_fact_*` files via `buf_write_fact`; cases consume via `rbtdri_invoke_global` + `rbtdri_read_burv_fact`. Handbook-style tabtargets without facts use raw `Command::new`.\n- **Git shell-out:** `Command::new(\"git\")` style, mirroring jjx/vvx. Explicit file paths. No `--no-verify` or `--amend`.\n\n## Work\n\n### Throwaway-prefix install helper (LANDED)\n\nFunction `rbtdrp_install_throwaway_prefixes(root: &Path) -> Result<(), String>` lives in `rbtdrp_pristine.rs`. Drop the `#[allow(dead_code)]` annotations from the helper, the two pub(crate) consts, and `rbtdrp_replace_prefix_lines` once cases 2/3 call them.\n\n### Fact-file emission (bash + spec, BEFORE Rust cases)\n\n- `Tools/rbk/rbgc_Constants.sh`: add `RBGP_FACT_DEPOT_PROJECT_ID=\"rbgp_fact_depot_project_id\"` and `RBGP_FACT_GOVERNOR_SA_EMAIL=\"rbgp_fact_governor_sa_email\"` consts inside the existing readonly block.\n- `Tools/rbk/rbgp_Payor.sh::rbgp_depot_levy`: after the depot project_id is generated/captured, `buf_write_fact \"${RBGP_FACT_DEPOT_PROJECT_ID}\" \"${z_depot_project_id}\"`.\n- `Tools/rbk/rbgp_Payor.sh::rbgp_governor_mantle`: after the governor SA email is known, `buf_write_fact \"${RBGP_FACT_GOVERNOR_SA_EMAIL}\" \"${z_governor_email}\"` (variable name to be confirmed in the function body).\n- `Tools/rbk/vov_veiled/RBS0-SpecTop.adoc`: add a \"Payor Fact Files\" section parallel to \"Foundry Fact Files\" with both new fact names as linked terms; add attribute references in the mapping section near the existing `rbf_fact_*` entries.\n\n### Case 2: depot-lifecycle (quota-bearing — one GCP project per run)\n\n- Call `rbtdrp_install_throwaway_prefixes(root)` (idempotent).\n- `rbtdri_invoke_global(ctx, \"rbw-dL\", &[\"pristq01\"], &[])` — levy throwaway depot.\n- `rbtdri_read_burv_fact(&result, \"rbgp_fact_depot_project_id\")` — capture depot project_id.\n- `rbtdri_invoke_global(ctx, \"rbw-dl\", &[], &[])` — assert project_id substring present in stdout.\n- `rbtdri_invoke_global(ctx, \"rbw-dU\", &[&project_id], &[(\"BURE_CONFIRM\", \"skip\")])` — soft-delete.\n- `rbtdri_invoke_global(ctx, \"rbw-dl\", &[], &[])` — assert project_id absent / DELETE_REQUESTED.\n\n### Case 3: governor-lifecycle (quota-bearing — one extra GCP project per run)\n\n`rbgp_governor_mantle` requires `RBRR_DEPOT_PROJECT_ID` set. Post-marshal-zero it is blank, so case 3 levies its own throwaway depot (different moniker from case 2's, e.g. `pristg01`), edits rbrr.env to set `RBRR_DEPOT_PROJECT_ID`, runs the governor lifecycle, then unmakes its own depot.\n\n- Call `rbtdrp_install_throwaway_prefixes(root)` (no-op if case 2 already installed).\n- `rbtdri_invoke_global(ctx, \"rbw-dL\", &[\"pristg01\"], &[])` — levy own throwaway depot.\n- `rbtdri_read_burv_fact(&result, \"rbgp_fact_depot_project_id\")` — capture project_id.\n- Edit `.rbk/rbrr.env` to set `RBRR_DEPOT_PROJECT_ID=<project_id>`, `git add`, `git commit`.\n- `rbtdri_invoke_global(ctx, \"rbw-aM\", &[], &[])` — mantle governor.\n- `rbtdri_read_burv_fact(&result, \"rbgp_fact_governor_sa_email\")` — capture SA email.\n- `rbtdri_invoke_global(ctx, \"rbw-aL\", &[], &[])` — assert governor SA email substring present.\n- `rbtdri_invoke_global(ctx, \"rbw-aF\", &[&sa_email], &[])` — forfeit.\n- `rbtdri_invoke_global(ctx, \"rbw-aL\", &[], &[])` — assert governor absent.\n- `rbtdri_invoke_global(ctx, \"rbw-dU\", &[&project_id], &[(\"BURE_CONFIRM\", \"skip\")])` — unmake own depot.\n\n### Section + manifest registration\n\n- Add section `pristine-lifecycle-simple` to `RBTDRP_SECTIONS_PRISTINE_LIFECYCLE` (alongside the existing `pristine-lifecycle-gate` from BBAAB).\n- Add colophon consts to `rbtdrm_manifest.rs`: `RBTDRM_COLOPHON_DEPOT_LEVY` (rbw-dL), `RBTDRM_COLOPHON_DEPOT_LIST` (rbw-dl), `RBTDRM_COLOPHON_DEPOT_UNMAKE` (rbw-dU), `RBTDRM_COLOPHON_GOV_MANTLE` (rbw-aM), `RBTDRM_COLOPHON_GOV_LIST_SAS` (rbw-aL), `RBTDRM_COLOPHON_GOV_FORFEIT` (rbw-aF).\n- Replace `Some(&[])` for `RBTDRM_FIXTURE_PRISTINE_LIFECYCLE` in `rbtdrm_required_colophons` with the six colophons above.\n\n## Verification\n\n- Single-case `depot-lifecycle`: creates+destroys throwaway depot; canonical depot (if present) untouched; rbrr.env commit lands on top of marshal-zero.\n- Single-case `governor-lifecycle`: levies own depot, sets RBRR_DEPOT_PROJECT_ID, mantles+forfeits governor, unmakes depot.\n- Full fixture on marshal-zero state: case 1 passes, install commit lands once, cases 2-3 each clean up.\n- Credentialed single-case verification is operator-action and is reported but not attempted by Claude.\n\n## Out of scope\n\n- Cases 4-5 (sibling pace BBAAK — needs probe + assay/canonical shuffle)\n- Canonical infrastructure setup (BBAAC)\n\n## Merge note (BBAAL parallel chat)\n\nBBAAL changes `rbgp_depot_list` output from 4 columns to 2 columns (drops moniker + region). Case 2 asserts on **project_id substring** (column 1, format-stable) — survives BBAAL's format change either landing order. No file overlap; merge is textually clean. BBAAL has already landed (commit fe3b7595).",
.claude/jjm/jjg_gallops.json:7652:              "text": "## Character\n\nSibling pace to BBAAB. Implements the simpler half of pristine-lifecycle: depot lifecycle (case 2) and governor lifecycle (case 3). No probe, no assay/canonical shuffle — these cases exercise create/list/destroy patterns without credential probing.\n\nDepends on BBAAB landing first (fixture skeleton + case 1 must compile and register the fixture).\n\n**Lands the shared `rbtdrp_install_throwaway_prefixes` helper that BBAAK reuses** for its case 4/5 prefix-installation step.\n\n## Locked patterns\n\nImplementation patterns are locked in the paddock under \"Implementation patterns (locked during BBAAJ)\". Read those before implementing. Summary:\n\n- **Throwaway prefixes:** edit `.rbk/rbrr.env` + `git add` + `git commit` (mimics human workflow). Env-override doesn't work — `rbrr_cli.sh` sources rbrr.env after kindle, overwriting any pre-set env.\n- **Idempotent install helper:** `rbtdrp_install_throwaway_prefixes` — no-op if prefixes already throwaway. One commit per fixture run.\n- **Tabtarget invocation:** Tabtargets that emit identifying values (project_ids, SA emails) write `rbgp_fact_*` files via `buf_write_fact`; cases consume via `rbtdri_invoke_global` + `rbtdri_read_burv_fact`. Handbook-style tabtargets without facts use raw `Command::new`.\n- **Git shell-out:** `Command::new(\"git\")` style, mirroring jjx/vvx. Explicit file paths. No `--no-verify` or `--amend`.\n\n## Work\n\n### Throwaway-prefix install helper (LANDED)\n\nFunction `rbtdrp_install_throwaway_prefixes(root: &Path) -> Result<(), String>` lives in `rbtdrp_pristine.rs`. Drop the `#[allow(dead_code)]` annotations from the helper, the two pub(crate) consts, and `rbtdrp_replace_prefix_lines` once cases 2/3 call them.\n\n### Fact-file emission (bash + spec, BEFORE Rust cases)\n\n- `Tools/rbk/rbgc_Constants.sh`: add `RBGP_FACT_DEPOT_PROJECT_ID=\"rbgp_fact_depot_project_id\"` and `RBGP_FACT_GOVERNOR_SA_EMAIL=\"rbgp_fact_governor_sa_email\"` consts inside the existing readonly block.\n- `Tools/rbk/rbgp_Payor.sh::rbgp_depot_levy`: after the depot project_id is generated/captured, `buf_write_fact \"${RBGP_FACT_DEPOT_PROJECT_ID}\" \"${z_depot_project_id}\"`.\n- `Tools/rbk/rbgp_Payor.sh::rbgp_governor_mantle`: after the governor SA email is known, `buf_write_fact \"${RBGP_FACT_GOVERNOR_SA_EMAIL}\" \"${z_governor_email}\"` (variable name to be confirmed in the function body).\n- `Tools/rbk/vov_veiled/RBS0-SpecTop.adoc`: add a \"Payor Fact Files\" section parallel to \"Foundry Fact Files\" with both new fact names as linked terms; add attribute references in the mapping section near the existing `rbf_fact_*` entries.\n\n### Case 2: depot-lifecycle (quota-bearing — one GCP project per run)\n\n- Call `rbtdrp_install_throwaway_prefixes(root)` (idempotent).\n- `rbtdri_invoke_global(ctx, \"rbw-dL\", &[\"pristq01\"], &[])` — levy throwaway depot.\n- `rbtdri_read_burv_fact(&result, \"rbgp_fact_depot_project_id\")` — capture depot project_id.\n- `rbtdri_invoke_global(ctx, \"rbw-dl\", &[], &[])` — assert project_id substring present in stdout.\n- `rbtdri_invoke_global(ctx, \"rbw-dU\", &[&project_id], &[(\"BURE_CONFIRM\", \"skip\")])` — soft-delete.\n- `rbtdri_invoke_global(ctx, \"rbw-dl\", &[], &[])` — assert project_id absent / DELETE_REQUESTED.\n\n### Case 3: governor-lifecycle (quota-bearing — one extra GCP project per run)\n\n`rbgp_governor_mantle` requires `RBRR_DEPOT_PROJECT_ID` set. Post-marshal-zero it is blank, so case 3 levies its own throwaway depot (different moniker from case 2's, e.g. `pristg01`), edits rbrr.env to set `RBRR_DEPOT_PROJECT_ID`, runs the governor lifecycle, then unmakes its own depot.\n\n- Call `rbtdrp_install_throwaway_prefixes(root)` (no-op if case 2 already installed).\n- `rbtdri_invoke_global(ctx, \"rbw-dL\", &[\"pristg01\"], &[])` — levy own throwaway depot.\n- `rbtdri_read_burv_fact(&result, \"rbgp_fact_depot_project_id\")` — capture project_id.\n- Edit `.rbk/rbrr.env` to set `RBRR_DEPOT_PROJECT_ID=<project_id>`, `git add`, `git commit`.\n- `rbtdri_invoke_global(ctx, \"rbw-aM\", &[], &[])` — mantle governor.\n- `rbtdri_read_burv_fact(&result, \"rbgp_fact_governor_sa_email\")` — capture SA email.\n- `rbtdri_invoke_global(ctx, \"rbw-aL\", &[], &[])` — assert governor SA email substring present.\n- `rbtdri_invoke_global(ctx, \"rbw-aF\", &[&sa_email], &[])` — forfeit.\n- `rbtdri_invoke_global(ctx, \"rbw-aL\", &[], &[])` — assert governor absent.\n- `rbtdri_invoke_global(ctx, \"rbw-dU\", &[&project_id], &[(\"BURE_CONFIRM\", \"skip\")])` — unmake own depot.\n\n### Section + manifest registration\n\n- Add section `pristine-lifecycle-simple` to `RBTDRP_SECTIONS_PRISTINE_LIFECYCLE` (alongside the existing `pristine-lifecycle-gate` from BBAAB).\n- Add colophon consts to `rbtdrm_manifest.rs`: `RBTDRM_COLOPHON_DEPOT_LEVY` (rbw-dL), `RBTDRM_COLOPHON_DEPOT_LIST` (rbw-dl), `RBTDRM_COLOPHON_DEPOT_UNMAKE` (rbw-dU), `RBTDRM_COLOPHON_GOV_MANTLE` (rbw-aM), `RBTDRM_COLOPHON_GOV_LIST_SAS` (rbw-aL), `RBTDRM_COLOPHON_GOV_FORFEIT` (rbw-aF).\n- Replace `Some(&[])` for `RBTDRM_FIXTURE_PRISTINE_LIFECYCLE` in `rbtdrm_required_colophons` with the six colophons above.\n\n## Verification\n\n- Single-case `depot-lifecycle`: creates+destroys throwaway depot; canonical depot (if present) untouched; rbrr.env commit lands on top of marshal-zero.\n- Single-case `governor-lifecycle`: levies own depot, sets RBRR_DEPOT_PROJECT_ID, mantles+forfeits governor, unmakes depot.\n- Full fixture on marshal-zero state: case 1 passes, install commit lands once, cases 2-3 each clean up.\n- Credentialed single-case verification is operator-action and is reported but not attempted by Claude.\n\n## Out of scope\n\n- Cases 4-5 (sibling pace BBAAK — needs probe + assay/canonical shuffle)\n- Canonical infrastructure setup (BBAAC)\n\n## Merge note (BBAAL parallel chat)\n\nBBAAL changes `rbgp_depot_list` output from 4 columns to 2 columns (drops moniker + region). Case 2 asserts on **project_id substring** (column 1, format-stable) — survives BBAAL's format change either landing order. No file overlap; merge is textually clean. BBAAL has already landed (commit fe3b7595).",
.claude/jjm/jjg_gallops.json:7659:              "text": "## Character\n\nSibling pace to BBAAB. Implements the simpler half of pristine-lifecycle: depot lifecycle (case 2) and governor lifecycle (case 3). No probe, no assay/canonical shuffle — these cases exercise create/list/destroy patterns without credential probing.\n\nDepends on BBAAB landing first (fixture skeleton + case 1 must compile and register the fixture).\n\n**Lands the shared `rbtdrp_install_throwaway_prefixes` helper that BBAAK reuses** for its case 4/5 prefix-installation step.\n\n## Locked patterns\n\nImplementation patterns are locked in the paddock under \"Implementation patterns (locked during BBAAJ)\". Read those before implementing. Summary:\n\n- **Throwaway prefixes:** edit `.rbk/rbrr.env` + `git add` + `git commit` (mimics human workflow). Env-override doesn't work — `rbrr_cli.sh` sources rbrr.env after kindle, overwriting any pre-set env.\n- **Idempotent install helper:** `rbtdrp_install_throwaway_prefixes` — no-op if prefixes already throwaway. One commit per fixture run.\n- **Tabtarget shell-out:** `rbtdri_find_tabtarget_global` + raw `Command::new(&tt).current_dir(&root)`, mirroring `rbtdrf_handbook.rs`. No BURV context for these.\n- **Git shell-out:** `Command::new(\"git\")` style, mirroring jjx/vvx. Explicit file paths. No `--no-verify` or `--amend`.\n\n## Work\n\n### Throwaway-prefix install helper (lands here, BBAAK reuses)\n\nFunction `rbtdrp_install_throwaway_prefixes(root: &Path) -> Result<(), String>` in `rbtdrp_pristine.rs`:\n\n- Read `.rbk/rbrr.env`\n- If `RBRR_CLOUD_PREFIX` and `RBRR_RUNTIME_PREFIX` are already `prlc-` and `prlr-`: no-op, return Ok\n- Otherwise: write rbrr.env with the throwaway values, `git add .rbk/rbrr.env`, `git commit -m \"<fixture-attribution message>\"`\n- Return error verdict on any step failure\n\n### Case 2: depot-lifecycle (quota-bearing)\n\n- Call `rbtdrp_install_throwaway_prefixes(root)` (idempotent)\n- `rbw-dL.PayorLeviesDepot` → create throwaway depot (moniker = e.g. `pristq01`)\n- `rbw-dl.PayorListsDepots` → assert throwaway present (substring match on project_id, column 1 — survives BBAAL's planned format change)\n- `rbw-dU.PayorUnmakesDepot` → soft-delete (must pipe confirmation token = full project_id)\n- `rbw-dl` → assert throwaway absent or in `DELETE_REQUESTED`\n- Cost: one GCP project per run, soft-delete graveyard accepted\n\n### Case 3: governor-lifecycle (free)\n\n- Call `rbtdrp_install_throwaway_prefixes(root)` (idempotent — case 2 already commits if running full fixture)\n- `rbw-aM.PayorMantlesGovernor` → mantle throwaway governor\n- `rbw-aL.GovernorListsServiceAccounts` → assert throwaway present\n- `rbw-aF.GovernorForfeitsServiceAccount` → forfeit\n- `rbw-aL` → assert throwaway absent\n- Cost: free\n\n### Section + manifest registration\n\n- Add section `pristine-lifecycle-simple` to `RBTDRP_SECTIONS_PRISTINE_LIFECYCLE` (alongside the existing `pristine-lifecycle-gate` from BBAAB)\n- Add colophon consts to `rbtdrm_manifest.rs`: `RBTDRM_COLOPHON_DEPOT_LEVY` (rbw-dL), `RBTDRM_COLOPHON_DEPOT_LIST` (rbw-dl), `RBTDRM_COLOPHON_DEPOT_UNMAKE` (rbw-dU), `RBTDRM_COLOPHON_GOV_MANTLE` (rbw-aM), `RBTDRM_COLOPHON_GOV_LIST_SAS` (rbw-aL), `RBTDRM_COLOPHON_GOV_FORFEIT` (rbw-aF)\n- Replace `Some(&[])` for `RBTDRM_FIXTURE_PRISTINE_LIFECYCLE` in `rbtdrm_required_colophons` with the six colophons above\n\n## Verification\n\n- Single-case `depot-lifecycle`: creates+destroys throwaway depot; canonical depot (if present) untouched; rbrr.env commit lands on top of marshal-zero\n- Single-case `governor-lifecycle`: same for governor; idempotent install no-op confirms helper correctness\n- Full fixture on marshal-zero state: case 1 passes, install commit lands once, cases 2-3 each clean up\n\n## Out of scope\n\n- Cases 4-5 (sibling pace BBAAK — needs probe + assay/canonical shuffle)\n- Canonical infrastructure setup (BBAAC)\n\n## Merge note (BBAAL parallel chat)\n\nBBAAL changes `rbgp_depot_list` output from 4 columns to 2 columns (drops moniker + region). Case 2 asserts on **project_id substring** (column 1, format-stable) — survives BBAAL's format change either landing order. No file overlap; merge is textually clean.",
.claude/jjm/jjg_gallops.json:7666:              "text": "## Character\n\nSibling pace to BBAAB. Implements the simpler half of pristine-lifecycle: depot lifecycle (case 2) and governor lifecycle (case 3). No probe, no assay/canonical shuffle — these cases exercise create/list/destroy patterns without credential probing.\n\nDepends on BBAAB landing first (fixture skeleton + case 1 must compile and register the fixture).\n\n## Work\n\n### Throwaway prefix mechanism\n\nCases 2-5 use throwaway RBRR prefixes locked in BBAAB design: `RBRR_CLOUD_PREFIX=\"prlc-\"` and `RBRR_RUNTIME_PREFIX=\"prlr-\"`. Mechanism (temp RBRR file vs runtime override) chosen at implementation time — both work; pick the simpler.\n\n### Case 2: depot-lifecycle (quota-bearing)\n\n- Apply throwaway prefixes\n- `rbw-dL.PayorLeviesDepot` → create throwaway depot\n- `rbw-dl.PayorListsDepots` → assert throwaway present\n- `rbw-dU.PayorUnmakesDepot` → soft-delete\n- `rbw-dl` → assert throwaway absent or in DELETE_REQUESTED\n- Cost: one GCP project per run, soft-delete graveyard accepted\n\n### Case 3: governor-lifecycle (free)\n\n- Apply throwaway prefixes\n- `rbw-aM.PayorMantlesGovernor` → mantle throwaway governor\n- `rbw-aL.GovernorListsServiceAccounts` → assert throwaway present\n- `rbw-aF.GovernorForfeitsServiceAccount` → forfeit\n- `rbw-aL` → assert throwaway absent\n- Cost: free\n\n### Registration\n\n- Add cases to `RBTDRC_SECTIONS_PRISTINE_LIFECYCLE` (alongside case 1 from BBAAB)\n- Add required colophons to `rbtdrm_required_colophons` for `pristine-lifecycle`: `rbw-dL`, `rbw-dl`, `rbw-dU`, `rbw-aM`, `rbw-aL`, `rbw-aF`\n\n## Verification\n\n- Single-case `depot-lifecycle`: creates+destroys throwaway depot; canonical depot (if present) untouched\n- Single-case `governor-lifecycle`: same for governor\n- Full fixture on marshal-zero state: case 1 passes, cases 2-3 each clean up\n\n## Out of scope\n\n- Cases 4-5 (sibling pace — needs probe + assay/canonical shuffle)\n- Canonical infrastructure setup (BBAAC)",
.claude/jjm/jjg_gallops.json:7705:              "text": "## Character\n\nSibling pace to BBAAJ. Implements credentialed cases 4 (retriever) and 5 (director) of pristine-lifecycle as theurge cases mirroring the operator's flow through `rbw-aC` / `rbw-aK` plus access-probe verification.\n\nThe character of this work is mechanical — no design conversation should be needed at implementation time. Implementation specifics surface in real-time conversation; this docket holds intent only, not pre-staged choices.\n\n## Scope\n\nCases 4 and 5 each: levy own throwaway depot, install post-levy rbrr fields, mantle throwaway governor, charter/knight role SA, stage RBRA through assay, run access-probe, forfeit role SA, forfeit governor, unmake depot.\n\n## Constraints\n\n- Honor the paddock's Release-branch execution contract: stop on very first failure cleanly and clearly; commits during the run are first-class; no recovery scaffolding.\n- Reuse BBAAJ-locked patterns from the paddock: throwaway-prefix install, fact-file invocation, edit-and-commit for rbrr field installation, section structure inside `RBTDRP_SECTIONS_PRISTINE_LIFECYCLE`.\n- Real-time review of each step before commit. Prior-Claude does not pre-engineer for current-Claude.\n\n## Out of scope\n\n- Cases 1-3 design (BBAAB, BBAAJ).\n- Canonical infrastructure setup (BBAAC).\n- Recovery from mid-run failures beyond `rbw-MZ` + retry.\n- Any test-orthogonality affordances that would conflict with wall-clock budget.",
.claude/jjm/jjg_gallops.json:7712:              "text": "## Character\n\nSibling pace to BBAAB and BBAAJ. Implements the more complex half of pristine-lifecycle: retriever lifecycle (case 4) and director lifecycle (case 5). Each case creates a throwaway governor + the role SA, exercises the credential via access-probe, then forfeits.\n\nIncludes the assay/canonical file movement choreography: SA-creator writes RBRA to `BURD_OUTPUT_DIR`; test stages to `assay/`; test copies `assay/` → canonical role path for probe; test cleans canonical and assay before forfeit. Mirrors operator-flow file movements rather than extending the probe to accept arbitrary paths.\n\n**Absorbs rbrr-completion fix-up.** BBAAJ landed cases 2-3 with `rbtdrp_install_depot_project_id` setting only `RBRR_DEPOT_PROJECT_ID` after levy. `zrbrr_enforce` (which runs on every non-`rbgp_depot_levy` invocation per `rbgp_cli.sh` differential furnish) requires `RBRR_GAR_REPOSITORY` and `RBRR_GCB_POOL_STEM` non-blank too — both blanked by marshal-zero. Credentialed cases 2-5 will fail at the first non-levy command without those fields populated. K lands two new `rbgp_fact_*` files for the values `rbgp_depot_levy` computes (`z_repository_name`, `z_pool_stem`), generalizes the install helper to write all three post-levy fields in one commit, and back-patches cases 2 and 3 to use it. Cases 4 and 5 follow the same pattern.\n\n**BBAAJ landed at `e59c49b8`** — `rbtdrp_install_throwaway_prefixes` and the throwaway-prefix consts are stable and `pub(crate)`. The thread-local invocation context is bridged by `rbtdrc_with_ctx` (now `pub(crate)` after BBAAJ); cases 4-5 use it the same way cases 2-3 do.\n\n## Locked patterns\n\nImplementation patterns are locked in the paddock under \"Implementation patterns (locked during BBAAJ)\". Read those before implementing. Reuse:\n\n- `rbtdrp_install_throwaway_prefixes(root)` — landed `pub(crate)` at e59c49b8; call as first step of cases 4 and 5 (idempotent — no-op if already invoked in same fixture run).\n- `RBTDRP_THROWAWAY_CLOUD_PREFIX` / `RBTDRP_THROWAWAY_RUNTIME_PREFIX` — `pub(crate)` constants; reference by name rather than re-declaring `\"prlc-\"` / `\"prlr-\"` string literals.\n- Field-const pattern — BBAAJ extracted `RBTDRP_FIELD_RBRR_*` as private module-scope consts; mirror this pattern for any new RBRR field-name strings K introduces (see `RBTDRP_FIELD_RBRR_GAR_REPOSITORY`, `RBTDRP_FIELD_RBRR_GCB_POOL_STEM` below).\n- Fact-file invocation pattern (BBAAJ-locked): tabtargets that emit identifying values write `rbgp_fact_*` files via `buf_write_fact`; cases consume via `rbtdri_invoke_global` + `rbtdri_read_burv_fact`.\n- `rbtdrc_with_ctx` (now `pub(crate)` after BBAAJ) for thread-local context bridging — same shape as case 3.\n- `rbtdri_find_tabtarget` (with role as imprint) for `rbtd-ap` — imprint channel.\n- Git shell-out style: `rbtdrp_git_add_and_commit` (BBAAJ-factored helper) handles the `git add <file> && git commit -m <msg>` pair.\n\n## Work\n\n### Step 1 — rbrr-post-levy completion (foundational, lands first)\n\n**Bash + spec edits (do BEFORE Rust changes):**\n\n- `Tools/rbk/rbgc_Constants.sh`: add two consts inside the existing readonly block (next to the `RBGP_FACT_*` pair from BBAAJ):\n  ```\n  readonly RBGP_FACT_GAR_REPOSITORY=\"rbgp_fact_gar_repository\"\n  readonly RBGP_FACT_GCB_POOL_STEM=\"rbgp_fact_gcb_pool_stem\"\n  ```\n- `Tools/rbk/rbgp_Payor.sh::rbgp_depot_levy`: after `z_repository_name` is computed (search for the local declaration), `buf_write_fact \"${RBGP_FACT_GAR_REPOSITORY}\" \"${z_repository_name}\"`. Same for `z_pool_stem` (search the body — assigned during pool-stem derivation around the worker-pool block). Place each `buf_write_fact` call immediately after the value becomes known so a partial-failure run still emits whatever was computed.\n- `Tools/rbk/vov_veiled/RBS0-SpecTop.adoc`: extend the Payor Fact Files section (added in BBAAJ) with two new linked terms — `rbgp_fact_gar_repository` and `rbgp_fact_gcb_pool_stem`. Add their attribute references to the mapping section near the existing `rbgp_fact_*` entries. Apply CMK MCM linked-term shape (attribute reference + anchor + definition with `axpof_fact` voicing).\n\n**Rust edits:**\n\n- `Tools/rbk/rbtd/src/rbtdrp_pristine.rs`:\n  - Add `RBTDRP_FIELD_RBRR_GAR_REPOSITORY` / `RBTDRP_FIELD_RBRR_GCB_POOL_STEM` private consts (mirror the existing `RBTDRP_FIELD_RBRR_DEPOT_PROJECT_ID`).\n  - Add `RBTDRP_FACT_GAR_REPOSITORY` / `RBTDRP_FACT_GCB_POOL_STEM` consts mirroring the bash side.\n  - **Rename** `rbtdrp_install_depot_project_id(root, project_id)` → `rbtdrp_install_post_levy_rbrr(root, project_id, gar_repository, gcb_pool_stem)`. Single rbrr.env read → `rbtdrp_replace_env_fields` with three entries → single write → single commit. Commit message: `\"pristine-lifecycle fixture: set RBRR_DEPOT_PROJECT_ID/RBRR_GAR_REPOSITORY/RBRR_GCB_POOL_STEM\"`.\n  - Patch `rbtdrp_depot_lifecycle_impl` (case 2): after the levy invocation, read all three facts; call `rbtdrp_install_post_levy_rbrr` BEFORE the `list-present` invocation (which runs enforce). Tee the three values to per-case files for diagnostics.\n  - Patch `rbtdrp_governor_lifecycle_impl` (case 3): replace the existing `rbtdrp_install_depot_project_id` call with the new three-arg helper, sourcing all three values from the levy result's facts.\n\n### Step 2 — Cases 4 and 5\n\n#### Pre-conditions (per case)\n\n- Each case pre-checks canonical role path empty (refuse with diagnostic if not — single-case independence requires clean canonical paths for the role being tested AND for governor, since each case mantles its own throwaway governor).\n\n#### Case 4: retriever-lifecycle (one GCP project per run + light read probe)\n\n- Call `rbtdrp_install_throwaway_prefixes(root)` (idempotent).\n- Pre-check: governor and retriever canonical RBRA paths empty.\n- `rbtdri_invoke_global(ctx, \"rbw-dL\", &[\"pristr01\"], &[])` — levy own throwaway depot (distinct moniker from cases 2/3).\n- Read all three post-levy facts (`rbgp_fact_depot_project_id`, `rbgp_fact_gar_repository`, `rbgp_fact_gcb_pool_stem`).\n- Call `rbtdrp_install_post_levy_rbrr(root, project_id, gar_repository, gcb_pool_stem)`.\n- `rbtdri_invoke_global(ctx, \"rbw-aM\", &[], &[])` → mantle throwaway governor; capture governor SA email via `rbgp_fact_governor_sa_email` fact.\n- Move: `BURD_OUTPUT_DIR/$instance.rbra` → `RBDC_ASSAY_RBRA_FILE` → `RBDC_GOVERNOR_RBRA_FILE` (canonical).\n- `rbtdri_invoke_global(ctx, \"rbw-aC\", &[], &[])` → charter throwaway retriever (its RBRA in `BURD_OUTPUT_DIR`).\n- Move: `BURD_OUTPUT_DIR/$instance.rbra` → `RBDC_ASSAY_RBRA_FILE` (overwrite) → `RBDC_RETRIEVER_RBRA_FILE` (canonical).\n- `rbtdri_invoke_imprint(ctx, \"rbtd-ap\", \"retriever\", &[])` → access-probe proves retriever permissions resolve.\n- Cleanup: rm canonical retriever, rm canonical governor, rm assay.\n- `rbtdri_invoke_global(ctx, \"rbw-aF\", &[&retriever_email], &[])` → forfeit retriever.\n- `rbtdri_invoke_global(ctx, \"rbw-aF\", &[&governor_email], &[])` → forfeit governor.\n- `rbtdri_invoke_global(ctx, \"rbw-dU\", &[&project_id], &[(\"BURE_CONFIRM\", \"skip\")])` → unmake own depot.\n\n#### Case 5: director-lifecycle (one GCP project per run + light Cloud Build / GAR write probe)\n\nSame shape as case 4 but with `rbw-aK` (knight director) and `rbtd-ap director`. Moniker: `pristd01`.\n\n#### Open question for implementation time (carried from prior docket revs)\n\n`RBDC_ASSAY_RBRA_FILE` / `RBDC_GOVERNOR_RBRA_FILE` / `RBDC_RETRIEVER_RBRA_FILE` / `RBDC_DIRECTOR_RBRA_FILE` are bash-side derived constants from `rbdc_DerivedConstants.sh`. The Rust fixture either re-derives the paths from RBRR fields or queries them by sourcing rbdc — pick at implementation time. Re-derivation is simpler if the path shape is stable (e.g., `${RBRR_SECRETS_DIR}/<role>/rbra.env` plus assay equivalent).\n\nSimilarly: cases 4/5 need to capture the role-SA email + RBRA file location after charter/knight. Whether `rbgg_charter_retriever` / `rbgg_knight_director` already emit fact files, or need new ones (`rbgp_fact_retriever_sa_email` / `rbgp_fact_director_sa_email`), or whether the email is discoverable via the BURD_OUTPUT_DIR file naming — choose at implementation time. Prefer fact files for symmetry with the governor pattern (BBAAJ).\n\n### Step 3 — Section + manifest registration\n\n- Add section `pristine-lifecycle-credentialed` to `RBTDRP_SECTIONS_PRISTINE_LIFECYCLE` (after `pristine-lifecycle-simple` from BBAAJ). Final layout: §1 gate → §2 simple → §3 credentialed.\n- Add colophon consts to `rbtdrm_manifest.rs`: `RBTDRM_COLOPHON_GOV_CHARTER` (rbw-aC), `RBTDRM_COLOPHON_GOV_KNIGHT` (rbw-aK).\n- `RBTDRM_COLOPHON_ACCESS_PROBE` (rbtd-ap) is already in `RBTDRM_FIXTURE_ACCESS_PROBE` — re-declare for `pristine-lifecycle` for clarity.\n- Extend the `RBTDRM_FIXTURE_PRISTINE_LIFECYCLE` required-colophon array (currently 6 entries from BBAAJ) with the three new entries.\n\n## Verification\n\n- Build green: `tt/rbtd-b.Build.sh`.\n- Unit tests green: `tt/rbtd-t.Test.sh`. The renamed helper invalidates the BBAAJ-landed `rbtdtp_pristine.rs` test surface; if any test references `rbtdrp_install_depot_project_id` by name, update or replace.\n- Fast suite green: `tt/rbtd-s.TestSuite.fast.sh`.\n- Single-case `depot-lifecycle` (post-rename): list/unmake commands now succeed past `zrbrr_enforce` because all three RBRR fields are populated. Operator-action.\n- Single-case `governor-lifecycle`: same, plus governor lifecycle. Operator-action.\n- Single-case `retriever-lifecycle`: creates throwaway depot+gov+ret, probe passes, cleans up cleanly; refuses with diagnostic if canonical role paths occupied. Operator-action.\n- Single-case `director-lifecycle`: same for director. Operator-action.\n- Full fixture on marshal-zero state: cases 1-5 all pass with one rbrr.env install commit (prefixes), then four post-levy commits (one per case 2/3/4/5), each documenting the depot they're running against.\n\n## Out of scope\n\n- Cases 1-3 design (BBAAB and BBAAJ); cases 2/3 are only patched here to consume the new helper, not redesigned.\n- Canonical infrastructure setup (BBAAC).\n- Crash-recovery cleanup of orphan SAs and orphan throwaway depots — handled via soft-delete graveyard discipline (depot) and operator-driven `rbw-aF` (SAs); a partial-failure run accumulates a soft-delete-pending project, which is acceptable per the \"start-over-from-zero\" failure-mode contract.\n\n## Quota footprint (full fixture run)\n\nFour throwaway depots per pristine-lifecycle run (one per case 2, 3, 4, 5). Each goes to `DELETE_REQUESTED` on case cleanup; soft-delete graveyard accepted. Plus one canonical depot from BBAAC for the rest of `rbw-tP`. Five quota-bearing project creations per pristine release-qualification cycle.",
.claude/jjm/jjg_gallops.json:7719:              "text": "## Character\n\nSibling pace to BBAAB and BBAAJ. Implements the more complex half of pristine-lifecycle: retriever lifecycle (case 4) and director lifecycle (case 5). Each case creates a throwaway governor + the role SA, exercises the credential via access-probe, then forfeits.\n\nIncludes the assay/canonical file movement choreography: SA-creator writes RBRA to `BURD_OUTPUT_DIR`; test stages to `assay/`; test copies `assay/` → canonical role path for probe; test cleans canonical and assay before forfeit. Mirrors operator-flow file movements rather than extending the probe to accept arbitrary paths.\n\n**Helper-unblocked at bc161bf8** — BBAAJ landed `rbtdrp_install_throwaway_prefixes` plus the throwaway-prefix constants in isolation; cases 2-3 + section §2 + J's manifest entries remain pending in the parallel chat. K can proceed independently and merge with J's pending work in either landing order.\n\n## Locked patterns\n\nImplementation patterns are locked in the paddock under \"Implementation patterns (locked during BBAAJ)\". Read those before implementing. Reuse:\n\n- `rbtdrp_install_throwaway_prefixes(root)` — landed `pub(crate)` at bc161bf8; call as first step of cases 4 and 5 (idempotent — no-op if already invoked in same fixture run)\n- `RBTDRP_THROWAWAY_CLOUD_PREFIX` / `RBTDRP_THROWAWAY_RUNTIME_PREFIX` — `pub(crate)` constants at bc161bf8; reference by name rather than re-declaring `\"prlc-\"` / `\"prlr-\"` string literals\n- Field-const pattern — BBAAJ extracted `RBTDRP_FIELD_RBRR_CLOUD_PREFIX` as private module-scope const; mirror this pattern for any new RBRR field-name strings K introduces\n- `rbtdri_find_tabtarget_global` + raw `Command::new` for `rbw-aM`, `rbw-aC`, `rbw-aK`, `rbw-aF`\n- `rbtdri_find_tabtarget` (with role as imprint) for `rbtd-ap` — imprint channel\n- Git shell-out style mirroring jjx/vvx (already established in BBAAJ helper) — BBAAK cases shouldn't need additional commits beyond what the install helper makes\n- Unit tests for cases 4-5 land in `rbtdtp_pristine.rs` (created by BBAAJ at bc161bf8) alongside helper tests — pub(crate) reach across module is already wired\n\n## Work\n\n### Pre-conditions\n\n- Each case pre-checks canonical role path empty (refuse with diagnostic if not — single-case independence requires clean canonical paths for the role being tested AND for governor, since each case mantles its own throwaway governor)\n\n### Case 4: retriever-lifecycle (free + light read probe)\n\n- Call `rbtdrp_install_throwaway_prefixes(root)` (idempotent)\n- Pre-check: governor and retriever canonical RBRA paths empty\n- `rbw-aM` → mantle throwaway governor (RBRA lands in `BURD_OUTPUT_DIR`)\n- Move: `BURD_OUTPUT_DIR/$instance.rbra` → `RBDC_ASSAY_RBRA_FILE` → `RBDC_GOVERNOR_RBRA_FILE` (canonical)\n- `rbw-aC` → charter throwaway retriever (its RBRA in `BURD_OUTPUT_DIR`)\n- Move: `BURD_OUTPUT_DIR/$instance.rbra` → `RBDC_ASSAY_RBRA_FILE` (overwrite) → `RBDC_RETRIEVER_RBRA_FILE` (canonical)\n- `rbtd-ap retriever` → access-probe proves retriever permissions resolve\n- Cleanup: rm canonical retriever, rm canonical governor, rm assay\n- `rbw-aF retriever`, `rbw-aF governor`\n\n### Case 5: director-lifecycle (light Cloud Build / GAR write probe)\n\n- Same shape as case 4 but with `rbw-aK` (knight director) and `rbtd-ap director`\n\n### Registration\n\n- Add section `pristine-lifecycle-credentialed` to `RBTDRP_SECTIONS_PRISTINE_LIFECYCLE`. **Bidirectional merge:** if BBAAJ's §2 `pristine-lifecycle-simple` lands first, K appends §3 after §2; if K lands first, K appends §3 directly after §1 and J inserts §2 between them on rebase. Final landing order §1 → §2 → §3 either way.\n- Add colophon consts to `rbtdrm_manifest.rs`: `RBTDRM_COLOPHON_GOV_CHARTER` (rbw-aC), `RBTDRM_COLOPHON_GOV_KNIGHT` (rbw-aK). `RBTDRM_COLOPHON_GOV_MANTLE` (rbw-aM) and `RBTDRM_COLOPHON_GOV_FORFEIT` (rbw-aF) are J-territory if J merges first; if K merges first, K declares them and J reuses.\n- Extend the `RBTDRM_FIXTURE_PRISTINE_LIFECYCLE` required-colophon array. **Bidirectional merge:** extend J's set if present, or set it with K's colophons if J has not landed yet (current state per inspection at bc161bf8: still `Some(&[])` at line 130 of `rbtdrm_manifest.rs`).\n- `rbtd-ap` is already in `RBTDRM_FIXTURE_ACCESS_PROBE` — re-declare for `pristine-lifecycle` for clarity.\n\n## Verification\n\n- Single-case `retriever-lifecycle`: creates throwaway gov+ret, probe passes, cleans up cleanly; refuses with diagnostic if canonical role paths occupied\n- Single-case `director-lifecycle`: same for director\n- Full fixture on marshal-zero state (after BBAAJ also lands): cases 1-5 all pass with one rbrr.env commit (from helper) on top of marshal-zero\n- Pre-merge with BBAAJ: K's cases 4-5 compile, unit-test, and run as single-cases independently; full-fixture integration verification deferred until both paces merged\n\n## Out of scope\n\n- Cases 1-3 (BBAAB and BBAAJ)\n- Canonical infrastructure setup (BBAAC)\n- Crash-recovery cleanup of orphan SAs (handled via soft-delete graveyard discipline)\n\n## Open question for implementation time\n\n`RBDC_ASSAY_RBRA_FILE` / `RBDC_GOVERNOR_RBRA_FILE` / `RBDC_RETRIEVER_RBRA_FILE` / `RBDC_DIRECTOR_RBRA_FILE` are bash-side derived constants from `rbdc_DerivedConstants.sh`. The Rust fixture either re-derives the paths from RBRR fields or queries them by sourcing rbdc — pick at implementation time. Re-derivation is simpler if the path shape is stable (e.g., `${RBRR_SECRETS_DIR}/<role>/rbra.env` plus assay equivalent).",
