=== cmk roe detail companion? ===
claude-cmk-rules-of-engagement.md
cmk-rules-of-engagement-detail.md

=== All references to each renamed basename (whole repo, excluding .git) ===
---- claude-cmk-rules-of-engagement ----
CLAUDE.md:239:@Tools/cmk/claude-cmk-rules-of-engagement.md
Tools/cmk/cmk-rules-of-engagement-detail.md:3:Companion to `claude-cmk-rules-of-engagement.md`. **Not `@`-included** — read on demand when a theme needs depth, or when filing a new instance. The always-on ROE carries the stance; this carries the mechanics, the ledger, and the history, so the always-on layer stays weightless.
---- cmk-rules-of-engagement-detail ----
CLAUDE.md:235:     Companion detail at Tools/cmk/cmk-rules-of-engagement-detail.md is read on
Tools/cmk/claude-cmk-rules-of-engagement.md:3:The dispositional layer of this collaboration. The coding guides (BCG, RCG) govern what we *make*; this governs how we *engage* — especially what we cannot fix. Mechanics, the full taxonomy, the instance ledger, and the lineage live in the companion `cmk-rules-of-engagement-detail.md` (read on demand). This document stays a screen.
Tools/cmk/claude-cmk-rules-of-engagement.md:39:Stances here; the verdict-less-boundary taxonomy, the membrane pattern in full, the instance ledger, and the lineage back to the headwater lenses are in `cmk-rules-of-engagement-detail.md`. The formal premise voicing (`axk_premise`) lives in the domain specs that voice it.
---- vok-claude-context ----
CLAUDE.md:16:- VOK: `@Tools/vok/vok-claude-context.md`
CLAUDE.md:254:@Tools/vok/vok-claude-context.md
---- rbk-claude-acronyms ----
CLAUDE.md:12:- RBK: `@Tools/rbk/rbk-claude-acronyms.md`
CLAUDE.md:289:@Tools/rbk/rbk-claude-acronyms.md
.claude/jjm/jjg_gallops.json:7738:              "jjgtn_text": "## Character\n\nMechanical: relocate per-module acronym mappings from project-root `CLAUDE.md` into the existing per-kit `*-claude-context.md` files. Each kit already has a context file `@`-included from CLAUDE.md, so payload is token-neutral — content moves, not duplicates. Blast radius for future per-module acronym mints collapses to domain-local.\n\n## Move\n\nPer-module acronym tables migrate to per-kit context files:\n\n- **RBK** acronyms (~140 lines under `### Tools Directory` / `#### RBK Subdirectory` in CLAUDE.md) → fold into `Tools/rbk/rbk-claude-tabtarget-context.md` as a new top section, OR mint `Tools/rbk/rbk-claude-acronyms.md` if the tabtarget file feels overloaded (mount-time call).\n- **BUK** acronyms → `Tools/buk/buk-claude-context.md` (which already has \"BUK Concepts\").\n- **JJK** acronyms → `Tools/jjk/vov_veiled/jjk-claude-context.md` (already has \"Job Jockey Configuration\").\n- **CMK** acronyms → `Tools/cmk/vov_veiled/cmk-claude-context.md` (already partially holds CMK file acronyms; consolidate, remove duplicates from CLAUDE.md).\n- **GAD** acronyms → CLAUDE.md currently redirects to `Tools/gad/CLAUDE.md`; verify target completeness, remove redirect stub from project-root.\n- **VOK** acronyms (RCG, VLS, VOS0) → fold into VOK context file (verify path at mount-time — context above mentions VOK but not its claude-context location).\n- **CNMP Lenses** acronyms → fold into the appropriate context file in `../cnmp_CellNodeMessagePrototype/`, or leave if cross-repo coordination required (mount-time call — likely defer).\n\n## Keep in CLAUDE.md\n\n- Directory Permissions\n- Project Prefix Registry (top-level prefix→project map)\n- Prefix Naming Discipline, Quoin Sub-Letter Discipline, Minting Workflow, Kit Infrastructure Suffixes\n- Heredoc Delimiter Selection\n- AsciiDoc Linked Terms guidance\n- Rust Build Discipline, Test Execution\n- Working Preferences\n- Common Workflows\n- Design Principles (Load-Bearing Complexity)\n- `@`-includes to kit context files (unchanged)\n\nThe split is: **rules and top-level prefix→project map stay in CLAUDE.md; per-module file listings live with the kit they describe**.\n\n## Verification\n\n- `tt/rbtd-s.TestSuite.fast.sh` green (handbook-render re-renders full handbook through includes — surfaces broken cross-references).\n- Spot-check three acronym lookups (`RBFH`, `BUC`, `MCM`) — each finds its entry in exactly one place across the loaded context.\n- Project Prefix Registry in CLAUDE.md still enumerates every top-level prefix (`rb`, `bu`, `jj`, `mcm`/`axl`, `gad`, etc.).\n- CLAUDE.md line-count drops materially (current acronym table dominates the file).\n\n## Out of scope\n\n- Adding new acronyms not currently in CLAUDE.md.\n- Restructuring kit context files beyond accommodating the moved tables.\n- Touching the GAD/CMK redirect entries if those already work end-to-end (verify-don't-fix).\n- Cross-repo CNMP coordination if it requires touching a sibling repository.\n\n## Silks\n\n`claude-md-acronym-table-explode` — \"explode\" chosen over \"split\"/\"decompose\" because the work is fan-out across many kit files, not bisection. If RBK ends up minting its own dedicated acronyms file rather than folding into tabtarget context, the silks still fits.",
.claude/jjm/jjg_gallops.json:7745:              "jjgtn_text": "## Character\n\nMechanical: relocate per-module acronym mappings from project-root `CLAUDE.md` into the existing per-kit `*-claude-context.md` files. Each kit already has a context file `@`-included from CLAUDE.md, so payload is token-neutral — content moves, not duplicates. Blast radius for future per-module acronym mints collapses to domain-local.\n\n## Move\n\nPer-module acronym tables migrate to per-kit context files:\n\n- **RBK** acronyms (~140 lines under `### Tools Directory` / `#### RBK Subdirectory` in CLAUDE.md) → fold into `Tools/rbk/rbk-claude-tabtarget-context.md` as a new top section, OR mint `Tools/rbk/rbk-claude-acronyms.md` if the tabtarget file feels overloaded (mount-time call).\n- **BUK** acronyms → `Tools/buk/buk-claude-context.md` (which already has \"BUK Concepts\").\n- **JJK** acronyms → `Tools/jjk/vov_veiled/jjk-claude-context.md` (already has \"Job Jockey Configuration\").\n- **CMK** acronyms → `Tools/cmk/vov_veiled/cmk-claude-context.md` (already partially holds CMK file acronyms; consolidate, remove duplicates from CLAUDE.md).\n- **GAD** acronyms → CLAUDE.md currently redirects to `Tools/gad/CLAUDE.md`; verify target completeness, remove redirect stub from project-root.\n- **VOK** acronyms (RCG, VLS, VOS0) → fold into VOK context file (verify path at mount-time — context above mentions VOK but not its claude-context location).\n- **CNMP Lenses** acronyms → fold into the appropriate context file in `../cnmp_CellNodeMessagePrototype/`, or leave if cross-repo coordination required (mount-time call — likely defer).\n\n## Keep in CLAUDE.md\n\n- Directory Permissions\n- Project Prefix Registry (top-level prefix→project map)\n- Prefix Naming Discipline, Quoin Sub-Letter Discipline, Minting Workflow, Kit Infrastructure Suffixes\n- Heredoc Delimiter Selection\n- AsciiDoc Linked Terms guidance\n- Rust Build Discipline, Test Execution\n- Working Preferences\n- Common Workflows\n- Design Principles (Load-Bearing Complexity)\n- `@`-includes to kit context files (unchanged)\n\nThe split is: **rules and top-level prefix→project map stay in CLAUDE.md; per-module file listings live with the kit they describe**.\n\n## Verification\n\n- `tt/rbtd-s.TestSuite.fast.sh` green (handbook-render re-renders full handbook through includes — surfaces broken cross-references).\n- Spot-check three acronym lookups (`RBFH`, `BUC`, `MCM`) — each finds its entry in exactly one place across the loaded context.\n- Project Prefix Registry in CLAUDE.md still enumerates every top-level prefix (`rb`, `bu`, `jj`, `mcm`/`axl`, `gad`, etc.).\n- CLAUDE.md line-count drops materially (current acronym table dominates the file).\n\n## Out of scope\n\n- Adding new acronyms not currently in CLAUDE.md.\n- Restructuring kit context files beyond accommodating the moved tables.\n- Touching the GAD/CMK redirect entries if those already work end-to-end (verify-don't-fix).\n- Cross-repo CNMP coordination if it requires touching a sibling repository.\n\n## Silks\n\n`claude-md-acronym-table-explode` — \"explode\" chosen over \"split\"/\"decompose\" because the work is fan-out across many kit files, not bisection. If RBK ends up minting its own dedicated acronyms file rather than folding into tabtarget context, the silks still fits.",
.claude/jjm/jjg_gallops.json:9690:              "jjgtn_text": "## Character\nMechanical rename + source-line fixups per module. Low judgment, except confirming each rbh0 module's gestalt ModuleName (guessed below).\n\n## Goal\nBring all 6 decomposed modules' kindle \"main submodule\" to the BCG 0-trick entry form `«prefix»0_«ModuleName».sh` (the single inclusion-guard + whole kindle). Every CLI already conforms (`«prefix»0_cli.sh`); only the kindle entry file is non-conforming. Authority: BCG \"Module Decomposition\" + Fading Memory FM-002.\n\n## Renames\nGroup 1 — k-form (FM-002 direct):\n- rbldk_Kindle.sh → rbld0_Lode.sh\n- rbfck_Kindle.sh → rbfc0_FoundryCore.sh\n- rbflk_Kindle.sh → rbfl0_FoundryLedger.sh\n\nGroup 2 — rbh0 family, b-base → 0_ModuleName (gestalt names GUESSED — confirm at mount):\n- rbhob_base.sh → rbho0_Onboarding.sh\n- rbhpb_base.sh → rbhp0_Payor.sh\n- rbhwb_base.sh → rbhw0_Windows.sh\n\n## Per rename\nRename the file, then `grep` the OLD basename across the tree to find every sourcer and update each `source` line. Sourcers cross modules: `rbfck_Kindle.sh` is sourced by `rbldk_Kindle.sh` as well as its own `rbfc0_cli.sh`, so the grep-for-sourcers step is load-bearing, not a single-line edit.\n\n## Cinched\n- CLIs already 0-form — touch them only for the source-line edit.\n- rbh0 `0` slot already holds content landings (`rbho0_start_here.sh`, `rbhw0_top.sh`); the renamed kindle entry is an ADDITIONAL 0-file, not a replacement for those.\n- No behavior change — pure rename + sourcing-path edits.\n\n## Done\nAll 6 kindle entries are `«prefix»0_«ModuleName».sh`; every sourcer updated (verified by grepping the old basenames → zero hits); `rbk-claude-acronyms.md` RBLDK/RBFCK/RBFLK + rbh*b entries updated to the new names; the relevant suite is green (dogfight + lode-lifecycle exercise rbld/rbfc/rbfl; handbook display smoke if one exists for rbh0).",
.claude/jjm/retired/jjh_b260521-r260530-rbk-10-mvp-modern-organization-features.md:491:RBS0-SpecTop.adoc, RBSCIG-IamGrantContracts.adoc, RBSCIP-IamPropagation.adoc reference rbgu_ symbols; rbk-claude-acronyms.md has the RBGU entry. Update all four: replace renamed symbols, retire the RBGU acronym entry, add rbuh / rba / rbge entries (NOT rbuj), and note rbgo gains the transient predicate. Also correct rbgc_Constants.sh's comment reference to rbgu_http_json (-> rbuh_json) — comment-only, not behavior.
.claude/jjm/retired/jjh_b260521-r260530-rbk-10-mvp-modern-organization-features.md:590:RBS0-SpecTop.adoc, RBSCIG-IamGrantContracts.adoc, RBSCIP-IamPropagation.adoc reference rbgu_ symbols; rbk-claude-acronyms.md has the RBGU entry. Update all four: replace renamed symbols, retire the RBGU acronym entry, add rbuh / rba / rbge entries (NOT rbuj), and note rbgo gains the transient predicate. Also correct rbgc_Constants.sh's comment reference to rbgu_http_json (-> rbuh_json) — comment-only, not behavior.
.claude/jjm/retired/jjh_b260521-r260530-rbk-10-mvp-modern-organization-features.md:683:RBS0-SpecTop.adoc, RBSCIG-IamGrantContracts.adoc, RBSCIP-IamPropagation.adoc reference rbgu_ symbols; rbk-claude-acronyms.md has the RBGU entry. Update all four: replace renamed symbols, retire the RBGU acronym entry, add rbuh / rba / rbge entries (NOT rbuj), and note rbgo gains the transient predicate. Also correct rbgc_Constants.sh's comment reference to rbgu_http_json (-> rbuh_json) — comment-only, not behavior.
.claude/jjm/retired/jjh_b260521-r260530-rbk-10-mvp-modern-organization-features.md:769:RBS0-SpecTop.adoc, RBSCIG-IamGrantContracts.adoc, RBSCIP-IamPropagation.adoc reference rbgu_ symbols; rbk-claude-acronyms.md has the rbgu entry. Update all four: replace renamed symbols, retire the RBGU acronym entry, add rbuh/rbuj/rba/rbge entries.
.claude/jjm/retired/jjh_b260521-r260530-rbk-10-mvp-modern-organization-features.md:824:··x RBS0-SpecTop.adoc, RBSCIG-IamGrantContracts.adoc, RBSCIP-IamPropagation.adoc, rba_Auth.sh, rbfc_FoundryCore.sh, rbfd_FoundryDirectorBuild.sh, rbfd_cli.sh, rbfk_cli.sh, rbfl_FoundryLedger.sh, rbfl_cli.sh, rbfv_FoundryVerify.sh, rbfv_cli.sh, rbga_ArtifactRegistry.sh, rbga_cli.sh, rbgb_Buckets.sh, rbgb_cli.sh, rbgc_Constants.sh, rbge_Rest.sh, rbgg_Governor.sh, rbgg_cli.sh, rbgi_IAM.sh, rbgo_OAuth.sh, rbgp_Payor.sh, rbgp_cli.sh, rbgu_Utility.sh, rbgv_AccessProbe.sh, rbgv_cli.sh, rbhp0_cli.sh, rbk-claude-acronyms.md, rbuh_Http.sh
.claude/jjm/retired/jjh_b260514-r260529-rbk-09-mvp-regime-sprues-misc.md:2430:·x··········· BCG-BashConsoleGuide.md, apcw-D.Deploy.sh, apcw-b.Build.sh, apcw-ba.BatchAssay.sh, apcw-cb.ContainerBuild.sh, apcw-ci.ContainerStatus.sh, apcw-cs.ContainerStart.sh, apcw-cx.ContainerStop.sh, apcw-dr.DictionaryRefresh.sh, apcw-fl.FixtureLoad.geriatric.sh, apcw-fl.FixtureLoad.progress.sh, apcw-nsa.NeuralStanfordAssay.sh, apcw-nsi.NeuralStanfordInstall.sh, apcw-r.Run.sh, apcw-t.Test.sh, buq_qualify.sh, burd_regime.sh, buut_tabtarget.sh, buw-hj0.HandbookJurisdictionTop.sh, buw-hjl.HandbookJurisdictionLinux.sh, buw-hjm.HandbookJurisdictionMacos.sh, buw-hjw.HandbookJurisdictionWindows.sh, buw-jpCL.CaparisonLinux.sh, buw-jpCM.CaparisonMacos.sh, buw-jpCW.CaparisonWindows.sh, buw-jpGb.GarrisonBash.sh, buw-jpGc.GarrisonCygwin.sh, buw-jpGw.GarrisonWsl.sh, buw-jpS.PrivilegedSsh.sh, buw-jwc.WorkloadCommandFile.sh, buw-jwk.WorkloadKnock.sh, buw-jws.WorkloadInteractiveSession.sh, buw-qsc.QualifyShellCheck.sh, buw-rcr.RenderConfigRegime.sh, buw-rcv.ValidateConfigRegime.sh, buw-rer.RenderEnvironmentRegime.sh, buw-rev.ValidateEnvironmentRegime.sh, buw-rnl.ListNodeRegime.sh, buw-rnr.RenderNodeRegime.sh, buw-rnv.ValidateNodeRegime.sh, buw-rpl.ListPrivilegeRegime.sh, buw-rpr.RenderPrivilegeRegime.sh, buw-rpv.ValidatePrivilegeRegime.sh, buw-rsr.RenderStationRegime.sh, buw-rsv.ValidateStationRegime.sh, buw-st.BukSelfTest.sh, buw-tt-cbl.CreateTabTargetBatchLogging.sh, buw-tt-cbn.CreateTabTargetBatchNolog.sh, buw-tt-cil.CreateTabTargetInteractiveLogging.sh, buw-tt-cin.CreateTabTargetInteractiveNolog.sh, buw-tt-cl.CreateLauncher.sh, buw-tt-ll.ListLaunchers.sh, buw-xd.Delay.sh, jjw-tfP1.ProvisionPhase1.sh, jjw-tfP2.ProvisionPhase2.cerebro.sh, jjw-tfP2.ProvisionPhase2.localhost.sh, jjw-tfS.TestFundusSingle.localhost.sh, jjw-tfs.TestFundusScenario.cerebro.sh, jjw-tfs.TestFundusScenario.localhost.sh, rbk-claude-acronyms.md, rbw-HWdc.DockerContextDiscipline.sh, rbw-HWdd.DockerDesktop.sh, rbw-Is.IfritSortie.moriah.sh, rbw-Is.IfritSortie.tadmor.sh, rbw-MG.MarshalGenerate.sh, rbw-MP.MarshalProofs.sh, rbw-MZ.MarshalZeroes.sh, rbw-Occ.OnboardingConfigureEnvironment.sh, rbw-Ocd.OnboardingCredentialDirector.sh, rbw-Ocr.OnboardingCredentialRetriever.sh, rbw-Oda.OnboardingDirectorAirgap.sh, rbw-Odb.OnboardingDirectorBind.sh, rbw-Odf.OnboardingDirectorFirstBuild.sh, rbw-Odg.OnboardingDirectorGraft.sh, rbw-Ofc.OnboardingFirstCrucible.sh, rbw-Og.OnboardingGovernor.sh, rbw-Op.OnboardingPayor.sh, rbw-Ots.OnboardingTadmorSecurity.sh, rbw-aM.PayorMantlesGovernor.sh, rbw-acd.CheckDirectorCredential.sh, rbw-acg.CheckGovernorCredential.sh, rbw-acp.CheckPayorCredential.sh, rbw-acr.CheckRetrieverCredential.sh, rbw-adD.GovernorDivestsDirector.sh, rbw-adI.GovernorInvestsDirector.sh, rbw-adr.GovernorRostersDirectors.sh, rbw-arD.GovernorDivestsRetriever.sh, rbw-arI.GovernorInvestsRetriever.sh, rbw-arr.GovernorRostersRetrievers.sh, rbw-cC.Charge.ccyolo.sh, rbw-cC.Charge.moriah.sh, rbw-cC.Charge.pluml.sh, rbw-cC.Charge.srjcl.sh, rbw-cC.Charge.tadmor.sh, rbw-cKB.KludgeBottle.sh, rbw-cKS.KludgeSentry.sh, rbw-cQ.Quench.ccyolo.sh, rbw-cQ.Quench.moriah.sh, rbw-cQ.Quench.pluml.sh, rbw-cQ.Quench.srjcl.sh, rbw-cQ.Quench.tadmor.sh, rbw-cS.SshTo.ccyolo.sh, rbw-cS.SshTo.moriah.sh, rbw-cb.Bark.moriah.sh, rbw-cb.Bark.pluml.sh, rbw-cb.Bark.srjcl.sh, rbw-cb.Bark.tadmor.sh, rbw-cf.Fiat.moriah.sh, rbw-cf.Fiat.pluml.sh, rbw-cf.Fiat.srjcl.sh, rbw-cf.Fiat.tadmor.sh, rbw-ch.Hail.sh, rbw-cic.CrucibleIsCharged.sh, rbw-cr.Rack.sh, rbw-cs.Scry.sh, rbw-cw.Writ.moriah.sh, rbw-cw.Writ.pluml.sh, rbw-cw.Writ.srjcl.sh, rbw-cw.Writ.tadmor.sh, rbw-dE.DirectorEnshrinesVessel.sh, rbw-dI.DirectorInscribesReliquary.sh, rbw-dL.PayorLeviesDepot.sh, rbw-dU.PayorUnmakesDepot.sh, rbw-dY.DirectorYokesReliquaryAllVessels.sh, rbw-di.DepotInfo.sh, rbw-dl.PayorListsDepots.sh, rbw-fA.DirectorAbjuresHallmark.sh, rbw-fO.DirectorOrdainsHallmark.sh, rbw-fV.DirectorVouchesHallmarks.sh, rbw-fhc.HygieneCheck.sh, rbw-fhv.HygieneCheckVessel.sh, rbw-fk.LocalKludge.sh, rbw-fpc.RetrieverPlumbsCompact.sh, rbw-fpf.RetrieverPlumbsFull.sh, rbw-fs.RetrieverSummonsHallmark.sh, rbw-ft.RetrieverTalliesHallmarks.sh, rbw-gPE.PayorEstablish.sh, rbw-gPI.PayorInstall.sh, rbw-gPR.PayorRefresh.sh, rbw-gq.QuotaBuild.sh, rbw-h0.HandbookTOP.sh, rbw-hw.HandbookWindows.sh, rbw-iJe.DirectorJettisonsEnshrinement.sh, rbw-iJh.DirectorJettisonsHallmarkImage.sh, rbw-iJr.DirectorJettisonsReliquaryImage.sh, rbw-iae.DirectorAuditsEnshrinements.sh, rbw-iah.DirectorAuditsHallmarks.sh, rbw-iar.DirectorAuditsReliquaries.sh, rbw-irh.DirectorRekonsHallmark.sh, rbw-irr.DirectorRekonsReliquary.sh, rbw-iwe.DirectorWrestsEnshrinedImage.sh, rbw-iwh.DirectorWrestsHallmarkImage.sh, rbw-iwr.DirectorWrestsReliquaryImage.sh, rbw-ni.NameplateInfo.sh, rbw-nv.ValidateNameplates.sh, rbw-o.ONBOARDING.sh, rbw-ral.ListAuthRegimes.sh, rbw-rar.RenderAuthRegime.sh, rbw-rav.ValidateAuthRegime.sh, rbw-rdc.CheckDepotRegime.sh, rbw-rdi.InscribeDepotRegime.sh, rbw-rdr.RenderDepotRegime.sh, rbw-rdv.ValidateDepotRegime.sh, rbw-rnl.ListNameplateRegime.sh, rbw-rnr.RenderNameplateRegime.sh, rbw-rnv.ValidateNameplateRegime.sh, rbw-ror.RenderOauthRegime.sh, rbw-rov.ValidateOauthRegime.sh, rbw-rpr.RenderPayorRegime.sh, rbw-rpv.ValidatePayorRegime.sh, rbw-rrr.RenderRepoRegime.sh, rbw-rrv.ValidateRepoRegime.sh, rbw-rsr.RenderStationRegime.sh, rbw-rsv.ValidateStationRegime.sh, rbw-rvl.ListVesselRegime.sh, rbw-rvr.RenderVesselRegime.sh, rbw-rvv.ValidateVesselRegime.sh, rbw-tK.KludgeCycle.tadmor.sh, rbw-tO.OrdainCycle.tadmor.sh, rbw-tr.QualifyRelease.sh, study-mpt.Run.FULL.sh, study-mpt.Run.api-FULL.sh, study-mpt.Run.smoke.sh, vow-F.Freshen.sh, vow-R.ParcelRelease.sh, vow-b.Build.sh, vow-c.Clean.sh, vow-r.RunVVX.sh, vow-t.Test.sh, vslk-i.InstallSlickEditProject.sh, vvw-r.RunVVX.sh, z-launcher.sh
.claude/jjm/retired/jjh_b260514-r260519-rbk-11-mvp-tactical.md:1766:····x········· .gitignore, RBSCC-crucible_charge.adoc, RBSCH-charge_hook.adoc, claude-netdiags.ipynb, compose.yml, ipycl-name-genie.ipynb, namegenie.py, netdiag.py, rbk-claude-acronyms.md, rbnnh_compose.yml, rbnnh_post_charge.sh
---- rbk-claude-tabtarget-context ----
CLAUDE.md:291:@Tools/rbk/rbk-claude-tabtarget-context.md
Tools/rbk/rbcc_Constants.sh:37:readonly RBCC_tabtarget_context_file="${RBCC_KIT_DIR}/rbk-claude-tabtarget-context.md"
.claude/jjm/jjg_gallops.json:1979:              "jjgtn_text": "## Character\n\nSmall infrastructure pace with a downstream pedagogy revision. The bottle has `rbw-cKB` (KludgeBottle) — nameplate-aware, builds and drives the hallmark into the nameplate in one step. The sentry has no such symmetric tool; the learner runs `rbw-fk rbev-sentry-deb-tether`, copies the hallmark tag from terminal output, opens `rbrn.env`, and pastes into `RBRN_SENTRY_HALLMARK` by hand.\n\nThe first-crucible handbook (`rbho_first_crucible()` in `rbhofc_first_crucible.sh`) currently rationalizes the asymmetry (\"the sentry is shared across nameplates, so you kludge it standalone and drive the hallmark yourself\"). In practice this is copy/paste friction the learner hits immediately after meeting the toolchain, and the handbook already teaches \"what a hallmark is and where it lives\" via the bottle step one screen earlier.\n\nCognitive posture: mechanical symmetry restoration plus trimming of the teaching prose that only existed to justify the missing tool. Parallel-safe with other A6 work — touches `rbob_bottle.sh`, the zipper, a new tabtarget, and the first-crucible handbook file.\n\n## Docket\n\n### Problem\n\nStep 1.1 + Step 2 of `rbho_first_crucible()` force the learner to transcribe a hallmark tag between terminal output and a config file. Mechanically identical to the bottle path one step earlier, but unautomated.\n\n### Approach\n\nMirror the bottle pattern:\n\n| Bottle (today) | Sentry (target) |\n|---|---|\n| `rbob_kludge()` in `rbob_bottle.sh` — kludges bottle vessel, drives hallmark into `RBRN_BOTTLE_HALLMARK` | New `rbob_kludge_sentry()` — kludges sentry vessel (from `RBRN_SENTRY_VESSEL`), drives hallmark into `RBRN_SENTRY_HALLMARK` |\n| `rbw-cKB.KludgeBottle.sh` (param1 moniker) | New `rbw-cKS.KludgeSentry.sh` (param1 moniker) |\n| Zipper group entry for `rbw-cKB` | Zipper group entry for `rbw-cKS` |\n\n### Commit cadence — load-bearing\n\n`rbfd_kludge()` (at `rbfd_FoundryDirectorBuild.sh:1213-1218`) hard-dies on any unstaged or staged change. Both auto-driving kludges leave the tree dirty after driving their hallmark. Consequence: **with two auto-driving kludges, the learner must commit between them**, not bundle at the end.\n\n**Current flow (manual sentry paste):** clean → `rbw-fk` (tree stays clean) → `rbw-cKB` (tree dirty: bottle hallmark) → manual paste sentry hallmark (tree dirty: both hallmarks, single commit point) → **one commit** → charge.\n\n**New flow (symmetric kludges):** clean → `rbw-cKS` (tree dirty: sentry hallmark) → **commit #1** (forced — next kludge gates on clean tree) → `rbw-cKB` (tree dirty: bottle hallmark) → **commit #2** → charge.\n\nNet change for the learner: manual-paste eliminated, one extra git commit added. The handbook must teach this commit-between-kludges cadence explicitly — it's the one new invariant the symmetry introduces.\n\n### Implementation steps\n\n1. Add `rbob_kludge_sentry()` in `Tools/rbk/rbob_bottle.sh` — structural twin of `rbob_kludge()`, targeting `RBRN_SENTRY_VESSEL` / `RBRN_SENTRY_HALLMARK`. Use `zrbob_drive_hallmark` (already generic on the variable name).\n2. Add thin dispatcher `tt/rbw-cKS.KludgeSentry.sh` (nolog launcher pattern matching `rbw-cKB`).\n3. Enroll `rbw-cKS` in `Tools/rbk/rbz_zipper.sh` under the Crucible group (next to `rbw-cKB`). Channel: param1.\n4. Add linked-term constant (if one exists for `rbw-cKB` — check `rbyc_common.sh` or the `RBZ_CRUCIBLE_KLUDGE_*` family in the zipper) so the handbook can reference it via a named constant, not a raw string.\n5. Update `Tools/rbk/rbh0/rbhofc_first_crucible.sh`:\n   - Replace Step 1.1's `rbw-fk` invocation and the copy/paste instruction with the new nameplate-aware call.\n   - Insert a **commit step between the two kludges** with concrete `git add` / `git commit` lines and a short explanation (\"kludge needs a clean tree, so commit the sentry hallmark before kludging the bottle\").\n   - Collapse old Step 2 (manual RBRN_SENTRY_HALLMARK edit) into a terminal-step commit for the bottle hallmark, symmetric with the post-sentry commit.\n   - Reframe the \"two kludge commands, two different jobs\" comparison (lines ~197–204) — both are now nameplate-aware; the remaining distinction (sentry shared across nameplates vs bottle per-nameplate) can be mentioned briefly or dropped.\n   - Keep the existing `buh_warn \"${RBYC_KLUDGE} requires a clean git tree.\"` — its importance *increases* under the new flow.\n   - Update probe names/comments if any reference the old flow.\n   - Update the \"Iteration loop\" section at the end (lines ~391–412) — currently describes a one-kludge cycle; with symmetric kludges it may still be one-kludge (the sentry rarely changes), but the commit cadence framing needs a once-over.\n6. Regenerate `CLAUDE.consumer.md` / `rbk-claude-tabtarget-context.md` colophon table if auto-generation is wired up (per `₢A6AAF` docket, the regenerator is the source of truth).\n\n### Pedagogy question for mount time\n\nThe original asymmetry carried a teaching moment: \"here's the file, here's the field, here's how hallmarks live in nameplates.\" With the symmetric tool in place, decide whether to:\n\n- **(a)** Drop the manual-paste teaching entirely — the bottle step already shows the same machinery via `zrbob_drive_hallmark` output, and a one-line `buh_line` can mention where hallmarks live without a manual exercise.\n- **(b)** Keep a vestigial \"open the nameplate and verify\" probe step — passive inspection instead of active editing.\n\nDefault to (a) unless the review at mount time argues for (b).\n\n### Coordination\n\n- `₢A6AAu` (review-onboarding-director-first-kludge, currently earlier on the rail but moving behind this pace) is the natural downstream beneficiary — the review proceeds against the cleaned-up track, not the old copy/paste flow.\n- No interaction with director tracks (cloud build, airgap, bind, graft) — those paths don't touch sentry kludge.\n\n### Requires\n\n- Nothing — the bottle path (`rbob_kludge`, `zrbob_drive_hallmark`) is the reference implementation already landed.\n\n### Out of scope\n\n- Any change to `rbw-fk` (LocalKludge) — it remains the standalone vessel-kludge entry point for non-nameplate contexts (and for the sentry-deb-airgap path where no single nameplate owns it).\n- Ordain-side symmetric sentry (cloud build). Director cloud-build tracks already drive sentry hallmarks through their own pipeline; this pace is kludge-only.\n- Changes to the airgap handbook track (`₢A6AAV`/`₢A6AA1`) — that track uses its own moriah-nameplate flow.\n\n### Validation\n\nEnd-to-end dry run against a fresh `ccyolo`:\n\n```\n# Clean tree\ntt/rbw-cKS.KludgeSentry.sh ccyolo\n# Expect: sentry image built, RBRN_SENTRY_HALLMARK field rewritten, tree dirty in exactly rbrn.env\ngit add .rbk/ccyolo/rbrn.env\ngit commit -m \"kludge sentry hallmark into ccyolo nameplate\"\n\ntt/rbw-cKB.KludgeBottle.sh ccyolo\n# Expect: bottle image built, RBRN_BOTTLE_HALLMARK field rewritten, tree dirty in exactly rbrn.env\ngit add .rbk/ccyolo/rbrn.env\ngit commit -m \"kludge bottle hallmark into ccyolo nameplate\"\n\ntt/rbw-cC.Charge.ccyolo.sh\n# Expect: charge succeeds (nameplate is committed)\n```\n\nAlso run `tt/rbw-Ofc.OnboardingFirstCrucible.sh` end-to-end and confirm handbook output no longer instructs copy/paste and teaches the two-commit cadence clearly.\n\n### Failure mode to verify\n\nRun `rbw-cKS ccyolo` then immediately `rbw-cKB ccyolo` without committing — confirm `rbfd_kludge`'s dirty-tree guard fires with a clear error. The handbook should anticipate this and guide the learner to commit; the runtime guard is the safety net.",
.claude/jjm/jjg_gallops.json:1986:              "jjgtn_text": "## Character\n\nSmall infrastructure pace with a downstream pedagogy revision. The bottle has `rbw-cKB` (KludgeBottle) — nameplate-aware, builds and drives the hallmark into the nameplate in one step. The sentry has no such symmetric tool; the learner runs `rbw-fk rbev-sentry-deb-tether`, copies the hallmark tag from terminal output, opens `rbrn.env`, and pastes into `RBRN_SENTRY_HALLMARK` by hand.\n\nThe first-crucible handbook (`rbho_first_crucible()` in `rbhofc_first_crucible.sh`) currently rationalizes the asymmetry (\"the sentry is shared across nameplates, so you kludge it standalone and drive the hallmark yourself\"). In practice this is copy/paste friction the learner hits immediately after meeting the toolchain, and the handbook already teaches \"what a hallmark is and where it lives\" via the bottle step one screen earlier.\n\nCognitive posture: mechanical symmetry restoration plus trimming of the teaching prose that only existed to justify the missing tool. Parallel-safe with other A6 work — touches `rbob_bottle.sh`, the zipper, a new tabtarget, and the first-crucible handbook file.\n\n## Docket\n\n### Problem\n\nStep 1.1 + Step 2 of `rbho_first_crucible()` force the learner to transcribe a hallmark tag between terminal output and a config file. Mechanically identical to the bottle path one step earlier, but unautomated.\n\n### Approach\n\nMirror the bottle pattern:\n\n| Bottle (today) | Sentry (target) |\n|---|---|\n| `rbob_kludge()` in `rbob_bottle.sh` — kludges bottle vessel, drives hallmark into `RBRN_BOTTLE_HALLMARK` | New `rbob_kludge_sentry()` — kludges sentry vessel (from `RBRN_SENTRY_VESSEL`), drives hallmark into `RBRN_SENTRY_HALLMARK` |\n| `rbw-cKB.KludgeBottle.sh` (param1 moniker) | New `rbw-cKS.KludgeSentry.sh` (param1 moniker) |\n| Zipper group entry for `rbw-cKB` | Zipper group entry for `rbw-cKS` |\n\n### Commit cadence — load-bearing\n\n`rbfd_kludge()` (at `rbfd_FoundryDirectorBuild.sh:1213-1218`) hard-dies on any unstaged or staged change. Both auto-driving kludges leave the tree dirty after driving their hallmark. Consequence: **with two auto-driving kludges, the learner must commit between them**, not bundle at the end.\n\n**Current flow (manual sentry paste):** clean → `rbw-fk` (tree stays clean) → `rbw-cKB` (tree dirty: bottle hallmark) → manual paste sentry hallmark (tree dirty: both hallmarks, single commit point) → **one commit** → charge.\n\n**New flow (symmetric kludges):** clean → `rbw-cKS` (tree dirty: sentry hallmark) → **commit #1** (forced — next kludge gates on clean tree) → `rbw-cKB` (tree dirty: bottle hallmark) → **commit #2** → charge.\n\nNet change for the learner: manual-paste eliminated, one extra git commit added. The handbook must teach this commit-between-kludges cadence explicitly — it's the one new invariant the symmetry introduces.\n\n### Implementation steps\n\n1. Add `rbob_kludge_sentry()` in `Tools/rbk/rbob_bottle.sh` — structural twin of `rbob_kludge()`, targeting `RBRN_SENTRY_VESSEL` / `RBRN_SENTRY_HALLMARK`. Use `zrbob_drive_hallmark` (already generic on the variable name).\n2. Add thin dispatcher `tt/rbw-cKS.KludgeSentry.sh` (nolog launcher pattern matching `rbw-cKB`).\n3. Enroll `rbw-cKS` in `Tools/rbk/rbz_zipper.sh` under the Crucible group (next to `rbw-cKB`). Channel: param1.\n4. Add linked-term constant (if one exists for `rbw-cKB` — check `rbyc_common.sh` or the `RBZ_CRUCIBLE_KLUDGE_*` family in the zipper) so the handbook can reference it via a named constant, not a raw string.\n5. Update `Tools/rbk/rbh0/rbhofc_first_crucible.sh`:\n   - Replace Step 1.1's `rbw-fk` invocation and the copy/paste instruction with the new nameplate-aware call.\n   - Insert a **commit step between the two kludges** with concrete `git add` / `git commit` lines and a short explanation (\"kludge needs a clean tree, so commit the sentry hallmark before kludging the bottle\").\n   - Collapse old Step 2 (manual RBRN_SENTRY_HALLMARK edit) into a terminal-step commit for the bottle hallmark, symmetric with the post-sentry commit.\n   - Reframe the \"two kludge commands, two different jobs\" comparison (lines ~197–204) — both are now nameplate-aware; the remaining distinction (sentry shared across nameplates vs bottle per-nameplate) can be mentioned briefly or dropped.\n   - Keep the existing `buh_warn \"${RBYC_KLUDGE} requires a clean git tree.\"` — its importance *increases* under the new flow.\n   - Update probe names/comments if any reference the old flow.\n   - Update the \"Iteration loop\" section at the end (lines ~391–412) — currently describes a one-kludge cycle; with symmetric kludges it may still be one-kludge (the sentry rarely changes), but the commit cadence framing needs a once-over.\n6. Regenerate `CLAUDE.consumer.md` / `rbk-claude-tabtarget-context.md` colophon table if auto-generation is wired up (per `₢A6AAF` docket, the regenerator is the source of truth).\n\n### Pedagogy question for mount time\n\nThe original asymmetry carried a teaching moment: \"here's the file, here's the field, here's how hallmarks live in nameplates.\" With the symmetric tool in place, decide whether to:\n\n- **(a)** Drop the manual-paste teaching entirely — the bottle step already shows the same machinery via `zrbob_drive_hallmark` output, and a one-line `buh_line` can mention where hallmarks live without a manual exercise.\n- **(b)** Keep a vestigial \"open the nameplate and verify\" probe step — passive inspection instead of active editing.\n\nDefault to (a) unless the review at mount time argues for (b).\n\n### Coordination\n\n- `₢A6AAu` (review-onboarding-director-first-kludge, currently earlier on the rail but moving behind this pace) is the natural downstream beneficiary — the review proceeds against the cleaned-up track, not the old copy/paste flow.\n- No interaction with director tracks (cloud build, airgap, bind, graft) — those paths don't touch sentry kludge.\n\n### Requires\n\n- Nothing — the bottle path (`rbob_kludge`, `zrbob_drive_hallmark`) is the reference implementation already landed.\n\n### Out of scope\n\n- Any change to `rbw-fk` (LocalKludge) — it remains the standalone vessel-kludge entry point for non-nameplate contexts (and for the sentry-deb-airgap path where no single nameplate owns it).\n- Ordain-side symmetric sentry (cloud build). Director cloud-build tracks already drive sentry hallmarks through their own pipeline; this pace is kludge-only.\n- Changes to the airgap handbook track (`₢A6AAV`/`₢A6AA1`) — that track uses its own moriah-nameplate flow.\n\n### Validation\n\nEnd-to-end dry run against a fresh `ccyolo`:\n\n```\n# Clean tree\ntt/rbw-cKS.KludgeSentry.sh ccyolo\n# Expect: sentry image built, RBRN_SENTRY_HALLMARK field rewritten, tree dirty in exactly rbrn.env\ngit add .rbk/ccyolo/rbrn.env\ngit commit -m \"kludge sentry hallmark into ccyolo nameplate\"\n\ntt/rbw-cKB.KludgeBottle.sh ccyolo\n# Expect: bottle image built, RBRN_BOTTLE_HALLMARK field rewritten, tree dirty in exactly rbrn.env\ngit add .rbk/ccyolo/rbrn.env\ngit commit -m \"kludge bottle hallmark into ccyolo nameplate\"\n\ntt/rbw-cC.Charge.ccyolo.sh\n# Expect: charge succeeds (nameplate is committed)\n```\n\nAlso run `tt/rbw-Ofc.OnboardingFirstCrucible.sh` end-to-end and confirm handbook output no longer instructs copy/paste and teaches the two-commit cadence clearly.\n\n### Failure mode to verify\n\nRun `rbw-cKS ccyolo` then immediately `rbw-cKB ccyolo` without committing — confirm `rbfd_kludge`'s dirty-tree guard fires with a clear error. The handbook should anticipate this and guide the learner to commit; the runtime guard is the safety net.",
.claude/jjm/jjg_gallops.json:1993:              "jjgtn_text": "## Character\n\nSmall infrastructure pace with a downstream pedagogy revision. The bottle has `rbw-cKB` (KludgeBottle) — nameplate-aware, builds and drives the hallmark into the nameplate in one step. The sentry has no such symmetric tool; the learner runs `rbw-fk rbev-sentry-deb-tether`, copies the hallmark tag from terminal output, opens `rbrn.env`, and pastes into `RBRN_SENTRY_HALLMARK` by hand.\n\nThe first-crucible handbook (`rbho_first_crucible()` in `rbhofc_first_crucible.sh`) currently rationalizes the asymmetry (\"the sentry is shared across nameplates, so you kludge it standalone and drive the hallmark yourself\"). In practice this is copy/paste friction the learner hits immediately after meeting the toolchain, and the handbook already teaches \"what a hallmark is and where it lives\" via the bottle step one screen earlier.\n\nCognitive posture: mechanical symmetry restoration plus trimming of the teaching prose that only existed to justify the missing tool. Parallel-safe with other A6 work — touches `rbob_bottle.sh`, the zipper, a new tabtarget, and the first-crucible handbook file.\n\n## Docket\n\n### Problem\n\nStep 1.1 + Step 2 of `rbho_first_crucible()` force the learner to transcribe a hallmark tag between terminal output and a config file. Mechanically identical to the bottle path one step earlier, but unautomated.\n\n### Approach\n\nMirror the bottle pattern:\n\n| Bottle (today) | Sentry (target) |\n|---|---|\n| `rbob_kludge()` in `rbob_bottle.sh` — kludges bottle vessel, drives hallmark into `RBRN_BOTTLE_HALLMARK` | New `rbob_kludge_sentry()` — kludges sentry vessel (from `RBRN_SENTRY_VESSEL`), drives hallmark into `RBRN_SENTRY_HALLMARK` |\n| `rbw-cKB.KludgeBottle.sh` (param1 moniker) | New `rbw-cKS.KludgeSentry.sh` (param1 moniker) |\n| Zipper group entry for `rbw-cKB` | Zipper group entry for `rbw-cKS` |\n\n### Implementation steps\n\n1. Add `rbob_kludge_sentry()` in `Tools/rbk/rbob_bottle.sh` — structural twin of `rbob_kludge()`, targeting `RBRN_SENTRY_VESSEL` / `RBRN_SENTRY_HALLMARK`. Use `zrbob_drive_hallmark` (already generic on the variable name).\n2. Add thin dispatcher `tt/rbw-cKS.KludgeSentry.sh` (nolog launcher pattern matching `rbw-cKB`).\n3. Enroll `rbw-cKS` in `Tools/rbk/rbz_zipper.sh` under the Crucible group (next to `rbw-cKB`). Channel: param1.\n4. Add linked-term constant (if one exists for `rbw-cKB` — check `rbyc_common.sh` or the `RBZ_CRUCIBLE_KLUDGE_*` family in the zipper) so the handbook can reference it via a named constant, not a raw string.\n5. Update `Tools/rbk/rbh0/rbhofc_first_crucible.sh`:\n   - Replace Step 1.1's `rbw-fk` invocation and the copy/paste instruction with the new nameplate-aware call.\n   - Collapse Step 2 (manual RBRN_SENTRY_HALLMARK edit) — the pedagogy for \"what a hallmark is and where it lives\" is already carried by the bottle kludge step and can be retained there.\n   - Reframe the \"two kludge commands, two different jobs\" comparison (lines ~197–204) to reflect that both are now nameplate-aware; the only distinction left is vessel scope (sentry shared vs bottle per-nameplate), which can be mentioned briefly or dropped.\n   - Update probe names/comments if any reference the old flow.\n6. Regenerate `CLAUDE.consumer.md` / `rbk-claude-tabtarget-context.md` colophon table if auto-generation is wired up (per `₢A6AAF` docket, the regenerator is the source of truth).\n\n### Pedagogy question for mount time\n\nThe original asymmetry carried a teaching moment: \"here's the file, here's the field, here's how hallmarks live in nameplates.\" With the symmetric tool in place, decide whether to:\n\n- **(a)** Drop the manual-paste teaching entirely — the bottle step already shows the same machinery via `zrbob_drive_hallmark` output, and a one-line `buh_line` can mention where hallmarks live without a manual exercise.\n- **(b)** Keep a vestigial \"open the nameplate and verify\" probe step — passive inspection instead of active editing.\n\nDefault to (a) unless the review at mount time argues for (b).\n\n### Coordination\n\n- `₢A6AAu` (review-onboarding-director-first-kludge, currently earlier on the rail but moving behind this pace) is the natural downstream beneficiary — the review proceeds against the cleaned-up track, not the old copy/paste flow.\n- No interaction with director tracks (cloud build, airgap, bind, graft) — those paths don't touch sentry kludge.\n\n### Requires\n\n- Nothing — the bottle path (`rbob_kludge`, `zrbob_drive_hallmark`) is the reference implementation already landed.\n\n### Out of scope\n\n- Any change to `rbw-fk` (LocalKludge) — it remains the standalone vessel-kludge entry point for non-nameplate contexts (and for the sentry-deb-airgap path where no single nameplate owns it).\n- Ordain-side symmetric sentry (cloud build). Director cloud-build tracks already drive sentry hallmarks through their own pipeline; this pace is kludge-only.\n- Changes to the airgap handbook track (`₢A6AAV`/`₢A6AA1`) — that track uses its own moriah-nameplate flow.\n\n### Validation\n\n- `tt/rbw-cKS.KludgeSentry.sh ccyolo` on a clean tree produces a kludged sentry image, writes the hallmark into `.rbk/ccyolo/rbrn.env` RBRN_SENTRY_HALLMARK, leaves the tree dirty with only that one field changed.\n- After committing, `tt/rbw-cC.Charge.ccyolo.sh` charges successfully.\n- Running `tt/rbw-Ofc.OnboardingFirstCrucible.sh` end-to-end produces handbook output that no longer instructs copy/paste.",
.claude/jjm/jjg_gallops.json:2079:              "jjgtn_text": "## Character\n\nMechanical implementation once the contract is locked. Two validations and a regime field rewrite — no cross-cutting refactors, no new concept work. The design conversation concluded in a separate onboarding-study session; this pace is the code delivery. Cognitive posture: precise, contract-driven, no speculative generality. Do not re-open design questions already settled below.\n\n## Context\n\n`tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` Step 1 currently ends with a manual-edit instruction to the learner:\n\n    Open rbev-sentry-deb-tether/rbrv.env and set the field,\n    then commit the change.\n\nThis is a teaching seam. A newcomer holding a fresh reliquary datestamp is exactly the person who will typo it, then discover the typo ~15-20 minutes later when a Conjure preflight fails. The remedy is a tabtarget that accepts the stamp and vessel as arguments, validates both, and rewrites the `RBRV_RELIQUARY` line safely.\n\n## Design decisions locked (do not re-litigate)\n\n- **Fact-file capture rejected.** `output-buk/current/` is cleared on every tabtarget entry; intermediate state there vanishes before the next command runs. The stamp MUST come from an explicit argument.\n- **Global reliquary (RBRR_RELIQUARY) rejected.** Per-vessel `RBRV_RELIQUARY` stays per-vessel.\n- **List-valid-reliquaries subcommand deferred.** If validation fails, the error message names the missing bits; a separate list verb is a future improvement, out of scope for this pace.\n- **Verb is Yoke.** Y is unencumbered across Director actions (current taken: E, I, O, A, V, J, r, w). \"Yoke A to B\" is inherently relational, mirroring the two-argument signature. No phonetic or semantic rhyme with Enshrine.\n- **Auto-commit rejected.** Learner commits afterward, matching the Kludge cadence.\n\n## Contract\n\nColophon `rbw-dY`, frontispiece `DirectorYokesVesselToReliquary`, no imprint. Two positional args (not a single-folio channel; the BUK `imprint`/`param1`/empty vocabulary does not apply here — enroll as a two-param tabtarget per whatever mechanism the zipper supports, or extend the channel if it does not).\n\nInvocation shape:\n\n    tt/rbw-dY.DirectorYokesVesselToReliquary.sh <reliquary-stamp> <vessel>\n\n### Argument order — load-bearing\n\nStamp FIRST, vessel SECOND. Rationale: keeps the door open to a future \"yoke one stamp across multiple vessels\" variant without breaking existing callers. Do NOT reverse the order for ergonomic reasons.\n\n### Behavior\n\n1. **Validate the reliquary stamp.** Confirm all four tool images (skopeo, docker, gcloud, syft) are present in GAR under the expected reliquary path for the given stamp. Reuse the path logic that `tt/rbw-dI.DirectorInscribesReliquary.sh` uses when it writes those images — the inscribe path template is the source of truth. On missing-image(s), error naming the stamp AND the specific missing image name(s).\n\n2. **Validate the vessel.** Confirm:\n   - Vessel regime directory exists at the expected location\n   - `rbrv.env` renders and validates (equivalent to what `tt/rbw-rvv.ValidateVesselRegime.sh <vessel>` checks)\n   - Vessel's mode is one that consumes a reliquary — `conjure` and `airgap` accept yoking; `bind` and `graft` do not (bind pins an upstream digest, graft pushes a local build — neither consumes a reliquary). Error clearly if mode is incompatible.\n\n3. **Both validations must pass before any write.** No partial state. If the stamp is valid but the vessel is not (or vice versa), error and exit without modifying any file.\n\n4. **Rewrite `RBRV_RELIQUARY=<stamp>` in the vessel's `rbrv.env`.** Preserve the rest of the file exactly. In-place edit; do NOT regenerate the regime file via a renderer. If the line is absent, add it in an appropriate location; if present, replace the value only.\n\n5. **Report clearly.**\n   - Before write: `Yoking <vessel> to reliquary <stamp>`\n   - After write: path to the modified file and the new line content\n   - Learner commits with the usual git cadence afterward\n\n### Validation errors must be actionable\n\nSample shapes (not literal strings — match the surrounding operator-message voice):\n\n- `Reliquary stamp 'r260324193327' not found in Depot — expected 4 tool images under <GAR path>; missing: skopeo, gcloud. Re-run tt/rbw-dI.DirectorInscribesReliquary.sh to mint a fresh reliquary, or verify the stamp spelling.`\n- `Vessel 'rbev-bottle-plantuml' has mode=bind; Yoke only applies to conjure and airgap vessels. Bind vessels pin an upstream digest; graft vessels push a local build — neither consumes a reliquary.`\n- `Vessel 'rbev-does-not-exist' not found — expected regime at <path>.`\n\nNo stack traces to the learner; operator-grade messages only.\n\n## What NOT to do\n\n- Do not cache or short-circuit validation (e.g., \"skip if stamp looks like a datestamp\"). The tabtarget's value IS the validation.\n- Do not add a `--force` flag. If validation fails, the fix is to inscribe or correct the input, not to bypass.\n- Do not auto-commit.\n- Do not add list-valid-reliquaries as a side quest; deferred.\n- Do not modify any file other than the target vessel's `rbrv.env`.\n- Do not relitigate argument order, verb choice, or scope.\n\n## Handbook update (same pace)\n\nEdit `tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` Step 1. Currently ends with the manual-edit instruction quoted in Context above. Replace with the Yoke invocation:\n\n    tt/rbw-dY.DirectorYokesVesselToReliquary.sh <reliquary-stamp> rbev-sentry-deb-tether\n\nplus a short line reminding the learner to commit `rbrv.env` afterward. Adjust surrounding prose as needed — the step can be a touch shorter.\n\nSweep the other Director subtracks (`rbw-Oda`, `rbw-Odb`, `rbw-Odg`) for the same pattern. Oda in particular does NOT appear to re-yoke (it assumes Odf ran first; the airgap vessel inherits the stamp) — verify rather than assume.\n\n## Registry updates\n\n- Zipper enrollment for the new tabtarget — see `Tools/rbk/rbz_zipper.sh` for the pattern; the new entry belongs in whichever group owns Director Depot/Foundry actions. Follow the existing `rbw-dI` / `rbw-dE` enrollment as the template.\n- Entry in `Tools/rbk/rbk-claude-tabtarget-context.md` under the Depot section: `rbw-dY | DirectorYokesVesselToReliquary | param1+param2 | Yoke vessel's regime to a specific reliquary stamp`.\n- Any colophon registry in RBS0 / RBDC / CLAUDE.md that enumerates Director actions.\n- `Tools/rbk/vov_veiled/` — check for a spec file covering the Yoke action (pattern: `RBSAY-ark_yoke.adoc` or similar). Create if that pattern is the norm for Director-action specs; otherwise leave for a later spec-hygiene sweep.\n\n## Done criteria\n\n- Tabtarget exists; validates stamp and vessel before any write; errors diagnostically; rewrites cleanly on success.\n- `rbw-Odf` Step 1 teaches Yoke instead of the manual edit.\n- Other Director subtracks checked for the same pattern and updated if needed.\n- `tt/rbw-tf.QualifyFast.sh` green (tabtarget discovery, colophon validation, nameplate health).\n- Registry / context files updated.\n\n## References\n\n- `tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` — seam location (Step 1)\n- `tt/rbw-dI.DirectorInscribesReliquary.sh` — source of GAR reliquary path template\n- `tt/rbw-rvv.ValidateVesselRegime.sh` — vessel regime validator (reusable check)\n- `tt/rbw-dE.DirectorEnshrinesVessel.sh` — the analogous self-driving pattern for `RBRV_IMAGE_N_ANCHOR`; Yoke is the explicit (human-chosen) counterpart since reliquary stamps cannot be auto-picked\n- `CLAUDE.md` — tabtarget naming conventions, zipper enrollment, BUK folio channel vocabulary\n- Design provenance: onboarding-study session ☉260423-1024",
.claude/jjm/jjg_gallops.json:2086:              "jjgtn_text": "## Character\n\nMechanical implementation once the contract is locked. Two validations and a regime field rewrite — no cross-cutting refactors, no new concept work. The design conversation concluded in a separate onboarding-study session; this pace is the code delivery. Cognitive posture: precise, contract-driven, no speculative generality. Do not re-open design questions already settled below.\n\n## Context\n\n`tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` Step 1 currently ends with a manual-edit instruction to the learner:\n\n    Open rbev-sentry-deb-tether/rbrv.env and set the field,\n    then commit the change.\n\nThis is a teaching seam. A newcomer holding a fresh reliquary datestamp is exactly the person who will typo it, then discover the typo ~15-20 minutes later when a Conjure preflight fails. The remedy is a tabtarget that accepts the stamp and vessel as arguments, validates both, and rewrites the `RBRV_RELIQUARY` line safely.\n\n## Design decisions locked (do not re-litigate)\n\n- **Fact-file capture rejected.** `output-buk/current/` is cleared on every tabtarget entry; intermediate state there vanishes before the next command runs. The stamp MUST come from an explicit argument.\n- **Global reliquary (RBRR_RELIQUARY) rejected.** Per-vessel `RBRV_RELIQUARY` stays per-vessel.\n- **List-valid-reliquaries subcommand deferred.** If validation fails, the error message names the missing bits; a separate list verb is a future improvement, out of scope for this pace.\n- **Verb is Yoke.** Y is unencumbered across Director actions (current taken: E, I, O, A, V, J, r, w). \"Yoke A to B\" is inherently relational, mirroring the two-argument signature. No phonetic or semantic rhyme with Enshrine.\n- **Auto-commit rejected.** Learner commits afterward, matching the Kludge cadence.\n\n## Contract\n\nColophon `rbw-dY`, frontispiece `DirectorYokesVesselToReliquary`, no imprint. Two positional args (not a single-folio channel; the BUK `imprint`/`param1`/empty vocabulary does not apply here — enroll as a two-param tabtarget per whatever mechanism the zipper supports, or extend the channel if it does not).\n\nInvocation shape:\n\n    tt/rbw-dY.DirectorYokesVesselToReliquary.sh <reliquary-stamp> <vessel>\n\n### Argument order — load-bearing\n\nStamp FIRST, vessel SECOND. Rationale: keeps the door open to a future \"yoke one stamp across multiple vessels\" variant without breaking existing callers. Do NOT reverse the order for ergonomic reasons.\n\n### Behavior\n\n1. **Validate the reliquary stamp.** Confirm all four tool images (skopeo, docker, gcloud, syft) are present in GAR under the expected reliquary path for the given stamp. Reuse the path logic that `tt/rbw-dI.DirectorInscribesReliquary.sh` uses when it writes those images — the inscribe path template is the source of truth. On missing-image(s), error naming the stamp AND the specific missing image name(s).\n\n2. **Validate the vessel.** Confirm:\n   - Vessel regime directory exists at the expected location\n   - `rbrv.env` renders and validates (equivalent to what `tt/rbw-rvv.ValidateVesselRegime.sh <vessel>` checks)\n   - Vessel's mode is one that consumes a reliquary — `conjure` and `airgap` accept yoking; `bind` and `graft` do not (bind pins an upstream digest, graft pushes a local build — neither consumes a reliquary). Error clearly if mode is incompatible.\n\n3. **Both validations must pass before any write.** No partial state. If the stamp is valid but the vessel is not (or vice versa), error and exit without modifying any file.\n\n4. **Rewrite `RBRV_RELIQUARY=<stamp>` in the vessel's `rbrv.env`.** Preserve the rest of the file exactly. In-place edit; do NOT regenerate the regime file via a renderer. If the line is absent, add it in an appropriate location; if present, replace the value only.\n\n5. **Report clearly.**\n   - Before write: `Yoking <vessel> to reliquary <stamp>`\n   - After write: path to the modified file and the new line content\n   - Learner commits with the usual git cadence afterward\n\n### Validation errors must be actionable\n\nSample shapes (not literal strings — match the surrounding operator-message voice):\n\n- `Reliquary stamp 'r260324193327' not found in Depot — expected 4 tool images under <GAR path>; missing: skopeo, gcloud. Re-run tt/rbw-dI.DirectorInscribesReliquary.sh to mint a fresh reliquary, or verify the stamp spelling.`\n- `Vessel 'rbev-bottle-plantuml' has mode=bind; Yoke only applies to conjure and airgap vessels. Bind vessels pin an upstream digest; graft vessels push a local build — neither consumes a reliquary.`\n- `Vessel 'rbev-does-not-exist' not found — expected regime at <path>.`\n\nNo stack traces to the learner; operator-grade messages only.\n\n## What NOT to do\n\n- Do not cache or short-circuit validation (e.g., \"skip if stamp looks like a datestamp\"). The tabtarget's value IS the validation.\n- Do not add a `--force` flag. If validation fails, the fix is to inscribe or correct the input, not to bypass.\n- Do not auto-commit.\n- Do not add list-valid-reliquaries as a side quest; deferred.\n- Do not modify any file other than the target vessel's `rbrv.env`.\n- Do not relitigate argument order, verb choice, or scope.\n\n## Handbook update (same pace)\n\nEdit `tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` Step 1. Currently ends with the manual-edit instruction quoted in Context above. Replace with the Yoke invocation:\n\n    tt/rbw-dY.DirectorYokesVesselToReliquary.sh <reliquary-stamp> rbev-sentry-deb-tether\n\nplus a short line reminding the learner to commit `rbrv.env` afterward. Adjust surrounding prose as needed — the step can be a touch shorter.\n\nSweep the other Director subtracks (`rbw-Oda`, `rbw-Odb`, `rbw-Odg`) for the same pattern. Oda in particular does NOT appear to re-yoke (it assumes Odf ran first; the airgap vessel inherits the stamp) — verify rather than assume.\n\n## Registry updates\n\n- Zipper enrollment for the new tabtarget — see `Tools/rbk/rbz_zipper.sh` for the pattern; the new entry belongs in whichever group owns Director Depot/Foundry actions. Follow the existing `rbw-dI` / `rbw-dE` enrollment as the template.\n- Entry in `Tools/rbk/rbk-claude-tabtarget-context.md` under the Depot section: `rbw-dY | DirectorYokesVesselToReliquary | param1+param2 | Yoke vessel's regime to a specific reliquary stamp`.\n- Any colophon registry in RBS0 / RBDC / CLAUDE.md that enumerates Director actions.\n- `Tools/rbk/vov_veiled/` — check for a spec file covering the Yoke action (pattern: `RBSAY-ark_yoke.adoc` or similar). Create if that pattern is the norm for Director-action specs; otherwise leave for a later spec-hygiene sweep.\n\n## Done criteria\n\n- Tabtarget exists; validates stamp and vessel before any write; errors diagnostically; rewrites cleanly on success.\n- `rbw-Odf` Step 1 teaches Yoke instead of the manual edit.\n- Other Director subtracks checked for the same pattern and updated if needed.\n- `tt/rbw-tf.QualifyFast.sh` green (tabtarget discovery, colophon validation, nameplate health).\n- Registry / context files updated.\n\n## References\n\n- `tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` — seam location (Step 1)\n- `tt/rbw-dI.DirectorInscribesReliquary.sh` — source of GAR reliquary path template\n- `tt/rbw-rvv.ValidateVesselRegime.sh` — vessel regime validator (reusable check)\n- `tt/rbw-dE.DirectorEnshrinesVessel.sh` — the analogous self-driving pattern for `RBRV_IMAGE_N_ANCHOR`; Yoke is the explicit (human-chosen) counterpart since reliquary stamps cannot be auto-picked\n- `CLAUDE.md` — tabtarget naming conventions, zipper enrollment, BUK folio channel vocabulary\n- Design provenance: onboarding-study session ☉260423-1024",
.claude/jjm/jjg_gallops.json:2165:              "jjgtn_text": "## Character\n\nComprehensive foundation-landing pace for the handbook restart. Absorbs what was previously split across `₢A6AAA` (drafts) and the now-dropped `₢A6AAB` (implementation). Produces a first-iteration working implementation of the two foundation tabtargets — the probe-aware StartHere menu and the universal Crash Course handbook — plus smoke testing across three repo states and a prototype agent-learner eval.\n\nLow-novelty in mechanism (same bash idioms, same `buh_*` primitives, same dispatch chain as the existing `rbho_*` sequences). High-novelty in pedagogical framing: first deployed artifacts of the Frame 4-refined handbook restart, first deliberate break from role-decomposition as the primary organizing axis. Review happens against the running code — user executes the tabtargets, reads the output, annotates what works and what doesn't, approves or requests revisions.\n\n## Status — as-built (resumption anchor)\n\nThis section is a snapshot of where the pace actually stands, intended as a pickup anchor for a fresh mounting session. The Deliverables below describe the *plan*; this section describes what has actually happened.\n\n### Initial implementation landed\n\nCommit `9844ce32` — *\"land handbook-restart foundation: rbho_start_here + rbho_crash_course sequences, tt/rbw-o and tt/rbw-Occ dispatchers, RBZ__GROUP_ONBOARDING zipper group + two enrollments\"*.\n\nFiles touched in that commit:\n- `Tools/rbk/rbho_onboarding.sh` — added `rbho_start_here()` and `rbho_crash_course()` at a new \"Handbook restart — foundation\" banner section near the end of the file\n- `Tools/rbk/rbz_zipper.sh` — added `RBZ__GROUP_ONBOARDING` group with two enrollments, positioned between the existing Guide and Hallmark groups\n- `Tools/rbk/rbk-claude-tabtarget-context.md` — regenerated from zipper via `rbw-MG.MarshalGenerate.sh`\n- `tt/rbw-o.OnboardingStartHere.sh` — new 4-line thin dispatcher\n- `tt/rbw-Occ.OnboardingCrashCourse.sh` — new 4-line thin dispatcher\n\n### Smoke testing — State 3 (Ready) verified\n\nBoth tabtargets run cleanly against the current repo state (populated RBRR + credentials present):\n\n- `tt/rbw-o.OnboardingStartHere.sh` — exit 0, renders menu with `→ Crash Course` as the only arrow (correct for Ready state). All tabtarget references resolve via `buz_healthcheck`.\n- `tt/rbw-Occ.OnboardingCrashCourse.sh` — exit 0, renders all 6 units. Unit 3 (RBRR) shows `[*] RBRR populated — depot project: rbwg-d-depot10041-260327170532`. Unit 4 (Station) shows `[*] BURC present` and `[*] Station file present at /Users/bhyslop/projects/rbm_alpha_recipemuster/../station-files/burs.env`. Unit 5 (Marshal Zero) shows `[*] Current RBRR state: populated (not a zero state right now)`.\n- `tt/rbw-tf.QualifyFast.sh` — passes (tabtarget structure OK, RBW colophon registrations OK, generated context freshness OK).\n\n### Smoke States 1 and 2 — deferred\n\nNot yet exercised; waiting on user approval to touch live repo state:\n\n- **State 1 (Bare-fork simulation)** — requires running `tt/rbw-MZ.MarshalZeroes.sh` to blank RBRR identity fields (reversible, preserves OAuth). Expected outcome: StartHere arrows Crash Course + Establish the Depot; Crash Course Unit 3 shows red (\"depot identity fields are blank\"); Unit 5 shows \"zero-like — this is where a bare-fork learner starts\".\n- **State 2 (Joining simulation)** — requires temporarily moving credential files out of `${RBRR_SECRETS_DIR}` and restoring. Expected outcome: StartHere arrows Crash Course + Take Your Station; Crash Course Unit 3 green, Unit 5 says \"populated (not a zero state right now)\".\n\n### RBGC_PUBLIC_DOCS_URL observation — coordination item for `₣A5`\n\nAnother officium has retargeted `RBGC_PUBLIC_DOCS_URL` to `https://github.com/scaleinv/recipebottle/blob/main/README.md`. The paddock's \"Public-Docs Anchor Coordination with `₣A5`\" section still references the `scaleinv.github.io` URL and implicitly assumes a github-pages build. The anchor scheme (PascalCase single-word concepts like `#Depot`, `#RBRR`, `#MarshalZero`) still works mechanically — the code uses the constant so it's automatically correct — but the paddock coordination text is stale about *where* A5 is curating anchors.\n\n`Tools/rbk/rbgc_Constants.sh` has been edited by another officium (visible as unstaged change in working tree during this pace's work). Not touched by this pace.\n\n**Paddock follow-up needed**: update the Anchor Coordination section to say A5 is curating `README.md` on `main`, not a github-pages build. Worth a short paddock reslate when convenient.\n\n### User revisions pending\n\nUser ran both tabtargets, confirmed they function, wants to do revisions on both. Specific revision items not yet captured at time of this reslate — user stepping away, revisions to be taken up on resume.\n\n### Prototype eval artifact — not yet delivered\n\nDeliverable 4 still open. Planned form: markdown file at `Tools/rbk/vov_veiled/A6-prototype-eval.md` containing the anti-inference system prompt, 3–5 application-focused scenario questions, and a differential-test note. Awaiting user signoff on the form before writing.\n\n### Paddock Artifact 3 items — still open\n\nDeliverable 5 items still pending:\n- **Track acronym table** — assign `rbw-O??` subfunction letters for the other 12 tracks; suggested table (`Ots/Ofc/Oed/Okr/Ork/Oof/Oea/Oir/Otc/Oac/Obc/Oar`) is a proposal pending confirmation\n- **Implementation-sequence decision** — paddock currently says \"First implementation target: Kindle Your First Crucible\" but Crash Course landed first; needs reconciliation\n- **Anchor Coordination section update** — fold in the `RBGC_PUBLIC_DOCS_URL` retargeting observation (see above)\n- **New open questions** from this implementation — none surfaced that aren't already captured\n\n### Downstream paces — swept, clean\n\n`₢A6AAC`, `₢A6AAD`, `₢A6AAE` were mass-reslated in commit `fe7e1d50` to fix stale colophons (`rbw-h`/`rbw-Hcc`/`rbw-Hfc` → `rbw-o`/`rbw-Occ`/`rbw-Ofc`), update dropped-A6AAB dependency references to `₢A6AAA`, and add a small amount of concrete guidance (zipper registration form, menu-update mechanics) that became visible while this pace's implementation was landing. The interview-transcript phantom was also removed from `₢A6AAE`'s References section in the same sweep — slightly beyond the user's explicit \"colophon\" scope, flagged in the report, user has not yet indicated whether to restore it.\n\n## Prefix Context\n\nTabtarget colophons use the **two-colophon onboarding scheme** settled in the `₣A6` paddock Prerequisites section:\n\n- `rbw-o` (lowercase, terminal) — the onboarding StartHere entry point, standalone tabtarget\n- `rbw-O*` (uppercase family) — individual onboarding handbook tracks (e.g. `rbw-Occ` for Crash Course, `rbw-Ofc` for First Crucible)\n\nThis replaces the initially-proposed `rbw-h?` family from the interview, which collided with the existing `rbw-h` Hallmark family. Case-split family is deliberate starting-letter differentiation for clarity.\n\n## Existing code as mechanism reference (NOT structural template)\n\nThe existing `rbho_*` sequences — `rbho_triage`, `rbho_reference`, `rbho_retriever`, `rbho_director`, `rbho_governor`, `rbho_payor` — are **mechanism references** for this pace. They demonstrate how filesystem probes, `buh_*` primitives, and the dispatch chain fit together. Their *organizational shapes* (role-based decomposition, credential-driven routing, two-mode walkthrough/reference rendering) are precisely what this heat replaces. **Copy the idioms, not the structure.**\n\nThe distinction is load-bearing. `rbho_triage` is a role-router: probe RBRA credentials → enumerate detected roles → link each to a role walkthrough. Its organizing axis is *authorization* — the paddock's exact diagnosis of the malformation this heat is curing. Using it as a \"structural template\" for `rbho_start_here` would import the role-axis into the very tabtarget meant to supplant it.\n\n**What to borrow from existing sequences**:\n- `buc_doc_brief \"description\"` + `buc_doc_shown || return 0` — dispatch-mode guard\n- `zrbho_po_extract_capture ${file} ${key}` — read a KEY=VALUE from a file without sourcing\n- `zrbho_po_status ${flag} \"${text}\"` — render `[*] text` or `[ ] text` status line\n- `zrbho_probe_role_credentials` — useful for the *any-credential-present* check even though per-role detail is not used\n- `buh_section`, `buh_e`, `buh_t` — section headers, blank lines, plain text\n- `buh_tlt \"prefix \" \"concept\" \"${z_docs}#Anchor\" \" suffix\"` — vocabulary-first teaching (concept as clickable OSC-8 hyperlink)\n- `buh_tc \"prefix \" \"tt/rbw-...sh\"` — text + cyan command reference\n- `buh_tT \"prefix \" \"${RBZ_*}\"` — text + resolved-via-glob tabtarget (also validates existence)\n- Filesystem-only probing convention (`test -f`, extract via helper, no env-var dependency on BURS)\n\n**What NOT to borrow**:\n- Role-based decomposition of menu items or teaching content\n- Credential detection as a primary routing signal\n- Two-mode (walkthrough vs reference) rendering\n- Unit structure organized around role tasks\n\n## Dispatch chain (reference)\n\n```\ntt/rbw-{colophon}.*.sh  (5-line thin dispatcher)\n  → .buk/launcher.rbw_workbench.sh\n  → rbw_workbench.sh :: rbw_route()\n  → buz_exec_lookup()  (exact-string colophon match)\n  → rbho_cli.sh        (CLI furnish sources buh/rbcc/rbgc/rbz/rbho)\n  → rbho_{sequence}()  (enrolled function in rbho_onboarding.sh)\n```\n\n## Deliverable 1 — `rbho_start_here()` + `tt/rbw-o.OnboardingStartHere.sh` + zipper\n\n**Status**: LANDED in commit `9844ce32`. Verified in State 3 (Ready). Pending user revision notes.\n\n**Sequence function** in `Tools/rbk/rbho_onboarding.sh`, added at a new \"Handbook restart — foundation\" banner section after the existing role-track sequences.\n\n**Probe set** (deliberately narrow — state, not role):\n- `z_rbrr_populated` — `.rbk/rbrr.env` exists AND has non-empty `RBRR_DEPOT_PROJECT_ID`\n- `z_has_any_rbra` — any of payor/governor/director/retriever credential files exist (via `zrbho_probe_role_credentials`; per-role values are collected but ignored for routing)\n\n**Highlighting logic** (three states, foundation-focused, no role inference):\n- **Cold start** (no RBRR) → arrow on Crash Course + arrow on Establish the Depot. These are the only unblocked actions.\n- **Joining** (RBRR populated, no credentials) → arrow on Crash Course + arrow on Take Your Station. These are the only unblocked actions.\n- **Ready** (RBRR populated, has credentials) → arrow on Crash Course only. Learner picks their own track from the static menu. No guessing.\n\nArrow rendering: leading `→ ` prefix on highlighted items, `  ` on others. Simple, discoverable, easy to revise.\n\n**Menu body** (static, intent-organized):\n\n1. **Preamble** — what Recipe Bottle is, what handbooks are, how to read this menu\n2. **Foundation** — Crash Course (always first, always arrowed), Take Your Station (`[planned]`, arrowed when joining)\n3. **Role-intent tracks** — Your First Crucible `[planned]`, Establish the Depot `[planned]` (arrowed when cold-start), Knight the Realm `[planned]`, Receive Your Knighthood `[planned]`\n4. **Director subtracks** — Your First Ordination, Enshrine the Ancestors, Inscribe the Reliquary, Tethered Cloud Builds, Airgap Your Builds, Building for Crucibles (all `[planned]`, none arrowed in first iteration)\n5. **Evaluation** — Assay the Realm `[planned]`\n6. **Reference** — Realm Dashboard (links to existing `rbw-gOr.OnboardReference.sh` until A6 produces Frame 4-refined replacement)\n7. **Find your role / legacy fallback** — \"Run Crash Course first if unsure\" guidance, plus honest pointer to legacy role-organized `rbw-go.OnboardMAIN.sh` triage as preserved fallback during the heat\n\n**Tabtarget dispatcher** at `tt/rbw-o.OnboardingStartHere.sh` (exact copy of existing 5-line pattern, with `BURD_NO_LOG=1` — handbook output is ephemeral per paddock decision):\n```bash\n#!/bin/bash\nexport BURD_LAUNCHER=\".buk/launcher.rbw_workbench.sh\"\nexport BURD_NO_LOG=1\nexec \"${BASH_SOURCE[0]%/*}/../${BURD_LAUNCHER}\" \"${0##*/}\" \"${@}\"\n```\n\n**Zipper enrollment** in `Tools/rbk/rbz_zipper.sh` — new `RBZ__GROUP_ONBOARDING` group adjacent to the existing `RBZ__GROUP_GUIDE`:\n```bash\nbuz_group   RBZ__GROUP_ONBOARDING  \"rbw-o\"   \"Onboarding — Handbook restart\"\nz_mod=\"rbho_cli.sh\"\nbuz_enroll  RBZ_ONBOARD_START_HERE \"rbw-o\"   \"${z_mod}\"  \"rbho_start_here\"   \"\"  \"Onboarding start — probe-aware menu into handbook tracks\"\n```\n\n## Deliverable 2 — `rbho_crash_course()` + `tt/rbw-Occ.OnboardingCrashCourse.sh` + zipper\n\n**Status**: LANDED in commit `9844ce32`. Verified in State 3 (Ready). Pending user revision notes.\n\n**Sequence function** in `Tools/rbk/rbho_onboarding.sh`, next to `rbho_start_here()`.\n\n**Probes**:\n- `z_rbrr_populated` + `z_rbrr_project` — RBRR identity state (for Unit 3 + Unit 5)\n- `z_burc_present` — `.buk/burc.env` existence (for Unit 4)\n- `z_station_present` — `${BURD_STATION_FILE}` existence (for Unit 4)\n\n**Unit breakdown** (6 units, alternating teaching-only and verified, per paddock per-unit brevity discipline):\n\n1. **What you're looking at** (teaching-only) — introduce `Tabtarget` (link), the `tt/` directory, the `{colophon}.{frontispiece}.sh` naming. Concrete example: `tt/rbw-go.OnboardMAIN.sh`. Teach that tab completion narrows by colophon. Vocabulary: tabtarget, colophon, frontispiece.\n2. **Regimes — the configuration map** (teaching-only) — introduce `Regime` (link), the 13-regime landscape, the six user-facing ones, the `rbw-r{letter}{r|v}` render/validate tabtarget pattern. Teach the *pattern*, not the inventory. Vocabulary: regime, render, validate.\n3. **Repository regime** (verified) — introduce `RBRR` (link), explain depot identity as shared team ground truth. Probe `.rbk/rbrr.env` + `RBRR_DEPOT_PROJECT_ID`. Vocabulary: RBRR, depot.\n4. **Station regime and log placement** (verified) — introduce `BURC` (link — project config) and `BURS` (link — per-developer station). Explain station-vs-repo split; call out `BURS_LOG_DIR` as the anchor. Probe `.buk/burc.env` existence + `${BURD_STATION_FILE}` existence. Teach: state-changing commands log, handbooks don't. Vocabulary: station, BURC, BURS.\n5. **Marshal Zero — the first lesson** (teaching + verified) — introduce `MarshalZero` (link). Walk through what `rblm_zero()` does (blank RBRR fields, delete credentials, preserve OAuth). Then describe the `DiagnosticFailure` (link) experience of running `rbw-rrv.ValidateRepoRegime.sh` against zero state. Teach the core lesson: *\"In this system, configuration errors fail diagnostically. Trust the error messages.\"* Probe current state as \"populated\" or \"zero-like\". Vocabulary: Marshal Zero, diagnostic failure.\n6. **Next steps** (teaching-only) — summarize learned capabilities, point back to StartHere menu via `buh_tT \" \" \"${RBZ_ONBOARD_START_HERE}\"`. Close the loop.\n\n**Two entry states** handled without branching: the same units render in both states. For bare-fork, verified units show red with teaching-focused context about what the learner will fill in via the payor track. For joining-existing, the same units show green with verification-focused context about what the existing team has done. Same text, same probes, different stories emerge from the probe outcomes.\n\n**Teaching voice** per paddock principles: vocabulary-first via `buh_tlt`, relational prose (nouns introduced in context with concrete referents, verbs connecting them), roles emerge from actions not labels, each new term has a reason to exist at the moment of introduction.\n\n**Tabtarget dispatcher** at `tt/rbw-Occ.OnboardingCrashCourse.sh` — same 5-line pattern.\n\n**Zipper enrollment** next to the START_HERE entry under the same group:\n```bash\nbuz_enroll  RBZ_ONBOARD_CRASH_COURSE \"rbw-Occ\" \"${z_mod}\"  \"rbho_crash_course\" \"\"  \"Crash Course — universal prerequisite: tabtargets, regimes, Marshal Zero\"\n```\n\n## Deliverable 3 — Smoke testing across three states\n\n**Status**: State 3 (Ready) VERIFIED. States 1 and 2 deferred pending user approval to touch live repo.\n\nRun both new tabtargets in three repo states, document observed behavior in the pace commit message.\n\n**State 1: Bare-fork simulation** — run `tt/rbw-MZ.MarshalZeroes.sh` to zero RBRR identity + credentials, preserving OAuth. Then:\n- `tt/rbw-o.OnboardingStartHere.sh` — expect: Crash Course arrowed, Establish the Depot arrowed, all other tracks shown without arrows\n- `tt/rbw-Occ.OnboardingCrashCourse.sh` — expect: Unit 3 (RBRR) shows red with \"depot identity fields are blank\", Unit 5 (Marshal Zero) confirms current state is zero-like\n\n**State 2: Joining-existing simulation** — with populated RBRR but credentials removed from `${RBRR_SECRETS_DIR}`. (This may require temporary manipulation; document exact steps.)\n- StartHere — expect: Crash Course arrowed, Take Your Station arrowed\n- Crash Course — expect: Unit 3 green with project ID, Unit 5 says \"populated (not a zero state right now)\"\n\n**State 3: Ready / current repo state** — with whatever's currently in `.rbk/`. Run both, expect:\n- StartHere — only Crash Course arrowed (Ready state)\n- Crash Course — whatever the current probe outcomes are\n\nDocument the actual observed rendering from each run in the pace commit. If any state reveals bugs, fix them before wrapping.\n\n**Do not touch ground truth** — use Marshal Zero (reversible) and backup files if needed. Do not blow away RBRR permanently for testing.\n\n## Deliverable 4 — Prototype agent-learner eval\n\n**Status**: NOT YET DELIVERED. Planned form below; awaiting user signoff on form/location before writing.\n\n**Scoped intent** per the original A6AAB design: *just enough to validate the concept end-to-end, not a full release gate.* The real eval harness is a later dedicated pace.\n\n**Form**: a markdown file or commit-message appendix containing:\n- **Anti-inference system prompt**: *\"You are a learner evaluating a handbook. Reason strictly from what the handbook tells you. If the handbook doesn't say it, you don't know it. Do not draw on general knowledge of containers, Google Cloud, or DevOps.\"*\n- **Scenario questions** (3–5): application-focused, not recall. Examples:\n    - *\"A learner runs `tt/rbw-rrv.ValidateRepoRegime.sh` and gets a list of blank fields. Based only on the Crash Course, what should they conclude and what should they do next?\"*\n    - *\"What's the difference between a station setting and a repository setting, and why does it matter?\"*\n    - *\"If you needed to find the tabtarget that validates the payor regime, how would you name it without looking it up?\"*\n- **Differential test**: run the same questions against the full Crash Course output AND against a stripped variant (teaching prose removed, probes and tabtarget references left). Compare scores. Signal is in the delta.\n- **Goodhart mitigation** note: questions not drawn from author's immediate working context; rotate if repeated.\n\n**Execution**: this pace DELIVERS the eval artifact but does NOT necessarily run it. Running the eval can happen inline (user pastes handbook output into a fresh Claude session with the system prompt) or is deferred as a sanity check. The pace succeeds if the eval artifact is present and well-formed.\n\n**Where the eval artifact lives**: as a new small markdown file at `Tools/rbk/vov_veiled/A6-prototype-eval.md` — drafter's call, note the choice in the commit.\n\n## Deliverable 5 — Paddock refinement (remaining items)\n\n**Status**: OPEN. Items itemized below.\n\nThe paddock was already updated during earlier reslates to:\n- Sweep the `rbw-h?` prefix collision to the two-colophon scheme\n- Remove the interview-transcript phantom and add the mounting-corrections section\n- Add the Public-Docs Anchor Coordination section\n- Flag the \"kindle\" vocabulary overload in Open Questions\n- Assign provisional `Occ` for Crash Course\n- Expand References with the dispatch-chain files\n\nRemaining work (via `jjx_paddock` setter):\n\n- **Finalize track acronym table** — assign `rbw-O??` subfunction letters for the other 12 tracks. Suggested (for review): `Ots` Take Your Station, `Ofc` First Crucible, `Oed` Establish Depot, `Okr` Knight the Realm, `Ork` Receive Knighthood, `Oof` First Ordination, `Oea` Enshrine Ancestors, `Oir` Inscribe Reliquary, `Otc` Tethered Cloud, `Oac` Airgap Builds, `Obc` Building for Crucibles, `Oar` Assay Realm. Record as a table. Open for revision.\n- **Update Anchor Coordination section** — replace the implicit `scaleinv.github.io` target framing with `github.com/scaleinv/recipebottle/blob/main/README.md` (see Status section above). Note that the anchor scheme (`#PascalCase`) still holds.\n- **Record implementation-sequence decision** — the paddock currently says \"First implementation target: Kindle Your First Crucible\" but this pace implements Crash Course + StartHere first. Either reclassify First Crucible as \"first *track* implementation\" (with Crash Course as prerequisite foundation), or rewrite to state Crash Course is the true first implementation. Pick one.\n- **Add any open questions** surfaced during implementation, especially around probe-aware highlighting mechanics, zero-state detection heuristics, and the teaching voice for the Marshal Zero moment.\n\n## Success criteria\n\n- `tt/rbw-o.OnboardingStartHere.sh` runs cleanly and renders the probe-aware menu ✓ (verified State 3)\n- `tt/rbw-Occ.OnboardingCrashCourse.sh` runs cleanly and renders the 6-unit Frame 4-refined document ✓ (verified State 3)\n- `buz_healthcheck` passes (both new tabtargets found on disk, matching their zipper enrollments) ✓\n- All three smoke-test states produce sensible output — **State 3 only; States 1 and 2 deferred**\n- Prototype eval artifact is present, well-formed, and captures the differential-test intent — **not yet delivered**\n- Paddock Artifact 3 items are landed (acronym table, implementation-sequence decision, anchor URL update, new open questions) — **not yet landed**\n- User reviews and accepts the behavior before the pace is wrapped — **review pending; user has confirmed both tabtargets function, wants revisions**\n\n## Local verification before commit (for each iteration)\n\n- Run `tt/rbw-o.OnboardingStartHere.sh` — no errors, no missing-tabtarget errors from `buz_healthcheck`, probe states visible in output\n- Run `tt/rbw-Occ.OnboardingCrashCourse.sh` — same checks, all six units render\n- Run `tt/rbw-tf.QualifyFast.sh` — catches tabtarget/colophon/nameplate health issues\n- `jjx_record` with explicit file list — do NOT use blanket staging (other officia may have uncommitted work)\n\n## Out of Scope\n\n- **Probe implementation beyond existing helpers** — reuse `zrbho_probe_role_credentials`, `zrbho_po_extract_capture`, `zrbho_po_status`. New probes only where the Crash Course units cannot be served by existing helpers, and only filesystem-level (no docker, no gcloud).\n- **Full agent-learner eval harness** — prototype only; release-gate mechanics deferred to a dedicated pace\n- **Retirement of `rbw-gO*` tabtargets and their `rbho_triage/retriever/director/governor/payor` sequences** — old and new coexist; cutover in a later pace once replacements are proven\n- **First Crucible handbook** — separate pace (`₢A6AAC` drafts, `₢A6AAD` implements)\n- **The other 12 handbook tracks** — separate paces\n- **Adding public-docs anchors to `README.md`** — `₣A5`'s job; this pace only consumes anchors and records new ones in the paddock coordination section\n- **Fancy probe-aware highlighting** — simple `→ ` prefix in first iteration; refinements after review\n\n## References\n\n- `Tools/rbk/rbho_onboarding.sh` — existing sequences as MECHANISM references only; new `rbho_start_here()` and `rbho_crash_course()` at end of file (post commit `9844ce32`)\n- `Tools/rbk/rbho_cli.sh` — CLI furnish (no changes needed; already sources buh/rbcc/rbgc/rbz/rbho)\n- `Tools/rbk/rbz_zipper.sh` — zipper registry; new `RBZ__GROUP_ONBOARDING` group and two enrollments\n- `Tools/rbk/rblm_cli.sh` — `rblm_zero()` reference for Unit 5 Marshal Zero teaching\n- `Tools/buk/buh_handbook.sh` — display primitive catalog\n- `Tools/buk/buz_zipper.sh` — zipper mechanism (`buz_group`, `buz_enroll`, `buz_exec_lookup`)\n- `tt/rbw-go.OnboardMAIN.sh` — 5-line thin dispatcher pattern (copied for both new tabtargets)\n- `₣A6` paddock — Prefix Context, Anchor Coordination, Teaching Voice, Open Questions, Provenance (Mounting corrections), Rough Track Roster\n- Commit `9844ce32` — initial implementation land\n- Commit `fe7e1d50` — downstream paces (A6AAC/AAD/AAE) colophon sweep",
.claude/jjm/jjg_gallops.json:6805:              "jjgtn_text": "## Character\n\nMechanical rename. JJ owns \"muster\" as a verb (`jjx_list`); RB's image-domain muster verb collides. Per paddock cult-verb naming decision: image muster → **audit**. This pace sweeps all landed image-muster sites in code, tabtargets, and project-memory files.\n\nNo design decisions in scope. The rename target and case rule are paddock-locked. Symbol-substitution discipline only.\n\nDepends on BBAAQ landing the spec-side rename first so spec is source-of-truth at code-rename time.\n\n## Scope\n\n**Bash:**\n\n- `Tools/rbk/rbz_zipper.sh` (L122-129) — `RBZ_MUSTER_HALLMARKS/RELIQUARIES/ENSHRINEMENTS` → `RBZ_AUDIT_*`; `buz_enroll` function refs `rbfl_muster_*` → `rbfl_audit_*`; verb-list comment.\n- `Tools/rbk/rbfl_FoundryLedger.sh` — three function defs (`rbfl_muster_hallmarks/reliquaries/enshrinements` → `rbfl_audit_*`); `buc_doc_brief` strings (\"Muster hallmarks...\" → \"Audit hallmarks...\"); `buc_success \"Muster complete...\"` strings; comments at L19-20 (verb listings).\n- `Tools/rbk/rbfc_FoundryCore.sh:524` — comment (\"shared by tally, rekon, muster, batch_vouch\" → \"audit\").\n\n**Rust:**\n\n- `Tools/rbk/rbtd/src/rbtdrm_manifest.rs:41-46` — three constants `RBTDRM_COLOPHON_MUSTER_HALLMARKS/RELIQUARIES/ENSHRINEMENTS` → `RBTDRM_COLOPHON_AUDIT_*`; verb-list comment at L41.\n\n**Tabtarget files (rename, content unchanged — they're colophon-agnostic launchers):**\n\n- `tt/rbw-imh.DirectorMustersHallmarks.sh` → `tt/rbw-iah.DirectorAuditsHallmarks.sh`\n- `tt/rbw-imr.DirectorMustersReliquaries.sh` → `tt/rbw-iar.DirectorAuditsReliquaries.sh`\n- `tt/rbw-ime.DirectorMustersEnshrinements.sh` → `tt/rbw-iae.DirectorAuditsEnshrinements.sh`\n\n**Project memory:**\n\n- `CLAUDE.md:84` — RBSIM acronym mapping. If BBAAQ renamed the spec file (RBSIM → RBSIA), update both the acronym key and the file path here.\n- `Tools/rbk/rbk-claude-tabtarget-context.md:103-105` — three image table rows (colophon, frontispiece, purpose).\n- `Tools/rbk/vov_veiled/CLAUDE.consumer.md:50` — verb table entry for \"muster\".\n\n**Permission file (operator-owned, surface only):**\n\n- `.claude/settings.local.json:1057-1059` — three tabtarget shim references. Surface to operator with the proposed three replacements; do not silently rewrite.\n\n## Verification\n\n- `tt/` lists three new `rbw-ia*.DirectorAudits*.sh` tabtargets; old `rbw-im*.DirectorMusters*.sh` files absent.\n- `grep -rni 'muster' Tools/rbk/ tt/` returns zero hits in image-domain code paths (SA-domain roster references — if BBAAN landed — and JJ-internal jjk/ references excluded).\n- `tt/rbtd-b.Build.sh` builds clean (Rust constant rename compiles).\n- `tt/rbtd-s.TestSuite.fast.sh` passes — fast suite covers manifest validation; rename should not break enrollment-validation.\n- `tt/rbw-iah.DirectorAuditsHallmarks.sh` invokable end-to-end against the canonical depot (smoke test).\n- `CLAUDE.md` and `rbk-claude-tabtarget-context.md` acronym/colophon mappings consistent with new filenames.\n\n## Constraints\n\n- Spec-side rename (RBSIM content + possible file rename to RBSIA, plus RBS0 image-muster section) lands first via BBAAQ. This pace begins after BBAAQ commits.\n- Symbol substitution only — no behavioral changes, no test additions, no helper extractions.\n- Honor paddock's Release-branch execution contract.\n- BCG compliance for any incidental touch beyond pure rename.\n\n## Out of scope\n\n- SA cult-verb roster work (BBAAN — separate domain, separate pace).\n- Spec/README/RBSIM file content (BBAAQ — already in flight).\n- New audit functionality, behavioral changes to listing semantics.\n- `.claude/settings.local.json` rewrite beyond surfacing the three references for operator review.\n- `Tools/rbk/vov_veiled/RBSIR-image_rekon.adoc:18` xref to `{rbtgo_image_muster}` — owned by BBAAQ's spec sweep.",
.claude/jjm/jjg_gallops.json:6812:              "jjgtn_text": "## Character\n\nMechanical rename. JJ owns \"muster\" as a verb (`jjx_list`); RB's image-domain muster verb collides. Per paddock cult-verb naming decision: image muster → **audit**. This pace sweeps all landed image-muster sites in code, tabtargets, and project-memory files.\n\nNo design decisions in scope. The rename target and case rule are paddock-locked. Symbol-substitution discipline only.\n\nDepends on BBAAQ landing the spec-side rename first so spec is source-of-truth at code-rename time.\n\n## Scope\n\n**Bash:**\n\n- `Tools/rbk/rbz_zipper.sh` (L122-129) — `RBZ_MUSTER_HALLMARKS/RELIQUARIES/ENSHRINEMENTS` → `RBZ_AUDIT_*`; `buz_enroll` function refs `rbfl_muster_*` → `rbfl_audit_*`; verb-list comment.\n- `Tools/rbk/rbfl_FoundryLedger.sh` — three function defs (`rbfl_muster_hallmarks/reliquaries/enshrinements` → `rbfl_audit_*`); `buc_doc_brief` strings (\"Muster hallmarks...\" → \"Audit hallmarks...\"); `buc_success \"Muster complete...\"` strings; comments at L19-20 (verb listings).\n- `Tools/rbk/rbfc_FoundryCore.sh:524` — comment (\"shared by tally, rekon, muster, batch_vouch\" → \"audit\").\n\n**Rust:**\n\n- `Tools/rbk/rbtd/src/rbtdrm_manifest.rs:41-46` — three constants `RBTDRM_COLOPHON_MUSTER_HALLMARKS/RELIQUARIES/ENSHRINEMENTS` → `RBTDRM_COLOPHON_AUDIT_*`; verb-list comment at L41.\n\n**Tabtarget files (rename, content unchanged — they're colophon-agnostic launchers):**\n\n- `tt/rbw-imh.DirectorMustersHallmarks.sh` → `tt/rbw-iah.DirectorAuditsHallmarks.sh`\n- `tt/rbw-imr.DirectorMustersReliquaries.sh` → `tt/rbw-iar.DirectorAuditsReliquaries.sh`\n- `tt/rbw-ime.DirectorMustersEnshrinements.sh` → `tt/rbw-iae.DirectorAuditsEnshrinements.sh`\n\n**Project memory:**\n\n- `CLAUDE.md:84` — RBSIM acronym mapping. If BBAAQ renamed the spec file (RBSIM → RBSIA), update both the acronym key and the file path here.\n- `Tools/rbk/rbk-claude-tabtarget-context.md:103-105` — three image table rows (colophon, frontispiece, purpose).\n- `Tools/rbk/vov_veiled/CLAUDE.consumer.md:50` — verb table entry for \"muster\".\n\n**Permission file (operator-owned, surface only):**\n\n- `.claude/settings.local.json:1057-1059` — three tabtarget shim references. Surface to operator with the proposed three replacements; do not silently rewrite.\n\n## Verification\n\n- `tt/` lists three new `rbw-ia*.DirectorAudits*.sh` tabtargets; old `rbw-im*.DirectorMusters*.sh` files absent.\n- `grep -rni 'muster' Tools/rbk/ tt/` returns zero hits in image-domain code paths (SA-domain roster references — if BBAAN landed — and JJ-internal jjk/ references excluded).\n- `tt/rbtd-b.Build.sh` builds clean (Rust constant rename compiles).\n- `tt/rbtd-s.TestSuite.fast.sh` passes — fast suite covers manifest validation; rename should not break enrollment-validation.\n- `tt/rbw-iah.DirectorAuditsHallmarks.sh` invokable end-to-end against the canonical depot (smoke test).\n- `CLAUDE.md` and `rbk-claude-tabtarget-context.md` acronym/colophon mappings consistent with new filenames.\n\n## Constraints\n\n- Spec-side rename (RBSIM content + possible file rename to RBSIA, plus RBS0 image-muster section) lands first via BBAAQ. This pace begins after BBAAQ commits.\n- Symbol substitution only — no behavioral changes, no test additions, no helper extractions.\n- Honor paddock's Release-branch execution contract.\n- BCG compliance for any incidental touch beyond pure rename.\n\n## Out of scope\n\n- SA cult-verb roster work (BBAAN — separate domain, separate pace).\n- Spec/README/RBSIM file content (BBAAQ — already in flight).\n- New audit functionality, behavioral changes to listing semantics.\n- `.claude/settings.local.json` rewrite beyond surfacing the three references for operator review.\n- `Tools/rbk/vov_veiled/RBSIR-image_rekon.adoc:18` xref to `{rbtgo_image_muster}` — owned by BBAAQ's spec sweep.",
.claude/jjm/jjg_gallops.json:7706:              "jjgtn_text": "## Character\n\nMechanical: mint a thin BCG CLI + two tabtargets for the `rbfh` module landed in `₢BBABI`, then add a ~9-case theurge fixture in the fast tier (lifted into the gauntlet suite) for regression coverage of the Dockerfile hygiene contract. **Test path goes through the new tabtargets** — theurge invokes contract surfaces, not module internals. Synthetic-case patterns follow established `rbtdrf_run_tt` usage with a small sibling helper minted here for expect-fail.\n\n## Mint\n\n- `Tools/rbk/rbfh_cli.sh` — BCG CLI paired with the existing `rbfh_FoundryHygiene.sh` library module (sourced therein). Kindle chain loads buc + rbfc (FoundryCore — for vessel resolution) + rbrr regime (for `RBRR_VESSEL_DIR`) + rbrv regime + rbfh. No credentials, no GCP machinery.\n- Public functions:\n  - `rbfh_check <dockerfile-path>` — direct path check; wraps `rbfh_dockerfile_check` after sentinel. No-arg → die with usage (parameter is a free-form path, not a listable set).\n  - `rbfh_check_vessel <vessel-sigil-or-path>` — calls `zrbfc_resolve_vessel` (which internally lists-and-dies on missing/invalid arg at `rbfc_FoundryCore.sh:166-195`), sources resolved vessel's `rbrv.env`, asserts conjure-mode (refuses bind/graft with operator-actionable message), runs check on `RBRV_CONJURE_DOCKERFILE`.\n- Tabtargets (both param1 channel; lowercase `h` paralleling `rbw-fpf`/`rbw-fpc`):\n  - `tt/rbw-fhc.HygieneCheck.sh` → `rbfh_check`\n  - `tt/rbw-fhv.HygieneCheckVessel.sh` → `rbfh_check_vessel`\n- Zipper entries in `rbz_zipper.sh`: `RBZ_HYGIENE_CHECK` and `RBZ_HYGIENE_CHECK_VESSEL`.\n- **New theurge helper `rbtdrf_run_tt_neg`** in `rbtdrf_fast.rs` (~20 lines): mirrors `rbtdrf_run_tt` shape but inverts success-direction — exit 0 → `Err(\"expected failure, got success\")`, exit non-zero → `Ok(())`. Invocation-error (tabtarget not found, launcher failure) still propagates as `Err`. Used only by the 3 negative synthetic cases. Sibling helper preferred over generalizing `rbtdrf_run_tt` with `expect_ok: bool` (which would touch 7 existing call sites).\n\n## Fixture\n\n- New theurge fixture `dockerfile-hygiene` defined in `rbtdrf_fast.rs`, registered in `rbtdrm_manifest.rs`.\n- **Suite membership** (Option A from mount-time gauntlet-coverage analysis):\n  - Add `\"dockerfile-hygiene\"` to `ZRBTE_SUITE_FAST` at `Tools/rbk/rbtd/rbte_engine.sh:48`. Reformat `ZRBTE_SUITE_FAST` to multiline style matching `ZRBTE_SUITE_GAUNTLET` (one fixture per line). The `\"${ZRBTE_SUITE_FAST[@]}\"` spread in `ZRBTE_SUITE_SERVICE`/`_CRUCIBLE`/`_COMPLETE` propagates transitively — fixture flows into all three for free.\n  - Add `\"dockerfile-hygiene\"` explicitly to `ZRBTE_SUITE_GAUNTLET` between `\"regime-smoke\"` and `\"hallmark-lifecycle\"` (post-§3 slot; the all-vessels case needs `${RBRR_VESSEL_DIR}` populated). Gauntlet hand-enumerates fast members — load-bearing addition for pristine-tier regression coverage.\n- **Synthetic cases (8)**: Dockerfile bodies as inline Rust `&'static str` constants. Each case writes the body to a temp file in the case's `dir`, invokes `tt/rbw-fhc <tempfile>` via either `rbtdrf_run_tt` (positive) or `rbtdrf_run_tt_neg` (negative), maps the helper's `Result` to `rbtdre_Verdict`. Coverage:\n  - `dh_accept_parameterized` (positive — `FROM ${RBF_IMAGE_1}`)\n  - `dh_accept_scratch` (positive — `FROM scratch`)\n  - `dh_accept_multistage_as` (positive — `FROM ${RBF_IMAGE_1} AS builder` coda admissible)\n  - `dh_accept_empty` (positive — empty file passes silently)\n  - `dh_accept_comments_only` (positive — column-0 `#` lines only)\n  - `dh_reject_hardcoded_literal` (negative — `FROM python:3.12-slim`)\n  - `dh_reject_tab_in_from` (negative — tab anywhere on FROM line)\n  - `dh_reject_trailing_backslash` (negative — line ends with `\\`)\n- **All-vessels case `dh_all_vessels_pass`** (Rust-led, no bash format-string loop): Rust loop over `read_dir(RBRR_VESSEL_DIR)`. `RBRR_VESSEL_DIR` resolved once via a small one-shot bash that sources rbrr.env + echoes the value (kindle ceremony is unavoidable for derived-path resolution). For each vessel directory: one-shot bash sources `<vessel>/rbrv.env` and echoes `RBRV_VESSEL_MODE`; if `conjure`, invoke `tt/rbw-fhv <sigil>` via `rbtdrf_run_tt`. Maintain a Rust counter; end-of-case assertion fails verdict if zero conjure vessels iterated — closes the silent-zero-iteration trap inherited from `rbtdrf_rv_rbrv_all_vessels`. Theurge knows colophons and sigils; not source paths or kindle chains.\n\n## Touches\n\n- CLAUDE.md File Acronym Mappings: extend the existing RBFH entry with a parenthetical mention of the CLI partner (`rbfh_cli.sh`). One acronym per module-family — mirrors RBOB pattern (`rbob_bottle.sh` listed; `rbob_cli.sh` not separately).\n- Regenerate `Tools/rbk/rbk-claude-tabtarget-context.md` via `tt/rbw-MG.MarshalGenerate.sh` so the two new tabtargets appear in the Foundry table.\n- No spec edits required: `RBSFH-dockerfile_hygiene.adoc` describes the rule, not the test surface. `RBSAK`/`RBSAC` references to `{rbsfh_dockerfile_hygiene}` already landed in `₢BBABI`.\n\n## Verification\n\n- `bash -n` clean on `rbfh_cli.sh`, `rbte_engine.sh`, and any other touched bash modules.\n- `cargo build` clean in `Tools/rbk/rbtd/` (the helper mint + fixture registration are Rust changes).\n- `tt/rbtd-s.TestSuite.fast.sh` green (case count grows ~98 → ~107).\n- Confirm `dockerfile-hygiene` listed in `ZRBTE_SUITE_GAUNTLET` by inspection (running gauntlet end-to-end is cost-prohibitive at notch-time; membership-by-inspection is sufficient).\n- Sanity-check: temporarily flip one synthetic case's positive/negative expectation, confirm the fixture reports the case as failed (verdict assertion is wired correctly), then revert.\n\n## Out of scope\n\n- Handbook prose for the new tabtargets — operators discover via `ls tt/` plus context. Chivvy a doc itch if drift becomes noticeable.\n- Spec subdoc edits to RBSFH for the test surface — fixture is implementation detail, not contract.\n- Decomposing `rbfh_check_vessel`'s no-arg behavior into a separate `rbfh_list_vessels` function — premature factoring; `zrbfc_resolve_vessel` already encapsulates list-and-die.\n- CLAUDE.md acronym-table relocation — chivvied as cantled pace `claude-md-acronym-table-explode` (`₢BBABK`).\n- Migrating the existing 74 enrollment/regime cases off direct-sourcing — chivvied as `₢BJAAB` (theurge-contract-surface-migration) in ₣BJ.\n- Architectural change to make `rbq_qualify_pristine` run fast → gauntlet rather than just gauntlet — out of pace scope; the explicit gauntlet hand-add covers BBABJ's regression-coverage purpose.\n- Generalizing `rbtdrf_run_tt` to take `expect_ok: bool` (touching 7 existing call sites) — the sibling-helper approach scope-isolates to the 3 new negative-case callsites.\n\n## Silks\n\nCurrent proposal `rbfh-cli-and-theurge-fixture` fits — CLI mints two functions + two tabtargets + zipper + helper; fixture adds 9 cases. Both halves load-bearing. Keep unless implementation reveals one collapsing.",
.claude/jjm/jjg_gallops.json:7713:              "jjgtn_text": "## Character\n\nMechanical: mint a thin BCG CLI + two tabtargets for the `rbfh` module landed in `₢BBABI`, then add a ~9-case theurge fixture in the fast tier (lifted into the gauntlet suite) for regression coverage of the Dockerfile hygiene contract. **Test path goes through the new tabtargets** — theurge invokes contract surfaces, not module internals. Synthetic-case patterns follow established `rbtdrf_run_tt` usage with a small sibling helper minted here for expect-fail.\n\n## Mint\n\n- `Tools/rbk/rbfh_cli.sh` — BCG CLI paired with the existing `rbfh_FoundryHygiene.sh` library module (sourced therein). Kindle chain loads buc + rbfc (FoundryCore — for vessel resolution) + rbrr regime (for `RBRR_VESSEL_DIR`) + rbrv regime + rbfh. No credentials, no GCP machinery.\n- Public functions:\n  - `rbfh_check <dockerfile-path>` — direct path check; wraps `rbfh_dockerfile_check` after sentinel. No-arg → die with usage (parameter is a free-form path, not a listable set).\n  - `rbfh_check_vessel <vessel-sigil-or-path>` — calls `zrbfc_resolve_vessel` (which internally lists-and-dies on missing/invalid arg at `rbfc_FoundryCore.sh:166-195`), sources resolved vessel's `rbrv.env`, asserts conjure-mode (refuses bind/graft with operator-actionable message), runs check on `RBRV_CONJURE_DOCKERFILE`.\n- Tabtargets (both param1 channel; lowercase `h` paralleling `rbw-fpf`/`rbw-fpc`):\n  - `tt/rbw-fhc.HygieneCheck.sh` → `rbfh_check`\n  - `tt/rbw-fhv.HygieneCheckVessel.sh` → `rbfh_check_vessel`\n- Zipper entries in `rbz_zipper.sh`: `RBZ_HYGIENE_CHECK` and `RBZ_HYGIENE_CHECK_VESSEL`.\n- **New theurge helper `rbtdrf_run_tt_neg`** in `rbtdrf_fast.rs` (~20 lines): mirrors `rbtdrf_run_tt` shape but inverts success-direction — exit 0 → `Err(\"expected failure, got success\")`, exit non-zero → `Ok(())`. Invocation-error (tabtarget not found, launcher failure) still propagates as `Err`. Used only by the 3 negative synthetic cases. Sibling helper preferred over generalizing `rbtdrf_run_tt` with `expect_ok: bool` (which would touch 7 existing call sites).\n\n## Fixture\n\n- New theurge fixture `dockerfile-hygiene` defined in `rbtdrf_fast.rs`, registered in `rbtdrm_manifest.rs`.\n- **Suite membership** (Option A from mount-time gauntlet-coverage analysis):\n  - Add `\"dockerfile-hygiene\"` to `ZRBTE_SUITE_FAST` at `Tools/rbk/rbtd/rbte_engine.sh:48`. Reformat `ZRBTE_SUITE_FAST` to multiline style matching `ZRBTE_SUITE_GAUNTLET` (one fixture per line). The `\"${ZRBTE_SUITE_FAST[@]}\"` spread in `ZRBTE_SUITE_SERVICE`/`_CRUCIBLE`/`_COMPLETE` propagates transitively — fixture flows into all three for free.\n  - Add `\"dockerfile-hygiene\"` explicitly to `ZRBTE_SUITE_GAUNTLET` between `\"regime-smoke\"` and `\"hallmark-lifecycle\"` (post-§3 slot; the all-vessels case needs `${RBRR_VESSEL_DIR}` populated). Gauntlet hand-enumerates fast members — load-bearing addition for pristine-tier regression coverage.\n- **Synthetic cases (8)**: Dockerfile bodies as inline Rust `&'static str` constants. Each case writes the body to a temp file in the case's `dir`, invokes `tt/rbw-fhc <tempfile>` via either `rbtdrf_run_tt` (positive) or `rbtdrf_run_tt_neg` (negative), maps the helper's `Result` to `rbtdre_Verdict`. Coverage:\n  - `dh_accept_parameterized` (positive — `FROM ${RBF_IMAGE_1}`)\n  - `dh_accept_scratch` (positive — `FROM scratch`)\n  - `dh_accept_multistage_as` (positive — `FROM ${RBF_IMAGE_1} AS builder` coda admissible)\n  - `dh_accept_empty` (positive — empty file passes silently)\n  - `dh_accept_comments_only` (positive — column-0 `#` lines only)\n  - `dh_reject_hardcoded_literal` (negative — `FROM python:3.12-slim`)\n  - `dh_reject_tab_in_from` (negative — tab anywhere on FROM line)\n  - `dh_reject_trailing_backslash` (negative — line ends with `\\`)\n- **All-vessels case `dh_all_vessels_pass`** (Rust-led, no bash format-string loop): Rust loop over `read_dir(RBRR_VESSEL_DIR)`. `RBRR_VESSEL_DIR` resolved once via a small one-shot bash that sources rbrr.env + echoes the value (kindle ceremony is unavoidable for derived-path resolution). For each vessel directory: one-shot bash sources `<vessel>/rbrv.env` and echoes `RBRV_VESSEL_MODE`; if `conjure`, invoke `tt/rbw-fhv <sigil>` via `rbtdrf_run_tt`. Maintain a Rust counter; end-of-case assertion fails verdict if zero conjure vessels iterated — closes the silent-zero-iteration trap inherited from `rbtdrf_rv_rbrv_all_vessels`. Theurge knows colophons and sigils; not source paths or kindle chains.\n\n## Touches\n\n- CLAUDE.md File Acronym Mappings: extend the existing RBFH entry with a parenthetical mention of the CLI partner (`rbfh_cli.sh`). One acronym per module-family — mirrors RBOB pattern (`rbob_bottle.sh` listed; `rbob_cli.sh` not separately).\n- Regenerate `Tools/rbk/rbk-claude-tabtarget-context.md` via `tt/rbw-MG.MarshalGenerate.sh` so the two new tabtargets appear in the Foundry table.\n- No spec edits required: `RBSFH-dockerfile_hygiene.adoc` describes the rule, not the test surface. `RBSAK`/`RBSAC` references to `{rbsfh_dockerfile_hygiene}` already landed in `₢BBABI`.\n\n## Verification\n\n- `bash -n` clean on `rbfh_cli.sh`, `rbte_engine.sh`, and any other touched bash modules.\n- `cargo build` clean in `Tools/rbk/rbtd/` (the helper mint + fixture registration are Rust changes).\n- `tt/rbtd-s.TestSuite.fast.sh` green (case count grows ~98 → ~107).\n- Confirm `dockerfile-hygiene` listed in `ZRBTE_SUITE_GAUNTLET` by inspection (running gauntlet end-to-end is cost-prohibitive at notch-time; membership-by-inspection is sufficient).\n- Sanity-check: temporarily flip one synthetic case's positive/negative expectation, confirm the fixture reports the case as failed (verdict assertion is wired correctly), then revert.\n\n## Out of scope\n\n- Handbook prose for the new tabtargets — operators discover via `ls tt/` plus context. Chivvy a doc itch if drift becomes noticeable.\n- Spec subdoc edits to RBSFH for the test surface — fixture is implementation detail, not contract.\n- Decomposing `rbfh_check_vessel`'s no-arg behavior into a separate `rbfh_list_vessels` function — premature factoring; `zrbfc_resolve_vessel` already encapsulates list-and-die.\n- CLAUDE.md acronym-table relocation — chivvied as cantled pace `claude-md-acronym-table-explode` (`₢BBABK`).\n- Migrating the existing 74 enrollment/regime cases off direct-sourcing — chivvied as `₢BJAAB` (theurge-contract-surface-migration) in ₣BJ.\n- Architectural change to make `rbq_qualify_pristine` run fast → gauntlet rather than just gauntlet — out of pace scope; the explicit gauntlet hand-add covers BBABJ's regression-coverage purpose.\n- Generalizing `rbtdrf_run_tt` to take `expect_ok: bool` (touching 7 existing call sites) — the sibling-helper approach scope-isolates to the 3 new negative-case callsites.\n\n## Silks\n\nCurrent proposal `rbfh-cli-and-theurge-fixture` fits — CLI mints two functions + two tabtargets + zipper + helper; fixture adds 9 cases. Both halves load-bearing. Keep unless implementation reveals one collapsing.",
.claude/jjm/jjg_gallops.json:7720:              "jjgtn_text": "## Character\n\nMechanical: mint a thin BCG CLI + two tabtargets for the `rbfh` module landed in `₢BBABI`, then add a ~9-case theurge fixture in the fast tier (lifted into the gauntlet suite) for regression coverage of the Dockerfile hygiene contract. Patterns follow `regime-validation` for both reach (path-mode tabtarget per synthetic case, single all-vessels case with bash-side iteration) and shape (inline Rust string constants for synthetic Dockerfile bodies, mirroring `rbtdrf_rv_*` setup strings).\n\n## Mint\n\n- `Tools/rbk/rbfh_cli.sh` — BCG CLI paired with the existing `rbfh_FoundryHygiene.sh` library module (sourced therein). Kindle chain loads buc + rbfc (FoundryCore — needed for vessel resolution) + rbrr regime (for `RBRR_VESSEL_DIR`) + rbrv regime + rbfh. No credentials, no GCP machinery.\n- Public functions:\n  - `rbfh_check <dockerfile-path>` — direct path check; wraps `rbfh_dockerfile_check` after sentinel. No-arg → die with usage (parameter is a free-form path, not a listable set).\n  - `rbfh_check_vessel <vessel-sigil-or-path>` — calls `zrbfc_resolve_vessel` (which internally lists-and-dies on missing/invalid arg at `rbfc_FoundryCore.sh:166-195`), sources resolved vessel's `rbrv.env`, asserts conjure-mode (refuses bind/graft with operator-actionable message), runs check on `RBRV_CONJURE_DOCKERFILE`. No separate list-and-die handling required — `zrbfc_resolve_vessel` fully encapsulates that behavior.\n- Tabtargets (both param1 channel; lowercase `h` since hygiene is read-only/local, paralleling `rbw-fpf`/`rbw-fpc`):\n  - `tt/rbw-fhc.HygieneCheck.sh` → `rbfh_check`\n  - `tt/rbw-fhv.HygieneCheckVessel.sh` → `rbfh_check_vessel`\n- Zipper entries in `rbz_zipper.sh`: `RBZ_HYGIENE_CHECK` and `RBZ_HYGIENE_CHECK_VESSEL`.\n\n## Fixture\n\n- New theurge fixture `dockerfile-hygiene` defined in `rbtdrf_fast.rs`, registered in `rbtdrm_manifest.rs`.\n- **Suite membership** — Option A from mount-time gauntlet-coverage analysis:\n  - Add `\"dockerfile-hygiene\"` to `ZRBTE_SUITE_FAST` at `Tools/rbk/rbtd/rbte_engine.sh:48`. Reformat `ZRBTE_SUITE_FAST` to multiline style matching `ZRBTE_SUITE_GAUNTLET` (one fixture per line). The `\"${ZRBTE_SUITE_FAST[@]}\"` spread used by `ZRBTE_SUITE_SERVICE`/`_CRUCIBLE`/`_COMPLETE` is preserved by bash multiline-array syntax — fixture flows transitively into those three.\n  - Add `\"dockerfile-hygiene\"` explicitly to `ZRBTE_SUITE_GAUNTLET` between `\"regime-smoke\"` and `\"hallmark-lifecycle\"` (post-§3 slot — state-indifferent at the level of synthetic cases, but the all-vessels case needs `${RBRR_VESSEL_DIR}` populated). Gauntlet hand-enumerates its fast members (per `rbte_engine.sh` comment) — new fast fixtures do NOT propagate automatically; this addition is load-bearing for pristine-tier regression coverage.\n- Synthetic cases (~8): Dockerfile bodies as inline Rust `&'static str` constants; each case writes the body to a temp file in the case's `dir`, invokes `tt/rbw-fhc <tempfile>` via `rbtdrf_run_tt`, asserts verdict. Coverage:\n  - `dh_accept_parameterized` (positive — `FROM ${RBF_IMAGE_1}`)\n  - `dh_accept_scratch` (positive — `FROM scratch`)\n  - `dh_accept_multistage_as` (positive — `AS builder` coda admissible)\n  - `dh_accept_empty` (positive — empty file passes silently)\n  - `dh_accept_comments_only` (positive — column-0 `#` lines only)\n  - `dh_reject_hardcoded_literal` (negative — `FROM python:3.12-slim`)\n  - `dh_reject_tab_in_from` (negative — tab anywhere on FROM line)\n  - `dh_reject_trailing_backslash` (negative — line ends with `\\`)\n- One all-vessels case `dh_all_vessels_pass`: bash script iterates `${RBRR_VESSEL_DIR}/*` (mirroring `rbtdrf_rv_rbrv_all_vessels` at `rbtdrf_fast.rs:967`), sources each `rbrv.env` in a subshell to read `RBRV_VESSEL_MODE`, skips non-conjure modes, invokes `tt/rbw-fhv <sigil>` per conjure vessel, fails on any non-zero. No hardcoded vessel paths in theurge — discovery flows through the regime's own pointer.\n\n## Touches\n\n- CLAUDE.md File Acronym Mappings: extend the existing RBFH entry with a parenthetical mention of the CLI partner (`rbfh_cli.sh`). Convention is one acronym per module-family — mirrors RBOB pattern (`rbob_bottle.sh` listed; `rbob_cli.sh` not separately).\n- Regenerate `Tools/rbk/rbk-claude-tabtarget-context.md` via `tt/rbw-MG.MarshalGenerate.sh` so the two new tabtargets appear in the Foundry table.\n- No spec edits required: `RBSFH-dockerfile_hygiene.adoc` describes the rule itself, not the test surface. `RBSAK`/`RBSAC` references to `{rbsfh_dockerfile_hygiene}` already landed in `₢BBABI`.\n\n## Verification\n\n- `bash -n` clean on `rbfh_cli.sh`, `rbte_engine.sh`, and any other touched bash modules.\n- `tt/rbtd-s.TestSuite.fast.sh` green (case count grows ~98 → ~107).\n- Confirm `dockerfile-hygiene` listed in `ZRBTE_SUITE_GAUNTLET` by inspection (running gauntlet end-to-end is cost-prohibitive at notch-time; membership-by-inspection is sufficient).\n- Sanity-check: temporarily flip one synthetic case's `expect_ok` flag, confirm the fixture reports the case as failed (verdict assertion is wired correctly), then revert.\n\n## Out of scope\n\n- Handbook prose for the new tabtargets — operators discover via `ls tt/` plus context. Chivvy a doc itch if drift becomes noticeable.\n- Spec subdoc edits to RBSFH for the test surface — fixture is implementation detail, not contract.\n- Decomposing `rbfh_check_vessel`'s no-arg behavior into a separate `rbfh_list_vessels` function — premature factoring; `zrbfc_resolve_vessel` already encapsulates list-and-die.\n- CLAUDE.md acronym-table relocation — chivvied as cantled pace `claude-md-acronym-table-explode` (`₢BBABK`).\n- Architectural change to make `rbq_qualify_pristine` run fast → gauntlet rather than just gauntlet — out of pace scope; the gauntlet hand-add covers BBABJ's regression-coverage purpose.\n\n## Silks\n\nCurrent proposal `rbfh-cli-and-theurge-fixture` fits if `cli` and `fixture` both feel load-bearing at mount time. If just one (probably the fixture) ends up doing most of the work, consider relabeling.",
.claude/jjm/jjg_gallops.json:7727:              "jjgtn_text": "## Character\n\nMechanical: mint a thin BCG CLI + two tabtargets for the `rbfh` module landed in `₢BBABI`, then add a ~9-case theurge fixture in the fast tier for regression coverage of the Dockerfile hygiene contract. Patterns follow `regime-validation` for both reach (path-mode tabtarget per synthetic case, single all-vessels case with bash-side iteration) and shape (inline Rust string constants for synthetic Dockerfile bodies, mirroring `rbtdrf_rv_*` setup strings).\n\n## Mint\n\n- `Tools/rbk/rbfh_cli.sh` — BCG CLI paired with the existing `rbfh_FoundryHygiene.sh` library module (sourced therein). Kindle chain loads buc + rbfc (FoundryCore — needed for vessel resolution) + rbrr regime (for `RBRR_VESSEL_DIR`) + rbrv regime + rbfh. No credentials, no GCP machinery.\n- Public functions:\n  - `rbfh_check <dockerfile-path>` — direct path check; wraps `rbfh_dockerfile_check` after sentinel.\n  - `rbfh_check_vessel <vessel-sigil-or-path>` — resolves via `zrbfc_resolve_vessel`, sources vessel rbrv.env, asserts conjure-mode (refuses bind/graft with operator-actionable message), runs check on `RBRV_CONJURE_DOCKERFILE`. No-arg → list available vessels and die helpfully (match `rbfd_build`'s pattern at `rbfd_FoundryDirectorBuild.sh:1178`).\n- Tabtargets (both param1 channel; lowercase `h` since hygiene is read-only/local, paralleling `rbw-fpf`/`rbw-fpc`):\n  - `tt/rbw-fhc.HygieneCheck.sh` → `rbfh_check`\n  - `tt/rbw-fhv.HygieneCheckVessel.sh` → `rbfh_check_vessel`\n- Zipper entries in `rbz_zipper.sh`: `RBZ_HYGIENE_CHECK` and `RBZ_HYGIENE_CHECK_VESSEL`.\n\n## Fixture\n\n- New theurge fixture `dockerfile-hygiene` defined in `rbtdrf_fast.rs`, registered in `rbtdrm_manifest.rs`, assigned to the fast suite.\n- Synthetic cases (~8): Dockerfile bodies as inline Rust `&'static str` constants; each case writes the body to a temp file in the case's `dir`, invokes `tt/rbw-fhc <tempfile>` via `rbtdrf_run_tt`, asserts verdict. Coverage:\n  - `dh_accept_parameterized` (positive — `FROM ${RBF_IMAGE_1}`)\n  - `dh_accept_scratch` (positive — `FROM scratch`)\n  - `dh_accept_multistage_as` (positive — `AS builder` coda admissible)\n  - `dh_accept_empty` (positive — empty file passes silently)\n  - `dh_accept_comments_only` (positive — column-0 `#` lines only)\n  - `dh_reject_hardcoded_literal` (negative — `FROM python:3.12-slim`)\n  - `dh_reject_tab_in_from` (negative — tab anywhere on FROM line)\n  - `dh_reject_trailing_backslash` (negative — line ends with `\\`)\n- One all-vessels case `dh_all_vessels_pass`: bash script iterates `${RBRR_VESSEL_DIR}/*` (mirroring `rbtdrf_rv_rbrv_all_vessels` at `rbtdrf_fast.rs:967`), sources each `rbrv.env` in a subshell to read `RBRV_VESSEL_MODE`, skips non-conjure modes, invokes `tt/rbw-fhv <sigil>` per conjure vessel, fails on any non-zero. No hardcoded vessel paths in theurge — discovery flows through the regime's own pointer.\n\n## Touches\n\n- CLAUDE.md File Acronym Mappings: extend the RBFH entry to mention the CLI counterpart, or add a sibling `rbfh_cli.sh` line.\n- Regenerate `rbk-claude-tabtarget-context.md` via `tt/rbw-MG.MarshalGenerate.sh` so the two new tabtargets appear in the Foundry table.\n- No spec edits required: `RBSFH-dockerfile_hygiene.adoc` describes the rule itself, not the test surface. `RBSAK`/`RBSAC` references to `{rbsfh_dockerfile_hygiene}` already landed in `₢BBABI`.\n\n## Verification\n\n- `bash -n` clean on `rbfh_cli.sh` and any touched modules.\n- `tt/rbtd-s.TestSuite.fast.sh` green (case count grows ~98 → ~107).\n- Sanity-check: temporarily flip one synthetic case's `expect_ok` flag, confirm the fixture reports the case as failed (verdict assertion is wired correctly), then revert.\n\n## Out of scope\n\n- Handbook prose for the new tabtargets — operators discover via `ls tt/` plus context. Chivvy a doc itch if drift becomes noticeable.\n- Spec subdoc edits to RBSFH for the test surface — fixture is implementation detail, not contract.\n- Decomposing `rbfh_check_vessel`'s no-arg list-and-die into a separate `rbfh_list_vessels` function — premature factoring.\n\n## Silks\n\nCurrent proposal `rbfh-cli-and-theurge-fixture` fits if `cli` and `fixture` both feel load-bearing at mount time. If just one (probably the fixture) ends up doing most of the work, consider relabeling.",
.claude/jjm/jjg_gallops.json:7738:              "jjgtn_text": "## Character\n\nMechanical: relocate per-module acronym mappings from project-root `CLAUDE.md` into the existing per-kit `*-claude-context.md` files. Each kit already has a context file `@`-included from CLAUDE.md, so payload is token-neutral — content moves, not duplicates. Blast radius for future per-module acronym mints collapses to domain-local.\n\n## Move\n\nPer-module acronym tables migrate to per-kit context files:\n\n- **RBK** acronyms (~140 lines under `### Tools Directory` / `#### RBK Subdirectory` in CLAUDE.md) → fold into `Tools/rbk/rbk-claude-tabtarget-context.md` as a new top section, OR mint `Tools/rbk/rbk-claude-acronyms.md` if the tabtarget file feels overloaded (mount-time call).\n- **BUK** acronyms → `Tools/buk/buk-claude-context.md` (which already has \"BUK Concepts\").\n- **JJK** acronyms → `Tools/jjk/vov_veiled/jjk-claude-context.md` (already has \"Job Jockey Configuration\").\n- **CMK** acronyms → `Tools/cmk/vov_veiled/cmk-claude-context.md` (already partially holds CMK file acronyms; consolidate, remove duplicates from CLAUDE.md).\n- **GAD** acronyms → CLAUDE.md currently redirects to `Tools/gad/CLAUDE.md`; verify target completeness, remove redirect stub from project-root.\n- **VOK** acronyms (RCG, VLS, VOS0) → fold into VOK context file (verify path at mount-time — context above mentions VOK but not its claude-context location).\n- **CNMP Lenses** acronyms → fold into the appropriate context file in `../cnmp_CellNodeMessagePrototype/`, or leave if cross-repo coordination required (mount-time call — likely defer).\n\n## Keep in CLAUDE.md\n\n- Directory Permissions\n- Project Prefix Registry (top-level prefix→project map)\n- Prefix Naming Discipline, Quoin Sub-Letter Discipline, Minting Workflow, Kit Infrastructure Suffixes\n- Heredoc Delimiter Selection\n- AsciiDoc Linked Terms guidance\n- Rust Build Discipline, Test Execution\n- Working Preferences\n- Common Workflows\n- Design Principles (Load-Bearing Complexity)\n- `@`-includes to kit context files (unchanged)\n\nThe split is: **rules and top-level prefix→project map stay in CLAUDE.md; per-module file listings live with the kit they describe**.\n\n## Verification\n\n- `tt/rbtd-s.TestSuite.fast.sh` green (handbook-render re-renders full handbook through includes — surfaces broken cross-references).\n- Spot-check three acronym lookups (`RBFH`, `BUC`, `MCM`) — each finds its entry in exactly one place across the loaded context.\n- Project Prefix Registry in CLAUDE.md still enumerates every top-level prefix (`rb`, `bu`, `jj`, `mcm`/`axl`, `gad`, etc.).\n- CLAUDE.md line-count drops materially (current acronym table dominates the file).\n\n## Out of scope\n\n- Adding new acronyms not currently in CLAUDE.md.\n- Restructuring kit context files beyond accommodating the moved tables.\n- Touching the GAD/CMK redirect entries if those already work end-to-end (verify-don't-fix).\n- Cross-repo CNMP coordination if it requires touching a sibling repository.\n\n## Silks\n\n`claude-md-acronym-table-explode` — \"explode\" chosen over \"split\"/\"decompose\" because the work is fan-out across many kit files, not bisection. If RBK ends up minting its own dedicated acronyms file rather than folding into tabtarget context, the silks still fits.",
.claude/jjm/jjg_gallops.json:7745:              "jjgtn_text": "## Character\n\nMechanical: relocate per-module acronym mappings from project-root `CLAUDE.md` into the existing per-kit `*-claude-context.md` files. Each kit already has a context file `@`-included from CLAUDE.md, so payload is token-neutral — content moves, not duplicates. Blast radius for future per-module acronym mints collapses to domain-local.\n\n## Move\n\nPer-module acronym tables migrate to per-kit context files:\n\n- **RBK** acronyms (~140 lines under `### Tools Directory` / `#### RBK Subdirectory` in CLAUDE.md) → fold into `Tools/rbk/rbk-claude-tabtarget-context.md` as a new top section, OR mint `Tools/rbk/rbk-claude-acronyms.md` if the tabtarget file feels overloaded (mount-time call).\n- **BUK** acronyms → `Tools/buk/buk-claude-context.md` (which already has \"BUK Concepts\").\n- **JJK** acronyms → `Tools/jjk/vov_veiled/jjk-claude-context.md` (already has \"Job Jockey Configuration\").\n- **CMK** acronyms → `Tools/cmk/vov_veiled/cmk-claude-context.md` (already partially holds CMK file acronyms; consolidate, remove duplicates from CLAUDE.md).\n- **GAD** acronyms → CLAUDE.md currently redirects to `Tools/gad/CLAUDE.md`; verify target completeness, remove redirect stub from project-root.\n- **VOK** acronyms (RCG, VLS, VOS0) → fold into VOK context file (verify path at mount-time — context above mentions VOK but not its claude-context location).\n- **CNMP Lenses** acronyms → fold into the appropriate context file in `../cnmp_CellNodeMessagePrototype/`, or leave if cross-repo coordination required (mount-time call — likely defer).\n\n## Keep in CLAUDE.md\n\n- Directory Permissions\n- Project Prefix Registry (top-level prefix→project map)\n- Prefix Naming Discipline, Quoin Sub-Letter Discipline, Minting Workflow, Kit Infrastructure Suffixes\n- Heredoc Delimiter Selection\n- AsciiDoc Linked Terms guidance\n- Rust Build Discipline, Test Execution\n- Working Preferences\n- Common Workflows\n- Design Principles (Load-Bearing Complexity)\n- `@`-includes to kit context files (unchanged)\n\nThe split is: **rules and top-level prefix→project map stay in CLAUDE.md; per-module file listings live with the kit they describe**.\n\n## Verification\n\n- `tt/rbtd-s.TestSuite.fast.sh` green (handbook-render re-renders full handbook through includes — surfaces broken cross-references).\n- Spot-check three acronym lookups (`RBFH`, `BUC`, `MCM`) — each finds its entry in exactly one place across the loaded context.\n- Project Prefix Registry in CLAUDE.md still enumerates every top-level prefix (`rb`, `bu`, `jj`, `mcm`/`axl`, `gad`, etc.).\n- CLAUDE.md line-count drops materially (current acronym table dominates the file).\n\n## Out of scope\n\n- Adding new acronyms not currently in CLAUDE.md.\n- Restructuring kit context files beyond accommodating the moved tables.\n- Touching the GAD/CMK redirect entries if those already work end-to-end (verify-don't-fix).\n- Cross-repo CNMP coordination if it requires touching a sibling repository.\n\n## Silks\n\n`claude-md-acronym-table-explode` — \"explode\" chosen over \"split\"/\"decompose\" because the work is fan-out across many kit files, not bisection. If RBK ends up minting its own dedicated acronyms file rather than folding into tabtarget context, the silks still fits.",
Tools/rbk/vov_veiled/CLAUDE.consumer.md:117:@Tools/rbk/rbk-claude-tabtarget-context.md
.claude/jjm/retired/jjh_b260401-r260409-rbk-theurge-rust-test-infrastructure.md:2850:Add per-colophon description support to BUK zipper and a Marshal operation that generates `rbk-claude-tabtarget-context.md` from the zipper registry. Both CLAUDE.md variants `@`-include the generated file, eliminating hand-maintained colophon tables.
.claude/jjm/retired/jjh_b260401-r260409-rbk-theurge-rust-test-infrastructure.md:2862:- Add Marshal generation operation: walk zipper declarations, emit `rbk-claude-tabtarget-context.md`
.claude/jjm/retired/jjh_b260401-r260409-rbk-theurge-rust-test-infrastructure.md:2884:Add per-colophon description support to BUK zipper and a Marshal operation that generates `rbk-claude-tabtarget-context.md` from the zipper registry. Both CLAUDE.md variants `@`-include the generated file, eliminating hand-maintained colophon tables.
.claude/jjm/retired/jjh_b260401-r260409-rbk-theurge-rust-test-infrastructure.md:2896:- Add Marshal generation operation: walk zipper declarations, emit `rbk-claude-tabtarget-context.md`
.claude/jjm/retired/jjh_b260401-r260409-rbk-theurge-rust-test-infrastructure.md:3919:···························xx·······x··· rbk-claude-tabtarget-context.md
.claude/jjm/retired/jjh_b260401-r260409-rbk-theurge-rust-test-infrastructure.md:4198:Added per-colophon description as 6th required arg to buz_enroll, made buz_group store metadata, added buz_emit_context to generate markdown from zipper registry. New rbw-MG marshal command generates rbk-claude-tabtarget-context.md (55 colophons across 11 groups). Qualify freshness gate ensures generated file stays current. Both CLAUDE files @-include the generated context, eliminating hand-maintained colophon tables. Updated buwz_zipper.sh for new 6-arg contract. 135 tests pass (9 BUK + 51 theurge unit + 75 fast suite).
.claude/jjm/retired/jjh_b260401-r260409-rbk-theurge-rust-test-infrastructure.md:4202:Added buz_describe as 6th required arg to buz_enroll, buz_group stores metadata, buz_emit_context generates markdown from zipper registry. New rbw-MG marshal command generates rbk-claude-tabtarget-context.md. Qualify freshness gate. Both CLAUDE files @-include generated context, replacing hand-maintained colophon tables. 135 tests pass.
.claude/jjm/retired/jjh_b260410-r260420-rbk-mvp-3-public-docs-refresh-02.md:1758:x······ RBSAK-ark_kludge.adoc, buh_handbook.sh, rbcc_Constants.sh, rbfc_FoundryCore.sh, rbho_cli.sh, rbho_onboarding.sh, rbk-claude-tabtarget-context.md, rbk-claude-theurge-ifrit-context.md, rbtdrm_manifest.rs, rbw-fA.DirectorAbjuresHallmark.sh, rbw-fO.DirectorOrdainsHallmark.sh, rbw-fV.DirectorVouchesHallmarks.sh, rbw-fk.LocalKludge.sh, rbw-fpc.RetrieverPlumbsCompact.sh, rbw-fpf.RetrieverPlumbsFull.sh, rbw-fs.RetrieverSummonsHallmark.sh, rbw-ft.DirectorTalliesHallmarks.sh, rbw-hA.DirectorAbjuresHallmark.sh, rbw-hO.DirectorOrdainsHallmark.sh, rbw-hV.DirectorVouchesHallmarks.sh, rbw-hk.LocalKludge.sh, rbw-hpc.RetrieverPlumbsCompact.sh, rbw-hpf.RetrieverPlumbsFull.sh, rbw-hs.RetrieverSummonsHallmark.sh, rbw-ht.DirectorTalliesHallmarks.sh, rbw_workbench.sh, rbz_zipper.sh
.claude/jjm/retired/jjh_b260520-r260529-rbk-13-mvp-redundancy-cleanup.md:1133:·xx···· rbk-claude-tabtarget-context.md
Memos/memo-20260409-system-prompt-capture-opus.md:149:- @Tools/rbk/rbk-claude-tabtarget-context.md include
Memos/memo-20260409-system-prompt-capture-opus.md:163:Contents of /Users/bhyslop/projects/rbm_alpha_recipemuster/Tools/rbk/rbk-claude-tabtarget-context.md (project instructions, checked into the codebase):
Memos/memo-20260409-system-prompt-capture-opus.md:243:  3. The @-included context files (buk-claude-context.md, cmk-claude-context.md, rbk-claude-tabtarget-context.md, jjk-claude-context.md, vvk-claude-context.md) were similarly summarized.
.claude/jjm/retired/jjh_b260415-r260514-rbk-10-mvp-regen-retest-depot.md:2238:- `tt/rbw-MG.MarshalGenerate.sh` regenerates `Tools/rbk/rbk-claude-tabtarget-context.md` to reflect the new signatures
.claude/jjm/retired/jjh_b260415-r260514-rbk-10-mvp-regen-retest-depot.md:2249:- Regenerated `rbk-claude-tabtarget-context.md` reflects the new signatures
.claude/jjm/retired/jjh_b260415-r260514-rbk-10-mvp-regen-retest-depot.md:2302:- `tt/rbw-MG.MarshalGenerate.sh` regenerates `Tools/rbk/rbk-claude-tabtarget-context.md` to reflect the new signatures
.claude/jjm/retired/jjh_b260415-r260514-rbk-10-mvp-regen-retest-depot.md:2313:- Regenerated `rbk-claude-tabtarget-context.md` reflects the new signatures
.claude/jjm/retired/jjh_b260415-r260514-rbk-10-mvp-regen-retest-depot.md:2361:- `tt/rbw-MG.MarshalGenerate.sh` regenerates `Tools/rbk/rbk-claude-tabtarget-context.md` to reflect the new signatures
.claude/jjm/retired/jjh_b260415-r260514-rbk-10-mvp-regen-retest-depot.md:2371:- Regenerated `rbk-claude-tabtarget-context.md` reflects the new signatures
.claude/jjm/retired/jjh_b260415-r260514-rbk-10-mvp-regen-retest-depot.md:5374:···x················· rbk-claude-tabtarget-context.md, rbz_zipper.sh
.claude/jjm/retired/jjh_b260415-r260514-rbk-10-mvp-regen-retest-depot.md:5879:Jettison locator grammar rewritten to post-AAK form in a single file (Tools/rbk/rbfl_FoundryLedger.sh lines 326-395). Locator parsing switched from first-colon split (%%:* / #*:) to last-colon split (%:* / ##*:), mirroring the rbfr_wrest idiom adopted in AAK Notch 2a — required because post-AAK package paths contain slashes (hallmarks/<H>/<basename>, reliquaries/<date>/<tool>) and the pre-AAK bare-moniker URL construction would mis-target any multi-segment path. URL `${ZRBFC_REGISTRY_API_BASE}/${RBRR_CLOUD_PREFIX}${z_moniker}/manifests/${z_tag}` rewritten to `${ZRBFC_REGISTRY_API_BASE}/${RBRR_CLOUD_PREFIX}${z_pkg_path}/manifests/${z_tag}` so the package path's sub-namespacing survives to the DELETE endpoint. Local `z_moniker` renamed to `z_pkg_path` throughout the function body. Docstring example updated from pre-AAK `rbev-busybox:20251231T160211Z-img` to post-AAK `hallmarks/H/image:H` (mirrors rbfr_wrest's docstring style). Validation prose: required-param die message 'moniker:tag' → 'package-path:tag', case-label error 'Expected moniker:tag' → 'Expected package-path:tag', empty-check 'Moniker is empty' → 'Package path is empty'. Docket-flagged Rust call-site audit was a no-op — grep for RBTDRM_COLOPHON_JETTISON / jettison across Tools/rbk/rbtd/src/ returned zero hits; theurge has no jettison consumers, so the 'check for any that pass pre-AAK locator strings' concern in the docket was speculative and there was nothing to fix. Scope came in narrower than the docket anticipated: a single file, no Rust, no tabtarget or zipper churn (channel already empty per the docket's cross-cutting note). BCG review performed pre-commit at user's explicit request: braced quoted expansion throughout, patterns in %:* and ##*: are literal colon-and-star so the BCG §381-417 'inner expansion quoted separately' rule is satisfied trivially (no variable-in-pattern glob risk), no new $() or stderr captures introduced since the edit is pure string manipulation, and local declarations stayed without -r to match the surrounding function's existing style (z_locator, z_force, z_status_file are all plain local in this function). Verification: bash -n clean on rbfl_FoundryLedger.sh; fast suite 90/90 green (47 enrollment-validation + 21 regime-validation + 7 regime-smoke + 15 handbook-render) matching baseline. Service and crucible suites not run from this officium (no live credentials, container runtime not required for a parsing-only change), and no live jettison against a known hallmark was exercised (no disposable test ark available in this session); live verification deferred to downstream — the parsing is mechanically equivalent to rbfr_wrest which is already exercised in live pull flows. Attribution clean: notched as commit 7ede2fd6 with only Tools/rbk/rbfl_FoundryLedger.sh in the file list; rbk-claude-tabtarget-context.md and rbz_zipper.sh were dirty at record time from another concurrent officium and were correctly left out of this pace's commit per multi-officium discipline. Heat state: with AAR closed, the two post-AAK orthogonal bugs that AAL flagged (AAQ rekon-hallmark-catalog and AAR jettison-locator-grammar) have split — AAR landed, AAQ remains the next actionable rough pace awaiting decision between the three shape options (raw hallmark listing / hallmark-indexed basename listing / dissolve) that its docket enumerates.
.claude/jjm/retired/jjh_b260415-r260514-rbk-10-mvp-regen-retest-depot.md:5887:Post-AAK endpoint rewrite for rbfl_rekon: dropped pre-AAK <prefix><moniker>/tags/list Docker V2 registry query in favor of the GAR packages.list categorical-layout enumeration via shared helper zrbfc_list_hallmarks_capture (landed in ₢A_AAO b5603dc6). Signature changed from (moniker) to (hallmark); output reshaped to the three-column docket-specified BASENAME/EXISTS/PACKAGE-PATH table iterating the six canonical basenames (image, about, vouch, attest, pouch, diags) with yes/no presence and absolute package path or (absent). Director auth posture preserved. Implementation hews to rbfl_tally's shape (authenticate, enumerate, filter, classify) with the classifier collapsed to per-basename membership since this is a single-hallmark diagnostic rather than tally's fleet-wide classifier; present-basenames accumulator uses the space-padded substring membership idiom (case match on " ${z_present} ") to stay bash 3.2 compatible without associative arrays. BCG review pre-commit: braced-quoted expansion throughout; local -r on the single hallmark param; loop-synthesized z_line/z_h/z_b and z_canon/z_mark/z_path use plain local per Exception 2; two-line capture pattern for z_token; test not [[; no heredocs; while IFS= read has trailing-newline guard; zrbfc_list_hallmarks_capture invoked bare (not inside $()) matching rbfl_tally's usage; no new stderr captures introduced (the shared helper owns its curl/jq error handling). Zipper help text for RBZ_REKON_IMAGE updated from 'Raw GAR tag listing for a vessel package' to 'List ark basenames present under a hallmark's GAR subtree' reflecting the new per-hallmark semantic; tabtarget context regenerated via tt/rbw-MG.MarshalGenerate.sh produced a one-line diff on the Image row. Multi-officium overlap: my rbfl_FoundryLedger.sh edit (the rbfl_rekon function rewrite) was staged in the working tree when ₢A_AAR's jjx_record for jettison-grammar fired; the record named rbfl_FoundryLedger.sh in its file list so my rekon function body rode along under commit 7ede2fd6's A_AAR attribution. git show HEAD:Tools/rbk/rbfl_FoundryLedger.sh confirms rbfl_rekon at line 597 with the new (hallmark) signature, zrbfc_list_hallmarks_capture consumption, and three-column printf table. Same overlap phenomenon AAK Notch 2a and AAL both documented — not destructive, work is live in the tree, only commit-log attribution is cross-cutting. Items explicitly landing under AAQ attribution: (1) rbz_zipper.sh help-text tweak, (2) rbk-claude-tabtarget-context.md regen. Out-of-scope docket items verified as no-ops: RBTDRM_COLOPHON_REKON does not exist in Tools/ (grep zero hits across the theurge crate) — the docket speculated the four-mode fixture might exercise rekon, but crucible has no rekon consumer, so no Rust call-site changes were needed. Verification: bash -n clean on both edited files; fast suite 90/90 green (47 enrollment-validation + 21 regime-validation + 7 regime-smoke + 15 handbook-render) matching baseline. Service and crucible suites not run from this officium (no live credentials / container runtime not guaranteed); live rbw-ir against a known hallmark deferred to downstream verification — the enumeration helper it consumes is already exercised live by rbfl_tally and rbfv_batch_vouch, so the new code path rides on already-validated infrastructure. Not updated as out-of-scope: RBSIR-image_rekon.adoc spec (the docket listed no spec-alignment task; spec can be swept in an RBS follow-up pace if needed).
.claude/jjm/retired/jjh_b260415-r260514-rbk-10-mvp-regen-retest-depot.md:5903:AAL vessel-param-drop pace shipped clean but asymmetrically attributed due to multi-officium overlap. Real code changes delivered: (1) rbfc_vessel_for_hallmark_capture helper minted in rbfc_FoundryCore.sh, authenticates as Retriever and extracts vouch ark to read .vessel from vouch_summary.json; single home prevents reinvention at each call site; BCG-mandated _capture suffix (not the bare name the docket proposed) because $() invocation requires it. (2) zrbfc_plumb_core refactored to hallmark-only signature (hallmark, mode) — removed the test-for-empty-vessel single-arg fallback that was AAK-era forward-compat scaffolding; vessel is now always resolved from vouch via the new helper, then rbfc_require_vessel_sigil + zrbfc_load_vessel for RBRV_VESSEL_MODE. (3) rbfc_plumb_full and rbfc_plumb_compact collapsed from (vessel, hallmark) to (hallmark) and pass through to the simplified plumb_core. (4) rbfl_abjure signature dropped $1 vessel — zrbfc_resolve_vessel + zrbfc_load_vessel calls removed (post-AAK they were dead weight; the registry path is hallmark-indexed and vessel doesn't participate); param-shift $2 hallmark → $1 and $3 force → $2; docstring updated. (5) rbfr_summon signature dropped $1 vessel — removed rbfc_require_vessel_sigil validation call (was gating on the dropped arg); removed path-prefix strip (accept-directory-path normalization no longer needed); final success prose changed from 'Hallmark summoned: ${vessel}/${hallmark}' to 'Hallmark summoned: ${hallmark}' since the bulleted per-artifact retrieval lines below show the full hallmark subtree paths (vessel would require a full vouch-ark extract just for display, not worth it). (6) rbtdrc_crucible.rs:2424 Rust call-site updated: rbw-fA invocation loop tuple destructure changed from (label, vdir, consec) to (label, consec), args slice from [vdir, consec, --force] to [consec, --force]; conjure_dir/bind_dir/graft_dir locals preserved because ORDAIN (rbw-fO) and KLUDGE (rbw-fk) still take vessel. Multi-officium overlap: commits 1-4 above landed in A_AAO's b5603dc6 because that officium's jjx_record file list named rbfc_FoundryCore.sh and rbfl_FoundryLedger.sh — my WIP edits to those files were already in the index at that moment and rode along under A_AAO's attribution. Same phenomenon AAK Notch 2a's paddock documented for rbhodf. Not destructive — all work is live in the repo, just the commit-log attribution is cross-cutting. Items 5-6 landed explicitly under AAL as commit addae3a1. Scope refinement from docket's expected 9 affected tabtargets: real vessel-param drops hit only rbw-fA/fs/fpf/fpc. The rbw-fV (batch_vouch) and rbw-ft (tally) functions were stubbed by AAK Notch 2b and A_AAO reimplemented them in the same b5603dc6 overlap window. The rbw-iJ/ir/iw image-family tabtargets already take single-arg locators from AAK Notch 2a. Two orthogonal post-AAK bugs discovered during the grep audit but flagged out-of-scope and slated as fresh paces: ₢A_AAQ rekon-hallmark-catalog (rbfl_rekon still hits <prefix><moniker>/tags/list — pre-AAK vessel-indexed package grammar that no longer exists; needs endpoint rewrite, likely consuming A_AAO's new zrbfc_list_hallmarks_capture helper) and ₢A_AAR jettison-locator-grammar (rbfl_jettison parses first-colon split into moniker:tag and constructs <prefix><moniker>/manifests/<tag> URL — needs last-colon split like rbfr_wrest adopted in AAK Notch 2a, and URL fix to interpret the path as containing slashes). Both slated first in heat per user request. Verification at close: git show HEAD:rbfc_FoundryCore.sh confirms all AAL structural changes present at expected line numbers (helper at 679, plumb_core hallmark-only at 716, plumb_full single-arg at 1332); rbfl_FoundryLedger.sh:397 shows the simplified abjure signature; bash -n clean on all edited files; theurge crate builds clean with 53 Rust unit tests passing; fast suite 90/90 green (47 enrollment-validation + 21 regime-validation + 7 regime-smoke + 15 handbook-render) matching baseline with no regression. tt/rbw-MG.MarshalGenerate.sh regen produced zero diff on rbk-claude-tabtarget-context.md because channel declarations for affected colophons were already empty at the zipper level — the generator shows colophon/frontispiece/channel but not underlying function signatures, so the change was invisible at that layer. Service and crucible suites not run from this officium (no live credentials / container runtime not guaranteed); deferred to downstream verification. Paddock and heat state advanced: AAL's docket-spec verification items all green except the service/crucible suites which require live infrastructure.
.claude/jjm/retired/jjh_b260415-r260514-rbk-10-mvp-regen-retest-depot.md:5923:AAL vessel-param drop — residual files after multi-officium overlap. The helper mint (rbfc_vessel_for_hallmark_capture), the zrbfc_plumb_core hallmark-only refactor, and the rbfl_abjure vessel-arg drop all rode along into A_AAO's commit b5603dc6 because that officium's jjx_record file list named rbfc_FoundryCore.sh and rbfl_FoundryLedger.sh — my WIP edits to those files were staged at that moment and committed under A_AAO's attribution. Same phenomenon the AAK Notch 2a paddock already documented for rbhodf. Tree confirms: git show HEAD:rbfc_FoundryCore.sh contains rbfc_vessel_for_hallmark_capture at line 679, zrbfc_plumb_core's hallmark-only signature, and rbfc_plumb_full/rbfc_plumb_compact single-arg collapse. Two files remain for explicit AAL attribution: (1) rbfr_FoundryRetriever.sh — rbfr_summon signature drops $1 vessel (was vessel, hallmark → now just hallmark); removed the rbfc_require_vessel_sigil validation call that was gating on the dropped arg; removed the path-prefix strip that normalized vessel path→sigil; final success prose changed from 'Hallmark summoned: ${z_vessel}/${z_hallmark}' to just 'Hallmark summoned: ${z_hallmark}' since the bulleted per-artifact retrieval lines below already show the full hallmark subtree paths. (2) rbtd/src/rbtdrc_crucible.rs:2424 — the crucible four-mode fixture was invoking rbw-fA with [vdir, consec, --force] matching the pre-AAL two-arg abjure signature; updated to [consec, --force]. Loop-tuple destructure changed from (label, vdir, consec) to (label, consec) with conjure_dir/bind_dir/graft_dir locals preserved because they're still used by ORDAIN (line 2236, 2257, 2293) and KLUDGE (line 2308) which take vessel and are unaffected by AAL. Scope refinement from docket's expected 9 affected tabtargets: real vessel-param drops hit only rbw-fA/fs/fpf/fpc. The rbw-fV (batch_vouch) and rbw-ft (tally) functions were stubbed by AAK Notch 2b and reimplemented in A_AAO; the rbw-iJ/ir/iw image-family tabtargets already take single-arg locators from AAK Notch 2b. Two orthogonal post-AAK bugs discovered but flagged out-of-scope (to be slated as follow-up paces): rbfl_rekon points at <prefix><moniker>/tags/list which is the pre-AAK vessel-package grammar (no longer valid post-migration); rbfl_jettison interprets its locator against <prefix><moniker>/manifests/<tag> similarly. Neither is a vessel-param-drop issue; both need endpoint-rewrite work along the lines of zrbfc_list_hallmarks_capture. Verification: bash -n clean on rbfr_FoundryRetriever.sh; theurge crate builds clean (cargo dev profile); 53 theurge unit tests pass; fast suite 90/90 green (47 enrollment-validation + 21 regime-validation + 7 regime-smoke + 15 handbook-render); tt/rbw-MG.MarshalGenerate.sh regen produced zero diff on rbk-claude-tabtarget-context.md because channel declarations for affected colophons were already '' (empty — pass all args through to function) at the zipper level — the generator shows colophon/frontispiece/channel and the underlying function signature change is invisible at that layer. Service and crucible suites not run from this officium (no live credentials / container runtime not guaranteed); deferred to downstream verification. AAL's docket-specified helper rbfc_vessel_for_hallmark exists as rbfc_vessel_for_hallmark_capture (BCG-mandated _capture suffix for $()-invoked public functions) and is consumed by zrbfc_plumb_core; abjure and summon don't route through it because abjure has no vessel-consumption post-simplification and summon's prose simplification dropped the vessel identifier rather than resolving it (the vouch ark extraction cost wasn't worth it for a display-only use).
.claude/jjm/retired/jjh_b260426-r260514-rbk-11-mvp-parallel-cleanups.md:272:- `Tools/rbk/rbk-claude-tabtarget-context.md` (Image section gets full rewrite + Muster section added)
.claude/jjm/retired/jjh_b260426-r260514-rbk-11-mvp-parallel-cleanups.md:379:- `Tools/rbk/rbk-claude-tabtarget-context.md` (Image section gets full rewrite + Muster section added)
.claude/jjm/retired/jjh_b260426-r260514-rbk-11-mvp-parallel-cleanups.md:471:- `Tools/rbk/rbk-claude-tabtarget-context.md` (Image section gets full rewrite + Muster section added)
.claude/jjm/retired/jjh_b260426-r260514-rbk-11-mvp-parallel-cleanups.md:4383:x··x···· rbk-claude-tabtarget-context.md, rbz_zipper.sh
.claude/jjm/retired/jjh_b260426-r260514-rbk-11-mvp-parallel-cleanups.md:4464:Mechanical hygiene sweep across 7 OAuth-touching files (rbgo/rbgp/rbgc/rbrp/rbgu/rbgg/buh) applying 12 findings from the upstream trust review: F1+F6a unused-constant deletes (8 total), F6b cross-module SC2153 directives in rbgp+rbgg, F3 OAuth URL constants minted in rbgc with four call-sites swapped in rbgp, F2 TOCTOU close on RBRO write (subshell + umask 077), F5 buh_prompt_secret mint and auth-code callsite swap, F8-F12 rationale comments at five sites, F13 producer-reality correction at the three RBRA writers (rbgu/rbgg/rbgp) and the consumer (rbgo) — multi-line PEM with real newlines, printf %b is defensive — plus BCG brace freebie at rbgu/rbgg writer sites. Three pre-existing bugs surfaced by Tier 2 verification and fixed in-scope (operator authorized): bud_dispatch.sh NO_LOG+INTERACTIVE branch unbound BURD_LOG_HIST (tightened condition so NO_LOG+INTERACTIVE falls through to the no-log elif); rbz_zipper.sh rbw-gPI channel enrollment '' to 'param1' (with regenerated rbk-claude-tabtarget-context.md so rbq_qualify_fast diff stays clean); rbgp_payor_install $1 to BUZ_FOLIO (matched canonical rbfd_enshrine convention — param1 channel exports the folio via env, strips it from positional args before exec). Mid-flow handbook amendment: documented new 'Sign in to Recipe Bottle Payor' Google screen between Choose-account and permissions consent (renumbered screen list to 1-5, hoisted z_yelp_continue declaration above first use). All F-findings verified empirically. Tier 1: bash -n clean on all 7 originally-planned files, fast suite 98 passed / 0 failed / 0 skipped, shellcheck delta 136 to 130 (drop of 6, short of docket-predicted 9-10 but no new findings introduced), regime validators rbw-rov and rbw-rpv PASS. Tier 2: rbw-gPI ceremony completed cleanly — F5 no-echo confirmed empirically (auth-code paste showed no echoed characters), F2 perms 600 confirmed via stat (rbro.env created at 0600 from first byte), F3 URLs round-tripped live to Google's OAuth endpoints, F12 fresh auth via zrbgp_authenticate_capture exercised by follow-on rbw-dl which authenticated and listed 38 depots. Tier 3: rbw-dL levied new depot cancbhl-d-canest2bhm100002 (after initial mantle 403 surfaced missing-depot precondition; operator authorized); rbw-aM minted Governor SA governor-202605130754 with 32-line multi-line PEM RBRA confirming F13 producer reality; rbw-arI invested Retriever t3r, rbw-adI invested Director t3d, both rostered confirming SA presence; rbw-ft Retriever tally signed JWT from multi-line PEM and queried GAR successfully — F13 consumer reality confirmed end-to-end (printf %b correctly handled real-newline PEM, openssl signed without error). Out-of-scope follow-ups surfaced for future paces: (a) RBRA writers (rbgu/rbgg/rbgp) produce files at mode 644 (world-readable) — F2's TOCTOU fix was scoped to RBRO only, RBRA writers don't apply umask 077; the parallel hygiene improvement is worth a sibling pace; (b) AAY remains slated for BCG-wide SC2153 false-positive study to retire the per-file directives added in F6b; (c) docket-baked Tier 3 tabtarget vocabulary (rbw-aC charter / rbw-aK knight / rbw-aL list) is stale relative to current tree (rbw-arI invest retriever / rbw-adI invest director / rbw-arr roster retrievers / rbw-adr roster directors / rbw-aL never existed). Live GCP resources created (operator-authorized): depot cancbhl-d-canest2bhm100002 with Governor, Retriever t3r, and Director t3d SAs. Four notches in this session: bcc29f58 (F1-F13 sweep across 7 files), 44d0f08f (dispatcher + zipper + tabtarget context), 79d8b115 (BUZ_FOLIO consumption in rbgp_payor_install), 50bc4756 (handbook prose for new Google screen).
.claude/jjm/retired/jjh_b260426-r260514-rbk-11-mvp-parallel-cleanups.md:4476:Fix two pre-existing bugs surfaced by Tier 2 verification of ₢BAAAA's F2/F5/F3 edits (operator authorized in-scope). Bug 1 (bud_dispatch.sh:358): the dispatcher's interactive branch unconditionally referenced BURD_LOG_HIST at the tee pipeline, but BURD_LOG_HIST is only initialized when BURD_NO_LOG is empty (line 179). The rbw-gPI tabtarget intentionally sets BOTH BURD_NO_LOG=1 (credentials must not be logged) and BURD_INTERACTIVE=1 (preserve line-buffered prompt display so the auth-code paste prompt actually appears), and was the only tabtarget hitting this combination. Result was 'BURD_LOG_HIST: unbound variable' from set -u before rbgp_payor_install could even start. Fix tightens the interactive-branch condition from `test -n BURD_INTERACTIVE` to `test -n BURD_INTERACTIVE && test -z BURD_NO_LOG`, so the NO_LOG+INTERACTIVE combination falls through to the existing NO_LOG elif branch — which already preserves line buffering (no while-read curating loop) and skips log writes. The two flags are now properly composable as originally intended. Bug 2 (rbz_zipper.sh:79): the rbw-gPI colophon was enrolled with channel '' (no folio), but rbgp_payor_install takes a JSON path as $1 — the workbench therefore stripped the argument and emitted 'Colophon takes no folio; ignoring unexpected argument(s)' before rbgp_payor_install died on missing arg. Fix changes the enrollment channel from '' to 'param1', matching the pattern used by neighbor enrollments like rbw-dE (param1, takes vessel moniker) and rbw-dY (param1, takes stamp). Also regenerated rbk-claude-tabtarget-context.md via rbw-MG.MarshalGenerate to reflect the channel change (single-line diff in the Guide table: rbw-gPI Folio column now shows 'param1' instead of '—'); this keeps rbq_qualify_fast's live-vs-committed diff clean. Tier 2 retry pending operator action; will run fast-suite regression check after Tier 2 succeeds to backstop the dispatcher fix.
.claude/jjm/retired/jjh_b260426-r260514-rbk-11-mvp-parallel-cleanups.md:4496:Regenerate tabtarget context to flush the rbw-Oda description edit from ae039b87. That commit updated rbz_zipper.sh's RBZ_ONBOARD_DIR_AIRGAP enrollment from 'conjure forge' to 'conjure base' (matching the buc_doc_brief sweep) but did not run tt/rbw-MG.MarshalGenerate.sh, leaving the generated rbk-claude-tabtarget-context.md one line stale. Surfaced by ₣BB BBAAs onboarding-sequence empirical re-run: rbq_qualify_fast (the fail-fast precondition gate that runs before every ordain) caught the drift via its diff between live buz_emit_context output and the committed file. Pure prose regeneration; no behavior change.
.claude/jjm/retired/jjh_b260512-r260529-rbk-14-mvp-testing-simplification.md:836:x···· rbgv_cli.sh, rbk-claude-tabtarget-context.md, rbtd-ap.AccessProbe.director.sh, rbtd-ap.AccessProbe.governor.sh, rbtd-ap.AccessProbe.payor.sh, rbtd-ap.AccessProbe.retriever.sh, rbtdrk_canonical.rs, rbtdro_onboarding.rs, rbtdrp_pristine.rs, rbtdti_invocation.rs, rbte_cli.sh, rbw-acd.CheckDirectorCredential.sh, rbw-acg.CheckGovernorCredential.sh, rbw-acp.CheckPayorCredential.sh, rbw-acr.CheckRetrieverCredential.sh, rbz_zipper.sh
.claude/jjm/retired/jjh_b260514-r260529-rbk-09-mvp-regime-sprues-misc.md:2431:x············ CLAUDE.consumer.md, launcher.rbtw_workbench.sh, rbhots_tadmor_security.sh, rbk-claude-tabtarget-context.md, rbk-claude-theurge-ifrit-context.md, rbq_Qualify.sh, rbtdth_helpers.rs, rbte_cli.sh, rbtw_workbench.sh, rbw-tb.Build.sh, rbw-tc.FixtureCase.sh, rbw-tf.FixtureRun.sh, rbw-tq.QualifyFast.sh, rbw-ts.TestSuite.complete.sh, rbw-ts.TestSuite.crucible.sh, rbw-ts.TestSuite.dogfight.sh, rbw-ts.TestSuite.fast.sh, rbw-ts.TestSuite.gauntlet.sh, rbw-ts.TestSuite.service.sh, rbw-ts.TestSuite.skirmish.sh, rbw-ts.TestSuite.tadmor.sh, rbw-tt.Test.sh, rbz_zipper.sh
.claude/jjm/retired/jjh_b260514-r260519-rbk-11-mvp-tactical.md:1752:··x·····xx···· rbk-claude-tabtarget-context.md, rbz_zipper.sh
.claude/jjm/retired/jjh_b260514-r260519-rbk-11-mvp-tactical.md:1817:Wire rbrd_cli.sh differential dispatch for tripwire commands, enroll rbw-rdc/rbw-rdi tabtargets, BCG-comply rbndb_base.sh, and soften rbgc_Constants.sh rbi_df comment block. rbrd_cli.sh's furnish now receives the command name as $1 and applies differential setup: rbrd_validate/rbrd_render stay lightweight (just RBRD enrollment); rbrd_check/rbrd_inscribe additionally source and kindle rbrr_regime, rbgc_Constants, rbgl_GarLayout, rbdc_DerivedConstants, and rbndb_base so the tripwire image FQN composes from validated regime state. rbz_zipper.sh gains two enrollments with channel=param1 (RBZ_INSCRIBE_DEPOT→rbw-rdi, RBZ_CHECK_DEPOT→rbw-rdc); both functions accept the bearer token from positional $1 or BUZ_FOLIO (the param1-channel dispatch path), so consuming CLIs can call rbrd_check $z_token in-process while operators can run `tt/rbw-rdc.sh $(gcloud auth print-access-token)` for manual drift verification. tt/rbw-rdc.CheckDepotRegime.sh and tt/rbw-rdi.InscribeDepotRegime.sh added as standard three-line launcher tabtargets. rbndb_base.sh rewritten for BCG compliance: line-2 shellcheck disable=SC2153 directive added, all assigned-once locals use local -r, temp file paths declared as kindle constants (ZRBNDB_INSCRIBE_PREFIX/ZRBNDB_CHECK_PREFIX) with per-op stderr files referenced in buc_die messages (`see ${z_*_stderr}` pattern from BCG line 542), 2>/dev/null suppressions all replaced with stderr→temp-file capture, docker create result captured via $(<temp_stdout_file) two-line pattern instead of inline $(cmd), diff exit-status explicitly handled (≤1 = expected, ≥2 = real error), docker rm cleanup failure downgraded to buc_warn with stderr file reference rather than `|| true` suppression. buc_doc_brief/buc_doc_param/buc_doc_shown added to rbrd_inscribe and rbrd_check so they participate in the CLI help system; module-level header documents the dual token-source pattern (positional $1 vs BUZ_FOLIO). rbgc_Constants.sh lines 175-189 updated: the comment block previously asserted RBRD shipped 'at a separate tag' outside rbi_df; revised to enumerate rbi_df's current artifacts (probe-tether:probe, probe-airgap:probe, rbrd:tripwire) and explain that rbrd:tripwire is host-inscribed by Payor at end of levy and consulted at every cloud submission, cross-referencing Tools/rbk/rbndb_base.sh. rbk-claude-tabtarget-context.md regenerated to surface the two new tabtargets under the Regime section. Verification: fast suite 107/107 green on both runs (enrollment-validation 47, regime-validation 27, regime-smoke 9, handbook-render 15, dockerfile-hygiene 9) — validate/render path unaffected by differential furnish, new dispatch routes don't break enrollment validation.
.claude/jjm/retired/jjh_b260402-r260409-rbk-mvp-3-onboarding-guides.md:3556:··x······ rbk-claude-tabtarget-context.md, rbq_Qualify.sh
---- apck-claude-context ----
CLAUDE.md:295:@Tools/apck/apck-claude-context.md
Tools/apck/APCPS-PrototypeSpecification.md:520:  apck-claude-context.md          # Build/run/deploy instructions
.claude/jjm/jjg_gallops.json:3475:              "jjgtn_text": "## Character\n\nMechanical with minting attention. Create every file and directory, wire BUK dispatch, get first Tauri window on screen.\n\n## Deliverables\n\n- `Tools/apck/` directory with full tree per APCPS project structure section\n- `apcw_workbench.sh` — routes `apcw-*` commands\n- `apcz_zipper.sh` — enrolls colophons with BUK dispatch\n- `apck-claude-context.md` — already exists from ₢A9AAA; review and update if needed\n- `apcd/Cargo.toml` — two `[[bin]]` targets: `apcap` (Tauri app), `apcal` (fixture loader)\n- `apcd/src/apcap_main.rs` — Tauri entry point, launches window with static \"Ann's PHI Clipbuddy\" title and initial instruction text\n- `apcd/src/lib.rs` — module declarations (empty stubs) and test module declarations per APCPS:\n  - Source: `apcre_engine`, `apcrp_parse`, `apcrm_match`, `apcrd_dictionaries`, `apcru_update`\n  - Tests: `#[cfg(test)]` for `apcte_engine`, `apctp_parse`, `apctm_match`, `apctd_dictionaries`\n- Empty stub files for all source and test modules\n- `apcd/ui/index.html`, `style.css`, `app.js` — minimal Tauri frontend showing initial state\n- Tabtargets: `tt/apcw-b.Build.sh`, `tt/apcw-r.Run.sh`, `tt/apcw-d.Deploy.sh`, `tt/apcw-fl.FixtureLoad.sh`, `tt/apcw-t.Test.sh`\n- `test_fixtures/` directory (empty, ready for next pace)\n- `apcd/dictionaries/` directory (empty, ready for later pace)\n- Tauri config (`tauri.conf.json` or Cargo-based Tauri 2.x config)\n\n## Verification\n\n- `tt/apcw-r.Run.sh` launches a Tauri window displaying \"Ann's PHI Clipbuddy\" with instruction text\n- `tt/apcw-b.Build.sh` produces a `.app` bundle\n- `tt/apcw-t.Test.sh` runs cargo test (passes trivially — no tests yet)",
.claude/jjm/jjg_gallops.json:3482:              "jjgtn_text": "## Character\n\nMechanical with minting attention. Create every file and directory, wire BUK dispatch, get first Tauri window on screen.\n\n## Deliverables\n\n- `Tools/apck/` directory with full tree per APCPS project structure section\n- `apcw_workbench.sh` — routes `apcw-*` commands\n- `apcz_zipper.sh` — enrolls colophons with BUK dispatch\n- `apck-claude-context.md` — already exists from ₢A9AAA; review and update if needed\n- `apcd/Cargo.toml` — two `[[bin]]` targets: `apcap` (Tauri app), `apcal` (fixture loader)\n- `apcd/src/apcap_main.rs` — Tauri entry point, launches window with static \"Ann's PHI Clipbuddy\" title and initial instruction text\n- `apcd/src/lib.rs` — module declarations (empty stubs) and test module declarations per APCPS:\n  - Source: `apcre_engine`, `apcrp_parse`, `apcrm_match`, `apcrd_dictionaries`, `apcru_update`\n  - Tests: `#[cfg(test)]` for `apcte_engine`, `apctp_parse`, `apctm_match`, `apctd_dictionaries`\n- Empty stub files for all source and test modules\n- `apcd/ui/index.html`, `style.css`, `app.js` — minimal Tauri frontend showing initial state\n- Tabtargets: `tt/apcw-b.Build.sh`, `tt/apcw-r.Run.sh`, `tt/apcw-d.Deploy.sh`, `tt/apcw-fl.FixtureLoad.sh`, `tt/apcw-t.Test.sh`\n- `test_fixtures/` directory (empty, ready for next pace)\n- `apcd/dictionaries/` directory (empty, ready for later pace)\n- Tauri config (`tauri.conf.json` or Cargo-based Tauri 2.x config)\n\n## Verification\n\n- `tt/apcw-r.Run.sh` launches a Tauri window displaying \"Ann's PHI Clipbuddy\" with instruction text\n- `tt/apcw-b.Build.sh` produces a `.app` bundle\n- `tt/apcw-t.Test.sh` runs cargo test (passes trivially — no tests yet)",
.claude/jjm/jjg_gallops.json:3489:              "jjgtn_text": "## Character\n\nMechanical with minting attention. Create every file and directory, wire BUK dispatch, get first Tauri window on screen.\n\n## Deliverables\n\n- `Tools/apck/` directory with full tree per paddock\n- `apcw_workbench.sh` — routes `apcw-*` commands\n- `apcz_zipper.sh` — enrolls colophons with BUK dispatch\n- `apck-claude-context.md` — build, run, deploy instructions; references RCG as Rust coding guide\n- `apcd/Cargo.toml` — two `[[bin]]` targets: `apcap` (Tauri app), `apcal` (fixture loader)\n- `apcd/src/apcap_main.rs` — Tauri entry point, launches window with static \"Hello from APCK\" text\n- `apcd/src/lib.rs` — module declarations (empty stubs for engine modules)\n- `apcd/ui/index.html`, `style.css`, `app.js` — minimal Tauri frontend\n- Tabtargets: `tt/apcw-b.Build.sh`, `tt/apcw-r.Run.sh`, `tt/apcw-d.Deploy.sh`, `tt/apcw-fl.FixtureLoad.sh`\n- `test_fixtures/` directory (empty, ready for next pace)\n- `apcd/dictionaries/` directory (empty, ready for later pace)\n- Tauri config (`tauri.conf.json` or Cargo-based Tauri 2.x config)\n\n## Verification\n\n`tt/apcw-r.Run.sh` launches a Tauri window displaying static text. `tt/apcw-b.Build.sh` produces a `.app` bundle.",
.claude/jjm/jjg_gallops.json:3646:              "jjgtn_text": "## Character\n\nInfrastructure and platform integration. Two independent pieces: self-update mechanism in the app, and the deploy tabtarget that ships to Ann's machine.\n\n## Deliverables\n\n**Self-update (`apcd/src/apcru_update.rs`):**\n- Watch `/Users/Shared/apcua/` using `notify` crate (FSEvents on macOS)\n- On new `.app` bundle detected: copy over current app bundle, spawn `open -n` on new path, exit current process\n- No confirmation dialog — fully automatic\n- Graceful handling: if watch directory doesn't exist, create it silently and keep watching\n- Run watcher on a background thread, independent of main Tauri event loop\n\n**Deploy tabtarget (`tt/apcw-d.Deploy.sh`):**\n- `cargo tauri build` (release)\n- `scp` the `.app` bundle to `anns-macbook-air:/Users/Shared/apcua/`\n- Report success with bundle size and path\n\n**Integration:**\n- Wire `apcru_update` into `apcap_main.rs` — start watcher on app launch\n- Update `apck-claude-context.md` with deploy workflow documentation\n\n## Verification\n\n1. Build and run app locally. Confirm watcher starts (log message).\n2. Copy a dummy `.app` to `/Users/Shared/apcua/` — app detects it (may not self-replace locally since paths differ, but detection fires).\n3. `tt/apcw-d.Deploy.sh` completes: bundle appears on Ann's machine at `/Users/Shared/apcua/`.\n4. End-to-end: Ann launches app, we deploy an update, app auto-restarts with new version.",
.claude/jjm/jjg_gallops.json:3653:              "jjgtn_text": "## Character\n\nInfrastructure and platform integration. Two independent pieces: self-update mechanism in the app, and the deploy tabtarget that ships to Ann's machine.\n\n## Deliverables\n\n**Self-update (`apcd/src/apcru_update.rs`):**\n- Watch `/Users/Shared/apcua/` using `notify` crate (FSEvents on macOS)\n- On new `.app` bundle detected: copy over current app bundle, spawn `open -n` on new path, exit current process\n- No confirmation dialog — fully automatic\n- Graceful handling: if watch directory doesn't exist, create it silently and keep watching\n- Run watcher on a background thread, independent of main Tauri event loop\n\n**Deploy tabtarget (`tt/apcw-d.Deploy.sh`):**\n- `cargo tauri build` (release)\n- `scp` the `.app` bundle to `anns-macbook-air:/Users/Shared/apcua/`\n- Report success with bundle size and path\n\n**Integration:**\n- Wire `apcru_update` into `apcap_main.rs` — start watcher on app launch\n- Update `apck-claude-context.md` with deploy workflow documentation\n\n## Verification\n\n1. Build and run app locally. Confirm watcher starts (log message).\n2. Copy a dummy `.app` to `/Users/Shared/apcua/` — app detects it (may not self-replace locally since paths differ, but detection fires).\n3. `tt/apcw-d.Deploy.sh` completes: bundle appears on Ann's machine at `/Users/Shared/apcua/`.\n4. End-to-end: Ann launches app, we deploy an update, app auto-restarts with new version.",
.claude/jjm/jjg_gallops.json:3682:              "jjgtn_text": "## Character\n\nMechanical Rust implementation + execution debugging. All research and algorithm design is complete — this pace is pure coding from a known spec, then running and fixing.\n\n## Context\n\nPrior pace (A9AAI) researched and verified all data sources, designed transform pipelines, and decided on Rust over shell (CSV quoting fragility in awk was the decisive factor). This docket captures everything needed to implement without rediscovery.\n\n## Deliverables\n\n### New binary: `apcad` (App Dictionary)\n\nNew file: `Tools/apck/apcd/src/apcad_main.rs`\n\nPrefix `apcad` under existing `apca` non-terminal (alongside `apcal` Loader, `apcap` Prototype).\n\nAdd `[[bin]] name = \"apcad\"` to `Tools/apck/apcd/Cargo.toml`.\n\nNew crate dependencies: `csv`, `zip` (for reading zip archives from bytes), `ureq` (lightweight sync HTTP — no async needed).\n\n### Tabtarget enrollment\n\n- New tabtarget: `tt/apcw-dr.DictionaryRefresh.sh` — runs `cargo run --bin apcad` in apcd/\n- New command in `apcc_cli.sh`: `apcc_dictionary_refresh`\n- Enroll in `apcz_zipper.sh`: colophon `apcw-dr`, no folio\n\n### Data sources and transforms\n\nAll sources are public domain or CC-BY 3.0. All URLs verified live as of 2026-04-10.\n\n**1. Surnames — Census 2010** (top 1000)\n- URL: `https://www2.census.gov/topics/genealogy/2010surnames/names.zip`\n- Inside zip: `Names_2010Census.csv`\n- CSV with header: `name,rank,count,prop100k,cum_prop100k,pctwhite,pctblack,pctapi,pctaian,pct2prace,pcthispanic`\n- Names are ALL CAPS — lowercase on output\n- File is pre-ranked by frequency — take first 1000 data rows by column `name`\n- License: CC0 / US government public domain\n\n**2. First names — SSA Baby Names** (top 1000)\n- URL: `https://www.ssa.gov/oact/babynames/names.zip`\n- Inside zip: `yob1880.txt` through `yob2024.txt` (no headers), plus `NationalReadMe.pdf`\n- Per-file format: `Name,Sex,Count` (e.g., `Mary,F,7065`)\n- Aggregation: HashMap by lowercase name (merge M+F), sum counts across all year files, sort desc by total count, take top 1000\n- **SSA uses Akamai bot protection — curl may get 403.** Handle gracefully: attempt download, on failure print instructions for manual download to a staging path, exit with clear message. Re-run should detect the pre-staged file.\n- License: CC0 / US government public domain\n\n**3. Cities — Census Population Estimates** (top 100 by pop + 50 state capitals)\n- URL: `https://www2.census.gov/programs-surveys/popest/datasets/2020-2023/cities/totals/sub-est2023.csv`\n- Use `csv` crate with headers — access columns by name, not position\n- Filter: `SUMLEV` == `162` (incorporated places)\n- Sort by `POPESTIMATE2023` descending, take top 100\n- Strip municipal suffixes from `NAME`: \" city\", \" town\", \" village\", \" borough\", \" municipality\", \" CDP\" (case-insensitive, trailing)\n- Remove surrounding quotes if present\n- Merge with hardcoded 50 state capitals (below)\n- Deduplicate, lowercase, alpha-sort\n- Expected output: ~130 entries\n- License: US government public domain\n\n**4. English whitelist — Norvig/Google** (top 5000)\n- URL: `https://norvig.com/ngrams/count_1w.txt`\n- TSV format: `word\\tcount`, 333K lines, pre-sorted by frequency desc\n- Filter: skip entries with digits, skip entries shorter than 2 chars\n- Take first 5000 that pass filter, lowercase, alpha-sort\n- License: CC-BY 3.0 (Google n-gram corpus)\n\n**5. Medical whitelist — UNTOUCHED**\n- Do not modify `medical_whitelist.txt`\n\n### 50 US State Capitals (hardcode in source)\n\n```\nalbany, annapolis, atlanta, augusta, austin,\nbaton rouge, bismarck, boise, boston, carson city,\ncharleston, cheyenne, columbia, columbus, concord,\ndenver, des moines, dover, frankfort, harrisburg,\nhartford, helena, honolulu, indianapolis, jackson,\njefferson city, juneau, lansing, lincoln, little rock,\nmadison, montgomery, montpelier, nashville, oklahoma city,\nolympia, phoenix, pierre, providence, raleigh,\nrichmond, sacramento, salem, salt lake city, santa fe,\nspringfield, st. paul, tallahassee, topeka, trenton\n```\n\nNote: current `cities.txt` uses \"saint paul\" (line 107) but \"st. louis\" / \"st. petersburg\" (lines 121-122). Normalize to match whatever the Census CSV uses after suffix stripping.\n\n### Output format\n\nEach file: one entry per line, lowercase, alphabetically sorted (`sort -u` equivalent), no trailing blank lines. File ends with a single newline after the last entry. This matches the current format.\n\nOutput directory: `Tools/apck/apcd/dictionaries/`\n\n### After implementation\n\n- Build: `tt/apcw-b.Build.sh` (or just `cargo build` in apcd/)\n- Run: `tt/apcw-dr.DictionaryRefresh.sh` (or `cargo run --bin apcad` in apcd/)\n- Verify output format matches current files (one-per-line, lowercase, sorted)\n- Spot-check: smith/johnson/williams in surnames, james/mary/john in firstnames, all 50 capitals in cities\n- Run tests: `tt/apcw-t.Test.sh` — fix any failures from changed dictionary content\n- Confirm idempotent: second run produces identical output\n- Update `apck-claude-context.md`: add APCAD to file acronym mappings and prefix tree\n\n## Verification\n\n- `cargo build --bin apcad` succeeds\n- `tt/apcw-dr.DictionaryRefresh.sh` runs end-to-end (or with manual SSA download workaround)\n- Four dictionary files regenerated from real sources\n- `tt/apcw-t.Test.sh` passes clean\n- Script is idempotent (second run = identical output)",
.claude/jjm/jjg_gallops.json:3689:              "jjgtn_text": "## Character\n\nMechanical Rust implementation + execution debugging. All research and algorithm design is complete — this pace is pure coding from a known spec, then running and fixing.\n\n## Context\n\nPrior pace (A9AAI) researched and verified all data sources, designed transform pipelines, and decided on Rust over shell (CSV quoting fragility in awk was the decisive factor). This docket captures everything needed to implement without rediscovery.\n\n## Deliverables\n\n### New binary: `apcad` (App Dictionary)\n\nNew file: `Tools/apck/apcd/src/apcad_main.rs`\n\nPrefix `apcad` under existing `apca` non-terminal (alongside `apcal` Loader, `apcap` Prototype).\n\nAdd `[[bin]] name = \"apcad\"` to `Tools/apck/apcd/Cargo.toml`.\n\nNew crate dependencies: `csv`, `zip` (for reading zip archives from bytes), `ureq` (lightweight sync HTTP — no async needed).\n\n### Tabtarget enrollment\n\n- New tabtarget: `tt/apcw-dr.DictionaryRefresh.sh` — runs `cargo run --bin apcad` in apcd/\n- New command in `apcc_cli.sh`: `apcc_dictionary_refresh`\n- Enroll in `apcz_zipper.sh`: colophon `apcw-dr`, no folio\n\n### Data sources and transforms\n\nAll sources are public domain or CC-BY 3.0. All URLs verified live as of 2026-04-10.\n\n**1. Surnames — Census 2010** (top 1000)\n- URL: `https://www2.census.gov/topics/genealogy/2010surnames/names.zip`\n- Inside zip: `Names_2010Census.csv`\n- CSV with header: `name,rank,count,prop100k,cum_prop100k,pctwhite,pctblack,pctapi,pctaian,pct2prace,pcthispanic`\n- Names are ALL CAPS — lowercase on output\n- File is pre-ranked by frequency — take first 1000 data rows by column `name`\n- License: CC0 / US government public domain\n\n**2. First names — SSA Baby Names** (top 1000)\n- URL: `https://www.ssa.gov/oact/babynames/names.zip`\n- Inside zip: `yob1880.txt` through `yob2024.txt` (no headers), plus `NationalReadMe.pdf`\n- Per-file format: `Name,Sex,Count` (e.g., `Mary,F,7065`)\n- Aggregation: HashMap by lowercase name (merge M+F), sum counts across all year files, sort desc by total count, take top 1000\n- **SSA uses Akamai bot protection — curl may get 403.** Handle gracefully: attempt download, on failure print instructions for manual download to a staging path, exit with clear message. Re-run should detect the pre-staged file.\n- License: CC0 / US government public domain\n\n**3. Cities — Census Population Estimates** (top 100 by pop + 50 state capitals)\n- URL: `https://www2.census.gov/programs-surveys/popest/datasets/2020-2023/cities/totals/sub-est2023.csv`\n- Use `csv` crate with headers — access columns by name, not position\n- Filter: `SUMLEV` == `162` (incorporated places)\n- Sort by `POPESTIMATE2023` descending, take top 100\n- Strip municipal suffixes from `NAME`: \" city\", \" town\", \" village\", \" borough\", \" municipality\", \" CDP\" (case-insensitive, trailing)\n- Remove surrounding quotes if present\n- Merge with hardcoded 50 state capitals (below)\n- Deduplicate, lowercase, alpha-sort\n- Expected output: ~130 entries\n- License: US government public domain\n\n**4. English whitelist — Norvig/Google** (top 5000)\n- URL: `https://norvig.com/ngrams/count_1w.txt`\n- TSV format: `word\\tcount`, 333K lines, pre-sorted by frequency desc\n- Filter: skip entries with digits, skip entries shorter than 2 chars\n- Take first 5000 that pass filter, lowercase, alpha-sort\n- License: CC-BY 3.0 (Google n-gram corpus)\n\n**5. Medical whitelist — UNTOUCHED**\n- Do not modify `medical_whitelist.txt`\n\n### 50 US State Capitals (hardcode in source)\n\n```\nalbany, annapolis, atlanta, augusta, austin,\nbaton rouge, bismarck, boise, boston, carson city,\ncharleston, cheyenne, columbia, columbus, concord,\ndenver, des moines, dover, frankfort, harrisburg,\nhartford, helena, honolulu, indianapolis, jackson,\njefferson city, juneau, lansing, lincoln, little rock,\nmadison, montgomery, montpelier, nashville, oklahoma city,\nolympia, phoenix, pierre, providence, raleigh,\nrichmond, sacramento, salem, salt lake city, santa fe,\nspringfield, st. paul, tallahassee, topeka, trenton\n```\n\nNote: current `cities.txt` uses \"saint paul\" (line 107) but \"st. louis\" / \"st. petersburg\" (lines 121-122). Normalize to match whatever the Census CSV uses after suffix stripping.\n\n### Output format\n\nEach file: one entry per line, lowercase, alphabetically sorted (`sort -u` equivalent), no trailing blank lines. File ends with a single newline after the last entry. This matches the current format.\n\nOutput directory: `Tools/apck/apcd/dictionaries/`\n\n### After implementation\n\n- Build: `tt/apcw-b.Build.sh` (or just `cargo build` in apcd/)\n- Run: `tt/apcw-dr.DictionaryRefresh.sh` (or `cargo run --bin apcad` in apcd/)\n- Verify output format matches current files (one-per-line, lowercase, sorted)\n- Spot-check: smith/johnson/williams in surnames, james/mary/john in firstnames, all 50 capitals in cities\n- Run tests: `tt/apcw-t.Test.sh` — fix any failures from changed dictionary content\n- Confirm idempotent: second run produces identical output\n- Update `apck-claude-context.md`: add APCAD to file acronym mappings and prefix tree\n\n## Verification\n\n- `cargo build --bin apcad` succeeds\n- `tt/apcw-dr.DictionaryRefresh.sh` runs end-to-end (or with manual SSA download workaround)\n- Four dictionary files regenerated from real sources\n- `tt/apcw-t.Test.sh` passes clean\n- Script is idempotent (second run = identical output)",
.claude/jjm/jjg_gallops.json:3772:              "jjgtn_text": "## Character\n\nFocused build-out of a new binary and its tabtarget integration. Mechanical Rust + BUK wiring — the detection engine and parser already exist as library functions. No design ambiguity; the format decisions are made.\n\n## Deliverables\n\nNew binary `apcab` (App Batch): reads `*.html` files from an input directory, runs the parse and detection pipeline (apcrp + apcre), writes one `{stem}.assay.txt` per input file to `BURD_OUTPUT_DIR`.\n\nAssay output format — guillemet-delimited elision markers with PHI flowing through:\n```\nPatient: ‹RED:PATIENT Margaret Thornton›\nDOB: ‹RED:DOB 03/15/1952›\n...presented to ‹YELLOW:FACILITY Maine Medical Center› with...\n```\nUnelided text passes through verbatim. Unamended classification — no toggle logic, pure engine output.\n\n## Implementation\n\n- `Tools/apck/apcd/src/apcab_main.rs` — new binary entry point\n  - Accept input directory as CLI argument\n  - Glob `*.html`, parse each, run detection engine, render assay text\n  - Write `{stem}.assay.txt` to `BURD_OUTPUT_DIR`\n  - No Tauri dependency — library crates only (apcrp, apcre, apcrm, apcrd)\n- Register `apcab` in `Cargo.toml` as a `[[bin]]` entry\n- Tabtarget `tt/apcw-ba.BatchAssay.sh` — folio via param1 (input directory path)\n- Enroll colophon `apcw-ba` in zipper, wire through workbench\n- Update `apck-claude-context.md` prefix tree and file mappings\n\n## Default invocation\n\n```\ntt/apcw-ba.BatchAssay.sh Tools/apck/test_fixtures/\n```\n\nEmits `epic_progress_note.assay.txt` and `epic_geriatric_consult.assay.txt` to `BURD_OUTPUT_DIR`.\n\n## Exit Criteria\n\nBoth fixture HTML files produce assay output. Elision markers present for known PHI in each fixture. Output is readable as a document with classifications visible inline.",
.claude/jjm/jjg_gallops.json:3779:              "jjgtn_text": "## Character\n\nFocused build-out of a new binary and its tabtarget integration. Mechanical Rust + BUK wiring — the detection engine and parser already exist as library functions. No design ambiguity; the format decisions are made.\n\n## Deliverables\n\nNew binary `apcab` (App Batch): reads `*.html` files from an input directory, runs the parse and detection pipeline (apcrp + apcre), writes one `{stem}.assay.txt` per input file to `BURD_OUTPUT_DIR`.\n\nAssay output format — guillemet-delimited elision markers with PHI flowing through:\n```\nPatient: ‹RED:PATIENT Margaret Thornton›\nDOB: ‹RED:DOB 03/15/1952›\n...presented to ‹YELLOW:FACILITY Maine Medical Center› with...\n```\nUnelided text passes through verbatim. Unamended classification — no toggle logic, pure engine output.\n\n## Implementation\n\n- `Tools/apck/apcd/src/apcab_main.rs` — new binary entry point\n  - Accept input directory as CLI argument\n  - Glob `*.html`, parse each, run detection engine, render assay text\n  - Write `{stem}.assay.txt` to `BURD_OUTPUT_DIR`\n  - No Tauri dependency — library crates only (apcrp, apcre, apcrm, apcrd)\n- Register `apcab` in `Cargo.toml` as a `[[bin]]` entry\n- Tabtarget `tt/apcw-ba.BatchAssay.sh` — folio via param1 (input directory path)\n- Enroll colophon `apcw-ba` in zipper, wire through workbench\n- Update `apck-claude-context.md` prefix tree and file mappings\n\n## Default invocation\n\n```\ntt/apcw-ba.BatchAssay.sh Tools/apck/test_fixtures/\n```\n\nEmits `epic_progress_note.assay.txt` and `epic_geriatric_consult.assay.txt` to `BURD_OUTPUT_DIR`.\n\n## Exit Criteria\n\nBoth fixture HTML files produce assay output. Elision markers present for known PHI in each fixture. Output is readable as a document with classifications visible inline.",
.claude/jjm/jjg_gallops.json:3790:              "jjgtn_text": "## Character\n\nFocused refactoring with a policy enforcement dimension. Mechanically moving rendering logic from JS to Rust string-building — the HTML structure and CSS stay, only the generation language changes. The policy part (documenting the no-JS rule) is small but must not be forgotten.\n\n## Problem\n\nThe JS frontend creates a language boundary (Rust ↔ JS) that produces gratuitous bugs like the byte-vs-char offset mismatch fixed in ₢A9AAK. JavaScript adds a second type system, a second set of string semantics, and a serialization boundary that exists only because the prototype chose the path of least resistance. For a HIPAA-adjacent tool that processes PHI, minimizing boundary surface is a correctness concern, not just aesthetics.\n\n## Approach — Option A (Tauri + Rust-rendered HTML)\n\nKeep Tauri's webview as a dumb renderer. Generate complete HTML in Rust for every state change. Remove all JS files.\n\n1. **Move focus handling to Rust**: Use Tauri's `WindowEvent::Focused` on the Rust side instead of JS `window.addEventListener(\"focus\")`. On focus, run the existing `consume_clipboard` logic, then push rendered HTML to the webview.\n2. **Build HTML renderer in Rust**: Port the 5 JS rendering functions (`zapcapHandleResult`, `zapcapShowTriage`, `zapcapShowNonClinical`, `zapcapRenderPreview`, `zapcapRenderFindings`) into Rust functions that produce complete HTML strings. The byte offsets from the engine are consumed directly — no char-offset conversion needed. Delete `zapcap_byte_to_char_offsets`.\n3. **Handle toggle state in Rust**: Toggle clicks invoke a Tauri command (e.g., `toggle_finding(index)`) that flips state in Rust and pushes re-rendered HTML. Minimal IPC: just an index integer.\n4. **Handle copy in Rust**: `copy_anonymized` command already exists; the button just needs to call it (one tiny inline `onclick` handler calling `window.__TAURI__.core.invoke` is acceptable as a Tauri webview primitive, or use Tauri's event system).\n5. **Delete JS**: Remove `app.js` entirely. `index.html` becomes a minimal shell that receives rendered content. `style.css` stays.\n\n## Policy Documentation Updates\n\nUpdate these locations to prohibit JavaScript in the APCK codebase:\n\n- **APCPS (Prototype Specification)** — Add a premise: \"No JavaScript. All rendering logic is Rust. The webview is a passive display surface.\" This is the authoritative technical constraint.\n- **apck-claude-context.md** — Add to the conventions section so future Claude sessions see it immediately.\n- **APCS (Product Specification)** — Note in the tech stack section that the frontend is Rust-rendered HTML, not a JS application.\n- **Heat paddock** — Note the rationale (byte/char boundary bug class elimination) so it's visible during this heat's remaining work.\n\n## Exit Criteria\n\n- `app.js` deleted from the repository\n- All three UI states (instruction, non-clinical, triage) render correctly from Rust\n- Toggle buttons work via Tauri command → Rust state → re-render cycle\n- Copy Anonymized works as before\n- `zapcap_byte_to_char_offsets` deleted (no longer needed)\n- All four documents updated with no-JS policy\n- Existing tests pass (`tt/apcw-t.Test.sh`)",
.claude/jjm/jjg_gallops.json:3797:              "jjgtn_text": "## Character\n\nFocused refactoring with a policy enforcement dimension. Mechanically moving rendering logic from JS to Rust string-building — the HTML structure and CSS stay, only the generation language changes. The policy part (documenting the no-JS rule) is small but must not be forgotten.\n\n## Problem\n\nThe JS frontend creates a language boundary (Rust ↔ JS) that produces gratuitous bugs like the byte-vs-char offset mismatch fixed in ₢A9AAK. JavaScript adds a second type system, a second set of string semantics, and a serialization boundary that exists only because the prototype chose the path of least resistance. For a HIPAA-adjacent tool that processes PHI, minimizing boundary surface is a correctness concern, not just aesthetics.\n\n## Approach — Option A (Tauri + Rust-rendered HTML)\n\nKeep Tauri's webview as a dumb renderer. Generate complete HTML in Rust for every state change. Remove all JS files.\n\n1. **Move focus handling to Rust**: Use Tauri's `WindowEvent::Focused` on the Rust side instead of JS `window.addEventListener(\"focus\")`. On focus, run the existing `consume_clipboard` logic, then push rendered HTML to the webview.\n2. **Build HTML renderer in Rust**: Port the 5 JS rendering functions (`zapcapHandleResult`, `zapcapShowTriage`, `zapcapShowNonClinical`, `zapcapRenderPreview`, `zapcapRenderFindings`) into Rust functions that produce complete HTML strings. The byte offsets from the engine are consumed directly — no char-offset conversion needed. Delete `zapcap_byte_to_char_offsets`.\n3. **Handle toggle state in Rust**: Toggle clicks invoke a Tauri command (e.g., `toggle_finding(index)`) that flips state in Rust and pushes re-rendered HTML. Minimal IPC: just an index integer.\n4. **Handle copy in Rust**: `copy_anonymized` command already exists; the button just needs to call it (one tiny inline `onclick` handler calling `window.__TAURI__.core.invoke` is acceptable as a Tauri webview primitive, or use Tauri's event system).\n5. **Delete JS**: Remove `app.js` entirely. `index.html` becomes a minimal shell that receives rendered content. `style.css` stays.\n\n## Policy Documentation Updates\n\nUpdate these locations to prohibit JavaScript in the APCK codebase:\n\n- **APCPS (Prototype Specification)** — Add a premise: \"No JavaScript. All rendering logic is Rust. The webview is a passive display surface.\" This is the authoritative technical constraint.\n- **apck-claude-context.md** — Add to the conventions section so future Claude sessions see it immediately.\n- **APCS (Product Specification)** — Note in the tech stack section that the frontend is Rust-rendered HTML, not a JS application.\n- **Heat paddock** — Note the rationale (byte/char boundary bug class elimination) so it's visible during this heat's remaining work.\n\n## Exit Criteria\n\n- `app.js` deleted from the repository\n- All three UI states (instruction, non-clinical, triage) render correctly from Rust\n- Toggle buttons work via Tauri command → Rust state → re-render cycle\n- Copy Anonymized works as before\n- `zapcap_byte_to_char_offsets` deleted (no longer needed)\n- All four documents updated with no-JS policy\n- Existing tests pass (`tt/apcw-t.Test.sh`)",
.claude/jjm/jjg_gallops.json:3808:              "jjgtn_text": "## Character\n\nExploratory engineering spike, not mechanical implementation. A parallel investigation that does NOT touch `apcre_engine.rs` — builds a standalone binary that runs the StanfordAIMI/stanford-deidentifier-base model locally via ONNX Runtime and emits `.stanford.assay.txt` alongside the existing `apcab` `.assay.txt` on the same fixtures for side-by-side comparison.\n\nMultiple unknowns to surface early: the model's BIO label set and its semantic overlap with APCK categories, token-to-byte offset alignment through the tokenizer, macOS ORT bundling implications if ever productized, and whether a ~440MB neural model earns its footprint versus the current 3-tier engine. The spike's deliverable is comparative evidence, not integration.\n\n**Anticipated hazard**: token-to-byte offset alignment is the same bug class that drove ₢A9AAP (byte-vs-char mismatch in the JS frontend). The Rust `tokenizers` crate returns offsets against whatever string you tokenized — ensure the plain text whose offsets get reported is byte-identical to the string fed to the tokenizer, and decide up front whether byte or char units are authoritative.\n\n## Goal\n\nLocal PHI de-identification on macOS using StanfordAIMI/stanford-deidentifier-base exported to ONNX, loaded via the `ort` crate, and compared against the current 3-tier engine on the existing HTML fixtures.\n\n## Architecture Decisions\n\n- **Parallel binary `apcnsa`** (App Neural Stanford Assay), mirrors `apcab`. No engine integration.\n- **Output format**: same guillemet markers as `apcab`, distinct suffix `{stem}.stanford.assay.txt` so both binaries can emit into `BURD_OUTPUT_DIR` without collision.\n- **Raw stanford labels** in the guillemets (e.g. `‹NAME Margaret Thornton›`, `‹DATE 03/15/1952›`). No mapping to APCK categories in this pace — mapping is a followup if the spike is promising.\n- **Model artifact policy**: hardcoded absolute path `/Users/bhyslop/models/stanford-deidentifier/` holds `model.onnx`, `tokenizer.json`, `vocab.txt`, `config.json`. Written as a constant `ZAPCC_STANFORD_DIR` in `apcc_cli.sh`. Distribution policy deferred — decide only if the spike earns productization.\n- **Python export as a workbench function**, not a `scripts/` file. Matches APCK's flat layout and BCG's function-over-script-file preference.\n- **Subtree prefix `apcns*`** declared for neural-stanford work, with `a` classifier for the binary → `apcnsa`. Mirrors `apca*`/`apcr*`/`apct*` convention. Future neural sources would be `apcnsr_*`, tests `apcnst_*`. Terminal exclusivity verified against the current prefix tree (`apcn*` is unused).\n\n## Deliverables\n\n**Three new tabtargets + colophons** (all under `apcw-ns*`):\n\n| Colophon | Folio | Function | Purpose |\n|----------|-------|----------|---------|\n| `apcw-nsx` | — | `apcc_neural_stanford_export` | Run `optimum-cli export onnx` if artifacts absent; idempotent |\n| `apcw-nsZ` | — | `apcc_neural_stanford_zap` | Nuclear reset: `rm -rf` the model dir, then re-run export |\n| `apcw-nsa` | param1 | `apcc_neural_stanford_assay` | Run `apcnsa` on an input directory of HTML files |\n\nCapital `Z` in `apcw-nsZ` follows RBW's case-discrimination convention for destructive siblings (compare `rbw-cC` Charge vs `rbw-cr` Rack).\n\n**New binary `apcnsa`** at `Tools/apck/apcd/src/apcnsa_main.rs`:\n- CLI argument: input directory\n- Glob `*.html`, parse via existing `apcrp`, feed plain text to tokenizer\n- ORT session on `model.onnx` with CPU EP, optimization Level3\n- argmax per token, decode BIO tags, map back to byte offsets\n- Render guillemet output to `{stem}.stanford.assay.txt` in `BURD_OUTPUT_DIR`\n- Regex pass (SSN, MRN, phone, DOB) as optional belt-and-suspenders layer — decide during implementation whether to include or defer\n\n**New document** `Tools/apck/APCNS-NeuralStanford.md`:\n- Model card summary (Stanford deidentifier base, architecture, label set)\n- Export procedure (Python deps, `optimum-cli` invocation — reproducible)\n- Artifact layout (hardcoded model dir contents)\n- Setup steps for a fresh machine\n- Observations section (filled during the spike: accuracy comparison, speed, false-positive/negative notes, productization recommendation)\n\n**Dependency additions** in `Tools/apck/apcd/Cargo.toml`:\n- `ort = \"2\"` (ONNX Runtime bindings)\n- `tokenizers = \"0.15\"` (HuggingFace tokenizer)\n- `ndarray = \"0.15\"` (tensor manipulation)\n\n**Command dependency discipline** (per BCG): export and zap functions `buc_die` with a helpful message if `optimum-cli` or `python` is absent from PATH. Don't fail opaquely.\n\n**Context doc updates** in `apck-claude-context.md`:\n- Prefix tree: add `apcns` non-terminal with `apcnsa` child\n- Tabtarget table: three new rows\n- File acronym mappings: add `APCNS`, `APCNSA`\n\n## Exit Criteria\n\n- Both fixtures (`epic_progress_note.html`, `epic_geriatric_consult.html`) produce `*.stanford.assay.txt` in `BURD_OUTPUT_DIR`\n- Side-by-side diff against the existing `apcab` output is readable\n- Written observations in `APCNS-NeuralStanford.md`: which PHI items each engine catches/misses, rough latency per fixture, recommendation on productization (integrate as Tier 4 / replace tiers / abandon)\n- Model artifacts staged at the hardcoded path\n- Three tabtargets dispatch cleanly; all three discoverable via `ls tt/`\n- Existing `tt/apcw-t.Test.sh` still passes (no regression)\n\n## Out of Scope (explicitly deferred)\n\n- Label-set mapping from stanford labels (NAME, DATE, ID…) to APCK categories (PATIENT, DOB, MRN, FACILITY…)\n- Integration into `apcre_engine.rs` as a Tier 4 mechanism\n- CoreML execution provider (CPU first; CoreML optional future work)\n- Model artifact distribution policy (hardcoded path for the spike; real policy decided post-spike)\n- Windows support\n- Tauri bundle size implications\n- Unit tests for the new binary (spike code, not production)",
.claude/jjm/jjg_gallops.json:3815:              "jjgtn_text": "## Character\n\nExploratory engineering spike, not mechanical implementation. A parallel investigation that does NOT touch `apcre_engine.rs` — builds a standalone binary that runs the StanfordAIMI/stanford-deidentifier-base model locally via ONNX Runtime and emits `.stanford.assay.txt` alongside the existing `apcab` `.assay.txt` on the same fixtures for side-by-side comparison.\n\nMultiple unknowns to surface early: the model's BIO label set and its semantic overlap with APCK categories, token-to-byte offset alignment through the tokenizer, macOS ORT bundling implications if ever productized, and whether a ~440MB neural model earns its footprint versus the current 3-tier engine. The spike's deliverable is comparative evidence, not integration.\n\n**Anticipated hazard**: token-to-byte offset alignment is the same bug class that drove ₢A9AAP (byte-vs-char mismatch in the JS frontend). The Rust `tokenizers` crate returns offsets against whatever string you tokenized — ensure the plain text whose offsets get reported is byte-identical to the string fed to the tokenizer, and decide up front whether byte or char units are authoritative.\n\n## Goal\n\nLocal PHI de-identification on macOS using StanfordAIMI/stanford-deidentifier-base exported to ONNX, loaded via the `ort` crate, and compared against the current 3-tier engine on the existing HTML fixtures.\n\n## Architecture Decisions\n\n- **Parallel binary `apcnsa`** (App Neural Stanford Assay), mirrors `apcab`. No engine integration.\n- **Output format**: same guillemet markers as `apcab`, distinct suffix `{stem}.stanford.assay.txt` so both binaries can emit into `BURD_OUTPUT_DIR` without collision.\n- **Raw stanford labels** in the guillemets (e.g. `‹NAME Margaret Thornton›`, `‹DATE 03/15/1952›`). No mapping to APCK categories in this pace — mapping is a followup if the spike is promising.\n- **Model artifact policy**: hardcoded absolute path `/Users/bhyslop/models/stanford-deidentifier/` holds `model.onnx`, `tokenizer.json`, `vocab.txt`, `config.json`. Written as a constant `ZAPCC_STANFORD_DIR` in `apcc_cli.sh`. Distribution policy deferred — decide only if the spike earns productization.\n- **Python export as a workbench function**, not a `scripts/` file. Matches APCK's flat layout and BCG's function-over-script-file preference.\n- **Subtree prefix `apcns*`** declared for neural-stanford work, with `a` classifier for the binary → `apcnsa`. Mirrors `apca*`/`apcr*`/`apct*` convention. Future neural sources would be `apcnsr_*`, tests `apcnst_*`. Terminal exclusivity verified against the current prefix tree (`apcn*` is unused).\n\n## Deliverables\n\n**Three new tabtargets + colophons** (all under `apcw-ns*`):\n\n| Colophon | Folio | Function | Purpose |\n|----------|-------|----------|---------|\n| `apcw-nsx` | — | `apcc_neural_stanford_export` | Run `optimum-cli export onnx` if artifacts absent; idempotent |\n| `apcw-nsZ` | — | `apcc_neural_stanford_zap` | Nuclear reset: `rm -rf` the model dir, then re-run export |\n| `apcw-nsa` | param1 | `apcc_neural_stanford_assay` | Run `apcnsa` on an input directory of HTML files |\n\nCapital `Z` in `apcw-nsZ` follows RBW's case-discrimination convention for destructive siblings (compare `rbw-cC` Charge vs `rbw-cr` Rack).\n\n**New binary `apcnsa`** at `Tools/apck/apcd/src/apcnsa_main.rs`:\n- CLI argument: input directory\n- Glob `*.html`, parse via existing `apcrp`, feed plain text to tokenizer\n- ORT session on `model.onnx` with CPU EP, optimization Level3\n- argmax per token, decode BIO tags, map back to byte offsets\n- Render guillemet output to `{stem}.stanford.assay.txt` in `BURD_OUTPUT_DIR`\n- Regex pass (SSN, MRN, phone, DOB) as optional belt-and-suspenders layer — decide during implementation whether to include or defer\n\n**New document** `Tools/apck/APCNS-NeuralStanford.md`:\n- Model card summary (Stanford deidentifier base, architecture, label set)\n- Export procedure (Python deps, `optimum-cli` invocation — reproducible)\n- Artifact layout (hardcoded model dir contents)\n- Setup steps for a fresh machine\n- Observations section (filled during the spike: accuracy comparison, speed, false-positive/negative notes, productization recommendation)\n\n**Dependency additions** in `Tools/apck/apcd/Cargo.toml`:\n- `ort = \"2\"` (ONNX Runtime bindings)\n- `tokenizers = \"0.15\"` (HuggingFace tokenizer)\n- `ndarray = \"0.15\"` (tensor manipulation)\n\n**Command dependency discipline** (per BCG): export and zap functions `buc_die` with a helpful message if `optimum-cli` or `python` is absent from PATH. Don't fail opaquely.\n\n**Context doc updates** in `apck-claude-context.md`:\n- Prefix tree: add `apcns` non-terminal with `apcnsa` child\n- Tabtarget table: three new rows\n- File acronym mappings: add `APCNS`, `APCNSA`\n\n## Exit Criteria\n\n- Both fixtures (`epic_progress_note.html`, `epic_geriatric_consult.html`) produce `*.stanford.assay.txt` in `BURD_OUTPUT_DIR`\n- Side-by-side diff against the existing `apcab` output is readable\n- Written observations in `APCNS-NeuralStanford.md`: which PHI items each engine catches/misses, rough latency per fixture, recommendation on productization (integrate as Tier 4 / replace tiers / abandon)\n- Model artifacts staged at the hardcoded path\n- Three tabtargets dispatch cleanly; all three discoverable via `ls tt/`\n- Existing `tt/apcw-t.Test.sh` still passes (no regression)\n\n## Out of Scope (explicitly deferred)\n\n- Label-set mapping from stanford labels (NAME, DATE, ID…) to APCK categories (PATIENT, DOB, MRN, FACILITY…)\n- Integration into `apcre_engine.rs` as a Tier 4 mechanism\n- CoreML execution provider (CPU first; CoreML optional future work)\n- Model artifact distribution policy (hardcoded path for the spike; real policy decided post-spike)\n- Windows support\n- Tauri bundle size implications\n- Unit tests for the new binary (spike code, not production)",
.claude/jjm/jjg_gallops.json:3826:              "jjgtn_text": "## Character\n\nFocused Rust addition. One new module, one integration point, no design ambiguity. Delivers the mechanism; collection is coordinated out-of-band.\n\nParallel-friendly: spec authoring and code implementation are orthogonal and can run concurrently in two officia committing against the same coronet.\n\n## Problem\n\nClipboard is consumed destructively — `get_text()` runs, detection runs, then the system clipboard is zeroed. We have no understanding of Epic's real flavor structure, and we can never grow the fixture library from real traffic.\n\n## Solution\n\nBefore the clipboard zero-out on the `Clinical` branch, write every arboard-accessible flavor verbatim to a numbered capture in `$HOME/apck_harvest/`.\n\n## Deliverables — Code\n\n- New module `apcrh_harvest.rs` exposing `pub fn apcrh_capture_all_flavors(dir: &Path) -> Result<u32, String>`; internal helpers `zapcrh_scan_next_index`, `zapcrh_write_text`, `zapcrh_write_html` (whichever flavors the crate version of arboard exposes).\n- Constants:\n  - `pub const APCRH_HARVEST_SEED_INDEX: u32 = 10000;`\n  - `pub const APCRH_HARVEST_DIR_NAME: &str = \"apck_harvest\";`\n  - Path is `$HOME/apck_harvest/` (no dotfile prefix). Resolve `$HOME` via `std::env::var(\"HOME\")`.\n- Test file `apcth_harvest.rs`:\n  - Empty directory → returns `APCRH_HARVEST_SEED_INDEX` (10000).\n  - Directory with `10000.html` and `10000.txt` present → next index is 10001 (same-index group counted as one).\n  - Non-numeric filenames present → ignored in max calculation.\n  - Gaps in numeric sequence → max+1, not lowest-gap-fill.\n- `lib.rs` wires `pub mod apcrh_harvest;` and `#[cfg(test)] mod apcth_harvest;`.\n- `apcap_main.rs::zapcap_handle_focus`: on `apcre_Result::Clinical`, before `clipboard.set_text(String::new())`, call `apcrh_capture_all_flavors(&harvest_dir)`. Failure logs via `apcrl_error_now!` and does NOT abort triage. Directory created lazily via `std::fs::create_dir_all`.\n- `apck-claude-context.md`: add `APCRH` file-acronym mapping, add `apcrh` node under `apcr*` in the prefix tree.\n- `.gitignore`: add `apck_harvest/` as belt-and-suspenders against accidental repo-tree placement.\n\n## Deliverables — Specification\n\nNew section in `Tools/apck/APCPS-PrototypeSpecification.md` titled `Clipboard Harvest` specifying:\n\n- *Purpose*: preserve verbatim real-world clipboard contents on Clinical-branch detection to grow the fixture library and understand Epic's flavor structure.\n- *Trigger*: the Clinical branch of clipboard analysis, executed before the system-clipboard zero-out.\n- *Storage*: `$HOME/apck_harvest/` — outside any repo tree; directory created lazily.\n- *Naming*: `{N}.{ext}` where `N` seeds at 10000 for empty directories, otherwise `max_existing_N + 1`. Same `N` groups all flavors of one capture.\n- *Flavors*: every flavor the clipboard abstraction exposes (text, HTML at minimum; RTF and image documented as gaps if the abstraction does not surface them).\n- *Failure mode*: a capture failure logs through the standard error channel and does not abort triage — user-visible behavior is unchanged.\n- *Privacy posture*: PHI-at-rest stays outside the repo; anonymization and promotion to `test_fixtures/` are manual, out-of-band.\n\n## Constraints\n\n- No new crate dependencies. arboard is already in the tree.\n- RCG conformance: `apcrh_` prefix (verified unused, terminal exclusive), `pub(crate)` + z-prefix for internal items, output via `apcrl_*_now!` only, `deny(warnings)` preserved.\n\n## Out of Scope\n\n- RTF and image flavor capture if the clipboard abstraction does not expose them — document as a gap, do not drop to platform-specific pasteboard APIs.\n- Harvest-inventory tabtarget.\n- `test_fixtures/harvested/` directory layout (defined when the first anonymized capture is promoted).\n- Automated anonymization tooling (user coordinates manually).\n\n## Exit Criteria\n\n- `tt/apcw-t.Test.sh` passes, including new `apcth_harvest` cases.\n- Local run via `tt/apcw-r.Run.sh` plus `tt/apcw-fl.FixtureLoad.progress.sh` produces `$HOME/apck_harvest/10000.html` and `10000.txt`.\n- A second fixture load increments to `10001.*`.\n- Triage UI behavior is visibly unchanged.\n- APCPS has the `Clipboard Harvest` section merged and proof-read by the user before wrap.",
.claude/jjm/jjg_gallops.json:3833:              "jjgtn_text": "## Character\n\nFocused Rust addition. One new module, one integration point, no design ambiguity. Delivers the mechanism; collection is coordinated out-of-band.\n\nParallel-friendly: spec authoring and code implementation are orthogonal and can run concurrently in two officia committing against the same coronet.\n\n## Problem\n\nClipboard is consumed destructively — `get_text()` runs, detection runs, then the system clipboard is zeroed. We have no understanding of Epic's real flavor structure, and we can never grow the fixture library from real traffic.\n\n## Solution\n\nBefore the clipboard zero-out on the `Clinical` branch, write every arboard-accessible flavor verbatim to a numbered capture in `$HOME/apck_harvest/`.\n\n## Deliverables — Code\n\n- New module `apcrh_harvest.rs` exposing `pub fn apcrh_capture_all_flavors(dir: &Path) -> Result<u32, String>`; internal helpers `zapcrh_scan_next_index`, `zapcrh_write_text`, `zapcrh_write_html` (whichever flavors the crate version of arboard exposes).\n- Constants:\n  - `pub const APCRH_HARVEST_SEED_INDEX: u32 = 10000;`\n  - `pub const APCRH_HARVEST_DIR_NAME: &str = \"apck_harvest\";`\n  - Path is `$HOME/apck_harvest/` (no dotfile prefix). Resolve `$HOME` via `std::env::var(\"HOME\")`.\n- Test file `apcth_harvest.rs`:\n  - Empty directory → returns `APCRH_HARVEST_SEED_INDEX` (10000).\n  - Directory with `10000.html` and `10000.txt` present → next index is 10001 (same-index group counted as one).\n  - Non-numeric filenames present → ignored in max calculation.\n  - Gaps in numeric sequence → max+1, not lowest-gap-fill.\n- `lib.rs` wires `pub mod apcrh_harvest;` and `#[cfg(test)] mod apcth_harvest;`.\n- `apcap_main.rs::zapcap_handle_focus`: on `apcre_Result::Clinical`, before `clipboard.set_text(String::new())`, call `apcrh_capture_all_flavors(&harvest_dir)`. Failure logs via `apcrl_error_now!` and does NOT abort triage. Directory created lazily via `std::fs::create_dir_all`.\n- `apck-claude-context.md`: add `APCRH` file-acronym mapping, add `apcrh` node under `apcr*` in the prefix tree.\n- `.gitignore`: add `apck_harvest/` as belt-and-suspenders against accidental repo-tree placement.\n\n## Deliverables — Specification\n\nNew section in `Tools/apck/APCPS-PrototypeSpecification.md` titled `Clipboard Harvest` specifying:\n\n- *Purpose*: preserve verbatim real-world clipboard contents on Clinical-branch detection to grow the fixture library and understand Epic's flavor structure.\n- *Trigger*: the Clinical branch of clipboard analysis, executed before the system-clipboard zero-out.\n- *Storage*: `$HOME/apck_harvest/` — outside any repo tree; directory created lazily.\n- *Naming*: `{N}.{ext}` where `N` seeds at 10000 for empty directories, otherwise `max_existing_N + 1`. Same `N` groups all flavors of one capture.\n- *Flavors*: every flavor the clipboard abstraction exposes (text, HTML at minimum; RTF and image documented as gaps if the abstraction does not surface them).\n- *Failure mode*: a capture failure logs through the standard error channel and does not abort triage — user-visible behavior is unchanged.\n- *Privacy posture*: PHI-at-rest stays outside the repo; anonymization and promotion to `test_fixtures/` are manual, out-of-band.\n\n## Constraints\n\n- No new crate dependencies. arboard is already in the tree.\n- RCG conformance: `apcrh_` prefix (verified unused, terminal exclusive), `pub(crate)` + z-prefix for internal items, output via `apcrl_*_now!` only, `deny(warnings)` preserved.\n\n## Out of Scope\n\n- RTF and image flavor capture if the clipboard abstraction does not expose them — document as a gap, do not drop to platform-specific pasteboard APIs.\n- Harvest-inventory tabtarget.\n- `test_fixtures/harvested/` directory layout (defined when the first anonymized capture is promoted).\n- Automated anonymization tooling (user coordinates manually).\n\n## Exit Criteria\n\n- `tt/apcw-t.Test.sh` passes, including new `apcth_harvest` cases.\n- Local run via `tt/apcw-r.Run.sh` plus `tt/apcw-fl.FixtureLoad.progress.sh` produces `$HOME/apck_harvest/10000.html` and `10000.txt`.\n- A second fixture load increments to `10001.*`.\n- Triage UI behavior is visibly unchanged.\n- APCPS has the `Clipboard Harvest` section merged and proof-read by the user before wrap.",
.claude/jjm/jjg_gallops.json:3880:              "jjgtn_text": "## Character\n\nMechanical application of settled decisions. The substantive design work happened in the session that produced this pace — every decision below is a write-down, not an open question. Sonnet-scale: judgment calls are narrow (prefix alignment, linked-term phrasing, quoin placement), but the decisions themselves need no re-deliberation.\n\n## Context\n\nThe Stanford spike (₢A9AAQ) exposed structural incoherence in APCS0. A design conversation reshaped the concept model along these lines:\n\n- **Byte ranges are implementation details, not vocabulary.** The concept model talks about regions of normalized text; byte offsets live in code only.\n- **Algorithms run in parallel on the same input**, not serially refining each other's output. Each discerner is a pure function of `apcs_normalized_text`.\n- **Severity vocabulary (RED/YELLOW/PASS) is dropped.** It conflated epistemic confidence, default disposition, and display affordance — three distinct things collapsed under one label.\n- **No generic `apcs_algorithm` or `apcs_reliability_profile`.** Discerners are named intimately; reconciliation rules reference them by name. Generic abstractions were premature and invited non-load-bearing complexity.\n- **Four concrete discerners**, each with its own registry/parameter quoin and its own finding-output quoin, shaped in parallel but intimately named (no common supertype).\n- **`apcs_reconciliation` is named** as the fusion procedure but its rules are deferred to the sibling pace `apcs-combining-algorithm`.\n\n## Scope\n\nApply these decisions to `Tools/apck/APCS0-SpecTop.adoc`. No code changes. No decisions re-opened.\n\n### Quoins to install\n\n**New in the mapping section:**\n\n- `apcs_normalization` — one-time light cleanup procedure producing the canonical input\n- `apcs_normalized_text` — the single input every discerner consumes\n- `apcs_regex_scan` — the discerner (parameterized by `apcs_regex_table`, which remains)\n- `apcs_label_scan` — the discerner (parameterized by `apcs_label_registry`, which remains)\n- `apcs_dictionary_scan` — the discerner (parameterized by dictionaries + whitelists, which remain)\n- `apcs_stanford_ner` — the discerner (parameterized by the frozen ONNX model + its flat taxonomy)\n- `apcs_regex_finding`, `apcs_label_finding`, `apcs_dictionary_finding`, `apcs_stanford_finding` — per-discerner output quoins, sibling-shaped, intimately named\n- `apcs_reconciliation` — stub with pointer to the deferred section\n- `apcs_unified_classification` — the reconciliation output consumed by anonymization\n- `apcs_anonymization` — the procedure consuming `apcs_unified_classification` and producing `apcs_anonymized_text`. Named for symmetry with `apcs_normalization` and `apcs_reconciliation` — the three procedures of the pipeline\n\n### Quoins to remove\n\n- `apcs_severity`, `apcs_red`, `apcs_yellow`, `apcs_pass` — severity gestalt is abandoned\n- `apcs_merge` — reconciliation replaces merge and is deferred\n- `apcs_finding` as a generic type — replaced by four intimately-named per-discerner outputs\n\n### Quoins to reshape\n\n- The \"Pipeline Steps\" section — no longer a linear sequence. Becomes three sections: `== Discerners` (parallel, four subsections), `== Reconciliation [DEFERRED]`, `== Anonymization`\n- `apcs_anonymized_text` — now consumes `apcs_unified_classification`, not the old merged-findings list\n- `apcs_assay` family — the diagnostic projection now renders per-discerner and per-unified views. Lift to a family with a variant dimension, or split as siblings (`apcs_discerner_assay`, `apcs_unified_assay`) — editor choice at write time\n- Dictionary scan contradiction — fix the RED-vs-YELLOW logic ambiguity in the prose to match actual code behavior (blacklist-only vs. blacklist+whitelist). Internal-disposition asymmetry flagged as deferred\n\n### Deferred in this pace (explicit stubs)\n\n- `== Reconciliation` section: stub header with `[DEFERRED — see ₢A9AAV apcs-combining-algorithm]` flag\n- Role/disposition terminology (TEXT/PHI/DECIDE vs cover/surface vs other) marked deferred\n- Dictionary-finding internal-disposition schema marked deferred\n- Clinician-toggle composition marked deferred\n\n### Non-goals\n\n- No reconciliation rules\n- No new disposition terminology\n- No code changes to `apcre_engine.rs` or any Rust file\n- No changes to APCAS, APCPS, or `apck-claude-context.md`\n\n## Exit Criteria\n\n- `APCS0-SpecTop.adoc` renders via AsciiDoc without errors\n- Link validation clean — every `{apcs_*}` resolves to an anchor; every anchor has at least one attribute reference\n- Every removed quoin is either replaced in vocabulary or the removal is explained in the spec\n- Every deferred stub is clearly marked and points to ₢A9AAV\n- A fresh reader can understand the architectural shape (parallel discerners → deferred reconciliation → anonymization) from the document alone\n- The dictionary-scan internal contradiction is resolved in prose",
.claude/jjm/jjg_gallops.json:3887:              "jjgtn_text": "## Character\n\nMechanical application of settled decisions. The substantive design work happened in the session that produced this pace — every decision below is a write-down, not an open question. Sonnet-scale: judgment calls are narrow (prefix alignment, linked-term phrasing, quoin placement), but the decisions themselves need no re-deliberation.\n\n## Context\n\nThe Stanford spike (₢A9AAQ) exposed structural incoherence in APCS0. A design conversation reshaped the concept model along these lines:\n\n- **Byte ranges are implementation details, not vocabulary.** The concept model talks about regions of normalized text; byte offsets live in code only.\n- **Algorithms run in parallel on the same input**, not serially refining each other's output. Each discerner is a pure function of `apcs_normalized_text`.\n- **Severity vocabulary (RED/YELLOW/PASS) is dropped.** It conflated epistemic confidence, default disposition, and display affordance — three distinct things collapsed under one label.\n- **No generic `apcs_algorithm` or `apcs_reliability_profile`.** Discerners are named intimately; reconciliation rules reference them by name. Generic abstractions were premature and invited non-load-bearing complexity.\n- **Four concrete discerners**, each with its own registry/parameter quoin and its own finding-output quoin, shaped in parallel but intimately named (no common supertype).\n- **`apcs_reconciliation` is named** as the fusion procedure but its rules are deferred to the sibling pace `apcs-combining-algorithm`.\n\n## Scope\n\nApply these decisions to `Tools/apck/APCS0-SpecTop.adoc`. No code changes. No decisions re-opened.\n\n### Quoins to install\n\n**New in the mapping section:**\n\n- `apcs_normalization` — one-time light cleanup procedure producing the canonical input\n- `apcs_normalized_text` — the single input every discerner consumes\n- `apcs_regex_scan` — the discerner (parameterized by `apcs_regex_table`, which remains)\n- `apcs_label_scan` — the discerner (parameterized by `apcs_label_registry`, which remains)\n- `apcs_dictionary_scan` — the discerner (parameterized by dictionaries + whitelists, which remain)\n- `apcs_stanford_ner` — the discerner (parameterized by the frozen ONNX model + its flat taxonomy)\n- `apcs_regex_finding`, `apcs_label_finding`, `apcs_dictionary_finding`, `apcs_stanford_finding` — per-discerner output quoins, sibling-shaped, intimately named\n- `apcs_reconciliation` — stub with pointer to the deferred section\n- `apcs_unified_classification` — the reconciliation output consumed by anonymization\n- `apcs_anonymization` — the procedure consuming `apcs_unified_classification` and producing `apcs_anonymized_text`. Named for symmetry with `apcs_normalization` and `apcs_reconciliation` — the three procedures of the pipeline\n\n### Quoins to remove\n\n- `apcs_severity`, `apcs_red`, `apcs_yellow`, `apcs_pass` — severity gestalt is abandoned\n- `apcs_merge` — reconciliation replaces merge and is deferred\n- `apcs_finding` as a generic type — replaced by four intimately-named per-discerner outputs\n\n### Quoins to reshape\n\n- The \"Pipeline Steps\" section — no longer a linear sequence. Becomes three sections: `== Discerners` (parallel, four subsections), `== Reconciliation [DEFERRED]`, `== Anonymization`\n- `apcs_anonymized_text` — now consumes `apcs_unified_classification`, not the old merged-findings list\n- `apcs_assay` family — the diagnostic projection now renders per-discerner and per-unified views. Lift to a family with a variant dimension, or split as siblings (`apcs_discerner_assay`, `apcs_unified_assay`) — editor choice at write time\n- Dictionary scan contradiction — fix the RED-vs-YELLOW logic ambiguity in the prose to match actual code behavior (blacklist-only vs. blacklist+whitelist). Internal-disposition asymmetry flagged as deferred\n\n### Deferred in this pace (explicit stubs)\n\n- `== Reconciliation` section: stub header with `[DEFERRED — see ₢A9AAV apcs-combining-algorithm]` flag\n- Role/disposition terminology (TEXT/PHI/DECIDE vs cover/surface vs other) marked deferred\n- Dictionary-finding internal-disposition schema marked deferred\n- Clinician-toggle composition marked deferred\n\n### Non-goals\n\n- No reconciliation rules\n- No new disposition terminology\n- No code changes to `apcre_engine.rs` or any Rust file\n- No changes to APCAS, APCPS, or `apck-claude-context.md`\n\n## Exit Criteria\n\n- `APCS0-SpecTop.adoc` renders via AsciiDoc without errors\n- Link validation clean — every `{apcs_*}` resolves to an anchor; every anchor has at least one attribute reference\n- Every removed quoin is either replaced in vocabulary or the removal is explained in the spec\n- Every deferred stub is clearly marked and points to ₢A9AAV\n- A fresh reader can understand the architectural shape (parallel discerners → deferred reconciliation → anonymization) from the document alone\n- The dictionary-scan internal contradiction is resolved in prose",
.claude/jjm/jjg_gallops.json:3894:              "jjgtn_text": "## Character\n\nMechanical application of settled decisions. The substantive design work happened in the session that produced this pace — every decision below is a write-down, not an open question. Sonnet-scale: judgment calls are narrow (prefix alignment, linked-term phrasing, quoin placement), but the decisions themselves need no re-deliberation.\n\n## Context\n\nThe Stanford spike (₢A9AAQ) exposed structural incoherence in APCS0. A design conversation reshaped the concept model along these lines:\n\n- **Byte ranges are implementation details, not vocabulary.** The concept model talks about regions of normalized text; byte offsets live in code only.\n- **Algorithms run in parallel on the same input**, not serially refining each other's output. Each discerner is a pure function of `apcs_normalized_text`.\n- **Severity vocabulary (RED/YELLOW/PASS) is dropped.** It conflated epistemic confidence, default disposition, and display affordance — three distinct things collapsed under one label.\n- **No generic `apcs_algorithm` or `apcs_reliability_profile`.** Discerners are named intimately; reconciliation rules reference them by name. Generic abstractions were premature and invited non-load-bearing complexity.\n- **Four concrete discerners**, each with its own registry/parameter quoin and its own finding-output quoin, shaped in parallel but intimately named (no common supertype).\n- **`apcs_reconciliation` is named** as the fusion procedure but its rules are deferred to the sibling pace `apcs-combining-algorithm`.\n\n## Scope\n\nApply these decisions to `Tools/apck/APCS0-SpecTop.adoc`. No code changes. No decisions re-opened.\n\n### Quoins to install\n\n**New in the mapping section:**\n\n- `apcs_normalization` — one-time light cleanup procedure producing the canonical input\n- `apcs_normalized_text` — the single input every discerner consumes\n- `apcs_regex_scan` — the discerner (parameterized by `apcs_regex_table`, which remains)\n- `apcs_label_scan` — the discerner (parameterized by `apcs_label_registry`, which remains)\n- `apcs_dictionary_scan` — the discerner (parameterized by dictionaries + whitelists, which remain)\n- `apcs_stanford_ner` — the discerner (parameterized by the frozen ONNX model + its flat taxonomy)\n- `apcs_regex_finding`, `apcs_label_finding`, `apcs_dictionary_finding`, `apcs_stanford_finding` — per-discerner output quoins, sibling-shaped, intimately named\n- `apcs_reconciliation` — stub with pointer to the deferred section\n- `apcs_unified_classification` — the reconciliation output consumed by anonymization\n\n### Quoins to remove\n\n- `apcs_severity`, `apcs_red`, `apcs_yellow`, `apcs_pass` — severity gestalt is abandoned\n- `apcs_merge` — reconciliation replaces merge and is deferred\n- `apcs_finding` as a generic type — replaced by four intimately-named per-discerner outputs\n\n### Quoins to reshape\n\n- The \"Pipeline Steps\" section — no longer a linear sequence. Becomes three sections: `== Discerners` (parallel, four subsections), `== Reconciliation [DEFERRED]`, `== Anonymization`\n- `apcs_anonymized_text` — now consumes `apcs_unified_classification`, not the old merged-findings list\n- `apcs_assay` family — the diagnostic projection now renders per-discerner and per-unified views. Lift to a family with a variant dimension, or split as siblings (`apcs_discerner_assay`, `apcs_unified_assay`) — editor choice at write time\n- Dictionary scan contradiction — fix the RED-vs-YELLOW logic ambiguity in the prose to match actual code behavior (blacklist-only vs. blacklist+whitelist). Internal-disposition asymmetry flagged as deferred\n\n### Deferred in this pace (explicit stubs)\n\n- `== Reconciliation` section: stub header with `[DEFERRED — see ₢A9AAS apcs-combining-algorithm]` flag\n- Role/disposition terminology (TEXT/PHI/DECIDE vs cover/surface vs other) marked deferred\n- Dictionary-finding internal-disposition schema marked deferred\n- Clinician-toggle composition marked deferred\n\n### Non-goals\n\n- No reconciliation rules\n- No new disposition terminology\n- No code changes to `apcre_engine.rs` or any Rust file\n- No changes to APCAS, APCPS, or `apck-claude-context.md`\n\n## Exit Criteria\n\n- `APCS0-SpecTop.adoc` renders via AsciiDoc without errors\n- Link validation clean — every `{apcs_*}` resolves to an anchor; every anchor has at least one attribute reference\n- Every removed quoin is either replaced in vocabulary or the removal is explained in the spec\n- Every deferred stub is clearly marked and points to ₢A9AAS\n- A fresh reader can understand the architectural shape (parallel discerners → deferred reconciliation → anonymization) from the document alone\n- The dictionary-scan internal contradiction is resolved in prose",
.claude/jjm/jjg_gallops.json:3933:              "jjgtn_text": "## Character\n\nOpen design conversation requiring judgment. The question \"how do multiple discerners' outputs get fused into a single unified classification?\" has no mechanical answer — it requires choosing between competing shapes (role/disposition terminology, rule formulation, dictionary asymmetry handling, clinician-toggle composition). Opus-scale: several non-obvious decisions, each with downstream consequences through the spec and eventual code.\n\n## Prerequisites\n\nPace ₢A9AAU (`apcs-discerner-restructure`) must land first. That pace installs the architectural skeleton (parallel discerners, normalized-text input, anonymization output) and stubs the reconciliation section. This pace fills the stub.\n\n## Context\n\nIn the same session that produced ₢A9AAU, a second design conversation began but was deliberately deferred rather than rushed. The open questions are known and named; the answers are not.\n\n## Open Questions Carried Forward\n\n1. **Role / disposition terminology — what replaces severity?**\n   - Candidate A: unified classification carries a role from `{TEXT, PHI, DECIDE}` per segment\n   - Candidate B: findings carry a disposition from `{cover, surface}` individually\n   - Candidate C: a third shape not yet surfaced\n   - The choice has downstream effects on anonymization, triage UI, and every discerner output schema\n2. **Dictionary internal disposition.** Does `apcs_dictionary_finding` carry a self-dispositioned field (confident vs. ambiguous, derived from whitelist corroboration), or does reconciliation derive this from external rules? Earlier conversation leaned toward internal — the dictionary has the whitelist, so it's the one with the evidence — but the asymmetry with other discerners needs to be documented honestly.\n3. **Clinician toggle composition.** Three candidate shapes:\n   - Clinician as a discerner producing `apcs_clinician_finding` that enters reconciliation\n   - Clinician as a post-reconciliation mutator operating on `apcs_unified_classification`\n   - Some third shape (e.g., clinician-as-disposition-override)\n4. **Granularity reconciliation.** When two discerners produce findings at different granularities over overlapping text (e.g., `apcs_stanford_finding` covering `Margaret J. Thornton` and `apcs_dictionary_finding` covering just `Margaret`), how is the unified classification sized? Phrase wins? Union? Explicit rule per discerner pair?\n\n## Scope\n\n- Resolve each open question above with concrete design decisions\n- Write the concrete reconciliation rules — pairwise as needed, following the \"intimate, not generic\" discipline. No `apcs_reliability_profile`; rules reference specific discerner outputs by name\n- Update `apcs_reconciliation` in APCS0 from stub to authoritative\n- Name any new quoins the rules require (candidates: `apcs_corroboration`, `apcs_subsumption`, `apcs_role`, `apcs_disposition`, `apcs_clinician_decision`)\n- Cascade changes through APCS0 as decisions crystallize — finding outputs may gain fields; `apcs_unified_classification` receives its concrete shape\n\n## Expected Concrete Rules (Starting Set, Open to Redirect)\n\nThese are hypotheses for the next session, not commitments:\n\n- `apcs_regex_finding` is authoritative for its category — no corroboration needed, no contradiction admitted\n- `apcs_stanford_finding(PATIENT)` overlapping a Patient-label `apcs_label_finding` → unified as the phrase-scoped finding (granularity merger)\n- `apcs_dictionary_finding` uncorroborated by `apcs_stanford_finding` or an `apcs_label_finding` → surfaced for clinician decision\n- `apcs_city_finding` from dictionary corroborated by `apcs_stanford_finding(HOSPITAL)` → unified as FACILITY (HOSPITAL subsumes CITY)\n- Category authority table: per-category rule for which discerner wins when discerners disagree\n\n## Non-goals\n\n- No code changes to Rust sources (the spec drives code, not the other way around — code changes come in a later pace once APCS0 is authoritative)\n- No changes to APCAS, APCPS, or `apck-claude-context.md` unless a cascade from APCS0 makes them incorrect\n- No changes to the four discerners themselves — their outputs are the inputs to reconciliation, not the subject of this pace\n\n## Exit Criteria\n\n- `== Reconciliation` section in APCS0 is authoritative (no DEFERRED flags anywhere)\n- Terminology for role/disposition is chosen, documented, and voiced in the right AXLA motifs\n- Dictionary-finding schema is decided and expressed in the `apcs_dictionary_finding` quoin\n- Clinician-toggle composition is decided and expressed as a quoin or as a rule\n- Every rule references specific discerner outputs by name — no generic abstractions\n- A reader can trace the complete path from `apcs_normalized_text` through each discerner, through reconciliation, to `apcs_unified_classification`, and on to anonymized output — no gaps, no hand-waving\n- `tt/apcw-t.Test.sh` still passes (the spec change is not code, but no cascade should break tests)",
.claude/jjm/jjg_gallops.json:3980:              "jjgtn_text": "## Character\n\nShipped. NSPasteboard FFI for declared-flavor capture via `apcrb_pasteboard.rs`.\n\n## What landed\n\n- `apcrb_pasteboard.rs` — `apcrb_capture_declared_flavors(dir, classification, index)`: reads NSPasteboard via objc2-app-kit 0.3.2, enumerates first item's declared UTIs, writes each `dataForType` payload to `{N}-in.{tag}.{ext}`.\n- UTI mapper: public.rtf→rtf, utf8-plain-text→utf8.txt, utf16-plain-text→utf16.txt, plain-text→txt, html→html, tiff/png/jpeg→matching, url/file-url→url/fileurl.\n- Unknown UTIs: `zapcrb_sanitize_uti` → `{sanitized}.bin`.\n- Non-macOS stub: honest error.\n- `apcrh_harvest` thinned to create-dir + scan + delegate; defensive arboard fallback rejected (silent duplication worse than visible failure).\n- Cargo.toml: macOS-only objc2=0.6.4, objc2-foundation=0.3.2, objc2-app-kit=0.3.2.\n- +11 apctb_pasteboard tests; 87 total pass; `deny(warnings)` holds.\n- APCPS Clipboard Harvest refreshed; apck-claude-context.md Source modules + Prefix Tree updated.\n\n## Deviations from original docket\n\n- Module named `apcrb` not `apcrk` (B-for-pasteBoard matches source-module initial-of-noun convention).\n- Defensive arboard fallback removed, not added.",
.claude/jjm/jjg_gallops.json:3987:              "jjgtn_text": "## Character\n\nShipped. NSPasteboard FFI for declared-flavor capture via `apcrb_pasteboard.rs`.\n\n## What landed\n\n- `apcrb_pasteboard.rs` — `apcrb_capture_declared_flavors(dir, classification, index)`: reads NSPasteboard via objc2-app-kit 0.3.2, enumerates first item's declared UTIs, writes each `dataForType` payload to `{N}-in.{tag}.{ext}`.\n- UTI mapper: public.rtf→rtf, utf8-plain-text→utf8.txt, utf16-plain-text→utf16.txt, plain-text→txt, html→html, tiff/png/jpeg→matching, url/file-url→url/fileurl.\n- Unknown UTIs: `zapcrb_sanitize_uti` → `{sanitized}.bin`.\n- Non-macOS stub: honest error.\n- `apcrh_harvest` thinned to create-dir + scan + delegate; defensive arboard fallback rejected (silent duplication worse than visible failure).\n- Cargo.toml: macOS-only objc2=0.6.4, objc2-foundation=0.3.2, objc2-app-kit=0.3.2.\n- +11 apctb_pasteboard tests; 87 total pass; `deny(warnings)` holds.\n- APCPS Clipboard Harvest refreshed; apck-claude-context.md Source modules + Prefix Tree updated.\n\n## Deviations from original docket\n\n- Module named `apcrb` not `apcrk` (B-for-pasteBoard matches source-module initial-of-noun convention).\n- Defensive arboard fallback removed, not added.",
.claude/jjm/jjg_gallops.json:4023:              "jjgtn_text": "## Character\n\nRust-side wiring of the container loop. Spec contracts from APCS0/APCPS. Mostly mechanical plumbing; empirical container behavior from ₢A9AAZ informs this docket.\n\n## Assumptions\n\n- Single-tenant: never more than one transaction in flight. `apcrc_await_output(N)` waits specifically for `{N}.json`; no M ≥ N complexity this pace.\n- Wire-protocol refinement (ping, drop-old hardening) is a separate pace.\n\n## Scope\n\n### Harvest\n\n- `apcrh_harvest.rs`: on Clinical branch, write `$HOME/apcjd/{N}-in.txt` containing `apcs_normalized_text` (plain UTF-8 via existing whitespace-collapse path). Non-Clinical: no `{N}-in.txt`. Failure logs via `apcrl_error_now!` and continues.\n- `apcth_harvest.rs`: tests for Clinical `{N}-in.txt` write; confirm scanner's leading-digit parse still handles the expanded family.\n\n### Container module\n\nCreate `apcd/src/apcrc_container.rs`:\n\n- Serde types per APCPS schemas: `ApcrcStanfordFinding`, `ApcrcSpacyFinding` (Token/Entity enum, `#[serde(tag = \"kind\")]`), `ApcrcStanzaFinding` (same pattern), `ApcrcContainerOutput`.\n- `apcrc_await_output(journal_dir: &Path, index: u64, timeout: Duration) -> Result<ApcrcContainerOutput>`: `notify` crate (FSEvents on macOS) watches for `{index}.json`; reads + deserializes on appearance. Informative error on timeout or deserialize failure.\n\nCreate `apcd/src/apctc_container.rs`:\n\n- Unit tests: fixture JSON strings per discerner variant (token + entity).\n- Golden-file test: copy `Memos/memo-20260420-apck-container-fixture-assay/epic_initial.json` to a tempdir, `apcrc_await_output` picks it up, deserializes, structural assertions (top-level keys, rough counts).\n\nWire `lib.rs`: `pub mod apcrc_container;` + `#[cfg(test)] mod apctc_container;`.\n\n### Focus handler\n\n`apcap_main.rs::zapcap_handle_focus`: after harvest + anonymized-output write, call `apcrc_await_output(journal_dir, N, Duration::from_secs(5))` on a non-blocking task (UI must not block).\n\n- Success: log `container output received for N: stanford=S spacy_tok=T spacy_ent=E stanza_tok=T stanza_ent=E`. Decomposed counts so spaCy's generic-ENTITY bloat and syntactic-token volume are visible.\n- Timeout/error: log `[WARN] container output not received within 5s for index N: {error}`.\n- Graceful degradation: UI/triage proceeds normally on container absence.\n\n### Docs\n\n- `apck-claude-context.md`: apcrc + apctc rows in Source modules table; prefix tree entry.\n- APCPS: tightening only if serde types expose wire divergence (spec stays master).\n\n## Timeout rationale\n\n5s reflects empirical sub-1s steady-state per memo. Cold-start readiness belongs to the start tabtarget, not the focus handler.\n\n## Exit criteria\n\n- `tt/apcw-t.Test.sh` passes; `deny(warnings)` holds.\n- Container running: `{N}-in.txt` present, `{N}.json` appears within 5s, decomposed-count log line emitted.\n- Container stopped: graceful timeout warning; normal triage.\n- Source modules table updated.\n\n## Out of scope\n\n- Combining (A9AAV).\n- Wire-protocol hardening, ping (new pace).\n- UI consumption of container findings.\n- Anonymized-output awareness of container findings.\n- Decomposing `apcrm_match.rs` per APCS0.",
.claude/jjm/jjg_gallops.json:4030:              "jjgtn_text": "## Character\n\nRust-side wiring of the container loop. Spec contracts from APCS0/APCPS. Mostly mechanical plumbing; empirical container behavior from ₢A9AAZ informs this docket.\n\n## Assumptions\n\n- Single-tenant: never more than one transaction in flight. `apcrc_await_output(N)` waits specifically for `{N}.json`; no M ≥ N complexity this pace.\n- Wire-protocol refinement (ping, drop-old hardening) is a separate pace.\n\n## Scope\n\n### Harvest\n\n- `apcrh_harvest.rs`: on Clinical branch, write `$HOME/apcjd/{N}-in.txt` containing `apcs_normalized_text` (plain UTF-8 via existing whitespace-collapse path). Non-Clinical: no `{N}-in.txt`. Failure logs via `apcrl_error_now!` and continues.\n- `apcth_harvest.rs`: tests for Clinical `{N}-in.txt` write; confirm scanner's leading-digit parse still handles the expanded family.\n\n### Container module\n\nCreate `apcd/src/apcrc_container.rs`:\n\n- Serde types per APCPS schemas: `ApcrcStanfordFinding`, `ApcrcSpacyFinding` (Token/Entity enum, `#[serde(tag = \"kind\")]`), `ApcrcStanzaFinding` (same pattern), `ApcrcContainerOutput`.\n- `apcrc_await_output(journal_dir: &Path, index: u64, timeout: Duration) -> Result<ApcrcContainerOutput>`: `notify` crate (FSEvents on macOS) watches for `{index}.json`; reads + deserializes on appearance. Informative error on timeout or deserialize failure.\n\nCreate `apcd/src/apctc_container.rs`:\n\n- Unit tests: fixture JSON strings per discerner variant (token + entity).\n- Golden-file test: copy `Memos/memo-20260420-apck-container-fixture-assay/epic_initial.json` to a tempdir, `apcrc_await_output` picks it up, deserializes, structural assertions (top-level keys, rough counts).\n\nWire `lib.rs`: `pub mod apcrc_container;` + `#[cfg(test)] mod apctc_container;`.\n\n### Focus handler\n\n`apcap_main.rs::zapcap_handle_focus`: after harvest + anonymized-output write, call `apcrc_await_output(journal_dir, N, Duration::from_secs(5))` on a non-blocking task (UI must not block).\n\n- Success: log `container output received for N: stanford=S spacy_tok=T spacy_ent=E stanza_tok=T stanza_ent=E`. Decomposed counts so spaCy's generic-ENTITY bloat and syntactic-token volume are visible.\n- Timeout/error: log `[WARN] container output not received within 5s for index N: {error}`.\n- Graceful degradation: UI/triage proceeds normally on container absence.\n\n### Docs\n\n- `apck-claude-context.md`: apcrc + apctc rows in Source modules table; prefix tree entry.\n- APCPS: tightening only if serde types expose wire divergence (spec stays master).\n\n## Timeout rationale\n\n5s reflects empirical sub-1s steady-state per memo. Cold-start readiness belongs to the start tabtarget, not the focus handler.\n\n## Exit criteria\n\n- `tt/apcw-t.Test.sh` passes; `deny(warnings)` holds.\n- Container running: `{N}-in.txt` present, `{N}.json` appears within 5s, decomposed-count log line emitted.\n- Container stopped: graceful timeout warning; normal triage.\n- Source modules table updated.\n\n## Out of scope\n\n- Combining (A9AAV).\n- Wire-protocol hardening, ping (new pace).\n- UI consumption of container findings.\n- Anonymized-output awareness of container findings.\n- Decomposing `apcrm_match.rs` per APCS0.",
.claude/jjm/jjg_gallops.json:4037:              "jjgtn_text": "## Character\n\nRust code wiring the existing Clipbuddy focus handler to the container's bind-mount interface. Not architectural — APCS0 and APCPS already pin every contract this pace consumes. Sonnet-scale, mostly mechanical plumbing with one interesting beat (graceful degradation when the container isn't up).\n\n## Prerequisites\n\n₢A9AAZ (`container-scaffolding`) must land first — the container has to exist and produce valid `{N}.json` before Rust can integrate with it. Smoke-test validation of the container is the entry gate for this pace.\n\n## Context\n\nA9AAV decided: Rust Clipbuddy stays the frontend; sophisticated algorithms live in the container; combining stays in Rust (strongly typed). ₢A9AAZ constructs the container in isolation. This pace builds the Rust half: write `{N}-in.txt`, watch for `{N}.json`, deserialize, log. No combining yet — this pace proves the loop, the next pace (combining-v1, deferred) does the intelligence once empirical evidence is in hand.\n\n## Scope — Harvest Update\n\nUpdate `apcrh_harvest.rs`:\n\n- On the Clinical branch, after the existing raw-flavor captures (`{N}-in.clinical.{ext}` family), also write `$HOME/apcjd/{N}-in.txt` containing `apcs_normalized_text`: plain UTF-8 output of the existing HTML-strip / whitespace-collapse / encoding-normalization path.\n- The bare `{N}-in.txt` name (no `{tag}` infix, no UTI suffix) is documented in APCPS Clipboard Harvest section as the container's input file.\n- Non-clinical branch still writes only raw captures; no `{N}-in.txt` on that branch (no detection pipeline runs).\n- Write failure is non-fatal: log via `apcrl_error_now!` and continue. Matches the existing harvest failure-mode discipline.\n\nUpdate `apcth_harvest.rs`:\n\n- Add tests covering `{N}-in.txt` write on Clinical branch.\n- Confirm the scanner's leading-digit parse still handles the expanded file family correctly.\n\n## Scope — Container Integration Module\n\nCreate `apcd/src/apcrc_container.rs`:\n\n- Rust types mirroring the APCPS JSON schemas:\n  - `ApcrcStanfordFinding { text, start, end, label, confidence }`\n  - `ApcrcSpacyFinding` — enum over `Token { text, start, end, pos, tag, morph, lemma, head, dep }` and `Entity { text, start, end, label }` variants discriminated by the `kind` field.\n  - `ApcrcStanzaFinding` — same pattern, `Token { text, start, end, upos, xpos, feats, lemma, head, deprel, ner }` and `Entity { text, start, end, label }`.\n  - `ApcrcContainerOutput { index, stanford: { findings: Vec<Stanford> }, spacy: { findings: Vec<Spacy> }, stanza: { findings: Vec<Stanza> } }` at top level.\n- `serde::Deserialize` derives on all types. Use `#[serde(tag = \"kind\")]` for the token/entity enums.\n- Function `apcrc_await_output(journal_dir: &Path, index: u64, timeout: Duration) -> Result<ApcrcContainerOutput>` that uses the `notify` crate (FSEvents on macOS) to watch for `{index}.json` existing, then reads and deserializes. Returns an informative error on timeout or on deserialize failure.\n\nCreate `apcd/src/apctc_container.rs`:\n\n- Tests against fixture JSON strings matching the APCPS schema for each discerner variant — token + entity, each with representative field values. Confirm deserialization produces the expected Rust values.\n- Add a golden-file test: a small fixture `{N}.json` on disk, `apcrc_await_output` picks it up and deserializes correctly.\n\nWire into `lib.rs`:\n\n- `pub mod apcrc_container;`\n- `#[cfg(test)] mod apctc_container;`\n\n## Scope — Focus Handler Wire-up\n\nUpdate `apcap_main.rs::zapcap_handle_focus`:\n\n- On Clinical branch, after the existing harvest + anonymized-output write path, call `apcrc_await_output(journal_dir, N, Duration::from_secs(30))`.\n- On success: log `[INFO] container output received: stanford={S} spacy={C} stanza={Z}` where the three counts are the `findings.len()` from each discerner key. No combining, no UI consumption — just the log line confirming the loop works.\n- On timeout or error: log `[WARN] container output not received within 30s for index N: {error}`. Focus handler continues normally — existing Rust-only triage path is authoritative for UI.\n- The graceful-degradation discipline is load-bearing: if the container is down, triage still works with Rust-side findings only.\n\n## Scope — Documentation Cascade\n\n- `apck-claude-context.md`: add `apcrc_container.rs` + `apctc_container.rs` rows to the Source modules table. Update the Detection Architecture section if the integration point warrants a note.\n- APCPS: minor tightening if any reality diverges from the JSON schema as written; spec is master, Rust types must match it.\n\n## Constraints\n\n- **No combining logic this pace**. Container findings accumulate in the log, not in the evidence pool yet. Combining is a later pace with empirical input.\n- **Graceful degradation is non-negotiable**: the app must start and function if the container is not running. Ann should not be blocked by a missing or crashed container.\n- **Timeout budget**: 30s is a placeholder — adjust based on observed container latency during smoke testing.\n\n## Exit Criteria\n\n- `tt/apcw-t.Test.sh` passes including new `apctc_container` tests. `deny(warnings)` holds.\n- `tt/apcw-fl.FixtureLoad.sh` followed by focus, with the container running, produces:\n  - `$HOME/apcjd/{N}-in.txt` with normalized text\n  - `$HOME/apcjd/{N}.json` from the container (within 30s)\n  - `apcap.log` line: `container output received: stanford=S spacy=C stanza=Z`\n- Same fixture load with the container **stopped** produces a graceful timeout warning in `apcap.log` and normal triage behavior in the UI.\n- `apck-claude-context.md` source modules table includes `apcrc` / `apctc` entries.\n\n## Out of Scope\n\n- Combining rules — deferred to a later pace once empirical data from this integration is available.\n- UI consumption of container findings — container findings are log-only this pace.\n- Decomposing the existing Rust-side `apcrm_match.rs` into five separate scans to match APCS0 — that's a refactoring pace, orthogonal from this integration, can be deferred until combining is spec'd.\n- Performance tuning of the container roundtrip.\n- Anonymized-output (`{N}-out.txt`) awareness of container findings — still derives from Rust-only findings until combining exists.",
.claude/jjm/jjg_gallops.json:4066:              "jjgtn_text": "## Character\n\nMechanical refactor applying quoin sub-letter discipline (CLAUDE.md \"Quoin Sub-Letter Discipline\"; minting memo Pattern J) to APCS0. Sonnet-scale. Legend decisions settled in this docket; pace executes without re-litigating.\n\n## Prerequisite\n\nCLAUDE.md + minting memo + cmk-claude-context.md updates already in place (committed in the work preceding this pace).\n\n## Domain letter assignments\n\n| Letter | Domain | Contains |\n|--------|--------|----------|\n| c | Container | Container meta + container-side discerners (stanford/spacy/stanza) |\n| w | Wire/protocol | Empty initially — populated by wire-protocol-spec pace |\n| d | Discernment | App-side discerners, resources, structure, evidence |\n| n | uNification | Combining, unified_finding, anonymization, placeholder |\n| u | UI | Clipboard, epic_html, clinical_gate, elision_marker, assays |\n| g | Global | Span, phi_category, placement, normalization, finding |\n\n## Sub-letter legends (to be committed in APCS0 mapping-section comment block)\n\n**D1 apcsc_**: c=container (singleton), s=scan (role), f=finding (role)\n\n**D3 apcsd_**: s=scan, f=finding, d=dictionary, w=whitelist, t=table/registry, e=evidence, o=document (o since d taken), l=labeled_field, h=section_header, n=narrative\n\n**D4 apcsn_**: c=combining, u=unified_finding, a=anonymization, t=text (anonymized_text; a taken), p=placeholder\n\n**D5 apcsu_**: c=clipboard_content, e=epic_html, g=clinical_gate (g from gate; c taken), m=elision_marker (m from marker; e taken), d=discerner_assay, u=unified_assay\n\n**D6 apcsg_**: s=span, c=phi_category (c from category; p used for placement), p=placement, n=normalization, t=text (normalized_text; n taken), f=finding\n\n## Quoin rename table (50 quoins)\n\n**D1 Container (7):**\n- container → apcscc_container\n- {stanford,spacy,stanza}_scan → apcscs_{stanford,spacy,stanza}\n- {stanford,spacy,stanza}_finding → apcscf_{stanford,spacy,stanza}\n\n**D3 Discernment (24):**\n- 7 scans: apcsds_{regex,label,surname,firstname,city,english,medical}\n- 7 findings: apcsdf_{regex,label,surname,firstname,city,english,medical}\n- 3 dictionaries: apcsdd_{surname,firstname,city}\n- 2 whitelists: apcsdw_{english,medical}\n- 2 tables: apcsdt_{label_registry,regex_table}\n- evidence → apcsde_evidence\n- document → apcsdo_document\n- labeled_field → apcsdl_labeled_field\n- section_header → apcsdh_section_header\n- narrative → apcsdn_narrative\n\n**D4 uNification (5):**\n- combining → apcsnc_combining\n- unified_finding → apcsnu_unified_finding\n- anonymization → apcsna_anonymization\n- anonymized_text → apcsnt_anonymized_text\n- placeholder → apcsnp_placeholder\n\n**D5 UI (6):**\n- clipboard_content → apcsuc_clipboard_content\n- epic_html → apcsue_epic_html\n- clinical_gate → apcsug_clinical_gate\n- elision_marker → apcsum_elision_marker\n- discerner_assay → apcsud_discerner_assay\n- unified_assay → apcsuu_unified_assay\n\n**D6 Global (6):**\n- span → apcsgs_span\n- phi_category → apcsgc_phi_category\n- placement → apcsgp_placement\n- normalization → apcsgn_normalization\n- normalized_text → apcsgt_normalized_text\n- finding → apcsgf_finding\n\n## Files to update (from scope check)\n\n- `Tools/apck/APCS0-SpecTop.adoc` (the spec itself — 50 quoins, anchors, attribute refs, cross-references)\n- `Tools/apck/APCPS-PrototypeSpecification.md` (28 refs, 19 distinct)\n- `Tools/apck/apck-claude-context.md` (13 refs)\n- `Tools/apck/apcd/container/README.md` (3 refs)\n- `Memos/memo-20260420-apck-container-fixture-assay/memo.md` (1 ref)\n\nNo Rust/bash/Python code changes needed — code uses its own internal names.\n\n## Execution notes\n\n- Subagent `cmsa-normalizer` can accelerate mechanical parts (attribute section, anchor updates, cross-reference sweeps)\n- Run `/cma-normalize` then `/cma-validate` to confirm mapping and link integrity\n- Sanity check: search repo for bare `apcs_` after migration; surviving occurrences should only appear in this docket and the minting memo's Pattern J\n\n## Exit criteria\n\n- All 50 APCS0 quoins follow `apcsXY_word` shape\n- Sub-letter legends committed to APCS0 mapping-section comment blocks (one per domain)\n- All 5 downstream files updated and resolve cleanly\n- `/cma-validate` passes\n- `/cma-normalize` applied\n\n## Out of scope\n\n- Any conceptual changes to quoins (pure rename)\n- Wire-protocol quoins (minted in the next pace)\n- Stale paddock references in `.claude/jjm/` (internal tooling state; not worth touching)",
.claude/jjm/jjg_gallops.json:4073:              "jjgtn_text": "## Character\n\nMechanical refactor applying quoin sub-letter discipline (CLAUDE.md \"Quoin Sub-Letter Discipline\"; minting memo Pattern J) to APCS0. Sonnet-scale. Legend decisions settled in this docket; pace executes without re-litigating.\n\n## Prerequisite\n\nCLAUDE.md + minting memo + cmk-claude-context.md updates already in place (committed in the work preceding this pace).\n\n## Domain letter assignments\n\n| Letter | Domain | Contains |\n|--------|--------|----------|\n| c | Container | Container meta + container-side discerners (stanford/spacy/stanza) |\n| w | Wire/protocol | Empty initially — populated by wire-protocol-spec pace |\n| d | Discernment | App-side discerners, resources, structure, evidence |\n| n | uNification | Combining, unified_finding, anonymization, placeholder |\n| u | UI | Clipboard, epic_html, clinical_gate, elision_marker, assays |\n| g | Global | Span, phi_category, placement, normalization, finding |\n\n## Sub-letter legends (to be committed in APCS0 mapping-section comment block)\n\n**D1 apcsc_**: c=container (singleton), s=scan (role), f=finding (role)\n\n**D3 apcsd_**: s=scan, f=finding, d=dictionary, w=whitelist, t=table/registry, e=evidence, o=document (o since d taken), l=labeled_field, h=section_header, n=narrative\n\n**D4 apcsn_**: c=combining, u=unified_finding, a=anonymization, t=text (anonymized_text; a taken), p=placeholder\n\n**D5 apcsu_**: c=clipboard_content, e=epic_html, g=clinical_gate (g from gate; c taken), m=elision_marker (m from marker; e taken), d=discerner_assay, u=unified_assay\n\n**D6 apcsg_**: s=span, c=phi_category (c from category; p used for placement), p=placement, n=normalization, t=text (normalized_text; n taken), f=finding\n\n## Quoin rename table (50 quoins)\n\n**D1 Container (7):**\n- container → apcscc_container\n- {stanford,spacy,stanza}_scan → apcscs_{stanford,spacy,stanza}\n- {stanford,spacy,stanza}_finding → apcscf_{stanford,spacy,stanza}\n\n**D3 Discernment (24):**\n- 7 scans: apcsds_{regex,label,surname,firstname,city,english,medical}\n- 7 findings: apcsdf_{regex,label,surname,firstname,city,english,medical}\n- 3 dictionaries: apcsdd_{surname,firstname,city}\n- 2 whitelists: apcsdw_{english,medical}\n- 2 tables: apcsdt_{label_registry,regex_table}\n- evidence → apcsde_evidence\n- document → apcsdo_document\n- labeled_field → apcsdl_labeled_field\n- section_header → apcsdh_section_header\n- narrative → apcsdn_narrative\n\n**D4 uNification (5):**\n- combining → apcsnc_combining\n- unified_finding → apcsnu_unified_finding\n- anonymization → apcsna_anonymization\n- anonymized_text → apcsnt_anonymized_text\n- placeholder → apcsnp_placeholder\n\n**D5 UI (6):**\n- clipboard_content → apcsuc_clipboard_content\n- epic_html → apcsue_epic_html\n- clinical_gate → apcsug_clinical_gate\n- elision_marker → apcsum_elision_marker\n- discerner_assay → apcsud_discerner_assay\n- unified_assay → apcsuu_unified_assay\n\n**D6 Global (6):**\n- span → apcsgs_span\n- phi_category → apcsgc_phi_category\n- placement → apcsgp_placement\n- normalization → apcsgn_normalization\n- normalized_text → apcsgt_normalized_text\n- finding → apcsgf_finding\n\n## Files to update (from scope check)\n\n- `Tools/apck/APCS0-SpecTop.adoc` (the spec itself — 50 quoins, anchors, attribute refs, cross-references)\n- `Tools/apck/APCPS-PrototypeSpecification.md` (28 refs, 19 distinct)\n- `Tools/apck/apck-claude-context.md` (13 refs)\n- `Tools/apck/apcd/container/README.md` (3 refs)\n- `Memos/memo-20260420-apck-container-fixture-assay/memo.md` (1 ref)\n\nNo Rust/bash/Python code changes needed — code uses its own internal names.\n\n## Execution notes\n\n- Subagent `cmsa-normalizer` can accelerate mechanical parts (attribute section, anchor updates, cross-reference sweeps)\n- Run `/cma-normalize` then `/cma-validate` to confirm mapping and link integrity\n- Sanity check: search repo for bare `apcs_` after migration; surviving occurrences should only appear in this docket and the minting memo's Pattern J\n\n## Exit criteria\n\n- All 50 APCS0 quoins follow `apcsXY_word` shape\n- Sub-letter legends committed to APCS0 mapping-section comment blocks (one per domain)\n- All 5 downstream files updated and resolve cleanly\n- `/cma-validate` passes\n- `/cma-normalize` applied\n\n## Out of scope\n\n- Any conceptual changes to quoins (pure rename)\n- Wire-protocol quoins (minted in the next pace)\n- Stale paddock references in `.claude/jjm/` (internal tooling state; not worth touching)",
.claude/jjm/jjg_gallops.json:4102:              "jjgtn_text": "## Character\n\nMechanical plumbing. Implement APCS0's wire protocol in Rust and container Python per the spec. Sonnet-scale. The spec is the contract; code complies.\n\n## Prerequisite\n\n`wire-protocol-spec` (₢A9AAd) complete. APCS0 D2 `apcsw_` authoritative; APCPS citing APCS0.\n\n## Scope\n\n### Rust side\n\n**Create `apcd/src/apcrc_container.rs`:**\n- Serde types for each apcsw_ transaction shape (naming per Rust conventions — `ApcrcDiscernerReply`, `ApcrcPingReply`, `ApcrcErrorReply`, etc. — mapping to quoins maintained in module doc)\n- `apcrc_await_output(journal_dir: &Path, index: u64, timeout: Duration) -> Result<DiscernerReply>`: `notify` crate (FSEvents on macOS) watches for `{index}.json`; reads + deserializes on appearance; informative error on timeout or deserialize failure\n- `apcrc_ping(journal_dir: &Path, timeout: Duration) -> Result<PingReply>`: ping request/reply round-trip\n\n**Modify `apcd/src/apcrh_harvest.rs`:**\n- On Clinical branch, write `$HOME/apcjd/{N}-in.txt` containing normalized text (existing whitespace-collapse path)\n- Non-Clinical: no request file\n- Failure logs via `apcrl_error_now!` and continues\n\n**Modify `apcd/src/apcap_main.rs::zapcap_handle_focus`:**\n- After harvest + anonymized-output write, spawn non-blocking task calling `apcrc_await_output(journal_dir, N, timeout)`\n- Success log: `container output received for N: stanford=S spacy_tok=T spacy_ent=E stanza_tok=T stanza_ent=E`\n- Timeout/error: `[WARN] container output not received within {timeout} for index N: {error}`\n- UI must not block on container\n\n**Create `apcd/src/apctc_container.rs` (tests):**\n- Unit tests per discerner variant using fixture JSON strings\n- Golden-file test: copy `Memos/memo-20260420-apck-container-fixture-assay/epic_initial.json` to tempdir; `apcrc_await_output` picks it up; structural assertions (top-level keys, rough counts)\n\n**Wire `apcd/src/lib.rs`:** `pub mod apcrc_container;` + `#[cfg(test)] mod apctc_container;`\n\n### Container side\n\n**Modify `apcd/container/entrypoint.py`:**\n- Dispatch on incoming filename: ping vs discerner-run\n- Ping handler: emit `ping.json` with readiness + per-model status, without running discerners\n- Error reply: on internal failure, emit error-reply JSON (don't silent-fail)\n- Atomic write: `.tmp` → `rename(2)`\n\n### Docs\n\n- `Tools/apck/apck-claude-context.md`: add `apcrc` + `apctc` rows to Source modules table; prefix tree entry\n\n## Timeout rationale\n\n5s reflects empirical sub-1s steady-state per fixture assay memo. Cold-start readiness belongs to ping, not the focus handler. Exact timeout tunable via constant.\n\n## Exit criteria\n\n- `tt/apcw-t.Test.sh` passes; `deny(warnings)` holds\n- Container running: full round-trip `{N}-in.txt` → `{N}.json` within spec timeout\n- Container absent: graceful WARN, UI proceeds\n- Ping works from Rust side\n- Error replies distinguishable from timeouts at the Rust API level\n- Tests cover: happy path, timeout, container internal error, ping success\n\n## Out of scope\n\n- Combining (₢A9AAV)\n- UI consumption of container findings\n- Anonymized-output awareness of container findings\n- Decomposing `apcrm_match.rs` per APCS0",
.claude/jjm/jjp_uAl9.md:42:| Claude Context | Build/run/deploy instructions, file mappings, prefix tree, tabtargets | `Tools/apck/apck-claude-context.md` |
---- buk-claude-context ----
Tools/rbk/vov_veiled/CLAUDE.consumer.md:115:@Tools/buk/buk-claude-context.md
.claude/jjm/jjg_gallops.json:1631:              "jjgtn_text": "## Character\nNew module mint plus per-platform preflight implementation.\n\n## Docket\n\nLocked decisions:\n\n- Mint `Tools/buk/bujp_preflight.sh` (acronym: BUJP) as the seat for all\n  garrison preflights. Three platform entries dispatched from garrison's\n  step-0 by `BURN_PLATFORM`: linux (sudo probe), mac (admin-group + sudo\n  probe), wsl (existing distro-present probe).\n\n- Migrate `zbujb_garrison_w_preflight` from bujb to bujp under the new\n  per-platform naming axis. Behavior unchanged; this pace owns the move.\n\n- Sudo probe: `sudo -ln <command>` per-command, exit code is the answer,\n  wrapped in a single-purpose helper. Command list per platform derived\n  from the actual `sudo -n` callsites currently in bujb_jurisdiction.sh.\n\n- Sudoers posture: `/etc/sudoers.d/` drop-in (filename\n  `rb-garrison-<BURP_PRIVILEGED_USER>`, mode 0440, root-owned). Not a\n  direct `/etc/sudoers` edit. Reuse `BURP_PRIVILEGED_USER` — no new\n  identity introduced.\n\n- Failure recovery: render snippet locally, scp to remote\n  `~/.cache/rb-garrison/<user>.sudoers`, validate with `visudo -cf` over\n  ssh, `buc_die` with a copy-paste-safe `sudo install` line referencing\n  the staged file. Happy path makes no extra ssh round-trips (probe-first,\n  stage only on miss).\n\n- Mac admin-group probe via `dseditgroup -o checkmember -m <user> admin`\n  (read-only, no sudo needed for the probe). On failure, diagnostic names\n  the `dseditgroup -o edit` fix path.\n\n- BUS0 spec: paragraph under §Remote Node Access naming preflight as the\n  precondition layer; BUJP entry in BUS0 mapping section.\n\n- Paddock Standing Note: sudo NOPASSWD for `BURP_PRIVILEGED_USER` on\n  linux/mac, and admin-group membership on mac, are operator-managed\n  preconditions.\n\nDone when:\n- bash -n green on bujp_preflight.sh and bujb_jurisdiction.sh\n- tt/buw-st.BukSelfTest.sh green\n- tt/rbtd-s.TestSuite.fast.sh green at baseline\n- Sudo-missing diagnostic produces a single `sudo install` line\n  referencing a remote-staged file that passes `visudo -cf` (verifiable\n  by inspection)\n- Mac admin-group diagnostic names `dseditgroup -o edit` fix path\n- BUJP entry added to acronym map (CLAUDE.md or buk-claude-context.md)\n- Paddock Standing Note added",
.claude/jjm/jjg_gallops.json:5961:              "jjgtn_text": "## Character\n\nTabtarget-surface refactor. Mechanical except for one judgment call:\nthe colophon name and channel.\n\n## Goals\n\nReplace the imprint-channel `rbtd-s.FixtureCase.<fixture>.sh [case]`\npattern with a single non-imprint, two-param colophon: param1 is the\nfixture name, param2 is the rust test case function name (e.g.\n`rbtdrf_rv_rbrv_all_vessels`). The new tabtarget covers ALL\nmanifest-registered fixtures, not just crucible ones.\n\nThe tabtarget is self-documenting — invocation with missing or invalid\narguments fails with a usage listing instead of an opaque error:\n\n- No params: list all valid param1 values (fixture names from the\n  manifest); exit nonzero.\n- Invalid param1: same — list all valid param1 values; exit nonzero.\n- Valid param1, missing or invalid param2: list valid param2 values\n  (case names registered for that fixture); exit nonzero.\n- Both valid: run the case.\n\n## Wrap criterion\n\n- Single new tabtarget routes by fixture-name lookup against the\n  rbtd manifest; rejects unknown fixture names.\n- Helpful-failure UX: missing/invalid args produce a list of valid\n  values for the offending position and a nonzero exit.\n- Per-fixture FixtureCase.<fixture>.sh files retired.\n- Crucible-fixture single-case semantics preserved: still operates on\n  already-charged state, never charges/quenches.\n- BUK channel discipline honored — the new colophon enrolls under the\n  param1 channel, not imprint; zipper updated; CLAUDE.md tabtarget table\n  reflects the change.\n- Fast suite green; canonical onboarding-sequence cases unchanged.\n\n## Out of scope\n\n- FixtureRun (whole-fixture) tabtargets — unchanged.\n- Charge/quench tabtargets — unchanged.\n- Adding new fixtures or new manifest fields.\n\n## References\n\n- Tools/buk/buk-claude-context.md — channel semantics (imprint vs param1)\n- BBAAz onboarding-sequence-buildout pattern — precedent for\n  manifest-driven dispatch\n- rbtd manifest: rbtd/src/rbtdrm_manifest.rs",
.claude/jjm/jjg_gallops.json:5968:              "jjgtn_text": "## Character\n\nTabtarget-surface refactor. Mechanical except for one judgment call:\nthe colophon name and channel.\n\n## Goals\n\nReplace the imprint-channel `rbtd-s.FixtureCase.<fixture>.sh [case]`\npattern with a single non-imprint, two-param colophon: param1 is the\nfixture name, param2 is the rust test case function name (e.g.\n`rbtdrf_rv_rbrv_all_vessels`). The new tabtarget covers ALL\nmanifest-registered fixtures, not just crucible ones.\n\nThe tabtarget is self-documenting — invocation with missing or invalid\narguments fails with a usage listing instead of an opaque error:\n\n- No params: list all valid param1 values (fixture names from the\n  manifest); exit nonzero.\n- Invalid param1: same — list all valid param1 values; exit nonzero.\n- Valid param1, missing or invalid param2: list valid param2 values\n  (case names registered for that fixture); exit nonzero.\n- Both valid: run the case.\n\n## Wrap criterion\n\n- Single new tabtarget routes by fixture-name lookup against the\n  rbtd manifest; rejects unknown fixture names.\n- Helpful-failure UX: missing/invalid args produce a list of valid\n  values for the offending position and a nonzero exit.\n- Per-fixture FixtureCase.<fixture>.sh files retired.\n- Crucible-fixture single-case semantics preserved: still operates on\n  already-charged state, never charges/quenches.\n- BUK channel discipline honored — the new colophon enrolls under the\n  param1 channel, not imprint; zipper updated; CLAUDE.md tabtarget table\n  reflects the change.\n- Fast suite green; canonical onboarding-sequence cases unchanged.\n\n## Out of scope\n\n- FixtureRun (whole-fixture) tabtargets — unchanged.\n- Charge/quench tabtargets — unchanged.\n- Adding new fixtures or new manifest fields.\n\n## References\n\n- Tools/buk/buk-claude-context.md — channel semantics (imprint vs param1)\n- BBAAz onboarding-sequence-buildout pattern — precedent for\n  manifest-driven dispatch\n- rbtd manifest: rbtd/src/rbtdrm_manifest.rs",
.claude/jjm/jjg_gallops.json:5975:              "jjgtn_text": "## Character\n\nTabtarget-surface refactor. Mechanical except for one judgment call:\nthe colophon name and channel.\n\n## Goals\n\nReplace the imprint-channel `rbtd-s.FixtureCase.<fixture>.sh [case]`\npattern with a single non-imprint, two-param colophon: param1 is the\nfixture name, param2 is the rust test case function name (e.g.\n`rbtdrf_rv_rbrv_all_vessels`). The new tabtarget covers ALL\nmanifest-registered fixtures, not just crucible ones.\n\n## Wrap criterion\n\n- Single new tabtarget routes by fixture-name lookup against the\n  rbtd manifest; rejects unknown fixture names\n- Per-fixture FixtureCase.<fixture>.sh files retired\n- Crucible-fixture single-case semantics preserved: still operates on\n  already-charged state, never charges/quenches\n- BUK channel discipline honored — the new colophon enrolls under the\n  param1 channel, not imprint; zipper updated; CLAUDE.md tabtarget table\n  reflects the change\n- Fast suite green; canonical onboarding-sequence cases unchanged\n\n## Out of scope\n\n- FixtureRun (whole-fixture) tabtargets — unchanged\n- Charge/quench tabtargets — unchanged\n- Adding new fixtures or new manifest fields\n\n## References\n\n- Tools/buk/buk-claude-context.md — channel semantics (imprint vs param1)\n- BBAAz onboarding-sequence-buildout pattern — precedent for\n  manifest-driven dispatch\n- rbtd manifest: rbtd/src/rbtdrm_manifest.rs",
.claude/jjm/jjg_gallops.json:5986:              "jjgtn_text": "## Character\n\nWire-format value rename plus inside-repo path simplification. Mechanical\nsweep across all GAR path producers and consumers; one structural change\n(drop `RBRR_CLOUD_PREFIX` from inside-repo path composition, post depot-\nidentity-collapse). Centralization mechanism (`rbgc_Constants.sh`\nkindle constants → `rbgl_GarLayout.sh` derived roots → all consumers\nvia `${RBGL_*_ROOT}`) already exists; this pace updates the values\nflowing through it and removes the vestigial double-prefix.\n\n## Goals\n\n### Wire-format value rename\n\nReplace the three GAR top-level subdirectory literals with short,\nunique, anchored path tokens. Held centrally in the existing kindle\nmodule; names retained (Option A — value-only change):\n\n```\nRBGC_GAR_CATEGORY_HALLMARKS=\"rbi_hm\"     (was \"hallmarks\")\nRBGC_GAR_CATEGORY_RELIQUARIES=\"rbi_rq\"   (was \"reliquaries\")\nRBGC_GAR_CATEGORY_ENSHRINES=\"rbi_es\"     (was \"enshrines\")\n```\n\nThe names `RBGC_GAR_CATEGORY_*` remain accurate — these ARE the GAR\ntop-level category namespaces; only the wire values change.\n\n### Inside-repo path simplification\n\nDrop `RBRR_CLOUD_PREFIX` from inside-repo path composition. Post\ndepot-identity-collapse (BBAAM), each depot owns its own GAR repo\n(`{prefix}{moniker}-gar`), so prefixing path segments inside that repo\nduplicates information already carried by the repo name. The locator\nformat from BBAAz is already prefix-free; consumers prepend prefix\nonly at use-site for cloud-visible resource names — paths within a\nGAR repo are not cloud-visible names.\n\n`rbgl_GarLayout.sh` roots simplify:\n\n```\nRBGL_HALLMARKS_ROOT=\"${RBGC_GAR_CATEGORY_HALLMARKS}\"     (drop ${RBRR_CLOUD_PREFIX})\nRBGL_RELIQUARIES_ROOT=\"${RBGC_GAR_CATEGORY_RELIQUARIES}\"\nRBGL_ENSHRINES_ROOT=\"${RBGC_GAR_CATEGORY_ENSHRINES}\"\n```\n\nThe `RBRR_CLOUD_PREFIX` precondition check in `zrbgl_kindle` can go.\n\n### Spec corpus\n\nThree AsciiDoc attribute references in RBS0's mapping section, each\nmapping a descriptive key to its literal value:\n\n```\n:gar_hallmarks_namespace:    rbi_hm\n:gar_reliquaries_namespace:  rbi_rq\n:gar_enshrines_namespace:    rbi_es\n```\n\nPlain attribute substitutions, not linked-term quoins — no definition\nsite, no anchor; the value is the concept. RBS0 includes every other\nspec, so attributes are visible corpus-wide.\n\nSpec wire-format use sites: replace literal `enshrines/` with\n`{gar_enshrines_namespace}/` (and similarly for the other two);\nalso drop `«PREFIX»` where it precedes these tokens, since paths\ninside the GAR repo no longer carry the cloud prefix. Conceptual\nprose (\"files held in the enshrines\", \"Total hallmarks: N\") stays\nbare English.\n\n## Wrap criterion\n\n- Three AsciiDoc attribute references emplaced in RBS0's mapping\n  section near the existing `:gar_*:` block.\n- All wire-format occurrences in spec prose use `{attr}/` substitution;\n  conceptual prose left as bare English; `«PREFIX»` removed where it\n  preceded the tokens.\n- Three kindle constants in `rbgc_Constants.sh:133-135` updated to the\n  new `rbi_*` values; names unchanged.\n- Rust constant duplicate `RBTDRC_GAR_CATEGORY_HALLMARKS` in\n  `rbtdrc_crucible.rs:2362` updated to match.\n- `rbgl_GarLayout.sh` roots drop `${RBRR_CLOUD_PREFIX}`; precondition\n  check on `RBRR_CLOUD_PREFIX` removed.\n- Direct-composition sites updated to drop `${RBRR_CLOUD_PREFIX}`:\n  `rbfd_FoundryDirectorBuild.sh:339, 341, 347, 358, 361, 414`;\n  `rbfv_FoundryVerify.sh:290, 716`;\n  `rbfl_FoundryLedger.sh:375, 754`.\n- `.rbk/rbob_compose.yml:23, 57, 77` updated: drop `${RBRR_CLOUD_PREFIX}`\n  AND change literal `hallmarks` to `rbi_hm`.\n- `rbfl_FoundryLedger.sh` operator-facing doc-string examples\n  (lines 329, 399, 415, 732) updated to new tokens (`rbi_hm/H/image:H`,\n  etc.).\n- `rbfl_FoundryLedger.sh:425` (`buc_doc_brief` for abjure showing\n  `hallmarks/<hallmark>/`) and any sibling doc-briefs updated.\n- Migrated populated value: `rbev-vessels/rbev-bottle-ifrit-forge/rbrv.env:41`\n  `enshrines/...` → `rbi_es/...` (lone populated wire-format value\n  repo-wide, verified by sweep).\n- Handbook example display: `rbhoda_director_airgap.sh:167`\n  `RBRV_IMAGE_1_ANCHOR=enshrines/...` → `rbi_es/...`.\n- `RBRR_CLOUD_PREFIX` description in `rbrr_regime.sh:41` reworded to\n  scope it to cloud-visible resource names (depot project, GAR repo,\n  GCS bucket, pool stem) — drop the implicit \"and inside-repo paths.\"\n- Locator parses (the inline `${anchor%:*}` / `${anchor##*:}` pattern\n  used at three sites in BBAAz) operate identically on the new tokens.\n- Listing-block (`----`) audit: confirmed by awk sweep — zero literal\n  occurrences inside listing blocks across the spec corpus. All wire-\n  format use sites are in inline backtick code spans which expand\n  attributes by default.\n- Cascade sites verified: every `${RBGL_*_ROOT}` consumer (rbob_bottle,\n  rbrn_cli, rbfd:650, rbfv:825, rbfl:494/787/833) and every GCB step\n  script receiving roots via JSON substitution (rbgjb*, rbgjm*, rbgje*,\n  rbgjv*, rbgja*) inherits the change automatically.\n- Fast suite green; canonical onboarding-sequence cases unchanged.\n\n## Out of scope\n\n- Code-identifier prefix mintings (`rbie_`, `rbih_`, `rbir_`, `RBI_`)\n  — wire-format only; existing `RBGC_*` prefix retained.\n- Nameplate field shape change (`RBRN_*_HALLMARK` carrying full\n  locator like `RBRV_IMAGE_n_ANCHOR`) — bare-identifier shape is\n  load-bearing for hallmarks, which address multiple arks (image,\n  about, vouch, diags, attest) under one identity. Unlike anchors\n  (single-use base-image references), hallmarks are build identities;\n  storing a locator would force every identifier-needing consumer\n  (drive_hallmark, tabtarget args, propagation, ledger queries,\n  handbook display) to parse it back. See ₣BB conversation memos.\n- `fqin` validator label on `RBRN_*_HALLMARK` (currently misleading —\n  field carries bare identifier, not fully-qualified image name).\n  Validator-typing question; mint a separate pace.\n- Section concept quoins (e.g. `gar_enshrines_section`) — bare English\n  carries the conceptual reference; only wire values are centralized.\n- New tabtarget colophons; the existing `rbw-i*` family is unchanged.\n- Migration of any abandoned-vessel registry artifacts under the old\n  paths — fresh-build assumption (release-qualification operates from\n  marshal-zero, no artifacts to honor).\n\n## References\n\n- BBAAz locator-shape refactor — established the locator parse pattern;\n  this pace swaps the values flowing through that pattern and aligns\n  prefix-application discipline (locators are prefix-free; prefix\n  applies to cloud-visible resource names only).\n- BBAAM depot-identity-collapse — made each depot own its GAR repo,\n  rendering the path-level prefix vestigial.\n- RBS0 mapping section — existing `:gar_*:` precedent for plain\n  attribute references visible across all included specs.\n- Tools/buk/buk-claude-context.md — kindle/tinder convention; new\n  values stay as kindle constants alongside the existing trio (BCG\n  line 363+ — pure literals can be tinder OR kindle; surrounding\n  convention in `rbgc_Constants.sh` puts even pure literals in kindle\n  for `readonly` enforcement).",
.claude/jjm/jjg_gallops.json:5993:              "jjgtn_text": "## Character\n\nWire-format value rename plus inside-repo path simplification. Mechanical\nsweep across all GAR path producers and consumers; one structural change\n(drop `RBRR_CLOUD_PREFIX` from inside-repo path composition, post depot-\nidentity-collapse). Centralization mechanism (`rbgc_Constants.sh`\nkindle constants → `rbgl_GarLayout.sh` derived roots → all consumers\nvia `${RBGL_*_ROOT}`) already exists; this pace updates the values\nflowing through it and removes the vestigial double-prefix.\n\n## Goals\n\n### Wire-format value rename\n\nReplace the three GAR top-level subdirectory literals with short,\nunique, anchored path tokens. Held centrally in the existing kindle\nmodule; names retained (Option A — value-only change):\n\n```\nRBGC_GAR_CATEGORY_HALLMARKS=\"rbi_hm\"     (was \"hallmarks\")\nRBGC_GAR_CATEGORY_RELIQUARIES=\"rbi_rq\"   (was \"reliquaries\")\nRBGC_GAR_CATEGORY_ENSHRINES=\"rbi_es\"     (was \"enshrines\")\n```\n\nThe names `RBGC_GAR_CATEGORY_*` remain accurate — these ARE the GAR\ntop-level category namespaces; only the wire values change.\n\n### Inside-repo path simplification\n\nDrop `RBRR_CLOUD_PREFIX` from inside-repo path composition. Post\ndepot-identity-collapse (BBAAM), each depot owns its own GAR repo\n(`{prefix}{moniker}-gar`), so prefixing path segments inside that repo\nduplicates information already carried by the repo name. The locator\nformat from BBAAz is already prefix-free; consumers prepend prefix\nonly at use-site for cloud-visible resource names — paths within a\nGAR repo are not cloud-visible names.\n\n`rbgl_GarLayout.sh` roots simplify:\n\n```\nRBGL_HALLMARKS_ROOT=\"${RBGC_GAR_CATEGORY_HALLMARKS}\"     (drop ${RBRR_CLOUD_PREFIX})\nRBGL_RELIQUARIES_ROOT=\"${RBGC_GAR_CATEGORY_RELIQUARIES}\"\nRBGL_ENSHRINES_ROOT=\"${RBGC_GAR_CATEGORY_ENSHRINES}\"\n```\n\nThe `RBRR_CLOUD_PREFIX` precondition check in `zrbgl_kindle` can go.\n\n### Spec corpus\n\nThree AsciiDoc attribute references in RBS0's mapping section, each\nmapping a descriptive key to its literal value:\n\n```\n:gar_hallmarks_namespace:    rbi_hm\n:gar_reliquaries_namespace:  rbi_rq\n:gar_enshrines_namespace:    rbi_es\n```\n\nPlain attribute substitutions, not linked-term quoins — no definition\nsite, no anchor; the value is the concept. RBS0 includes every other\nspec, so attributes are visible corpus-wide.\n\nSpec wire-format use sites: replace literal `enshrines/` with\n`{gar_enshrines_namespace}/` (and similarly for the other two);\nalso drop `«PREFIX»` where it precedes these tokens, since paths\ninside the GAR repo no longer carry the cloud prefix. Conceptual\nprose (\"files held in the enshrines\", \"Total hallmarks: N\") stays\nbare English.\n\n## Wrap criterion\n\n- Three AsciiDoc attribute references emplaced in RBS0's mapping\n  section near the existing `:gar_*:` block.\n- All wire-format occurrences in spec prose use `{attr}/` substitution;\n  conceptual prose left as bare English; `«PREFIX»` removed where it\n  preceded the tokens.\n- Three kindle constants in `rbgc_Constants.sh:133-135` updated to the\n  new `rbi_*` values; names unchanged.\n- Rust constant duplicate `RBTDRC_GAR_CATEGORY_HALLMARKS` in\n  `rbtdrc_crucible.rs:2362` updated to match.\n- `rbgl_GarLayout.sh` roots drop `${RBRR_CLOUD_PREFIX}`; precondition\n  check on `RBRR_CLOUD_PREFIX` removed.\n- Direct-composition sites updated to drop `${RBRR_CLOUD_PREFIX}`:\n  `rbfd_FoundryDirectorBuild.sh:339, 341, 347, 358, 361, 414`;\n  `rbfv_FoundryVerify.sh:290, 716`;\n  `rbfl_FoundryLedger.sh:375, 754`.\n- `.rbk/rbob_compose.yml:23, 57, 77` updated: drop `${RBRR_CLOUD_PREFIX}`\n  AND change literal `hallmarks` to `rbi_hm`.\n- `rbfl_FoundryLedger.sh` operator-facing doc-string examples\n  (lines 329, 399, 415, 732) updated to new tokens (`rbi_hm/H/image:H`,\n  etc.).\n- `rbfl_FoundryLedger.sh:425` (`buc_doc_brief` for abjure showing\n  `hallmarks/<hallmark>/`) and any sibling doc-briefs updated.\n- Migrated populated value: `rbev-vessels/rbev-bottle-ifrit-forge/rbrv.env:41`\n  `enshrines/...` → `rbi_es/...` (lone populated wire-format value\n  repo-wide, verified by sweep).\n- Handbook example display: `rbhoda_director_airgap.sh:167`\n  `RBRV_IMAGE_1_ANCHOR=enshrines/...` → `rbi_es/...`.\n- `RBRR_CLOUD_PREFIX` description in `rbrr_regime.sh:41` reworded to\n  scope it to cloud-visible resource names (depot project, GAR repo,\n  GCS bucket, pool stem) — drop the implicit \"and inside-repo paths.\"\n- Locator parses (the inline `${anchor%:*}` / `${anchor##*:}` pattern\n  used at three sites in BBAAz) operate identically on the new tokens.\n- Listing-block (`----`) audit: confirmed by awk sweep — zero literal\n  occurrences inside listing blocks across the spec corpus. All wire-\n  format use sites are in inline backtick code spans which expand\n  attributes by default.\n- Cascade sites verified: every `${RBGL_*_ROOT}` consumer (rbob_bottle,\n  rbrn_cli, rbfd:650, rbfv:825, rbfl:494/787/833) and every GCB step\n  script receiving roots via JSON substitution (rbgjb*, rbgjm*, rbgje*,\n  rbgjv*, rbgja*) inherits the change automatically.\n- Fast suite green; canonical onboarding-sequence cases unchanged.\n\n## Out of scope\n\n- Code-identifier prefix mintings (`rbie_`, `rbih_`, `rbir_`, `RBI_`)\n  — wire-format only; existing `RBGC_*` prefix retained.\n- Nameplate field shape change (`RBRN_*_HALLMARK` carrying full\n  locator like `RBRV_IMAGE_n_ANCHOR`) — bare-identifier shape is\n  load-bearing for hallmarks, which address multiple arks (image,\n  about, vouch, diags, attest) under one identity. Unlike anchors\n  (single-use base-image references), hallmarks are build identities;\n  storing a locator would force every identifier-needing consumer\n  (drive_hallmark, tabtarget args, propagation, ledger queries,\n  handbook display) to parse it back. See ₣BB conversation memos.\n- `fqin` validator label on `RBRN_*_HALLMARK` (currently misleading —\n  field carries bare identifier, not fully-qualified image name).\n  Validator-typing question; mint a separate pace.\n- Section concept quoins (e.g. `gar_enshrines_section`) — bare English\n  carries the conceptual reference; only wire values are centralized.\n- New tabtarget colophons; the existing `rbw-i*` family is unchanged.\n- Migration of any abandoned-vessel registry artifacts under the old\n  paths — fresh-build assumption (release-qualification operates from\n  marshal-zero, no artifacts to honor).\n\n## References\n\n- BBAAz locator-shape refactor — established the locator parse pattern;\n  this pace swaps the values flowing through that pattern and aligns\n  prefix-application discipline (locators are prefix-free; prefix\n  applies to cloud-visible resource names only).\n- BBAAM depot-identity-collapse — made each depot own its GAR repo,\n  rendering the path-level prefix vestigial.\n- RBS0 mapping section — existing `:gar_*:` precedent for plain\n  attribute references visible across all included specs.\n- Tools/buk/buk-claude-context.md — kindle/tinder convention; new\n  values stay as kindle constants alongside the existing trio (BCG\n  line 363+ — pure literals can be tinder OR kindle; surrounding\n  convention in `rbgc_Constants.sh` puts even pure literals in kindle\n  for `readonly` enforcement).",
.claude/jjm/jjg_gallops.json:6000:              "jjgtn_text": "## Character\n\nWire-format value rename plus centralization. Mechanical sweep across\nall GAR path producers and consumers; no semantic change. The\nmechanism — central attribute reference feeding many consumers — is\nthe load-bearing piece.\n\n## Goals\n\nReplace the three GAR top-level subdirectory literals with short,\nunique, anchored path tokens, held centrally:\n\n- `enshrines/`   → `rbi_es/`\n- `hallmarks/`   → `rbi_hm/`\n- `reliquaries/` → `rbi_rq/`\n\n**Spec corpus** — three AsciiDoc attribute references in RBS0's\nmapping section, each mapping a descriptive key to its literal value:\n\n```\n:gar_enshrines_namespace:   rbi_es\n:gar_hallmarks_namespace:   rbi_hm\n:gar_reliquaries_namespace: rbi_rq\n```\n\nThese are plain attribute substitutions, not linked-term quoins —\nno definition site, no anchor; the value is the concept. RBS0\nincludes every other spec, so attributes are visible corpus-wide.\n\nSpec prose: replace literal `enshrines/` with `{gar_enshrines_namespace}/`\n(and similarly for the other two) at every wire-format use site.\nConceptual prose (\"files held in the enshrines\") stays bare English —\nno quoin needed, no attribute reference needed.\n\n**Code** — three named constants in the appropriate kindle/tinder\nmodule, descriptive names with the short values:\n\n```\nRBI_ENSHRINES_GAR_NAMESPACE=\"rbi_es\"\nRBI_HALLMARKS_GAR_NAMESPACE=\"rbi_hm\"\nRBI_RELIQUARIES_GAR_NAMESPACE=\"rbi_rq\"\n```\n\nAll call sites composing GAR locators reference the constants rather\nthan the bare literal strings.\n\n## Wrap criterion\n\n- Three AsciiDoc attribute references emplaced in RBS0's mapping\n  section.\n- All wire-format occurrences in spec prose use `{attr}` substitution;\n  conceptual prose left as bare English.\n- Three named constants emplaced in the appropriate kindle/tinder\n  module; all bash/rust call sites composing GAR locators reference\n  the constants.\n- Locator parses (the inline `${anchor%:*}` / `${anchor##*:}` pattern\n  used at three sites in BBAAz) operate identically on the new tokens.\n- Existing populated value migrated: the lone `enshrines/...` ANCHOR\n  field repo-wide in `rbev-bottle-ifrit-forge/rbrv.env`.\n- Listing-block (`----`) audit: any spec code samples carrying these\n  literals get `subs=\"attributes\"` or a deliberate-literal exception\n  documented.\n- Fast suite green; canonical onboarding-sequence cases unchanged.\n\n## Out of scope\n\n- Code-identifier prefix mintings (`rbie_`, `rbih_`, `rbir_` were\n  considered and rejected — wire-format only).\n- Section concept quoins (e.g. `gar_enshrines_section`) — bare English\n  carries the conceptual reference; only wire values are centralized.\n- New tabtarget colophons; the existing `rbw-i*` family is unchanged.\n- Migration of any abandoned-vessel registry artifacts under the old\n  paths — fresh-build assumption.\n\n## References\n\n- BBAAz locator-shape refactor — locator parsing pattern; this pace\n  swaps the values flowing through that pattern.\n- RBS0 mapping section — existing precedent for attribute references\n  visible across all included specs.\n- Tools/buk/buk-claude-context.md — kindle/tinder convention.",
.claude/jjm/jjg_gallops.json:6007:              "jjgtn_text": "## Character\n\nWire-format value rename. Mechanical sweep across all GAR path\nproducers and consumers; no semantic change.\n\n## Goals\n\nReplace the three GAR top-level subdirectory literals with short,\nunique, anchored path tokens:\n\n- `enshrines/`   → `rbi_es/`\n- `hallmarks/`   → `rbi_hm/`\n- `reliquaries/` → `rbi_rq/`\n\nHold each value in a named constant (kindle or tinder, per call-site\nconvention) — long descriptive name, short value:\n\n- `RBI_ENSHRINES_GAR_NAMESPACE=\"rbi_es\"`\n- `RBI_HALLMARKS_GAR_NAMESPACE=\"rbi_hm\"`\n- `RBI_RELIQUARIES_GAR_NAMESPACE=\"rbi_rq\"`\n\nThe `rbi_` lead anchors the token as recipemuster-image-namespace;\nthe suffix discriminates the three categories. Tabtarget colophons\n(`rbw-i*`) and prefix discipline are unchanged — this is value-space\nonly.\n\n## Wrap criterion\n\n- Three named constants emplaced in the appropriate kindle/tinder\n  module.\n- All call sites composing GAR locators reference the constants\n  rather than the bare literal strings.\n- Locator parses (the inline `${anchor%:*}` / `${anchor##*:}` pattern\n  used at three sites in BBAAz) operate identically on the new tokens.\n- Existing populated values in vessel rbrv.env files migrated\n  (the one ANCHOR field repo-wide carrying `enshrines/...`).\n- Spec sweep: RBS0/RBSAE/RBSAC/RBSAG and any other doc that names the\n  GAR namespace literals reflects the new tokens.\n- Handbook prose using these literals as examples updated.\n- Fast suite green; canonical onboarding-sequence cases unchanged.\n\n## Out of scope\n\n- Code-identifier prefix changes (`rbie_`, `rbih_`, `rbir_` minting\n  was considered and rejected — wire-format only).\n- New tabtarget colophons; the existing `rbw-i*` family is unchanged.\n- Migration of any abandoned-vessel registry artifacts under the old\n  paths — fresh-build assumption.\n\n## References\n\n- BBAAz locator-shape refactor — locator parsing pattern; this pace\n  swaps the values flowing through that pattern.\n- Tools/buk/buk-claude-context.md — kindle/tinder convention.",
.claude/jjm/jjg_gallops.json:7738:              "jjgtn_text": "## Character\n\nMechanical: relocate per-module acronym mappings from project-root `CLAUDE.md` into the existing per-kit `*-claude-context.md` files. Each kit already has a context file `@`-included from CLAUDE.md, so payload is token-neutral — content moves, not duplicates. Blast radius for future per-module acronym mints collapses to domain-local.\n\n## Move\n\nPer-module acronym tables migrate to per-kit context files:\n\n- **RBK** acronyms (~140 lines under `### Tools Directory` / `#### RBK Subdirectory` in CLAUDE.md) → fold into `Tools/rbk/rbk-claude-tabtarget-context.md` as a new top section, OR mint `Tools/rbk/rbk-claude-acronyms.md` if the tabtarget file feels overloaded (mount-time call).\n- **BUK** acronyms → `Tools/buk/buk-claude-context.md` (which already has \"BUK Concepts\").\n- **JJK** acronyms → `Tools/jjk/vov_veiled/jjk-claude-context.md` (already has \"Job Jockey Configuration\").\n- **CMK** acronyms → `Tools/cmk/vov_veiled/cmk-claude-context.md` (already partially holds CMK file acronyms; consolidate, remove duplicates from CLAUDE.md).\n- **GAD** acronyms → CLAUDE.md currently redirects to `Tools/gad/CLAUDE.md`; verify target completeness, remove redirect stub from project-root.\n- **VOK** acronyms (RCG, VLS, VOS0) → fold into VOK context file (verify path at mount-time — context above mentions VOK but not its claude-context location).\n- **CNMP Lenses** acronyms → fold into the appropriate context file in `../cnmp_CellNodeMessagePrototype/`, or leave if cross-repo coordination required (mount-time call — likely defer).\n\n## Keep in CLAUDE.md\n\n- Directory Permissions\n- Project Prefix Registry (top-level prefix→project map)\n- Prefix Naming Discipline, Quoin Sub-Letter Discipline, Minting Workflow, Kit Infrastructure Suffixes\n- Heredoc Delimiter Selection\n- AsciiDoc Linked Terms guidance\n- Rust Build Discipline, Test Execution\n- Working Preferences\n- Common Workflows\n- Design Principles (Load-Bearing Complexity)\n- `@`-includes to kit context files (unchanged)\n\nThe split is: **rules and top-level prefix→project map stay in CLAUDE.md; per-module file listings live with the kit they describe**.\n\n## Verification\n\n- `tt/rbtd-s.TestSuite.fast.sh` green (handbook-render re-renders full handbook through includes — surfaces broken cross-references).\n- Spot-check three acronym lookups (`RBFH`, `BUC`, `MCM`) — each finds its entry in exactly one place across the loaded context.\n- Project Prefix Registry in CLAUDE.md still enumerates every top-level prefix (`rb`, `bu`, `jj`, `mcm`/`axl`, `gad`, etc.).\n- CLAUDE.md line-count drops materially (current acronym table dominates the file).\n\n## Out of scope\n\n- Adding new acronyms not currently in CLAUDE.md.\n- Restructuring kit context files beyond accommodating the moved tables.\n- Touching the GAD/CMK redirect entries if those already work end-to-end (verify-don't-fix).\n- Cross-repo CNMP coordination if it requires touching a sibling repository.\n\n## Silks\n\n`claude-md-acronym-table-explode` — \"explode\" chosen over \"split\"/\"decompose\" because the work is fan-out across many kit files, not bisection. If RBK ends up minting its own dedicated acronyms file rather than folding into tabtarget context, the silks still fits.",
.claude/jjm/jjg_gallops.json:7745:              "jjgtn_text": "## Character\n\nMechanical: relocate per-module acronym mappings from project-root `CLAUDE.md` into the existing per-kit `*-claude-context.md` files. Each kit already has a context file `@`-included from CLAUDE.md, so payload is token-neutral — content moves, not duplicates. Blast radius for future per-module acronym mints collapses to domain-local.\n\n## Move\n\nPer-module acronym tables migrate to per-kit context files:\n\n- **RBK** acronyms (~140 lines under `### Tools Directory` / `#### RBK Subdirectory` in CLAUDE.md) → fold into `Tools/rbk/rbk-claude-tabtarget-context.md` as a new top section, OR mint `Tools/rbk/rbk-claude-acronyms.md` if the tabtarget file feels overloaded (mount-time call).\n- **BUK** acronyms → `Tools/buk/buk-claude-context.md` (which already has \"BUK Concepts\").\n- **JJK** acronyms → `Tools/jjk/vov_veiled/jjk-claude-context.md` (already has \"Job Jockey Configuration\").\n- **CMK** acronyms → `Tools/cmk/vov_veiled/cmk-claude-context.md` (already partially holds CMK file acronyms; consolidate, remove duplicates from CLAUDE.md).\n- **GAD** acronyms → CLAUDE.md currently redirects to `Tools/gad/CLAUDE.md`; verify target completeness, remove redirect stub from project-root.\n- **VOK** acronyms (RCG, VLS, VOS0) → fold into VOK context file (verify path at mount-time — context above mentions VOK but not its claude-context location).\n- **CNMP Lenses** acronyms → fold into the appropriate context file in `../cnmp_CellNodeMessagePrototype/`, or leave if cross-repo coordination required (mount-time call — likely defer).\n\n## Keep in CLAUDE.md\n\n- Directory Permissions\n- Project Prefix Registry (top-level prefix→project map)\n- Prefix Naming Discipline, Quoin Sub-Letter Discipline, Minting Workflow, Kit Infrastructure Suffixes\n- Heredoc Delimiter Selection\n- AsciiDoc Linked Terms guidance\n- Rust Build Discipline, Test Execution\n- Working Preferences\n- Common Workflows\n- Design Principles (Load-Bearing Complexity)\n- `@`-includes to kit context files (unchanged)\n\nThe split is: **rules and top-level prefix→project map stay in CLAUDE.md; per-module file listings live with the kit they describe**.\n\n## Verification\n\n- `tt/rbtd-s.TestSuite.fast.sh` green (handbook-render re-renders full handbook through includes — surfaces broken cross-references).\n- Spot-check three acronym lookups (`RBFH`, `BUC`, `MCM`) — each finds its entry in exactly one place across the loaded context.\n- Project Prefix Registry in CLAUDE.md still enumerates every top-level prefix (`rb`, `bu`, `jj`, `mcm`/`axl`, `gad`, etc.).\n- CLAUDE.md line-count drops materially (current acronym table dominates the file).\n\n## Out of scope\n\n- Adding new acronyms not currently in CLAUDE.md.\n- Restructuring kit context files beyond accommodating the moved tables.\n- Touching the GAD/CMK redirect entries if those already work end-to-end (verify-don't-fix).\n- Cross-repo CNMP coordination if it requires touching a sibling repository.\n\n## Silks\n\n`claude-md-acronym-table-explode` — \"explode\" chosen over \"split\"/\"decompose\" because the work is fan-out across many kit files, not bisection. If RBK ends up minting its own dedicated acronyms file rather than folding into tabtarget context, the silks still fits.",
.claude/jjm/retired/jjh_b260327-r260331-rbk-mvp-5-verb-colorization.md:1920:- `Tools/buk/buk-claude-context.md` — Claude context examples
.claude/jjm/retired/jjh_b260327-r260331-rbk-mvp-5-verb-colorization.md:1956:- `Tools/buk/buk-claude-context.md` — Claude context examples
.claude/jjm/retired/jjh_b260327-r260331-rbk-mvp-5-verb-colorization.md:2162:······x·x········ buk-claude-context.md
.claude/jjm/retired/jjh_b260327-r260331-rbk-mvp-5-verb-colorization.md:2329:Renamed diagnostic/interactive commands: ConnectBottle→Rack (rbw-B→rbw-cr), ConnectSentry→Hail (rbw-S→rbw-ch), ObserveNetworks→Scry (rbw-o→rbw-cs), eliminated ConnectCenser (rbw-C, no diagnostic use case). Shell: rbob_connect_bottle→rbob_rack, rbob_connect_sentry→rbob_hail, rbob_observe→rbob_scry, deleted rbob_connect_pentacle. Zipper: RBZ_CRUCIBLE_HAIL/RACK/SCRY with colophons rbw-ch/cr/cs. git mv 8 tabtargets + git rm ConnectCenser. Consumer docs, BUK docs (BUS0, README, buk-claude-context), current heat examples aligned. Straggler grep clean.
.claude/jjm/retired/jjh_b260327-r260331-rbk-mvp-5-verb-colorization.md:2345:Renamed start→charge and stop→quench across all layers: rbob_start→rbob_charge, rbob_stop→rbob_quench (shell), RBZ_BOTTLE_START→RBZ_CRUCIBLE_CHARGE rbw-cC, RBZ_BOTTLE_STOP→RBZ_CRUCIBLE_QUENCH rbw-cQ (zipper+workbench), git mv 6 tabtargets (3 Start→Charge + 3 Stop→Quench for tadmor/srjcl/pluml), spec linked term display text + definition prose, consumer docs, BUK docs (BCG guide, buk-claude-context, README examples), rbf_Foundry, rbcc_Constants, rbgm_ManualProcedures, rbtb_testbench, RBSAK subdoc. Straggler grep clean.
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:1118:   - `<!-- MANAGED:BUK -->` → `Tools/buk/buk-claude-context.md`
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:1121:2. **Migrate BUK vocabulary** — The "BUK Concepts" section in main CLAUDE.md (colophon, frontispiece, imprint, zipper, workbench, testbench definitions) is BUK knowledge that was manually authored outside the managed section. Move it into `buk-claude-context.md` so BUK owns all its context. This shrinks the main CLAUDE.md and ensures the vocabulary travels with the kit.
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:1125:   @Tools/buk/buk-claude-context.md
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:1153:- BUK vocabulary (colophon, frontispiece, imprint, etc.) appears in buk-claude-context.md, not main CLAUDE.md
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:1168:   - `<!-- MANAGED:BUK -->` → `Tools/buk/buk-claude-context.md`
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:1171:2. **Migrate BUK vocabulary** — The "BUK Concepts" section in main CLAUDE.md (colophon, frontispiece, imprint, zipper, workbench, testbench definitions) is BUK knowledge that was manually authored outside the managed section. Move it into `buk-claude-context.md` so BUK owns all its context. This shrinks the main CLAUDE.md and ensures the vocabulary travels with the kit.
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:1175:   @Tools/buk/buk-claude-context.md
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:1203:- BUK vocabulary (colophon, frontispiece, imprint, etc.) appears in buk-claude-context.md, not main CLAUDE.md
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:1218:   - `<!-- MANAGED:BUK -->` → `Tools/buk/buk-claude-context.md`
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:1223:   @Tools/buk/buk-claude-context.md
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:5224:··········x······························· buk-claude-context.md, cmk-claude-context.md, cmw_workbench.sh, vvk-claude-context.md
.claude/jjm/retired/jjh_b260131-r260601-rbk-mvp-release-finalize.md:5626:Veil forge-only context files (JJK, VVK, CMK), consolidate shared discipline sections (Forbidden Shell Ops, Test Execution, TabTarget System) into buk-claude-context.md as single source of truth, update consumer CLAUDE.md to use @-include for BUK context
.claude/jjm/retired/jjh_b260328-r260331-rbk-mvp-4-tadmor-ifrit-creation.md:2587:·······x············ BUS0-BashUtilitiesSpec.adoc, CLAUDE.consumer.md, README.md, buk-claude-context.md, rbrv_cli.sh, rbw-B.ConnectBottle.nsproto.sh, rbw-B.ConnectBottle.tadmor.sh, rbw-C.ConnectCenser.nsproto.sh, rbw-C.ConnectCenser.tadmor.sh, rbw-S.ConnectSentry.nsproto.sh, rbw-S.ConnectSentry.tadmor.sh, rbw-o.ObserveNetworks.nsproto.sh, rbw-o.ObserveNetworks.tadmor.sh, rbw-s.Start.nsproto.sh, rbw-s.Start.tadmor.sh, rbw-tf.TestFixture.nsproto-security.sh, rbw-tf.TestFixture.tadmor-security.sh, rbw-z.Stop.nsproto.sh, rbw-z.Stop.tadmor.sh
Memos/memo-20260409-system-prompt-capture-opus.md:146:- @Tools/buk/buk-claude-context.md include
Memos/memo-20260409-system-prompt-capture-opus.md:153:Contents of /Users/bhyslop/projects/rbm_alpha_recipemuster/Tools/buk/buk-claude-context.md (project instructions, checked into the codebase):
Memos/memo-20260409-system-prompt-capture-opus.md:243:  3. The @-included context files (buk-claude-context.md, cmk-claude-context.md, rbk-claude-tabtarget-context.md, jjk-claude-context.md, vvk-claude-context.md) were similarly summarized.
.claude/jjm/retired/jjh_b260401-r260409-rbk-theurge-rust-test-infrastructure.md:3939:····································x··· buk-claude-context.md
.claude/jjm/retired/jjh_b260401-r260409-rbk-theurge-rust-test-infrastructure.md:4038:Added Folio column to generated tabtarget context table (buz_emit_context), distinguishing imprint vs param1 channel invocation patterns. Fixes spook where Claude constructed wrong tabtarget paths for param1 colophons (e.g., rbw-cKB.KludgeBottle.tadmor.sh instead of rbw-cKB.KludgeBottle.sh tadmor). Added Folio and Channel to BUK vocabulary in buk-claude-context.md.
