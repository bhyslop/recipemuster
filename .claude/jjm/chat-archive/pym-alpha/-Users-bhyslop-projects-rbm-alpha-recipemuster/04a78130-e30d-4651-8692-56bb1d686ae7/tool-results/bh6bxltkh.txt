4fe8f500 jjb:1011-ca1af15c7:₢BOAAA:n: restore site-specific RBRR fields and increment moniker to canest2bhl100011 for fresh depot levy under the 32-cpu gauntlet shakedown
a7a98cd8 jjb:1011-e702507ed:₢BOAAA:n: marshal-zero defaults RBRR_GCB_MACHINE_TYPE to e2-highcpu-32 — preserves the 32-cpu config across regime resets so the gauntlet shakedown stays exercised
0ee5726f gauntlet: rbw-tP pristine qualification passes clean
c9d199f9 jjb:1011-fde26cba:₣BO:S: gauntlet-32cpu-shakedown
e6e2a2e4 jjb:1011-293dd501:₢BBABJ:n: Mint rbfh BCG CLI partner with rbw-fhc/rbw-fhv tabtargets; add 9-case dockerfile-hygiene theurge fixture wired into fast and gauntlet suites for FROM-line hygiene contract regression coverage
c0a4a351 jjb:1011-4e3c064f:₢BBABI:n: Wire rbfh into rbob_cli.sh — wiring miss surfaced empirically by the post-notch kludge sentry verification. The original BBABI notch added rbfh source + zrbfh_kindle to rbfk_cli.sh and rbfd_cli.sh, but missed rbob_cli.sh which is the actual operator entry point for tt/rbw-cKS.KludgeSentry.sh (routes via rbob_kludge_sentry → rbfk_kludge → rbfh_dockerfile_check). Empirical signature: kludge sentry got past origin-slot auto-pull (debian:bookworm-slim pulled fresh, presence guard cleared), hit the new buc_step 'Validating Dockerfile hygiene', then failed with 'rbfh_dockerfile_check: command not found' at rbfk_kludge.sh:192 — function literal exists in rbfh_FoundryHygiene.sh but the module wasn't sourced in this CLI's furnish so the symbol was unresolved at call time. Same wiring fix applies for the ordain path too (rbob_ordain → rbfd_build → rbfh_dockerfile_check), which would have surfaced on the next pristine gauntlet run; preempted here. Fix shape mirrors the rbfk_cli/rbfd_cli edits from the parent notch — single source line inserted between rbgo_OAuth.sh and rbob_bottle.sh (alphabetical-ish + dependency-before-consumer per BCG); single zrbfh_kindle call inserted before the differential rbfk/rbfd kindle case so both branches inherit hygiene-kindled state. The hygiene kindle is unconditional (not differential) since both kludge AND ordain commands route through callsites that invoke rbfh_dockerfile_check — a discriminator case would be premature factoring. Comment added at the kindle site documents this load-bearing-for-both-paths property. Verification — bash -n clean on rbob_cli.sh; kludge sentry re-run follows this notch under the same dirty-tree guard that exposed the issue (the dirty-tree refusal is itself the proof that my fix has been staged correctly, since the test depends on a committed state). Open: pristine kludge sentry against tadmor itself, which after this commit will run from a clean tree as the empirical confirmation that the hygiene gate fires correctly inline.
73e94f90 jjb:1011-92c2d8ed:₣A_:T: aak-acceptance-tP-macos-linux
040d0a18 jjb:1011-196b696eb:₣BB:n: Repair SA invest keys.list 404 flap: delete the rbgu_poll_until_ok subresource proxy probe and wrap keys.list in retry-on-404 sharing the keys.create RBGC_SA_KEY_CREATE_RETRY_* budget (same underlying physics — subresource visibility after parent creation). Update RBSCIP doctrine, RBSRK + RBSDK step 6 specs, and record transcript-level evidence in Memos/memo-20260514-pristine-gauntlet-iam-flap.md.
b35e58bf jjb:1011-ea949e6f:₢BBABH:W: Made pristine-state kludge actually work end-to-end. Three notches landed, plus this wrap. (1) 6486d74e — origin-slot auto-pull: rbfd_kludge's presence guard now auto-pulls public-upstream origin misses inline with info-level announcement and post-pull docker-image-inspect sanity gate; anchored-slot miss path unchanged (refusal + wrest); RBSAK/RBS0/RBSRV spec coherence sweep. (2) abbd85bf — rbfk module split: empirical test surfaced a parasitic kindle-time gate (zrbfd_kindle's RBDC_DIRECTOR_RBRA_FILE presence check + transitive zrbfv_kindle call) that forced kludge to inherit credentialed-module preconditions despite never sourcing or using the credential. Factored kludge into its own BCG module (rbfk) — minted rbfk_kludge.sh + rbfk_cli.sh, sources only rbfc, minimal kindle. rbob_cli's differential kindle now routes kludge commands to zrbfk_kindle (uncredentialed) while keeping ordain on zrbfd_kindle. RBSAK Limitations parenthetical about the contract violation removed. rbfd description narrowed (kludge no longer listed). (3) ff28ac84 — drop --pull=never: buildx rejected the tri-state value ('strconv.ParseBool: parsing never: invalid syntax'); flag was already broken on modern docker. Removed the flag, updated rbfk_kludge comments + RBSAK Step 6 prose to document the lost hygiene and its current mitigation (presence guard + buildx default no-re-pull). The hygiene gap (no defense against hardcoded FROM bypassing RBF_IMAGE_n parameterization) is documented in the spec and chivvied as ₢BBABI for project-owned Dockerfile syntax constraint + bash hygiene check. Empirical verification: complete docker cache purge (zero images, zero containers, zero build cache), then tt/rbw-cKS.KludgeSentry.sh tadmor on a pristine state (no director RBRA installed) — full sequence green: rbfk kindle (no credential gate fired), slot detection (Image slot 1 pass-through: debian:bookworm-slim), auto-pull (Origin image not cached → pulling from upstream → re-inspect passed), hallmark mint (k260513212837-ff28ac84, ff28ac84 matches commit context), buildkit 11-stage build clean, vouch tag, hallmark drove into .rbk/tadmor/rbrn.env (committed with this wrap). The wrap-time nameplate change is the test's install record; a future operator who purges docker without re-kludging will find tadmor pointing at an absent image — operationally fine, just a recovery note. Fast suite 98/98 (enrollment-validation 47 + regime-validation 27 + regime-smoke 9 + handbook-render 15) re-run after every notch. Departures from original docket — all surfaced empirically: (a) rbfk module split was not in scope but was necessary to reach pristine-state correctness (the docket assumed kludge would just work uncredentialed; the kindle gate blocked it); (b) --pull=never drop was explicitly forbidden by the docket constraint ('--pull=never backstop on the build stays as tag-drift defense') but the constraint was authored assuming classic-builder semantics that buildx no longer provides — the flag was dead-on-arrival; (c) the Dockerfile hygiene rule that would substitute for --pull=never's defense is chivvied as ₢BBABI rather than landed here, capped this pace at empirical correctness. Open BCG debt in rbfk_kludge.sh: inherited multi-var local declarations, two if-not-docker-image-inspect-stderr-suppressed patterns, unbraced ${#z_build_args[@]} test — flagged for transparency, fold-in scheduled in ₢BBABI. Blocked-resolved: ₢BBAAX (next rbw-tP gauntlet run) is now unblocked. Cross-pace note: the rbfk minting (k for Kludge) preserves terminal exclusivity in the rbf family; new acronyms RBFK + RBFK0 fit cleanly alongside RBFC/RBFD/RBFL/RBFR/RBFV.
6486d74e jjb:1011-5f42a13f:₢BBABH:n: Switch rbfd_kludge to auto-pull origin-slot misses inline, splitting the previously-unified presence guard at rbfd_FoundryDirectorBuild.sh:1391-1448 by slot type. Surfaced by ₢BBAAX's first gauntlet run: onboarding-sequence/rbtdro_onboarding_kludge_tadmor failed in pristine state because debian:bookworm-slim (tadmor sentry's only slot, unanchored pass-through) wasn't cached locally — the operator-facing diagnostic prescribed docker pull for every slot, which kludge itself can just do for origin slots without changing the credentialed-out-of-band property of anchored slots. Origin-slot branch at :1417-1426 now announces (buc_info), runs docker pull inline with explicit || buc_die, then re-inspects as a sanity gate before falling through to the build. Anchored-slot miss path unchanged: accumulate locator, refuse after the loop with a wrest remediation block. Removed the now-unreachable z_origin_misses=() array and the pass-through branch of the diagnostic. --pull=never on docker build stays as tag-drift defense (RBSAK Step 6 prose unchanged). Comment block at :1381-1386 rewritten to distinguish the two slot types (anchored: GAR + credentials → wrest out-of-band; origin: public upstream → auto-pull inline). BCG compliance verified: braced+quoted expansions throughout, explicit || buc_die on every new external command (loop-body discipline per BCG 'set -e is Not Sufficient'), docker image inspect re-check matches the existing predicate-style pattern at :1414/:1420, docker pull stderr flows to terminal (not suppressed). Tier A spec sync: RBSAK-ark_kludge top NOTE scoped 'must be pre-cached locally' to anchored layers and added an origin auto-pull clause; Step 5 'Verify Local Image Presence' replaces the unified refusal-with-remediation flow with a two-branch flow (origin auto-pulls inline with re-inspect sanity gate, anchored accumulates for fatal-after-loop refusal) and removes the now-false 'kludge never reaches a registry' claim; Limitations bullet rewrites the cold-cache-path framing (origin-only vessels: one operator step; vessels with anchored slots: two steps — wrest then kludge). Tier B coherence: RBS0-SpecTop kludge stub (lines 1322-1324) scoped the pre-cache requirement to anchored images and added an origin auto-pull clause matching RBSAK; RBSRV-RegimeVessel {rbrv_image_anchor} 'When absent' branch gained a kludge auto-pull mention parallel to the 'When present' coverage on the next line. No edits to RBSAS/RBSCB/RBSDI/RBSIW/RBSOB/RBSIP — survey confirmed no other RBS* file carried the stale 'pre-cache all layers' framing. No handbook edits (explicitly OOS per docket; handbook says nothing about pre-pulling, which becomes accurate again with this change). No tabtarget or fixture edits. Verification: bash -n clean on rbfd_FoundryDirectorBuild.sh; tt/rbtd-b.Build.sh green; tt/rbtd-s.TestSuite.fast.sh 98/98 (enrollment-validation 47 + regime-validation 27 + regime-smoke 9 + handbook-render 15) re-run after both code and spec edits — handbook-render exercises asciidoctor on the RBSAK/RBS0/RBSRV touched specs via their include chain into the rendered handbook surfaces. Empirical end-to-end exercise of the auto-pull path deferred to the next step (docker cache purge + tadmor sentry kludge).
86d8822a jjb:1011-59ed3355:₢BBABF:W: Closed silent semantic gap between IAM-grant propagation classes by recognizing a third class — caller-recently-empowered (HTTP 403 from resource-scope IAM cache lag on a freshly-empowered governor) — alongside the existing two member-visibility classes (HTTP 400 "does not exist" / "is not deleted"). zrbgi_propagation_error_predicate generalized to accept per-site (code, body-glob) tolerance pairs with empty-glob short-circuit semantics; RBGC_PROPAGATION_{INITIAL_DELAY,MAX_DELAY,DEADLINE}_SEC minted as the single shared budget for every IAM-grant retry site (3/20/420 — empirical 7-min ceiling for Google's "typically <2 min" propagation); (403, "") tolerance added to four resource-scope rbgi_* functions (repo, SA, bucket, secret) plus the inline GAR retry in rbgg_invest_director. Project-scope rbgi_add_project_iam_role keeps only the two 400 patterns — governor's roles/owner reaches project-scope IAM caches effectively instantly, so 403 there would slow real-denial diagnosis without absorbing any known race. Class C uses time-bound discrimination rather than body fingerprinting: the bound IS the discriminator (proven model from rbgv_jwt_sa_probe). Tier A specs: RBSCIP three-class taxonomy + constant-referenced backoff profile; RBSCIG new propagation-error catalog table (code/glob/scope/discriminator) + class C prose with 2026-05-13 empirical evidence (fresh-depot AR repo 403 against governor with ~30s-old roles/owner grant); RBSDK AR-step NOTE on class C absorption. Tier B coherence: RBSGM/RBSDE/RBSRK each distinguish project-scope-instant from resource-scope-laggy IAM cache propagation. Per docket, loop-skeleton extraction deferred (bash higher-order is ugly); Tier C drift deferred to ₢BBAAq's running-list mechanism. BCG audit pass on touched code: five `local z_prop_deadline=` upgraded to `local -r`; inline `# shellcheck disable=SC2254` removed (already globally suppressed in busc_shellcheckrc:62); `local -ra z_tolerance=(...)` readonly; comments WHY-only. Verification: bash -n clean ×3, shellcheck delta 0, fast suite 98/98 — handbook-render exercises asciidoctor rendering of all six touched specs. The wrap criterion of one-shot fresh-depot PayorMantle → InvestRetriever → InvestDirector remains operator-verified at next gauntlet run (deferred per ₣BB paddock's fast-suite reservation against ₣A_'s parallel-period burn-in).
dda3bdfb jjb:1011-bd88cebd:₢A_AAb:W: Sentry entry-port DNAT/FORWARD rescope complete. Per-IP exclusion form (PREROUTING per-IP RETURN short-circuit for the two enclave-internal IPs + unconditional DNAT + POSTROUTING MASQUERADE + RBM-FORWARD conntrack-DNAT ACCEPT + ESTABLISHED,RELATED baseline + rp_filter=2 loose) is committed to rbev-vessels/common-sentry-context/rbjs_sentry.sh. Canonical-declaration gate satisfied via cross-platform pristine PASS recorded in commits a30823a8 (macOS, 1h 39m, merge) and 8afa7ff6 (Linux/cerebro, 95m 58s); synthesis line in commit 1743b347. Source tree at HEAD contains the validated per-IP form. Wrap attributes those pristine runs as the canonical evidence for this pace; no fresh gauntlet runs needed. Architecture spec deltas remain queued in ₢A_AAd; structural-presence assertions queued in ₢A_AAn. Anchor memo: Memos/memo-20260512-sentry-entry-port-repair-anchor.md.
8afa7ff6 Linux gauntlet PASS — cerebro host
a30823a8 Merge beta release-qualification: macOS pristine PASS
a1001a4b jjb:1011-593b69ef:₢A_AAb:n: Capture empirical cross-platform validation of the per-IP exclusion form. Both macOS Docker Desktop 28.x and Linux Docker Engine 28.x report three-fixture parallel green: srjcl 3/3 (including the prior-regression target rbtdrc_srjcl_jupyter_connectivity on Linux), pluml 5/5, tadmor 54/54 each including the decisive security cases (direct_sentry_probe, net_dnat_entry_reflection, net_srcip_spoof). Cerebro provided mechanism-level confirmation: DNAT counter incremented to 1 packet under per-IP form against the same host where whole-CIDR showed 0 packets across 20 SYN retransmits; source IP flow traced as 10.242.2.1 (host bridge gateway) → sentry PREROUTING → DNAT to 10.242.2.3:8000 → POSTROUTING MASQUERADE rewrites source to 10.242.2.2 (sentry's enclave IP) → bottle sees enclave-internal peer. Return-path symmetry working exactly as the architectural argument predicted. Memo updates: (1) TL;DR empirical status section restructured to show cross-platform green under per-IP form with mechanism-level confirmation; (2) Reset handoff notes updated from 'in-flight hypothesis' framing to 'post-empirical-validation' framing; first move on cold mount is now 'verify per-IP RETURN pattern is in place' rather than 'implement per-IP'; what done looks like simplifies to 'pristine gauntlet on both platforms'; anti-patterns retained; new operational note added about cerebro's stale-hallmark workflow observation (not architectural, but worth flagging); (3) Empirical validation table dual-column format updated: per-IP column now shows green on both platforms with mechanism details; whole-CIDR column shows the historical state for lineage; (4) Lessons captured Attempt 6 marked 'empirically validated cross-platform at three-fixture level' with mechanism-level confirmation note; pristine gauntlet remains pending for canonical declaration; (5) Open questions: items 2 and 3 (Linux verification, macOS re-verification) marked resolved (green); new item 3a added for pristine gauntlet as the remaining canonical-declaration gate. Architecture is now empirically validated; pristine gauntlet is hardening margin for canonical pass. The work has crossed the threshold from 'hypothesis with empirical evidence' to 'empirically validated architecture'.
b4cabdd2 jjb:1011-b1c6dcb5:₢A_AAb:n: Add 'Reset handoff notes' subsection to the anchor memo positioned right after TL;DR. This is reset-readiness prep — if the architect chat resets while the per-IP-exclusion hypothesis is still empirically pending, a fresh-cold agent reading the memo can pick up the in-flight state cleanly. The subsection covers: where we are right now (source tree has prior whole-CIDR form; memo Fix Recipe has per-IP hypothesis; the divergence is by design); first-move discipline on cold mount (verify divergence, read Fix Recipe, run next experiment); the bounded next experiment (per-IP implementation, kludge + refresh, cross-platform three-fixture parallel); what 'done' looks like (both platforms green under per-IP form + full pristine gauntlet for canonical); iteration discipline if the experiment surfaces another structural surprise (process result, update memo, iterate within the architecture); and four explicit anti-patterns (do not simplify, do not skip cross-platform, do not abandon architecture, do not change source tree before committing per-IP form). Pairs with the parallel jjx_redocket update to ₢A_AAb's docket that surfaces the same in-flight state at navigation level. Together, the docket update + memo subsection give a cold-mount agent the orientation needed to execute the bounded next step without losing the iteration context this chat built up.
977456ae jjb:1011-05a9787b:₢A_AAb:n: Correct the memo's experiment protocol in response to coding-agent clarifying questions. (1) Port mapping was wrong: said the directive value was 'unchanged' (7999:7999), but without sentry's PREROUTING DNAT translating 7999→8000(/8080), the publish itself must translate. Corrected to asymmetric form `"${RBRN_ENTRY_PORT_WORKSTATION}:${RBRN_ENTRY_PORT_ENCLAVE}"` — for srjcl `7999:8000`, for pluml `7999:8080`. (2) Added new Step 3: lift `ip_forward=1` enablement out of the entry-port block in rbjs_sentry.sh to an unconditional earlier line, so kernel-forward state is independent of which access-mode iptables block runs. The current entry-port block at line 121-122 enables ip_forward as a side-effect; deletion leaves pluml (access_mode=disabled) with ip_forward=0 because pluml's access-mode block doesn't touch ip_forward. Lifting normalizes kernel state across access modes; functional behavior unchanged (pluml outbound remains iptables-blocked). (3) Expanded hallmark refresh from 'srjcl and pluml' to 'every nameplate exercised in the test iteration' — sentry vessel is shared across srjcl/pluml/tadmor/moriah/ccyolo, so any unrefreshed nameplate in the iteration runs against the OLD sentry image and its pass/fail validates the old image rather than the new one. For the three-fixture iteration (srjcl + pluml + tadmor), refresh all three. (4) Split verification into iteration-cycle (three-fixture, early signal) and canonical-cycle (full pristine gauntlet on both platforms, declaration gate). Memo principle and topology reframe conclusion unchanged; this is housekeeping on the experiment protocol following coding-agent triage.
9eb7588d jjb:1011-28d1411f:₢A_AAP:n: Capture the srjcl jupyter-connectivity regression diagnosis surfaced by the AAP gauntlet acceptance run. The memo records: symptom (HTTP 000 from curl localhost:7999/lab against pristine-conjured srjcl crucible); root cause (Docker port-publishes via alphabetically-first attached network, which is the enclave — packets arrive on eth0, but rbjs_sentry.sh:124-126 scopes the PREROUTING DNAT to eth1 via ${RBJ_UPLINK_IF}, so DNAT matches 0 packets); lineage (₢BBABC commit 6d836814 renamed sentry's services.networks.default → transit which removed the load-bearing 'default' magic that made compose pick the right gateway and port-publish target by accident; ₢BBABE commit ed898867 caught and fixed the default-route half of this same fragility, but the port-publish half was missed); cross-platform empirical confirmation (linux Docker engine 28.x and macOS Docker Desktop 28.x, both against branch tip 28d1411f); macOS-specific second-order blocker (after relaxing the DNAT scope, RBM-FORWARD's eth1-ingress/eth0-egress ACCEPT still drops the hairpinned post-DNAT path — linux's parallel investigation reported HTTP 200 from DNAT-half alone but the macOS evidence shows the FORWARD half is empirically necessary too); fix shape (sentry-side, both halves, drop ${RBJ_UPLINK_IF} from both PREROUTING DNAT at line 125 and RBM-FORWARD ACCEPT at line 129; consistent with BBABE's 'sentry takes ownership of its networking' precedent; requires sentry vessel re-kludge + nameplate hallmark refresh on srjcl and pluml since the sentry image is shared); sibling risk (pluml shares the sentry image and entry-mode-enabled posture — same bug will surface there); files involved (rbjs_sentry.sh lines 124-130, rbob_compose.yml lines 32-37, rbtdrc_crucible.rs line 1620); verification gate (rbtdrc_srjcl_jupyter_connectivity green via full pristine gauntlet on BOTH platforms, non-negotiable). The memo is structured to be self-contained for a clean-chat mount in a separate pace — it points at sources rather than restating them where possible, but captures enough empirical detail (per-interface PREROUTING log counters, both platforms' DNAT outputs, FORWARD chain dump) to support implementation without re-probing the running crucibles.
28d1411f jjb:1011-ecff6d9b:₢A_AAP:n: Bump srjcl's RBRN_BOTTLE_READINESS_DELAY_SEC from 30 to 120. Surfaced during the AAP gauntlet acceptance run: pristine-lifecycle reached srjcl after 54/54 tadmor cases passed, but rbtdrc_srjcl_jupyter_connectivity got HTTP 000 (curl couldn't establish TCP) against a freshly-conjured Jupyter image. Re-running the fixture in isolation reproduced HTTP 000 every time. The jupyter process was up inside the bottle (ps aux confirmed at +1min into the container's lifetime, bound to 0.0.0.0:8000), and the 30s readiness delay did execute per the rbob_charge log line 'Waiting 30s for bottle service readiness' — but a freshly-conjured Jupyter image takes longer than 30s to bind its HTTP listener. Triage class: grammar — the value introduced in ₢BBAAt (commit 0a850d57) was calibrated against cached/warm Jupyter images and is insufficient for the pristine-cutover path the gauntlet exercises. 120s is the operator-chosen value from the 60/90/120 menu, providing strong margin for the cold-conjure path. RBRN_BOTTLE_READINESS_DELAY_SEC survives marshal-zero (which only blanks hallmark fields), so this value persists across the next depot regen cycle. The charge tabtarget refused to proceed with the file uncommitted ('Nameplate has uncommitted changes: .rbk/srjcl/rbrn.env — commit before charging'), forcing this commit before fixture re-run. Pluml carries the same 30s value and will need a parallel bump if its connectivity case fails the same way later in the gauntlet.
667ef2cf jjb:1011-a637fc06:₢A_AAP:n: Ignore .claude/scheduled_tasks.lock — runtime artifact from Claude Code's ScheduleWakeup machinery, parallel to the existing .claude/settings.local.json and .claude/settings.json entries. Surfaced when rblm_zero's working-tree-clean precheck refused to proceed at the start of the AAP gauntlet acceptance run because the file was untracked.
bfd52c82 jjb:1011-44837042:₢BBAAy:W: Collapsed rbtdre_Section into a flat cases: &[Case] field on rbtdre_Fixture. The Section struct, its name field, the rbtdre_run_sections function, and the per-section header printout are gone; rbtdre_run_cases now iterates one ordered list and emits only per-case PASSED/FAILED/SKIPPED. Step 1 (drop kebab from user-facing case output) folded into Step 2 — the only kebab surface in case output was section names, which vanish with the Section struct; case!() macro already emitted function names. Every fixture-defining module (rbtdrf_fast, rbtdrf_handbook, rbtdrp_pristine, rbtdro_onboarding, rbtdrk_canonical, rbtdrl_calibrant, rbtdrc_crucible) replaced its RBTDR*_SECTIONS_* statics with flat RBTDR*_CASES_* arrays preserving declaration order across the former section boundaries. calibrant-fail-fast lost the artificial 2-section split: 4 cases → 3 by dropping rbtdrl_failfast_not_reached_inter (equivalent to _intra once sections vanish); diagnostic needle shifted from 'intra-section' to 'fail trigger'. Engine self-tests in rbtdte_engine.rs dropped two now-redundant section-specific tests (fail_fast_stops_across_sections, multiple_sections_run_sequentially) with the ordering invariant preserved as rbtdte_cases_run_in_declaration_order; rbtdte_zero_sections + rbtdte_section_with_zero_cases collapsed to rbtdte_zero_cases. Test-side renames *_sections_registered → *_cases_registered across rbtdtk_canonical/rbtdto_onboarding/rbtdtl_calibrant, reading fix.cases directly. main.rs single-case dispatch now reads fixture_def.cases. colors.white disappeared with the section header. Theurge-ifrit context doc retitled 'Test Sections' → 'Test Cases', section table converted to purpose-grouping bullets, 'Adding a New Test' guidance points at RBTDRC_CASES_SECURITY. Verified: tt/rbtd-b.Build.sh clean, tt/rbtd-t.Test.sh 94/94 pass, tt/rbtd-s.TestSuite.fast.sh 98/98 across enrollment-validation/regime-validation/regime-smoke/handbook-render with operator output now showing pure function names (rbtdrf_ev_string_valid etc.). Crucible fixtures (tadmor/moriah/srjcl/pluml) and credentialed fixtures (hallmark-lifecycle, batch-vouch, access-probe, pristine-lifecycle, canonical-establish, onboarding-sequence) compile and pass unit-level case_registered tests, but not exercised at runtime in this pace — that surfaces under BBAAX's full rbw-tP run. Notch landed at 44837042 with size_limit=80000 (72,118 bytes, mechanically-large refactor touching 14 files with no surprise bulk).
10497fb4 jjb:1011-3a3ad5f2:₣A_:T: aak-acceptance-tP-macos-linux
1d10105a jjb:1011-9f39fcdd:₢BBABB:W: BURS_TINCTURE per-station tincture landed under heat-affiliated commit b2ffca63 (deferred from pace-affiliation pending BBABA collision-awareness reslate, which has since landed). Verification on this officium: theurge unit suite 97/97 (including both wrap-criterion tests rbtdtp_pristine_dual_station_disjoint and rbtdtk_canonical_dual_station_disjoint covering disjoint project_id, GAR repo, GCS bucket, and SA email across two distinct tinctures with 30-char GCP project-ID cap assertion); fast suite 98/98 across 4 fixtures. Mount-time open question resolved: BURS_TINCTURE is required (1-3 char lowercase alphanumeric leading-letter, validated at burs_regime.sh:42,66-67). Production code unchanged per docket — only burs_regime.sh, BUS0, rbrr_regime.sh comment-only validator update, and theurge fixture composers/tests touched. BBABA's docket already references BBABB landing as a satisfied cross-pace dependency; the parallel-station property is now first-class for BBABA's dual-machine rbw-tP execution.
7124cec7 jjb:1011-66aa5e5d:₣A-:n: Add §Supported Platforms qualifying release-1 against native Docker on Linux, Docker Desktop on Windows (WSL2 backend with per-distro integration, single-daemon), and Docker Desktop on Mac (Apple Virtualization or Docker VMM). Aligns with RBS0 §at_runtime stance (Docker today, Podman deferred); points readers at §Future Work for the Podman accommodation. Linux qualification verified by inspecting cerebro (Ubuntu 24.04.2 LTS, kernel 6.8.0-107, Docker 28.1.1, cgroup v2/systemd) currently running rbtd-s.TestSuite.gauntlet.sh — the release-gate suite consumed by ₣BB rbk-mvp-3-release-qualification. Inserted as H3 inside §Environment after the two-capability summary so the qualification appears before any setup-flow mentions; complements paddock ₣A- §Standing Notes Docker Desktop entry landed in commit 66aa5e5d.
b2ffca63 jjb:1011-1ae8b6c2:₣BB:n: Add BURS_TINCTURE per-station tincture (1-3 char lowercase alphanumeric, leading letter, no hyphen, required) to BURS schema; pristine and canonical theurge fixtures compose it into both the throwaway/canonical RBRR cloud and runtime prefixes AND the family stem used for moniker construction, so parallel rbw-tP runs from different stations land in disjoint depot project IDs, GAR repos, GCS buckets, and SA emails on a shared payor manor. Fixture-side mechanism: const RBTDRP_THROWAWAY_CLOUD_PREFIX='prlc-' (and runtime/canonical/family-stem siblings) split into _BASE consts (no tincture, no trailing hyphen) plus composer fns rbtdr{p,k}_{throwaway,canonical}_{cloud,runtime}_prefix(t) / rbtdr{p,k}_family_stem{,_arc}(t); install/probe/moniker-pick sites call the composer with tincture read from BURD_STATION_FILE via rbtdr{p,k}_burs_tincture() (parses station-file BURS_TINCTURE; BURS validation upstream guarantees format). Director/Retriever SA names left unchanged per docket out-of-scope — their emails inherit project-id disjointness once depots disjoint, so 'pristl-ret' and 'canest-ret' SA names are stable across stations. RBRR joint-length validator updated comment-only (no arithmetic change) acknowledging tincture composition behavior; the validator does NOT cross-reference BURS_TINCTURE because BURS isn't kindled in every RBRR-enforce context (rbrr_cli furnish doesn't source burs_regime), so reaching ${BURS_TINCTURE} would trip set -u. Fixture bases are short (≤7 chars: prlc/prlr/canc/canr=4, pristl=6, canest2=7) so tinctured composed values (≤25-26 chars) stay well under the 30-char GCP project-ID cap. Verification: 5 new Rust unit tests covering composer shape, per-tincture disjointness, and dual-station dry-run of full RBDC composition (mirrors rbdc_DerivedConstants.sh:84-87 inline) for project_id, GAR repo, bucket, SA email — all pass; theurge unit suite green at 97/97; fast suite green at 98/98 (regression). BUS0 spec gains :burs_tincture: linked term with axtu_string motif and constraint description. Station file (../station-files/burs.env, outside repo) updated separately with BURS_TINCTURE=bhm per operator direction; existing canonical rbrr.env values (canest2100007 from pre-tincture run) will be replaced on next canonical fixture run with tinctured form. Heat-affiliated rather than pace-affiliated since wrap is deferred pending BBABA collision-awareness reslate decision.
685d58ec jjb:1011-5d64410e:₢BBAAX:n: Add missing per-nameplate compose fragment for moriah — second BBAAX surface uncovered by the auto-summon repair (with charge now bootstrapping correctly, the next layer of broken config emerged: 'exec /usr/local/bin/rbid: operation not permitted' on case 3 of moriah's ifrit-attacks section). Diagnosis chain: arch hypothesis ruled out (both ifrit-bottle images are linux/arm64 matching the apple-silicon host); binary inspect identical (794072 bytes, -rwxr-xr-x, file cap cap_net_raw=ep); reproduction in a fresh container with --cap-drop=NET_RAW reproduced the EPERM for both tadmor's AND moriah's binaries; the asymmetry was external — tadmor has .rbk/tadmor/compose.yml with 'cap_add: NET_RAW' that undoes rbob_compose.yml:82-83's 'cap_drop: NET_RAW' on the bottle service. Without that override the file cap can't promote into the effective set (kernel refuses execve with EPERM since file caps cannot exceed the bounding set). Git history confirms .rbk/moriah/compose.yml has never existed (vs .rbk/tadmor/compose.yml landed in A6AAD/AUAAv) — long-standing latent gap that was reachable only after BBAAX's auto-summon repair let charge succeed. Moriah's compose fragment mirrors tadmor's: same user/working_dir/env_file/environment/cap_add/volumes (ifrit bottle, same rbid binary, same Python attack scripts mounted from Tools/rbk/rbtid), with header noting the airgap variant and env_file path swapped to moriah/rbrn.env. srjcl and pluml not addressed — they're non-ifrit bottles (jupyter, plantuml respectively), don't run rbid, and may not need any compose override; if their fixtures fail later they get separate diagnosis. Pace-affiliated to BBAAX since this is the same surface (rbw-tP first-run uncovering broken config that was hidden behind charge-fail) — tiny inline (single file, mirror with one path swap) rather than structural follow-up.
5d64410e jjb:1011-bb615427:₣BB:S: rbw-tP-branched-rerun
854eda41 jjb:1011-b8a6d184:₣BB:n: Capture spook surfaced during BBAAX first-run: add 'TabTarget Invocation Discipline' section to buk-claude-context.md, parallel to the existing 'Forbidden Shell Operations' (cd) rule. Forbids wrapping tabtarget invocations with tee/tail/head/grep/2>&1/any pipe, since tabtargets self-log to ../logs-buk/ AND zsh defaults pipefail OFF (so a 'tabtarget | tail' pipeline returns tail's exit code of 0 even when the tabtarget failed — a failing test reports as success). Section names the three log files (last.txt, same-{cmd}.txt, hist-{cmd}-{ts}.txt), gives wrong/right examples, and provides escape hatch (set -o pipefail or PIPESTATUS) for rare cases that need live filtering. Heat-affiliated rather than pace-affiliated since the rule benefits future Claude sessions broadly, not just BBAAX. Spook caught during BBAAX first-run when the agent's 'rbw-tP 2>&1 | tee /tmp/log | tail -80' invocation reported exit 0 despite moriah fixture failing — the failure was correctly caught by set -e through rbte_suite → rbq_qualify_pristine → rbw-tP, but the bash pipeline ate the non-zero exit.
9d15fff9 jjb:1011-121e73e8:₢BBAAX:n: Insert rbgu_poll_until_ok probe before LIST-keys preflight in zrbgg_create_service_account_with_key. Second BBAAX gauntlet attempt failed at rbtdrk_retriever_invest with HTTP 404 'Service account does not exist' on the LIST keys preflight (rbgg_Governor.sh:196-202), immediately after a successful SA create returning a uid. The post-create rbgu_poll_until_ok at line 193-194 confirmed the SA was visible at the by-email GET endpoint, but the keys subresource has its own eventual-consistency lag relative to the SA itself — already known and mitigated for keys.create at line 240-243 with retry-on-404, but the LIST-keys preflight one step earlier was single-shot. Fix is the two-call shape: rbgu_poll_until_ok against the keys subpath URL absorbs the propagation delay, then the existing rbgu_http_json + rbgu_http_require_ok pair captures the canonical response into ZRBGG_INFIX_LIST_KEYS for downstream USER_MANAGED counting at line 209-210. New ZRBGG_INFIX_KEYS_PROBE='keys_probe' constant added next to ZRBGG_INFIX_LIST_KEYS to avoid temp-file basename collision with ZRBGG_INFIX_VERIFY (which the by-email probe at line 194 consumes). Single-point-of-fix covers both invest paths — rbgg_invest_retriever and rbgg_invest_director both route through zrbgg_create_service_account_with_key. Operator question 'use existing helper or bespoke amendment' answered: rbgu_poll_until_ok is the right helper, already used parallel-shape at line 194 for the SA-by-email probe; bespoke retry-on-404 at keys.create is necessarily inline because writes can't safely poll-until-OK (idempotency would mint N keys on retry), but reads are exactly the helper's contract. Asymmetry observed: rbfc_FoundryCore.sh poll loop tolerates curl rc=28 timeouts with 3-consecutive policy, rbgu_poll_until_ok tolerates GET propagation 404s, but rbfl_FoundryLedger.sh wrest's docker login is single-shot — the broader transient-tolerance survey is slated as ₢BBAA8 docker-login-retry-shape for design conversation per operator's pre-implementation-discussion constraint. This commit is a tactical inline application of the existing helper to close one specific call-site asymmetry, not a structural change. shellcheck -S error clean. Validation deferred to clean-chat BBAAX retry per operator instruction.
d7714913 jjb:1011-27d43888:₢BBAA7:W: Dissolved four-mode fixture (4 cases: conjure/bind/graft/kludge) into hallmark-lifecycle (1 load-bearing case: ordain → audit baseline-diff → rekon all 5 basenames → abjure → post-abjure rekon Unix-exit assertion → final audit baseline restored). Absorbed verification tails into onboarding-sequence ordain_conjure (wrest+summon+inspect-3-arks+plumb_full+rekon-4-basenames+rmi), ordain_bind (wrest+plumb_compact+jettison-pouch+rekon-after+rmi), and ordain_graft (source-layer-capture+wrest+wrested-layer-capture+DiffID-compare+rmi+abjure). Added airgap-mode+empty-ANCHOR fail-fast to enshrine-preflight loop in rbfd_FoundryDirectorBuild.sh, modeled on the existing 404-path remediation diagnostic. rbev-busybox switched airgap→tether (correct shape for vessel without enshrined base). audit-hallmarks gained fact-multi emission via buf_write_fact_multi (new RBCC_fact_ext_audit_hallmark) and a Rust enumerator rbtdri_read_burv_facts_multi — stdout parsing eliminated in favor of the machine-contract path. Renamed across 3 suite arrays (SERVICE/COMPLETE/GAUNTLET) and 2 tabtargets (rbtd-r/rbtd-s.{four-mode → hallmark-lifecycle}.sh). Live-validated end-to-end on canc-d-canest2100004: hallmark-lifecycle ran cloud-built busybox ordain (~1m33s build), exercised the full 8-step round-trip, baseline restored to 8 hallmarks (one stranded from earlier debug iteration available for operator cleanup via rbw-fA c260504140304-r260504210308). Five debug commits captured the iteration arc (vessel config flip, fact-multi pivot, exit-status-only assertion). Out-of-scope deferred per Mid-execution posture: paddock update at jjp_uBuB.md lines 67/91 (still names 'four-mode'); full gauntlet rerun on release branch from marshal-zero (operator-initiated per pristine-tier contract — BBAAX is the verifier per cross-pace deps).
