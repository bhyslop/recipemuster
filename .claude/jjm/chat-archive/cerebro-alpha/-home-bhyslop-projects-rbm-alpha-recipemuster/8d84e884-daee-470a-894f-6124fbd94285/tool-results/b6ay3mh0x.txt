3d5a2c847 jjb:1011-66814134:₢BBABI:n: Mint rbfh_FoundryHygiene library module, wire it as a precondition into both kludge (rbfk_kludge) and conjure (rbfd_build) paths, fold BCG cleanups that BBABH accumulated in rbfk_kludge, remediate the rbev-bottle-anthropic-jupyter vessel that bypassed the RBF_IMAGE_n parameterization, mint the RBSFH spec subdoc as the canonical rule home, and synchronize RBSAK Step 6 + RBSAC Step 1 + CLAUDE.md acronym mappings. Mechanics — new Tools/rbk/rbfh_FoundryHygiene.sh (no CLI per docket; CLI deferred to BBABJ for the fixture coverage work): minimal BCG library with ZRBFH_SOURCED guard, zrbfh_kindle/sentinel pair, public rbfh_dockerfile_check(path) that reads the file line-by-line, filters column-0 # comments, matches column-0 FROM lines via [[ =~ ^FROM[[:space:]] ]], and applies three case-pattern rules with explicit buc_die emitting {path}:{lineno}: {rule} — {offending line} precision; rule 1 catches any tab character on a FROM line, rule 2 verifies the second whitespace-delimited token (read -r z_pre z_token2 z_post <<<"${z_line}") is exactly ${RBF_IMAGE_1}, ${RBF_IMAGE_2}, ${RBF_IMAGE_3}, or scratch (lowercase), rule 3 refuses lines ending in `\` literal — multi-stage AS <name> codas after token 2 are admissible by design (rule constrains token 2 only), empty Dockerfiles and comment-only files pass silently per docket. CLI furnishings: both rbfk_cli.sh and rbfd_cli.sh now source rbfh_FoundryHygiene.sh (inserted between rbgu_Utility.sh and the module-specific source) and invoke zrbfh_kindle in their kindle chains; minimal kindle has no constant or scratch-file dependencies, so the addition is purely additive ordering. Callsite wiring — rbfk_kludge.sh: rbfh_dockerfile_check invoked after the anchored-misses guard completes and before the hallmark mint, in a buc_step "Validating Dockerfile hygiene" block at the natural conceptual position (post slot-resolution, pre docker-build). rbfd_FoundryDirectorBuild.sh: identical pattern inserted immediately after the existing test -d "${RBRV_CONJURE_BLDCONTEXT}" validation in rbfd_build (line 1198 → 1200 gap), before buc_info "Building vessel image" — fail-fast before any GAR/credential/Cloud Build cost incurs on a malformed Dockerfile. BCG cleanups in rbfk_kludge.sh (BBABH debt fold-in): (a) ZRBFK_INSPECT_STDERR_PREFIX readonly kindle constant defined as "${BURD_TEMP_DIR}/rbfk_inspect_stderr_" plus mutable z_rbfk_inspect_counter=0 mutable kindle state in zrbfk_kindle; (b) new zrbfk_image_present_predicate sentinel'd wrapper around docker image inspect that increments the counter, routes stderr to ${ZRBFK_INSPECT_STDERR_PREFIX}${counter} forensic temp file, and returns docker's exit status — three previous "docker image inspect ... 2>/dev/null" sites (anchored-miss check, origin-miss check, origin re-inspect after pull) all routed through the predicate; (c) ten multi-var local declarations from the slot-resolution loop split one-per-line (z_slot, z_origin_var, z_anchor_var, z_origin, z_anchor, z_slot_ref, z_pkg_path, z_tag, z_miss, all separate local statements); (d) two `test ${#array[@]} -gt 0` sites converted to `(( ${#array[@]} ))` arithmetic-context idiom for build_args presence + anchored_misses presence. Vessel remediation — rbev-vessels/rbev-bottle-anthropic-jupyter/Dockerfile: replaced the hardcoded `FROM python:3.12.7-slim-bookworm` with the standard pattern `# Base image resolved by Foundry (anchored GAR ref or upstream pass-through)\nARG RBF_IMAGE_1\nFROM ${RBF_IMAGE_1}` matching rbev-busybox and common-sentry-context style — the rbrv.env already had RBRV_IMAGE_1_ORIGIN=python:3.12.7-slim-bookworm set so the build-arg propagation was always wired; the Dockerfile just ignored it. Empirically verified by surveying all 7 conjure-mode vessel Dockerfiles (common-ifrit-forge-context, common-sentry-context, jupyter, ccyolo, busybox, common-ifrit-context/Dockerfile.airgap, common-ifrit-context/Dockerfile.tether): every one now uses ARG RBF_IMAGE_1 + FROM ${RBF_IMAGE_1} or AS coda variants. Also tidied rbrv.env: removed a rogue `RBRV_IMAGE_1_ANCHOR=` empty-value line that lived AFTER the `# eof` marker — pre-existing cleanup matching the shape of working conjure vessels like rbev-bottle-ccyolo which omit the empty ANCHOR entirely. Spec subdoc RBSFH-dockerfile_hygiene.adoc minted in Tools/rbk/vov_veiled/: follows RBSDS-dns_step shape (//axhob_operation header with quoin reference, prose intro establishing scope across kludge and conjure callsites, three //axhos_step blocks one per rule), MCM-annotated, uses linked terms {rbtgo_ark_kludge}, {rbtgo_ark_conjure}, {rbbc_fatal}, {rbrv_image_origin}, {rbtgo_ark_enshrine} all verified present in mapping section. RBS0-SpecTop.adoc updated in two places: (1) mapping section gained a new "// Foundry Internals (build-side hygiene contracts shared across modes)" block with the single quoin `:rbsfh_dockerfile_hygiene: <<rbsfh_dockerfile_hygiene,Dockerfile Hygiene>>` immediately after the Script Internals block (line ~313); (2) new top-level section `== Foundry Internals` inserted between `== Script Internals` and `== Trade Studies` (line ~4120), containing the anchor block `[[rbsfh_dockerfile_hygiene]]` + `//axvo_procedure axd_transient axd_internal` annotation + `=== {rbsfh_dockerfile_hygiene}` header + 5-line intro + `include::RBSFH-dockerfile_hygiene.adoc[]`. RBSAK-ark_kludge.adoc Step 6 (Build Image Locally) reworked: replaced BBABH's gap-acknowledging prose about hardcoded FROM stages being a residual risk with a new leading bullet calling out the hygiene validation as the precondition ("Validate the vessel Dockerfile against {rbsfh_dockerfile_hygiene}: any column-0 `FROM` line must use ${RBF_IMAGE_1..3} or scratch...") and reframed the no-network paragraph to credit {rbsfh_dockerfile_hygiene} for closing the residual hardcoded-FROM risk that --pull=never used to backstop. RBSAC-ark_conjure.adoc Step 1 (Validate Input Parameters) gained a third {rbbc_require} bullet — "vessel Dockerfile passes {rbsfh_dockerfile_hygiene} — any column-0 FROM line must use ${RBF_IMAGE_1..3} or scratch as its image token; hardcoded literals abort before {gcb_service} submission". CLAUDE.md File Acronym Mappings: added RBFH entry under RBK Subdirectory pointing at rbk/rbfh_FoundryHygiene.sh with parenthetical description "(Foundry Hygiene — Dockerfile FROM-line constraint shared by kludge and conjure)", slotted between RBF and RBGA in the existing alphabetical block; added RBSFH entry pointing at rbk/vov_veiled/RBSFH-dockerfile_hygiene.adoc between RBSDY and RBSGD in the spec subsection. Verification — bash -n clean on all five touched bash modules (rbfh_FoundryHygiene.sh, rbfk_kludge.sh, rbfk_cli.sh, rbfd_FoundryDirectorBuild.sh, rbfd_cli.sh) confirmed individually; tt/rbtd-s.TestSuite.fast.sh green at 98/98 across enrollment-validation (47), regime-validation (27), regime-smoke (9), handbook-render (15) — handbook-render in particular exercises asciidoctor on the full rendered handbook through the RBS0 include chain, validating the new RBSFH subdoc syntax + the RBSAK/RBSAC edits render cleanly; standalone smoke test of rbfh_dockerfile_check authored against a minimal harness (stubbed buc_die/buc_log_args, kindle, then check against all 7 real conjure-vessel Dockerfiles plus 6 synthetic positive/negative cases: hardcoded literal → refused, tab in FROM → refused, trailing backslash → refused, multi-stage AS coda → admitted, empty file → admitted, comment-only file → admitted) — all expected verdicts produced with the exact `path:lineno: rule — offending line` diagnostic format spec'd by the docket. Departures from docket: minor only — (a) docket said "verify field name [RBRV_IMAGE_1_ORIGIN] against a working conjure vessel's rbrv.env before editing", verified empirically (ccyolo, busybox, etc.) and the jupyter rbrv.env already had the correct field set so the field-name verification step was a no-op (the bypass was purely in the Dockerfile); (b) the rogue `RBRV_IMAGE_1_ANCHOR=` line removal in jupyter rbrv.env was opportunistic cleanup not explicitly docketed but matches the shape of working conjure vessels and was clearly broken (positioned after `# eof`). Pristine kludge sentry verification (tt/rbw-cKS.KludgeSentry.sh tadmor) deferred to the post-notch step per the kludge dirty-tree guard; will run against this commit's state. Open BCG debt: none surfaced during the rbfk fold-in beyond what was explicitly listed in the docket. Theurge fixture coverage for the rbfh hygiene-check function is chivvied as ₢BBABJ (rbfh-cli-and-theurge-fixture pace cantled after this one) to keep BBABI's scope focused on the contract and its initial callsite wiring; BBABJ will add the operator-facing CLI + tabtargets + ~9-case fast-tier fixture for regression protection.
e044dd3f7 jjb:1011-6a7bfeb1:₣A_:T: research-config-directory-naming
040d0a181 jjb:1011-196b696eb:₣BB:n: Repair SA invest keys.list 404 flap: delete the rbgu_poll_until_ok subresource proxy probe and wrap keys.list in retry-on-404 sharing the keys.create RBGC_SA_KEY_CREATE_RETRY_* budget (same underlying physics — subresource visibility after parent creation). Update RBSCIP doctrine, RBSRK + RBSDK step 6 specs, and record transcript-level evidence in Memos/memo-20260514-pristine-gauntlet-iam-flap.md.
724e0163d jjb:1011-196b696eb:₣BB:n: Repair SA invest keys.list 404 flap: delete the rbgu_poll_until_ok subresource proxy probe and wrap keys.list in retry-on-404 sharing the keys.create RBGC_SA_KEY_CREATE_RETRY_* budget (same underlying physics — subresource visibility after parent creation). Update RBSCIP doctrine, RBSRK + RBSDK step 6 specs, and record transcript-level evidence in Memos/memo-20260514-pristine-gauntlet-iam-flap.md.
b35e58bfa jjb:1011-ea949e6f:₢BBABH:W: Made pristine-state kludge actually work end-to-end. Three notches landed, plus this wrap. (1) 6486d74e — origin-slot auto-pull: rbfd_kludge's presence guard now auto-pulls public-upstream origin misses inline with info-level announcement and post-pull docker-image-inspect sanity gate; anchored-slot miss path unchanged (refusal + wrest); RBSAK/RBS0/RBSRV spec coherence sweep. (2) abbd85bf — rbfk module split: empirical test surfaced a parasitic kindle-time gate (zrbfd_kindle's RBDC_DIRECTOR_RBRA_FILE presence check + transitive zrbfv_kindle call) that forced kludge to inherit credentialed-module preconditions despite never sourcing or using the credential. Factored kludge into its own BCG module (rbfk) — minted rbfk_kludge.sh + rbfk_cli.sh, sources only rbfc, minimal kindle. rbob_cli's differential kindle now routes kludge commands to zrbfk_kindle (uncredentialed) while keeping ordain on zrbfd_kindle. RBSAK Limitations parenthetical about the contract violation removed. rbfd description narrowed (kludge no longer listed). (3) ff28ac84 — drop --pull=never: buildx rejected the tri-state value ('strconv.ParseBool: parsing never: invalid syntax'); flag was already broken on modern docker. Removed the flag, updated rbfk_kludge comments + RBSAK Step 6 prose to document the lost hygiene and its current mitigation (presence guard + buildx default no-re-pull). The hygiene gap (no defense against hardcoded FROM bypassing RBF_IMAGE_n parameterization) is documented in the spec and chivvied as ₢BBABI for project-owned Dockerfile syntax constraint + bash hygiene check. Empirical verification: complete docker cache purge (zero images, zero containers, zero build cache), then tt/rbw-cKS.KludgeSentry.sh tadmor on a pristine state (no director RBRA installed) — full sequence green: rbfk kindle (no credential gate fired), slot detection (Image slot 1 pass-through: debian:bookworm-slim), auto-pull (Origin image not cached → pulling from upstream → re-inspect passed), hallmark mint (k260513212837-ff28ac84, ff28ac84 matches commit context), buildkit 11-stage build clean, vouch tag, hallmark drove into .rbk/tadmor/rbrn.env (committed with this wrap). The wrap-time nameplate change is the test's install record; a future operator who purges docker without re-kludging will find tadmor pointing at an absent image — operationally fine, just a recovery note. Fast suite 98/98 (enrollment-validation 47 + regime-validation 27 + regime-smoke 9 + handbook-render 15) re-run after every notch. Departures from original docket — all surfaced empirically: (a) rbfk module split was not in scope but was necessary to reach pristine-state correctness (the docket assumed kludge would just work uncredentialed; the kindle gate blocked it); (b) --pull=never drop was explicitly forbidden by the docket constraint ('--pull=never backstop on the build stays as tag-drift defense') but the constraint was authored assuming classic-builder semantics that buildx no longer provides — the flag was dead-on-arrival; (c) the Dockerfile hygiene rule that would substitute for --pull=never's defense is chivvied as ₢BBABI rather than landed here, capped this pace at empirical correctness. Open BCG debt in rbfk_kludge.sh: inherited multi-var local declarations, two if-not-docker-image-inspect-stderr-suppressed patterns, unbraced ${#z_build_args[@]} test — flagged for transparency, fold-in scheduled in ₢BBABI. Blocked-resolved: ₢BBAAX (next rbw-tP gauntlet run) is now unblocked. Cross-pace note: the rbfk minting (k for Kludge) preserves terminal exclusivity in the rbf family; new acronyms RBFK + RBFK0 fit cleanly alongside RBFC/RBFD/RBFL/RBFR/RBFV.
ff28ac849 jjb:1011-abbd85bf:₢BBABH:n: Drop --pull=never from kludge's docker build invocation. Surfaced empirically when post-decoupling test got past the director gate and finally exercised the build step against a real buildx-enabled docker daemon: buildx's --pull flag is boolean-only ('strconv.ParseBool: parsing never: invalid syntax'), there is no --pull=never analog in modern docker. The flag was inherited from the classic-builder era when it was the explicit no-network assertion at build time. Empirically, the original ₢BBABH docket framed --pull=never as a load-bearing 'tag-drift defense backstop' constraint, but that assumed classic docker semantics this environment no longer provides. The actual tag-drift defense lives in the presence guard above the build (every slot verified by docker image inspect — origin auto-pulled if absent, anchored refused if absent); buildx's default behavior is to not re-pull base images, so combined with the presence guard the build runs entirely against the local cache without networking. Code change: removed the --pull=never \ line from the docker build invocation in rbfk_kludge.sh, updated the two comment blocks that referenced the flag — the resolve-base-images preamble now says 'docker build (buildx default: no re-pull) runs against the cached image' as the tag-drift defense, and the build-step comment says 'Buildx defaults to not re-pulling base images; the presence guard above has already verified every slot is in the local cache, so the build never networks'. Spec sync at RBSAK Step 6 (Build Image Locally): replaced the prescriptive --pull=never prose with descriptive prose explaining the no-network property as an emergent consequence of the presence check + buildx's default behavior; documented the lost capability explicitly — 'Hardcoded FROM stages that bypass RBF_IMAGE_n parameterization remain a residual risk; buildx no longer exposes a --pull=never analog, so explicit assertion of the no-network contract at build time is no longer available.' Departure from docket: the original constraint '--pull=never backstop on the build stays as tag-drift defense' is invalidated by the empirical-test discovery that the flag was already broken on the current docker environment — the constraint preserved a flag that produced an error rather than the asserted defense. The substantive defense (presence guard + buildx-default) is preserved; only the explicit flag drops. Verification: bash -n clean on rbfk_kludge.sh; tt/rbtd-s.TestSuite.fast.sh 98/98 (enrollment-validation 47 + regime-validation 27 + regime-smoke 9 + handbook-render 15) — handbook-render renders the spec including the updated Step 6 prose. Empirical end-to-end re-run pending after this notch (kludge requires clean tree).
abbd85bf8 jjb:1011-6486d74e:₢BBABH:n: Decouple kludge from the director credential chain by factoring it into its own BCG module (rbfk). Empirical test of ₢BBABH's auto-pull behavior surfaced a parasitic coupling: rbob_cli's differential kindle for kludge commands fires zrbfd_kindle, which gates on RBDC_DIRECTOR_RBRA_FILE at module-load time even though rbfd_kludge itself never sources or uses the credential (the four `source RBDC_DIRECTOR_RBRA_FILE` sites in rbfd live in enshrine/build/mirror/graft — all cloud-build, none reached by kludge). Worse, zrbfd_kindle transitively calls zrbfv_kindle (line 90) because rbfd_ordain needs rbfv for the post-build vouch step; kludge has zero rbfv references but inherited rbfv's identical director gate too. The spec (RBSAK Limitations) already documented this contract violation: 'foundry kindle validates RBDC_DIRECTOR_RBRA_FILE exists, though kludge does not use credentials' — load-bearing-complexity test fails, so the load-bearing complexity gets factored out. Mint: rbfk (k for Kludge, semantically resonant with the kludge hallmark prefix). Terminal exclusivity preserved — rbf family already non-terminal with siblings rbfc/rbfd/rbfl/rbfr/rbfv; k unused; rbfk_kludge.sh + rbfk_cli.sh follow standard BCG implementation+CLI pair. New module rbfk_kludge.sh sources only rbfc_FoundryCore.sh (for ZRBFC_REGISTRY_HOST/PATH registry constants + zrbfc_resolve_vessel/zrbfc_load_vessel helpers — kludge's only non-builtin non-bash dependencies); zrbfk_kindle is minimal (delegate zrbfc_kindle, mark ZRBFK_KINDLED=1); zrbfk_sentinel delegates to zrbfc_sentinel plus its own KINDLED check. The rbfk_kludge function body is the unchanged ₢BBABH auto-pull implementation (origin auto-pull inline with re-inspect sanity gate, anchored accumulates for fatal-after-loop refusal) — moved verbatim from rbfd_FoundryDirectorBuild.sh:1336-1489, with zrbfd_sentinel → zrbfk_sentinel as the only edit. New CLI rbfk_cli.sh mirrors rbfd_cli.sh's furnish source/kindle chain identically except (a) sources rbfk_kludge.sh instead of rbfd_FoundryDirectorBuild.sh, and (b) calls zrbfk_kindle instead of zrbfd_kindle — so the rbfd kindle's director RBRA gate never fires for kludge invocations. The CLI's executable bit is set per BCG's _cli.sh-must-be-executable rule. Five updated call sites: rbfd_FoundryDirectorBuild.sh — kludge function block deleted (header comment + body, ~153 lines), module description on line 19 narrowed from 'ordain, conjure, enshrine, kludge, mirror, graft' to 'ordain, conjure, enshrine, mirror, graft'; rbfd's director gate at lines 43-44 stays load-bearing for its remaining credentialed operations and is intentionally untouched. rbob_bottle.sh — both kludge delegation sites (rbob_kludge at line 642, rbob_kludge_sentry at line 668) call rbfk_kludge instead of rbfd_kludge; companion comment at :641 updated to match. rbob_cli.sh — added `source rbfk_kludge.sh` after the existing rbfd source (no removal — rbob_ordain still needs rbfd), and split the differential kindle case so kludge commands (rbob_kludge|rbob_kludge_bottle|rbob_kludge_sentry) call zrbfk_kindle while rbob_ordain keeps zrbfd_kindle. rbz_zipper.sh — RBZ_KLUDGE_VESSEL enrollment retargeted: module `rbfd_cli.sh` → `rbfk_cli.sh`, function `rbfd_kludge` → `rbfk_kludge` (rbw-fk tabtarget invocation unchanged from operator's view). RBSAK-ark_kludge.adoc Limitations bullet — replaced the now-obsolete parenthetical ('foundry kindle validates RBDC_DIRECTOR_RBRA_FILE exists, though kludge does not use credentials') with accurate framing: GCP depot configuration is needed to construct GAR-formatted image tags (registry host/project/repository), but no credentials are consumed and no registry is contacted for hallmark publication — only origin-slot upstream pulls reach a registry, unauthenticated. Architecture-wide outcome: rbfd has uniform credentialed contract (every function in rbfd consumes director credentials); rbfk has uniform uncredentialed contract (no director, no Cloud Build, no OAuth); no exceptional per-function gating within either module; the documented spec quirk goes away. BCG compliance: rbfk_kludge.sh has standard SOURCED multiple-inclusion guard, sources only rbfc per BCG's 'module sources nothing except own sub-dependencies' pattern (matches rbfd's pattern of sourcing rbfc + rbfv); kindle uses readonly ZRBFK_KINDLED=1 as last statement; sentinel checks ZRBFK_KINDLED='1'; function body preserves all braced+quoted expansions and explicit || buc_die discipline from ₢BBABH; no inline shellcheck directives. CLI has set -euo pipefail, sources only buc_command.sh at header, all other deps in furnish after buc_doc_env_done gate. Verification: bash -n clean on all seven touched files (rbfk_kludge.sh, rbfk_cli.sh, rbfd_FoundryDirectorBuild.sh, rbob_bottle.sh, rbob_cli.sh, rbz_zipper.sh new contents); tt/rbtd-b.Build.sh green; tt/rbtd-s.TestSuite.fast.sh 98/98 (enrollment-validation 47 + regime-validation 27 + regime-smoke 9 + handbook-render 15) — handbook-render exercises asciidoctor on RBSAK including the updated Limitations bullet. Empirical end-to-end exercise: tt/rbw-cKS.KludgeSentry.sh tadmor invoked against a completely-purged docker cache (zero images, zero containers, zero build cache after the earlier `docker rm -f ... && docker rmi -f ... && docker system prune -a --volumes -f` sweep) with no director RBRA installed. First post-fix invocation got past the director-credential-gate failure mode (which was 'GCB service env file not found: ../station-files/secrets/director/rbra.env' before the structural split) — now reaches rbfk_kludge's dirty-tree guard, confirming the kindle chain no longer requires director credentials. The dirty-tree failure (this commit's own uncommitted state) is the expected next gate; once notched, the kludge proceeds into the auto-pull path that ₢BBABH constructed.
6486d74e6 jjb:1011-5f42a13f:₢BBABH:n: Switch rbfd_kludge to auto-pull origin-slot misses inline, splitting the previously-unified presence guard at rbfd_FoundryDirectorBuild.sh:1391-1448 by slot type. Surfaced by ₢BBAAX's first gauntlet run: onboarding-sequence/rbtdro_onboarding_kludge_tadmor failed in pristine state because debian:bookworm-slim (tadmor sentry's only slot, unanchored pass-through) wasn't cached locally — the operator-facing diagnostic prescribed docker pull for every slot, which kludge itself can just do for origin slots without changing the credentialed-out-of-band property of anchored slots. Origin-slot branch at :1417-1426 now announces (buc_info), runs docker pull inline with explicit || buc_die, then re-inspects as a sanity gate before falling through to the build. Anchored-slot miss path unchanged: accumulate locator, refuse after the loop with a wrest remediation block. Removed the now-unreachable z_origin_misses=() array and the pass-through branch of the diagnostic. --pull=never on docker build stays as tag-drift defense (RBSAK Step 6 prose unchanged). Comment block at :1381-1386 rewritten to distinguish the two slot types (anchored: GAR + credentials → wrest out-of-band; origin: public upstream → auto-pull inline). BCG compliance verified: braced+quoted expansions throughout, explicit || buc_die on every new external command (loop-body discipline per BCG 'set -e is Not Sufficient'), docker image inspect re-check matches the existing predicate-style pattern at :1414/:1420, docker pull stderr flows to terminal (not suppressed). Tier A spec sync: RBSAK-ark_kludge top NOTE scoped 'must be pre-cached locally' to anchored layers and added an origin auto-pull clause; Step 5 'Verify Local Image Presence' replaces the unified refusal-with-remediation flow with a two-branch flow (origin auto-pulls inline with re-inspect sanity gate, anchored accumulates for fatal-after-loop refusal) and removes the now-false 'kludge never reaches a registry' claim; Limitations bullet rewrites the cold-cache-path framing (origin-only vessels: one operator step; vessels with anchored slots: two steps — wrest then kludge). Tier B coherence: RBS0-SpecTop kludge stub (lines 1322-1324) scoped the pre-cache requirement to anchored images and added an origin auto-pull clause matching RBSAK; RBSRV-RegimeVessel {rbrv_image_anchor} 'When absent' branch gained a kludge auto-pull mention parallel to the 'When present' coverage on the next line. No edits to RBSAS/RBSCB/RBSDI/RBSIW/RBSOB/RBSIP — survey confirmed no other RBS* file carried the stale 'pre-cache all layers' framing. No handbook edits (explicitly OOS per docket; handbook says nothing about pre-pulling, which becomes accurate again with this change). No tabtarget or fixture edits. Verification: bash -n clean on rbfd_FoundryDirectorBuild.sh; tt/rbtd-b.Build.sh green; tt/rbtd-s.TestSuite.fast.sh 98/98 (enrollment-validation 47 + regime-validation 27 + regime-smoke 9 + handbook-render 15) re-run after both code and spec edits — handbook-render exercises asciidoctor on the RBSAK/RBS0/RBSRV touched specs via their include chain into the rendered handbook surfaces. Empirical end-to-end exercise of the auto-pull path deferred to the next step (docker cache purge + tadmor sentry kludge).
8d49b8068 jjb:1011-4fb4cf38:₢BBAAw:W: Sterilized rbw-HWdw (Native dockerd in WSL) ahead of upcoming WSL handbook rework, rather than expanding the handbook-render fixture to cover the lone param1-channel handbook colophon. Original pace scope was design-decision work: extend handbook-render fixture to exercise rbw-HWdw alongside the other 15 display tabtargets, picking between three architectural options (per-colophon default_args slice in the Rust registry, dedicated param1 subsection, split into two fixtures). Mount-time judgment redirected: the colophon and its handbook page are scheduled for removal in an upcoming heat that will redo Windows-Docker handbook guidance, so investing in coverage infrastructure for one offender that's about to vanish is wasted complexity. Sterilization preempts the gap. Narrow scope cut: only the WSL-native dockerd guidance and its tabtarget go; broader WSL plumbing (ZRBHW_WSL_DISTRO, Phase 3 WSL setup, Phase 4 Garrison-WSL, Docker context discipline page) remains intact, deferred to the future rework heat. Touchpoints (2 deletions + 7 edits): deleted tt/rbw-HWdw.DockerWSLNative.sh and Tools/rbk/rbh0/rbhwdn_docker_wsl_native.sh; removed the RBZ_HW_DOCKER_WSL_NATIVE enrollment from rbz_zipper.sh, the source line from rbhw0_cli.sh, the 'Phase 6 step 10. Native dockerd in WSL' entry plus 'Pass distro name' follow-up from rbhw0_top.sh (Docker context discipline renumbers 11 -> 10), the rbw-HWdw row from rbk-claude-tabtarget-context.md, the obsolete file-level scope-deferral comment referencing ₢A-AAS and the '4 of 5' Windows-cases section header in rbtdrf_handbook.rs, and the '4 of 5' Windows group comment in rbtdrm_manifest.rs. Memos/memo-20260412-windows-handbook-draft.md draft prose mentioning WSL native daemon was intentionally left as historical artifact for the future heat to revisit. Verification: bash -n clean on rbz_zipper.sh, rbhw0_cli.sh, rbhw0_top.sh; residual grep across Tools/, tt/, CLAUDE.md for HWdw / rbhw_docker_wsl_native / RBZ_HW_DOCKER_WSL_NATIVE / DockerWSLNative returned empty; tt/rbtd-b.Build.sh green; tt/rbtd-s.TestSuite.fast.sh green at 98/98 (enrollment-validation 47 + regime-validation 27 + regime-smoke 9 + handbook-render 15) — handbook-render case count unchanged because rbw-HWdw was never among the 15 covered cases. Single notch landed at 4fb4cf38.
86d8822a9 jjb:1011-59ed3355:₢BBABF:W: Closed silent semantic gap between IAM-grant propagation classes by recognizing a third class — caller-recently-empowered (HTTP 403 from resource-scope IAM cache lag on a freshly-empowered governor) — alongside the existing two member-visibility classes (HTTP 400 "does not exist" / "is not deleted"). zrbgi_propagation_error_predicate generalized to accept per-site (code, body-glob) tolerance pairs with empty-glob short-circuit semantics; RBGC_PROPAGATION_{INITIAL_DELAY,MAX_DELAY,DEADLINE}_SEC minted as the single shared budget for every IAM-grant retry site (3/20/420 — empirical 7-min ceiling for Google's "typically <2 min" propagation); (403, "") tolerance added to four resource-scope rbgi_* functions (repo, SA, bucket, secret) plus the inline GAR retry in rbgg_invest_director. Project-scope rbgi_add_project_iam_role keeps only the two 400 patterns — governor's roles/owner reaches project-scope IAM caches effectively instantly, so 403 there would slow real-denial diagnosis without absorbing any known race. Class C uses time-bound discrimination rather than body fingerprinting: the bound IS the discriminator (proven model from rbgv_jwt_sa_probe). Tier A specs: RBSCIP three-class taxonomy + constant-referenced backoff profile; RBSCIG new propagation-error catalog table (code/glob/scope/discriminator) + class C prose with 2026-05-13 empirical evidence (fresh-depot AR repo 403 against governor with ~30s-old roles/owner grant); RBSDK AR-step NOTE on class C absorption. Tier B coherence: RBSGM/RBSDE/RBSRK each distinguish project-scope-instant from resource-scope-laggy IAM cache propagation. Per docket, loop-skeleton extraction deferred (bash higher-order is ugly); Tier C drift deferred to ₢BBAAq's running-list mechanism. BCG audit pass on touched code: five `local z_prop_deadline=` upgraded to `local -r`; inline `# shellcheck disable=SC2254` removed (already globally suppressed in busc_shellcheckrc:62); `local -ra z_tolerance=(...)` readonly; comments WHY-only. Verification: bash -n clean ×3, shellcheck delta 0, fast suite 98/98 — handbook-render exercises asciidoctor rendering of all six touched specs. The wrap criterion of one-shot fresh-depot PayorMantle → InvestRetriever → InvestDirector remains operator-verified at next gauntlet run (deferred per ₣BB paddock's fast-suite reservation against ₣A_'s parallel-period burn-in).
---
