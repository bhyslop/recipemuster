=== heats in ORIGIN gallops (firemark-ish keys + silks) ===
  "jjgrn_next_heat_seed": "Bb",
      "jjghn_next_pace_seed": "ABL",
              "jjgtn_text": "## Character\nDesign conversation (BUSJGW redraft + step-order rework) then mechanical\nimplementation.\n\n## Docket\nRefactor garrison-w: workload owns its WSL distribution. Admin no longer\ninstalls rbtww-main globally; garrison opens an SSH-as-workload session\nand drives `wsl --import` from inside that session, so registration\nlands in bujuw_user's HKCU\\Lxss naturally. Garrison conducts; workload's\nown logon session executes — the identity boundary is the SSH session\nitself, not a scheduler hop.\n\nStructural shift in step ordering: the wsl-bound `command=` directive\ncan't be the first authorized_keys form (SSH-as-workload would route\nthrough unimported wsl). New shape: bare authorized_keys →\nSSH-as-workload imports rbtww-main + places privkey + validates →\nreplace with locked-down `command=` entry.\n\nDesign decisions:\n- Seed tarball provenance (admin pre-stages via `wsl --export`?\n  per-host bootstrap? committed/cached?).\n- Fate of `passwd --lock` in step 3 — vestigial after cycle-9's profile\n  registration landed, or still load-bearing for some Windows logon\n  path?\n- BUSJGW namespace framing: was \"Windows + WSL-Linux\"; now adds\n  workload's WSL distribution registration as third namespace.\n\nImplementation: bujb_jurisdiction.sh step structure update; BUSJGW\nspec redraft; BUHJ handbook update for any operator-manual\nprerequisites the redesign surfaces.\n\nDone when:\n- BUSJGW reads coherently against implementation intent.\n- bash -n green; tt/buw-st.BukSelfTest.sh green;\n  tt/rbtd-s.TestSuite.fast.sh green at baseline.\n- Empirical end-to-end deferred to the empirical pace.",
              "jjgtn_text": "## Character\nDesign conversation (BUSJGW redraft + step-order rework) then mechanical\nimplementation.\n\n## Docket\nRefactor garrison-w: workload owns its WSL distribution. Admin no longer\ninstalls rbtww-main globally; garrison opens an SSH-as-workload session\nand drives `wsl --import` from inside that session, so registration\nlands in bujuw_user's HKCU\\Lxss naturally. Garrison conducts; workload's\nown logon session executes — the identity boundary is the SSH session\nitself, not a scheduler hop.\n\nStructural shift in step ordering: the wsl-bound `command=` directive\ncan't be the first authorized_keys form (SSH-as-workload would route\nthrough unimported wsl). New shape: bare authorized_keys →\nSSH-as-workload imports rbtww-main + places privkey + validates →\nreplace with locked-down `command=` entry.\n\nDesign decisions:\n- Seed tarball provenance (admin pre-stages via `wsl --export`?\n  per-host bootstrap? committed/cached?).\n- Fate of `passwd --lock` in step 3 — vestigial after cycle-9's profile\n  registration landed, or still load-bearing for some Windows logon\n  path?\n- BUSJGW namespace framing: was \"Windows + WSL-Linux\"; now adds\n  workload's WSL distribution registration as third namespace.\n\nImplementation: bujb_jurisdiction.sh step structure update; BUSJGW\nspec redraft; BUHJ handbook update for any operator-manual\nprerequisites the redesign surfaces.\n\nDone when:\n- BUSJGW reads coherently against implementation intent.\n- bash -n green; tt/buw-st.BukSelfTest.sh green;\n  tt/rbtd-s.TestSuite.fast.sh green at baseline.\n- Empirical end-to-end deferred to the empirical pace.",
              "jjgtn_text": "## Character\nDesign conversation (BUSJGW redraft) then mechanical implementation.\nLocked by the spike pace's empirical outcome.\n\n## Docket\nRefactor garrison-w: workload owns its WSL distribution. Admin no longer\ninstalls rbtww-main globally; garrison creates the workload's own WSL\ninstance from a seed tarball, registered in workload's HKCU\\Lxss.\n\nDesign decisions (informed by spike):\n- Profile bootstrap (operator-manual interactive logon vs automated).\n- Seed tarball source (admin pre-stages? per-host bootstrap?).\n- Fate of `passwd --lock` in step 3 (vestigial or load-bearing?).\n- BUSJGW namespace framing: was \"Windows + WSL-Linux\"; now adds\n  workload's WSL distribution registration as third namespace.\n\nImplementation: garrison-w step structure update in bujb_jurisdiction.sh;\nBUSJGW spec redraft to match; BUHJ handbook update for any operator-manual\nprerequisites the spike surfaces.\n\nDone when:\n- BUSJGW reads coherently against implementation intent.\n- bash -n green; tt/buw-st.BukSelfTest.sh green;\n  tt/rbtd-s.TestSuite.fast.sh green at baseline.\n- Empirical end-to-end deferred to the empirical pace.",
              "jjgtn_text": "## Character\nMechanical string-scan plus constant promotion in a single file.\n\n## Goal\nPromote repeated string literals in `Tools/buk/bujb_jurisdiction.sh` to BCG tinder constants (`BUJB_lower_name` — pure literals at source time) or kindle constants (defined inside `zbujb_kindle()` with `readonly`; internal `Z` form by default, public `BUJB_` form only if exported to other modules). BCG line 138 governs the tinder-vs-kindle choice; line 134 governs the internal-vs-public form.\n\n## Boundary\n- Touch only `Tools/buk/bujb_jurisdiction.sh`.\n- Runtime-emitted directives from `BUJB_command_{b,c,w}` (what lands in `authorized_keys`) must be byte-identical post-expansion. Source-text may change only if the post-expansion value is preserved.\n- For `BUJB_command_w`: leave the directive single-quoted as today and accept the literal `rbtww-main` inside it. The tempting tinder-on-tinder interpolation of `${BUJB_wsl_distribution}` would also expand the existing `${BUJB_workload_user}` placeholder, which is intentionally late-bound at use-time by `bujb_command_for_capture`. Reworking that substitution scheme is a separate refactor — out of scope.\n- Apply BCG load-bearing test: promote only where a literal repeats enough, or carries enough meaning, to earn a name. Resist over-extraction.\n\n## Discovery\nRe-scan the file at mount. High-value zones from a prior survey: `authorized_keys` and `.ssh` path fragments, Windows profile root (`C:\\Users\\` and the forward-slash variant), seed tarball plus distro-fs paths (`rbtww-seed.tar`, `rbtww-fs`), SSH option set (IdentitiesOnly / BatchMode / StrictHostKeyChecking / ConnectTimeout), PowerShell prelude (`$ErrorActionPreference`, `$env:WSL_UTF8`, `$LASTEXITCODE` initialization), Ubuntu seed name and install dir, `/home/` and `/Users/` home-root prefixes, `set -euo pipefail` first-arg pattern. Verify each candidate's repeat count before promoting.\n\n## Done when\n- Identified duplicates promoted to tinder or kindle and collapsed at usage sites.\n- Runtime-emitted `BUJB_command_{b,c,w}` directives byte-identical post-expansion.\n- `bash -n Tools/buk/bujb_jurisdiction.sh` green.\n- `tt/buw-st.BukSelfTest.sh` green.\n- `tt/rbtd-s.TestSuite.fast.sh` green.",
              "jjgtn_text": "## Character\nMechanical string-scan plus constant promotion in a single file.\n\n## Goal\nPromote repeated string literals in `Tools/buk/bujb_jurisdiction.sh` to BCG tinder constants (`BUJB_lower_name` — pure literals at source time) or kindle constants (defined inside `zbujb_kindle()` with `readonly`; internal `Z` form by default, public `BUJB_` form only if exported to other modules). BCG line 138 governs the tinder-vs-kindle choice; line 134 governs the internal-vs-public form.\n\n## Boundary\n- Touch only `Tools/buk/bujb_jurisdiction.sh`.\n- Runtime-emitted directives from `BUJB_command_{b,c,w}` (what lands in `authorized_keys`) must be byte-identical post-expansion. Source-text may change only if the post-expansion value is preserved.\n- For `BUJB_command_w`: leave the directive single-quoted as today and accept the literal `rbtww-main` inside it. The tempting tinder-on-tinder interpolation of `${BUJB_wsl_distribution}` would also expand the existing `${BUJB_workload_user}` placeholder, which is intentionally late-bound at use-time by `bujb_command_for_capture`. Reworking that substitution scheme is a separate refactor — out of scope.\n- Apply BCG load-bearing test: promote only where a literal repeats enough, or carries enough meaning, to earn a name. Resist over-extraction.\n\n## Discovery\nRe-scan the file at mount. High-value zones from a prior survey: `authorized_keys` and `.ssh` path fragments, Windows profile root (`C:\\Users\\` and the forward-slash variant), seed tarball plus distro-fs paths (`rbtww-seed.tar`, `rbtww-fs`), SSH option set (IdentitiesOnly / BatchMode / StrictHostKeyChecking / ConnectTimeout), PowerShell prelude (`$ErrorActionPreference`, `$env:WSL_UTF8`, `$LASTEXITCODE` initialization), Ubuntu seed name and install dir, `/home/` and `/Users/` home-root prefixes, `set -euo pipefail` first-arg pattern. Verify each candidate's repeat count before promoting.\n\n## Done when\n- Identified duplicates promoted to tinder or kindle and collapsed at usage sites.\n- Runtime-emitted `BUJB_command_{b,c,w}` directives byte-identical post-expansion.\n- `bash -n Tools/buk/bujb_jurisdiction.sh` green.\n- `tt/buw-st.BukSelfTest.sh` green.\n- `tt/rbtd-s.TestSuite.fast.sh` green.",
              "jjgtn_text": "## Character\nMechanical string-scan plus constant promotion in a single file.\n\n## Goal\nPromote repeated string literals in `Tools/buk/bujb_jurisdiction.sh` to BCG tinder constants (`BUJB_lower_name` — pure literals at source time) or kindle constants (`ZBUJB_NAME` — runtime-derived inside `zbujb_kindle()` with `readonly`). BCG line 138 governs the choice.\n\n## Boundary\n- Touch only `Tools/buk/bujb_jurisdiction.sh`.\n- Leave the three `BUJB_command_{b,c,w}` directives byte-identical — comments mark them locked spec content mirrored in BUSJG{B,C,W}. Reordering the tinder block so `BUJB_wsl_distribution` precedes the directives and gets interpolated is permitted (tinder-on-tinder, BCG line 138).\n- Apply BCG load-bearing test: promote only where a literal repeats enough, or carries enough meaning, to earn a name. Resist over-extraction.\n\n## Discovery\nRe-scan the file at mount. High-value zones from a prior survey: `authorized_keys` and `.ssh` path fragments, Windows profile root (`C:\\Users\\` and the forward-slash variant), seed tarball plus distro-fs paths (`rbtww-seed.tar`, `rbtww-fs`), SSH option set (IdentitiesOnly / BatchMode / StrictHostKeyChecking / ConnectTimeout), PowerShell prelude (`$ErrorActionPreference`, `$env:WSL_UTF8`, `$LASTEXITCODE` initialization), Ubuntu seed name and install dir, `/home/` and `/Users/` home-root prefixes, `set -euo pipefail` first-arg pattern. Verify each candidate's repeat count before promoting.\n\n## Done when\n- Identified duplicates promoted to tinder or kindle and collapsed at usage sites.\n- `BUJB_command_{b,c,w}` byte-identical.\n- File sources cleanly through its consuming CLIs.",
              "jjgtn_text": "## Character\nImplementation — small surface; one extraction (Check from existing verify_ssh), two new verbs (Run, Ssh).\n\n## Docket\nMint `buw-nwc <workload-alias>` (Check), `buw-nwr <workload-alias> <cmd-file>` (Run with output capture), `buw-nws <workload-alias>` (interactive SSH).\n\nCheck absorbs the workload portion of `burn_verify_ssh`. The privileged portion belongs inside Garrison's verify phase, not as a separate tabtarget.\n\nNote colophon proximity: `buw-nwc` (Check) sits next to `buw-nwc{platform}` (Conscript). Distinct colophons, accepted.\n\nTier assertion: workload-alias arg rejects privileged tier.",
              "jjgtn_text": "## Character\nImplementation — small surface; one extraction (Check from existing verify_ssh), two new verbs (Run, Ssh).\n\n## Docket\nMint `buw-nwc <workload-alias>` (Check), `buw-nwr <workload-alias> <cmd-file>` (Run with output capture), `buw-nws <workload-alias>` (interactive SSH).\n\nCheck absorbs the workload portion of `burn_verify_ssh`. The privileged portion belongs inside Garrison's verify phase, not as a separate tabtarget.\n\nNote colophon proximity: `buw-nwc` (Check) sits next to `buw-nwc{platform}` (Conscript). Distinct colophons, accepted.\n\nTier assertion: workload-alias arg rejects privileged tier.",
              "jjgtn_text": "## Character\nCode amendment with curia-side verification.\n\n## Docket\n\nCorrect the `w` shell-letter so the workload identity is mirrored\nacross two namespaces on Windows hosts: a Windows user (via `net.exe\nuser /add`) for the SSH auth boundary, and a Linux user inside\n`rbtww-main` (via `useradd`) for the WSL execution context. Same\nWindows-user shape as Cygwin's `c` branch, plus the WSL-side\n`useradd`. Authorized_keys lives Windows-side (Windows OpenSSH\ndiscovers it natively); workload privkey for outbound stays inside\nWSL at `/home/${BURC_WORKLOAD_USER}/.ssh/id_ed25519`.\n\nChanges in `bujb_jurisdiction.sh`:\n\n- `BUJB_command_w`: add `--user ${BURC_WORKLOAD_USER}` to the\n  wsl.exe forced command so the session lands as the WSL Linux user\n  rather than the distro's default user.\n- Step 2 (destroy) `w` branch: prepend `net.exe user delete` before\n  the WSL-side `userdel`. Both purges idempotent.\n- Step 3 (create) `w` branch: prepend `net.exe user /add /passwordreq\n  :no /active:yes` before `useradd`. Mirrors c-branch shape.\n- Step 4 (place trust) `w` branch: write authorized_keys to the\n  Windows-side home — `c` branch is the structural reference. Forced\n  command is the wsl.exe shim from `BUJB_command_w`.\n- Step 5 (plant privkey) `w` branch: unchanged. Privkey lives at\n  `/home/${BURC_WORKLOAD_USER}/.ssh/id_ed25519` inside WSL so the\n  Linux session reads it natively for outbound to GitHub.\n- Pre-flight: prepend a step (or extend step 1) that runs `wsl.exe\n  --list --verbose` via admin SSH and asserts a distribution named\n  `${BUJB_wsl_distribution}` exists. On absence, surface a BUK\n  diagnostic naming the constant and pointing at the WSL install\n  procedure — replaces opaque Microsoft `wsl.exe` failures.\n\nBUSJGW spec updated to describe the mirrored-identity architecture\n(Windows-user auth boundary + WSL-user execution context).\n\nSequencing: lands first in the Practice run so the operator's WSL\ninstall (next pace) is followed by a working garrison-WSL ceremony.\nPre-flight is verifiable against the current state of bujn-winpc\n(no `rbtww-main` yet) — exercises the diagnostic deliberately\nbefore any operator install action.\n\nDone when:\n- `bash -n` green on `bujb_jurisdiction.sh`\n- `tt/buw-st.BukSelfTest.sh` green\n- `tt/rbtd-s.TestSuite.fast.sh` green at baseline (no regression)\n- Pre-flight diagnostic surfaces the absent-distro condition cleanly\n  when run against bujn-winpc before WSL install",
              "jjgtn_text": "## Character\nCode amendment with curia-side verification.\n\n## Docket\n\nAdd `buw-jpS` privileged-SSH tabtarget — thin admin-side pipe that\nruns an arbitrary command as `BURP_PRIVILEGED_USER` on a BURN node.\nChannel: param1 = investiture; remaining args joined as the remote\ncommand. Pass-through — no shell wrapping; operator prepends\n`powershell -Command`, `bash -c`, etc. as needed for the platform.\n\nScope: any BURN that has a local BURP profile (Linux/Mac/Windows\nalike). Linux/Mac admin trust comes from operator-managed\n`ssh-copy-id`; Windows admin trust comes from fenestrate. The\ntabtarget itself does not assert platform — the privileged-key auth\npath handles success/failure uniformly.\n\nDrop \"interactive if no extra args\" from the earlier sketch — require\n≥1 command arg; die with a usage diagnostic on missing command.\nInteractive variant deferred to real need.\n\nCode shape:\n\n- New public function `bujb_privileged_ssh COMMAND...` in\n  `bujb_jurisdiction.sh`. Uses `BURP_PRIVILEGED_KEY_FILE`,\n  `IdentitiesOnly=yes`, `BatchMode=yes`, key-only auth — no password\n  fallback. No shell wrapping.\n- New CLI command in `bujb_cli.sh` with appropriate furnish (BURC +\n  BURN + BURP regimes kindled, `bujb_resolve_investiture` called for\n  privkey validation per existing pattern).\n- New tabtarget `tt/buw-jpS.PrivilegedSsh.sh` enrolled in\n  `buz_zipper.sh`. Channel: param1.\n\nBUS0 update:\n\n- New §BUSJPS section parallel to BUSJPF, describing the privileged-\n  SSH verb with: any-BURN scope, param1+args channel, pass-through\n  shell semantics, no persistent state change (ephemeral exec).\n- Mapping section gains the new tabtarget entry.\n\nDownstream consumer (next pace, AAv): pre-flight diagnostic emits\ncopy-paste hints in the form `tt/buw-jpS <investiture> 'wsl --install\n…'`. AAv depends on this pace landing first.\n\nDone when:\n\n- `bash -n` green on `bujb_jurisdiction.sh`, `bujb_cli.sh`\n- `tt/buw-st.BukSelfTest.sh` green\n- `tt/rbtd-s.TestSuite.fast.sh` green at baseline (no regression)\n- BUSJPS section present in BUS0; mapping section updated\n- Curia-side smoke: `tt/buw-jpS <investiture> 'whoami'` returns the\n  expected privileged user name on at least one platform reachable\n  from the test station",
              "jjgtn_text": "## Character\nCode amendment with curia-side verification.\n\n## Docket\n\nAdd `buw-jpS` privileged-SSH tabtarget — thin admin-side pipe that\nruns an arbitrary command as `BURP_PRIVILEGED_USER` on a BURN node.\nChannel: param1 = investiture; remaining args joined as the remote\ncommand. Pass-through — no shell wrapping; operator prepends\n`powershell -Command`, `bash -c`, etc. as needed for the platform.\n\nScope: any BURN that has a local BURP profile (Linux/Mac/Windows\nalike). Linux/Mac admin trust comes from operator-managed\n`ssh-copy-id`; Windows admin trust comes from fenestrate. The\ntabtarget itself does not assert platform — the privileged-key auth\npath handles success/failure uniformly.\n\nDrop \"interactive if no extra args\" from the earlier sketch — require\n≥1 command arg; die with a usage diagnostic on missing command.\nInteractive variant deferred to real need.\n\nCode shape:\n\n- New public function `bujb_privileged_ssh COMMAND...` in\n  `bujb_jurisdiction.sh`. Uses `BURP_PRIVILEGED_KEY_FILE`,\n  `IdentitiesOnly=yes`, `BatchMode=yes`, key-only auth — no password\n  fallback. No shell wrapping.\n- New CLI command in `bujb_cli.sh` with appropriate furnish (BURC +\n  BURN + BURP regimes kindled, `bujb_resolve_investiture` called for\n  privkey validation per existing pattern).\n- New tabtarget `tt/buw-jpS.PrivilegedSsh.sh` enrolled in\n  `buz_zipper.sh`. Channel: param1.\n\nBUS0 update:\n\n- New §BUSJPS section parallel to BUSJPF, describing the privileged-\n  SSH verb with: any-BURN scope, param1+args channel, pass-through\n  shell semantics, no persistent state change (ephemeral exec).\n- Mapping section gains the new tabtarget entry.\n\nDownstream consumer (next pace, AAv): pre-flight diagnostic emits\ncopy-paste hints in the form `tt/buw-jpS <investiture> 'wsl --install\n…'`. AAv depends on this pace landing first.\n\nDone when:\n\n- `bash -n` green on `bujb_jurisdiction.sh`, `bujb_cli.sh`\n- `tt/buw-st.BukSelfTest.sh` green\n- `tt/rbtd-s.TestSuite.fast.sh` green at baseline (no regression)\n- BUSJPS section present in BUS0; mapping section updated\n- Curia-side smoke: `tt/buw-jpS <investiture> 'whoami'` returns the\n  expected privileged user name on at least one platform reachable\n  from the test station",
              "jjgtn_text": "## Character\nMechanical sweep across `Tools/buk/bujb_jurisdiction.sh`.\n\n## Shape\nMove auto-numbered forensic capture into `zbujb_admin_powershell` and `_with_password_fallback`. New signature: `CHIT BODY...`. Internal redirect to `${BURD_TEMP_DIR}/bujb_${CHIT}${idx}_{stdout,stderr}.txt`; expose `ZBUJB_LAST_AP_STDOUT` / `ZBUJB_LAST_AP_STDERR`. Single shared `z_bujb_emit_index` counter across all chits.\n\n## Kindle\nFive `ZBUJB_CHIT_*` namespace fragments (`caparison-`, `place-trust-`, `w-init-`, `obliterate-`, `validate-`) replace the five `ZBUJB_*_PREFIX` constants. Orthogonality is grep-auditable: each fragment should appear only inside its owning phase function.\n\n## Deletes\nFive `_run` wrappers (`zbujb_{place_trust,validate,w_init,obliterate,caparison}_run`); `zbujb_obliterate_diag_dump` and its `ZBUJB_OBLITERATE_STDOUT`/`STDERR` singleton constants (no callers).\n\n## Done\nAll `zbujb_admin_powershell*` call sites converted; parse sites read `$(<\"${ZBUJB_LAST_AP_STDOUT}\")`; probes ignore globals. `bash -n` and `tt/buw-st.BukSelfTest.sh` green. Field exercise on bujn-winpc deferred to operator's next garrison-w run.",
              "jjgtn_text": "## Character\nMechanical sweep across `Tools/buk/bujb_jurisdiction.sh`.\n\n## Shape\nMove auto-numbered forensic capture into `zbujb_admin_powershell` and `_with_password_fallback`. New signature: `CHIT BODY...`. Internal redirect to `${BURD_TEMP_DIR}/bujb_${CHIT}${idx}_{stdout,stderr}.txt`; expose `ZBUJB_LAST_AP_STDOUT` / `ZBUJB_LAST_AP_STDERR`. Single shared `z_bujb_emit_index` counter across all chits.\n\n## Kindle\nFive `ZBUJB_CHIT_*` namespace fragments (`caparison-`, `place-trust-`, `w-init-`, `obliterate-`, `validate-`) replace the five `ZBUJB_*_PREFIX` constants. Orthogonality is grep-auditable: each fragment should appear only inside its owning phase function.\n\n## Deletes\nFive `_run` wrappers (`zbujb_{place_trust,validate,w_init,obliterate,caparison}_run`); `zbujb_obliterate_diag_dump` and its `ZBUJB_OBLITERATE_STDOUT`/`STDERR` singleton constants (no callers).\n\n## Done\nAll `zbujb_admin_powershell*` call sites converted; parse sites read `$(<\"${ZBUJB_LAST_AP_STDOUT}\")`; probes ignore globals. `bash -n` and `tt/buw-st.BukSelfTest.sh` green. Field exercise on bujn-winpc deferred to operator's next garrison-w run.",
              "jjgtn_text": "## Character\nArchitectural promotion of a debug-loop accelerator, not a revert.\n\n## Goal\nPromote the DEV CACHE shape in `zbujb_caparison_windows_stage_wsl` to\nformal architecture under the admin-no-WSL principle: admin produces\nthe seed tar on disk and never registers rbtww-main in admin's\nHKCU\\Lxss. Drop garrison-w's `[w-export-seed]` step (which only\nexisted to export admin's running rbtww-main; admin no longer has\none).\n\nConcretely:\n- Remove the DEV CACHE comment-block scaffolding; the shape becomes\n  the canonical implementation.\n- Decide and document the canonical cache path (currently\n  `C:\\rbtww-seed.tar`) and where caparison places it: rename if\n  warranted, or formalize the current path with rationale.\n- Caparison Phase 3 retains [3/6] install + [4/6] export to the\n  cache path, and [6/6] unregister of the Ubuntu-24.04 seed; drops\n  [5/6] import-into-admin and the tar-removal half of [6/6].\n- Garrison-w drops `zbujb_garrison_w_export_seed` and its\n  `[w-seed-cleanup]` companion; `[w-session-1/4]` imports directly\n  from the canonical cache path.\n- Operator must still drive a one-time cache pre-stage on hosts\n  where caparison has never produced the tar (or caparison must\n  produce it idempotently on first run).\n\n## Done when\n- Caparison-windows on a fresh bujn-winpc (or equivalent) lands the\n  cache tar at the canonical path and does NOT register rbtww-main\n  in admin's HKCU\\Lxss (verify: admin's wsl.exe --list does not\n  show rbtww-main).\n- Garrison-w on the same host imports rbtww-main into workload's\n  HKCU\\Lxss from the cache tar, without going through any admin-\n  side rbtww-main export.\n- BUSJCW and BUSJGW specs reflect the admin-no-WSL shape (this\n  intersects with ₢A-ABK's scope — coordinate at mount time).",
              "jjgtn_text": "## Character\nSurgical revert of a debug-loop accelerator.\n\n## Goal\nRemove the DEV CACHE shortcut from `zbujb_caparison_windows_stage_wsl` in\n`Tools/buk/bujb_jurisdiction.sh` so caparison-windows again creates the\nseed tar from scratch every run (download Ubuntu-24.04, export, import,\nunregister, delete tar). Restore the four commented-out blocks: [3/6]\ninstall, [4/6] export, [6/6] cleanup, and the `${BUJB_wsl_distribution}.tar`\npurge that was dropped from [1/6]. Restore the `z_tar_path` local back to\n`${BUJB_path_win_wsl_install_root}\\${BUJB_wsl_distribution}.tar` (drop the\nhardcoded `C:\\rbtww-seed.tar`).\n\n## Done when\n- Phase 3 stage_wsl matches its pre-cache shape (no DEV CACHE comments,\n  no hardcoded C:\\rbtww-seed.tar).\n- A caparison-windows run on bujn-winpc completes Phase 3 end-to-end with\n  the from-scratch download/export/import/cleanup path.\n- Operator manually removes the pre-staged `C:\\rbtww-seed.tar` from\n  bujn-winpc (one privileged-ssh Remove-Item) so no stale cache lingers.",
              "jjgtn_text": "## Character\nSpec coherence to match unconditional-reboot architecture\nlanded under ₢A-ABG, plus admin-no-WSL shape landing under ₢A-ABJ.\n\n## Goal\nBUS*.adoc specs reflect both the reboot-as-precondition shape and\nthe admin-no-WSL caparison/garrison restructure:\n- BUSJGW gains the reboot step + simplified Phase 2; drops\n  [w-export-seed] / [w-seed-cleanup] step descriptions.\n- BUSJCW adds a cross-reference noting caparison's role as the\n  reboot helper's enabling contract (sshd-at-boot, Tailscale\n  autostart, authkeys persistence). Caparison Phase 3 spec\n  reflects the admin-no-WSL shape (cache produced, no admin\n  registration).\n- BUSJCW (or BUSJI as appropriate) adds a paragraph explaining\n  why the absolute-path AuthorizedKeysFile in the Match block is\n  structurally required, not a Cygwin path organization choice:\n  Windows OpenSSH's relative-path resolution via\n  GetUserProfileDirectoryW fails on headless accounts (no\n  ProfileList registry entry). Cite\n  `Memos/memo-20260516-windows-headless-account-anatomy.md`\n  §Chain step 3.\n- WSG mints (or doesn't) a WSp item codifying reboot-as-canonical\n  -Windows-state-reset for destructive cleanup. Decision recorded\n  either way.\n- All three documents cross-reference the design memo at\n  `Memos/memo-20260511-windows-hive-cleanup-reboot-decision.md`,\n  which captures the rejected alternatives (Tier 1 in-process\n  unload, Tier 2 handle-hunt, Tier 3 UserProfileSvc cycle, Level 3\n  offline NTUSER.DAT injection) and the rationale for the chosen\n  primitive. Cross-references prevent the rationale from being\n  re-discovered every time someone reads the helper.\n\n## Done when\n- BUSJGW step numbering and prose match landed bash.\n- BUSJCW cross-references the reboot dependency + admin-no-WSL\n  caparison shape.\n- BUSJCW (or BUSJI) cites\n  `Memos/memo-20260516-windows-headless-account-anatomy.md` for\n  the GetUserProfileDirectoryW rationale behind the absolute-path\n  Match block.\n- WSG decision recorded (new WSp or \"deemed not load-bearing as a\n  separate principle\").\n- All three specs cite `Memos/memo-20260511-windows-hive-cleanup-\n  reboot-decision.md` at the appropriate sections.\n- Implementation commits under ₢A-ABG (reboot) and ₢A-ABJ (admin-\n  no-WSL) cited in spec edits.",
              "jjgtn_text": "## Character\nSpec coherence to match unconditional-reboot architecture\nlanded under ₢A-ABG, plus admin-no-WSL shape landing under ₢A-ABJ.\n\n## Goal\nBUS*.adoc specs reflect both the reboot-as-precondition shape and\nthe admin-no-WSL caparison/garrison restructure:\n- BUSJGW gains the reboot step + simplified Phase 2; drops\n  [w-export-seed] / [w-seed-cleanup] step descriptions.\n- BUSJCW adds a cross-reference noting caparison's role as the\n  reboot helper's enabling contract (sshd-at-boot, Tailscale\n  autostart, authkeys persistence). Caparison Phase 3 spec\n  reflects the admin-no-WSL shape (cache produced, no admin\n  registration).\n- WSG mints (or doesn't) a WSp item codifying reboot-as-canonical\n  -Windows-state-reset for destructive cleanup. Decision recorded\n  either way.\n- All three documents cross-reference the design memo at\n  `Memos/memo-20260511-windows-hive-cleanup-reboot-decision.md`,\n  which captures the rejected alternatives (Tier 1 in-process\n  unload, Tier 2 handle-hunt, Tier 3 UserProfileSvc cycle, Level 3\n  offline NTUSER.DAT injection) and the rationale for the chosen\n  primitive. Cross-references prevent the rationale from being\n  re-discovered every time someone reads the helper.\n\n## Done when\n- BUSJGW step numbering and prose match landed bash.\n- BUSJCW cross-references the reboot dependency + admin-no-WSL\n  caparison shape.\n- WSG decision recorded (new WSp or \"deemed not load-bearing as a\n  separate principle\").\n- All three specs cite `Memos/memo-20260511-windows-hive-cleanup-\n  reboot-decision.md` at the appropriate sections.\n- Implementation commits under ₢A-ABG (reboot) and ₢A-ABJ (admin-\n  no-WSL) cited in spec edits.",
      "jjghn_next_pace_seed": "ABA",
              "jjgtn_text": "## Character\n\nVoice-driven pedagogical writing, modeled on `rbhodf_director_first_build.sh` and `rbhofc_first_crucible.sh`. The infrastructure (forge vessel, airgap-ifrit vessel, moriah nameplate, tabtargets, theurge fixture) is landed by `₢A6AAV`. This pace writes the teaching content.\n\n**Depends on**: `₢A6AAV` (infrastructure landed), `₢A6AAU` (first cloud build handbook finalized — the airgap track is the next step in the director learning sequence).\n\n## Docket\n\nImplement `tt/rbw-Oda.OnboardingDirectorAirgap.sh` — the airgap cloud build handbook track.\n\n### Teaching arc (5 units, Frame 4-refined)\n\n1. **Enshrine upstream base** — `rust:slim-bookworm` for the forge. Teach: airgap requires a pre-staged supply chain.\n2. **Conjure forge tethered, then enshrine** — build a project-authored base image that pre-stages apt packages and warms the cargo cache. Teach: forge as customer-controlled toolchain image; layered enshrinement.\n3. **Conjure airgap-ifrit airgapped from enshrined forge** — zero network during the airgap build. Teach: customer code never exposed to internet during build.\n4. **Charge moriah, run security suite** — drive the cloud-built airgap hallmark into the moriah nameplate, run `tt/rbtd-r.Run.moriah.sh`, watch the same attack tests pass against the airgap-built bottle. Teach: the learner has built validated security infrastructure end-to-end.\n5. **Compare Plumb output** — tethered ifrit (tadmor) vs airgap ifrit (moriah) side-by-side. Teach: functionally equivalent images, distinguished by supply-chain properties; both carry SLSA provenance.\n\n### Implementation\n\n- Zipper enrollment in `rbz_zipper.sh` — colophon `rbw-Oda`, group `RBZ__GROUP_ONBOARDING`\n- Thin dispatcher tabtarget `tt/rbw-Oda.OnboardingDirectorAirgap.sh` (nolog launcher pattern)\n- Sequence function `rbho_director_airgap()` in `Tools/rbk/rbh0/rbhoda_director_airgap.sh`\n- Teaching content using `buh_*` primitives, vocabulary-first via `buh_tlt`\n- Probes: reliquary inscribed, rust:slim-bookworm enshrined, forge hallmark exists, airgap ifrit vouched, moriah chargeable, tethered ifrit present (for plumb comparison)\n\n### Model\n\nFollow the patterns in `rbhodf_director_first_build.sh` (probe structure, teaching unit layout, `buc_doc_brief`/`buc_doc_shown` gate, yelp capture idiom) and `rbhofc_first_crucible.sh` (crucible-oriented teaching).\n\n### Out of scope\n\n- Infrastructure creation (₢A6AAV)\n- Reliquary inscription (₢A6AAU)\n- Bind or graft modes (₢A6AAW, ₢A6AAX)",
              "jjgtn_text": "## Character\n\nVoice-driven pedagogical writing, modeled on `rbhodf_director_first_build.sh` and `rbhofc_first_crucible.sh`. The infrastructure (forge vessel, airgap-ifrit vessel, moriah nameplate, tabtargets, theurge fixture) is landed by `₢A6AAV`. This pace writes the teaching content.\n\n**Depends on**: `₢A6AAV` (infrastructure landed), `₢A6AAU` (first cloud build handbook finalized — the airgap track is the next step in the director learning sequence).\n\n## Docket\n\nImplement `tt/rbw-Oda.OnboardingDirectorAirgap.sh` — the airgap cloud build handbook track.\n\n### Teaching arc (5 units, Frame 4-refined)\n\n1. **Enshrine upstream base** — `rust:slim-bookworm` for the forge. Teach: airgap requires a pre-staged supply chain.\n2. **Conjure forge tethered, then enshrine** — build a project-authored base image that pre-stages apt packages and warms the cargo cache. Teach: forge as customer-controlled toolchain image; layered enshrinement.\n3. **Conjure airgap-ifrit airgapped from enshrined forge** — zero network during the airgap build. Teach: customer code never exposed to internet during build.\n4. **Charge moriah, run security suite** — drive the cloud-built airgap hallmark into the moriah nameplate, run `tt/rbtd-r.Run.moriah.sh`, watch the same attack tests pass against the airgap-built bottle. Teach: the learner has built validated security infrastructure end-to-end.\n5. **Compare Plumb output** — tethered ifrit (tadmor) vs airgap ifrit (moriah) side-by-side. Teach: functionally equivalent images, distinguished by supply-chain properties; both carry SLSA provenance.\n\n### Implementation\n\n- Zipper enrollment in `rbz_zipper.sh` — colophon `rbw-Oda`, group `RBZ__GROUP_ONBOARDING`\n- Thin dispatcher tabtarget `tt/rbw-Oda.OnboardingDirectorAirgap.sh` (nolog launcher pattern)\n- Sequence function `rbho_director_airgap()` in `Tools/rbk/rbh0/rbhoda_director_airgap.sh`\n- Teaching content using `buh_*` primitives, vocabulary-first via `buh_tlt`\n- Probes: reliquary inscribed, rust:slim-bookworm enshrined, forge hallmark exists, airgap ifrit vouched, moriah chargeable, tethered ifrit present (for plumb comparison)\n\n### Model\n\nFollow the patterns in `rbhodf_director_first_build.sh` (probe structure, teaching unit layout, `buc_doc_brief`/`buc_doc_shown` gate, yelp capture idiom) and `rbhofc_first_crucible.sh` (crucible-oriented teaching).\n\n### Out of scope\n\n- Infrastructure creation (₢A6AAV)\n- Reliquary inscription (₢A6AAU)\n- Bind or graft modes (₢A6AAW, ₢A6AAX)",
              "jjgtn_text": "## Character\n\nSmall infrastructure pace with a downstream pedagogy revision. The bottle has `rbw-cKB` (KludgeBottle) — nameplate-aware, builds and drives the hallmark into the nameplate in one step. The sentry has no such symmetric tool; the learner runs `rbw-fk rbev-sentry-deb-tether`, copies the hallmark tag from terminal output, opens `rbrn.env`, and pastes into `RBRN_SENTRY_HALLMARK` by hand.\n\nThe first-crucible handbook (`rbho_first_crucible()` in `rbhofc_first_crucible.sh`) currently rationalizes the asymmetry (\"the sentry is shared across nameplates, so you kludge it standalone and drive the hallmark yourself\"). In practice this is copy/paste friction the learner hits immediately after meeting the toolchain, and the handbook already teaches \"what a hallmark is and where it lives\" via the bottle step one screen earlier.\n\nCognitive posture: mechanical symmetry restoration plus trimming of the teaching prose that only existed to justify the missing tool. Parallel-safe with other A6 work — touches `rbob_bottle.sh`, the zipper, a new tabtarget, and the first-crucible handbook file.\n\n## Docket\n\n### Problem\n\nStep 1.1 + Step 2 of `rbho_first_crucible()` force the learner to transcribe a hallmark tag between terminal output and a config file. Mechanically identical to the bottle path one step earlier, but unautomated.\n\n### Approach\n\nMirror the bottle pattern:\n\n| Bottle (today) | Sentry (target) |\n|---|---|\n| `rbob_kludge()` in `rbob_bottle.sh` — kludges bottle vessel, drives hallmark into `RBRN_BOTTLE_HALLMARK` | New `rbob_kludge_sentry()` — kludges sentry vessel (from `RBRN_SENTRY_VESSEL`), drives hallmark into `RBRN_SENTRY_HALLMARK` |\n| `rbw-cKB.KludgeBottle.sh` (param1 moniker) | New `rbw-cKS.KludgeSentry.sh` (param1 moniker) |\n| Zipper group entry for `rbw-cKB` | Zipper group entry for `rbw-cKS` |\n\n### Commit cadence — load-bearing\n\n`rbfd_kludge()` (at `rbfd_FoundryDirectorBuild.sh:1213-1218`) hard-dies on any unstaged or staged change. Both auto-driving kludges leave the tree dirty after driving their hallmark. Consequence: **with two auto-driving kludges, the learner must commit between them**, not bundle at the end.\n\n**Current flow (manual sentry paste):** clean → `rbw-fk` (tree stays clean) → `rbw-cKB` (tree dirty: bottle hallmark) → manual paste sentry hallmark (tree dirty: both hallmarks, single commit point) → **one commit** → charge.\n\n**New flow (symmetric kludges):** clean → `rbw-cKS` (tree dirty: sentry hallmark) → **commit #1** (forced — next kludge gates on clean tree) → `rbw-cKB` (tree dirty: bottle hallmark) → **commit #2** → charge.\n\nNet change for the learner: manual-paste eliminated, one extra git commit added. The handbook must teach this commit-between-kludges cadence explicitly — it's the one new invariant the symmetry introduces.\n\n### Implementation steps\n\n1. Add `rbob_kludge_sentry()` in `Tools/rbk/rbob_bottle.sh` — structural twin of `rbob_kludge()`, targeting `RBRN_SENTRY_VESSEL` / `RBRN_SENTRY_HALLMARK`. Use `zrbob_drive_hallmark` (already generic on the variable name).\n2. Add thin dispatcher `tt/rbw-cKS.KludgeSentry.sh` (nolog launcher pattern matching `rbw-cKB`).\n3. Enroll `rbw-cKS` in `Tools/rbk/rbz_zipper.sh` under the Crucible group (next to `rbw-cKB`). Channel: param1.\n4. Add linked-term constant (if one exists for `rbw-cKB` — check `rbyc_common.sh` or the `RBZ_CRUCIBLE_KLUDGE_*` family in the zipper) so the handbook can reference it via a named constant, not a raw string.\n5. Update `Tools/rbk/rbh0/rbhofc_first_crucible.sh`:\n   - Replace Step 1.1's `rbw-fk` invocation and the copy/paste instruction with the new nameplate-aware call.\n   - Insert a **commit step between the two kludges** with concrete `git add` / `git commit` lines and a short explanation (\"kludge needs a clean tree, so commit the sentry hallmark before kludging the bottle\").\n   - Collapse old Step 2 (manual RBRN_SENTRY_HALLMARK edit) into a terminal-step commit for the bottle hallmark, symmetric with the post-sentry commit.\n   - Reframe the \"two kludge commands, two different jobs\" comparison (lines ~197–204) — both are now nameplate-aware; the remaining distinction (sentry shared across nameplates vs bottle per-nameplate) can be mentioned briefly or dropped.\n   - Keep the existing `buh_warn \"${RBYC_KLUDGE} requires a clean git tree.\"` — its importance *increases* under the new flow.\n   - Update probe names/comments if any reference the old flow.\n   - Update the \"Iteration loop\" section at the end (lines ~391–412) — currently describes a one-kludge cycle; with symmetric kludges it may still be one-kludge (the sentry rarely changes), but the commit cadence framing needs a once-over.\n6. Regenerate `CLAUDE.consumer.md` / `rbk-claude-tabtarget-context.md` colophon table if auto-generation is wired up (per `₢A6AAF` docket, the regenerator is the source of truth).\n\n### Pedagogy question for mount time\n\nThe original asymmetry carried a teaching moment: \"here's the file, here's the field, here's how hallmarks live in nameplates.\" With the symmetric tool in place, decide whether to:\n\n- **(a)** Drop the manual-paste teaching entirely — the bottle step already shows the same machinery via `zrbob_drive_hallmark` output, and a one-line `buh_line` can mention where hallmarks live without a manual exercise.\n- **(b)** Keep a vestigial \"open the nameplate and verify\" probe step — passive inspection instead of active editing.\n\nDefault to (a) unless the review at mount time argues for (b).\n\n### Coordination\n\n- `₢A6AAu` (review-onboarding-director-first-kludge, currently earlier on the rail but moving behind this pace) is the natural downstream beneficiary — the review proceeds against the cleaned-up track, not the old copy/paste flow.\n- No interaction with director tracks (cloud build, airgap, bind, graft) — those paths don't touch sentry kludge.\n\n### Requires\n\n- Nothing — the bottle path (`rbob_kludge`, `zrbob_drive_hallmark`) is the reference implementation already landed.\n\n### Out of scope\n\n- Any change to `rbw-fk` (LocalKludge) — it remains the standalone vessel-kludge entry point for non-nameplate contexts (and for the sentry-deb-airgap path where no single nameplate owns it).\n- Ordain-side symmetric sentry (cloud build). Director cloud-build tracks already drive sentry hallmarks through their own pipeline; this pace is kludge-only.\n- Changes to the airgap handbook track (`₢A6AAV`/`₢A6AA1`) — that track uses its own moriah-nameplate flow.\n\n### Validation\n\nEnd-to-end dry run against a fresh `ccyolo`:\n\n```\n# Clean tree\ntt/rbw-cKS.KludgeSentry.sh ccyolo\n# Expect: sentry image built, RBRN_SENTRY_HALLMARK field rewritten, tree dirty in exactly rbrn.env\ngit add .rbk/ccyolo/rbrn.env\ngit commit -m \"kludge sentry hallmark into ccyolo nameplate\"\n\ntt/rbw-cKB.KludgeBottle.sh ccyolo\n# Expect: bottle image built, RBRN_BOTTLE_HALLMARK field rewritten, tree dirty in exactly rbrn.env\ngit add .rbk/ccyolo/rbrn.env\ngit commit -m \"kludge bottle hallmark into ccyolo nameplate\"\n\ntt/rbw-cC.Charge.ccyolo.sh\n# Expect: charge succeeds (nameplate is committed)\n```\n\nAlso run `tt/rbw-Ofc.OnboardingFirstCrucible.sh` end-to-end and confirm handbook output no longer instructs copy/paste and teaches the two-commit cadence clearly.\n\n### Failure mode to verify\n\nRun `rbw-cKS ccyolo` then immediately `rbw-cKB ccyolo` without committing — confirm `rbfd_kludge`'s dirty-tree guard fires with a clear error. The handbook should anticipate this and guide the learner to commit; the runtime guard is the safety net.",
              "jjgtn_text": "## Character\n\nSmall infrastructure pace with a downstream pedagogy revision. The bottle has `rbw-cKB` (KludgeBottle) — nameplate-aware, builds and drives the hallmark into the nameplate in one step. The sentry has no such symmetric tool; the learner runs `rbw-fk rbev-sentry-deb-tether`, copies the hallmark tag from terminal output, opens `rbrn.env`, and pastes into `RBRN_SENTRY_HALLMARK` by hand.\n\nThe first-crucible handbook (`rbho_first_crucible()` in `rbhofc_first_crucible.sh`) currently rationalizes the asymmetry (\"the sentry is shared across nameplates, so you kludge it standalone and drive the hallmark yourself\"). In practice this is copy/paste friction the learner hits immediately after meeting the toolchain, and the handbook already teaches \"what a hallmark is and where it lives\" via the bottle step one screen earlier.\n\nCognitive posture: mechanical symmetry restoration plus trimming of the teaching prose that only existed to justify the missing tool. Parallel-safe with other A6 work — touches `rbob_bottle.sh`, the zipper, a new tabtarget, and the first-crucible handbook file.\n\n## Docket\n\n### Problem\n\nStep 1.1 + Step 2 of `rbho_first_crucible()` force the learner to transcribe a hallmark tag between terminal output and a config file. Mechanically identical to the bottle path one step earlier, but unautomated.\n\n### Approach\n\nMirror the bottle pattern:\n\n| Bottle (today) | Sentry (target) |\n|---|---|\n| `rbob_kludge()` in `rbob_bottle.sh` — kludges bottle vessel, drives hallmark into `RBRN_BOTTLE_HALLMARK` | New `rbob_kludge_sentry()` — kludges sentry vessel (from `RBRN_SENTRY_VESSEL`), drives hallmark into `RBRN_SENTRY_HALLMARK` |\n| `rbw-cKB.KludgeBottle.sh` (param1 moniker) | New `rbw-cKS.KludgeSentry.sh` (param1 moniker) |\n| Zipper group entry for `rbw-cKB` | Zipper group entry for `rbw-cKS` |\n\n### Commit cadence — load-bearing\n\n`rbfd_kludge()` (at `rbfd_FoundryDirectorBuild.sh:1213-1218`) hard-dies on any unstaged or staged change. Both auto-driving kludges leave the tree dirty after driving their hallmark. Consequence: **with two auto-driving kludges, the learner must commit between them**, not bundle at the end.\n\n**Current flow (manual sentry paste):** clean → `rbw-fk` (tree stays clean) → `rbw-cKB` (tree dirty: bottle hallmark) → manual paste sentry hallmark (tree dirty: both hallmarks, single commit point) → **one commit** → charge.\n\n**New flow (symmetric kludges):** clean → `rbw-cKS` (tree dirty: sentry hallmark) → **commit #1** (forced — next kludge gates on clean tree) → `rbw-cKB` (tree dirty: bottle hallmark) → **commit #2** → charge.\n\nNet change for the learner: manual-paste eliminated, one extra git commit added. The handbook must teach this commit-between-kludges cadence explicitly — it's the one new invariant the symmetry introduces.\n\n### Implementation steps\n\n1. Add `rbob_kludge_sentry()` in `Tools/rbk/rbob_bottle.sh` — structural twin of `rbob_kludge()`, targeting `RBRN_SENTRY_VESSEL` / `RBRN_SENTRY_HALLMARK`. Use `zrbob_drive_hallmark` (already generic on the variable name).\n2. Add thin dispatcher `tt/rbw-cKS.KludgeSentry.sh` (nolog launcher pattern matching `rbw-cKB`).\n3. Enroll `rbw-cKS` in `Tools/rbk/rbz_zipper.sh` under the Crucible group (next to `rbw-cKB`). Channel: param1.\n4. Add linked-term constant (if one exists for `rbw-cKB` — check `rbyc_common.sh` or the `RBZ_CRUCIBLE_KLUDGE_*` family in the zipper) so the handbook can reference it via a named constant, not a raw string.\n5. Update `Tools/rbk/rbh0/rbhofc_first_crucible.sh`:\n   - Replace Step 1.1's `rbw-fk` invocation and the copy/paste instruction with the new nameplate-aware call.\n   - Insert a **commit step between the two kludges** with concrete `git add` / `git commit` lines and a short explanation (\"kludge needs a clean tree, so commit the sentry hallmark before kludging the bottle\").\n   - Collapse old Step 2 (manual RBRN_SENTRY_HALLMARK edit) into a terminal-step commit for the bottle hallmark, symmetric with the post-sentry commit.\n   - Reframe the \"two kludge commands, two different jobs\" comparison (lines ~197–204) — both are now nameplate-aware; the remaining distinction (sentry shared across nameplates vs bottle per-nameplate) can be mentioned briefly or dropped.\n   - Keep the existing `buh_warn \"${RBYC_KLUDGE} requires a clean git tree.\"` — its importance *increases* under the new flow.\n   - Update probe names/comments if any reference the old flow.\n   - Update the \"Iteration loop\" section at the end (lines ~391–412) — currently describes a one-kludge cycle; with symmetric kludges it may still be one-kludge (the sentry rarely changes), but the commit cadence framing needs a once-over.\n6. Regenerate `CLAUDE.consumer.md` / `rbk-claude-tabtarget-context.md` colophon table if auto-generation is wired up (per `₢A6AAF` docket, the regenerator is the source of truth).\n\n### Pedagogy question for mount time\n\nThe original asymmetry carried a teaching moment: \"here's the file, here's the field, here's how hallmarks live in nameplates.\" With the symmetric tool in place, decide whether to:\n\n- **(a)** Drop the manual-paste teaching entirely — the bottle step already shows the same machinery via `zrbob_drive_hallmark` output, and a one-line `buh_line` can mention where hallmarks live without a manual exercise.\n- **(b)** Keep a vestigial \"open the nameplate and verify\" probe step — passive inspection instead of active editing.\n\nDefault to (a) unless the review at mount time argues for (b).\n\n### Coordination\n\n- `₢A6AAu` (review-onboarding-director-first-kludge, currently earlier on the rail but moving behind this pace) is the natural downstream beneficiary — the review proceeds against the cleaned-up track, not the old copy/paste flow.\n- No interaction with director tracks (cloud build, airgap, bind, graft) — those paths don't touch sentry kludge.\n\n### Requires\n\n- Nothing — the bottle path (`rbob_kludge`, `zrbob_drive_hallmark`) is the reference implementation already landed.\n\n### Out of scope\n\n- Any change to `rbw-fk` (LocalKludge) — it remains the standalone vessel-kludge entry point for non-nameplate contexts (and for the sentry-deb-airgap path where no single nameplate owns it).\n- Ordain-side symmetric sentry (cloud build). Director cloud-build tracks already drive sentry hallmarks through their own pipeline; this pace is kludge-only.\n- Changes to the airgap handbook track (`₢A6AAV`/`₢A6AA1`) — that track uses its own moriah-nameplate flow.\n\n### Validation\n\nEnd-to-end dry run against a fresh `ccyolo`:\n\n```\n# Clean tree\ntt/rbw-cKS.KludgeSentry.sh ccyolo\n# Expect: sentry image built, RBRN_SENTRY_HALLMARK field rewritten, tree dirty in exactly rbrn.env\ngit add .rbk/ccyolo/rbrn.env\ngit commit -m \"kludge sentry hallmark into ccyolo nameplate\"\n\ntt/rbw-cKB.KludgeBottle.sh ccyolo\n# Expect: bottle image built, RBRN_BOTTLE_HALLMARK field rewritten, tree dirty in exactly rbrn.env\ngit add .rbk/ccyolo/rbrn.env\ngit commit -m \"kludge bottle hallmark into ccyolo nameplate\"\n\ntt/rbw-cC.Charge.ccyolo.sh\n# Expect: charge succeeds (nameplate is committed)\n```\n\nAlso run `tt/rbw-Ofc.OnboardingFirstCrucible.sh` end-to-end and confirm handbook output no longer instructs copy/paste and teaches the two-commit cadence clearly.\n\n### Failure mode to verify\n\nRun `rbw-cKS ccyolo` then immediately `rbw-cKB ccyolo` without committing — confirm `rbfd_kludge`'s dirty-tree guard fires with a clear error. The handbook should anticipate this and guide the learner to commit; the runtime guard is the safety net.",
              "jjgtn_text": "## Character\n\nSmall infrastructure pace with a downstream pedagogy revision. The bottle has `rbw-cKB` (KludgeBottle) — nameplate-aware, builds and drives the hallmark into the nameplate in one step. The sentry has no such symmetric tool; the learner runs `rbw-fk rbev-sentry-deb-tether`, copies the hallmark tag from terminal output, opens `rbrn.env`, and pastes into `RBRN_SENTRY_HALLMARK` by hand.\n\nThe first-crucible handbook (`rbho_first_crucible()` in `rbhofc_first_crucible.sh`) currently rationalizes the asymmetry (\"the sentry is shared across nameplates, so you kludge it standalone and drive the hallmark yourself\"). In practice this is copy/paste friction the learner hits immediately after meeting the toolchain, and the handbook already teaches \"what a hallmark is and where it lives\" via the bottle step one screen earlier.\n\nCognitive posture: mechanical symmetry restoration plus trimming of the teaching prose that only existed to justify the missing tool. Parallel-safe with other A6 work — touches `rbob_bottle.sh`, the zipper, a new tabtarget, and the first-crucible handbook file.\n\n## Docket\n\n### Problem\n\nStep 1.1 + Step 2 of `rbho_first_crucible()` force the learner to transcribe a hallmark tag between terminal output and a config file. Mechanically identical to the bottle path one step earlier, but unautomated.\n\n### Approach\n\nMirror the bottle pattern:\n\n| Bottle (today) | Sentry (target) |\n|---|---|\n| `rbob_kludge()` in `rbob_bottle.sh` — kludges bottle vessel, drives hallmark into `RBRN_BOTTLE_HALLMARK` | New `rbob_kludge_sentry()` — kludges sentry vessel (from `RBRN_SENTRY_VESSEL`), drives hallmark into `RBRN_SENTRY_HALLMARK` |\n| `rbw-cKB.KludgeBottle.sh` (param1 moniker) | New `rbw-cKS.KludgeSentry.sh` (param1 moniker) |\n| Zipper group entry for `rbw-cKB` | Zipper group entry for `rbw-cKS` |\n\n### Implementation steps\n\n1. Add `rbob_kludge_sentry()` in `Tools/rbk/rbob_bottle.sh` — structural twin of `rbob_kludge()`, targeting `RBRN_SENTRY_VESSEL` / `RBRN_SENTRY_HALLMARK`. Use `zrbob_drive_hallmark` (already generic on the variable name).\n2. Add thin dispatcher `tt/rbw-cKS.KludgeSentry.sh` (nolog launcher pattern matching `rbw-cKB`).\n3. Enroll `rbw-cKS` in `Tools/rbk/rbz_zipper.sh` under the Crucible group (next to `rbw-cKB`). Channel: param1.\n4. Add linked-term constant (if one exists for `rbw-cKB` — check `rbyc_common.sh` or the `RBZ_CRUCIBLE_KLUDGE_*` family in the zipper) so the handbook can reference it via a named constant, not a raw string.\n5. Update `Tools/rbk/rbh0/rbhofc_first_crucible.sh`:\n   - Replace Step 1.1's `rbw-fk` invocation and the copy/paste instruction with the new nameplate-aware call.\n   - Collapse Step 2 (manual RBRN_SENTRY_HALLMARK edit) — the pedagogy for \"what a hallmark is and where it lives\" is already carried by the bottle kludge step and can be retained there.\n   - Reframe the \"two kludge commands, two different jobs\" comparison (lines ~197–204) to reflect that both are now nameplate-aware; the only distinction left is vessel scope (sentry shared vs bottle per-nameplate), which can be mentioned briefly or dropped.\n   - Update probe names/comments if any reference the old flow.\n6. Regenerate `CLAUDE.consumer.md` / `rbk-claude-tabtarget-context.md` colophon table if auto-generation is wired up (per `₢A6AAF` docket, the regenerator is the source of truth).\n\n### Pedagogy question for mount time\n\nThe original asymmetry carried a teaching moment: \"here's the file, here's the field, here's how hallmarks live in nameplates.\" With the symmetric tool in place, decide whether to:\n\n- **(a)** Drop the manual-paste teaching entirely — the bottle step already shows the same machinery via `zrbob_drive_hallmark` output, and a one-line `buh_line` can mention where hallmarks live without a manual exercise.\n- **(b)** Keep a vestigial \"open the nameplate and verify\" probe step — passive inspection instead of active editing.\n\nDefault to (a) unless the review at mount time argues for (b).\n\n### Coordination\n\n- `₢A6AAu` (review-onboarding-director-first-kludge, currently earlier on the rail but moving behind this pace) is the natural downstream beneficiary — the review proceeds against the cleaned-up track, not the old copy/paste flow.\n- No interaction with director tracks (cloud build, airgap, bind, graft) — those paths don't touch sentry kludge.\n\n### Requires\n\n- Nothing — the bottle path (`rbob_kludge`, `zrbob_drive_hallmark`) is the reference implementation already landed.\n\n### Out of scope\n\n- Any change to `rbw-fk` (LocalKludge) — it remains the standalone vessel-kludge entry point for non-nameplate contexts (and for the sentry-deb-airgap path where no single nameplate owns it).\n- Ordain-side symmetric sentry (cloud build). Director cloud-build tracks already drive sentry hallmarks through their own pipeline; this pace is kludge-only.\n- Changes to the airgap handbook track (`₢A6AAV`/`₢A6AA1`) — that track uses its own moriah-nameplate flow.\n\n### Validation\n\n- `tt/rbw-cKS.KludgeSentry.sh ccyolo` on a clean tree produces a kludged sentry image, writes the hallmark into `.rbk/ccyolo/rbrn.env` RBRN_SENTRY_HALLMARK, leaves the tree dirty with only that one field changed.\n- After committing, `tt/rbw-cC.Charge.ccyolo.sh` charges successfully.\n- Running `tt/rbw-Ofc.OnboardingFirstCrucible.sh` end-to-end produces handbook output that no longer instructs copy/paste.",
              "jjgtn_text": "## Character\n\nIntricate but mechanical. The interview phase landed in ☉260420-1014 — option (B′), two handbooks with a shared mechanical trunk. File names, tabtarget name, shared-helper contracts, tail content shape, and cloud-build exclusion are all locked. No open design questions.\n\nCognitive posture: careful attention to per-file content role. The evaluator tail is substantially richer than the explorer's — treat the depth asymmetry as intended. Avoid making the shared trunk too thick (voice collapses into one frame) or too thin (factoring fails to earn weight). Mirror the `rbhodf_director_first_build.sh` consumption pattern for `RBYC_HANDBOOK_*` kindle constants (export-teaching via `_NAME`, buh_tt arg expansion via `_REF`).\n\n## Docket\n\n### Landed design decision — option (B′)\n\nA fourth option beyond the original (A)/(B)/(C) in the prior docket. **Two handbooks, shared mechanical trunk, intimate voice-specific top and tail.**\n\nWhy (B′) over the prior three:\n\n- **Audience separation is load-bearing.** Explorer (\"inhabit the sandbox, feel the walls\") and evaluator (\"verify the sandbox holds under attack\") are distinct framings. Option (A) collapses them into one voice; option (C) lands them in a menu. Neither serves the audience split.\n- **Verification is where actions genuinely diverge** — ccyolo's manual `curl`-by-hand against the allowlist vs tadmor's theurge suite run against adversarial attacks. Same goal (\"prove the sandbox holds\"), different *actions*, not just prose voice.\n- **The mechanical trunk (kludge sentry → commit → kludge bottle → commit → charge) is voice-neutral and thick enough to factor.** Status-quo (B) pays duplication cost with no pedagogical benefit.\n\nResult: two handbook files with independent top/tail, one shared trunk helper, one shared quench helper.\n\n### File structure\n\n**Shared helpers (new, siblings in `Tools/rbk/rbh0/`):**\n\n- `rbhoct_crucible_trunk.sh` — factored kludge/commit/charge choreography\n- `rbhocq_crucible_quench.sh` — factored quench step surfacing the charge-auto-quench pedagogy at its natural moment (replacing the currently-buried iteration-loop mention at `rbhofc:370-371`)\n\n**Refactored:**\n\n- `rbhofc_first_crucible.sh` — keeps explorer-intimate top (framing + export teaching + docker gate) and explorer-intimate tail (SSH + Claude OAuth + manual curl verification + iteration-loop wrap); delegates middle choreography to the shared helpers\n\n**New evaluator handbook:**\n\n- `rbhots_tadmor_security.sh` — evaluator-intimate top + shared trunk call + evaluator-intimate tail + shared quench call + wrap\n\n**New tabtarget + zipper enrollment:**\n\n- `tt/rbw-Ots.OnboardingTadmorSecurity.sh` — evaluator handbook entry point\n- Enroll `rbw-Ots` in `RBZ__GROUP_ONBOARDING` in `rbz_zipper.sh`\n\n### Shared trunk contract (`rbhoct_crucible_trunk`)\n\nCaller signature — authorial bash args carry per-nameplate facts:\n\n```\nrbhoct_crucible_trunk \"${z_moniker}\" \"${z_sentry_vessel}\" \"${z_bottle_vessel}\" \"${z_nameplate_file}\"\n```\n\nTrunk body renders, in order:\n\n1. Section header: \"Build images locally\" + kludge/hallmark/nameplate concept teaching (voice-neutral)\n2. Kludge sentry step — `buh_tt` with `${RBYC_HANDBOOK_NAMEPLATE_REF}` for learner-shell expansion\n3. Sentry-image-exists + sentry-hallmark-populated probes\n4. Commit cadence teaching (sentry)\n5. Kludge bottle step — `buh_tt` with `${RBYC_HANDBOOK_NAMEPLATE_REF}`\n6. Bottle-image-exists + bottle-hallmark-populated probes\n7. Commit cadence teaching (bottle)\n8. Charge section — brief sentry/pentacle/bottle mention (architecture deep-dive belongs in evaluator tail)\n9. Charge step — `buh_tt` with moniker interpolation\n10. Crucible-charged probe\n\nProbe discrimination works naturally — probes key off the `z_sentry_vessel` / `z_bottle_vessel` bash args, which differ per nameplate.\n\n### Shared quench contract (`rbhocq_crucible_quench`)\n\nCaller signature:\n\n```\nrbhocq_crucible_quench \"${z_moniker}\"\n```\n\nQuench body renders:\n\n1. Section header: \"Quench the crucible\"\n2. Brief teaching: stops containers, tears down enclave network\n3. Quench tabtarget call — `buh_tt` with moniker interpolation\n4. **Charge-auto-quench pedagogy at its natural moment:** \"you don't need to quench between iterations — charge tears down prior state for you; quench when you're done.\" (Current location of this insight at `rbhofc:370-371` gets removed from the explorer wrap since the shared quench now carries it for both audiences.)\n\n### Caller structure (both handbooks)\n\n```\n1. zrbho_sentinel\n2. buc_doc_brief / buc_doc_shown\n3. Authorial bash: local -r z_moniker=<nameplate>\n                   z_sentry_vessel=...\n                   z_bottle_vessel=...\n                   z_nameplate_file=...\n4. Audience-framing header prose (intimate voice)\n5. Export teaching line (using ${RBYC_HANDBOOK_NAMEPLATE_NAME})\n6. Docker gate probe + early-return on failure\n7. rbhoct_crucible_trunk \"${z_moniker}\" \"${z_sentry_vessel}\" \"${z_bottle_vessel}\" \"${z_nameplate_file}\"\n8. Verification ritual (intimate — curl for ccyolo, architecture + theurge for tadmor)\n9. rbhocq_crucible_quench \"${z_moniker}\"\n10. Wrap prose (iteration loop for ccyolo, next-tests guidance for tadmor)\n11. Return-to-start buh_tt\n```\n\n### Evaluator tail content shape (rich)\n\nThe evaluator tail is substantially richer than the explorer's. Do not under-scope. Three parts:\n\n**Architecture deep-dive** — not the brief charge-time mention the trunk carries; a real tour:\n\n- Sentry layer: iptables kernel rules + dnsmasq DNS layer — why both layers, what each enforces, what happens when either fails\n- Pentacle layer: network namespace ownership; why a dedicated container rather than bottle-side namespace manipulation\n- Bottle layer: unprivileged workload context; lack of network primitives means no circumvention path\n- Defense-in-depth across the three containers\n\n**Ifrit/Theurge methodology intro:**\n\n- Ifrit is the attack binary delivered into the bottle\n- Claude Code sessions with full visibility authored the attack suite against known primitives\n- Theurge coordinates inside (bottle) and outside (host) observation to verify attacks fail\n- Test results show both the attack tried AND the sandbox response\n\n**Fixture invocation + result inspection:**\n\n- Full fixture (teach as the default pass): `tt/rbtd-r.Run.tadmor-security.sh` — 34 cases, exercises the complete containment attack surface against a kludged tadmor\n- Single case (teach for iterative debugging / deep-dive on one attack): `tt/rbtd-s.SingleCase.tadmor-security.sh [case-name]`\n- Result reading: teach the learner how to interpret pass/fail output, what a green case means, what a red case would surface\n\n**Strictly no cloud-build or global-suite invocations.** Do not reference `rbtd-s.TestSuite.crucible.sh` (pulls in moriah, which is cloud-built), `rbtd-s.TestSuite.complete.sh`, or the service suite. If `rbtd-r.Run.tadmor-security.sh` / `rbtd-s.SingleCase.tadmor-security.sh` carry framing that pulls in global theurge-apparatus prose the handbook shouldn't teach, create thin custom wrappers under a new colophon family rather than redirecting learners to the raw rbtd tabtargets.\n\n### Explorer tail content shape (preserve)\n\nPreserve `rbhofc`'s existing tail content, lightly edited:\n\n- SSH into bottle via `tt/rbw-cS.SshTo.ccyolo.sh`\n- Claude Code OAuth copy-paste teaching (preserve the v2.1.89 paste-regression warning at current lines 275-279)\n- Manual curl verification ritual (api.anthropic.com → 404, claude.ai → 403, www.internic.net → 200, google.com → timeout, registry.npmjs.org → timeout)\n- `rbw-cr.Rack` diagnostic fallback guidance\n- Iteration-loop summary — **minus** the charge-auto-quench insight (moved into shared quench where it serves both audiences)\n\n### Validation\n\n- `rbhofc` dry-render matches current output closely (small acceptable deltas where shared quench surfaces the auto-quench insight earlier than current iteration-loop summary)\n- `rbhots` dry-render produces a coherent evaluator tour: architecture + methodology + fixture invocation + result inspection\n- Probes discriminate correctly by nameplate — no false greens/reds (vessel names are the discriminator; already in authorial bash args)\n- Zero hardcoded nameplate sigils outside the three authorial declarations per handbook\n- Grep sweep for `HANDBOOK_NAMEPLATE` raw literal in yelp args across `Tools/rbk/` returns zero (all consumption via `${RBYC_HANDBOOK_NAMEPLATE_REF}`)\n- Zipper entry for `rbw-Ots` present in `RBZ__GROUP_ONBOARDING`; launcher wired; tabtarget file exists and dispatches correctly\n- `rbhoct` and `rbhocq` sourced/kindled correctly from both callers\n- Fast qualify passes\n- No theurge invocations in handbook content call cloud builds or global test suites (verified by grep for `TestSuite.crucible`, `TestSuite.complete`, `TestSuite.service`)\n\n### Requires\n\n₢A6AA3 (landed: `f131f83f`, `33f8d192`, `0bdd62e9`) — installed `RBYC_HANDBOOK_*` kindle constants in `rbyc_common.sh` and proved consumption pattern across 9 sites in `rbhodf_director_first_build.sh`. This pace's consumption follows the same shape: `${RBYC_HANDBOOK_NAMEPLATE_NAME}` in export-teaching lines, `${RBYC_HANDBOOK_NAMEPLATE_REF}` in buh_tt arg positions.\n\n### Out of scope\n\n- **Paddock Rough Track Roster update** (track 3 ↔ track 14 shared-trunk annotation, track 14 promotion from placeholder to implemented) — deferred to a later pace per user direction.\n- Changes to the kindle constants themselves (shape is settled).\n- Re-deciding infrastructure channel (env var + authorial bash args is the established pattern).\n- Any other handbook beyond `rbhofc` and the new `rbhots`.\n- New theurge test cases or test suite changes.\n- Nameplate regime changes or new nameplates.\n- ccyolo/tadmor vessel Dockerfile changes.\n- Cloud-build or global-suite theurge invocations (explicitly prohibited in handbook content).\n\n### Interview origin\n\nDesign conversation `☉260420-1014` landed the (B′) decision and all structural/naming choices: file names (`rbhoct`, `rbhocq`, `rbhots`), tabtarget name (`rbw-Ots`), shared-helper contracts, tail-depth asymmetry, cloud-build exclusion. Implementation agent should proceed without re-opening these decisions.",
              "jjgtn_text": "## Character\n\nIntricate but mechanical. The interview phase landed in ☉260420-1014 — option (B′), two handbooks with a shared mechanical trunk. File names, tabtarget name, shared-helper contracts, tail content shape, and cloud-build exclusion are all locked. No open design questions.\n\nCognitive posture: careful attention to per-file content role. The evaluator tail is substantially richer than the explorer's — treat the depth asymmetry as intended. Avoid making the shared trunk too thick (voice collapses into one frame) or too thin (factoring fails to earn weight). Mirror the `rbhodf_director_first_build.sh` consumption pattern for `RBYC_HANDBOOK_*` kindle constants (export-teaching via `_NAME`, buh_tt arg expansion via `_REF`).\n\n## Docket\n\n### Landed design decision — option (B′)\n\nA fourth option beyond the original (A)/(B)/(C) in the prior docket. **Two handbooks, shared mechanical trunk, intimate voice-specific top and tail.**\n\nWhy (B′) over the prior three:\n\n- **Audience separation is load-bearing.** Explorer (\"inhabit the sandbox, feel the walls\") and evaluator (\"verify the sandbox holds under attack\") are distinct framings. Option (A) collapses them into one voice; option (C) lands them in a menu. Neither serves the audience split.\n- **Verification is where actions genuinely diverge** — ccyolo's manual `curl`-by-hand against the allowlist vs tadmor's theurge suite run against adversarial attacks. Same goal (\"prove the sandbox holds\"), different *actions*, not just prose voice.\n- **The mechanical trunk (kludge sentry → commit → kludge bottle → commit → charge) is voice-neutral and thick enough to factor.** Status-quo (B) pays duplication cost with no pedagogical benefit.\n\nResult: two handbook files with independent top/tail, one shared trunk helper, one shared quench helper.\n\n### File structure\n\n**Shared helpers (new, siblings in `Tools/rbk/rbh0/`):**\n\n- `rbhoct_crucible_trunk.sh` — factored kludge/commit/charge choreography\n- `rbhocq_crucible_quench.sh` — factored quench step surfacing the charge-auto-quench pedagogy at its natural moment (replacing the currently-buried iteration-loop mention at `rbhofc:370-371`)\n\n**Refactored:**\n\n- `rbhofc_first_crucible.sh` — keeps explorer-intimate top (framing + export teaching + docker gate) and explorer-intimate tail (SSH + Claude OAuth + manual curl verification + iteration-loop wrap); delegates middle choreography to the shared helpers\n\n**New evaluator handbook:**\n\n- `rbhots_tadmor_security.sh` — evaluator-intimate top + shared trunk call + evaluator-intimate tail + shared quench call + wrap\n\n**New tabtarget + zipper enrollment:**\n\n- `tt/rbw-Ots.OnboardingTadmorSecurity.sh` — evaluator handbook entry point\n- Enroll `rbw-Ots` in `RBZ__GROUP_ONBOARDING` in `rbz_zipper.sh`\n\n### Shared trunk contract (`rbhoct_crucible_trunk`)\n\nCaller signature — authorial bash args carry per-nameplate facts:\n\n```\nrbhoct_crucible_trunk \"${z_moniker}\" \"${z_sentry_vessel}\" \"${z_bottle_vessel}\" \"${z_nameplate_file}\"\n```\n\nTrunk body renders, in order:\n\n1. Section header: \"Build images locally\" + kludge/hallmark/nameplate concept teaching (voice-neutral)\n2. Kludge sentry step — `buh_tt` with `${RBYC_HANDBOOK_NAMEPLATE_REF}` for learner-shell expansion\n3. Sentry-image-exists + sentry-hallmark-populated probes\n4. Commit cadence teaching (sentry)\n5. Kludge bottle step — `buh_tt` with `${RBYC_HANDBOOK_NAMEPLATE_REF}`\n6. Bottle-image-exists + bottle-hallmark-populated probes\n7. Commit cadence teaching (bottle)\n8. Charge section — brief sentry/pentacle/bottle mention (architecture deep-dive belongs in evaluator tail)\n9. Charge step — `buh_tt` with moniker interpolation\n10. Crucible-charged probe\n\nProbe discrimination works naturally — probes key off the `z_sentry_vessel` / `z_bottle_vessel` bash args, which differ per nameplate.\n\n### Shared quench contract (`rbhocq_crucible_quench`)\n\nCaller signature:\n\n```\nrbhocq_crucible_quench \"${z_moniker}\"\n```\n\nQuench body renders:\n\n1. Section header: \"Quench the crucible\"\n2. Brief teaching: stops containers, tears down enclave network\n3. Quench tabtarget call — `buh_tt` with moniker interpolation\n4. **Charge-auto-quench pedagogy at its natural moment:** \"you don't need to quench between iterations — charge tears down prior state for you; quench when you're done.\" (Current location of this insight at `rbhofc:370-371` gets removed from the explorer wrap since the shared quench now carries it for both audiences.)\n\n### Caller structure (both handbooks)\n\n```\n1. zrbho_sentinel\n2. buc_doc_brief / buc_doc_shown\n3. Authorial bash: local -r z_moniker=<nameplate>\n                   z_sentry_vessel=...\n                   z_bottle_vessel=...\n                   z_nameplate_file=...\n4. Audience-framing header prose (intimate voice)\n5. Export teaching line (using ${RBYC_HANDBOOK_NAMEPLATE_NAME})\n6. Docker gate probe + early-return on failure\n7. rbhoct_crucible_trunk \"${z_moniker}\" \"${z_sentry_vessel}\" \"${z_bottle_vessel}\" \"${z_nameplate_file}\"\n8. Verification ritual (intimate — curl for ccyolo, architecture + theurge for tadmor)\n9. rbhocq_crucible_quench \"${z_moniker}\"\n10. Wrap prose (iteration loop for ccyolo, next-tests guidance for tadmor)\n11. Return-to-start buh_tt\n```\n\n### Evaluator tail content shape (rich)\n\nThe evaluator tail is substantially richer than the explorer's. Do not under-scope. Three parts:\n\n**Architecture deep-dive** — not the brief charge-time mention the trunk carries; a real tour:\n\n- Sentry layer: iptables kernel rules + dnsmasq DNS layer — why both layers, what each enforces, what happens when either fails\n- Pentacle layer: network namespace ownership; why a dedicated container rather than bottle-side namespace manipulation\n- Bottle layer: unprivileged workload context; lack of network primitives means no circumvention path\n- Defense-in-depth across the three containers\n\n**Ifrit/Theurge methodology intro:**\n\n- Ifrit is the attack binary delivered into the bottle\n- Claude Code sessions with full visibility authored the attack suite against known primitives\n- Theurge coordinates inside (bottle) and outside (host) observation to verify attacks fail\n- Test results show both the attack tried AND the sandbox response\n\n**Fixture invocation + result inspection:**\n\n- Full fixture (teach as the default pass): `tt/rbtd-r.Run.tadmor-security.sh` — 34 cases, exercises the complete containment attack surface against a kludged tadmor\n- Single case (teach for iterative debugging / deep-dive on one attack): `tt/rbtd-s.SingleCase.tadmor-security.sh [case-name]`\n- Result reading: teach the learner how to interpret pass/fail output, what a green case means, what a red case would surface\n\n**Strictly no cloud-build or global-suite invocations.** Do not reference `rbtd-s.TestSuite.crucible.sh` (pulls in moriah, which is cloud-built), `rbtd-s.TestSuite.complete.sh`, or the service suite. If `rbtd-r.Run.tadmor-security.sh` / `rbtd-s.SingleCase.tadmor-security.sh` carry framing that pulls in global theurge-apparatus prose the handbook shouldn't teach, create thin custom wrappers under a new colophon family rather than redirecting learners to the raw rbtd tabtargets.\n\n### Explorer tail content shape (preserve)\n\nPreserve `rbhofc`'s existing tail content, lightly edited:\n\n- SSH into bottle via `tt/rbw-cS.SshTo.ccyolo.sh`\n- Claude Code OAuth copy-paste teaching (preserve the v2.1.89 paste-regression warning at current lines 275-279)\n- Manual curl verification ritual (api.anthropic.com → 404, claude.ai → 403, www.internic.net → 200, google.com → timeout, registry.npmjs.org → timeout)\n- `rbw-cr.Rack` diagnostic fallback guidance\n- Iteration-loop summary — **minus** the charge-auto-quench insight (moved into shared quench where it serves both audiences)\n\n### Validation\n\n- `rbhofc` dry-render matches current output closely (small acceptable deltas where shared quench surfaces the auto-quench insight earlier than current iteration-loop summary)\n- `rbhots` dry-render produces a coherent evaluator tour: architecture + methodology + fixture invocation + result inspection\n- Probes discriminate correctly by nameplate — no false greens/reds (vessel names are the discriminator; already in authorial bash args)\n- Zero hardcoded nameplate sigils outside the three authorial declarations per handbook\n- Grep sweep for `HANDBOOK_NAMEPLATE` raw literal in yelp args across `Tools/rbk/` returns zero (all consumption via `${RBYC_HANDBOOK_NAMEPLATE_REF}`)\n- Zipper entry for `rbw-Ots` present in `RBZ__GROUP_ONBOARDING`; launcher wired; tabtarget file exists and dispatches correctly\n- `rbhoct` and `rbhocq` sourced/kindled correctly from both callers\n- Fast qualify passes\n- No theurge invocations in handbook content call cloud builds or global test suites (verified by grep for `TestSuite.crucible`, `TestSuite.complete`, `TestSuite.service`)\n\n### Requires\n\n₢A6AA3 (landed: `f131f83f`, `33f8d192`, `0bdd62e9`) — installed `RBYC_HANDBOOK_*` kindle constants in `rbyc_common.sh` and proved consumption pattern across 9 sites in `rbhodf_director_first_build.sh`. This pace's consumption follows the same shape: `${RBYC_HANDBOOK_NAMEPLATE_NAME}` in export-teaching lines, `${RBYC_HANDBOOK_NAMEPLATE_REF}` in buh_tt arg positions.\n\n### Out of scope\n\n- **Paddock Rough Track Roster update** (track 3 ↔ track 14 shared-trunk annotation, track 14 promotion from placeholder to implemented) — deferred to a later pace per user direction.\n- Changes to the kindle constants themselves (shape is settled).\n- Re-deciding infrastructure channel (env var + authorial bash args is the established pattern).\n- Any other handbook beyond `rbhofc` and the new `rbhots`.\n- New theurge test cases or test suite changes.\n- Nameplate regime changes or new nameplates.\n- ccyolo/tadmor vessel Dockerfile changes.\n- Cloud-build or global-suite theurge invocations (explicitly prohibited in handbook content).\n\n### Interview origin\n\nDesign conversation `☉260420-1014` landed the (B′) decision and all structural/naming choices: file names (`rbhoct`, `rbhocq`, `rbhots`), tabtarget name (`rbw-Ots`), shared-helper contracts, tail-depth asymmetry, cloud-build exclusion. Implementation agent should proceed without re-opening these decisions.",
              "jjgtn_text": "## Character\n\nPaddock-level design conversation followed by mechanical refactor. The infrastructure question that originally made this pace uncertain is now settled by ₢A6AA3 — the `RBYC_HANDBOOK_*` kindle constants exist in `rbyc_common.sh` and the consumption pattern is proven in `rbhodf_director_first_build.sh`. What remains in this pace is pure pedagogy: should tracks 3 (ccyolo) and 14 (tadmor) merge, stay split, or share a trunk with split tails?\n\nCognitive posture: the interview phase has a narrower focus than before (pedagogy only, not infrastructure); the implementation phase has a concrete prescription to follow (consume `RBYC_HANDBOOK_NAMEPLATE_*` constants; bash-side `${z_moniker}` / `${z_bottle_vessel}` for authorial/display-text sites; no new patterns to invent).\n\n## Docket\n\n### Problem\n\nThe First Crucible handbook `rbhofc_first_crucible.sh` is hardcoded to the ccyolo nameplate at lines 47-49:\n\n```\nlocal -r z_moniker=\"ccyolo\"\nlocal -r z_sentry_vessel=\"rbev-sentry-deb-tether\"\nlocal -r z_bottle_vessel=\"rbev-bottle-ccyolo\"\n```\n\nThe paddock's Rough Track Roster splits ccyolo (track 3 \"Kindle Your First Crucible\") from tadmor (track 14 \"Assay: Containment\"). But ccyolo and tadmor share more than they differ:\n\n- Same sentry vessel (`rbev-sentry-deb-tether`)\n- Kludge-only hallmark path (no cloud build, no SA credentials required for trunk)\n- Identical charge/quench/ssh choreography\n- Same nameplate-file probe shape\n- Same fast-cycling learner experience\n\nWhat differs:\n\n- Bottle vessel (`rbev-bottle-ccyolo` vs `rbev-bottle-ifrit-tether`)\n- Post-charge activity:\n  - **ccyolo**: ssh in, use Claude Code locally, attempt manual escapes by hand (pedagogy: experience the sandbox as a user)\n  - **tadmor**: run the full adversarial test suite via theurge (pedagogy: experience the sandbox through automated attack)\n\n### Design options\n\nThree approaches surfaced in the ₢A6AA3 conversation from which this pace was spawned:\n\n**(A) Full merge.** One handbook handles both nameplates. Shared trunk (kludge sentry, kludge bottle, charge, verify containment primitives); tail splits into nameplate-specific substeps. Retire track 14 as a distinct handbook — it becomes the tadmor tail of track 3.\n\n**(B) Split / status quo.** Keep tracks 3 and 14 separate. Each handbook speaks in its own voice (explorer vs evaluator). Accept the duplication in the common trunk as the price of audience separation.\n\n**(C) Middle path.** Shared-trunk handbook ends in a \"pick your next adventure\" menu pointing to nameplate-specific tails — either inline as substeps, or as separate tabtargets invoked with the nameplate selected.\n\n### Tradeoffs\n\n| Option | Pro | Con |\n|--------|-----|-----|\n| (A) Merge | Shared trunk; parallel teaching moment (\"same dance, different bottles\"); one file to maintain | Long handbook; risks addressing explorer and evaluator audiences in one voice |\n| (B) Split | Each handbook in right voice; audiences stay separated | Duplicated trunk prose; maintenance cost; loses the teaching parallel |\n| (C) Middle | Shared trunk without voice collapse; tails as long as needed | More surface area (menu + tail tabtargets); more to get right structurally |\n\n### Interview phase\n\nThe infrastructure question is settled (`HANDBOOK_NAMEPLATE` via `RBYC_HANDBOOK_NAMEPLATE_*` kindle constants, consumption pattern proven in rbhodf). The interview phase is pedagogical only.\n\n1. Review tradeoffs with user. The central question: do ccyolo's \"manual escape by hand\" and tadmor's \"theurge suite\" share enough pedagogy to live under one framing, or are the framings (explorer vs evaluator) genuinely distinct enough to warrant separation?\n2. Pick option (A), (B), or (C).\n3. Update paddock Rough Track Roster accordingly (merge tracks, annotate relationship, or confirm status quo with stronger rationale).\n\n### Implementation phase\n\nConditional on interview outcome.\n\n**If (A) or (C):** parameterize `rbhofc_first_crucible.sh` by nameplate selection, consuming the kindle constants installed in ₢A6AA3.\n\n- Teach the learner to `export HANDBOOK_NAMEPLATE=<ccyolo|tadmor>` at the top of the handbook, exactly as rbhodf teaches `export HANDBOOK_VESSEL=...`. In authorial code:\n  - `buh_code \"   export ${RBYC_HANDBOOK_NAMEPLATE_NAME}=<value>\"` in the teaching line — renders `export HANDBOOK_NAMEPLATE=<value>`.\n  - `buh_tt \"...\" \"...\" \"\" \" ${RBYC_HANDBOOK_NAMEPLATE_REF}\"` in tabtarget arg positions — renders `${HANDBOOK_NAMEPLATE}` for learner shell expansion.\n- The authorial bash variables (`z_moniker`, `z_sentry_vessel`, `z_bottle_vessel`) remain — they encode per-nameplate facts the handbook branches on (e.g., which bottle image to kludge). Dispatch options:\n  - Param1 channel (one tabtarget per nameplate, identical to `rbw-cC.Charge.<nameplate>.sh` pattern), or\n  - A nameplate-selection probe at handbook entry driven by an existing `HANDBOOK_NAMEPLATE` env var.\n  The choice comes from the interview phase (pedagogy decides it, not mechanics).\n- Add tadmor tail: theurge suite invocation (`rbtd-s.TestSuite.crucible.sh` or equivalent entry point) + result inspection teaching. Target fixture: `tadmor-security`.\n- Preserve ccyolo tail (ssh in via `rbw-cS.SshTo.ccyolo.sh`, Claude Code OAuth copy-paste, manual escape attempt prose) as-is or lightly edited.\n- Update probes to discriminate by nameplate where needed — verify via `rbtd-s.SingleCase.*` if a specific fixture case helps gate the tadmor tail.\n\n**If (B):** paddock-only work.\n\n- Strengthen rationale in paddock for why tracks 3 and 14 stay separate.\n- No code changes to rbhofc.\n- Track 14 implementation (tadmor handbook as a separate tabtarget consuming the same kindle constants) becomes a future pace.\n\n### Validation\n\n- Dry render produces sensible output for ccyolo (matches current behavior or improves it clearly).\n- Dry render (option A/C) produces sensible output for tadmor.\n- Probes discriminate correctly by nameplate (no false greens/reds).\n- Zero hardcoded nameplate sigils outside the authorial declarations.\n- Paddock Rough Track Roster updated to reflect the chosen option.\n- Fast qualify passes.\n- Grep sweep for learner-facing env var literals like `HANDBOOK_NAMEPLATE` in yelp args (raw form) returns zero — all such references go through `${RBYC_HANDBOOK_NAMEPLATE_REF}`.\n\n### Requires\n\n₢A6AA3 (landed: `f131f83f`, `33f8d192`, `0bdd62e9`). That pace installed the `RBYC_HANDBOOK_*` kindle constants in `rbyc_common.sh` and demonstrated the consumption pattern across 9 sites in `rbhodf_director_first_build.sh`. This pace's implementation phase consumes the `_NAMEPLATE_NAME` / `_NAMEPLATE_REF` variants and follows the same consumption shape.\n\n### Out of scope\n\n- Changes to the kindle constants themselves (shape is settled: `_NAME` + `_REF` pairing, `HANDBOOK_` prefix).\n- Re-deciding infrastructure channel (env var vs param1 vs menu was never really the open question — the env var plus a dispatch helper is the established pattern; pedagogy picks among the dispatch variants within that family).\n- Any other handbook (only `rbhofc_first_crucible.sh` is in this pace's scope).\n- New theurge test cases (tadmor uses the existing suite).\n- Nameplate regime changes or new nameplates.\n- ccyolo/tadmor vessel Dockerfile changes.\n- Family-wide migration sweep of `ONBOARD_*` references (none remain after ₢A6AA3).",
              "jjgtn_text": "## Character\n\nDesign conversation requiring judgment, followed by mechanical refactor. First phase is an interview decision: merge, split, or middle-path for ccyolo + tadmor coverage in the First Crucible handbook. Second phase implements whichever option the interview selects. Paddock revision expected — track 3 (Kindle Your First Crucible) and track 14 (Assay: Containment) may reorganize.\n\nCognitive posture: patient exploration in the interview phase (don't rush to one option; understand the pedagogy); mechanical precision in the implementation phase (preserve ccyolo's current teaching voice, add tadmor's without disrupting it).\n\n## Docket\n\n### Problem\n\nThe First Crucible handbook `rbhofc_first_crucible.sh` is hardcoded to the ccyolo nameplate at lines 47-49:\n\n```\nlocal -r z_moniker=\"ccyolo\"\nlocal -r z_sentry_vessel=\"rbev-sentry-deb-tether\"\nlocal -r z_bottle_vessel=\"rbev-bottle-ccyolo\"\n```\n\nThe paddock's Rough Track Roster splits ccyolo (track 3 \"Kindle Your First Crucible\") from tadmor (track 14 \"Assay: Containment\"). But ccyolo and tadmor share more than they differ:\n\n- Same sentry vessel (`rbev-sentry-deb-tether`)\n- Kludge-only hallmark path (no cloud build, no SA credentials required for trunk)\n- Identical charge/quench/ssh choreography\n- Same nameplate-file probe shape\n- Same fast-cycling learner experience\n\nWhat differs:\n\n- Bottle vessel (`rbev-bottle-ccyolo` vs `rbev-bottle-ifrit-tether`)\n- Post-charge activity:\n  - **ccyolo**: ssh in, use Claude Code locally, attempt manual escapes by hand (pedagogy: experience the sandbox as a user)\n  - **tadmor**: run the full adversarial test suite via theurge (pedagogy: experience the sandbox through automated attack)\n\n### Design options\n\nThree approaches surfaced in the ₢A6AA3 conversation from which this pace was spawned:\n\n**(A) Full merge.** One handbook handles both nameplates. Shared trunk (kludge sentry, kludge bottle, charge, verify containment primitives); tail splits into nameplate-specific substeps. Retire track 14 as a distinct handbook — it becomes the tadmor tail of track 3.\n\n**(B) Split / status quo.** Keep tracks 3 and 14 separate. Each handbook speaks in its own voice (explorer vs evaluator). Accept the duplication in the common trunk as the price of audience separation.\n\n**(C) Middle path.** Shared-trunk handbook ends in a \"pick your next adventure\" menu pointing to nameplate-specific tails — either inline as substeps, or as separate tabtargets invoked with the nameplate selected.\n\n### Tradeoffs\n\n| Option | Pro | Con |\n|--------|-----|-----|\n| (A) Merge | Shared trunk; parallel teaching moment (\"same dance, different bottles\"); one file to maintain | Long handbook; risks addressing explorer and evaluator audiences in one voice |\n| (B) Split | Each handbook in right voice; audiences stay separated | Duplicated trunk prose; maintenance cost; loses the teaching parallel |\n| (C) Middle | Shared trunk without voice collapse; tails as long as needed | More surface area (menu + tail tabtargets); more to get right structurally |\n\n### Interview phase\n\n1. Review tradeoffs with user; probe whether ccyolo's \"manual escape by hand\" and tadmor's \"theurge suite\" share enough pedagogy to live under the same framing, or whether the framings are genuinely distinct.\n2. Pick option (A), (B), or (C).\n3. Update paddock Rough Track Roster accordingly (merge tracks, annotate relationship, or confirm status quo with stronger rationale).\n4. If chosen option implies a new pattern for other multi-nameplate handbooks, capture that as a paddock principle.\n\n### Implementation phase\n\nConditional on interview outcome.\n\n**If (A) or (C):**\n\n- Parameterize `rbhofc_first_crucible.sh` by nameplate selection. Channel options: param1 to tabtarget (one tabtarget per nameplate, like `rbw-cC.Charge.{nameplate}.sh`), or a learner-facing env var (e.g., `HANDBOOK_NAMEPLATE` per the earlier muse), or a menu-driven selection at handbook start.\n- Replace hardcoded `ccyolo`/`rbev-bottle-ccyolo` literals with variable references; decide bash-side vs shell-side substitution per the pattern established in ₢A6AA3.\n- Add tadmor tail: theurge suite invocation + result inspection teaching.\n- Preserve ccyolo tail (ssh in + Claude Code auth + manual escape attempt) as-is or lightly edited.\n- Update probes to discriminate by nameplate where needed.\n\n**If (B):**\n\n- Enhance rationale in paddock for why tracks stay separate.\n- No code changes to rbhofc.\n- Track 14 implementation (tadmor handbook as separate tabtarget) remains a future pace.\n\n### Validation\n\n- Dry render produces sensible output for ccyolo (matches current behavior or improves it clearly).\n- Dry render (option A/C) produces sensible output for tadmor.\n- Probes discriminate correctly by nameplate (no false greens/reds).\n- Zero hardcoded nameplate sigils outside the authorial declaration(s).\n- Paddock Rough Track Roster updated to reflect the chosen option.\n- Fast qualify passes.\n\n### Requires\n\n- ₢A6AA3 (handbook-inline-identifier-hygiene) complete. Establishes the pattern for `${z_vessel}` vs `${ONBOARD_VESSEL}` substitution at teaching sites; its outcome directly informs parameterization choices here (bash-side vs shell-side; ONBOARD_* vs HANDBOOK_* naming).\n\n### Out of scope\n\n- Changes to `rbhodf_director_first_build.sh` or any other handbook.\n- New theurge test cases (tadmor uses the existing suite).\n- Nameplate regime changes or new nameplates.\n- ccyolo/tadmor vessel Dockerfile changes.\n- Family-wide rename of `ONBOARD_VESSEL` → `HANDBOOK_VESSEL` (revisit later if pattern warrants).",
              "jjgtn_text": "## Character\n\nMechanical implementation once the contract is locked. Two validations and a regime field rewrite — no cross-cutting refactors, no new concept work. The design conversation concluded in a separate onboarding-study session; this pace is the code delivery. Cognitive posture: precise, contract-driven, no speculative generality. Do not re-open design questions already settled below.\n\n## Context\n\n`tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` Step 1 currently ends with a manual-edit instruction to the learner:\n\n    Open rbev-sentry-deb-tether/rbrv.env and set the field,\n    then commit the change.\n\nThis is a teaching seam. A newcomer holding a fresh reliquary datestamp is exactly the person who will typo it, then discover the typo ~15-20 minutes later when a Conjure preflight fails. The remedy is a tabtarget that accepts the stamp and vessel as arguments, validates both, and rewrites the `RBRV_RELIQUARY` line safely.\n\n## Design decisions locked (do not re-litigate)\n\n- **Fact-file capture rejected.** `output-buk/current/` is cleared on every tabtarget entry; intermediate state there vanishes before the next command runs. The stamp MUST come from an explicit argument.\n- **Global reliquary (RBRR_RELIQUARY) rejected.** Per-vessel `RBRV_RELIQUARY` stays per-vessel.\n- **List-valid-reliquaries subcommand deferred.** If validation fails, the error message names the missing bits; a separate list verb is a future improvement, out of scope for this pace.\n- **Verb is Yoke.** Y is unencumbered across Director actions (current taken: E, I, O, A, V, J, r, w). \"Yoke A to B\" is inherently relational, mirroring the two-argument signature. No phonetic or semantic rhyme with Enshrine.\n- **Auto-commit rejected.** Learner commits afterward, matching the Kludge cadence.\n\n## Contract\n\nColophon `rbw-dY`, frontispiece `DirectorYokesVesselToReliquary`, no imprint. Two positional args (not a single-folio channel; the BUK `imprint`/`param1`/empty vocabulary does not apply here — enroll as a two-param tabtarget per whatever mechanism the zipper supports, or extend the channel if it does not).\n\nInvocation shape:\n\n    tt/rbw-dY.DirectorYokesVesselToReliquary.sh <reliquary-stamp> <vessel>\n\n### Argument order — load-bearing\n\nStamp FIRST, vessel SECOND. Rationale: keeps the door open to a future \"yoke one stamp across multiple vessels\" variant without breaking existing callers. Do NOT reverse the order for ergonomic reasons.\n\n### Behavior\n\n1. **Validate the reliquary stamp.** Confirm all four tool images (skopeo, docker, gcloud, syft) are present in GAR under the expected reliquary path for the given stamp. Reuse the path logic that `tt/rbw-dI.DirectorInscribesReliquary.sh` uses when it writes those images — the inscribe path template is the source of truth. On missing-image(s), error naming the stamp AND the specific missing image name(s).\n\n2. **Validate the vessel.** Confirm:\n   - Vessel regime directory exists at the expected location\n   - `rbrv.env` renders and validates (equivalent to what `tt/rbw-rvv.ValidateVesselRegime.sh <vessel>` checks)\n   - Vessel's mode is one that consumes a reliquary — `conjure` and `airgap` accept yoking; `bind` and `graft` do not (bind pins an upstream digest, graft pushes a local build — neither consumes a reliquary). Error clearly if mode is incompatible.\n\n3. **Both validations must pass before any write.** No partial state. If the stamp is valid but the vessel is not (or vice versa), error and exit without modifying any file.\n\n4. **Rewrite `RBRV_RELIQUARY=<stamp>` in the vessel's `rbrv.env`.** Preserve the rest of the file exactly. In-place edit; do NOT regenerate the regime file via a renderer. If the line is absent, add it in an appropriate location; if present, replace the value only.\n\n5. **Report clearly.**\n   - Before write: `Yoking <vessel> to reliquary <stamp>`\n   - After write: path to the modified file and the new line content\n   - Learner commits with the usual git cadence afterward\n\n### Validation errors must be actionable\n\nSample shapes (not literal strings — match the surrounding operator-message voice):\n\n- `Reliquary stamp 'r260324193327' not found in Depot — expected 4 tool images under <GAR path>; missing: skopeo, gcloud. Re-run tt/rbw-dI.DirectorInscribesReliquary.sh to mint a fresh reliquary, or verify the stamp spelling.`\n- `Vessel 'rbev-bottle-plantuml' has mode=bind; Yoke only applies to conjure and airgap vessels. Bind vessels pin an upstream digest; graft vessels push a local build — neither consumes a reliquary.`\n- `Vessel 'rbev-does-not-exist' not found — expected regime at <path>.`\n\nNo stack traces to the learner; operator-grade messages only.\n\n## What NOT to do\n\n- Do not cache or short-circuit validation (e.g., \"skip if stamp looks like a datestamp\"). The tabtarget's value IS the validation.\n- Do not add a `--force` flag. If validation fails, the fix is to inscribe or correct the input, not to bypass.\n- Do not auto-commit.\n- Do not add list-valid-reliquaries as a side quest; deferred.\n- Do not modify any file other than the target vessel's `rbrv.env`.\n- Do not relitigate argument order, verb choice, or scope.\n\n## Handbook update (same pace)\n\nEdit `tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` Step 1. Currently ends with the manual-edit instruction quoted in Context above. Replace with the Yoke invocation:\n\n    tt/rbw-dY.DirectorYokesVesselToReliquary.sh <reliquary-stamp> rbev-sentry-deb-tether\n\nplus a short line reminding the learner to commit `rbrv.env` afterward. Adjust surrounding prose as needed — the step can be a touch shorter.\n\nSweep the other Director subtracks (`rbw-Oda`, `rbw-Odb`, `rbw-Odg`) for the same pattern. Oda in particular does NOT appear to re-yoke (it assumes Odf ran first; the airgap vessel inherits the stamp) — verify rather than assume.\n\n## Registry updates\n\n- Zipper enrollment for the new tabtarget — see `Tools/rbk/rbz_zipper.sh` for the pattern; the new entry belongs in whichever group owns Director Depot/Foundry actions. Follow the existing `rbw-dI` / `rbw-dE` enrollment as the template.\n- Entry in `Tools/rbk/rbk-claude-tabtarget-context.md` under the Depot section: `rbw-dY | DirectorYokesVesselToReliquary | param1+param2 | Yoke vessel's regime to a specific reliquary stamp`.\n- Any colophon registry in RBS0 / RBDC / CLAUDE.md that enumerates Director actions.\n- `Tools/rbk/vov_veiled/` — check for a spec file covering the Yoke action (pattern: `RBSAY-ark_yoke.adoc` or similar). Create if that pattern is the norm for Director-action specs; otherwise leave for a later spec-hygiene sweep.\n\n## Done criteria\n\n- Tabtarget exists; validates stamp and vessel before any write; errors diagnostically; rewrites cleanly on success.\n- `rbw-Odf` Step 1 teaches Yoke instead of the manual edit.\n- Other Director subtracks checked for the same pattern and updated if needed.\n- `tt/rbw-tf.QualifyFast.sh` green (tabtarget discovery, colophon validation, nameplate health).\n- Registry / context files updated.\n\n## References\n\n- `tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` — seam location (Step 1)\n- `tt/rbw-dI.DirectorInscribesReliquary.sh` — source of GAR reliquary path template\n- `tt/rbw-rvv.ValidateVesselRegime.sh` — vessel regime validator (reusable check)\n- `tt/rbw-dE.DirectorEnshrinesVessel.sh` — the analogous self-driving pattern for `RBRV_IMAGE_N_ANCHOR`; Yoke is the explicit (human-chosen) counterpart since reliquary stamps cannot be auto-picked\n- `CLAUDE.md` — tabtarget naming conventions, zipper enrollment, BUK folio channel vocabulary\n- Design provenance: onboarding-study session ☉260423-1024",
              "jjgtn_text": "## Character\n\nMechanical implementation once the contract is locked. Two validations and a regime field rewrite — no cross-cutting refactors, no new concept work. The design conversation concluded in a separate onboarding-study session; this pace is the code delivery. Cognitive posture: precise, contract-driven, no speculative generality. Do not re-open design questions already settled below.\n\n## Context\n\n`tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` Step 1 currently ends with a manual-edit instruction to the learner:\n\n    Open rbev-sentry-deb-tether/rbrv.env and set the field,\n    then commit the change.\n\nThis is a teaching seam. A newcomer holding a fresh reliquary datestamp is exactly the person who will typo it, then discover the typo ~15-20 minutes later when a Conjure preflight fails. The remedy is a tabtarget that accepts the stamp and vessel as arguments, validates both, and rewrites the `RBRV_RELIQUARY` line safely.\n\n## Design decisions locked (do not re-litigate)\n\n- **Fact-file capture rejected.** `output-buk/current/` is cleared on every tabtarget entry; intermediate state there vanishes before the next command runs. The stamp MUST come from an explicit argument.\n- **Global reliquary (RBRR_RELIQUARY) rejected.** Per-vessel `RBRV_RELIQUARY` stays per-vessel.\n- **List-valid-reliquaries subcommand deferred.** If validation fails, the error message names the missing bits; a separate list verb is a future improvement, out of scope for this pace.\n- **Verb is Yoke.** Y is unencumbered across Director actions (current taken: E, I, O, A, V, J, r, w). \"Yoke A to B\" is inherently relational, mirroring the two-argument signature. No phonetic or semantic rhyme with Enshrine.\n- **Auto-commit rejected.** Learner commits afterward, matching the Kludge cadence.\n\n## Contract\n\nColophon `rbw-dY`, frontispiece `DirectorYokesVesselToReliquary`, no imprint. Two positional args (not a single-folio channel; the BUK `imprint`/`param1`/empty vocabulary does not apply here — enroll as a two-param tabtarget per whatever mechanism the zipper supports, or extend the channel if it does not).\n\nInvocation shape:\n\n    tt/rbw-dY.DirectorYokesVesselToReliquary.sh <reliquary-stamp> <vessel>\n\n### Argument order — load-bearing\n\nStamp FIRST, vessel SECOND. Rationale: keeps the door open to a future \"yoke one stamp across multiple vessels\" variant without breaking existing callers. Do NOT reverse the order for ergonomic reasons.\n\n### Behavior\n\n1. **Validate the reliquary stamp.** Confirm all four tool images (skopeo, docker, gcloud, syft) are present in GAR under the expected reliquary path for the given stamp. Reuse the path logic that `tt/rbw-dI.DirectorInscribesReliquary.sh` uses when it writes those images — the inscribe path template is the source of truth. On missing-image(s), error naming the stamp AND the specific missing image name(s).\n\n2. **Validate the vessel.** Confirm:\n   - Vessel regime directory exists at the expected location\n   - `rbrv.env` renders and validates (equivalent to what `tt/rbw-rvv.ValidateVesselRegime.sh <vessel>` checks)\n   - Vessel's mode is one that consumes a reliquary — `conjure` and `airgap` accept yoking; `bind` and `graft` do not (bind pins an upstream digest, graft pushes a local build — neither consumes a reliquary). Error clearly if mode is incompatible.\n\n3. **Both validations must pass before any write.** No partial state. If the stamp is valid but the vessel is not (or vice versa), error and exit without modifying any file.\n\n4. **Rewrite `RBRV_RELIQUARY=<stamp>` in the vessel's `rbrv.env`.** Preserve the rest of the file exactly. In-place edit; do NOT regenerate the regime file via a renderer. If the line is absent, add it in an appropriate location; if present, replace the value only.\n\n5. **Report clearly.**\n   - Before write: `Yoking <vessel> to reliquary <stamp>`\n   - After write: path to the modified file and the new line content\n   - Learner commits with the usual git cadence afterward\n\n### Validation errors must be actionable\n\nSample shapes (not literal strings — match the surrounding operator-message voice):\n\n- `Reliquary stamp 'r260324193327' not found in Depot — expected 4 tool images under <GAR path>; missing: skopeo, gcloud. Re-run tt/rbw-dI.DirectorInscribesReliquary.sh to mint a fresh reliquary, or verify the stamp spelling.`\n- `Vessel 'rbev-bottle-plantuml' has mode=bind; Yoke only applies to conjure and airgap vessels. Bind vessels pin an upstream digest; graft vessels push a local build — neither consumes a reliquary.`\n- `Vessel 'rbev-does-not-exist' not found — expected regime at <path>.`\n\nNo stack traces to the learner; operator-grade messages only.\n\n## What NOT to do\n\n- Do not cache or short-circuit validation (e.g., \"skip if stamp looks like a datestamp\"). The tabtarget's value IS the validation.\n- Do not add a `--force` flag. If validation fails, the fix is to inscribe or correct the input, not to bypass.\n- Do not auto-commit.\n- Do not add list-valid-reliquaries as a side quest; deferred.\n- Do not modify any file other than the target vessel's `rbrv.env`.\n- Do not relitigate argument order, verb choice, or scope.\n\n## Handbook update (same pace)\n\nEdit `tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` Step 1. Currently ends with the manual-edit instruction quoted in Context above. Replace with the Yoke invocation:\n\n    tt/rbw-dY.DirectorYokesVesselToReliquary.sh <reliquary-stamp> rbev-sentry-deb-tether\n\nplus a short line reminding the learner to commit `rbrv.env` afterward. Adjust surrounding prose as needed — the step can be a touch shorter.\n\nSweep the other Director subtracks (`rbw-Oda`, `rbw-Odb`, `rbw-Odg`) for the same pattern. Oda in particular does NOT appear to re-yoke (it assumes Odf ran first; the airgap vessel inherits the stamp) — verify rather than assume.\n\n## Registry updates\n\n- Zipper enrollment for the new tabtarget — see `Tools/rbk/rbz_zipper.sh` for the pattern; the new entry belongs in whichever group owns Director Depot/Foundry actions. Follow the existing `rbw-dI` / `rbw-dE` enrollment as the template.\n- Entry in `Tools/rbk/rbk-claude-tabtarget-context.md` under the Depot section: `rbw-dY | DirectorYokesVesselToReliquary | param1+param2 | Yoke vessel's regime to a specific reliquary stamp`.\n- Any colophon registry in RBS0 / RBDC / CLAUDE.md that enumerates Director actions.\n- `Tools/rbk/vov_veiled/` — check for a spec file covering the Yoke action (pattern: `RBSAY-ark_yoke.adoc` or similar). Create if that pattern is the norm for Director-action specs; otherwise leave for a later spec-hygiene sweep.\n\n## Done criteria\n\n- Tabtarget exists; validates stamp and vessel before any write; errors diagnostically; rewrites cleanly on success.\n- `rbw-Odf` Step 1 teaches Yoke instead of the manual edit.\n- Other Director subtracks checked for the same pattern and updated if needed.\n- `tt/rbw-tf.QualifyFast.sh` green (tabtarget discovery, colophon validation, nameplate health).\n- Registry / context files updated.\n\n## References\n\n- `tt/rbw-Odf.OnboardingDirectorFirstBuild.sh` — seam location (Step 1)\n- `tt/rbw-dI.DirectorInscribesReliquary.sh` — source of GAR reliquary path template\n- `tt/rbw-rvv.ValidateVesselRegime.sh` — vessel regime validator (reusable check)\n- `tt/rbw-dE.DirectorEnshrinesVessel.sh` — the analogous self-driving pattern for `RBRV_IMAGE_N_ANCHOR`; Yoke is the explicit (human-chosen) counterpart since reliquary stamps cannot be auto-picked\n- `CLAUDE.md` — tabtarget naming conventions, zipper enrollment, BUK folio channel vocabulary\n- Design provenance: onboarding-study session ☉260423-1024",
              "jjgtn_text": "## Character\n\nPer-handbook authoring requiring judgment. Not mechanical — each closer is the distilled \"what the reader should walk away with\" for its track, and the right abstraction differs per handbook. A single author handling all four closers will produce more consistent closer shape than parallelizing across files; the convention itself is load-bearing. Cognitive posture: ask \"what generative rule does this handbook teach?\" and surface that rule as the retained takeaway.\n\n## Context\n\n`tt/rbw-Occ.OnboardingConfigureEnvironment.sh` Step 7 (\"The pattern\") is the best single teaching moment in the current onboarding. It shows that all regimes follow `rbw-r{letter}{r|v}` tabtargets and lists them in a table. The reader leaves with a *generative rule*, not a memorized list — they can derive any regime's tools by knowing one letter.\n\nFresh-reader study found that the other handbooks end without equivalent recaps. They walk the learner through a sequence and stop. A closer in Occ's shape would turn each handbook from a walkthrough into a retained mental model.\n\nNot every handbook needs a closer. The Director subtracks (`Odf`, `Oda`, `Odb`) and the primary Crucible track (`Ofc`) each carry a generative rule worth surfacing. Short tracks (`Ocr`, `Ocd`, `Odg`) don't — their whole content is already a short recap.\n\n## Target files\n\nUnder `Tools/rbk/rbh0/`. Locate the sequence function for each handbook (per CLAUDE.md the onboarding decomposition lives there). Per recent commits the file naming pattern is likely `rbho*_*.sh` with one function per file.\n\nHandbooks in scope (in priority order):\n\n- `rbw-Ofc.OnboardingFirstCrucible.sh`\n- `rbw-Odf.OnboardingDirectorFirstBuild.sh`\n- `rbw-Odb.OnboardingDirectorBind.sh`\n- `rbw-Oda.OnboardingDirectorAirgap.sh`\n\n## Closer shape (common across all)\n\nNew section appended after the existing final step. Consistent header voice — e.g., `Step N — The pattern` or `What you now know` (pick one phrasing and apply uniformly across all four; don't mix). Body:\n\n- 2-4 lines of prose naming the rule\n- A compact table or bullet list enacting the rule\n- No probes, no tabtarget invocations to run — pure retained-knowledge content\n- Close with a one-line bridge back to start-menu or related tracks\n\nThe Occ Step 7 body shape is the reference: prose setup → table → one-line closer. Match that rhythm.\n\n## Per-handbook closer content\n\nEach closer's content is distilled per handbook. Do NOT copy a generic template — the whole point is that each track teaches a different generative rule.\n\n### `Ofc` — Crucible lifecycle\n\nFour verbs that describe the full local-crucible iteration loop:\n\n    Kludge → Charge → SSH → Quench\n\nProse should name these as the iteration primitives: once the learner owns the four verbs, they can repeat the loop against any Nameplate without re-reading the handbook. A 4-row table mapping verb → tabtarget → purpose is the right enactment.\n\n### `Odf` — The six arks\n\nEvery Conjured Hallmark produces six artifacts in GAR: `pouch`, `image`, `attest`, `about`, `vouch`, `diags`. These already appear mid-flow in Step 4, which is fine — but the closer should promote them to the retained position as a reference card (a six-row table). The pattern: \"every Hallmark is a structured bundle; know the six basenames and you can tour any Hallmark by hand.\" Consider referencing `Plumb` (full vs compact) as the pattern-driven inspector for these arks.\n\n### `Odb` — Three ordain modes, three trust contracts\n\nThis table already appears mid-flow in `Odb` Step 2 (Conjure / Bind / Graft trust-contract comparison). It is arguably the single best architectural summary in the entire onboarding. The closer's job is to promote it to the retained position — either by moving it to the end or duplicating it there. The rule: \"three modes, three trust contracts, mode-aware Vouch verdicts.\" Consider whether moving it breaks Step 2's flow; if so, duplicate.\n\n### `Oda` — Airgap supply chain\n\nThe three-link chain the handbook already describes in its opening prose:\n\n    1. Enshrine upstream base into Depot\n    2. Conjure forge Tethered, then re-Enshrine\n    3. Conjure final Bottle in Airgap mode\n\nThe closer names the chain as the reusable pattern: any future airgap build follows these three links. Consider surfacing the Tethered/Airgap/Kludged three-way build-info comparison (already in Step 5) as a secondary micro-card if it fits without bloat.\n\n## Consistency constraints across all four closers\n\n- Same header phrasing (pick one and use everywhere).\n- Same rough shape (prose → table/list → one-line closer).\n- Same length budget (~10-15 rendered lines each; don't balloon).\n- Same voice register — matches Occ Step 7's teacher-voice, not step-voice.\n- No probes, no `[*]`/`[ ]` markers (this is pure retained-knowledge content).\n- Closer is the LAST thing the reader sees before the \"return to start\" line; if a handbook has a \"Next steps\" section, the closer precedes it.\n\n## What NOT to do\n\n- Do not author closers for short handbooks (`Ocr`, `Ocd`, `Odg`, `Op`, `Og`, `Ots`) — they don't need them. If a short handbook genuinely would benefit, flag it but do not add speculative closers.\n- Do not invent patterns that aren't already taught in the handbook body. If the pattern isn't in the content, the closer would be introducing new material — out of scope for this pace.\n- Do not merge closers across handbooks. Each track has its own generative rule.\n- Do not restructure existing step content (exception: the `Odb` three-mode table may be moved from Step 2 to the closer if the flow holds; if moving it damages Step 2, duplicate).\n- Do not add cross-track \"see also\" navigation inside closers beyond a single bridge line.\n\n## Done criteria\n\n- Four handbook closers authored and rendering correctly in their respective tabtargets (`Ofc`, `Odf`, `Odb`, `Oda`).\n- Same header phrasing and shape across all four.\n- `tt/rbw-o.ONBOARDING.sh` unchanged by this pace (menu is P1's territory).\n- `tt/rbw-tf.QualifyFast.sh` green.\n- Visual spot-check: each closer renders cleanly under ~100 cols terminal width.\n- Occ Step 7 unchanged — it's the reference shape, not the target.\n\n## References\n\n- `tt/rbw-Occ.OnboardingConfigureEnvironment.sh` Step 7 — reference shape\n- Existing mid-flow patterns worth promoting:\n  - `Odb` Step 2 three-mode trust-contract table\n  - `Odf` Step 4 six-ark artifact listing\n  - `Oda` opening prose three-link chain\n- Onboarding menu module: `Tools/rbk/rbh0/rbho0_start_here.sh` (for context on project voice — do not modify here)\n- Design provenance: onboarding-study session ☉260423-1024",
              "jjgtn_text": "## Character\n\nPer-handbook authoring requiring judgment. Not mechanical — each closer is the distilled \"what the reader should walk away with\" for its track, and the right abstraction differs per handbook. A single author handling all four closers will produce more consistent closer shape than parallelizing across files; the convention itself is load-bearing. Cognitive posture: ask \"what generative rule does this handbook teach?\" and surface that rule as the retained takeaway.\n\n## Context\n\n`tt/rbw-Occ.OnboardingConfigureEnvironment.sh` Step 7 (\"The pattern\") is the best single teaching moment in the current onboarding. It shows that all regimes follow `rbw-r{letter}{r|v}` tabtargets and lists them in a table. The reader leaves with a *generative rule*, not a memorized list — they can derive any regime's tools by knowing one letter.\n\nFresh-reader study found that the other handbooks end without equivalent recaps. They walk the learner through a sequence and stop. A closer in Occ's shape would turn each handbook from a walkthrough into a retained mental model.\n\nNot every handbook needs a closer. The Director subtracks (`Odf`, `Oda`, `Odb`) and the primary Crucible track (`Ofc`) each carry a generative rule worth surfacing. Short tracks (`Ocr`, `Ocd`, `Odg`) don't — their whole content is already a short recap.\n\n## Target files\n\nUnder `Tools/rbk/rbh0/`. Locate the sequence function for each handbook (per CLAUDE.md the onboarding decomposition lives there). Per recent commits the file naming pattern is likely `rbho*_*.sh` with one function per file.\n\nHandbooks in scope (in priority order):\n\n- `rbw-Ofc.OnboardingFirstCrucible.sh`\n- `rbw-Odf.OnboardingDirectorFirstBuild.sh`\n- `rbw-Odb.OnboardingDirectorBind.sh`\n- `rbw-Oda.OnboardingDirectorAirgap.sh`\n\n## Closer shape (common across all)\n\nNew section appended after the existing final step. Consistent header voice — e.g., `Step N — The pattern` or `What you now know` (pick one phrasing and apply uniformly across all four; don't mix). Body:\n\n- 2-4 lines of prose naming the rule\n- A compact table or bullet list enacting the rule\n- No probes, no tabtarget invocations to run — pure retained-knowledge content\n- Close with a one-line bridge back to start-menu or related tracks\n\nThe Occ Step 7 body shape is the reference: prose setup → table → one-line closer. Match that rhythm.\n\n## Per-handbook closer content\n\nEach closer's content is distilled per handbook. Do NOT copy a generic template — the whole point is that each track teaches a different generative rule.\n\n### `Ofc` — Crucible lifecycle\n\nFour verbs that describe the full local-crucible iteration loop:\n\n    Kludge → Charge → SSH → Quench\n\nProse should name these as the iteration primitives: once the learner owns the four verbs, they can repeat the loop against any Nameplate without re-reading the handbook. A 4-row table mapping verb → tabtarget → purpose is the right enactment.\n\n### `Odf` — The six arks\n\nEvery Conjured Hallmark produces six artifacts in GAR: `pouch`, `image`, `attest`, `about`, `vouch`, `diags`. These already appear mid-flow in Step 4, which is fine — but the closer should promote them to the retained position as a reference card (a six-row table). The pattern: \"every Hallmark is a structured bundle; know the six basenames and you can tour any Hallmark by hand.\" Consider referencing `Plumb` (full vs compact) as the pattern-driven inspector for these arks.\n\n### `Odb` — Three ordain modes, three trust contracts\n\nThis table already appears mid-flow in `Odb` Step 2 (Conjure / Bind / Graft trust-contract comparison). It is arguably the single best architectural summary in the entire onboarding. The closer's job is to promote it to the retained position — either by moving it to the end or duplicating it there. The rule: \"three modes, three trust contracts, mode-aware Vouch verdicts.\" Consider whether moving it breaks Step 2's flow; if so, duplicate.\n\n### `Oda` — Airgap supply chain\n\nThe three-link chain the handbook already describes in its opening prose:\n\n    1. Enshrine upstream base into Depot\n    2. Conjure forge Tethered, then re-Enshrine\n    3. Conjure final Bottle in Airgap mode\n\nThe closer names the chain as the reusable pattern: any future airgap build follows these three links. Consider surfacing the Tethered/Airgap/Kludged three-way build-info comparison (already in Step 5) as a secondary micro-card if it fits without bloat.\n\n## Consistency constraints across all four closers\n\n- Same header phrasing (pick one and use everywhere).\n- Same rough shape (prose → table/list → one-line closer).\n- Same length budget (~10-15 rendered lines each; don't balloon).\n- Same voice register — matches Occ Step 7's teacher-voice, not step-voice.\n- No probes, no `[*]`/`[ ]` markers (this is pure retained-knowledge content).\n- Closer is the LAST thing the reader sees before the \"return to start\" line; if a handbook has a \"Next steps\" section, the closer precedes it.\n\n## What NOT to do\n\n- Do not author closers for short handbooks (`Ocr`, `Ocd`, `Odg`, `Op`, `Og`, `Ots`) — they don't need them. If a short handbook genuinely would benefit, flag it but do not add speculative closers.\n- Do not invent patterns that aren't already taught in the handbook body. If the pattern isn't in the content, the closer would be introducing new material — out of scope for this pace.\n- Do not merge closers across handbooks. Each track has its own generative rule.\n- Do not restructure existing step content (exception: the `Odb` three-mode table may be moved from Step 2 to the closer if the flow holds; if moving it damages Step 2, duplicate).\n- Do not add cross-track \"see also\" navigation inside closers beyond a single bridge line.\n\n## Done criteria\n\n- Four handbook closers authored and rendering correctly in their respective tabtargets (`Ofc`, `Odf`, `Odb`, `Oda`).\n- Same header phrasing and shape across all four.\n- `tt/rbw-o.ONBOARDING.sh` unchanged by this pace (menu is P1's territory).\n- `tt/rbw-tf.QualifyFast.sh` green.\n- Visual spot-check: each closer renders cleanly under ~100 cols terminal width.\n- Occ Step 7 unchanged — it's the reference shape, not the target.\n\n## References\n\n- `tt/rbw-Occ.OnboardingConfigureEnvironment.sh` Step 7 — reference shape\n- Existing mid-flow patterns worth promoting:\n  - `Odb` Step 2 three-mode trust-contract table\n  - `Odf` Step 4 six-ark artifact listing\n  - `Oda` opening prose three-link chain\n- Onboarding menu module: `Tools/rbk/rbh0/rbho0_start_here.sh` (for context on project voice — do not modify here)\n- Design provenance: onboarding-study session ☉260423-1024",
              "jjgtn_text": "## Character\n\nComprehensive foundation-landing pace for the handbook restart. Absorbs what was previously split across `₢A6AAA` (drafts) and the now-dropped `₢A6AAB` (implementation). Produces a first-iteration working implementation of the two foundation tabtargets — the probe-aware StartHere menu and the universal Crash Course handbook — plus smoke testing across three repo states and a prototype agent-learner eval.\n\nLow-novelty in mechanism (same bash idioms, same `buh_*` primitives, same dispatch chain as the existing `rbho_*` sequences). High-novelty in pedagogical framing: first deployed artifacts of the Frame 4-refined handbook restart, first deliberate break from role-decomposition as the primary organizing axis. Review happens against the running code — user executes the tabtargets, reads the output, annotates what works and what doesn't, approves or requests revisions.\n\n## Status — as-built (resumption anchor)\n\nThis section is a snapshot of where the pace actually stands, intended as a pickup anchor for a fresh mounting session. The Deliverables below describe the *plan*; this section describes what has actually happened.\n\n### Initial implementation landed\n\nCommit `9844ce32` — *\"land handbook-restart foundation: rbho_start_here + rbho_crash_course sequences, tt/rbw-o and tt/rbw-Occ dispatchers, RBZ__GROUP_ONBOARDING zipper group + two enrollments\"*.\n\nFiles touched in that commit:\n- `Tools/rbk/rbho_onboarding.sh` — added `rbho_start_here()` and `rbho_crash_course()` at a new \"Handbook restart — foundation\" banner section near the end of the file\n- `Tools/rbk/rbz_zipper.sh` — added `RBZ__GROUP_ONBOARDING` group with two enrollments, positioned between the existing Guide and Hallmark groups\n- `Tools/rbk/rbk-claude-tabtarget-context.md` — regenerated from zipper via `rbw-MG.MarshalGenerate.sh`\n- `tt/rbw-o.OnboardingStartHere.sh` — new 4-line thin dispatcher\n- `tt/rbw-Occ.OnboardingCrashCourse.sh` — new 4-line thin dispatcher\n\n### Smoke testing — State 3 (Ready) verified\n\nBoth tabtargets run cleanly against the current repo state (populated RBRR + credentials present):\n\n- `tt/rbw-o.OnboardingStartHere.sh` — exit 0, renders menu with `→ Crash Course` as the only arrow (correct for Ready state). All tabtarget references resolve via `buz_healthcheck`.\n- `tt/rbw-Occ.OnboardingCrashCourse.sh` — exit 0, renders all 6 units. Unit 3 (RBRR) shows `[*] RBRR populated — depot project: rbwg-d-depot10041-260327170532`. Unit 4 (Station) shows `[*] BURC present` and `[*] Station file present at /Users/bhyslop/projects/rbm_alpha_recipemuster/../station-files/burs.env`. Unit 5 (Marshal Zero) shows `[*] Current RBRR state: populated (not a zero state right now)`.\n- `tt/rbw-tf.QualifyFast.sh` — passes (tabtarget structure OK, RBW colophon registrations OK, generated context freshness OK).\n\n### Smoke States 1 and 2 — deferred\n\nNot yet exercised; waiting on user approval to touch live repo state:\n\n- **State 1 (Bare-fork simulation)** — requires running `tt/rbw-MZ.MarshalZeroes.sh` to blank RBRR identity fields (reversible, preserves OAuth). Expected outcome: StartHere arrows Crash Course + Establish the Depot; Crash Course Unit 3 shows red (\"depot identity fields are blank\"); Unit 5 shows \"zero-like — this is where a bare-fork learner starts\".\n- **State 2 (Joining simulation)** — requires temporarily moving credential files out of `${RBRR_SECRETS_DIR}` and restoring. Expected outcome: StartHere arrows Crash Course + Take Your Station; Crash Course Unit 3 green, Unit 5 says \"populated (not a zero state right now)\".\n\n### RBGC_PUBLIC_DOCS_URL observation — coordination item for `₣A5`\n\nAnother officium has retargeted `RBGC_PUBLIC_DOCS_URL` to `https://github.com/scaleinv/recipebottle/blob/main/README.md`. The paddock's \"Public-Docs Anchor Coordination with `₣A5`\" section still references the `scaleinv.github.io` URL and implicitly assumes a github-pages build. The anchor scheme (PascalCase single-word concepts like `#Depot`, `#RBRR`, `#MarshalZero`) still works mechanically — the code uses the constant so it's automatically correct — but the paddock coordination text is stale about *where* A5 is curating anchors.\n\n`Tools/rbk/rbgc_Constants.sh` has been edited by another officium (visible as unstaged change in working tree during this pace's work). Not touched by this pace.\n\n**Paddock follow-up needed**: update the Anchor Coordination section to say A5 is curating `README.md` on `main`, not a github-pages build. Worth a short paddock reslate when convenient.\n\n### User revisions pending\n\nUser ran both tabtargets, confirmed they function, wants to do revisions on both. Specific revision items not yet captured at time of this reslate — user stepping away, revisions to be taken up on resume.\n\n### Prototype eval artifact — not yet delivered\n\nDeliverable 4 still open. Planned form: markdown file at `Tools/rbk/vov_veiled/A6-prototype-eval.md` containing the anti-inference system prompt, 3–5 application-focused scenario questions, and a differential-test note. Awaiting user signoff on the form before writing.\n\n### Paddock Artifact 3 items — still open\n\nDeliverable 5 items still pending:\n- **Track acronym table** — assign `rbw-O??` subfunction letters for the other 12 tracks; suggested table (`Ots/Ofc/Oed/Okr/Ork/Oof/Oea/Oir/Otc/Oac/Obc/Oar`) is a proposal pending confirmation\n- **Implementation-sequence decision** — paddock currently says \"First implementation target: Kindle Your First Crucible\" but Crash Course landed first; needs reconciliation\n- **Anchor Coordination section update** — fold in the `RBGC_PUBLIC_DOCS_URL` retargeting observation (see above)\n- **New open questions** from this implementation — none surfaced that aren't already captured\n\n### Downstream paces — swept, clean\n\n`₢A6AAC`, `₢A6AAD`, `₢A6AAE` were mass-reslated in commit `fe7e1d50` to fix stale colophons (`rbw-h`/`rbw-Hcc`/`rbw-Hfc` → `rbw-o`/`rbw-Occ`/`rbw-Ofc`), update dropped-A6AAB dependency references to `₢A6AAA`, and add a small amount of concrete guidance (zipper registration form, menu-update mechanics) that became visible while this pace's implementation was landing. The interview-transcript phantom was also removed from `₢A6AAE`'s References section in the same sweep — slightly beyond the user's explicit \"colophon\" scope, flagged in the report, user has not yet indicated whether to restore it.\n\n## Prefix Context\n\nTabtarget colophons use the **two-colophon onboarding scheme** settled in the `₣A6` paddock Prerequisites section:\n\n- `rbw-o` (lowercase, terminal) — the onboarding StartHere entry point, standalone tabtarget\n- `rbw-O*` (uppercase family) — individual onboarding handbook tracks (e.g. `rbw-Occ` for Crash Course, `rbw-Ofc` for First Crucible)\n\nThis replaces the initially-proposed `rbw-h?` family from the interview, which collided with the existing `rbw-h` Hallmark family. Case-split family is deliberate starting-letter differentiation for clarity.\n\n## Existing code as mechanism reference (NOT structural template)\n\nThe existing `rbho_*` sequences — `rbho_triage`, `rbho_reference`, `rbho_retriever`, `rbho_director`, `rbho_governor`, `rbho_payor` — are **mechanism references** for this pace. They demonstrate how filesystem probes, `buh_*` primitives, and the dispatch chain fit together. Their *organizational shapes* (role-based decomposition, credential-driven routing, two-mode walkthrough/reference rendering) are precisely what this heat replaces. **Copy the idioms, not the structure.**\n\nThe distinction is load-bearing. `rbho_triage` is a role-router: probe RBRA credentials → enumerate detected roles → link each to a role walkthrough. Its organizing axis is *authorization* — the paddock's exact diagnosis of the malformation this heat is curing. Using it as a \"structural template\" for `rbho_start_here` would import the role-axis into the very tabtarget meant to supplant it.\n\n**What to borrow from existing sequences**:\n- `buc_doc_brief \"description\"` + `buc_doc_shown || return 0` — dispatch-mode guard\n- `zrbho_po_extract_capture ${file} ${key}` — read a KEY=VALUE from a file without sourcing\n- `zrbho_po_status ${flag} \"${text}\"` — render `[*] text` or `[ ] text` status line\n- `zrbho_probe_role_credentials` — useful for the *any-credential-present* check even though per-role detail is not used\n- `buh_section`, `buh_e`, `buh_t` — section headers, blank lines, plain text\n- `buh_tlt \"prefix \" \"concept\" \"${z_docs}#Anchor\" \" suffix\"` — vocabulary-first teaching (concept as clickable OSC-8 hyperlink)\n- `buh_tc \"prefix \" \"tt/rbw-...sh\"` — text + cyan command reference\n- `buh_tT \"prefix \" \"${RBZ_*}\"` — text + resolved-via-glob tabtarget (also validates existence)\n- Filesystem-only probing convention (`test -f`, extract via helper, no env-var dependency on BURS)\n\n**What NOT to borrow**:\n- Role-based decomposition of menu items or teaching content\n- Credential detection as a primary routing signal\n- Two-mode (walkthrough vs reference) rendering\n- Unit structure organized around role tasks\n\n## Dispatch chain (reference)\n\n```\ntt/rbw-{colophon}.*.sh  (5-line thin dispatcher)\n  → .buk/launcher.rbw_workbench.sh\n  → rbw_workbench.sh :: rbw_route()\n  → buz_exec_lookup()  (exact-string colophon match)\n  → rbho_cli.sh        (CLI furnish sources buh/rbcc/rbgc/rbz/rbho)\n  → rbho_{sequence}()  (enrolled function in rbho_onboarding.sh)\n```\n\n## Deliverable 1 — `rbho_start_here()` + `tt/rbw-o.OnboardingStartHere.sh` + zipper\n\n**Status**: LANDED in commit `9844ce32`. Verified in State 3 (Ready). Pending user revision notes.\n\n**Sequence function** in `Tools/rbk/rbho_onboarding.sh`, added at a new \"Handbook restart — foundation\" banner section after the existing role-track sequences.\n\n**Probe set** (deliberately narrow — state, not role):\n- `z_rbrr_populated` — `.rbk/rbrr.env` exists AND has non-empty `RBRR_DEPOT_PROJECT_ID`\n- `z_has_any_rbra` — any of payor/governor/director/retriever credential files exist (via `zrbho_probe_role_credentials`; per-role values are collected but ignored for routing)\n\n**Highlighting logic** (three states, foundation-focused, no role inference):\n- **Cold start** (no RBRR) → arrow on Crash Course + arrow on Establish the Depot. These are the only unblocked actions.\n- **Joining** (RBRR populated, no credentials) → arrow on Crash Course + arrow on Take Your Station. These are the only unblocked actions.\n- **Ready** (RBRR populated, has credentials) → arrow on Crash Course only. Learner picks their own track from the static menu. No guessing.\n\nArrow rendering: leading `→ ` prefix on highlighted items, `  ` on others. Simple, discoverable, easy to revise.\n\n**Menu body** (static, intent-organized):\n\n1. **Preamble** — what Recipe Bottle is, what handbooks are, how to read this menu\n2. **Foundation** — Crash Course (always first, always arrowed), Take Your Station (`[planned]`, arrowed when joining)\n3. **Role-intent tracks** — Your First Crucible `[planned]`, Establish the Depot `[planned]` (arrowed when cold-start), Knight the Realm `[planned]`, Receive Your Knighthood `[planned]`\n4. **Director subtracks** — Your First Ordination, Enshrine the Ancestors, Inscribe the Reliquary, Tethered Cloud Builds, Airgap Your Builds, Building for Crucibles (all `[planned]`, none arrowed in first iteration)\n5. **Evaluation** — Assay the Realm `[planned]`\n6. **Reference** — Realm Dashboard (links to existing `rbw-gOr.OnboardReference.sh` until A6 produces Frame 4-refined replacement)\n7. **Find your role / legacy fallback** — \"Run Crash Course first if unsure\" guidance, plus honest pointer to legacy role-organized `rbw-go.OnboardMAIN.sh` triage as preserved fallback during the heat\n\n**Tabtarget dispatcher** at `tt/rbw-o.OnboardingStartHere.sh` (exact copy of existing 5-line pattern, with `BURD_NO_LOG=1` — handbook output is ephemeral per paddock decision):\n```bash\n#!/bin/bash\nexport BURD_LAUNCHER=\".buk/launcher.rbw_workbench.sh\"\nexport BURD_NO_LOG=1\nexec \"${BASH_SOURCE[0]%/*}/../${BURD_LAUNCHER}\" \"${0##*/}\" \"${@}\"\n```\n\n**Zipper enrollment** in `Tools/rbk/rbz_zipper.sh` — new `RBZ__GROUP_ONBOARDING` group adjacent to the existing `RBZ__GROUP_GUIDE`:\n```bash\nbuz_group   RBZ__GROUP_ONBOARDING  \"rbw-o\"   \"Onboarding — Handbook restart\"\nz_mod=\"rbho_cli.sh\"\nbuz_enroll  RBZ_ONBOARD_START_HERE \"rbw-o\"   \"${z_mod}\"  \"rbho_start_here\"   \"\"  \"Onboarding start — probe-aware menu into handbook tracks\"\n```\n\n## Deliverable 2 — `rbho_crash_course()` + `tt/rbw-Occ.OnboardingCrashCourse.sh` + zipper\n\n**Status**: LANDED in commit `9844ce32`. Verified in State 3 (Ready). Pending user revision notes.\n\n**Sequence function** in `Tools/rbk/rbho_onboarding.sh`, next to `rbho_start_here()`.\n\n**Probes**:\n- `z_rbrr_populated` + `z_rbrr_project` — RBRR identity state (for Unit 3 + Unit 5)\n- `z_burc_present` — `.buk/burc.env` existence (for Unit 4)\n- `z_station_present` — `${BURD_STATION_FILE}` existence (for Unit 4)\n\n**Unit breakdown** (6 units, alternating teaching-only and verified, per paddock per-unit brevity discipline):\n\n1. **What you're looking at** (teaching-only) — introduce `Tabtarget` (link), the `tt/` directory, the `{colophon}.{frontispiece}.sh` naming. Concrete example: `tt/rbw-go.OnboardMAIN.sh`. Teach that tab completion narrows by colophon. Vocabulary: tabtarget, colophon, frontispiece.\n2. **Regimes — the configuration map** (teaching-only) — introduce `Regime` (link), the 13-regime landscape, the six user-facing ones, the `rbw-r{letter}{r|v}` render/validate tabtarget pattern. Teach the *pattern*, not the inventory. Vocabulary: regime, render, validate.\n3. **Repository regime** (verified) — introduce `RBRR` (link), explain depot identity as shared team ground truth. Probe `.rbk/rbrr.env` + `RBRR_DEPOT_PROJECT_ID`. Vocabulary: RBRR, depot.\n4. **Station regime and log placement** (verified) — introduce `BURC` (link — project config) and `BURS` (link — per-developer station). Explain station-vs-repo split; call out `BURS_LOG_DIR` as the anchor. Probe `.buk/burc.env` existence + `${BURD_STATION_FILE}` existence. Teach: state-changing commands log, handbooks don't. Vocabulary: station, BURC, BURS.\n5. **Marshal Zero — the first lesson** (teaching + verified) — introduce `MarshalZero` (link). Walk through what `rblm_zero()` does (blank RBRR fields, delete credentials, preserve OAuth). Then describe the `DiagnosticFailure` (link) experience of running `rbw-rrv.ValidateRepoRegime.sh` against zero state. Teach the core lesson: *\"In this system, configuration errors fail diagnostically. Trust the error messages.\"* Probe current state as \"populated\" or \"zero-like\". Vocabulary: Marshal Zero, diagnostic failure.\n6. **Next steps** (teaching-only) — summarize learned capabilities, point back to StartHere menu via `buh_tT \" \" \"${RBZ_ONBOARD_START_HERE}\"`. Close the loop.\n\n**Two entry states** handled without branching: the same units render in both states. For bare-fork, verified units show red with teaching-focused context about what the learner will fill in via the payor track. For joining-existing, the same units show green with verification-focused context about what the existing team has done. Same text, same probes, different stories emerge from the probe outcomes.\n\n**Teaching voice** per paddock principles: vocabulary-first via `buh_tlt`, relational prose (nouns introduced in context with concrete referents, verbs connecting them), roles emerge from actions not labels, each new term has a reason to exist at the moment of introduction.\n\n**Tabtarget dispatcher** at `tt/rbw-Occ.OnboardingCrashCourse.sh` — same 5-line pattern.\n\n**Zipper enrollment** next to the START_HERE entry under the same group:\n```bash\nbuz_enroll  RBZ_ONBOARD_CRASH_COURSE \"rbw-Occ\" \"${z_mod}\"  \"rbho_crash_course\" \"\"  \"Crash Course — universal prerequisite: tabtargets, regimes, Marshal Zero\"\n```\n\n## Deliverable 3 — Smoke testing across three states\n\n**Status**: State 3 (Ready) VERIFIED. States 1 and 2 deferred pending user approval to touch live repo.\n\nRun both new tabtargets in three repo states, document observed behavior in the pace commit message.\n\n**State 1: Bare-fork simulation** — run `tt/rbw-MZ.MarshalZeroes.sh` to zero RBRR identity + credentials, preserving OAuth. Then:\n- `tt/rbw-o.OnboardingStartHere.sh` — expect: Crash Course arrowed, Establish the Depot arrowed, all other tracks shown without arrows\n- `tt/rbw-Occ.OnboardingCrashCourse.sh` — expect: Unit 3 (RBRR) shows red with \"depot identity fields are blank\", Unit 5 (Marshal Zero) confirms current state is zero-like\n\n**State 2: Joining-existing simulation** — with populated RBRR but credentials removed from `${RBRR_SECRETS_DIR}`. (This may require temporary manipulation; document exact steps.)\n- StartHere — expect: Crash Course arrowed, Take Your Station arrowed\n- Crash Course — expect: Unit 3 green with project ID, Unit 5 says \"populated (not a zero state right now)\"\n\n**State 3: Ready / current repo state** — with whatever's currently in `.rbk/`. Run both, expect:\n- StartHere — only Crash Course arrowed (Ready state)\n- Crash Course — whatever the current probe outcomes are\n\nDocument the actual observed rendering from each run in the pace commit. If any state reveals bugs, fix them before wrapping.\n\n**Do not touch ground truth** — use Marshal Zero (reversible) and backup files if needed. Do not blow away RBRR permanently for testing.\n\n## Deliverable 4 — Prototype agent-learner eval\n\n**Status**: NOT YET DELIVERED. Planned form below; awaiting user signoff on form/location before writing.\n\n**Scoped intent** per the original A6AAB design: *just enough to validate the concept end-to-end, not a full release gate.* The real eval harness is a later dedicated pace.\n\n**Form**: a markdown file or commit-message appendix containing:\n- **Anti-inference system prompt**: *\"You are a learner evaluating a handbook. Reason strictly from what the handbook tells you. If the handbook doesn't say it, you don't know it. Do not draw on general knowledge of containers, Google Cloud, or DevOps.\"*\n- **Scenario questions** (3–5): application-focused, not recall. Examples:\n    - *\"A learner runs `tt/rbw-rrv.ValidateRepoRegime.sh` and gets a list of blank fields. Based only on the Crash Course, what should they conclude and what should they do next?\"*\n    - *\"What's the difference between a station setting and a repository setting, and why does it matter?\"*\n    - *\"If you needed to find the tabtarget that validates the payor regime, how would you name it without looking it up?\"*\n- **Differential test**: run the same questions against the full Crash Course output AND against a stripped variant (teaching prose removed, probes and tabtarget references left). Compare scores. Signal is in the delta.\n- **Goodhart mitigation** note: questions not drawn from author's immediate working context; rotate if repeated.\n\n**Execution**: this pace DELIVERS the eval artifact but does NOT necessarily run it. Running the eval can happen inline (user pastes handbook output into a fresh Claude session with the system prompt) or is deferred as a sanity check. The pace succeeds if the eval artifact is present and well-formed.\n\n**Where the eval artifact lives**: as a new small markdown file at `Tools/rbk/vov_veiled/A6-prototype-eval.md` — drafter's call, note the choice in the commit.\n\n## Deliverable 5 — Paddock refinement (remaining items)\n\n**Status**: OPEN. Items itemized below.\n\nThe paddock was already updated during earlier reslates to:\n- Sweep the `rbw-h?` prefix collision to the two-colophon scheme\n- Remove the interview-transcript phantom and add the mounting-corrections section\n- Add the Public-Docs Anchor Coordination section\n- Flag the \"kindle\" vocabulary overload in Open Questions\n- Assign provisional `Occ` for Crash Course\n- Expand References with the dispatch-chain files\n\nRemaining work (via `jjx_paddock` setter):\n\n- **Finalize track acronym table** — assign `rbw-O??` subfunction letters for the other 12 tracks. Suggested (for review): `Ots` Take Your Station, `Ofc` First Crucible, `Oed` Establish Depot, `Okr` Knight the Realm, `Ork` Receive Knighthood, `Oof` First Ordination, `Oea` Enshrine Ancestors, `Oir` Inscribe Reliquary, `Otc` Tethered Cloud, `Oac` Airgap Builds, `Obc` Building for Crucibles, `Oar` Assay Realm. Record as a table. Open for revision.\n- **Update Anchor Coordination section** — replace the implicit `scaleinv.github.io` target framing with `github.com/scaleinv/recipebottle/blob/main/README.md` (see Status section above). Note that the anchor scheme (`#PascalCase`) still holds.\n- **Record implementation-sequence decision** — the paddock currently says \"First implementation target: Kindle Your First Crucible\" but this pace implements Crash Course + StartHere first. Either reclassify First Crucible as \"first *track* implementation\" (with Crash Course as prerequisite foundation), or rewrite to state Crash Course is the true first implementation. Pick one.\n- **Add any open questions** surfaced during implementation, especially around probe-aware highlighting mechanics, zero-state detection heuristics, and the teaching voice for the Marshal Zero moment.\n\n## Success criteria\n\n- `tt/rbw-o.OnboardingStartHere.sh` runs cleanly and renders the probe-aware menu ✓ (verified State 3)\n- `tt/rbw-Occ.OnboardingCrashCourse.sh` runs cleanly and renders the 6-unit Frame 4-refined document ✓ (verified State 3)\n- `buz_healthcheck` passes (both new tabtargets found on disk, matching their zipper enrollments) ✓\n- All three smoke-test states produce sensible output — **State 3 only; States 1 and 2 deferred**\n- Prototype eval artifact is present, well-formed, and captures the differential-test intent — **not yet delivered**\n- Paddock Artifact 3 items are landed (acronym table, implementation-sequence decision, anchor URL update, new open questions) — **not yet landed**\n- User reviews and accepts the behavior before the pace is wrapped — **review pending; user has confirmed both tabtargets function, wants revisions**\n\n## Local verification before commit (for each iteration)\n\n- Run `tt/rbw-o.OnboardingStartHere.sh` — no errors, no missing-tabtarget errors from `buz_healthcheck`, probe states visible in output\n- Run `tt/rbw-Occ.OnboardingCrashCourse.sh` — same checks, all six units render\n- Run `tt/rbw-tf.QualifyFast.sh` — catches tabtarget/colophon/nameplate health issues\n- `jjx_record` with explicit file list — do NOT use blanket staging (other officia may have uncommitted work)\n\n## Out of Scope\n\n- **Probe implementation beyond existing helpers** — reuse `zrbho_probe_role_credentials`, `zrbho_po_extract_capture`, `zrbho_po_status`. New probes only where the Crash Course units cannot be served by existing helpers, and only filesystem-level (no docker, no gcloud).\n- **Full agent-learner eval harness** — prototype only; release-gate mechanics deferred to a dedicated pace\n- **Retirement of `rbw-gO*` tabtargets and their `rbho_triage/retriever/director/governor/payor` sequences** — old and new coexist; cutover in a later pace once replacements are proven\n- **First Crucible handbook** — separate pace (`₢A6AAC` drafts, `₢A6AAD` implements)\n- **The other 12 handbook tracks** — separate paces\n- **Adding public-docs anchors to `README.md`** — `₣A5`'s job; this pace only consumes anchors and records new ones in the paddock coordination section\n- **Fancy probe-aware highlighting** — simple `→ ` prefix in first iteration; refinements after review\n\n## References\n\n- `Tools/rbk/rbho_onboarding.sh` — existing sequences as MECHANISM references only; new `rbho_start_here()` and `rbho_crash_course()` at end of file (post commit `9844ce32`)\n- `Tools/rbk/rbho_cli.sh` — CLI furnish (no changes needed; already sources buh/rbcc/rbgc/rbz/rbho)\n- `Tools/rbk/rbz_zipper.sh` — zipper registry; new `RBZ__GROUP_ONBOARDING` group and two enrollments\n- `Tools/rbk/rblm_cli.sh` — `rblm_zero()` reference for Unit 5 Marshal Zero teaching\n- `Tools/buk/buh_handbook.sh` — display primitive catalog\n- `Tools/buk/buz_zipper.sh` — zipper mechanism (`buz_group`, `buz_enroll`, `buz_exec_lookup`)\n- `tt/rbw-go.OnboardMAIN.sh` — 5-line thin dispatcher pattern (copied for both new tabtargets)\n- `₣A6` paddock — Prefix Context, Anchor Coordination, Teaching Voice, Open Questions, Provenance (Mounting corrections), Rough Track Roster\n- Commit `9844ce32` — initial implementation land\n- Commit `fe7e1d50` — downstream paces (A6AAC/AAD/AAE) colophon sweep",
              "jjgtn_text": "## Character\n\nComprehensive foundation-landing pace for the handbook restart. Absorbs what was previously split across `₢A6AAA` (drafts) and the now-dropped `₢A6AAB` (implementation). Produces a first-iteration working implementation of the two foundation tabtargets — the probe-aware StartHere menu and the universal Crash Course handbook — plus smoke testing across three repo states and a prototype agent-learner eval.\n\nLow-novelty in mechanism (same bash idioms, same `buh_*` primitives, same dispatch chain as the existing `rbho_*` sequences). High-novelty in pedagogical framing: first deployed artifacts of the Frame 4-refined handbook restart, first deliberate break from role-decomposition as the primary organizing axis. Review happens against the running code — user executes the tabtargets, reads the output, annotates what works and what doesn't, approves or requests revisions.\n\n## Prefix Context\n\nTabtarget colophons use the **two-colophon onboarding scheme** settled in the `₣A6` paddock Prerequisites section:\n\n- `rbw-o` (lowercase, terminal) — the onboarding StartHere entry point, standalone tabtarget\n- `rbw-O*` (uppercase family) — individual onboarding handbook tracks (e.g. `rbw-Occ` for Crash Course, `rbw-Ofc` for First Crucible)\n\nThis replaces the initially-proposed `rbw-h?` family from the interview, which collided with the existing `rbw-h` Hallmark family. Case-split family is deliberate starting-letter differentiation for clarity.\n\n## Existing code as mechanism reference (NOT structural template)\n\nThe existing `rbho_*` sequences — `rbho_triage`, `rbho_reference`, `rbho_retriever`, `rbho_director`, `rbho_governor`, `rbho_payor` — are **mechanism references** for this pace. They demonstrate how filesystem probes, `buh_*` primitives, and the dispatch chain fit together. Their *organizational shapes* (role-based decomposition, credential-driven routing, two-mode walkthrough/reference rendering) are precisely what this heat replaces. **Copy the idioms, not the structure.**\n\nThe distinction is load-bearing. `rbho_triage` is a role-router: probe RBRA credentials → enumerate detected roles → link each to a role walkthrough. Its organizing axis is *authorization* — the paddock's exact diagnosis of the malformation this heat is curing. Using it as a \"structural template\" for `rbho_start_here` would import the role-axis into the very tabtarget meant to supplant it.\n\n**What to borrow from existing sequences**:\n- `buc_doc_brief \"description\"` + `buc_doc_shown || return 0` — dispatch-mode guard\n- `zrbho_po_extract_capture ${file} ${key}` — read a KEY=VALUE from a file without sourcing\n- `zrbho_po_status ${flag} \"${text}\"` — render `[*] text` or `[ ] text` status line\n- `zrbho_probe_role_credentials` — useful for the *any-credential-present* check even though per-role detail is not used\n- `buh_section`, `buh_e`, `buh_t` — section headers, blank lines, plain text\n- `buh_tlt \"prefix \" \"concept\" \"${z_docs}#Anchor\" \" suffix\"` — vocabulary-first teaching (concept as clickable OSC-8 hyperlink)\n- `buh_tc \"prefix \" \"tt/rbw-...sh\"` — text + cyan command reference\n- `buh_tT \"prefix \" \"${RBZ_*}\"` — text + resolved-via-glob tabtarget (also validates existence)\n- Filesystem-only probing convention (`test -f`, extract via helper, no env-var dependency on BURS)\n\n**What NOT to borrow**:\n- Role-based decomposition of menu items or teaching content\n- Credential detection as a primary routing signal\n- Two-mode (walkthrough vs reference) rendering\n- Unit structure organized around role tasks\n\n## Dispatch chain (reference)\n\n```\ntt/rbw-{colophon}.*.sh  (5-line thin dispatcher)\n  → .buk/launcher.rbw_workbench.sh\n  → rbw_workbench.sh :: rbw_route()\n  → buz_exec_lookup()  (exact-string colophon match)\n  → rbho_cli.sh        (CLI furnish sources buh/rbcc/rbgc/rbz/rbho)\n  → rbho_{sequence}()  (enrolled function in rbho_onboarding.sh)\n```\n\n## Deliverable 1 — `rbho_start_here()` + `tt/rbw-o.OnboardingStartHere.sh` + zipper\n\n**Sequence function** in `Tools/rbk/rbho_onboarding.sh`, added at a new \"Handbook restart — foundation\" banner section after the existing role-track sequences.\n\n**Probe set** (deliberately narrow — state, not role):\n- `z_rbrr_populated` — `.rbk/rbrr.env` exists AND has non-empty `RBRR_DEPOT_PROJECT_ID`\n- `z_has_any_rbra` — any of payor/governor/director/retriever credential files exist (via `zrbho_probe_role_credentials`; per-role values are collected but ignored for routing)\n\n**Highlighting logic** (three states, foundation-focused, no role inference):\n- **Cold start** (no RBRR) → arrow on Crash Course + arrow on Establish the Depot. These are the only unblocked actions.\n- **Joining** (RBRR populated, no credentials) → arrow on Crash Course + arrow on Take Your Station. These are the only unblocked actions.\n- **Ready** (RBRR populated, has credentials) → arrow on Crash Course only. Learner picks their own track from the static menu. No guessing.\n\nArrow rendering: leading `→ ` prefix on highlighted items, `  ` on others. Simple, discoverable, easy to revise.\n\n**Menu body** (static, intent-organized):\n\n1. **Preamble** — what Recipe Bottle is, what handbooks are, how to read this menu\n2. **Foundation** — Crash Course (always first, always arrowed), Take Your Station (`[planned]`, arrowed when joining)\n3. **Role-intent tracks** — Your First Crucible `[planned]`, Establish the Depot `[planned]` (arrowed when cold-start), Knight the Realm `[planned]`, Receive Your Knighthood `[planned]`\n4. **Director subtracks** — Your First Ordination, Enshrine the Ancestors, Inscribe the Reliquary, Tethered Cloud Builds, Airgap Your Builds, Building for Crucibles (all `[planned]`, none arrowed in first iteration)\n5. **Evaluation** — Assay the Realm `[planned]`\n6. **Reference** — Realm Dashboard (links to existing `rbw-gOr.OnboardReference.sh` until A6 produces Frame 4-refined replacement)\n7. **Find your role / legacy fallback** — \"Run Crash Course first if unsure\" guidance, plus honest pointer to legacy role-organized `rbw-go.OnboardMAIN.sh` triage as preserved fallback during the heat\n\n**Tabtarget dispatcher** at `tt/rbw-o.OnboardingStartHere.sh` (exact copy of existing 5-line pattern, with `BURD_NO_LOG=1` — handbook output is ephemeral per paddock decision):\n```bash\n#!/bin/bash\nexport BURD_LAUNCHER=\".buk/launcher.rbw_workbench.sh\"\nexport BURD_NO_LOG=1\nexec \"${BASH_SOURCE[0]%/*}/../${BURD_LAUNCHER}\" \"${0##*/}\" \"${@}\"\n```\n\n**Zipper enrollment** in `Tools/rbk/rbz_zipper.sh` — new `RBZ__GROUP_ONBOARDING` group adjacent to the existing `RBZ__GROUP_GUIDE`:\n```bash\nbuz_group   RBZ__GROUP_ONBOARDING  \"rbw-o\"   \"Onboarding — Handbook restart\"\nz_mod=\"rbho_cli.sh\"\nbuz_enroll  RBZ_ONBOARD_START_HERE \"rbw-o\"   \"${z_mod}\"  \"rbho_start_here\"   \"\"  \"Onboarding start — probe-aware menu into handbook tracks\"\n```\n\n## Deliverable 2 — `rbho_crash_course()` + `tt/rbw-Occ.OnboardingCrashCourse.sh` + zipper\n\n**Sequence function** in `Tools/rbk/rbho_onboarding.sh`, next to `rbho_start_here()`.\n\n**Probes**:\n- `z_rbrr_populated` + `z_rbrr_project` — RBRR identity state (for Unit 3 + Unit 5)\n- `z_burc_present` — `.buk/burc.env` existence (for Unit 4)\n- `z_station_present` — `${BURD_STATION_FILE}` existence (for Unit 4)\n\n**Unit breakdown** (6 units, alternating teaching-only and verified, per paddock per-unit brevity discipline):\n\n1. **What you're looking at** (teaching-only) — introduce `Tabtarget` (link), the `tt/` directory, the `{colophon}.{frontispiece}.sh` naming. Concrete example: `tt/rbw-go.OnboardMAIN.sh`. Teach that tab completion narrows by colophon. Vocabulary: tabtarget, colophon, frontispiece.\n2. **Regimes — the configuration map** (teaching-only) — introduce `Regime` (link), the 13-regime landscape, the six user-facing ones, the `rbw-r{letter}{r|v}` render/validate tabtarget pattern. Teach the *pattern*, not the inventory. Vocabulary: regime, render, validate.\n3. **Repository regime** (verified) — introduce `RBRR` (link), explain depot identity as shared team ground truth. Probe `.rbk/rbrr.env` + `RBRR_DEPOT_PROJECT_ID`. Vocabulary: RBRR, depot.\n4. **Station regime and log placement** (verified) — introduce `BURC` (link — project config) and `BURS` (link — per-developer station). Explain station-vs-repo split; call out `BURS_LOG_DIR` as the anchor. Probe `.buk/burc.env` existence + `${BURD_STATION_FILE}` existence. Teach: state-changing commands log, handbooks don't. Vocabulary: station, BURC, BURS.\n5. **Marshal Zero — the first lesson** (teaching + verified) — introduce `MarshalZero` (link). Walk through what `rblm_zero()` does (blank RBRR fields, delete credentials, preserve OAuth). Then describe the `DiagnosticFailure` (link) experience of running `rbw-rrv.ValidateRepoRegime.sh` against zero state. Teach the core lesson: *\"In this system, configuration errors fail diagnostically. Trust the error messages.\"* Probe current state as \"populated\" or \"zero-like\". Vocabulary: Marshal Zero, diagnostic failure.\n6. **Next steps** (teaching-only) — summarize learned capabilities, point back to StartHere menu via `buh_tT \" \" \"${RBZ_ONBOARD_START_HERE}\"`. Close the loop.\n\n**Two entry states** handled without branching: the same units render in both states. For bare-fork, verified units show red with teaching-focused context about what the learner will fill in via the payor track. For joining-existing, the same units show green with verification-focused context about what the existing team has done. Same text, same probes, different stories emerge from the probe outcomes.\n\n**Teaching voice** per paddock principles: vocabulary-first via `buh_tlt`, relational prose (nouns introduced in context with concrete referents, verbs connecting them), roles emerge from actions not labels, each new term has a reason to exist at the moment of introduction.\n\n**Tabtarget dispatcher** at `tt/rbw-Occ.OnboardingCrashCourse.sh` — same 5-line pattern.\n\n**Zipper enrollment** next to the START_HERE entry under the same group:\n```bash\nbuz_enroll  RBZ_ONBOARD_CRASH_COURSE \"rbw-Occ\" \"${z_mod}\"  \"rbho_crash_course\" \"\"  \"Crash Course — universal prerequisite: tabtargets, regimes, Marshal Zero\"\n```\n\n## Deliverable 3 — Smoke testing across three states\n\nRun both new tabtargets in three repo states, document observed behavior in the pace commit message.\n\n**State 1: Bare-fork simulation** — run `tt/rbw-MZ.MarshalZeroes.sh` to zero RBRR identity + credentials, preserving OAuth. Then:\n- `tt/rbw-o.OnboardingStartHere.sh` — expect: Crash Course arrowed, Establish the Depot arrowed, all other tracks shown without arrows\n- `tt/rbw-Occ.OnboardingCrashCourse.sh` — expect: Unit 3 (RBRR) shows red with \"depot identity fields are blank\", Unit 5 (Marshal Zero) confirms current state is zero-like\n\n**State 2: Joining-existing simulation** — with populated RBRR but credentials removed from `${RBRR_SECRETS_DIR}`. (This may require temporary manipulation; document exact steps.)\n- StartHere — expect: Crash Course arrowed, Take Your Station arrowed\n- Crash Course — expect: Unit 3 green with project ID, Unit 5 says \"populated (not a zero state right now)\"\n\n**State 3: Ready / current repo state** — with whatever's currently in `.rbk/`. Run both, expect:\n- StartHere — only Crash Course arrowed (Ready state)\n- Crash Course — whatever the current probe outcomes are\n\nDocument the actual observed rendering from each run in the pace commit. If any state reveals bugs, fix them before wrapping.\n\n**Do not touch ground truth** — use Marshal Zero (reversible) and backup files if needed. Do not blow away RBRR permanently for testing.\n\n## Deliverable 4 — Prototype agent-learner eval\n\n**Scoped intent** per the original A6AAB design: *just enough to validate the concept end-to-end, not a full release gate.* The real eval harness is a later dedicated pace.\n\n**Form**: a markdown file or commit-message appendix containing:\n- **Anti-inference system prompt**: *\"You are a learner evaluating a handbook. Reason strictly from what the handbook tells you. If the handbook doesn't say it, you don't know it. Do not draw on general knowledge of containers, Google Cloud, or DevOps.\"*\n- **Scenario questions** (3–5): application-focused, not recall. Examples:\n    - *\"A learner runs `tt/rbw-rrv.ValidateRepoRegime.sh` and gets a list of blank fields. Based only on the Crash Course, what should they conclude and what should they do next?\"*\n    - *\"What's the difference between a station setting and a repository setting, and why does it matter?\"*\n    - *\"If you needed to find the tabtarget that validates the payor regime, how would you name it without looking it up?\"*\n- **Differential test**: run the same questions against the full Crash Course output AND against a stripped variant (teaching prose removed, probes and tabtarget references left). Compare scores. Signal is in the delta.\n- **Goodhart mitigation** note: questions not drawn from author's immediate working context; rotate if repeated.\n\n**Execution**: this pace DELIVERS the eval artifact but does NOT necessarily run it. Running the eval can happen inline (user pastes handbook output into a fresh Claude session with the system prompt) or is deferred as a sanity check. The pace succeeds if the eval artifact is present and well-formed.\n\n**Where the eval artifact lives**: as a fenced code block in the pace commit message, or as a new small markdown file at `Tools/rbk/vov_veiled/A6-prototype-eval.md` — drafter's call, note the choice in the commit.\n\n## Deliverable 5 — Paddock refinement (remaining items)\n\nThe paddock was already updated during earlier reslates to:\n- Sweep the `rbw-h?` prefix collision to the two-colophon scheme\n- Remove the interview-transcript phantom and add the mounting-corrections section\n- Add the Public-Docs Anchor Coordination section\n- Flag the \"kindle\" vocabulary overload in Open Questions\n- Assign provisional `Occ` for Crash Course\n- Expand References with the dispatch-chain files\n\nRemaining work (during or after implementation, via `jjx_paddock` setter):\n\n- **Finalize track acronym table** — assign `rbw-O??` subfunction letters for the other 12 tracks. Suggested (for review): `Ots` Take Your Station, `Ofc` First Crucible, `Oed` Establish Depot, `Okr` Knight the Realm, `Ork` Receive Knighthood, `Oof` First Ordination, `Oea` Enshrine Ancestors, `Oir` Inscribe Reliquary, `Otc` Tethered Cloud, `Oac` Airgap Builds, `Obc` Building for Crucibles, `Oar` Assay Realm. Record as a table. Open for revision.\n- **Update Anchor Coordination section** with any new anchors the Crash Course drafting reveals beyond the initial set.\n- **Record implementation-sequence decision** — the paddock currently says \"First implementation target: Kindle Your First Crucible\" but this pace implements Crash Course + StartHere first. Either reclassify First Crucible as \"first *track* implementation\" (with Crash Course as prerequisite foundation), or rewrite to state Crash Course is the true first implementation. Pick one.\n- **Add any open questions** surfaced during implementation, especially around probe-aware highlighting mechanics, zero-state detection heuristics, and the teaching voice for the Marshal Zero moment.\n\n## Success criteria\n\n- `tt/rbw-o.OnboardingStartHere.sh` runs cleanly and renders the probe-aware menu\n- `tt/rbw-Occ.OnboardingCrashCourse.sh` runs cleanly and renders the 6-unit Frame 4-refined document\n- `buz_healthcheck` passes (both new tabtargets found on disk, matching their zipper enrollments)\n- All three smoke-test states produce sensible output\n- Prototype eval artifact is present, well-formed, and captures the differential-test intent\n- Paddock Artifact 3 items are landed (acronym table, implementation-sequence decision, new open questions)\n- User reviews and accepts the behavior before the pace is wrapped\n\n## Local verification before commit\n\n- Run `tt/rbw-o.OnboardingStartHere.sh` — no errors, no missing-tabtarget errors from `buz_healthcheck`, probe states visible in output\n- Run `tt/rbw-Occ.OnboardingCrashCourse.sh` — same checks, all six units render\n- Run `tt/rbw-tf.QualifyFast.sh` if available — catches tabtarget/colophon/nameplate health issues\n- `jjx_record` with explicit file list — do NOT use blanket staging\n\n## Out of Scope\n\n- **Probe implementation beyond existing helpers** — reuse `zrbho_probe_role_credentials`, `zrbho_po_extract_capture`, `zrbho_po_status`. New probes only where the Crash Course units cannot be served by existing helpers, and only filesystem-level (no docker, no gcloud).\n- **Full agent-learner eval harness** — prototype only; release-gate mechanics deferred to a dedicated pace\n- **Retirement of `rbw-gO*` tabtargets and their `rbho_triage/retriever/director/governor/payor` sequences** — old and new coexist; cutover in a later pace once replacements are proven\n- **First Crucible handbook** — separate pace (`₢A6AAC` drafts, `₢A6AAD` implements)\n- **The other 12 handbook tracks** — separate paces\n- **Adding public-docs anchors to `README.md`** — `₣A5`'s job; this pace only consumes anchors and records new ones in the paddock coordination section\n- **Fancy probe-aware highlighting** — simple `→ ` prefix in first iteration; refinements after review\n\n## References\n\n- `Tools/rbk/rbho_onboarding.sh` — existing sequences as MECHANISM references only\n- `Tools/rbk/rbho_cli.sh` — CLI furnish (no changes needed; already sources buh/rbcc/rbgc/rbz/rbho)\n- `Tools/rbk/rbz_zipper.sh` — zipper registry; add new `RBZ__GROUP_ONBOARDING` group and two enrollments\n- `Tools/rbk/rblm_cli.sh` — `rblm_zero()` reference for Unit 5 Marshal Zero teaching\n- `Tools/buk/buh_handbook.sh` — display primitive catalog\n- `Tools/buk/buz_zipper.sh` — zipper mechanism (`buz_group`, `buz_enroll`, `buz_exec_lookup`)\n- `tt/rbw-go.OnboardMAIN.sh` — 5-line thin dispatcher pattern to copy\n- `₣A6` paddock — Prefix Context, Anchor Coordination, Teaching Voice, Open Questions, Provenance (Mounting corrections), Rough Track Roster",
              "jjgtn_text": "## Character\n\nFirst-iteration implementation of two onboarding handbook sequences plus their tabtarget dispatchers and zipper enrollments. Follows the existing `rbho_*` + `rbw-gO*` pattern concretely. Low-novelty in mechanism (same bash idioms, same `buh_*` primitives, same dispatch chain), high-novelty in pedagogical framing (first deployed artifacts of the Frame 4-refined handbook restart). Review happens against the running code — user executes the tabtargets, reads the output, annotates what works and what doesn't, approves or requests revisions.\n\n## Prefix Context\n\nTabtarget colophons in this docket use the **two-colophon onboarding scheme** settled in the `₣A6` paddock Prerequisites section:\n\n- `rbw-o` (lowercase, terminal) — the onboarding StartHere entry point, standalone tabtarget\n- `rbw-O*` (uppercase family) — individual onboarding handbook tracks (e.g. `rbw-Occ` for Crash Course, `rbw-Ofc` for First Crucible)\n\nThis replaces the initially-proposed `rbw-h?` family from the interview, which collided with the existing `rbw-h` Hallmark family. Case-split family is deliberate starting-letter differentiation for clarity. When drafting artifacts, verify any tabtarget reference against `ls tt/` before inventing a colophon.\n\n## Follows Existing Pattern\n\nThe architecture already exists and works. This pace instantiates two new entries in it.\n\n**Dispatch chain**: `tt/rbw-{colophon}.*.sh` (5-line thin dispatcher) → `.buk/launcher.rbw_workbench.sh` → `rbw_workbench.sh:rbw_route()` → `buz_exec_lookup()` (exact-string colophon match) → `rbho_cli.sh` (CLI furnish sources buh/rbcc/rbgc/rbz/rbho and kindles dependencies) → the enrolled sequence function in `rbho_onboarding.sh`.\n\n**Structural templates**:\n- `rbho_triage()` (lines 332–359 of `rbho_onboarding.sh`) is the structural model for `rbho_start_here()`. Same filesystem-probe + menu shape. The new sequence adds probe-aware highlighting (unpopulated RBRR highlights payor path; populated RBRR + existing RBRA highlights \"find your role\"; Crash Course is always visible as first item).\n- `rbho_retriever()` (lines 438+) is the structural model for `rbho_crash_course()`. Same `buh_tlt` vocabulary-first teaching with per-section probe status lines and tabtarget references. The new sequence organizes units around crash course teaching objectives rather than retriever role tasks.\n\n**Display primitives** in `Tools/buk/buh_handbook.sh`:\n- `buh_section` — bright white section header\n- `buh_e` — blank line\n- `buh_t` — plain text\n- `buh_tlt \"prefix \" \"concept\" \"${z_docs}#Anchor\" \" suffix\"` — vocabulary-first teaching (concept as clickable OSC-8 hyperlink)\n- `buh_tc \"prefix \" \"tt/rbw-...sh\"` — text + cyan command reference\n- `buh_cT \"prefix \" \"rbw-colophon\"` — cyan + resolved tabtarget path (via colophon glob)\n- `buh_tT \"prefix \" \"rbw-colophon\"` — text + resolved tabtarget\n- `zrbho_po_status $flag \"text\"` — `[*] text` (detected) or `[ ] text` (absent) status line\n\n**Anchor convention**: `${RBGC_PUBLIC_DOCS_URL}#{Anchor}` where `{Anchor}` is PascalCase single-word. The paddock's \"Public-Docs Anchor Coordination with `₣A5`\" section lists existing, new-for-Crash-Course, and new-for-later-tracks anchors. Crash Course drafting may reveal additional anchors — add them to the paddock section if so.\n\n**Retirement**: The old `rbw-gO*` family and its `rbho_triage/retriever/director/governor/payor` sequences stay live during A6. Both old and new coexist until A6 has full replacement coverage. **Retirement is out of scope for `₢A6AAA`** — a later pace handles the cutover once all replacements are proven.\n\n## Artifact 1 — `rbho_start_here()` + `tt/rbw-o.OnboardingStartHere.sh` + zipper entry\n\n**Sequence function** in `Tools/rbk/rbho_onboarding.sh`, added after the existing `rbho_triage` (or at a new \"Handbook Entry Points\" banner section): `rbho_start_here()` with the same guard shape as existing sequences (`buc_doc_brief` + `buc_doc_shown || return 0`), running pre-kindle filesystem probes and rendering the probe-aware menu.\n\n**Menu contents** (in this order):\n\n1. **Preamble** — orientation for someone landing here for the first time, explaining what Recipe Bottle is, what this tool does, and how to use this menu. Links `${z_docs}` at the top.\n2. **Crash Course** — first item, always visible, suggested with `[→]` arrow-style indicator when learner appears to be cold-start (no probes have been run, no RBRA placed). Links to `rbw-Occ.OnboardingCrashCourse.sh` via `buh_cT`.\n3. **Foundation tracks** — Take Your Station (for joining an existing configured project). Highlighted when RBRR is populated but RBRA is absent.\n4. **Role-intent tracks** — Your First Crucible, Establish the Depot, Knight the Realm, Receive Your Knighthood. Highlighted based on role probes (payor highlighted when RBRR is empty; director/retriever highlighted when their RBRA is present).\n5. **Director subtracks** — Your First Ordination, Enshrine the Ancestors, Inscribe the Reliquary, Tethered Cloud Builds, Airgap Your Builds, Building for Crucibles. Always visible; not highlighted in first iteration (refine after review).\n6. **Evaluation track** — Assay the Realm. Always visible, not highlighted.\n7. **Realm Dashboard** — trailing reference surface linking to `rbw-gOr.OnboardReference.sh` (the existing reference tabtarget, until A6 produces a Frame 4-refined replacement in a later pace).\n8. **\"Find your role\" guidance** — short text block explaining that a learner who doesn't know which path to pick can run the Crash Course first, or use the old `rbw-go.OnboardMAIN.sh` triage (preserved for fallback during the heat).\n\n**Tracks without tabtargets yet** (all 13 except Crash Course): display the track name + short description + a `[planned]` marker instead of a tabtarget reference. The menu is honest about coverage.\n\n**Probe-aware highlighting mechanism** (first iteration): use `zrbho_probe_role_credentials` (existing helper) to determine RBRA placement. Highlight with a leading `→ ` prefix on the suggested menu item; plain `  ` leading on others. Simple, discoverable, easy to revise after review.\n\n**Tabtarget dispatcher** at `tt/rbw-o.OnboardingStartHere.sh`:\n```bash\n#!/bin/bash\nexport BURD_LAUNCHER=\".buk/launcher.rbw_workbench.sh\"\nexport BURD_NO_LOG=1\nexec \"${BASH_SOURCE[0]%/*}/../${BURD_LAUNCHER}\" \"${0##*/}\" \"${@}\"\n```\n(Exact copy of the existing 5-line pattern; handbook output is ephemeral per the paddock's \"Handbook Output Is Ephemeral\" decision, so `BURD_NO_LOG=1` is correct.)\n\n**Zipper enrollment** in `Tools/rbk/rbz_zipper.sh` — add a new `RBZ__GROUP_ONBOARDING` group (description: \"Onboarding — Handbook restart\") positioned adjacent to the existing `RBZ__GROUP_GUIDE`. First enrollment:\n```bash\nbuz_group   RBZ__GROUP_ONBOARDING  \"rbw-o\"   \"Onboarding — Handbook restart\"\nz_mod=\"rbho_cli.sh\"\nbuz_enroll  RBZ_ONBOARD_START_HERE \"rbw-o\"   \"${z_mod}\"  \"rbho_start_here\"   \"\"  \"Onboarding start — probe-aware menu into handbook tracks\"\n```\n\n## Artifact 2 — `rbho_crash_course()` + `tt/rbw-Occ.OnboardingCrashCourse.sh` + zipper entry\n\n**Sequence function** in `Tools/rbk/rbho_onboarding.sh` next to `rbho_start_here()`: `rbho_crash_course()` rendering the full Crash Course handbook at Frame 4-refined fidelity.\n\n**Learning outcomes** (stated explicitly in a preamble section): after completing the Crash Course, a learner can:\n- Invoke any tabtarget and interpret its output\n- Locate the six user-facing regimes and run validate/render against each\n- Recognize the diagnostic-failure pattern and read it as teaching\n- Know where logs land and why\n- Understand the difference between station-scoped (BURS) and repo-scoped (BURC/RBRR) config\n- Decide which track to run next\n\n**Unit breakdown** (6–10 units, per the per-unit brevity discipline in the paddock — 3–5 sentences of teaching + 3–6 probe lines where applicable):\n\n1. **What you're looking at** (teaching-only) — introduce Recipe Bottle, tabtargets (link: `#Tabtarget`), the `tt/` directory, the `{colophon}.{frontispiece}.sh` naming. Concrete examples (`tt/rbw-go.OnboardMAIN.sh`). Vocabulary: tabtarget, colophon, frontispiece.\n2. **Regimes — the configuration map** (teaching-only) — introduce regimes (link: `#Regime`), the `rbw-r{letter}{r|v}` tabtarget pattern (render vs validate per regime), and the 13-regime landscape with the 6 user-facing ones called out. Teach the *pattern*, not the inventory. Vocabulary: regime, render, validate.\n3. **Repository regime** (verified) — introduce RBRR (link: `#RBRR`), explain it holds depot identity shared across the team, show concrete example from `.rbk/rbrr.env` if populated or say \"not yet populated\" if not. Probe: does `${RBBC_rbrr_file}` exist and have non-empty `RBRR_DEPOT_PROJECT_ID`? Fix path: `tt/rbw-rrv.ValidateRepoRegime.sh`. Vocabulary: RBRR, depot.\n4. **Station regime — per-developer settings** (verified) — introduce BURS (link: `#BURS`) and BURC (link: `#BURC`), explain station-scoped vs repo-scoped, call out `BURS_LOG_DIR` as the anchor. Probe: `BURS_LOG_DIR` defined in station env and directory exists. Fix path: `tt/rbw-rsv.ValidateStationRegime.sh`. Vocabulary: station, BURS, BURC.\n5. **The Marshal Zero first lesson** (teaching + verified, the crash course's anchor moment) — introduce `rblm_zero()` (link: `#MarshalZero`), explain what the zero state looks like (blanked identity fields, deleted credentials, preserved OAuth), then describe the diagnostic-failure experience (link: `#DiagnosticFailure`) of running `tt/rbw-rrv.ValidateRepoRegime.sh` against a zero state. Teach: *\"In this system, configuration errors fail diagnostically. Trust the error messages.\"* Probe: show whether the current state is fully populated, partially populated, or zero-like. Vocabulary: Marshal Zero, diagnostic failure.\n6. **Where logs land** (verified) — probe `BURS_LOG_DIR` exists and has recent activity. Teach: state-changing commands log; handbook display commands don't. Show the log directory if accessible. No vocabulary new to this unit; reinforces BURS.\n7. **Next steps** (teaching-only) — summarize what the learner now knows, and point at the StartHere menu for selecting a track based on their intent. Link back to `rbw-o.OnboardingStartHere.sh` via `buh_cT`.\n\n**Two entry states** handled without branching: for bare-fork (blank RBRR), the verified units show red with teaching-focused context about what the learner will fill in via the payor track; for joining-existing-project (populated RBRR), the same units show green with verification-focused context about what the existing team has done.\n\n**Tabtarget dispatcher** at `tt/rbw-Occ.OnboardingCrashCourse.sh` — same 5-line pattern as Artifact 1.\n\n**Zipper enrollment** in `rbz_zipper.sh`, next to the START_HERE entry under the same `RBZ__GROUP_ONBOARDING` group:\n```bash\nbuz_enroll  RBZ_ONBOARD_CRASH_COURSE \"rbw-Occ\" \"${z_mod}\"  \"rbho_crash_course\" \"\"  \"Crash Course — universal prerequisite: tabtargets, regimes, Marshal Zero\"\n```\n\n## Artifact 3 — Paddock refinement (remaining items)\n\nThe paddock was already updated during reslate to:\n- Sweep the `rbw-h?` prefix collision to the two-colophon scheme\n- Remove the interview-transcript phantom and add the mounting-corrections section\n- Add the Public-Docs Anchor Coordination section (existing / new-for-Crash-Course / new-for-later-tracks)\n- Flag the \"kindle\" vocabulary overload in Open Questions\n- Assign provisional `Occ` for Crash Course\n- Expand References with the dispatch-chain files\n\nRemaining Artifact 3 work (during or after drafting, via `jjx_paddock` setter):\n\n- **Finalize full track acronym table** — assign `rbw-O??` subfunction letters for the other 12 tracks. Suggested (for review): `Ots` Take Your Station, `Ofc` First Crucible, `Oed` Establish Depot, `Okr` Knight the Realm, `Ork` Receive Knighthood, `Oof` First Ordination, `Oea` Enshrine Ancestors, `Oir` Inscribe Reliquary, `Otc` Tethered Cloud, `Oac` Airgap Builds, `Obc` Building for Crucibles, `Oar` Assay Realm. Record as a table, not just a list. Open for revision.\n- **Update the Anchor Coordination section** with any new anchors the Crash Course drafting reveals. The listed `Tabtarget`, `Regime`, `RBRR`, `BURC`, `BURS`, `MarshalZero`, `DiagnosticFailure` are the initial set — if drafting uses more, add them.\n- **Record the implementation sequence decision**. Paddock currently says \"First implementation target: Kindle Your First Crucible\" — but this pace implements the Crash Course (and StartHere menu) first. Either (a) this pace is a sequencing revision (Crash Course first because it's the universal foundation), or (b) this pace is a StartHere-and-entry-point foundation that precedes the First Crucible implementation. Pick one and state it.\n- **Add any open questions** surfaced during drafting, especially around probe-aware highlighting mechanics, the zero-state detection heuristic, and the teaching voice for the Marshal Zero moment.\n\n## Deliverable shape\n\nThis pace produces a **first-iteration working implementation**:\n\n- Two new sequence functions in `Tools/rbk/rbho_onboarding.sh`: `rbho_start_here()` and `rbho_crash_course()`\n- Two new tabtarget dispatcher files: `tt/rbw-o.OnboardingStartHere.sh` and `tt/rbw-Occ.OnboardingCrashCourse.sh`\n- New `RBZ__GROUP_ONBOARDING` group + two `buz_enroll` entries in `Tools/rbk/rbz_zipper.sh`\n- Paddock refinement per Artifact 3\n\n**Review happens against the running code**: user invokes `tt/rbw-o.OnboardingStartHere.sh` and `tt/rbw-Occ.OnboardingCrashCourse.sh`, reads the output, annotates what works and what doesn't. Approval unblocks later tracks (First Crucible, Take Your Station, etc.). Rejection or revision request triggers another drafting cycle on this pace.\n\n**Local verification before handoff**: run both new tabtargets, confirm dispatch works (no kindle errors, no missing-tabtarget errors from `buz_healthcheck`), confirm probe results render, confirm `buh_tlt` links resolve. Then `jjx_record` the commit with an explicit file list.\n\n## Out of Scope\n\n- **Probe implementation beyond existing helpers** — use `zrbho_probe_role_credentials`, `zrbho_po_extract_capture`, and `zrbho_po_status` as-is. New probes only where the Crash Course units cannot be served by existing helpers, and only filesystem-level (no docker, no gcloud).\n- **Agent-learner eval design** — deferred to a dedicated pace\n- **Retirement of `rbw-gO*` tabtargets and their `rbho_triage/retriever/director/governor/payor` sequences** — old and new coexist; cutover happens in a later pace once all replacements are proven\n- **First Crucible handbook** — separate pace\n- **The other 12 handbook tracks** — separate paces\n- **Adding public-docs anchors to `README.md`** — that's `₣A5`'s job; this pace only consumes the anchors listed in the paddock's coordination section and records any new ones\n- **Fancy probe-aware highlighting** — simple `→ ` prefix in first iteration; refinements after review\n\n## References\n\n- `Tools/rbk/rbho_onboarding.sh` — existing sequences (especially `rbho_triage` ~L332 and `rbho_retriever` ~L438) as structural templates\n- `Tools/rbk/rbho_cli.sh` — CLI furnish; no changes needed\n- `Tools/rbk/rbz_zipper.sh` — zipper registry; add new group and entries\n- `Tools/rbk/rblm_cli.sh` — `rblm_zero()` reference for Crash Course Marshal Zero unit\n- `Tools/buk/buh_handbook.sh` — display primitive catalog\n- `Tools/buk/buz_zipper.sh` — zipper mechanism reference\n- `tt/rbw-go.OnboardMAIN.sh` — 5-line thin dispatcher pattern (copy for both new tabtargets)\n- `₣A6` paddock — Prefix Context (Prerequisites), Anchor Coordination, Teaching Voice, Open Questions, Provenance (Mounting corrections), Rough Track Roster",
              "jjgtn_text": "## Character\nSecond implementation pace. By this point, Frame 4-refined is proven on Crash Course, and the First Crucible draft is reviewed and accepted. This pace is a larger build-out than `₢A6AAA` but lower-risk because patterns are established. The first full learner track to work end-to-end — the validation that the heat's core thesis (teach-first pedagogy) actually delivers.\n\n## Docket\n\nImplement the \"Your First Crucible\" handbook (`tt/rbw-Ofc.OnboardingFirstCrucible.sh`) from the draft produced in Pace `₢A6AAC`, plus any infrastructure identified as needed during that draft.\n\n### Prerequisites\n- Pace `₢A6AAC` complete and accepted\n- Pace `₢A6AAA` implementations stable (no regressions in Crash Course or OnboardingStartHere)\n\n### Implement `tt/rbw-Ofc.OnboardingFirstCrucible.sh`\n\nFollowing the same pattern established in Pace `₢A6AAA`:\n- Dispatch to new function in `rbho_onboarding.sh`: `rbho_first_crucible` (or similar)\n- Renders the full Frame 4-refined document from the Pace `₢A6AAC` draft\n- Probes embedded inline\n- Uses `buh_*` primitives for display\n- Register in `rbz_zipper.sh` under `RBZ__GROUP_ONBOARDING` as `RBZ_ONBOARD_FIRST_CRUCIBLE` (colophon `rbw-Ofc`)\n\n### Build out infrastructure identified in the draft\n\nFrom the Pace `₢A6AAC` draft's \"probes needed\" inventory, build only what's necessary:\n- Missing probes → add minimal probe functions to `rbho_onboarding.sh`\n- Missing tabtargets → only if they're essential to the crucible explorer path\n- Helper functions → scoped tightly, not generalized\n\nDo NOT generalize infrastructure beyond First Crucible's needs in this pace. The second track will tell us what to generalize; over-generalizing now risks building the wrong abstraction.\n\n### Update the menu\n\n`rbw-o.OnboardingStartHere.sh` menu from Pace `₢A6AAA` has \"Your First Crucible\" as a `[planned]` item. Replace the placeholder with a resolved `buh_tT` reference to `RBZ_ONBOARD_FIRST_CRUCIBLE`, drop the `[planned]` marker, and verify highlighting still works based on probe state.\n\n### Smoke testing\n\nManual test scenarios:\n1. **Cold start**: fresh user runs `tt/rbw-o.OnboardingStartHere.sh`, picks First Crucible, works through the handbook, produces a running crucible. Every probe in the handbook should turn green by the end.\n2. **Return visit**: after charging a crucible, re-run `tt/rbw-Ofc.OnboardingFirstCrucible.sh`. Verify probes show green (crucible charged, nameplate present, etc.) and teaching content is still readable as reference.\n3. **Quench and retry**: quench the crucible, re-run the handbook. Verify probes correctly show mixed state (kludged hallmark present, but crucible not charged).\n4. **Fresh nameplate**: if multiple nameplates are supported, verify handbook works for each.\n\nDocument observed behavior.\n\n### Agent-learner eval\n\nRun the prototype eval from `₢A6AAA` against the First Crucible handbook. Compare against Crash Course scores. Use the delta to sharpen the eval: a good track should score meaningfully higher on comprehension than a stripped variant.\n\nIf the eval reveals systematic failures (agent can't understand what a crucible IS, agent fails to predict what `charge` does), note them — they're the next interview's material.\n\n### Success criteria\n- `tt/rbw-Ofc.OnboardingFirstCrucible.sh` renders a complete Frame 4-refined document\n- All probe states work correctly across cold-start, return, quench+retry, and fresh-nameplate scenarios\n- Menu from Pace `₢A6AAA` correctly routes and highlights First Crucible\n- Prototype eval produces scores and failure modes\n- A real human (user) walks through the track cold and produces a running crucible without outside help\n\n### Out of Scope\n- Other learner tracks\n- Full eval harness\n- Shared snippet mechanism\n- Generalizing any infrastructure beyond First Crucible's direct needs\n\n## References\n- Pace `₢A6AAC` draft\n- Pace `₢A6AAA` outputs (patterns, eval prototype, `rbho_onboarding.sh` post-Crash Course state)\n- `Tools/rbk/vov_veiled/RBSCC-crucible_charge.adoc`\n- `Tools/rbk/vov_veiled/RBSAK-ark_kludge.adoc`\n- `.rbk/tadmor/rbrn.env` — nameplate reference\n- `₣A6` paddock",
              "jjgtn_text": "## Character\nSecond implementation pace. By this point, Frame 4-refined is proven on Crash Course, and the First Crucible draft is reviewed and accepted. This pace is a larger build-out than `₢A6AAB` but lower-risk because patterns are established. The first full learner track to work end-to-end — the validation that the heat's core thesis (teach-first pedagogy) actually delivers.\n\n## Docket\n\nImplement the \"Your First Crucible\" handbook (`tt/rbw-Hfc.FirstCrucible.sh`) from the draft produced in Pace `₢A6AAC`, plus any infrastructure identified as needed during that draft.\n\n### Prerequisites\n- Pace `₢A6AAC` (first-crucible-draft) complete and accepted\n- Pace `₢A6AAB` implementations stable (no regressions in Crash Course or HandbookStartHere)\n\n### Implement `tt/rbw-Hfc.FirstCrucible.sh`\n\nFollowing the same pattern established in Pace `₢A6AAB`:\n- Dispatch to new function in `rbho_onboarding.sh`: `rbho_first_crucible` (or similar)\n- Renders the full Frame 4-refined document from the Pace `₢A6AAC` draft\n- Probes embedded inline\n- Uses `buh_*` primitives for display\n- Register in `rbz_zipper.sh`\n\n### Build out infrastructure identified in the draft\n\nFrom the Pace `₢A6AAC` draft's \"probes needed\" inventory, build only what's necessary:\n- Missing probes → add minimal probe functions to `rbho_onboarding.sh`\n- Missing tabtargets → only if they're essential to the crucible explorer path\n- Helper functions → scoped tightly, not generalized\n\nDo NOT generalize infrastructure beyond First Crucible's needs in this pace. The second track will tell us what to generalize; over-generalizing now risks building the wrong abstraction.\n\n### Update the menu\n\n`rbw-h.HandbookStartHere.sh` menu from Pace `₢A6AAB` had First Crucible as a menu item. Verify it still renders correctly and highlights appropriately based on probe state. If the draft refined the menu text around First Crucible, update accordingly.\n\n### Smoke testing\n\nManual test scenarios:\n1. **Cold start**: fresh user runs `tt/rbw-h.HandbookStartHere.sh`, picks First Crucible, works through the handbook, produces a running crucible. Every probe in the handbook should turn green by the end.\n2. **Return visit**: after charging a crucible, re-run `tt/rbw-Hfc.FirstCrucible.sh`. Verify probes show green (crucible charged, nameplate present, etc.) and teaching content is still readable as reference.\n3. **Quench and retry**: quench the crucible, re-run the handbook. Verify probes correctly show mixed state (kludged hallmark present, but crucible not charged).\n4. **Fresh nameplate**: if multiple nameplates are supported, verify handbook works for each.\n\nDocument observed behavior.\n\n### Agent-learner eval\n\nRun the prototype eval from `₢A6AAB` against the First Crucible handbook. Compare against Crash Course scores. Use the delta to sharpen the eval: a good track should score meaningfully higher on comprehension than a stripped variant.\n\nIf the eval reveals systematic failures (agent can't understand what a crucible IS, agent fails to predict what `charge` does), note them — they're the next interview's material.\n\n### Success criteria\n- `tt/rbw-Hfc.FirstCrucible.sh` renders a complete Frame 4-refined document\n- All probe states work correctly across cold-start, return, quench+retry, and fresh-nameplate scenarios\n- Menu from Pace `₢A6AAB` correctly routes and highlights First Crucible\n- Prototype eval produces scores and failure modes\n- A real human (user) walks through the track cold and produces a running crucible without outside help\n\n### Out of Scope\n- Other learner tracks\n- Full eval harness\n- Shared snippet mechanism\n- Generalizing any infrastructure beyond First Crucible's direct needs\n\n## References\n- Pace `₢A6AAC` draft\n- Pace `₢A6AAB` outputs (patterns, eval prototype, `rbho_onboarding.sh` post-CC state)\n- `Tools/rbk/vov_veiled/RBSCC-crucible_charge.adoc`\n- `Tools/rbk/vov_veiled/RBSAK-ark_kludge.adoc`\n- `.rbk/tadmor/rbrn.env` — nameplate reference\n- `₣A6` paddock",
              "jjgtn_text": "## Character\nInterview pace — distinct from drafting and implementing. This is where we pause after the first complete track lands and integrate what the implementation taught us. Reconciling built reality with planned design. A conversation pace, not a code-producing one, but one that directly unblocks subsequent drafting paces.\n\n## Docket\n\nAfter First Crucible ships as working code, open an interview session to absorb implementation learnings, refine the paddock, and spec the next track(s).\n\n### Prerequisites\n- Pace `₢A6AAD` complete and accepted\n- All three handbook tabtargets (`rbw-o.OnboardingStartHere.sh`, `rbw-Occ.OnboardingCrashCourse.sh`, `rbw-Ofc.OnboardingFirstCrucible.sh`) stable and exercised\n\n### Review what was built\n\nWalk through the outcomes of the first implementation cycle:\n- What did Frame 4-refined feel like in practice? Any friction?\n- What did the prototype eval reveal? Any surprises in agent scores?\n- What infrastructure ended up being built vs planned?\n- Where did the draft-then-implement gap show up? (i.e., what did the draft assume that implementation proved wrong?)\n- Which assumptions flagged in Paces `₢A6AAA` and `₢A6AAC` turned out right, which were wrong?\n\n### Refine the paddock\n\nBased on implementation lessons:\n- Update the Frame 4-refined design decision with concrete observations\n- Adjust per-unit brevity discipline if the measured length/readability didn't match the prediction\n- Refine the rough track roster if Crucible implementation revealed structural issues\n- Update open questions list — close answered ones, add newly-surfaced ones\n- Record any new technical terms discovered or vocabulary overloads caught\n\n### Pick the next track\n\nCandidates (in rough order of implementation ease):\n1. **Take Your Station** — shortest path, probe-heavy, exercises monotonic probe framing, minimal teaching\n2. **Establish the Depot** — highest-touch for first-time users, longest wall-clock (~15 min), most external dependencies (GCP, OAuth)\n3. **Knight the Realm** — governor administration, introduces Knight the Realm + Receive Your Knighthood pair\n\nEach choice has trade-offs:\n- Take Your Station tests probe framing but not teaching depth\n- Establish the Depot tests real-world applicability but adds cloud-flake risk\n- Knight the Realm exercises cross-track shared snippets (Secret Handling Primer appears in both governor-side and recipient-side tracks)\n\nPick one or two based on what implementation lessons suggest would advance the heat most. Spec it at the level of the `₢A6AAC` docket.\n\n### Slate the next drafting pace\n\nProduce a gazette entry for the next track's draft pace, to be enrolled at the end of this interview pace. The pattern continues: draft → implement → reconcile, for each track in sequence.\n\n### Consider eval sophistication\n\nBy this point, the prototype eval from `₢A6AAA` has run against two tracks. Is it time to graduate from prototype to real release gate? Or does the prototype still need more iteration?\n\nIf graduating: slate a dedicated \"eval-harness-formalize\" pace that builds the release-gate mechanism properly. If not: note what the prototype still needs to learn before we commit to gate mechanics.\n\n### Wait-time pedagogy (defer or engage?)\n\nEstablish the Depot and Tethered Cloud Builds both have real wall-clock wait times (15 min and 20 min respectively). The pedagogy for \"what does the learner do during the wait\" is an open question we haven't addressed. If the next track is one of these, this question becomes blocking. If the next track is Take Your Station, we can defer.\n\n### Record in paddock\n\nAt the end of this pace, the paddock should be meaningfully updated with:\n- Concrete observations from implementation, not speculation\n- Next pace(s) slated and visible on the rail\n- A clearer picture of what the release gate looks like\n- Refined rough track roster\n\n### Out of Scope\n- Drafting the next track (that's the next drafting pace)\n- Implementing anything\n- Agent-learner eval harness beyond what's already running\n\n## References\n- Paces `₢A6AAA` through `₢A6AAD` outputs\n- `₣A6` paddock\n- Any new paddock/docket/gazette artifacts from subsequent officia working in this heat",
              "jjgtn_text": "## Character\nInterview pace — distinct from drafting and implementing. This is where we pause after the first complete track lands and integrate what the implementation taught us. Reconciling built reality with planned design. A conversation pace, not a code-producing one, but one that directly unblocks subsequent drafting paces.\n\n## Docket\n\nAfter First Crucible ships as working code, open an interview session to absorb implementation learnings, refine the paddock, and spec the next track(s).\n\n### Prerequisites\n- Pace `₢A6AAD` complete and accepted\n- All three handbook tabtargets (`rbw-o.OnboardingStartHere.sh`, `rbw-Occ.OnboardingCrashCourse.sh`, `rbw-Ofc.OnboardingFirstCrucible.sh`) stable and exercised\n\n### Review what was built\n\nWalk through the outcomes of the first implementation cycle:\n- What did Frame 4-refined feel like in practice? Any friction?\n- What did the prototype eval reveal? Any surprises in agent scores?\n- What infrastructure ended up being built vs planned?\n- Where did the draft-then-implement gap show up? (i.e., what did the draft assume that implementation proved wrong?)\n- Which assumptions flagged in Paces `₢A6AAA` and `₢A6AAC` turned out right, which were wrong?\n\n### Refine the paddock\n\nBased on implementation lessons:\n- Update the Frame 4-refined design decision with concrete observations\n- Adjust per-unit brevity discipline if the measured length/readability didn't match the prediction\n- Refine the rough track roster if Crucible implementation revealed structural issues\n- Update open questions list — close answered ones, add newly-surfaced ones\n- Record any new technical terms discovered or vocabulary overloads caught\n\n### Pick the next track\n\nCandidates (in rough order of implementation ease):\n1. **Take Your Station** — shortest path, probe-heavy, exercises monotonic probe framing, minimal teaching\n2. **Establish the Depot** — highest-touch for first-time users, longest wall-clock (~15 min), most external dependencies (GCP, OAuth)\n3. **Knight the Realm** — governor administration, introduces Knight the Realm + Receive Your Knighthood pair\n\nEach choice has trade-offs:\n- Take Your Station tests probe framing but not teaching depth\n- Establish the Depot tests real-world applicability but adds cloud-flake risk\n- Knight the Realm exercises cross-track shared snippets (Secret Handling Primer appears in both governor-side and recipient-side tracks)\n\nPick one or two based on what implementation lessons suggest would advance the heat most. Spec it at the level of the `₢A6AAC` docket.\n\n### Slate the next drafting pace\n\nProduce a gazette entry for the next track's draft pace, to be enrolled at the end of this interview pace. The pattern continues: draft → implement → reconcile, for each track in sequence.\n\n### Consider eval sophistication\n\nBy this point, the prototype eval from `₢A6AAA` has run against two tracks. Is it time to graduate from prototype to real release gate? Or does the prototype still need more iteration?\n\nIf graduating: slate a dedicated \"eval-harness-formalize\" pace that builds the release-gate mechanism properly. If not: note what the prototype still needs to learn before we commit to gate mechanics.\n\n### Wait-time pedagogy (defer or engage?)\n\nEstablish the Depot and Tethered Cloud Builds both have real wall-clock wait times (15 min and 20 min respectively). The pedagogy for \"what does the learner do during the wait\" is an open question we haven't addressed. If the next track is one of these, this question becomes blocking. If the next track is Take Your Station, we can defer.\n\n### Record in paddock\n\nAt the end of this pace, the paddock should be meaningfully updated with:\n- Concrete observations from implementation, not speculation\n- Next pace(s) slated and visible on the rail\n- A clearer picture of what the release gate looks like\n- Refined rough track roster\n\n### Out of Scope\n- Drafting the next track (that's the next drafting pace)\n- Implementing anything\n- Agent-learner eval harness beyond what's already running\n\n## References\n- Paces `₢A6AAA` through `₢A6AAD` outputs\n- `₣A6` paddock\n- Any new paddock/docket/gazette artifacts from subsequent officia working in this heat",
