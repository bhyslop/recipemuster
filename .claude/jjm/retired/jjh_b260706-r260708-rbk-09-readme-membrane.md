# Heat Trophy: rbk-09-readme-membrane

**Firemark:** ₣Bx
**Created:** 260706
**Retired:** 260708
**Status:** retired

## Paddock

## Character

Concept-tier judgment already spent in the founding design conversation (260706);
what remains is execution against cinched design.
Every pace in this heat mounts on Opus — operator direction;
lower-tier delegation is deferred pending upcoming features.

## Context

The veiled spec family is written with full lapidary rigor;
README.md is the only sanctioned bridge between that interior and the outside world.
The exposure ladder names the rungs (hearting, quoin, ashlar; bondstone, toothing);
the wall itself — the shipped face as a first-class, maintained artifact with its own discipline —
was unnamed.
This heat lands the treatment: concept in MCM, taxonomy stitch in the RoE detail,
RB instance designation plus a by-hand audit, and the mechanized checker banked to the matricula constellation.

## Cinched

The face law: a token on the face is legal iff it is an ashlar in display form or plain prose;
a non-ashlar interior name — or the lapidary meta-register itself — surfacing on the face is the defect.
The exposure ladder already carries the sanction semantics (ashlar marking plus broadside registration);
no translation table exists or is needed.

Concept home is MCM Lapidary, not AXLA:
the masonry asterism holds the seat (the standing definitions already speak of "the wall" ambiently),
and splitting the family across documents would break asterism cohesion and the audible register.

Exactly two mints, both grep-gated clean (260706):
mcm_revetment — the maintained public face artifact,
a shipped prose surface presenting a veiled interior in dressed form
(fortification masonry; the protective valence is load-bearing — a leak here is an IP leak, not an aesthetic slip);
mcm_spall — the leak defect
(masonry spalling: the dressed face breaking away to expose the core).
Deliberately NOT minted: the translation act ("dressing" stays ambient) and "the wall" itself
(it names the whole, face plus hearting, and stays ambient scenery).
A felt need for a third quoin is stop-and-surface, never a quiet mint.

The four-invariant face law:
ashlar-or-prose only;
broadside-complete — every ashlar used on the face resolves to its broadside entry;
references resolve — every ](#X) lands on a declared <a id="X">;
register silence — the lapidary meta-register never crosses.
Recursion clause: the revetment vocabulary is itself interior —
revetment and spall never appear on a revetment.

Membrane placement: this completes the interior disciplines as a triad of edges —
Interface Contamination guards the input edge,
the Palisade membrane the foreign outcome edge,
the revetment the publication edge, fully sovereign on both sides,
the system's only transmitting membrane (it ships a faithful translation by design).
Distinct from the veil family (VOr_q4f):
the veil decides which files leave;
the face decides what a leaving file may say.

The checker is NOT built here — it banks into the matricula constellation:
face files as a file-role,
the display-form README anchors as a non-cipher vesture,
the inscription-leak check as a presentment producer over face files,
and the anchor sweep as the second consumer of the artifact-integrity check (paddock/docket is the first).
Word-surface leaks (the word, not the inscription) are grimoire-tier
and stay deferred with the grimoire by standing decision.
₣Bg's terminal fence pace performs the placement walk;
₣Bj receives what crosses the fence.

## Revetment audit residue (260706)

The by-hand audit of README.md ran all three batteries and every hit was operator-verdicted.
RB's revetment was designated in CLAUDE.md, beside the broadside designation, pointing to MCM Lapidary for the concept and the face law.

Two spall were found and evicted (not whitelisted):
rbid — an interior binary name editorialized into a reference-tree description (README:751, now "[Ifrit] attack-binary source");
colophon and frontispiece — the BUK tabtarget-naming meta-register surfacing in prose (README:53, now "{prefix}.{label}").

The accepted-homograph roster — face tokens ratified as sanctioned despite colliding with interior vocabulary, which the future facade-lint must NOT flag:
ashlar-homographs (registered RB ashlars overlapping the interior meta-register) — Census (vs the grep-as-registry census), Tally (vs AXLA axd_tally), Ark, Muniment, Reliquary;
English homographs (plain sense, unrelated to the interior term) — mint/minted (token-minting, not name-minting), channel (network path, not BUK Channel), seam (code seam, not the test-seam register), roster.

Anchor sweep landed clean: every ](#X) resolves to a declared anchor;
two orphan anchors (HowThisIsNormallyDone, Roadmap) are benign appendix deep-link targets, noted not fixed.

Second face: the public project page (scaleinv.github.io/recipebottle) as a possible second revetment is deferred;
README stands as RB's sole revetment for now.

## Done when

The MCM Lapidary subsection stands (two quoins, the law, the recursion clause);
the RoE detail carries the publication edge citing MCM;
RB's revetment is designated and the by-hand audit has run with every hit operator-verdicted;
and the facade lint is banked into ₣Bg's fence-walk paddock line.

## Paces

### mint-revetment-subsection (₢BxAAA) [complete]

**[260706-1049] complete**

## Character
High-craft authoring against cinched design; the judgment is spent — do not reopen the mints.
Implementation model: Opus (operator direction).

## Shape
Author the new subsection of MCM `== Lapidary` (following Exposure) that homes the face treatment:
mint mcm_revetment and mcm_spall into the masonry asterism,
state the four-invariant face law,
and carry the recursion clause — the revetment vocabulary never appears on a revetment.
Mints, non-mints, and law are cinched in the paddock;
exactly two quoins — a felt need for a third is stop-and-surface.
Match the register of the adjacent Exposure definitions (ashlar, bondstone, hearting are the exemplars);
linked-term structure and mapping-section registration per MCM's own patterns;
no rationale restatement in definition text.
Cold-probe the final definition text with the operator before landing.
Resolve inside this pace: whether the face-law invariants mint as rivets now
(MCM has no rivet family yet) or stay prose until the lint cites them — lean prose-until-cited.

## Done when
The subsection stands in MCM with both quoins fully catalogued,
the law and recursion clause stated,
and the operator has ratified the prose.

### stitch-roe-publication-edge (₢BxAAB) [complete]

**[260706-1058] complete**

## Character
One-screen additive edit under the RoE covenant;
content was ratified in the founding design conversation (260706).
Implementation model: Opus (operator direction).

## Shape
Add the publication edge to the interior-disciplines relationship in claude-cmk-roe-detail.md:
Interface Contamination guards the input edge,
the Palisade membrane the foreign outcome edge,
the revetment the publication edge — fully sovereign both sides, the system's only transmitting membrane.
Distinguish from the veil family (VOr_q4f):
the veil decides which files leave; the face decides what a leaving file may say.
Cite MCM Lapidary as the vocabulary home; restate no definitions.
Weigh a one-line mention in the always-on claude-cmk-roe.md
and settle that call with the operator in-session — the covenant bars unilateral edits.

## Done when
The detail doc carries the third edge with the MCM citation,
the always-on question is decided with the operator,
and no vocabulary is restated outside MCM.

### designate-audit-rb-revetment (₢BxAAC) [complete]

**[260706-1126] complete**

## Character
Mechanical batteries with operator-ratified verdicts;
the audit proposes, the operator decides.
Implementation model: Opus (operator direction).

## Shape
Designate README.md as RB's revetment beside the broadside designation in CLAUDE.md
(the broadside precedent for instance designations; surface the canonical-home question if it snags).
Decide with the operator whether the public project page is a second revetment now or later.
Run the by-hand audit as the ratification oracle:
the inscription battery (seated interior names, rivet IDs, veiled paths — against README);
the word-surface battery (the lapidary meta-register and interior quoin words as words —
derive the list from the MCM/AXLA/VOS0 mapping sections and the CLAUDE.md tables);
the anchor sweep (every ](#X) resolves to a declared <a id="X">; orphaned anchors noted).
Deliver classified hit tables with proposed verdicts — leak, innocent homograph, or sanctioned ashlar;
verdicts land with the operator.
Durable residue (an accepted-homograph roster the future lint needs) goes to this heat's paddock as shape;
the run record is the commit.

## Done when
The designation is committed,
the second-face question is decided,
all three batteries have run with every hit operator-verdicted,
and a clean (or cleaned) face is on record.

### bank-facade-lint (₢BxAAD) [complete]

**[260707-1225] complete**

## Character
Single cross-heat paddock curry through the gazette ceremony; content settled.
Implementation model: Opus (operator direction).

## Shape
Curry ₣Bg's paddock:
add the facade lint to the placement walk its terminal fence pace performs
(re-derive that pace from jjx_show; do not trust a remembered ID),
aimed at ₣Bj —
the same conscious-placement move that paddock already makes for the reference-hygiene lint.
The banking line carries:
face files as a file-role;
the display-form README anchors as a non-cipher vesture (the "Vesture gaps" seat);
the inscription-leak check as a presentment producer over face files;
the anchor sweep as the second consumer of the artifact-integrity check (paddock/docket first).
State that word-surface leaks are grimoire-tier and stay deferred with the grimoire,
so the fence walk does not resurrect them.
Cite the MCM subsection this heat lands as the doctrine home; the lint cites, never restates.

## Done when
₣Bg's paddock carries the banking line
and the fence-walk pace can place the facade lint without re-derivation.

### payor-credential-lifecycle-model (₢BxAAE) [complete]

**[260708-1136] complete**

## Character
Authoring + surface reconciliation against a now-established model, Fable-tier:
the research core is resolved;
what remains authors canon every user-facing surface obeys,
plus two operator console acts and one open empirical question.

## Done when
- RBSRO carries the one canonical Payor credential lifecycle statement:
  one human secret (the OAuth client JSON, identifying the app not a person);
  Establish then Install (browser consent mints the refresh token, stored disk-resident in the payor rbro.env);
  "refresh" is only the invisible refresh-token to access-token exchange inside every Payor call (access tokens are memory-only);
  no scheduled expiry in the sanctioned console state;
  death modes: explicit revocation, six months idle, the 50-live-token-per-client cap, org reauth policy (invalid_rapt);
  recovery is always re-Install with the same JSON (re-Establish / rotate only if the client itself is lost or compromised);
  rbw-ap is the read-only health probe.
- The sanctioned console state is prescribed and the operator's manor conforms.
  Recommendation awaiting operator ratification: Internal user type —
  every RB consumer owns a GCP org by construction (the workforce pool requires it;
  Cloud Identity Free suffices, no Workspace/corporate-gmail needed);
  External+Testing becomes a named misconfiguration (test-user gate + 7-day tokens).
  RBSPE's audience step re-cut from "External (testing mode, no verification required)" to the ratified prescription.
  Operator acts pending: click Make internal on the Audience page, re-install, probe green —
  or the operator's decline is recorded and the prescription re-thought.
- README resolves the phantom Refresh glossary term per Lapidary:
  recast to name the real act (re-Install) or retire it —
  one definition site plus one cross-reference in the OAuth Probe entry;
  the Install entry's "persists until explicitly revoked" corrected to the true rule;
  anchor sweep stays clean if the Refresh anchor is removed.
- The client-secret JSON gains a prescribed durable private home under RBRR_SECRETS_DIR
  (operator practice: a client_secrets/ subdirectory),
  replacing the ~/Downloads glob across RBSPE, RBSPI, RELEASE Step 1, and onboarding prose;
  the pass-exact-path-when-multiple caveat survives.
- The refresh-exchange error path discriminates error_subtype invalid_rapt
  and yawps the specific truth — the org's Google Cloud sign-in session lapsed;
  re-run the Install tabtarget with the saved client-secret JSON —
  instead of folding it into the generic expired-or-invalid die.
  Find sites: grep invalid_rapt / expired in rbgp_payor.sh and the rba/rbgo exchange siblings.
- CLAUDE.consumer.md and RBSAO tell consistent subsets;
  the RBS0 rbro_refresh_token quoin wording checked against the model.

## Cinched
- "Refresh" is not a separate user operation: the only refreshing is the automatic
  uncached refresh-token to access-token exchange inside every Payor call;
  recovering an expired or compromised token is re-install.
  (Cinch of the payor-refresh retirement; this session's research reinforces it —
  in no console state does any act renew a refresh token short of fresh consent.)
- The true expiry rule is ESTABLISHED (260708, web-verified against current Google docs):
  the 7-day rule binds solely to External+Testing test-user authorizations;
  Internal and In-production tokens are durable, subject only to the universal death modes.
  The "7-day" claims were wrong about our console state, not about Google;
  the prior neutralization (find via git log --grep=Neutralize) already cleared them — nothing to revert.

## Established 260708 (discovery state — read before executing)
- Live Audience-page reading: External + Testing, test-user list EMPTY, lifetime cap 0/100 (0 test, 0 other) —
  yet the payor (bhyslop@scaleinvariant.org, org owner) has consented repeatedly since February
  and held month-plus durable tokens.
  Google is demonstrably not enforcing the test-user gate or the 7-day expiry on same-org owner consents:
  UNDOCUMENTED lenience (searched, no Google doc found) — never build documentation on it;
  the Internal prescription exits it.
  Record this characterization at execution time in the W commit or a memo, never in the spec.
- Operator had NOT clicked Make internal at session end;
  the live token was freshly minted 260708 under External+Testing, probe green 5/5.
- invalid_rapt fired twice (260704 and 260708), the second ~72h after the 260705 install;
  operator reads the exact-72h spacing as an accident of timing; the cadence question is OPEN.
  Optional discriminator: admin console > Security > Access and data control > Google Cloud session control.
  Whatever the finding, operator-facing prose says only "your organization's sign-in policy" — org-generic.
- Switching user type External to Internal: effect on already-issued grants is undocumented;
  contained recovery is the practiced re-install plus rbw-ap probe.

## Sources
- Google current docs: Manage App Audience (support.google.com/cloud/answer/15549945);
  OAuth2 expiration causes (developers.google.com/identity/protocols/oauth2, Refresh token expiration section).
- Surfaces: README glossary anchors (Install / Refresh / OAuthProbe / Establish), RBSPE, RBSPI, RBSRO,
  RBS0 rbro_refresh_token quoin, RELEASE Step 1, Tools/rbk/vov_veiled/CLAUDE.consumer.md,
  the rbgp_payor.sh install and refresh-exchange paths.
- MCM Lapidary for the broadside/bondstone treatment of the Refresh term.

## Commit Activity

```
File-touch bitmap (x = pace commit touched file):

  1 A mint-revetment-subsection
  2 B stitch-roe-publication-edge
  3 C designate-audit-rb-revetment
  4 D bank-facade-lint
  5 E payor-credential-lifecycle-model

ABCDE
··x·x README.md
····x CLAUDE.consumer.md, RBSPE-payor_establish.adoc, RBSPI-payor_install.adoc, RBSRO-RegimeOauth.adoc, RELEASE.md, rbgc_constants.sh, rbgp_payor.sh, rbhopw_payor_wrapper.sh, rbhpe_establish.sh
··x·· CLAUDE.md
·x··· claude-cmk-roe-detail.md
x···· MCM-MetaConceptModel.adoc

Commit swim lanes (x = commit affiliated with pace):

  1 A mint-revetment-subsection
  2 B stitch-roe-publication-edge
  3 C designate-audit-rb-revetment
  4 D bank-facade-lint
  5 E payor-credential-lifecycle-model

123456789abcdefghijklm
···xx·················  A  2c
·····xx···············  B  2c
·······x·x············  C  2c
···········xx·········  D  2c
·················xxxxx  E  5c
```

## Steeplechase

### 2026-07-08 11:36 - ₢BxAAE - W

Payor credential lifecycle model landed as canon and enforced live. RBSRO carries the one canonical statement (one human secret — the OAuth client JSON, identifying the app never a person; Establish then Install; refresh only as the invisible refresh-token-to-access-token exchange inside every Payor call, access tokens memory-only; no scheduled expiry under the sanctioned Internal console state; death modes revocation / six months idle / per-client live-token cap / org sign-in policy as invalid_rapt; recovery always re-Install with the same JSON; rbw-ap the read-only probe). Internal prescription RATIFIED BY ACT: rbw-mI gained the consent-screen audience gate reading orgInternalOnly via the IAP brands API (read-only there — no API can flip it, so the gate names the console remedy); it failed honestly red on the live External manor, the operator clicked Make internal, and the full instaurate then proved green. The flip exposed the install ceremony's dead OOB flow (Testing-status apps are documented-exempt from the 2023 OOB block; Internal is production from birth) — install migrated to loopback redirect with paste-the-address-bar-URL ceremony, prompt=consent guaranteeing a refresh token on re-install; fresh Internal-provenance token minted live, probe 5/5, instaurate green under it. Surfaces reconciled: RBSPE audience step + establish handbook re-cut to Internal (test-user block removed), RBSPI re-cut to loopback + rotate-only-on-compromise, client_secrets/ durable home under RBRR_SECRETS_DIR replacing every ~/Downloads glob (RBSPI, RELEASE Step 1, CLAUDE.consumer.md, onboarding wrappers, exact-path caveat preserved), README phantom Refresh term retired with Install entry corrected to the true expiry rule (anchor sweep clean), invalid_rapt discriminated in the refresh exchange (org-generic message; positive firing untestable without a live policy lapse — verified by inspection only). Picket 122/123; the single red is pre-existing (rbtdru_kit_bash vs rbq_qualify.sh anchor gate from ₢BtAAG, untouched here). EMPIRICAL RECORD (deliberately homed here, never in spec): (1) pre-flip Audience page read External+Testing with EMPTY test-user list and 0/100 cap while the org-owner payor held month-plus durable tokens since February — Google demonstrably not enforcing the test-user gate or 7-day expiry on same-org owner consents; no documenting page found; the Internal state exits this lenience. (2) Already-issued grants SURVIVE the External-to-Internal flip: the pre-flip token authenticated instaurate and probed 5/5 immediately after the click; the fresh mint was hygiene, not necessity. (3) The invalid_rapt cadence question stays OPEN (72h spacing read as timing accident; admin-console session control the optional discriminator).

### 2026-07-08 11:30 - ₢BxAAE - n

Payor credential lifecycle canon and surface reconciliation: RBSRO carries the one canonical statement (one human secret, Establish-then-Install, refresh as invisible exchange only, no scheduled expiry under Internal, four death modes, re-Install as universal recovery, rbw-ap as probe); RBSPE audience step and the establish handbook re-cut to Internal with the test-user block removed; RBSPI re-cut to the loopback ceremony, rotate-only-on-compromise, and the client_secrets/ durable home under RBRR_SECRETS_DIR replacing every ~/Downloads glob (RBSPI, RELEASE Step 1, CLAUDE.consumer.md, onboarding wrappers); README retires the phantom Refresh glossary term, corrects the Install entry to the true expiry rule, and recasts the OAuth Probe cross-reference (anchor sweep clean); invalid_rapt discriminated in the refresh exchange with the org-generic sign-in-policy message

### 2026-07-08 11:20 - ₢BxAAE - n

Install ceremony step 5 hardened with the literal browser error text — the connection-refused landing read as failure in live use even when forewarned, so the prose now names 'This site can't be reached / localhost refused to connect' and calls it the success landing

### 2026-07-08 11:09 - ₢BxAAE - n

Payor install migrated off the dead OOB flow onto loopback redirect: the Internal flip removed the Testing-status OOB exemption (OOB blocked for production apps since 2023, and Internal is production from birth), so the ceremony now pastes the address-bar redirect URL — code parsed from the code= param, percent-decoded — with prompt=consent forcing a fresh refresh token on every re-install; testing-mode doc REQUIREMENT recut to Internal

### 2026-07-08 10:56 - ₢BxAAE - n

Audience gate in manor instaurate: read the payor OAuth brand via the IAP brands API (orgInternalOnly is read-only there — no API can flip it) and die with the console remedy when the consent screen is absent or External; iap added to the payor-project API roster

### 2026-07-08 08:23 - Heat - T

bridled ₢BxAAE at fable

### 2026-07-08 08:23 - Heat - d

batch: 1 reslate

### 2026-07-07 19:25 - Heat - T

bridled ₢BxAAE at fable xhigh

### 2026-07-07 19:20 - Heat - S

payor-credential-lifecycle-model

### 2026-07-07 12:25 - ₢BxAAD - W

Cross-heat curry: banked the facade lint into ₣Bg's ## Open questions as one conscious-placement item for the terminal fence walk (₢BgAAF), mirroring the reference-placement lint. Seats it onto ₣Bg's existing constellation (face file-role, non-cipher anchor vesture extending the Vesture-gaps seat, inscription-leak check as a presentment producer, anchor sweep as the artifact-integrity check's second consumer), marks word-surface leaks grimoire-tier/deferred, and cites ₣Bx's MCM Lapidary 'The Revetment' as doctrine home (cites, never restates). Aimed at ₣Bj. Closes ₣Bx.

### 2026-07-07 12:23 - ₢BxAAD - L

claude-opus-4-8 landed

### 2026-07-07 11:42 - Heat - T

bridled ₢BxAAD at opus

### 2026-07-06 11:26 - ₢BxAAC - W

Ran RB's revetment audit. Designated README.md as RB's revetment in CLAUDE.md (a Revetment bullet beside the broadside designation, concept + four-invariant face law pointed to MCM Lapidary, not restated). Ran all three by-hand batteries: inscription and word-surface as a parallel fan-out, plus the anchor sweep. Found and evicted two spall — rbid (interior binary name, README:751 -> [Ifrit] attack-binary source) and colophon/frontispiece (BUK tabtarget-naming meta-register, README:53 -> {prefix}.{label}); every other hit ratified as sanctioned ashlar or innocent homograph. Anchor sweep clean: 0 broken refs, 2 benign orphan appendix anchors noted. Face verified clean post-eviction. Accepted-homograph roster and second-face deferral (project page held as later; README stands as RB's sole revetment) banked to the heat paddock as residue.

### 2026-07-06 11:25 - Heat - d

paddock curried: revetment audit residue: homograph roster + second-face deferral

### 2026-07-06 11:23 - ₢BxAAC - n

Designate README.md as RB's revetment and evict two spall from the face. CLAUDE.md: add a Revetment bullet beside the broadside designation in the Word-Selection (Lapidary) subsection — RB's revetment is README.md, concept home and four-invariant face law pointed to MCM Lapidary, not restated. README: evict rbid (interior binary name -> [Ifrit] attack-binary source, line 751) and colophon/frontispiece (BUK tabtarget-naming meta-register -> {prefix}.{label}, line 53). All three by-hand audit batteries run: inscription and word-surface (fan-out) plus anchor sweep (clean: 0 broken refs, 2 benign orphan anchors noted). Face verified clean post-eviction.

### 2026-07-06 10:58 - ₢BxAAB - W

Stitch the publication edge into the RoE interior-disciplines relationship: add a triad bullet to claude-cmk-roe-detail.md making the input/outcome dual a three-edge set — Interface Contamination (input edge), Palisade membrane (foreign outcome edge), revetment (publication edge, the singular transmitting membrane sovereign on both sides). Distinguishes the revetment from the veil family (VOr_q4f) and cites MCM Lapidary (mcm_revetment, mcm_spall, four-invariant face law) as vocabulary home, restating no definitions. Always-on claude-cmk-roe.md held as-is per operator ruling (covenant bars unilateral edits).

### 2026-07-06 10:58 - ₢BxAAB - n

Homed the three-edges frame in the CMK ROE detail: added a bullet to "Relationship to the interior disciplines" positioning Interface Contamination (input edge), the Palisade membrane (foreign outcome edge), and the revetment (publication edge) as the three sovereign-discipline boundaries where the system meets the outside world. Cast the revetment as the singular member — sovereign on both sides, the only transmitting membrane — distinguished it from the veil family (which decides which files leave, not what a leaving file may say), and pointed vocabulary home to MCM Lapidary rather than restating it.

### 2026-07-06 10:49 - ₢BxAAA - W

Authored MCM Lapidary === The Revetment subsection: minted mcm_revetment and mcm_spall into the masonry asterism (mapping-section registered + anchored definitions with masonry gestalts), stated the four-invariant face law and the reflexive recursion clause, matched the Exposure register. Rivet decision resolved prose-until-cited (no MCM rivet family opened). Operator ratified the prose before landing.

### 2026-07-06 10:48 - ₢BxAAA - n

Home the revetment face treatment in MCM Lapidary: mint mcm_revetment (the maintained public face of a veiled interior) and mcm_spall (the face-leak defect) into the masonry asterism, state the four-invariant face law (ashlar-or-prose only, broadside-complete, references resolve, register silence), and carry the recursion clause that the revetment vocabulary never appears on a revetment

### 2026-07-06 17:24 - Heat - d

batch: paddock, 4 slate

### 2026-07-06 17:23 - Heat - f

racing

### 2026-07-06 17:23 - Heat - N

rbk-09-readme-membrane

