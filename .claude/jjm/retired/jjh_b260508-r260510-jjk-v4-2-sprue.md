# Heat Trophy: jjk-v4-2-sprue

**Firemark:** ₣BE
**Created:** 260508
**Retired:** 260510
**Status:** retired

## Paddock

## Goal

Mint the unprefixed JSON property names and enum values in `.claude/jjm/jjg_gallops.json` so wire-level tokens follow the project's prefix discipline. The kit's `jjrt_types.rs` types are already prefixed (quoins, in MCM terms); this heat introduces the matching wire-side names (sprues — inlays once they follow discipline).

## Sprue scheme

Three sub-letters after `jj`:

- **X** (constant): `g` — Gallops domain
- **Y** (table-letter): the carrier struct in `jjrt_types.rs` — `r` for `jjrg_Gallops` (root), `h` for `jjrg_Heat`, `p` for `jjrg_Pace`, `t` for `jjrg_Tack`. For enum values, Y is the carrier of the field that holds the enum
- **Z** (role-letter): `n` for property name, `e` for enum value

The suffix after the underscore is the Rust field name (for properties) or the lowercase variant name (for enum values).

## Migration recipe

Per target, two edits with an MCP restart between them:

1. Add `#[serde(rename = "<minted>", alias = "<old-wire-name>")]`. Build. **Restart the chat session** so MCP loads the fresh binary. Run a *gallops-mutating* jjx call to round-trip the gallops file through the new wire-names. The robust idempotent pattern is to flip the heat's status: `jjx_alter stabled` then `jjx_alter racing` — guaranteed to fire `save()` regardless of starting state. (Setting status to its current value may short-circuit; a real flip avoids the ambiguity.) `jjx_record` and `jjx_paddock`-setter do **not** call `save()` on `jjg_gallops.json` — only heat/pace/tack mutations do, so only those round-trip.
2. Remove the transitional `alias` line. Build.

The blanket `#[serde(rename_all = "lowercase")]` comes off `jjrg_PaceState` and `jjrg_HeatStatus` once explicit per-variant renames replace it. The pre-existing `alias = "primed"` on `Bridled` stays as legacy compat — outside this heat's window.

## Batches and order

Carrier-isolated batches keep the silks doppelganger (Heat + Tack) and the three Gallops-vs-Heat near-doppelgangers (heats/paces, heat_order/order, next_heat_seed/next_pace_seed) in distinct paces:

1. **mint-heat-silks-pace-states** — `jjghn_silks` plus the four `jjrg_PaceState` variants. Proves the recipe end-to-end.
2. **mint-gallops-root-sprues** — the four properties on `jjrg_Gallops`.
3. **mint-heat-rest-and-status** — the five remaining `jjrg_Heat` properties plus the three `jjrg_HeatStatus` variants.
4. **mint-pace-and-tack-sprues** — `jjgpn_tacks` plus the six `jjrg_Tack` properties.

Subsequent dockets reference these by silks rather than re-listing targets.

## Spec touch

JJS0's prescriptive sprue declarations follow the AXLA mechanism: property-name sites (`axr_member` under `axr_record_json`) carry `axd_property` or `axd_positional` with the sprue inlined in body prose; enum-value sites (`axt_enum_value`) carry `axd_string` with the sprue in a `Serialized as <sprue> in JSON.` body line. JJS0 already carries these declarations for the gallops surface; the four mint paces below carry no JJS0 obligation — only the Rust serde renames and gallops-file rewrite. The same AXLA discipline extends across other specs via the foreign-sweep pace, which adds `axd_string` to existing-wire enum-value voicings outside the gallops surface.

## Out of scope

`.claude/jjm/jjs_studbook.json` (unconsumed by Rust source); the MCP tool wire format (different boundary); the gazette wire format (already prefixed `jjezs_*`); CLAUDE.md ceiling rule edit (completed at groom-time); a memo footnote about the relaxed ceiling (deferred, not blocking).

## Done when

`.claude/jjm/jjg_gallops.json` carries zero bare property names from the original 16 and zero bare enum values from the original 7. Build green; tests green.

## Paces

### axla-extension-and-foreign-sweep (₢BEAAG) [complete]

**[260509-1057] complete**

## Character

Editorial sweep applying axd_string to axt_enum_value voicings outside the gallops surface. Per-site mechanical; the breadth (~30 sites across RBS0/BUS0/JJSTF/JJSCGZ + JJS0 column-align/pensum-report families) earns a dedicated pace.

## Targets

Each axt_enum_value voicing in the foreign specs receives axd_string on its //axl_voices line and a body line of the canonical form `Serialized as `<existing-wire>` in <carrier>.` — wire token unchanged (no rename), carrier names the surrounding format (JSON, bash regime variable value, etc.).

## Out of scope

Property-name voicings (already carry axd_property/axd_positional). Gallops surface enums (pre-discharged by the heat-affiliated AXLA-mechanism touch). Sites whose enumeration genuinely lacks a string-encoded wire form — document and skip; future encoding dimensions (axd_integer, axd_object, etc.) cover them.

## Done when

- No bare `//axl_voices axt_enum_value` line remains in RBS0, BUS0, JJSTF, JJSCGZ, or the non-gallops JJS0 enum families, except documented non-string-wire skips.
- Build green.

## Discovery hints

`grep -rnE '^//axl_voices axt_enum_value$' Tools/{rbk,buk,jjk}/vov_veiled/*.adoc` enumerates the foreign sites.

**[260509-1046] rough**

## Character

Editorial sweep applying axd_string to axt_enum_value voicings outside the gallops surface. Per-site mechanical; the breadth (~30 sites across RBS0/BUS0/JJSTF/JJSCGZ + JJS0 column-align/pensum-report families) earns a dedicated pace.

## Targets

Each axt_enum_value voicing in the foreign specs receives axd_string on its //axl_voices line and a body line of the canonical form `Serialized as `<existing-wire>` in <carrier>.` — wire token unchanged (no rename), carrier names the surrounding format (JSON, bash regime variable value, etc.).

## Out of scope

Property-name voicings (already carry axd_property/axd_positional). Gallops surface enums (pre-discharged by the heat-affiliated AXLA-mechanism touch). Sites whose enumeration genuinely lacks a string-encoded wire form — document and skip; future encoding dimensions (axd_integer, axd_object, etc.) cover them.

## Done when

- No bare `//axl_voices axt_enum_value` line remains in RBS0, BUS0, JJSTF, JJSCGZ, or the non-gallops JJS0 enum families, except documented non-string-wire skips.
- Build green.

## Discovery hints

`grep -rnE '^//axl_voices axt_enum_value$' Tools/{rbk,buk,jjk}/vov_veiled/*.adoc` enumerates the foreign sites.

### mint-heat-silks-pace-states (₢BEAAA) [complete]

**[260509-1102] complete**

## Character

Mechanical migration of two targets to validate the sprue-minting recipe end-to-end. Low judgment surface; sonnet-friendly.

## Locked decisions

Two targets, no others:

- `jjrg_Heat::silks` → wire-name `jjghn_silks`
- `jjrg_PaceState` variants → `jjgte_rough`, `jjgte_bridled`, `jjgte_complete`, `jjgte_abandoned`

Migration mechanism: `#[serde(rename, alias)]` window. In one edit, each target receives both its new `rename` (minted wire-name) and a transitional `alias` for its current wire-name. Build, trigger a gallops rewrite via the pace's own `jjx_record`, then remove the transitional `alias` lines in a separate edit.

Keep `alias = "primed"` on `Bridled` — pre-existing legacy compat, outside this migration window.

Remove `#[serde(rename_all = "lowercase")]` from `jjrg_PaceState`; explicit per-variant renames replace it.

JJS0 spec-side declarations for these enum sites are already in place via the AXLA axd_string mechanism — this pace carries no spec touch.

## Out of scope

`jjrg_Tack::silks` stays bare (fan-out target). Other unmigrated property names and enum values stay bare. No CLAUDE.md or memo edits.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: `jjghn_silks` at heat level (no bare heat-level `"silks":`); `jjgte_*` for state values throughout.
- Transitional `alias` lines removed; only `alias = "primed"` remains.

## Discovery hints

Types: `Tools/jjk/vov_veiled/src/jjrt_types.rs`. Gallops: `.claude/jjm/jjg_gallops.json`.

**[260509-1046] rough**

## Character

Mechanical migration of two targets to validate the sprue-minting recipe end-to-end. Low judgment surface; sonnet-friendly.

## Locked decisions

Two targets, no others:

- `jjrg_Heat::silks` → wire-name `jjghn_silks`
- `jjrg_PaceState` variants → `jjgte_rough`, `jjgte_bridled`, `jjgte_complete`, `jjgte_abandoned`

Migration mechanism: `#[serde(rename, alias)]` window. In one edit, each target receives both its new `rename` (minted wire-name) and a transitional `alias` for its current wire-name. Build, trigger a gallops rewrite via the pace's own `jjx_record`, then remove the transitional `alias` lines in a separate edit.

Keep `alias = "primed"` on `Bridled` — pre-existing legacy compat, outside this migration window.

Remove `#[serde(rename_all = "lowercase")]` from `jjrg_PaceState`; explicit per-variant renames replace it.

JJS0 spec-side declarations for these enum sites are already in place via the AXLA axd_string mechanism — this pace carries no spec touch.

## Out of scope

`jjrg_Tack::silks` stays bare (fan-out target). Other unmigrated property names and enum values stay bare. No CLAUDE.md or memo edits.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: `jjghn_silks` at heat level (no bare heat-level `"silks":`); `jjgte_*` for state values throughout.
- Transitional `alias` lines removed; only `alias = "primed"` remains.

## Discovery hints

Types: `Tools/jjk/vov_veiled/src/jjrt_types.rs`. Gallops: `.claude/jjm/jjg_gallops.json`.

**[260508-1027] rough**

## Character

Mechanical migration of two targets to validate the sprue-minting recipe end-to-end. Low judgment surface; sonnet-friendly.

## Locked decisions

Two targets, no others:

- `jjrg_Heat::silks` → wire-name `jjghn_silks`
- `jjrg_PaceState` variants → `jjgte_rough`, `jjgte_bridled`, `jjgte_complete`, `jjgte_abandoned`

Migration mechanism: `#[serde(rename, alias)]` window. In one edit, each target receives both its new `rename` (minted wire-name) and a transitional `alias` for its current wire-name. Build, trigger a gallops rewrite via the pace's own `jjx_record`, then remove the transitional `alias` lines in a separate edit.

Keep `alias = "primed"` on `Bridled` — pre-existing legacy compat, outside this migration window.

Remove `#[serde(rename_all = "lowercase")]` from `jjrg_PaceState`; explicit per-variant renames replace it.

JJS0 gets a new "Sprue Vocabulary" section seeded with the five sprues plus one worked example of the Rust-field-to-sprue mapping pattern. Designed for cumulative extension by fan-out paces.

## Out of scope

`jjrg_Tack::silks` stays bare (fan-out target). Other unmigrated property names and enum values stay bare. No CLAUDE.md or memo edits.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: `jjghn_silks` at heat level (no bare heat-level `"silks":`); `jjgte_*` for state values throughout.
- Transitional `alias` lines removed; only `alias = "primed"` remains.
- JJS0 carries the vocabulary section and worked example.
- `jjx_show ₣BE` confirms the kit reads its own state.

## Discovery hints

Types: `Tools/jjk/vov_veiled/src/jjrt_types.rs`. Gallops: `.claude/jjm/jjg_gallops.json`. Spec: `Tools/jjk/vov_veiled/JJS0_JobJockeySpec.adoc`.

### mint-gallops-root-sprues (₢BEAAB) [complete]

**[260509-1120] complete**

## Character

Apply the paddock's migration recipe to the four `jjrg_Gallops` root properties. Mechanical; sonnet-friendly.

## Targets

`jjgrn_schema_version`, `jjgrn_next_heat_seed`, `jjgrn_heat_order`, `jjgrn_heats`.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: zero bare `schema_version`, `next_heat_seed`, `heat_order`, `heats` at the root path; `jjgrn_*` present.
- Transitional `alias` lines removed.

**[260509-1046] rough**

## Character

Apply the paddock's migration recipe to the four `jjrg_Gallops` root properties. Mechanical; sonnet-friendly.

## Targets

`jjgrn_schema_version`, `jjgrn_next_heat_seed`, `jjgrn_heat_order`, `jjgrn_heats`.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: zero bare `schema_version`, `next_heat_seed`, `heat_order`, `heats` at the root path; `jjgrn_*` present.
- Transitional `alias` lines removed.

**[260508-1045] rough**

## Character

Apply the paddock's migration recipe to the four `jjrg_Gallops` root properties. Mechanical; sonnet-friendly.

## Targets

`jjgrn_schema_version`, `jjgrn_next_heat_seed`, `jjgrn_heat_order`, `jjgrn_heats`.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: zero bare `schema_version`, `next_heat_seed`, `heat_order`, `heats` at the root path; `jjgrn_*` present.
- Transitional `alias` lines removed.
- JJS0 Sprue Vocabulary section gains four entries.

### mint-heat-rest-and-status (₢BEAAC) [complete]

**[260509-1142] complete**

## Character

Apply the paddock's migration recipe to the remaining `jjrg_Heat` properties and the `jjrg_HeatStatus` enum. Mechanical; sonnet-friendly.

## Targets

Five `jjrg_Heat` properties — `jjghn_creation_time`, `jjghn_status`, `jjghn_order`, `jjghn_next_pace_seed`, `jjghn_paces`. Three `jjrg_HeatStatus` variants — `jjghe_racing`, `jjghe_stabled`, `jjghe_retired`. Remove `#[serde(rename_all = "lowercase")]` from `jjrg_HeatStatus`.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: zero bare versions of those five property names on heat objects; zero bare HeatStatus values; `jjghn_*` and `jjghe_*` present.
- Transitional `alias` lines removed.

**[260509-1046] rough**

## Character

Apply the paddock's migration recipe to the remaining `jjrg_Heat` properties and the `jjrg_HeatStatus` enum. Mechanical; sonnet-friendly.

## Targets

Five `jjrg_Heat` properties — `jjghn_creation_time`, `jjghn_status`, `jjghn_order`, `jjghn_next_pace_seed`, `jjghn_paces`. Three `jjrg_HeatStatus` variants — `jjghe_racing`, `jjghe_stabled`, `jjghe_retired`. Remove `#[serde(rename_all = "lowercase")]` from `jjrg_HeatStatus`.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: zero bare versions of those five property names on heat objects; zero bare HeatStatus values; `jjghn_*` and `jjghe_*` present.
- Transitional `alias` lines removed.

**[260508-1045] rough**

## Character

Apply the paddock's migration recipe to the remaining `jjrg_Heat` properties and the `jjrg_HeatStatus` enum. Mechanical; sonnet-friendly.

## Targets

Five `jjrg_Heat` properties — `jjghn_creation_time`, `jjghn_status`, `jjghn_order`, `jjghn_next_pace_seed`, `jjghn_paces`. Three `jjrg_HeatStatus` variants — `jjghe_racing`, `jjghe_stabled`, `jjghe_retired`. Remove `#[serde(rename_all = "lowercase")]` from `jjrg_HeatStatus`.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: zero bare versions of those five property names on heat objects; zero bare HeatStatus values; `jjghn_*` and `jjghe_*` present.
- Transitional `alias` lines removed.
- JJS0 Sprue Vocabulary section gains eight entries.

### mint-pace-and-tack-sprues (₢BEAAD) [complete]

**[260510-0809] complete**

## Character

Apply the paddock's migration recipe to `jjrg_Pace` and `jjrg_Tack`. Mechanical, larger surface (7 sprues); sonnet-friendly.

## Targets

`jjgpn_tacks` on `jjrg_Pace`. Six `jjrg_Tack` properties — `jjgtn_ts`, `jjgtn_state`, `jjgtn_text`, `jjgtn_silks`, `jjgtn_basis`, `jjgtn_direction`. The `basis` field's existing `#[serde(rename = "commit")]` retires — minted name follows the Rust field name, dropping the historical `commit` wire-name.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: zero bare versions of `tacks`, `ts`, `state`, `text`, tack-level `silks`, `commit`, `direction`; `jjgpn_tacks` and `jjgtn_*` present.
- Transitional `alias` lines removed.

**[260509-1046] rough**

## Character

Apply the paddock's migration recipe to `jjrg_Pace` and `jjrg_Tack`. Mechanical, larger surface (7 sprues); sonnet-friendly.

## Targets

`jjgpn_tacks` on `jjrg_Pace`. Six `jjrg_Tack` properties — `jjgtn_ts`, `jjgtn_state`, `jjgtn_text`, `jjgtn_silks`, `jjgtn_basis`, `jjgtn_direction`. The `basis` field's existing `#[serde(rename = "commit")]` retires — minted name follows the Rust field name, dropping the historical `commit` wire-name.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: zero bare versions of `tacks`, `ts`, `state`, `text`, tack-level `silks`, `commit`, `direction`; `jjgpn_tacks` and `jjgtn_*` present.
- Transitional `alias` lines removed.

**[260508-1046] rough**

## Character

Apply the paddock's migration recipe to `jjrg_Pace` and `jjrg_Tack`. Mechanical, larger surface (7 sprues); sonnet-friendly.

## Targets

`jjgpn_tacks` on `jjrg_Pace`. Six `jjrg_Tack` properties — `jjgtn_ts`, `jjgtn_state`, `jjgtn_text`, `jjgtn_silks`, `jjgtn_basis`, `jjgtn_direction`. The `basis` field's existing `#[serde(rename = "commit")]` retires — minted name follows the Rust field name, dropping the historical `commit` wire-name.

## Done when

- `tt/vow-b.Build.sh` clean; `tt/vow-t.Test.sh` green.
- Gallops file: zero bare versions of `tacks`, `ts`, `state`, `text`, tack-level `silks`, `commit`, `direction`; `jjgpn_tacks` and `jjgtn_*` present.
- Transitional `alias` lines removed.
- JJS0 Sprue Vocabulary section gains seven entries.

### restore-roundtrip-check (₢BEAAE) [complete]

**[260510-0814] complete**

## Character

Mechanical revert + verification. Sonnet-friendly.

## Locked decisions

Restore the strict round-trip byte-compare check in `Tools/jjk/vov_veiled/src/jjri_io.rs` that was disabled during the sprue rename window. The bypass site carries the marker `SPRUE_MIGRATION_BYPASS` — find it via grep, restore the original `if !is_migration_mode { ... }` block, and remove the `#[allow(dead_code)]` from `zjjdr_find_first_diff`.

The verification step: build, then run any state-mutating jjx call (e.g., `jjx_paddock` setter with a no-op note). It must succeed — proving the gallops file is now canonical for the current source attributes and strict round-trip passes.

## Out of scope

The serde rename attributes themselves (those are the intended permanent state). Any further sprue-rename work (handled by the four batch paces).

## Done when

- `SPRUE_MIGRATION_BYPASS` marker absent from `jjri_io.rs`.
- `#[allow(dead_code)]` removed from `zjjdr_find_first_diff`.
- Build clean.
- A state-mutating jjx call succeeds without "Round-trip validation failed" — proves the canonical file passes strict validation under the restored check.

## Discovery hints

`grep -n SPRUE_MIGRATION_BYPASS Tools/jjk/vov_veiled/src/jjri_io.rs`. The original check is preserved verbatim inside the marker comment block.

**[260508-1438] rough**

## Character

Mechanical revert + verification. Sonnet-friendly.

## Locked decisions

Restore the strict round-trip byte-compare check in `Tools/jjk/vov_veiled/src/jjri_io.rs` that was disabled during the sprue rename window. The bypass site carries the marker `SPRUE_MIGRATION_BYPASS` — find it via grep, restore the original `if !is_migration_mode { ... }` block, and remove the `#[allow(dead_code)]` from `zjjdr_find_first_diff`.

The verification step: build, then run any state-mutating jjx call (e.g., `jjx_paddock` setter with a no-op note). It must succeed — proving the gallops file is now canonical for the current source attributes and strict round-trip passes.

## Out of scope

The serde rename attributes themselves (those are the intended permanent state). Any further sprue-rename work (handled by the four batch paces).

## Done when

- `SPRUE_MIGRATION_BYPASS` marker absent from `jjri_io.rs`.
- `#[allow(dead_code)]` removed from `zjjdr_find_first_diff`.
- Build clean.
- A state-mutating jjx call succeeds without "Round-trip validation failed" — proves the canonical file passes strict validation under the restored check.

## Discovery hints

`grep -n SPRUE_MIGRATION_BYPASS Tools/jjk/vov_veiled/src/jjri_io.rs`. The original check is preserved verbatim inside the marker comment block.

### restore-roundtrip-check (₢BEAAF) [abandoned]

**[260509-0724] abandoned**

## Character

Mechanical revert + verification. Sonnet-friendly.

## Locked decisions

Restore the strict round-trip byte-compare check in `Tools/jjk/vov_veiled/src/jjri_io.rs` that was disabled during the sprue rename window. The bypass site carries the marker `SPRUE_MIGRATION_BYPASS` — find it via grep, restore the original `if !is_migration_mode { ... }` block, and remove the `#[allow(dead_code)]` from `zjjdr_find_first_diff`.

The verification step: build, then run any state-mutating jjx call (e.g., `jjx_paddock` setter with a no-op note). It must succeed — proving the gallops file is now canonical for the current source attributes and strict round-trip passes.

## Out of scope

The serde rename attributes themselves (those are the intended permanent state). Any further sprue-rename work (handled by the four batch paces).

## Done when

- `SPRUE_MIGRATION_BYPASS` marker absent from `jjri_io.rs`.
- `#[allow(dead_code)]` removed from `zjjdr_find_first_diff`.
- Build clean.
- A state-mutating jjx call succeeds without "Round-trip validation failed" — proves the canonical file passes strict validation under the restored check.

## Discovery hints

`grep -n SPRUE_MIGRATION_BYPASS Tools/jjk/vov_veiled/src/jjri_io.rs`. The original check is preserved verbatim inside the marker comment block.

**[260508-1440] rough**

## Character

Mechanical revert + verification. Sonnet-friendly.

## Locked decisions

Restore the strict round-trip byte-compare check in `Tools/jjk/vov_veiled/src/jjri_io.rs` that was disabled during the sprue rename window. The bypass site carries the marker `SPRUE_MIGRATION_BYPASS` — find it via grep, restore the original `if !is_migration_mode { ... }` block, and remove the `#[allow(dead_code)]` from `zjjdr_find_first_diff`.

The verification step: build, then run any state-mutating jjx call (e.g., `jjx_paddock` setter with a no-op note). It must succeed — proving the gallops file is now canonical for the current source attributes and strict round-trip passes.

## Out of scope

The serde rename attributes themselves (those are the intended permanent state). Any further sprue-rename work (handled by the four batch paces).

## Done when

- `SPRUE_MIGRATION_BYPASS` marker absent from `jjri_io.rs`.
- `#[allow(dead_code)]` removed from `zjjdr_find_first_diff`.
- Build clean.
- A state-mutating jjx call succeeds without "Round-trip validation failed" — proves the canonical file passes strict validation under the restored check.

## Discovery hints

`grep -n SPRUE_MIGRATION_BYPASS Tools/jjk/vov_veiled/src/jjri_io.rs`. The original check is preserved verbatim inside the marker comment block.

### marker-commit-primitive (₢BEAAH) [complete]

**[260510-0832] complete**

## Character

Refactor `vvc` to support genuine marker commits — index-invariant, guard-free. Three jjk call sites today set `allow_empty + no_stage` but use `git commit --allow-empty`, which still bundles pre-staged content into the marker; the size guard fires against a tree the marker doesn't author. Surfaced from ₢BEAAD's flip-pattern, when staged round-trip content blocked `jjx_open`'s invitatory.

## Targets

- `Tools/vvc/src/vvcc_commit.rs` — add `marker_only: bool` (or new `vvcc_marker_commit` primitive). When set: skip staging, skip size guard, use `git commit-tree HEAD^{tree} -p HEAD -m "..."` + `git update-ref HEAD <sha>` for true empty commit invariant to index state.
- Three call sites migrate to the new path: `jjrm_mcp.rs:695` (invitatory), `jjrld_landing.rs:60` (landing), `jjrch_chalk.rs:78` (chalk).
- `JJSRPS-persist.adoc` — replace stale `jjdo_mark` reference with the actual marker family (invitatory + landing + chalk) and document the marker-only contract.

## Done when

- Build clean; unit tests green.
- New test: with pre-staged dirty content, a marker commit produces an empty diff against parent AND the pre-staged content remains staged afterward (index-invariance).
- All three call sites use the new path.
- JJSRPS lexicon current.

**[260510-0802] rough**

## Character

Refactor `vvc` to support genuine marker commits — index-invariant, guard-free. Three jjk call sites today set `allow_empty + no_stage` but use `git commit --allow-empty`, which still bundles pre-staged content into the marker; the size guard fires against a tree the marker doesn't author. Surfaced from ₢BEAAD's flip-pattern, when staged round-trip content blocked `jjx_open`'s invitatory.

## Targets

- `Tools/vvc/src/vvcc_commit.rs` — add `marker_only: bool` (or new `vvcc_marker_commit` primitive). When set: skip staging, skip size guard, use `git commit-tree HEAD^{tree} -p HEAD -m "..."` + `git update-ref HEAD <sha>` for true empty commit invariant to index state.
- Three call sites migrate to the new path: `jjrm_mcp.rs:695` (invitatory), `jjrld_landing.rs:60` (landing), `jjrch_chalk.rs:78` (chalk).
- `JJSRPS-persist.adoc` — replace stale `jjdo_mark` reference with the actual marker family (invitatory + landing + chalk) and document the marker-only contract.

## Done when

- Build clean; unit tests green.
- New test: with pre-staged dirty content, a marker commit produces an empty diff against parent AND the pre-staged content remains staged afterward (index-invariance).
- All three call sites use the new path.
- JJSRPS lexicon current.

## Commit Activity

```
File-touch bitmap (x = pace commit touched file):

  1 G axla-extension-and-foreign-sweep
  2 A mint-heat-silks-pace-states
  3 B mint-gallops-root-sprues
  4 C mint-heat-rest-and-status
  5 D mint-pace-and-tack-sprues
  6 E restore-roundtrip-check
  7 H marker-commit-primitive

GABCDEH
·xxxx·· jjrt_types.rs
·x···x· jjri_io.rs
xx····· JJS0_JobJockeySpec.adoc
······x JJSCLD-landing.adoc, JJSRPS-persist.adoc, jjk-claude-context.md, jjrch_chalk.rs, jjrld_landing.rs, jjrm_mcp.rs, lib.rs, vvcc_commit.rs
···x··· BUSJGW-GarrisonWsl.adoc
·x····· jjtg_gallops.rs
x······ BUS0-BashUtilitiesSpec.adoc, JJSCGZ-gazette.adoc, JJSTF-test-fundus.adoc, RBS0-SpecTop.adoc

Commit swim lanes (x = commit affiliated with pace):

(showing last 35 of 47 commits)

  1 A mint-heat-silks-pace-states
  2 G axla-extension-and-foreign-sweep
  3 B mint-gallops-root-sprues
  4 C mint-heat-rest-and-status
  5 D mint-pace-and-tack-sprues
  6 E restore-roundtrip-check
  7 H marker-commit-primitive

123456789abcdefghijklmnopqrstuvwxyz
x··x······x························  A  3c
········xx·························  G  2c
···········xx·x····················  B  3c
················x··x·xx············  C  4c
·······················x··x·x······  D  3c
·······························xx··  E  2c
·································xx  H  2c
```

## Steeplechase

### 2026-05-10 08:32 - ₢BEAAH - W

Added vvc marker-commit primitive: index-invariant, guard-free empty commits via plumbing (git commit-tree HEAD^{tree} -p HEAD + update-ref with old-sha guard). New types vvcc_MarkerArgs, vvcc_CommitLock::vvcc_marker, vvcc_marker_run; re-exported as vvc::marker. Migrated three jjk call sites — invitatory (jjrm_mcp.rs:694), landing (jjrld_landing.rs:55), chalk (jjrch_chalk.rs:78) — off the misleading vvc::commit + allow_empty path that still bundled pre-staged content into 'empty' commits and ran the size guard against a tree the marker doesn't author. Surfaced from BEAAD when staged round-trip content blocked jjx_open's invitatory at the size guard. Pruned dead size_limit plumbing from jjrm_LandingParams and jjrld_LandingArgs (the field was a no-op once the guard skipped). Three inline tests in vvcc_commit.rs prove the contract: vvtc_marker_index_invariance (tree(HEAD) == tree(HEAD~) AND git diff --cached byte-identical before and after), vvtc_marker_skips_size_guard (200KB pre-staged passes through), vvtc_marker_applies_prefix. JJSRPS-persist.adoc swapped stale {jjdo_mark} for the marker family ({jjdo_open}/invitatory + {jjdo_landing} + {jjdo_chalk}) and documented the marker-only contract. JJSCLD-landing.adoc dropped stale {jjda_size_limit} and 'git commit --allow-empty' references; behavior step is now 'Mint via vvc::marker primitive (index-invariant, guard-free)'. jjk-claude-context.md dropped size_limit? from the jjx_landing schema and the size-guard rule list went from 8 to 7 commands. Build clean (release, 20.42s). Unit tests green: 26 vvc + 337 jjk = 363 passed. The 13 jjtlg_fundus_scenario integration failures are the same out-of-scope environmental set documented across BEAAA-BEAAE (missing BURN profiles + localhost station regime not configured).

### 2026-05-10 08:32 - ₢BEAAH - n

Introduce vvc::marker primitive for index-invariant, guard-free event commits and route officium invitatory, landing, and chalk through it so pre-staged content survives untouched.

### 2026-05-10 08:14 - ₢BEAAE - W

Restored strict round-trip byte-compare check in jjri_io.rs that was disabled during the sprue rename window. Removed the SPRUE_MIGRATION_BYPASS comment block (replaced with the original `if !is_migration_mode { ... }` strict reserialize-and-byte-compare guard) and dropped the `#[allow(dead_code)]` from `zjjdr_find_first_diff`. Build clean (release, 21.25s). Verification: two consecutive jjx_alter calls flipping BE status (stabled commit 36bf29e4, racing commit 4c98934d) — each fires both load-side strict round-trip on the existing canonical gallops AND save-side strict round-trip on the temp file produced by serialization (jjdr_save validates by jjdr_load on the temp before atomic rename). Both directions of the flip succeeded without `Round-trip validation failed`, proving the four-batch sprue migration (BEAAA-BEAAD) landed canonically and the source-side serde rename attributes match the on-disk wire format byte-for-byte. Final post-edit check: zero SPRUE_MIGRATION_BYPASS markers anywhere under Tools/jjk/, zero #[allow(dead_code)] attributes in jjri_io.rs. Heat ₣BE migration window now closed; future loads enforce strict round-trip on every jjdr_load and jjdr_save call.

### 2026-05-10 08:14 - ₢BEAAE - n

Restore round-trip validation in jjdr_load and remove the SPRUE_MIGRATION_BYPASS dead-code allow on zjjdr_find_first_diff, now that the wire-name rename window has closed.

### 2026-05-10 08:14 - Heat - f

racing

### 2026-05-10 08:14 - Heat - f

stabled

### 2026-05-10 08:09 - ₢BEAAD - W

Minted seven jjg{p,t}n_* sprues for the Pace/Tack carrier (jjgpn_tacks; jjgtn_ts/state/text/silks/basis/direction). Two-pass migration recipe applied: edit-pass 1 added rename+alias; round-trip via flip-pattern (jjx_alter BE stabled→racing) rewrote .claude/jjm/jjg_gallops.json through new wire-names; edit-pass 2 dropped the seven transitional aliases. The basis field's historical commit wire-name retired entirely. Build clean, 337/337 unit tests green, 13 jjtlg_fundus_scenario environmental failures = expected baseline. Verified: zero bare versions of the seven names in gallops (jjgpn_tacks=379, jjgtn_ts/state/text/silks/basis=1086 each, jjgtn_direction=7 narrowed by skip_serializing_if). Heat ₣BE has 4 of 4 carrier-isolated mint paces complete; only out-of-scope alias remaining in jjrt_types.rs is the V3-legacy primed→jjgte_bridled compat at line 23 (₣An). Two spooks surfaced this session: (a) jjx_alter lacked size_limit, fixed mid-pace as heat-affiliated commit 99228774; (b) jjx_open invitatory commits whatever is staged rather than producing a pure history marker — captured for separate handling.

### 2026-05-10 08:02 - Heat - S

marker-commit-primitive

### 2026-05-10 08:02 - ₢BEAAD - n

Edit-pass 2: drop the 7 transitional alias lines from jjrg_Pace.tacks and the six jjrg_Tack fields (ts, state, text, silks, basis, direction). The basis field's historical commit wire-name fully retires here — the previous serde rename = "commit" was replaced by rename = "jjgtn_basis", alias = "commit" in edit-pass 1; this pass drops the alias, retiring the commit wire-name entirely. Round-trip of .claude/jjm/jjg_gallops.json fired earlier this session via the paddock's flip-pattern: jjx_alter BE stabled (initially blocked at default 50KB invitatory size guard, gallops bytes legitimate at 6.35MB; resolved by direct git commit 11012a04 with provenance and authorization) followed by jjx_alter BE racing with size_limit=7000000 (committed 756e43a4, succeeded with the spook-fix from notch 99228774 that added size_limit acceptance to jjrfu_FurloughArgs and jjrm_AlterParams). Build clean (release, 20.57s). Unit tests green: 337 passed, 0 failed across vvr/jjk/vof/vvc. The 13 jjtlg_fundus_scenario integration failures are the same out-of-scope environmental set documented in ₢BEAAA/B/C wraps (missing .buk/users/bhyslop/jjfu-{full,nogit,nokey,norepo}/burn.env BURN profiles + localhost station regime not configured). Verification: zero bare versions of tacks/ts/state/text/tack-silks/commit/direction in Tools/jjk/vov_veiled/src/jjrt_types.rs; jjgpn_tacks present on jjrg_Pace; six jjgtn_* attributes present on jjrg_Tack; only remaining alias in file is the documented-out-of-scope primed→jjgte_bridled V3→V4 compat at line 23 (₣An). Operator-mediated MCP restart pending; final gallops round-trip verification (zero bare keys; jjgpn_tacks + 6 jjgtn_* keys present in serialized form) deferred to post-restart session.

### 2026-05-10 07:48 - Heat - n

Spook fix: jjx_alter accepts size_limit override. Mid-execution surface during ₢BEAAD round-trip — paddock's flip-pattern recipe assumed jjx_alter accepted size_limit like its peers (record/close/archive/paddock/enroll/redocket/landing/transfer), but jjrfu_FurloughArgs hardcoded vvc::VVCG_SIZE_LIMIT at the persist call. Mirror notch's pattern: add Option<u64> field to jjrfu_FurloughArgs (with #[arg(long)]), thread args.size_limit.unwrap_or(vvc::VVCG_SIZE_LIMIT) to jjri_persist; mirror on MCP side via size_limit field on jjrm_AlterParams and threaded into jjrfu_FurloughArgs constructor in the JJRM_CMD_NAME_ALTER branch. Build clean (release, 21.26s). Separate spook still standing: jjx_open's invitatory commit lacks size_limit override — surfaced when staged gallops content from the failed alter call blocked all jjx commands at the gateway; resolved this session by direct git commit (11012a04). Both fixes are heat ₣BE infrastructure, outside ₢BEAAD's seven-sprue scope.

### 2026-05-10 07:48 - Heat - f

racing

### 2026-05-10 07:38 - ₢BEAAD - n

Edit-pass 1: add #[serde(rename = "jjgpn_tacks", alias = "tacks")] to jjrg_Pace.tacks; add #[serde(rename = "jjgtn_*", alias = "<old>")] to all six jjrg_Tack fields (ts, state, text, silks, basis, direction). The basis field's existing rename = "commit" is replaced with rename = "jjgtn_basis", alias = "commit" — historical commit wire-name retires after edit-pass 2 drops the alias. direction keeps skip_serializing_if. Build clean (release, 22.44s). Operator-mediated MCP restart required between this edit-pass and the gallops-mutating jjx_alter round-trip per paddock recipe — deferred to next session.

### 2026-05-09 11:42 - ₢BEAAC - W

Applied paddock migration recipe to remaining jjrg_Heat properties and jjrg_HeatStatus enum. Edit-pass 1 (a3ae1b34) added #[serde(rename = "jjghn_*", alias = "<old>")] to creation_time/status/order/next_pace_seed/paces and #[serde(rename = "jjghe_*", alias = "<old>")] per HeatStatus variant, removing the blanket #[serde(rename_all = "lowercase")]. Operator-mediated MCP restart between edit passes loaded the fresh binary. Round-trip fired via flip-pattern jjx_alter on BE: stabled (04e71c9b) then racing (b839399f) — guaranteed save() regardless of starting state, rewriting the gallops file through the new wire-names. Edit-pass 2 (828cb1c3) dropped the 8 transitional aliases. Paddock recipe tightened (b7f4c366) to name the flip-pattern as the robust idempotent round-trip mechanism — surfaces to ₢BEAAD's mounter without re-derivation. Done-when verified: build clean (release, 19.89s), unit tests green (337/0 across vvr/jjk/vof/vvc), gallops file carries 167 jjghn_* keys + 29 jjghe_* values with zero bare versions of the five property names and zero bare HeatStatus values, all 8 transitional aliases removed (alias = "primed" on jjrg_PaceState::Bridled is unrelated legacy V3→V4 compat, explicitly out of heat scope). The 13 jjtlg_fundus_scenario integration failures are the same out-of-scope environmental set documented in ₢BEAAA/B wraps (missing .buk/users/bhyslop/jjfu-{full,nogit,nokey,norepo}/burn.env BURN profiles + localhost station regime not configured).

### 2026-05-09 11:42 - ₢BEAAC - n

BUSJGW garrison-w: rewrite ceremony for three-namespace workload identity (Windows user + workload-owned WSL distribution in HKCU\Lxss + inner Linux user), forced by Microsoft's per-user WSL design where admin's distributions are invisible to workload sessions. Mechanism is SSH-as-workload conducted by the privileged orchestrator: place bare authorized_keys, open workload SSH session (real Windows logon with HKCU mounted), run wsl --import from admin-exported seed tarball + useradd + passwd --lock + workload privkey placement inside the session, then replace bare entry with locked-down command= form and validate round-trip. Adds HKLM ProfileList registration for the Windows account (Win32-OpenSSH #1383). Obliterate scope expands across all three namespaces; admin's rbtww-main remains pristine and serves as seed source.

### 2026-05-09 11:41 - Heat - d

paddock curried: robustify round-trip example to flip-pattern (stabled→racing) per spook surfaced in ₢BEAAC

### 2026-05-09 11:38 - ₢BEAAC - n

Edit-pass 2: drop the 8 transitional alias lines from jjrg_Heat (creation_time, status, order, next_pace_seed, paces) and jjrg_HeatStatus (Racing, Stabled, Retired). Round-trip of .claude/jjm/jjg_gallops.json fired earlier in this session via two jjx_alter calls on BE (stabled→racing, commits 04e71c9b and b839399f) — the heat-status mutation called save() through the freshly-restarted MCP binary carrying rename+alias, rewriting all heat-property keys from bare to jjghn_* form and all heat statuses from bare to jjghe_* form. Now jjrt_types carries only rename, matching the file shape; future loads require jjghn_*/jjghe_* keys (alias = "primed" on jjrg_PaceState::Bridled is unrelated legacy V3→V4 compat, explicitly out-of-scope per paddock). Build clean (release profile, 19.89s). Unit tests green: 337 passed, 0 failed across vvr/jjk/vof/vvc. The jjtlg_fundus_scenario integration suite reports 13 environmental failures (missing .buk/users/bhyslop/jjfu-{full,nogit,nokey,norepo}/burn.env BURN profiles + localhost station regime not configured) — same out-of-scope set documented in ₢BEAAA/B wraps. Verification: zero bare versions of the five property names, zero bare HeatStatus values; 167 jjghn_* keys + 29 jjghe_* values present in gallops file.

### 2026-05-09 11:36 - Heat - f

racing

### 2026-05-09 11:36 - Heat - f

stabled

### 2026-05-09 11:34 - ₢BEAAC - n

Edit-pass 1: add #[serde(rename = "jjghn_*", alias = "<old>")] to the five remaining jjrg_Heat fields (creation_time, status, order, next_pace_seed, paces); add per-variant #[serde(rename = "jjghe_*", alias = "<old>")] to jjrg_HeatStatus (Racing, Stabled, Retired) and remove the blanket #[serde(rename_all = "lowercase")]. Builds clean (release profile, 19.97s). Unit tests green: 337 passed, 0 failed. The 13 jjtlg_fundus_scenario integration failures are the same out-of-scope environmental set documented in ₢BEAAA/B wraps (missing .buk/users/bhyslop/jjfu-{full,nogit,nokey,norepo}/burn.env BURN profiles + localhost station regime not configured). Round-trip of .claude/jjm/jjg_gallops.json deferred to next session — operator-mediated MCP restart required between this edit-pass and the gallops-mutating jjx_alter call per the paddock recipe.

### 2026-05-09 11:23 - Heat - d

paddock curried: Tighten migration recipe: name jjx_alter as round-trip trigger; flag jjx_record/jjx_paddock-setter as save()-non-firing; require MCP restart between Edit-pass 1 build and round-trip

### 2026-05-09 11:20 - ₢BEAAB - W

Mint-gallops-root-sprues complete. jjrt_types.rs:80-87 carries #[serde(rename = "jjgrn_*")] on all four jjrg_Gallops root fields (schema_version, next_heat_seed, heat_order, heats); transitional alias lines removed. .claude/jjm/jjg_gallops.json shows jjgrn_schema_version/next_heat_seed/heat_order/heats at root — zero bare wire-names for these targets. Done-when criteria all satisfied: tt/vow-b.Build.sh clean (release profile, 20.19s); tt/vow-t.Test.sh — 337 unit tests passed across vvr/jjk/vof/vvc; the jjtlg_fundus_scenario integration suite reports 13 environmental failures (missing .buk/users/bhyslop/jjfu-{full,nogit,nokey,norepo}/burn.env BURN profiles + localhost station regime not configured) — same out-of-scope set accepted in ₢BEAAA's wrap (5180789a). Three pace-affiliated commits: f4ea4b32 (Edit-pass 1 — rename+alias added) and 193392f1 (Edit-pass 2 — alias removed) are pace-affiliated; ad6d99a2 (₣BE:f racing furlough no-op) is heat-affiliated and triggered the actual gallops round-trip. Spook surfaced and discussed: the paddock recipe says 'any state-mutating jjx call to round-trip the gallops file', but jjx_record and jjx_paddock-setter do not call save() on jjg_gallops.json — only commands that mutate gallops state (heats/paces/tacks like jjx_alter, jjx_relabel, jjx_create) actually round-trip. f4ea4b32's commit message claims a round-trip happened but it did not (file was untouched until ad6d99a2). Worth tightening the paddock recipe for ₢BEAAC/D to name a specific round-trip command (e.g., 'jjx_alter racing' on the heat) — surfaced to operator, not redocketed in this pace. Intermediate-step complexity from this migration is fully scheduled for cleanup: per-pace transitional aliases drop in each pace's Edit-pass 2; the SPRUE_MIGRATION_BYPASS scaffolding in jjri_io.rs (allow(dead_code) at L45, is_migration_mode flag at L101-103, commented-out original check at L105-122, migration-mode field-population conditional at L126-131) is fully removed by ₢BEAAE restore-roundtrip-check. The residual alias = 'primed' on jjrg_PaceState::Bridled is from a prior V3→V4 migration, not from this heat — explicitly out-of-heat-scope per paddock, untracked by any current pace or itch.

### 2026-05-09 11:15 - Heat - f

racing

### 2026-05-09 11:13 - ₢BEAAB - n

Edit-pass 2: drop the four transitional alias lines from jjrg_Gallops root fields (schema_version, next_heat_seed, heat_order, heats). The notch's save() round-trips .claude/jjm/jjg_gallops.json via the running MCP binary (still carrying rename+alias from prior session) — root keys rewrite from bare to jjgrn_* form, the round-trip step deferred from f4ea4b32 (whose save() ran via stale MCP binary that pre-dated the rename). Now jjrt_types carries only rename, matching the file shape; future loads require jjgrn_* keys at root. Build clean (release profile, 20.19s). Unit tests green: 337 passed, 0 failed across vvr/jjk/vof/vvc. The jjtlg_fundus_scenario integration suite reports 13 environmental failures (missing .buk/users/bhyslop/jjfu-{full,nogit,nokey,norepo}/burn.env BURN profiles + localhost station regime not configured) — same out-of-scope set documented in ₢BEAAA's wrap (5180789a).

### 2026-05-09 11:07 - ₢BEAAB - n

Edit-pass 1: add #[serde(rename = "jjgrn_*", alias = "<old>")] to all four jjrg_Gallops root fields (schema_version, next_heat_seed, heat_order, heats). Builds clean. The notch's save() round-trips .claude/jjm/jjg_gallops.json through the new wire-names — root keys rewrite from bare to jjgrn_* form while the alias keeps existing-wire reads working during transition. Heat-affiliated nesteds (jjghn_silks already minted by ₢BEAAA, others bare) untouched in this batch — carrier isolation per paddock.

### 2026-05-09 11:02 - ₢BEAAA - W

Mint-heat-silks-pace-states verified complete at mount-time. jjrt_types.rs:68 carries #[serde(rename = "jjghn_silks")] on jjrg_Heat::silks; jjrt_types.rs:21-28 carry explicit per-variant #[serde(rename = "jjgte_rough/bridled/complete/abandoned")] on jjrg_PaceState (Bridled retains alias = "primed" as legacy compat per docket); no #[serde(rename_all = "lowercase")] on PaceState (the surviving one at line 33 is on jjrg_HeatStatus, out of scope). .claude/jjm/jjg_gallops.json shows jjghn_silks at heat level and jjgte_* state values throughout — zero bare wire-names for these targets. Done-when criteria 1-3 satisfied by prior pace-affiliated commits a073f531 (Edit-pass 2 — drop transitional aliases) and 29e2bb47 (apply final-form sprue declarations to JJS0 per AXLA discipline); the heat was subsequently re-paddocked at 43ed4fdb and the foreign-sweep pace ₢BEAAG completed before this pace was wrapped. Build verified: tt/vow-b.Build.sh clean (release profile, 22.03s). Unit tests verified: tt/vow-t.Test.sh — 337 passed, 0 failed across vvr binary + jjk lib + vof + vvc. The jjtlg_fundus_scenario integration suite reports 13 environmental failures (missing .buk/users/bhyslop/jjfu-{nogit,nokey,norepo,full}/burn.env BURN profiles + localhost station regime not configured); operator confirms fundus is broken in this environment and these failures are accepted as out-of-scope for this pace.

### 2026-05-09 10:57 - ₢BEAAG - W

Foreign-sweep complete: 26 axt_enum_value voicings across RBS0/BUS0/JJSTF/JJSCGZ/JJS0 now carry axd_string + canonical 'Serialized as `<wire>` in <carrier>.' body line per the AXLA discipline minted at 333b3907. Wire tokens preserved verbatim — this is catch-up, not migration. Carriers: bash regime variable value (RBS0 regime enums, 13 sites), fact-file extension token (BUS0 buf_ext_alias, 1 site), fundus OS account name (JJSTF jjdxf_* profiles → jjfu_* usernames, 4 sites), gazette markdown (JJSCGZ jjezs_* slugs, 4 sites), pensum liveness report (JJS0 jjdxpe_* states, 4 sites). Two documented skips: jjdye_left/right (column align) — pure jjrp_Align::Left/Right Rust-internal enum, no string wire form, future axd_integer or similar dimension covers. Verified by grep: 26 voicings carry axd_string with matching Serialized lines; only 2 documented skips remain bare. Heat ₣BE remaining work: four mint paces (BEAAA-D) still pending — they update gallops-affiliated JJK enum sites (PaceState already done, HeatStatus + jjrg_Gallops + jjrg_Tack still to come) with axd_string + new-wire sprues.

### 2026-05-09 10:57 - ₢BEAAG - n

Apply axd_string + canonical 'Serialized as `<wire>` in <carrier>.' body line to 26 axt_enum_value voicings outside the gallops surface per the AXLA discipline added in 333b3907. Wire tokens unchanged (no rename — this is catch-up, not migration). Carriers named per surrounding format: 'bash regime variable value' for RBS0 regime enums (rbrv_vessel_mode_*, rbrn_runtime_*, rbrn_entry_mode_*, rbrn_uplink_dns_mode_*, rbrn_uplink_access_mode_* — 13 sites), 'fact-file extension token' for BUS0 buf_ext_alias (1 site), 'fundus OS account name' for JJSTF profile constants (jjdxf_full/nokey/norepo/nogit — 4 sites mapping to jjfu_* OS usernames), 'gazette markdown' for JJSCGZ slugs (jjezs_slate/reslate/paddock/pace — 4 sites; tokens already prefix-disciplined per paddock's out-of-scope note), 'pensum liveness report' for JJS0 jjdxpe_* states (running/orphaned/stopped/lost — 4 sites; wire form is the literal token in the JJRLG_REPORT:<state> sentinel line emitted by zjjrlg_format_probe_output). Two sites left bare as documented non-string-wire skips per docket: jjdye_left and jjdye_right (column align values, lines 1660-1668 of JJS0). These are pure jjrp_Align::Left/Right Rust-internal enum variants used only by jjrp_print formatting code; they never serialize to JSON, env files, MCP responses, or any other wire — future encoding dimensions (axd_integer for repr(u8) enums, etc.) cover the non-string cases. Verified by grep: all 26 formerly-bare voicings now carry axd_string with matching Serialized lines; only the 2 documented skips remain. asciidoctor not available locally; structural integrity verified via grep that anchors are intact and counts match expectations (RBS0=13, BUS0=1, JJSTF=4, JJSCGZ=4, JJS0=4 pensum, plus 2 documented skips = 30 total enumerated sites, 28 minus 2 skips = 26 edits).

### 2026-05-09 10:47 - Heat - d

paddock curried: Spec touch revised — AXLA mechanism replaces Sprue Vocabulary section; foreign-sweep pace named

### 2026-05-09 10:46 - Heat - S

axla-extension-and-foreign-sweep

### 2026-05-09 10:41 - Heat - n

Apply axd_string dimension to the seven gallops enum-value voicings in JJS0 per the AXLA discipline added in 333b3907. Each axt_enum_value site for HeatStatus (jjdhe_racing/stabled/retired) and PaceState (jjdpe_rough/complete/abandoned/bridled) gains axd_string on its //axl_voices line and a body line in the canonical 'Serialized as `<sprue>` in JSON.' form. Sprues follow the X/Y/Z scheme established at 29e2bb47: jjghe_* for Heat carrier (Y=h, Z=e), jjgte_* for Tack carrier (Y=t, Z=e — PaceState lives on Tack snapshots). PaceState sprues (jjgte_rough/complete/abandoned/bridled) match the wire already migrated by ₢BEAAA; HeatStatus sprues (jjghe_racing/stabled/retired) are prescriptive truth — the Rust serde renames and gallops-file rewrite land in batch 3 (mint-heat-rest-and-status). jjdpe_bridled special-cased: voicing extends to 'axt_enum_value axd_string axd_internal' (axd_string before axd_internal), and the legacy 'serde alias: primed' note is folded into the new Serialized line as a parenthetical, replacing the single-line form 'V3-legacy state: human-claimed for interactive work (serde alias: `primed`).' with the two-line form. Heat-affiliated to BE — editorial spec work spanning both already-migrated PaceState and not-yet-migrated HeatStatus, doesn't fit any existing pace's scope. Out-of-scope sites left bare per discovery: pensum report states (jjdxpe_* lines 744-765), column align values (jjdye_* lines 1657-1662) under axo_entity. asciidoctor not available locally; verified by grep that all 7 voicings carry axd_string and 7 'Serialized as `jjg(he|te)_*` in JSON.' body lines exist with no anchor breakage.

### 2026-05-09 07:49 - Heat - n

Mint axd_string dimension in AXLA carrying the 1-arity sprue lookahead obligation for axt_enum_value voicings. Three-part change to AXLA-Lexicon.adoc: (1) mapping section gets :axd_string: <<axd_string,String Value>> entry alongside other axd_* dimensions; (2) axt_enum_value definition extended with encoding-dimension contract — exactly one encoding dimension must qualify every axt_enum_value voicing whose axt_enumeration participates in a wire form, the encoding dimension specifies the mcm_sprue arity, axd_string named as the example for string-token-encoded values; (3) axd_string definition added in the JSON Dimensions section near axd_property/axd_positional but written carrier-agnostic (the dimension describes the wire form — a literal string token — not the carrier, so it covers JSON strings, bash regime variable values, YAML scalars, and env-var values uniformly). The 1-arity sprue lookahead is fulfilled by a backtick-delimited mcm_sprue in the following text — same mechanism axvo_tabtarget uses, same lookahead AXLA already documents for axr_member under axd_property. Forbidden when the wire form is non-string (numeric, bitmask, object-tagged, positional, untagged variant) — those encodings will mint companion dimensions later. Design choice: dimension over inherent-arity-on-the-motif. Considered putting 1-arity sprue inherent in axt_enum_value (mirroring axvo_tabtarget which has no encoding dimension), but enum encodings genuinely vary — untagged-variant enums have 0-arity sprue (variant identity is inferred from value shape), integer-repr enums use numeric literals not string tokens, discriminator-tagged variants encode multi-arity. Inherent 1-arity would over-promise. The dimension approach mirrors b67e1813 precedent (axd_property/axd_positional as paired format-specific dimensions on axr_member, with the motif declaring the contract and dimensions deciding whether to honor or suppress) but for axt_enum_value the analog is one-sided — there's no positional analog where the value is its own key — so axd_string lands as a singleton in a future encoding-dimension family (axd_integer, axd_object, axd_array would shadow axt_* motifs by category, not collide within category, per AXLA's category-prefix discipline). Naming: chose axd_string over axd_string_value or axd_token — fits the dominant single-word AXLA dimension convention (axd_property, axd_positional, axd_list, axd_required, ...), AXLA's category-prefix system already permits suffix-overloading between categories (axd_list vs axt_map, axd_property vs axj_field), and the visual similarity to axt_string is a real but-mild concern that AXLA's prefix discipline already handles. Heat-affiliated to ₣BE — preparatory AXLA infrastructure for the sprue migration. Replaces the planned 'Sprue Vocabulary section' approach in JJS0 (originally seeded by ₢BEAAA, extended cumulatively by ₢BEAAB-D) with an inline mechanism: each axt_enum_value voicing site carries its own axd_string + backtick-delimited sprue, no separate vocabulary section needed. Implementation plan for ₣BE downstream: new pace at head of heat (axla-extension-and-foreign-sweep) commits this AXLA edit and converts ~30 enum-value sites NOT touched by mint paces (RBS0:14 vessel-mode and other regime enums, BUS0:1, JJSTF:4, JJSCGZ:4 gazette slugs, JJS0:~7 column-align and other) to direct form with axd_string + current-wire sprue; the four mint paces ₢BEAAA-D update the JJK enum sites they touch (PaceState, HeatStatus) with direct form + axd_string + new-wire sprue (jjgte_*, jjghe_*); paddock revised to remove the Sprue Vocabulary section paragraph and name the AXLA mechanism instead. Awaiting user confirmation before slating the new pace and redocketing the four mint paces.

### 2026-05-09 07:27 - ₢BEAAA - n

Apply final-form sprue declarations to JJS0 per the AXLA discipline added in b67e1813. All 19 axr_member sites under axr_record_json carriers (Gallops, Heat, Pace, Tack) gain the JSON-form qualifier: 17 carry axd_property with explicit sprue prose (`Serialized as \`<sprue>\` in JSON.`); 2 carry axd_positional and document encoding-as-parent-map-key (jjdhm_firemark, jjdpm_coronet). Sprues use the X/Y/Z scheme: jjgrn_* for Gallops root, jjghn_* for Heat, jjgpn_* for Pace, jjgtn_* for Tack — suffix is the lowercase Rust field name. jjdcm_basis adopts jjgtn_basis as final form (replacing the legacy commit serde rename, to be enacted in batch 4). Two pseudo-members (jjdhm_pensum_seed, jjdhm_paddock) had their misclassifying axr_member voicing dropped — they live outside the gallops JSON (officium-local and computed-from-firemark respectively) and never were JSON record members. The Column Table family (jjdym_*) under axo_entity is correctly untouched. Enum-value sprues (PaceState, HeatStatus) deferred to a future pace pending AXLA discussion of enum-value sprue arity. Only jjghn_silks currently matches the wire (this batch); other sprues are prescriptive truth that batches 2-4 will materialize in the gallops file via Rust serde renames.

### 2026-05-09 07:24 - Heat - T

restore-roundtrip-check

### 2026-05-09 07:24 - Heat - n

Add JSON-form dimension vocabulary to AXLA: axd_property and axd_positional, paired format-specific dimensions on axr_member that distinguish how a JSON-encoded member appears in the wire. axd_property keeps axr_member's 1-arity sprue contract (the member sits at a named slot in the parent JSON object). axd_positional suppresses the sprue lookahead (the member's value provides the parent map's key directly; no separate sprue exists). Parallels axd_xml_form precedent. Type-scheme rule: exactly one of the pair must qualify every axr_member under an axr_record_json carrier whose value participates in the JSON wire form; computed/ephemeral pseudo-members shouldn't carry axr_member at all. axr_member's body cross-references the new dimensions and notes the arity-suppression behavior. Heat-affiliated preparatory work for ₣BE jjk-v4-2-sprue.

### 2026-05-08 14:43 - ₢BEAAA - n

Edit-pass 2: drop the transitional alias attributes that admitted old-format wire-names during the sprue rename window. jjrg_Heat::silks now carries only #[serde(rename = "jjghn_silks")]; jjrg_PaceState variants carry only their #[serde(rename = "jjgte_*")] attributes (Bridled retains the legacy alias = "primed" — outside this migration). The kit reads and writes only the new sprues going forward. jjtg_pace_state_serialization updated to assert against the new wire-name ("jjgte_rough").

### 2026-05-08 14:40 - ₢BEAAA - n

Disable strict round-trip byte-compare validation in jjdr_load for the duration of the sprue-rename window. The check (jjri_io.rs:104-114) is replaced by a SPRUE_MIGRATION_BYPASS comment block preserving the original code verbatim; zjjdr_find_first_diff carries #[allow(dead_code)] to suppress the unused-helper lint. Without the bypass, any load of the pre-migration gallops file fails byte-compare (old wire-names re-serialize as new sprues). Closing pace ₢BEAAF (restore-roundtrip-check) reverts both the bypass and the dead_code attribute, then verifies strict round-trip passes against the now-canonical gallops.

### 2026-05-08 14:40 - Heat - S

restore-roundtrip-check

### 2026-05-08 14:24 - ₢BEAAA - n

Apply edit-pass 1 of the sprue-rename window to jjrt_types.rs: add #[serde(rename = "jjghn_silks", alias = "silks")] on jjrg_Heat::silks, and replace the blanket #[serde(rename_all = "lowercase")] on jjrg_PaceState with explicit per-variant rename+alias pairs (jjgte_rough/bridled/complete/abandoned). The legacy alias = "primed" on Bridled is folded into the new attribute. Wire-name reads accept both old and new strings; writes always emit the new sprues. jjrg_Tack::silks and jjrg_HeatStatus stay bare (out of scope for this batch).

### 2026-05-08 14:11 - Heat - f

racing

### 2026-05-08 10:46 - Heat - S

mint-pace-and-tack-sprues

### 2026-05-08 10:45 - Heat - S

mint-heat-rest-and-status

### 2026-05-08 10:45 - Heat - S

mint-gallops-root-sprues

### 2026-05-08 10:36 - Heat - n

Relax the quoin sub-letter ceiling from 2 to 3 to admit the three-axis sprue prefix layout (X = domain, Y = carrier table, Z = role) used by this heat's gallops minting work; drop the 'proven opaque in RBS0/JJS0' parenthetical since the empirical claim was specifically against 3-letter prefixes and no longer holds when 3 is admitted.

### 2026-05-08 10:34 - Heat - d

paddock curried: initial paddock — sprue scheme, recipe, batch layout

### 2026-05-08 10:27 - Heat - S

mint-heat-silks-pace-states

### 2026-05-08 09:59 - Heat - n

Drop the `jjg_` (aGent) prefix row from JJK README's prefix table — the placeholder slot for future agent definitions is no longer slated; remaining rows (jja_ Action, jjb_ Brand, jjc_ Chase, jjh_ Heat, jji_ Itch, jjk_ sKill, ...) carry the live JJK prefix universe.

### 2026-05-08 09:24 - Heat - N

jjk-v4-2-sprue

