## Charter — ensoul the hierophant

Build the hierophant: the veiled Rust application that conducts the delivery ceremony,
as the theurge conducts the tests.
It owns four commands, one per ceremony act:
essai — the reversible repair lap (gates, cut, prove, rig), run as many times as it takes;
docimasy — the reveal's reversible proving act (quarantine gates, freshness, preview, credential preflight, the gauntlet), granting the cachet;
ostend — the reveal's guided irreversible showing, where the machine performs every assert and the operator's hands perform every push;
harbinger — the stranger rig against promoted public main, summonable on its own.
The command seam is the ceremony's one load-bearing boundary: reversible versus irreversible.
The app sequences proven workers and absorbs orchestration, never authority —
with the one exception ruled below: the cut, which it owns outright in Rust.

## Cinched (design discussion, 260717; spec-first ruling, 260718; cut-absorption re-ruling, 260718; reveal-split ruling, 260718)

- The proscription stays homed in its shipped bash library —
  rblm_lustrate.sh ships because candidate-side damnatio must source it —
  and the hierophant reads it through the same subprocess reach damnatio already uses (rbtdrq_damnatio.rs is the worked pattern), never carrying a copy.
- Cut absorption (operator re-ruling, 260718, reversing the 260717 expede-as-worker cinch):
  the hierophant absorbs the cut — expede and perambulation re-authored in Rust inside the crate, called by essai in-process;
  the r02 release-machinery reassessment no longer carries that absorption (its residue: the loupe/damnatio lift-out question and the leak-status disposition record).
  One matcher only — Rust replaces bash with no interim where both are live —
  so the cut instruments (rblm_expede.sh, rblm_perambulation.sh, the theurge perambulation fixture, the rbw-ME enrollment and tabtarget, /rbk-expede)
  retire with the absorbing pace: a deliberate carve-out from the retire-after-service gate below,
  accepted knowing it surrenders the bash-cut fallback before ostend stands — a cut defect during laps is repaired in the hierophant, no bash path back.
  The candidate's own rblm_sterilize.sh remains a subprocess of the Rust cut — it must be the candidate's copy.
- Reveal split (operator ruling, 260718): the reveal ceremony is conducted in two acts
  split exactly at the reversible/irreversible line —
  the docimasy proves (quarantine gates, freshness, the preview conduction, credential preflight, the gauntlet)
  and grants the cachet;
  ostend shows (re-asserted ground, file-list eyes, disclosure, promotion, harbinger hand-off).
  The cachet — a tree-hash-keyed verdict fact beside the candidate — is the machine-enforced once-per-cycle bridge,
  surviving interruption and the days between acts;
  a rehearsal never grants one.
  The 260717 gauntlet placement holds unchanged: after the candidate stands on the quarantine, never inside a lap.
  Spec-homed in RBSHD/RBSHO as amended at the ruling;
  the words docimasy and cachet were grep-gated then.
- The crate lives under Tools/rbk/vov_veiled/ — withheld by directory construction — and is referenced by no shipped manifest:
  the delivered tree must build with the veiled directory absent.
  It carries its own withheld build tabtarget; crate directory, module prefix, and tabtarget names are minted at mount under the minting rules.
- Public acts remain human-hands-only by structural incapacity: the hierophant never holds a push.
- Marshal workers that read the proscription (zero, lustrate, feign) are sequenced, never reimplemented.
- The harbinger module's git surgery (clone, sever, hook, walk branch, prompt) is truly absorbed — it reads no shared authority —
  one implementation serving both essai's rig stage and the harbinger command.
- Old instruments outside the cut (the bash harbinger and rbw-MH, the standalone marshal tabtargets still hand-run)
  stand until the hierophant earns their retirement through real service in the release heat's laps;
  their retirement is this heat's closing pace, never a side effect — only the cut instruments retire early, per the re-ruling above.
- The ceremony and the hierophant's vocabulary are spec-homed (spec-first ruling, 260718, authored by this heat's opening pace):
  the RBS0 Marshal Operations branch defines the rbth_ terms and includes the sheaf family —
  RBSHC the cosmology, RBSHE/RBSHD/RBSHO/RBSHH the four operation sheaves — the single authority;
  this heat implements the spec and does not restate it.
  The spec precedes the code under the cut-absorption re-ruling too:
  the absorbing pace opens by amending RBSHC/RBSHE to the absorbed shape and re-homing the single-matcher rule there.
  The release heat's (₣B0) paddock holds its rulings as provenance and points here too.

## Done when

The hierophant stands proven in real release service:
essai has driven the release heat's repair laps, the docimasy its proving, ostend its showing, harbinger its confirmation coldwalk;
and the superseded instruments are retired.