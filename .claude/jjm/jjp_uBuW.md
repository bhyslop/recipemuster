## Character

Aspirant-shaping heat, not a build heat — concept-tier judgment (the ₣Bg/VOSMM pattern).
The deliverable is a bounded gestalt a future build heat can adopt without drift, not running code.

## Context

Concept home: `Tools/rbk/vov_veiled/RBSPC-podman_crucible.adoc` (the aspirant sheaf), mounted under RBS0 `== Aspirants` beside the Crucible Variant sheaf.
The sheaf is the authority for this heat — shape and reason-to-believe live there; this paddock points, it does not restate.

The subject is incorporating podman as a first-class crucible runtime with host-side podman-VM provisioning.
Reason-to-believe is a retained, once-working prototype quarantined in `Tools/rbk/vov_veiled/FUTURE/` (host-side fetch/init/lifecycle + a three-var station regime).

This heat is **stabled by design** while the shape crystallizes; design and word-minting are deferred to the sheaf's graduation and the future build heat.

## Cinched

The capture half of the prototype is superseded by the landed cloud-side `immure`/Lode model (RBSLI); the host-side consume half (fetch → init → lifecycle + provenance brand) survives and is the build's spine, re-targeted to consume the immure'd Lode.
The Podman-5.3+ `setns()` breakage is retired history — the architecture moved to Docker Compose `network_mode: "service:pentacle"` — not a live viability gate; the live runtime question is the ordinary one of podman-compose parity (`network_mode: service:` + `--wait`).
The three retained station vars are genuine host-local consume-side config, not superseded; their *home* (stashed RBRS station regime vs a BURN node profile vs a new host regime) is the load-bearing open question the sheaf must fence.

The host station-config reinstatement is build work — a candidate the sheaf evaluates and the future build heat executes against the quarantine-commit blueprint — not this heat's deliverable.

## Done when

This is the aspirant-shaping heat, not the build heat (the ₣Bg terminus).
It closes when the podman aspirant — homed in RBSPC — has gelled into a coherent, bounded gestalt: the station-config home consciously fenced (in/out of the future heat's scope), the kept/cut prototype boundary confirmed, and the podman-compose-parity question placed — with open design questions allowed to remain open, answered inside the future build heat rather than as a precondition to lighting it.
The terminal pace draws the fence and nominates the future build heat; nothing follows it.