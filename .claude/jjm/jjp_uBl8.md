## Character

Mulling heat. Design banks in the aspirant sheaf; this paddock banks work-shape
and the cinches a mount agent must not re-litigate.

## Shape

Paired with the aspirant sheaf VOSYD-diptych.adoc (voiced `axd_aspirant
axd_entity`, incorporated in VOS0 under its `== Diptych` branch).
The diptych is one word-per-line file per codex — the canonical spec source git
tracks at word grain — with AsciiDoc/Markdown rendered just in time and agent
edits returning through a verifying reverse parse.

Two phases, in order.
First, mulling: settle VOSYD under MCM/AXLA law toward infusion into VOS0 —
its settling register (in the sheaf) is the worklist, sigil-grammar first.
Sheaf-authoring paces may race; implementation waits for settling, never
infusion (JJS0 mulling doctrine).
Second, the codec MVP in the vom crate — encode / emit / prove — proven on one
pilot codex before any corpus-wide migration.

## Cinched

Decisions settled this session; do not reopen without operator direction.
Rationale lives in the sheaf, not here.

- Home is VOSYD under VOS0, VOSMM mold. Not a standalone codex; hives off only
  if it ascends.
- Word-per-line storage; single file per codex holding every sheaf.
- AsciiDoc-first projection (Markdown deferred). The fidelity gate is
  asciidoctor render-equivalence of original vs projection; the pure membrane
  is the version-pinned Ruby `asciidoctor` gem.
- normalize = emit∘encode — no separate normalizer.
- Reverse parse is required; its risk is managed by the echo-diff-for-review
  write loop, and both encode and reverse parse reject loud on unclaimed
  constructs.
- New AXLA annotation form only; legacy voicing translated on ingestion, never
  stored. AXLA is unchanged — a grammar need is an AXLA amendment, not a local
  dialect.
- No stored mapping section; the matricula owns reference↔definition integrity.
- Codec rides the vom binary (charter widens: census → census-and-codec) and
  inherits its never-ships boundary.
- Category-legend representation deferred to the matricula.

## Done when

VOSYD hardens past aspirant and infuses into VOS0, and the encode/emit/prove
MVP lands green on one pilot codex — fidelity proven, corpus migration still
ahead.