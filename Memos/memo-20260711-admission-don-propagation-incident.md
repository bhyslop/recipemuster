# Incident report: first don against a just-written admission grant 403'd — gauntlet attempt 3 (2026-07-09)

Status: absorbed with membrane (`rbtdrk_invoke_admission_settled`, rivets RBr_7a9/RBr_3f4);
standing under the Axis-B premise, not demolition-dated.
Collection context: `memo-20260711-gauntlet-indeterminacy-incident-collection.md`, roster row 3.

## Signature (grep anchors)

- `brevet director exit 109` — the fixture verdict line (`rbtdrk_brevet_don_director`)
- Exit 109 = `BUBC_band_admission`, minted only at the don preamble (`rba_don_capture`)
- `Leg 3 (don) denied (HTTP 403)` … `Permission 'iam.serviceAccounts.getAccessToken' denied on resource (or it may not exist)` — against `rbma-governor@cancbhm-d-canest3bhm100009.iam.gserviceaccount.com`
- Pre-repair forensic line claimed `admission deficit, not a propagation race` — this incident falsified that clause (since softened)
- Sitting HEALTHY throughout: `Sitting already live — reusing the cached federated token (runway 14754s)`

## What happened

Gauntlet attempt 3 (suite log `hist-rbw-ts-gauntlet-20260709-231056-1960153-179.txt`) ran
marshal-zero attestation, depot-lifecycle (4 green, 422s), then freehold-establish on a fresh
levy (`cancbhm-d-canest3bhm100009`): freehold_ensure, avow, gird green — then
`rbtdrk_brevet_don_director` died at 23:25:29. Inside `rbw-pB`, the governor-mantle don
(`generateAccessToken`) drew the 403 seconds after gird had written that very tokenCreator
binding (fixture invocation `hist-rbw-pB-sh-20260709-232529-1971756-399.txt`; suite halted 3/1).
The docket-prescribed disambiguation — one direct `rbw-pB … director` rerun — executed at
23:27:26 (`hist-rbw-pB-sh-20260709-232726-1972197-362.txt`, ~2 min after the kill): donned,
muniment engrossed, breveted, clean. Transient propagation, not an admission regression.
Second observation of the signature; the first (freehold ladder, brevet exit 109, healthy
sitting, single retry clean) was carried in the pace docket as manual-retry guidance only.

## Party analysis

- **Vendor (Google IAM):** a just-written admission binding is unenforceable at the
  resource-scope cache for ~minutes, and no terminal-state signal exists to poll — the
  RBSCIP Class-C physics at a third operation type (`generateAccessToken` by a
  just-admitted principal). Prior class records cross-linked, not restated:
  `memo-20260513-iam-propagation-race-director-invest-gar.md`,
  `memo-20260514-pristine-gauntlet-iam-flap.md`,
  `memo-20260604-credential-churn-leak-and-propagation-races.md`, and retired
  `memo-20260611-heat-BH-class-c-setiampolicy-write-flap.md` (read-back-as-gate falsified).
- **Foreign tool:** none — pure REST via curl; the transport behaved.
- **Our conduct gaps:** the don's forensic line overclaimed "not a propagation race";
  and the fixture sat immediately downstream of the grant it had just issued with no
  post-grant budget — contradicting RBSCIP's own call-context law, which the repair
  restored rather than invented.

## Repair

Commit `f8efe5c29`. Membrane site: the theurge fixture (the one party that CAN prove
enforcement — gird/brevet run payor/governor-credentialed and structurally cannot perform
the citizen's don, so the consumer downstream of the grant owns the wait).
`rbtdrk_invoke_admission_settled` re-invokes while exit == `RBTDGC_BAND_ADMISSION` under the
shared `RBGC_PROPAGATION_*` budget (3s initial, 2× backoff, 20s cap, 420s deadline),
logging every bend; safe around the mutating brevet because band 109 mints pre-mutation at
the don preamble; any other exit fails fast; an exhausted budget surfaces the final 109 so a
real denial still kills the case. Budget projected to Rust via `rbgc_emit_consts` (zipper
fourth section). Production don unchanged except the softened forensic line. Rivets:
RBr_7a9 (spoor), RBr_3f4 (membrane), both defined at RBSCIP with an evidence row.

## Doctrine linkage

- **Axis-B premise** (`memo-20260521`, § Operation-completion contracts): the missing
  completion contract makes this budget permanent design, not a workaround awaiting a fix.
- **RBSCIP:** Class C extended to the don; the call-context law (post-grant sites wait the
  budget, cold-call sites fail fast) and the admission-window siting rule are the spec home
  for every durable fact here — this memo is provenance only. The premise's own spec
  elevation to an `rbsk_` Key Premise quoin is paced (₢BsAAU, axis-b-premise-mint).
- **RoE Palisade conduct, point by point:** characterized (exact signature, two surveyed
  observations); contained (one membrane, fixture-side; production untouched); absorbed
  only the signature (exit 109 alone; everything else dies clean); logged (a bend line per
  retry — attempt 8's 16 absorbed bends are recorded in the collection memo's roster);
  retirement stated (below).

## Retirement

Standing under the Axis-B premise — stated at RBr_3f4: retire only if the vendor ships a
terminal-state contract for IAM-grant propagation (the premise expects never) or hot-path
admission grants themselves retire. Deliberately not demolition-dated; the premise, not a
healing neighbor, is what it absorbs.

<!-- eof -->
