# FABLE-REVIEW-QUEUE

*Standing terminal-review agenda for model Fable — born from heat ₣BZ's review (pace ₢BZAAN) and feeding the ₣Bf terminal Fable-review pace. Executed under OPERATOR SANCTION while Fable was unavailable.*

## What this memo is

A **consumed-and-retired agenda**, not authority. Every mint made in Fable's absence is **PROVISIONAL and EVICTION-SWEEPABLE** — if Fable disagrees it retires by a tree-wide grep sweep (the *dingleberry doctrine*: using the current canonical word meanwhile is never a dingleberry; the sweep targets only non-canonical paraphrases). Each item **POINTS at where the decision really lives**; durable facts must land in their spec HOMES when ratified. **Retires when Fable has worked through it.** Bucket 1 = ratify-or-sweep; Bucket 2 = deferred, never decided.

**Stale-by-mount caution:** every line number, site count, and token snapshot below is provenance — verify against the live tree. The canon-scrub's authoritative leak-site list is the ledger (RBS0 lines 1890/1971/1980/1985/1331/2030/3486/3497 + RBSRF + acronyms); the speculative phrase-greps in Bucket-1 rows are **illustrative — verify they cover those sites before trusting a clean result** (a grep that misses the real sites reads false-clean under the dingleberry doctrine).

---

## BUCKET 1 — PROVISIONAL DECISIONS TO RATIFY-OR-SWEEP

### Federation cosmology (RATIFIED 260622)

- **composite-manor-model** — Manor is a COMPOSITE HOLDING (has-a payor/manor GCP project + clustered resources + commands-an-org), not identical to its project. HOME: RBS0 canon-scrub pace + frozen ₣BZ paddock `jjp_uBuZ.md`.
- **deed-sense-overturned** — the "deed / pure-org / not a building" sense is OVERTURNED as confabulated (paddock-only, zero spec/code). HOME: RBS0 canon-scrub + `jjp_uBuZ.md`.
- **manor-commands-org-asymmetry** — Manor holds org-level authority (control plane); Depot is org-blind; resolves the RBS0:1774 contradiction. HOME: RBS0 canon-scrub.
- **organization-is-hearting** — Organization is NOT a 6th bondstone; it rides under the Manor (commanded). Bondstones stay Manor/Depot/Citizen/Mantle/Foedus. HOME: RBS0 / MCM exposure-ladder.
- **foedus-hosted-not-member** — FOEDUS = the re-mintable federation INSTANCE hosted at the Manor's org with its own affiance/jilt lifecycle, NOT a composite member. HOME: RBS0 canon-scrub + RBSRF.
- **mechanism-not-vendor-axis** — Entra vs Keycloak is a MECHANISM axis (interactive device-flow vs programmatic self-JWT), the schema discriminator; vendor identity is not a regime field. HOME: RBSRF. (Distinct from the unmade `mechanism-discriminator-quoin`, B2-A — the axis is settled, the quoin+value-words are not.)
- **cardinality-depot-per-manor** — each Depot belongs to exactly one Manor; a Manor owns zero-or-more Depots. HOME: RBS0 canon-scrub.
- **rbs0-canon-scrub-framing** — the org-level "manor" leak into canonical RBS0 requires its own scrub pace disambiguating Manor (project/holding) vs organization. HOME: RBS0 dedicated scrub pace (ledger site list authoritative).

### Frozen-paddock + record corrections (RATIFIED)

- **frozen-bz-paddock-corrections** — strike "(org anchor, a deed not a building)"; downgrade banner "under review"→"resolved"; strike "at levy"→"interim scaffold" (terrier is scaffold-created). HOME: ₣BZ paddock `jjp_uBuZ.md`.

### Freehold / test-cosmology (block-RATIFIED)

- **freehold-test-infra-vocabulary** — freehold/leasehold = TEST-INFRASTRUCTURE vocabulary, NOT a federation bondstone; gets a spec home. HOME: RBST0 theurge subdoc + RBPC.
- **foedus-lifecycle-vs-freehold-split** — foedus-LIFECYCLE is the only BUILT fixture (throwaway create→destroy, no-suite, fail-loud); foedus-FREEHOLD is an un-tested durable-reuse posture. HOME: RBST0 + ₣Bf residual.
- **roster-lives-in-terrier** — standing-citizen roster lives in the TERRIER (manor-homed, read via rehearse), NOT the IdP; one row this heat. HOME: RBSTR/RBS0 terrier quoin + RBPC.
- **muniment-is-foedus-depot-join** — a muniment (subject, mantle) is the foedus↔depot join row (manor-wide roll = foedus view, per-polity slice = depot view); built at rbgft. HOME: RBSTR + rbgft data layer.
- **quota-conservative-assumption** — soft-deleted pools ASSUMED to count against the 100/org cap; commit to conservative never-churn. HOME: `memo-20260617-BZ-workforce-pool-constraints.md` + never-jilt posture.
- **affiance-refuse-and-rotate** — affiance hard-errors on a soft-deleted pool + rotates the pool id, NEVER undeletes; STRIKE the stale undelete prerequisite in `Bf-paddock`. HOME: RBSMA + ₣Bf paddock strike.

### Terrier founding-home (CINCHED 260622)

- **terrier-fork-b-resolution** — founding-home → OPTION B (Manor-establishment provisions the terrier); decisive: muniment content = {subject, mantle} only, foedus-independent. HOME: RBSTR/RBS0 terrier quoin + RBSMF.
- **terrier-b-shape-finisher** — terrier-build + all scriptable manor-setup → ONE idempotent post-payor-guide finisher (dedicated op); retires interim scaffold rbw-dt / proof rbw-dT. HOME: RBSMF / new manor-setup-finisher spec + ₣Bf pace.
- **terrier-grain-write-folder-read-bucket** — WRITE folder-scoped own-polity (objectAdmin, `<depot>/<mantle>/<subject>`); READ bucket-wide manor-wide (objectViewer, rehearse wields governor). HOME: RBSTR + RBSPO + RBS0:2030.

### Divergences + suite-gate ("yes to all" RATIFIED 260622)

- **divergence-checklist-yes-to-all** — 17 Opus/Fable divergences (4 subsumed, 13 open) ratify-as-recorded, ZERO overturns. HOME: `memo-20260615-BZ-m4-review-findings.md` (verify 13-item list live).
- **M4-five-judgment-calls** — the 5 high-confidence M4 calls (levy-split, admission-keep-one, compear/don cut, rename-depth-full, +2 M5) ratified. HOME: `memo-20260615-BZ-m4-review-findings.md` (scrutinize rename blast radius + levy split).
- **compear-cult-cut-divergence** — the enrobe/defrock/roster cult-rename-then-demolish arc landed correctly + was removed at M7. HOME: `memo-20260615-BZ-pace-design.md`.
- **suite-gate-resolved-early** — org disableServiceAccountKeyCreation fired the revisit trigger, forcing the keyless rewrite ahead of M7; freehold-establish = gauntlet-only keyless bootstrap. HOME: suite defs (gauntlet) + ₣Bf credential-heal idea.
- **M4-M7-suite-gate-residual** — the one medium-confidence M4 call resolved early by the keyless-policy rewrite. HOME: `memo-20260615-BZ-m4-review-findings.md` (sanity-check spike-trust-only proof through M5/M6).
- **mantle-homonym-guard-vestige** — FACT settled (self-delete condition MET, doubly moot after invest/divest→brevet/unseat); but the vestige lives entirely in FROZEN ₣BZ paddock PROSE — **there is no code site** (`grep homonym Tools/rbk/` is empty). Its TEXT DISPOSITION (re-aim/rewrite/retire) was deliberately routed to Fable as a vocabulary judgment per `memo-20260615-BZ-pace-design.md` — see the open `homonym-guard-text-disposition` item in B2-B. HOME (this row, fact only): `jjp_uBuZ.md` prose.

### Routed AWAY from Fable (confirm routing only)

- **m7-onboarding-keyless-gap** — keyless freehold-establish writes no governor-RBRA key file, but onboarding tracks + menu-intro (in `rbho0_onboarding.sh`) still expect one. Routed to **₣A6** (handbook-restart), NOT Fable. NOTE: the ledger named tracks `rbw-Ocr/Ocd` — **those tabtargets do not exist** (live `rbw-O*` = Occ/Oda/Odb/Odf/Odg/Ofc/Op/Ots); ₣A6 must resolve the real entry point (likely rbw-Op + the `rbho0_` start-here menu) before acting.

---

## BUCKET 2 — OPEN QUESTIONS DEFERRED TO FABLE

A *lean* is a working preference, not a decision — the heat's posture is vocabulary-waits-on-Fable.

### A. Vocabulary mints — fresh births (asterism + grep gate before adoption)

- **foedus-noun-mint** — mint `foedus` (Latin treaty) as the configured-federation noun? HOME: `Bf-paddock`; RBS0 civic quoin `rbtf_`. Lean: adopt.
- **foedus-cold-probe-homograph** — does `foedus` clear the fair-faced cold-probe despite Latin opacity + adjective `foedus` = "foul/ugly"? **(load-bearing gate)** HOME: `Bf-paddock`; ledger ch.1.
- **foedera-plural** — confirm neuter plural `foedera` rides the noun mint. HOME: `Bf-paddock`. Lean: foedera.
- **mechanism-discriminator-quoin** — mint the acquisition-mechanism discriminator quoin + value-words (interactive vs programmatic — flagged trodden-word risk) + a wire-value family parallel to vessel's `rbnve_`. HOME: `Bf-paddock`; RBSRF; `memo-20260618-Bf-federation-config-model.md`.
- **two-new-RBS0-subdoc-acronyms** — mint the 2 RBS0 federation-subdoc acronyms (caged-establishment marshal-only; programmatic-accessor self-mint→STS). HOME: `Bf-paddock`; `memo-20260618...`.
- **parley-suite-word** — ratify `parley` as the suite word for the foedus-freehold-VERIFY fixture (operator-elected, grep-clean). HOME: ledger ch.2; `Bf-paddock`. Lean: parley.

### B. Vocabulary reminting — eviction-sweep on existing identifiers

- **heed-compear-remint** — remint `compear` (obscure Scots-law sign-in verb, high-traffic error ashlar) → `heed` (grep-isolated, 0 hits)? All forms sweep together. HOME: `Bf-paddock`; `rba_auth.sh`. Lean: heed.
- **assize-rename** — retire `assize` (live sign-in window; "ass-" substring wart + obscurity) and mint a replacement. HOME: `Bf-paddock`; `RBRF_SESSION_DURATION`, `zrba_assize_*`. Lean: retire; no word picked.
- **assize-keep-a-noun-fork** — keep an operator-facing noun for the live-window concept, or demote to hearting? **(load-bearing fork)** HOME: `Bf-paddock`. Lean: keep a noun, soften error text.
- **clear-assize-verb** — name a new verb (+tabtarget) discarding a cached assize to force fresh sign-in; keys off the renamed noun. HOME: `Bf-paddock`. Lean: undecided.
- **homonym-guard-text-disposition** — re-aim / rewrite / retire the mantle-homonym-guard PROSE in the frozen ₣BZ paddock — a vocabulary judgment Fable owns (the FACT is settled in B1). HOME: `jjp_uBuZ.md` prose; `memo-20260615-BZ-pace-design.md`.

### C. Asterism / placement calls (word may be ratifiable; PLACEMENT is Fable's)

- **jilt-mint** — ratify `jilt` (rbw-mJ, RBSMJ — inverse of affiance, dissolves a workforce pool); operator-sanctioned mid-heat mint 260616. HOME: `memo-20260615-BZ-pace-design.md`; RBSMJ. Lean: ratify. NOTE: RBSMJ-manor_jilt.adoc exists but `claude-rbk-acronyms.md` has NO RBSMJ entry — add one (parallel to RBSPG) on ratification.
- **gird-mint** — ratify `gird` (rbw-pE, RBSPG — payor seats the FIRST governor); operator-sanctioned mid-heat mint 260620. **ESPECIALLY the fair-faced cold-probe** ("gird the governor" may not parse as "appoint" cold). HOME: `memo-20260615-BZ-pace-design.md`; `claude-rbk-acronyms.md`; RBSPG. Lean: ratify, cold-probe is the gate. SCOPE: gird landed TWO RBS0 quoins — `rbtf_gird` (civic) AND `rbtgo_governor_gird` (Operations-section anchor, RBS0:1232); a ratify/sweep must dispose of both.
- **payor-vs-manor-project-noun** — keep "payor project" or remint "manor project" for the GCP project-member? HOME: ledger ch.1; RBS0 canon-scrub. Lean: undecided.
- **M5-manor-demesne-regroup** — rule the which-axis regroup of the rbw-m family, then author a fresh pace OR affirm the scatter as load-bearing. HOME: `memo-20260615-BZ-pace-design.md`. Lean: contested, costly, cosmetic.
- **manor-affiance-spec-evolution** — RBSRF + RBSMA recast to core-plus-mechanism-gate shape, gated on Fable delivering PLACED quoins (word + asterism + category + anchor). HOME: `memo-20260618...`; `Bf-paddock`.

### D. Premise-touching (civic hierarchy ₣BZ-frozen — graduates only after this review)

- **foedus-verb-register** — do founding verbs (affiance/jilt, monogamous) migrate to the pluralizing foedus/treaty register, or coexist? (Un-gates terrier founding-home + cardinality.) HOME: `Bf-paddock`; ledger ch.2/3.
- **governor-role-in-federation** — ratify payor-founds / governor-selects split (found DECLINED payor-only; select OPEN + healthy); verify the GCP IAM/attribute-condition bounding the sanctioned set. HOME: `Bf-paddock`. Lean: select = the healthy model to aim for.
- **governors-role-words** — if governor-selects graduates, mint the affiliate verb + sanctioned-set/eligibility noun. HOME: `Bf-paddock`.
- **payor-as-health-assurer / seneschal-superrole-word** — rule the fork: payor wields org-health authority directly via OAuth (default) vs a new federated super-governor role; ruled-out: payor as a federation identity. If a role graduates, mint a civic word (NOT "admin"; "seneschal" illustrative only). HOME: `Bf-paddock`. Lean: payor-direct via OAuth; defer the role.
- **degenerate-test-federation-adoption** — ADOPT a caged (programmatic self-JWT) test path (a durable secret against the zero-keys premise), or keep per-run human click? HOME: `Bf-paddock`; `memo-20260619-Bf-static-negative-test-rig.md`.
- **multiple-federations-axis** — does the Manor ever hold several federations at once? The degenerate-test own-pool requirement may force it. HOME: `Bf-paddock`; `memo-20260618...`. Lean: front-of-heat design pace.
- **terrier-cardinality-residual** — one terrier per Manor vs per foedus? Turns on multiple-federations. HOME: `Bf-paddock`. Lean: one-per-Manor (default).
- **headless-compearance-premise** — must "human present" mean "controlling terminal present" (reaffirm TTY-gate), or amend the cinched premise to allow out-of-band-prompt + headless-poll? HOME: `Bf-paddock`; pace ₢BfAAA/₢BfAAD. Lean: relaxes terminal-present, not human-present.

### E. Spec-authoring worklists (Fable re-authors; gated on prior settles)

- **rbst0-theurge-subdoc / theurge-test-cosmology-spec-gap** — give the test cosmology (theurge/fixture/suite/freehold/leasehold/cupel) a spec home: choose a FRESH quoin stem, mint the "theurge" word, rule the asterism unify-vs-split. HOME: ledger + itch (260622); NO committed spec yet. **NOTE (VERIFIED 260622): `rbst_` IS taken** — 14 production quoins in RBS0:492–506 (`rbst_xname`/`rbst_image_ref`/…), so a FRESH quoin sub-stem is genuinely required. The FILE acronym `RBST0` stays clean (terminal-exclusivity holds at the filename level); only the quoin sub-stem must be new. (An earlier completeness check doubted this but grep'd the wrong file — RBSRS regime-station uses `burs_`; the `rbst_` family lives in RBS0.) The operator's standing call: **subdoc of RBS0** (not a sibling), for free production-quoin reference; Fable mints the fresh stem + the `theurge` word + the asterism call.
- **FABLE-REANCHOR-RBS0-governor-ops** — re-author RBS0 `== {rbtgo_governor_gird} Operations` title + intro for federation (gird is a payor founding act, not a governor op). HOME: `RBS0-SpecTop.adoc:1230` marker.
- **FABLE-REANCHOR-RBSCIG** — confirm a surviving federation proof re-anchors the demolished keyfile-JWT evidence, or prune; re-anchor Member-Removal/Revoke to unseat/attaint. HOME: `RBSCIG-IamGrantContracts.adoc:321` marker (ride with RBSCIP).
- **FABLE-REANCHOR-RBSCIP** — re-anchor time-bound-discriminator evidence to a surviving proof; relocate flap physics to mantle-SA levy/unmake; prune dead re-key churn. HOME: `RBSCIP-IamPropagation.adoc:34` marker.
- **FABLE-REANCHOR-RBSGS** — author the federation getting-started narrative (brevet/unseat/attaint, IdP-owned rotation), or confirm ₣A6 owns it. HOME: `RBSGS-GettingStarted.adoc:125,:219` (2 markers).
- **orphaned-SA-key-retry-constants** — remove the 5 dead `RBGC_SA_KEY_*_RETRY_*` constants (zero bash consumers) + reconcile prose in ONE pass — gated on RBSCIG/RBSCIP (removing ahead dangles citations RBSCIP:53/205, RBSCIG:350-351). HOME: `rbgc_constants.sh` (verify count + sites live).
- **RBS0-vouch-director-grant** — confirm the director's `cloudbuild.workerPoolUser` grant rides the director mantle capability-set (granted at levy). HOME: `memo-20260615-BZ-pace-design.md`.
- **RBSMF-levy-preflight-note** — re-anchor the resource-scope cache-lag absorption from director-enrobe to levy time. HOME: `memo-20260615-BZ-pace-design.md`.

---

## DEDUPE NOTE

Merged across the two source sweeps by handle:
- **Foedus cluster** — `foedus-noun-mint` / `foedus-cold-probe-homograph` / `foedera-plural` / `foedus-verb-register` is ONE concern split across B2-A + B2-D; the verb-register is the premise-touching tail that un-gates terrier founding-home.
- **`foedus-hosted-not-member`** — single B1 item (both sources).
- **Mechanism** — settled AXIS (`mechanism-not-vendor-axis`, B1) vs unmade QUOIN+value-words (`mechanism-discriminator-quoin`, B2-A): two distinct things.
- **Terrier** — three B1-RATIFIED rows (fork-B + B-shape finisher + grain) plus one genuinely-open residual (`terrier-cardinality-residual`, B2-D).
- **jilt/gird** — split (C) because cold-probe stakes differ (gird carries the load-bearing cold-probe + a second quoin).
- **Assize cluster** — `assize-rename` + `assize-keep-a-noun-fork` + `clear-assize-verb` coupled (the verb keys off the renamed noun).
- **mantle-homonym-guard** — SPLIT, not single-bucketed: the FACT (condition met) is B1; the TEXT DISPOSITION is an open vocabulary call routed to Fable (`homonym-guard-text-disposition`, B2-B). The "code site / delete-on-sight" framing was wrong — zero code hits; the vestige is frozen-paddock prose, and pace-design.md routed the rewrite to Fable.
- **`m7-onboarding-keyless-gap`** — recorded so the review CONFIRMS routing to ₣A6; substance is NOT in the Fable queue (and its ledger track names rbw-Ocr/Ocd are stale/nonexistent).

The retired `memo-20260610-heat-BH-fable-*` set and the quoin-minting-introspection memo are a PRIOR heat's closed recommendations — excluded.

REJECTED CORRECTIONS: one — the completeness check's claim that "`rbst_`/RBSRS is empty" (read as `rbst_` possibly free) was itself OVERTURNED by direct verification: `rbst_` IS taken (14 quoins, RBS0:492–506); the check had grep'd the wrong file. The other five completeness corrections verified and folded (homonym zero-hits, Ocr/Ocd nonexistent, RBSMJ acronym gap, speculative-grep caveat, second gird quoin `rbtgo_governor_gird`).
