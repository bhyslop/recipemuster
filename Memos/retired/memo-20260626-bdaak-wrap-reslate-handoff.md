# Hand-off prompt — mount/verify-wrap ₢BDAAk, then reslate ₢BDAAj item 2

**Prepared:** 2026-06-26 · heat ₣BD (jjk-v4-1-fast-tweaks)
**Status:** born retired — transient single-use hand-off, not durable knowledge.

A clean-chat prompt to mount and close `₢BDAAk` (multichannel-session-id-capture)
and then remember the easy-to-forget follow-up: reslate its omnibus parent
`₢BDAAj` (explore-and-expand-capture-and-display-musings) so its item 2
("Session-ID-in-commit") records that `₢BDAAk` is wrapped.

Filed directly under `Memos/retired/` because it is consumed the moment that chat
runs; nothing here is load-bearing afterward.

---

```
Mount and complete ₢BDAAk (multichannel-session-id-capture) in heat ₣BD
(jjk-v4-1-fast-tweaks), wrap it on my confirmation, then update its omnibus
parent. Mount it the normal way (open an officium, halter notice → orient,
read the paddock before the docket).

Two things to know up front:

- ₢BDAAk's design was settled in a prior chat and its implementation appears
  to have ALREADY landed (JJS0 spec + the jjrm_mcp invitatory handler, kit
  tests green, live-tested). So treat this as verify-then-wrap, not
  re-implement: check each of the docket's "Done when" criteria against the
  current tree, finish only a genuine gap, and notch before any test run.
  Report what you found already-done vs. what you actually completed.

- ₢BDAAk is line-item 2 ("Session-ID-in-commit") of the omnibus capture pace
  ₢BDAAj (explore-and-expand-capture-and-display-musings), same heat ₣BD.
  That parent MUST be updated once ₢BDAAk closes. This is the easy-to-forget
  step, so it is spelled out explicitly below — do not skip it.

Sequence:

1. Mount ₢BDAAk; verify its "Done when" against the live tree (JJS0 — and
   JJSRPS if warranted — define the multi-channel capture grammar + relaxed
   record-only posture; the invitatory handler emits all six channels with
   -/(ambiguous) sentinels; kit tests green). Finish any real gap, notching
   before you test. The docket is the authority on done-ness, not this prompt.

2. Ask me to confirm the wrap — do not auto-wrap. On my OK, wrap ₢BDAAk with a
   summary and an honest spook.

3. IMMEDIATELY after the wrap succeeds, in this same chat, reslate ₢BDAAj to
   mark its item 2 complete:
   - jjx_brief ₢BDAAj to pull the current docket text.
   - Edit ONLY item 2's status marker to record that ₢BDAAk is now WRAPPED
     (e.g. turn "RESOLVED, graduated into ₢BDAAk" into a DONE/closed marker
     dated today). Preserve every other item and all surrounding text verbatim.
   - jjx_redocket with a single jjezs_reslate notice for ₢BDAAj (same-heat, so
     the batch is valid).
   - Confirm the per-coronet diff shows ONLY item 2 changed, and report it.
```
